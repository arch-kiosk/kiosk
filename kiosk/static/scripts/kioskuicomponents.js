/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function Jt(s, n = "24.5.5") {
  Object.defineProperty(s, "version", {
    get() {
      return n;
    }
  });
  const e = customElements.get(s.is);
  if (!e)
    customElements.define(s.is, s);
  else {
    const t = e.version;
    t && s.version && s.version;
  }
}
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Jc extends HTMLElement {
  static get is() {
    return "vaadin-lumo-styles";
  }
}
Jt(Jc);
/**
 * @license
 * Copyright 2019 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const an = globalThis, $o = an.ShadowRoot && (an.ShadyCSS === void 0 || an.ShadyCSS.nativeShadow) && "adoptedStyleSheets" in Document.prototype && "replace" in CSSStyleSheet.prototype, es = Symbol(), sa = /* @__PURE__ */ new WeakMap();
let ts = class {
  constructor(n, e, t) {
    if (this._$cssResult$ = !0, t !== es) throw Error("CSSResult is not constructable. Use `unsafeCSS` or `css` instead.");
    this.cssText = n, this.t = e;
  }
  get styleSheet() {
    let n = this.o;
    const e = this.t;
    if ($o && n === void 0) {
      const t = e !== void 0 && e.length === 1;
      t && (n = sa.get(e)), n === void 0 && ((this.o = n = new CSSStyleSheet()).replaceSync(this.cssText), t && sa.set(e, n));
    }
    return n;
  }
  toString() {
    return this.cssText;
  }
};
const wr = (s) => new ts(typeof s == "string" ? s : s + "", void 0, es), Ee = (s, ...n) => {
  const e = s.length === 1 ? s[0] : n.reduce(((t, i, r) => t + ((o) => {
    if (o._$cssResult$ === !0) return o.cssText;
    if (typeof o == "number") return o;
    throw Error("Value passed to 'css' function must be a 'css' function result: " + o + ". Use 'unsafeCSS' to pass non-literal values, but take care to ensure page security.");
  })(i) + s[r + 1]), s[0]);
  return new ts(e, s, es);
}, nl = (s, n) => {
  if ($o) s.adoptedStyleSheets = n.map(((e) => e instanceof CSSStyleSheet ? e : e.styleSheet));
  else for (const e of n) {
    const t = document.createElement("style"), i = an.litNonce;
    i !== void 0 && t.setAttribute("nonce", i), t.textContent = e.cssText, s.appendChild(t);
  }
}, aa = $o ? (s) => s : (s) => s instanceof CSSStyleSheet ? ((n) => {
  let e = "";
  for (const t of n.cssRules) e += t.cssText;
  return wr(e);
})(s) : s;
/**
 * @license
 * Copyright 2017 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const { is: $c, defineProperty: eh, getOwnPropertyDescriptor: th, getOwnPropertyNames: ih, getOwnPropertySymbols: rh, getPrototypeOf: nh } = Object, kn = globalThis, la = kn.trustedTypes, oh = la ? la.emptyScript : "", sh = kn.reactiveElementPolyfillSupport, hr = (s, n) => s, _n = { toAttribute(s, n) {
  switch (n) {
    case Boolean:
      s = s ? oh : null;
      break;
    case Object:
    case Array:
      s = s == null ? s : JSON.stringify(s);
  }
  return s;
}, fromAttribute(s, n) {
  let e = s;
  switch (n) {
    case Boolean:
      e = s !== null;
      break;
    case Number:
      e = s === null ? null : Number(s);
      break;
    case Object:
    case Array:
      try {
        e = JSON.parse(s);
      } catch {
        e = null;
      }
  }
  return e;
} }, is = (s, n) => !$c(s, n), ca = { attribute: !0, type: String, converter: _n, reflect: !1, useDefault: !1, hasChanged: is };
Symbol.metadata ??= Symbol("metadata"), kn.litPropertyMetadata ??= /* @__PURE__ */ new WeakMap();
let Ii = class extends HTMLElement {
  static addInitializer(n) {
    this._$Ei(), (this.l ??= []).push(n);
  }
  static get observedAttributes() {
    return this.finalize(), this._$Eh && [...this._$Eh.keys()];
  }
  static createProperty(n, e = ca) {
    if (e.state && (e.attribute = !1), this._$Ei(), this.prototype.hasOwnProperty(n) && ((e = Object.create(e)).wrapped = !0), this.elementProperties.set(n, e), !e.noAccessor) {
      const t = Symbol(), i = this.getPropertyDescriptor(n, t, e);
      i !== void 0 && eh(this.prototype, n, i);
    }
  }
  static getPropertyDescriptor(n, e, t) {
    const { get: i, set: r } = th(this.prototype, n) ?? { get() {
      return this[e];
    }, set(o) {
      this[e] = o;
    } };
    return { get: i, set(o) {
      const l = i?.call(this);
      r?.call(this, o), this.requestUpdate(n, l, t);
    }, configurable: !0, enumerable: !0 };
  }
  static getPropertyOptions(n) {
    return this.elementProperties.get(n) ?? ca;
  }
  static _$Ei() {
    if (this.hasOwnProperty(hr("elementProperties"))) return;
    const n = nh(this);
    n.finalize(), n.l !== void 0 && (this.l = [...n.l]), this.elementProperties = new Map(n.elementProperties);
  }
  static finalize() {
    if (this.hasOwnProperty(hr("finalized"))) return;
    if (this.finalized = !0, this._$Ei(), this.hasOwnProperty(hr("properties"))) {
      const e = this.properties, t = [...ih(e), ...rh(e)];
      for (const i of t) this.createProperty(i, e[i]);
    }
    const n = this[Symbol.metadata];
    if (n !== null) {
      const e = litPropertyMetadata.get(n);
      if (e !== void 0) for (const [t, i] of e) this.elementProperties.set(t, i);
    }
    this._$Eh = /* @__PURE__ */ new Map();
    for (const [e, t] of this.elementProperties) {
      const i = this._$Eu(e, t);
      i !== void 0 && this._$Eh.set(i, e);
    }
    this.elementStyles = this.finalizeStyles(this.styles);
  }
  static finalizeStyles(n) {
    const e = [];
    if (Array.isArray(n)) {
      const t = new Set(n.flat(1 / 0).reverse());
      for (const i of t) e.unshift(aa(i));
    } else n !== void 0 && e.push(aa(n));
    return e;
  }
  static _$Eu(n, e) {
    const t = e.attribute;
    return t === !1 ? void 0 : typeof t == "string" ? t : typeof n == "string" ? n.toLowerCase() : void 0;
  }
  constructor() {
    super(), this._$Ep = void 0, this.isUpdatePending = !1, this.hasUpdated = !1, this._$Em = null, this._$Ev();
  }
  _$Ev() {
    this._$ES = new Promise(((n) => this.enableUpdating = n)), this._$AL = /* @__PURE__ */ new Map(), this._$E_(), this.requestUpdate(), this.constructor.l?.forEach(((n) => n(this)));
  }
  addController(n) {
    (this._$EO ??= /* @__PURE__ */ new Set()).add(n), this.renderRoot !== void 0 && this.isConnected && n.hostConnected?.();
  }
  removeController(n) {
    this._$EO?.delete(n);
  }
  _$E_() {
    const n = /* @__PURE__ */ new Map(), e = this.constructor.elementProperties;
    for (const t of e.keys()) this.hasOwnProperty(t) && (n.set(t, this[t]), delete this[t]);
    n.size > 0 && (this._$Ep = n);
  }
  createRenderRoot() {
    const n = this.shadowRoot ?? this.attachShadow(this.constructor.shadowRootOptions);
    return nl(n, this.constructor.elementStyles), n;
  }
  connectedCallback() {
    this.renderRoot ??= this.createRenderRoot(), this.enableUpdating(!0), this._$EO?.forEach(((n) => n.hostConnected?.()));
  }
  enableUpdating(n) {
  }
  disconnectedCallback() {
    this._$EO?.forEach(((n) => n.hostDisconnected?.()));
  }
  attributeChangedCallback(n, e, t) {
    this._$AK(n, t);
  }
  _$ET(n, e) {
    const t = this.constructor.elementProperties.get(n), i = this.constructor._$Eu(n, t);
    if (i !== void 0 && t.reflect === !0) {
      const r = (t.converter?.toAttribute !== void 0 ? t.converter : _n).toAttribute(e, t.type);
      this._$Em = n, r == null ? this.removeAttribute(i) : this.setAttribute(i, r), this._$Em = null;
    }
  }
  _$AK(n, e) {
    const t = this.constructor, i = t._$Eh.get(n);
    if (i !== void 0 && this._$Em !== i) {
      const r = t.getPropertyOptions(i), o = typeof r.converter == "function" ? { fromAttribute: r.converter } : r.converter?.fromAttribute !== void 0 ? r.converter : _n;
      this._$Em = i;
      const l = o.fromAttribute(e, r.type);
      this[i] = l ?? this._$Ej?.get(i) ?? l, this._$Em = null;
    }
  }
  requestUpdate(n, e, t) {
    if (n !== void 0) {
      const i = this.constructor, r = this[n];
      if (t ??= i.getPropertyOptions(n), !((t.hasChanged ?? is)(r, e) || t.useDefault && t.reflect && r === this._$Ej?.get(n) && !this.hasAttribute(i._$Eu(n, t)))) return;
      this.C(n, e, t);
    }
    this.isUpdatePending === !1 && (this._$ES = this._$EP());
  }
  C(n, e, { useDefault: t, reflect: i, wrapped: r }, o) {
    t && !(this._$Ej ??= /* @__PURE__ */ new Map()).has(n) && (this._$Ej.set(n, o ?? e ?? this[n]), r !== !0 || o !== void 0) || (this._$AL.has(n) || (this.hasUpdated || t || (e = void 0), this._$AL.set(n, e)), i === !0 && this._$Em !== n && (this._$Eq ??= /* @__PURE__ */ new Set()).add(n));
  }
  async _$EP() {
    this.isUpdatePending = !0;
    try {
      await this._$ES;
    } catch (e) {
      Promise.reject(e);
    }
    const n = this.scheduleUpdate();
    return n != null && await n, !this.isUpdatePending;
  }
  scheduleUpdate() {
    return this.performUpdate();
  }
  performUpdate() {
    if (!this.isUpdatePending) return;
    if (!this.hasUpdated) {
      if (this.renderRoot ??= this.createRenderRoot(), this._$Ep) {
        for (const [i, r] of this._$Ep) this[i] = r;
        this._$Ep = void 0;
      }
      const t = this.constructor.elementProperties;
      if (t.size > 0) for (const [i, r] of t) {
        const { wrapped: o } = r, l = this[i];
        o !== !0 || this._$AL.has(i) || l === void 0 || this.C(i, void 0, r, l);
      }
    }
    let n = !1;
    const e = this._$AL;
    try {
      n = this.shouldUpdate(e), n ? (this.willUpdate(e), this._$EO?.forEach(((t) => t.hostUpdate?.())), this.update(e)) : this._$EM();
    } catch (t) {
      throw n = !1, this._$EM(), t;
    }
    n && this._$AE(e);
  }
  willUpdate(n) {
  }
  _$AE(n) {
    this._$EO?.forEach(((e) => e.hostUpdated?.())), this.hasUpdated || (this.hasUpdated = !0, this.firstUpdated(n)), this.updated(n);
  }
  _$EM() {
    this._$AL = /* @__PURE__ */ new Map(), this.isUpdatePending = !1;
  }
  get updateComplete() {
    return this.getUpdateComplete();
  }
  getUpdateComplete() {
    return this._$ES;
  }
  shouldUpdate(n) {
    return !0;
  }
  update(n) {
    this._$Eq &&= this._$Eq.forEach(((e) => this._$ET(e, this[e]))), this._$EM();
  }
  updated(n) {
  }
  firstUpdated(n) {
  }
};
Ii.elementStyles = [], Ii.shadowRootOptions = { mode: "open" }, Ii[hr("elementProperties")] = /* @__PURE__ */ new Map(), Ii[hr("finalized")] = /* @__PURE__ */ new Map(), sh?.({ ReactiveElement: Ii }), (kn.reactiveElementVersions ??= []).push("2.1.1");
/**
 * @license
 * Copyright 2017 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const rs = globalThis, vn = rs.trustedTypes, ha = vn ? vn.createPolicy("lit-html", { createHTML: (s) => s }) : void 0, ol = "$lit$", Kt = `lit$${Math.random().toFixed(9).slice(2)}$`, sl = "?" + Kt, ah = `<${sl}>`, gi = document, fr = () => gi.createComment(""), pr = (s) => s === null || typeof s != "object" && typeof s != "function", ns = Array.isArray, lh = (s) => ns(s) || typeof s?.[Symbol.iterator] == "function", mo = `[ 	
\f\r]`, ir = /<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g, da = /-->/g, ua = />/g, ai = RegExp(`>|${mo}(?:([^\\s"'>=/]+)(${mo}*=${mo}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`, "g"), fa = /'/g, pa = /"/g, al = /^(?:script|style|textarea|title)$/i, ch = (s) => (n, ...e) => ({ _$litType$: s, strings: n, values: e }), Be = ch(1), mi = Symbol.for("lit-noChange"), Te = Symbol.for("lit-nothing"), ga = /* @__PURE__ */ new WeakMap(), hi = gi.createTreeWalker(gi, 129);
function ll(s, n) {
  if (!ns(s) || !s.hasOwnProperty("raw")) throw Error("invalid template strings array");
  return ha !== void 0 ? ha.createHTML(n) : n;
}
const hh = (s, n) => {
  const e = s.length - 1, t = [];
  let i, r = n === 2 ? "<svg>" : n === 3 ? "<math>" : "", o = ir;
  for (let l = 0; l < e; l++) {
    const a = s[l];
    let c, d, f = -1, p = 0;
    for (; p < a.length && (o.lastIndex = p, d = o.exec(a), d !== null); ) p = o.lastIndex, o === ir ? d[1] === "!--" ? o = da : d[1] !== void 0 ? o = ua : d[2] !== void 0 ? (al.test(d[2]) && (i = RegExp("</" + d[2], "g")), o = ai) : d[3] !== void 0 && (o = ai) : o === ai ? d[0] === ">" ? (o = i ?? ir, f = -1) : d[1] === void 0 ? f = -2 : (f = o.lastIndex - d[2].length, c = d[1], o = d[3] === void 0 ? ai : d[3] === '"' ? pa : fa) : o === pa || o === fa ? o = ai : o === da || o === ua ? o = ir : (o = ai, i = void 0);
    const b = o === ai && s[l + 1].startsWith("/>") ? " " : "";
    r += o === ir ? a + ah : f >= 0 ? (t.push(c), a.slice(0, f) + ol + a.slice(f) + Kt + b) : a + Kt + (f === -2 ? l : b);
  }
  return [ll(s, r + (s[e] || "<?>") + (n === 2 ? "</svg>" : n === 3 ? "</math>" : "")), t];
};
class gr {
  constructor({ strings: n, _$litType$: e }, t) {
    let i;
    this.parts = [];
    let r = 0, o = 0;
    const l = n.length - 1, a = this.parts, [c, d] = hh(n, e);
    if (this.el = gr.createElement(c, t), hi.currentNode = this.el.content, e === 2 || e === 3) {
      const f = this.el.content.firstChild;
      f.replaceWith(...f.childNodes);
    }
    for (; (i = hi.nextNode()) !== null && a.length < l; ) {
      if (i.nodeType === 1) {
        if (i.hasAttributes()) for (const f of i.getAttributeNames()) if (f.endsWith(ol)) {
          const p = d[o++], b = i.getAttribute(f).split(Kt), x = /([.?@])?(.*)/.exec(p);
          a.push({ type: 1, index: r, name: x[2], strings: b, ctor: x[1] === "." ? uh : x[1] === "?" ? fh : x[1] === "@" ? ph : In }), i.removeAttribute(f);
        } else f.startsWith(Kt) && (a.push({ type: 6, index: r }), i.removeAttribute(f));
        if (al.test(i.tagName)) {
          const f = i.textContent.split(Kt), p = f.length - 1;
          if (p > 0) {
            i.textContent = vn ? vn.emptyScript : "";
            for (let b = 0; b < p; b++) i.append(f[b], fr()), hi.nextNode(), a.push({ type: 2, index: ++r });
            i.append(f[p], fr());
          }
        }
      } else if (i.nodeType === 8) if (i.data === sl) a.push({ type: 2, index: r });
      else {
        let f = -1;
        for (; (f = i.data.indexOf(Kt, f + 1)) !== -1; ) a.push({ type: 7, index: r }), f += Kt.length - 1;
      }
      r++;
    }
  }
  static createElement(n, e) {
    const t = gi.createElement("template");
    return t.innerHTML = n, t;
  }
}
function Ni(s, n, e = s, t) {
  if (n === mi) return n;
  let i = t !== void 0 ? e._$Co?.[t] : e._$Cl;
  const r = pr(n) ? void 0 : n._$litDirective$;
  return i?.constructor !== r && (i?._$AO?.(!1), r === void 0 ? i = void 0 : (i = new r(s), i._$AT(s, e, t)), t !== void 0 ? (e._$Co ??= [])[t] = i : e._$Cl = i), i !== void 0 && (n = Ni(s, i._$AS(s, n.values), i, t)), n;
}
class dh {
  constructor(n, e) {
    this._$AV = [], this._$AN = void 0, this._$AD = n, this._$AM = e;
  }
  get parentNode() {
    return this._$AM.parentNode;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  u(n) {
    const { el: { content: e }, parts: t } = this._$AD, i = (n?.creationScope ?? gi).importNode(e, !0);
    hi.currentNode = i;
    let r = hi.nextNode(), o = 0, l = 0, a = t[0];
    for (; a !== void 0; ) {
      if (o === a.index) {
        let c;
        a.type === 2 ? c = new xr(r, r.nextSibling, this, n) : a.type === 1 ? c = new a.ctor(r, a.name, a.strings, this, n) : a.type === 6 && (c = new gh(r, this, n)), this._$AV.push(c), a = t[++l];
      }
      o !== a?.index && (r = hi.nextNode(), o++);
    }
    return hi.currentNode = gi, i;
  }
  p(n) {
    let e = 0;
    for (const t of this._$AV) t !== void 0 && (t.strings !== void 0 ? (t._$AI(n, t, e), e += t.strings.length - 2) : t._$AI(n[e])), e++;
  }
}
class xr {
  get _$AU() {
    return this._$AM?._$AU ?? this._$Cv;
  }
  constructor(n, e, t, i) {
    this.type = 2, this._$AH = Te, this._$AN = void 0, this._$AA = n, this._$AB = e, this._$AM = t, this.options = i, this._$Cv = i?.isConnected ?? !0;
  }
  get parentNode() {
    let n = this._$AA.parentNode;
    const e = this._$AM;
    return e !== void 0 && n?.nodeType === 11 && (n = e.parentNode), n;
  }
  get startNode() {
    return this._$AA;
  }
  get endNode() {
    return this._$AB;
  }
  _$AI(n, e = this) {
    n = Ni(this, n, e), pr(n) ? n === Te || n == null || n === "" ? (this._$AH !== Te && this._$AR(), this._$AH = Te) : n !== this._$AH && n !== mi && this._(n) : n._$litType$ !== void 0 ? this.$(n) : n.nodeType !== void 0 ? this.T(n) : lh(n) ? this.k(n) : this._(n);
  }
  O(n) {
    return this._$AA.parentNode.insertBefore(n, this._$AB);
  }
  T(n) {
    this._$AH !== n && (this._$AR(), this._$AH = this.O(n));
  }
  _(n) {
    this._$AH !== Te && pr(this._$AH) ? this._$AA.nextSibling.data = n : this.T(gi.createTextNode(n)), this._$AH = n;
  }
  $(n) {
    const { values: e, _$litType$: t } = n, i = typeof t == "number" ? this._$AC(n) : (t.el === void 0 && (t.el = gr.createElement(ll(t.h, t.h[0]), this.options)), t);
    if (this._$AH?._$AD === i) this._$AH.p(e);
    else {
      const r = new dh(i, this), o = r.u(this.options);
      r.p(e), this.T(o), this._$AH = r;
    }
  }
  _$AC(n) {
    let e = ga.get(n.strings);
    return e === void 0 && ga.set(n.strings, e = new gr(n)), e;
  }
  k(n) {
    ns(this._$AH) || (this._$AH = [], this._$AR());
    const e = this._$AH;
    let t, i = 0;
    for (const r of n) i === e.length ? e.push(t = new xr(this.O(fr()), this.O(fr()), this, this.options)) : t = e[i], t._$AI(r), i++;
    i < e.length && (this._$AR(t && t._$AB.nextSibling, i), e.length = i);
  }
  _$AR(n = this._$AA.nextSibling, e) {
    for (this._$AP?.(!1, !0, e); n !== this._$AB; ) {
      const t = n.nextSibling;
      n.remove(), n = t;
    }
  }
  setConnected(n) {
    this._$AM === void 0 && (this._$Cv = n, this._$AP?.(n));
  }
}
class In {
  get tagName() {
    return this.element.tagName;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  constructor(n, e, t, i, r) {
    this.type = 1, this._$AH = Te, this._$AN = void 0, this.element = n, this.name = e, this._$AM = i, this.options = r, t.length > 2 || t[0] !== "" || t[1] !== "" ? (this._$AH = Array(t.length - 1).fill(new String()), this.strings = t) : this._$AH = Te;
  }
  _$AI(n, e = this, t, i) {
    const r = this.strings;
    let o = !1;
    if (r === void 0) n = Ni(this, n, e, 0), o = !pr(n) || n !== this._$AH && n !== mi, o && (this._$AH = n);
    else {
      const l = n;
      let a, c;
      for (n = r[0], a = 0; a < r.length - 1; a++) c = Ni(this, l[t + a], e, a), c === mi && (c = this._$AH[a]), o ||= !pr(c) || c !== this._$AH[a], c === Te ? n = Te : n !== Te && (n += (c ?? "") + r[a + 1]), this._$AH[a] = c;
    }
    o && !i && this.j(n);
  }
  j(n) {
    n === Te ? this.element.removeAttribute(this.name) : this.element.setAttribute(this.name, n ?? "");
  }
}
class uh extends In {
  constructor() {
    super(...arguments), this.type = 3;
  }
  j(n) {
    this.element[this.name] = n === Te ? void 0 : n;
  }
}
class fh extends In {
  constructor() {
    super(...arguments), this.type = 4;
  }
  j(n) {
    this.element.toggleAttribute(this.name, !!n && n !== Te);
  }
}
let ph = class extends In {
  constructor(n, e, t, i, r) {
    super(n, e, t, i, r), this.type = 5;
  }
  _$AI(n, e = this) {
    if ((n = Ni(this, n, e, 0) ?? Te) === mi) return;
    const t = this._$AH, i = n === Te && t !== Te || n.capture !== t.capture || n.once !== t.once || n.passive !== t.passive, r = n !== Te && (t === Te || i);
    i && this.element.removeEventListener(this.name, this, t), r && this.element.addEventListener(this.name, this, n), this._$AH = n;
  }
  handleEvent(n) {
    typeof this._$AH == "function" ? this._$AH.call(this.options?.host ?? this.element, n) : this._$AH.handleEvent(n);
  }
};
class gh {
  constructor(n, e, t) {
    this.element = n, this.type = 6, this._$AN = void 0, this._$AM = e, this.options = t;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  _$AI(n) {
    Ni(this, n);
  }
}
const mh = rs.litHtmlPolyfillSupport;
mh?.(gr, xr), (rs.litHtmlVersions ??= []).push("3.3.1");
const cl = (s, n, e) => {
  const t = e?.renderBefore ?? n;
  let i = t._$litPart$;
  if (i === void 0) {
    const r = e?.renderBefore ?? null;
    t._$litPart$ = i = new xr(n.insertBefore(fr(), r), r, void 0, e ?? {});
  }
  return i._$AI(s), i;
};
/**
 * @license
 * Copyright 2017 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const os = globalThis;
let Mt = class extends Ii {
  constructor() {
    super(...arguments), this.renderOptions = { host: this }, this._$Do = void 0;
  }
  createRenderRoot() {
    const n = super.createRenderRoot();
    return this.renderOptions.renderBefore ??= n.firstChild, n;
  }
  update(n) {
    const e = this.render();
    this.hasUpdated || (this.renderOptions.isConnected = this.isConnected), super.update(n), this._$Do = cl(e, this.renderRoot, this.renderOptions);
  }
  connectedCallback() {
    super.connectedCallback(), this._$Do?.setConnected(!0);
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this._$Do?.setConnected(!1);
  }
  render() {
    return mi;
  }
};
Mt._$litElement$ = !0, Mt.finalized = !0, os.litElementHydrateSupport?.({ LitElement: Mt });
const _h = os.litElementPolyfillSupport;
_h?.({ LitElement: Mt });
(os.litElementVersions ??= []).push("4.2.1");
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const vh = (s) => class extends s {
  static get properties() {
    return {
      /**
       * Helper property with theme attribute value facilitating propagation
       * in shadow DOM.
       *
       * Enables the component implementation to propagate the `theme`
       * attribute value to the sub-components in Shadow DOM by binding
       * the sub-component's "theme" attribute to the `theme` property of
       * the host.
       *
       * **NOTE:** Extending the mixin only provides the property for binding,
       * and does not make the propagation alone.
       *
       * See [Styling Components: Sub-components](https://vaadin.com/docs/latest/styling/styling-components/#sub-components).
       * page for more information.
       *
       * @protected
       */
      _theme: {
        type: String,
        readOnly: !0
      }
    };
  }
  static get observedAttributes() {
    return [...super.observedAttributes, "theme"];
  }
  /** @protected */
  attributeChangedCallback(e, t, i) {
    super.attributeChangedCallback(e, t, i), e === "theme" && this._set_theme(i);
  }
};
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const hl = [], zo = /* @__PURE__ */ new Set(), ss = /* @__PURE__ */ new Set();
function dl(s) {
  return s && Object.prototype.hasOwnProperty.call(s, "__themes");
}
function yh(s) {
  return dl(customElements.get(s));
}
function bh(s = []) {
  return [s].flat(1 / 0).filter((n) => n instanceof ts);
}
function ul(s, n) {
  return (s || "").split(" ").some((e) => new RegExp(`^${e.split("*").join(".*")}$`, "u").test(n));
}
function fl(s) {
  return s.map((n) => n.cssText).join(`
`);
}
const yn = "vaadin-themable-mixin-style";
function wh(s, n) {
  const e = document.createElement("style");
  e.id = yn, e.textContent = fl(s), n.content.appendChild(e);
}
function xh(s) {
  if (!s.shadowRoot)
    return;
  const n = s.constructor;
  if (s instanceof Mt)
    [...s.shadowRoot.querySelectorAll("style")].forEach((e) => e.remove()), nl(s.shadowRoot, n.elementStyles);
  else {
    const e = s.shadowRoot.getElementById(yn), t = n.prototype._template;
    e.textContent = t.content.getElementById(yn).textContent;
  }
}
function Ch(s) {
  zo.forEach((n) => {
    const e = n.deref();
    e instanceof s ? xh(e) : e || zo.delete(n);
  });
}
function pl(s) {
  if (s.prototype instanceof Mt)
    s.elementStyles = s.finalizeStyles(s.styles);
  else {
    const n = s.prototype._template;
    n.content.getElementById(yn).textContent = fl(s.getStylesForThis());
  }
  ss.forEach((n) => {
    const e = customElements.get(n);
    e !== s && e.prototype instanceof s && pl(e);
  });
}
function Th(s, n) {
  const e = s.__themes;
  return !e || !n ? !1 : e.some(
    (t) => t.styles.some((i) => n.some((r) => r.cssText === i.cssText))
  );
}
function Ze(s, n, e = {}) {
  n = bh(n), window.Vaadin && window.Vaadin.styleModules ? window.Vaadin.styleModules.registerStyles(s, n, e) : hl.push({
    themeFor: s,
    styles: n,
    include: e.include,
    moduleId: e.moduleId
  }), s && ss.forEach((t) => {
    if (ul(s, t) && yh(t)) {
      const i = customElements.get(t);
      Th(i, n) || !window.Vaadin || window.Vaadin.suppressPostFinalizeStylesWarning, pl(i), Ch(i);
    }
  });
}
function Fo() {
  return window.Vaadin && window.Vaadin.styleModules ? window.Vaadin.styleModules.getAllThemes() : hl;
}
function Eh(s = "") {
  let n = 0;
  return s.startsWith("lumo-") || s.startsWith("material-") ? n = 1 : s.startsWith("vaadin-") && (n = 2), n;
}
function gl(s) {
  const n = [];
  return s.include && [].concat(s.include).forEach((e) => {
    const t = Fo().find((i) => i.moduleId === e);
    t && n.push(...gl(t), ...t.styles);
  }, s.styles), n;
}
function Ph(s) {
  const n = `${s}-default-theme`, e = Fo().filter((t) => t.moduleId !== n && ul(t.themeFor, s)).map((t) => ({
    ...t,
    // Prepend styles from included themes
    styles: [...gl(t), ...t.styles],
    // Map moduleId to includePriority
    includePriority: Eh(t.moduleId)
  })).sort((t, i) => i.includePriority - t.includePriority);
  return e.length > 0 ? e : Fo().filter((t) => t.moduleId === n);
}
const Cr = (s) => class extends vh(s) {
  constructor() {
    super(), zo.add(new WeakRef(this));
  }
  /**
   * Covers PolymerElement based component styling
   * @protected
   */
  static finalize() {
    if (super.finalize(), this.is && ss.add(this.is), this.elementStyles)
      return;
    const e = this.prototype._template;
    !e || dl(this) || wh(this.getStylesForThis(), e);
  }
  /**
   * Covers LitElement based component styling
   *
   * @protected
   */
  static finalizeStyles(e) {
    const t = this.getStylesForThis();
    return e ? [...[e].flat(1 / 0), ...t] : t;
  }
  /**
   * Get styles for the component type
   *
   * @private
   */
  static getStylesForThis() {
    const e = s.__themes || [], t = Object.getPrototypeOf(this.prototype), i = (t ? t.constructor.__themes : []) || [];
    this.__themes = [...e, ...i, ...Ph(this.is)];
    const r = this.__themes.flatMap((o) => o.styles);
    return r.filter((o, l) => l === r.lastIndexOf(o));
  }
};
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Sh = (s, ...n) => {
  const e = document.createElement("style");
  e.id = s, e.textContent = n.map((t) => t.toString()).join(`
`).replace(":host", "html"), document.head.insertAdjacentElement("afterbegin", e);
}, Ui = (s, ...n) => {
  Sh(`lumo-${s}`, n);
};
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Rh = Ee`
  :host {
    /* Base (background) */
    --lumo-base-color: #fff;

    /* Tint */
    --lumo-tint-5pct: hsla(0, 0%, 100%, 0.3);
    --lumo-tint-10pct: hsla(0, 0%, 100%, 0.37);
    --lumo-tint-20pct: hsla(0, 0%, 100%, 0.44);
    --lumo-tint-30pct: hsla(0, 0%, 100%, 0.5);
    --lumo-tint-40pct: hsla(0, 0%, 100%, 0.57);
    --lumo-tint-50pct: hsla(0, 0%, 100%, 0.64);
    --lumo-tint-60pct: hsla(0, 0%, 100%, 0.7);
    --lumo-tint-70pct: hsla(0, 0%, 100%, 0.77);
    --lumo-tint-80pct: hsla(0, 0%, 100%, 0.84);
    --lumo-tint-90pct: hsla(0, 0%, 100%, 0.9);
    --lumo-tint: #fff;

    /* Shade */
    --lumo-shade-5pct: hsla(214, 61%, 25%, 0.05);
    --lumo-shade-10pct: hsla(214, 57%, 24%, 0.1);
    --lumo-shade-20pct: hsla(214, 53%, 23%, 0.16);
    --lumo-shade-30pct: hsla(214, 50%, 22%, 0.26);
    --lumo-shade-40pct: hsla(214, 47%, 21%, 0.38);
    --lumo-shade-50pct: hsla(214, 45%, 20%, 0.52);
    --lumo-shade-60pct: hsla(214, 43%, 19%, 0.6);
    --lumo-shade-70pct: hsla(214, 42%, 18%, 0.69);
    --lumo-shade-80pct: hsla(214, 41%, 17%, 0.83);
    --lumo-shade-90pct: hsla(214, 40%, 16%, 0.94);
    --lumo-shade: hsl(214, 35%, 15%);

    /* Contrast */
    --lumo-contrast-5pct: var(--lumo-shade-5pct);
    --lumo-contrast-10pct: var(--lumo-shade-10pct);
    --lumo-contrast-20pct: var(--lumo-shade-20pct);
    --lumo-contrast-30pct: var(--lumo-shade-30pct);
    --lumo-contrast-40pct: var(--lumo-shade-40pct);
    --lumo-contrast-50pct: var(--lumo-shade-50pct);
    --lumo-contrast-60pct: var(--lumo-shade-60pct);
    --lumo-contrast-70pct: var(--lumo-shade-70pct);
    --lumo-contrast-80pct: var(--lumo-shade-80pct);
    --lumo-contrast-90pct: var(--lumo-shade-90pct);
    --lumo-contrast: var(--lumo-shade);

    /* Text */
    --lumo-header-text-color: var(--lumo-contrast);
    --lumo-body-text-color: var(--lumo-contrast-90pct);
    --lumo-secondary-text-color: var(--lumo-contrast-70pct);
    --lumo-tertiary-text-color: var(--lumo-contrast-50pct);
    --lumo-disabled-text-color: var(--lumo-contrast-30pct);

    /* Primary */
    --lumo-primary-color: hsl(214, 100%, 48%);
    --lumo-primary-color-50pct: hsla(214, 100%, 49%, 0.76);
    --lumo-primary-color-10pct: hsla(214, 100%, 60%, 0.13);
    --lumo-primary-text-color: hsl(214, 100%, 43%);
    --lumo-primary-contrast-color: #fff;

    /* Error */
    --lumo-error-color: hsl(3, 85%, 48%);
    --lumo-error-color-50pct: hsla(3, 85%, 49%, 0.5);
    --lumo-error-color-10pct: hsla(3, 85%, 49%, 0.1);
    --lumo-error-text-color: hsl(3, 89%, 42%);
    --lumo-error-contrast-color: #fff;

    /* Success */
    --lumo-success-color: hsl(145, 72%, 30%);
    --lumo-success-color-50pct: hsla(145, 72%, 31%, 0.5);
    --lumo-success-color-10pct: hsla(145, 72%, 31%, 0.1);
    --lumo-success-text-color: hsl(145, 85%, 25%);
    --lumo-success-contrast-color: #fff;

    /* Warning */
    --lumo-warning-color: hsl(48, 100%, 50%);
    --lumo-warning-color-10pct: hsla(48, 100%, 50%, 0.25);
    --lumo-warning-text-color: hsl(32, 100%, 30%);
    --lumo-warning-contrast-color: var(--lumo-shade-90pct);
  }

  /* forced-colors mode adjustments */
  @media (forced-colors: active) {
    html {
      --lumo-disabled-text-color: GrayText;
    }
  }
`;
Ui("color-props", Rh);
const Ah = Ee`
  [theme~='dark'] {
    /* Base (background) */
    --lumo-base-color: hsl(214, 35%, 21%);

    /* Tint */
    --lumo-tint-5pct: hsla(214, 65%, 85%, 0.06);
    --lumo-tint-10pct: hsla(214, 60%, 80%, 0.14);
    --lumo-tint-20pct: hsla(214, 64%, 82%, 0.23);
    --lumo-tint-30pct: hsla(214, 69%, 84%, 0.32);
    --lumo-tint-40pct: hsla(214, 73%, 86%, 0.41);
    --lumo-tint-50pct: hsla(214, 78%, 88%, 0.5);
    --lumo-tint-60pct: hsla(214, 82%, 90%, 0.58);
    --lumo-tint-70pct: hsla(214, 87%, 92%, 0.69);
    --lumo-tint-80pct: hsla(214, 91%, 94%, 0.8);
    --lumo-tint-90pct: hsla(214, 96%, 96%, 0.9);
    --lumo-tint: hsl(214, 100%, 98%);

    /* Shade */
    --lumo-shade-5pct: hsla(214, 0%, 0%, 0.07);
    --lumo-shade-10pct: hsla(214, 4%, 2%, 0.15);
    --lumo-shade-20pct: hsla(214, 8%, 4%, 0.23);
    --lumo-shade-30pct: hsla(214, 12%, 6%, 0.32);
    --lumo-shade-40pct: hsla(214, 16%, 8%, 0.41);
    --lumo-shade-50pct: hsla(214, 20%, 10%, 0.5);
    --lumo-shade-60pct: hsla(214, 24%, 12%, 0.6);
    --lumo-shade-70pct: hsla(214, 28%, 13%, 0.7);
    --lumo-shade-80pct: hsla(214, 32%, 13%, 0.8);
    --lumo-shade-90pct: hsla(214, 33%, 13%, 0.9);
    --lumo-shade: hsl(214, 33%, 13%);

    /* Contrast */
    --lumo-contrast-5pct: var(--lumo-tint-5pct);
    --lumo-contrast-10pct: var(--lumo-tint-10pct);
    --lumo-contrast-20pct: var(--lumo-tint-20pct);
    --lumo-contrast-30pct: var(--lumo-tint-30pct);
    --lumo-contrast-40pct: var(--lumo-tint-40pct);
    --lumo-contrast-50pct: var(--lumo-tint-50pct);
    --lumo-contrast-60pct: var(--lumo-tint-60pct);
    --lumo-contrast-70pct: var(--lumo-tint-70pct);
    --lumo-contrast-80pct: var(--lumo-tint-80pct);
    --lumo-contrast-90pct: var(--lumo-tint-90pct);
    --lumo-contrast: var(--lumo-tint);

    /* Text */
    --lumo-header-text-color: var(--lumo-contrast);
    --lumo-body-text-color: var(--lumo-contrast-90pct);
    --lumo-secondary-text-color: var(--lumo-contrast-70pct);
    --lumo-tertiary-text-color: var(--lumo-contrast-50pct);
    --lumo-disabled-text-color: var(--lumo-contrast-30pct);

    /* Primary */
    --lumo-primary-color: hsl(214, 90%, 48%);
    --lumo-primary-color-50pct: hsla(214, 90%, 70%, 0.69);
    --lumo-primary-color-10pct: hsla(214, 90%, 55%, 0.13);
    --lumo-primary-text-color: hsl(214, 90%, 77%);
    --lumo-primary-contrast-color: #fff;

    /* Error */
    --lumo-error-color: hsl(3, 79%, 49%);
    --lumo-error-color-50pct: hsla(3, 75%, 62%, 0.5);
    --lumo-error-color-10pct: hsla(3, 75%, 62%, 0.14);
    --lumo-error-text-color: hsl(3, 100%, 80%);

    /* Success */
    --lumo-success-color: hsl(145, 72%, 30%);
    --lumo-success-color-50pct: hsla(145, 92%, 51%, 0.5);
    --lumo-success-color-10pct: hsla(145, 92%, 51%, 0.1);
    --lumo-success-text-color: hsl(145, 85%, 46%);

    /* Warning */
    --lumo-warning-color: hsl(43, 100%, 48%);
    --lumo-warning-color-10pct: hsla(40, 100%, 50%, 0.2);
    --lumo-warning-text-color: hsl(45, 100%, 60%);
    --lumo-warning-contrast-color: var(--lumo-shade-90pct);
  }

  html {
    color: var(--lumo-body-text-color);
    background-color: var(--lumo-base-color);
    color-scheme: light;
  }

  [theme~='dark'] {
    color: var(--lumo-body-text-color);
    background-color: var(--lumo-base-color);
    color-scheme: dark;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    color: var(--lumo-header-text-color);
  }

  a:where(:any-link) {
    color: var(--lumo-primary-text-color);
  }

  a:not(:any-link) {
    color: var(--lumo-disabled-text-color);
  }

  blockquote {
    color: var(--lumo-secondary-text-color);
  }

  code,
  pre {
    background-color: var(--lumo-contrast-10pct);
    border-radius: var(--lumo-border-radius-m);
  }
  pre code {
    background: transparent;
  }
`;
Ze("", Ah, { moduleId: "lumo-color" });
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const kh = Ee`
  @font-face {
    font-family: 'lumo-icons';
    src: url(data:application/font-woff;charset=utf-8;base64,)
      format('woff');
    font-weight: normal;
    font-style: normal;
  }

  html {
    --lumo-icons-align-center: '\\ea01';
    --lumo-icons-align-left: '\\ea02';
    --lumo-icons-align-right: '\\ea03';
    --lumo-icons-angle-down: '\\ea04';
    --lumo-icons-angle-left: '\\ea05';
    --lumo-icons-angle-right: '\\ea06';
    --lumo-icons-angle-up: '\\ea07';
    --lumo-icons-arrow-down: '\\ea08';
    --lumo-icons-arrow-left: '\\ea09';
    --lumo-icons-arrow-right: '\\ea0a';
    --lumo-icons-arrow-up: '\\ea0b';
    --lumo-icons-bar-chart: '\\ea0c';
    --lumo-icons-bell: '\\ea0d';
    --lumo-icons-calendar: '\\ea0e';
    --lumo-icons-checkmark: '\\ea0f';
    --lumo-icons-chevron-down: '\\ea10';
    --lumo-icons-chevron-left: '\\ea11';
    --lumo-icons-chevron-right: '\\ea12';
    --lumo-icons-chevron-up: '\\ea13';
    --lumo-icons-clock: '\\ea14';
    --lumo-icons-cog: '\\ea15';
    --lumo-icons-cross: '\\ea16';
    --lumo-icons-download: '\\ea17';
    --lumo-icons-dropdown: '\\ea18';
    --lumo-icons-edit: '\\ea19';
    --lumo-icons-error: '\\ea1a';
    --lumo-icons-eye: '\\ea1b';
    --lumo-icons-eye-disabled: '\\ea1c';
    --lumo-icons-menu: '\\ea1d';
    --lumo-icons-minus: '\\ea1e';
    --lumo-icons-ordered-list: '\\ea1f';
    --lumo-icons-phone: '\\ea20';
    --lumo-icons-photo: '\\ea21';
    --lumo-icons-play: '\\ea22';
    --lumo-icons-plus: '\\ea23';
    --lumo-icons-redo: '\\ea24';
    --lumo-icons-reload: '\\ea25';
    --lumo-icons-search: '\\ea26';
    --lumo-icons-undo: '\\ea27';
    --lumo-icons-unordered-list: '\\ea28';
    --lumo-icons-upload: '\\ea29';
    --lumo-icons-user: '\\ea2a';
  }
`;
Ui("font-icons", kh);
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ih = Ee`
  :host {
    --lumo-size-xs: 1.625rem;
    --lumo-size-s: 1.875rem;
    --lumo-size-m: 2.25rem;
    --lumo-size-l: 2.75rem;
    --lumo-size-xl: 3.5rem;

    /* Icons */
    --lumo-icon-size-s: 1.25em;
    --lumo-icon-size-m: 1.5em;
    --lumo-icon-size-l: 2.25em;
    /* For backwards compatibility */
    --lumo-icon-size: var(--lumo-icon-size-m);
  }
`;
Ui("sizing-props", Ih);
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Dh = Ee`
  :host {
    /* Square */
    --lumo-space-xs: 0.25rem;
    --lumo-space-s: 0.5rem;
    --lumo-space-m: 1rem;
    --lumo-space-l: 1.5rem;
    --lumo-space-xl: 2.5rem;

    /* Wide */
    --lumo-space-wide-xs: calc(var(--lumo-space-xs) / 2) var(--lumo-space-xs);
    --lumo-space-wide-s: calc(var(--lumo-space-s) / 2) var(--lumo-space-s);
    --lumo-space-wide-m: calc(var(--lumo-space-m) / 2) var(--lumo-space-m);
    --lumo-space-wide-l: calc(var(--lumo-space-l) / 2) var(--lumo-space-l);
    --lumo-space-wide-xl: calc(var(--lumo-space-xl) / 2) var(--lumo-space-xl);

    /* Tall */
    --lumo-space-tall-xs: var(--lumo-space-xs) calc(var(--lumo-space-xs) / 2);
    --lumo-space-tall-s: var(--lumo-space-s) calc(var(--lumo-space-s) / 2);
    --lumo-space-tall-m: var(--lumo-space-m) calc(var(--lumo-space-m) / 2);
    --lumo-space-tall-l: var(--lumo-space-l) calc(var(--lumo-space-l) / 2);
    --lumo-space-tall-xl: var(--lumo-space-xl) calc(var(--lumo-space-xl) / 2);
  }
`;
Ui("spacing-props", Dh);
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Oh = Ee`
  :host {
    /* Border radius */
    --lumo-border-radius-s: 0.25em; /* Checkbox, badge, date-picker year indicator, etc */
    --lumo-border-radius-m: var(--lumo-border-radius, 0.25em); /* Button, text field, menu overlay, etc */
    --lumo-border-radius-l: 0.5em; /* Dialog, notification, etc */

    /* Shadow */
    --lumo-box-shadow-xs: 0 1px 4px -1px var(--lumo-shade-50pct);
    --lumo-box-shadow-s: 0 2px 4px -1px var(--lumo-shade-20pct), 0 3px 12px -1px var(--lumo-shade-30pct);
    --lumo-box-shadow-m: 0 2px 6px -1px var(--lumo-shade-20pct), 0 8px 24px -4px var(--lumo-shade-40pct);
    --lumo-box-shadow-l: 0 3px 18px -2px var(--lumo-shade-20pct), 0 12px 48px -6px var(--lumo-shade-40pct);
    --lumo-box-shadow-xl: 0 4px 24px -3px var(--lumo-shade-20pct), 0 18px 64px -8px var(--lumo-shade-40pct);

    /* Clickable element cursor */
    --lumo-clickable-cursor: default;
  }
`;
Ee`
  html {
    /* Button */
    --vaadin-button-background: var(--lumo-contrast-5pct);
    --vaadin-button-border: none;
    --vaadin-button-border-radius: var(--lumo-border-radius-m);
    --vaadin-button-font-size: var(--lumo-font-size-m);
    --vaadin-button-font-weight: 500;
    --vaadin-button-height: var(--lumo-size-m);
    --vaadin-button-margin: var(--lumo-space-xs) 0;
    --vaadin-button-min-width: calc(var(--vaadin-button-height) * 2);
    --vaadin-button-padding: 0 calc(var(--vaadin-button-height) / 3 + var(--lumo-border-radius-m) / 2);
    --vaadin-button-text-color: var(--lumo-primary-text-color);
    --vaadin-button-primary-background: var(--lumo-primary-color);
    --vaadin-button-primary-border: none;
    --vaadin-button-primary-font-weight: 600;
    --vaadin-button-primary-text-color: var(--lumo-primary-contrast-color);
    --vaadin-button-tertiary-background: transparent !important;
    --vaadin-button-tertiary-text-color: var(--lumo-primary-text-color);
    --vaadin-button-tertiary-font-weight: 500;
    --vaadin-button-tertiary-padding: 0 calc(var(--vaadin-button-height) / 6);
    /* Checkbox */
    --vaadin-checkbox-background: var(--lumo-contrast-20pct);
    --vaadin-checkbox-background-hover: var(--lumo-contrast-30pct);
    --vaadin-checkbox-border-radius: var(--lumo-border-radius-s);
    --vaadin-checkbox-checkmark-char: var(--lumo-icons-checkmark);
    --vaadin-checkbox-checkmark-char-indeterminate: '';
    --vaadin-checkbox-checkmark-color: var(--lumo-primary-contrast-color);
    --vaadin-checkbox-checkmark-size: calc(var(--vaadin-checkbox-size) + 2px);
    --vaadin-checkbox-label-color: var(--lumo-body-text-color);
    --vaadin-checkbox-label-font-size: var(--lumo-font-size-m);
    --vaadin-checkbox-label-padding: var(--lumo-space-xs) var(--lumo-space-s) var(--lumo-space-xs) var(--lumo-space-xs);
    --vaadin-checkbox-size: calc(var(--lumo-size-m) / 2);
    --vaadin-checkbox-disabled-checkmark-color: var(--lumo-contrast-30pct);
    --vaadin-checkbox-disabled-background: var(--lumo-contrast-10pct);
    /* Radio button */
    --vaadin-radio-button-background: var(--lumo-contrast-20pct);
    --vaadin-radio-button-background-hover: var(--lumo-contrast-30pct);
    --vaadin-radio-button-dot-color: var(--lumo-primary-contrast-color);
    --vaadin-radio-button-dot-size: 3px;
    --vaadin-radio-button-label-color: var(--lumo-body-text-color);
    --vaadin-radio-button-label-font-size: var(--lumo-font-size-m);
    --vaadin-radio-button-label-padding: var(--lumo-space-xs) var(--lumo-space-s) var(--lumo-space-xs)
      var(--lumo-space-xs);
    --vaadin-radio-button-size: calc(var(--lumo-size-m) / 2);
    --vaadin-radio-button-disabled-background: var(--lumo-contrast-10pct);
    --vaadin-radio-button-disabled-dot-color: var(--lumo-contrast-30pct);
    --vaadin-selection-color: var(--lumo-primary-color);
    --vaadin-selection-color-text: var(--lumo-primary-text-color);
    --vaadin-input-field-border-radius: var(--lumo-border-radius-m);
    --vaadin-focus-ring-color: var(--lumo-primary-color-50pct);
    --vaadin-focus-ring-width: 2px;
    /* Label */
    --vaadin-input-field-label-color: var(--lumo-secondary-text-color);
    --vaadin-input-field-focused-label-color: var(--lumo-primary-text-color);
    --vaadin-input-field-hovered-label-color: var(--lumo-body-text-color);
    --vaadin-input-field-label-font-size: var(--lumo-font-size-s);
    --vaadin-input-field-label-font-weight: 500;
    /* Helper */
    --vaadin-input-field-helper-color: var(--lumo-secondary-text-color);
    --vaadin-input-field-helper-font-size: var(--lumo-font-size-xs);
    --vaadin-input-field-helper-font-weight: 400;
    --vaadin-input-field-helper-spacing: 0.4em;
    /* Error message */
    --vaadin-input-field-error-color: var(--lumo-error-text-color);
    --vaadin-input-field-error-font-size: var(--lumo-font-size-xs);
    --vaadin-input-field-error-font-weight: 400;
    /* Input field */
    --vaadin-input-field-background: var(--lumo-contrast-10pct);
    --vaadin-input-field-icon-color: var(--lumo-contrast-60pct);
    --vaadin-input-field-icon-size: var(--lumo-icon-size-m);
    --vaadin-input-field-invalid-background: var(--lumo-error-color-10pct);
    --vaadin-input-field-invalid-hover-highlight: var(--lumo-error-color-50pct);
    --vaadin-input-field-disabled-background: var(--lumo-contrast-5pct);
    --vaadin-input-field-disabled-value-color: var(--lumo-disabled-text-color);
    --vaadin-input-field-height: var(--lumo-size-m);
    --vaadin-input-field-hover-highlight: var(--lumo-contrast-50pct);
    --vaadin-input-field-placeholder-color: var(--lumo-secondary-text-color);
    --vaadin-input-field-readonly-border: 1px dashed var(--lumo-contrast-30pct);
    --vaadin-input-field-value-color: var(--lumo-body-text-color);
    --vaadin-input-field-value-font-size: var(--lumo-font-size-m);
    --vaadin-input-field-value-font-weight: 500;
  }
`;
Ui("style-props", Oh);
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const zh = Ee`
  :host {
    /* prettier-ignore */
    --lumo-font-family: -apple-system, BlinkMacSystemFont, 'Roboto', 'Segoe UI', Helvetica, Arial, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol';

    /* Font sizes */
    --lumo-font-size-xxs: 0.75rem;
    --lumo-font-size-xs: 0.8125rem;
    --lumo-font-size-s: 0.875rem;
    --lumo-font-size-m: 1rem;
    --lumo-font-size-l: 1.125rem;
    --lumo-font-size-xl: 1.375rem;
    --lumo-font-size-xxl: 1.75rem;
    --lumo-font-size-xxxl: 2.5rem;

    /* Line heights */
    --lumo-line-height-xs: 1.25;
    --lumo-line-height-s: 1.375;
    --lumo-line-height-m: 1.625;
  }
`, Fh = Ee`
  body,
  :host {
    font-family: var(--lumo-font-family);
    font-size: var(--lumo-font-size-m);
    line-height: var(--lumo-line-height-m);
    -webkit-text-size-adjust: 100%;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  small,
  [theme~='font-size-s'] {
    font-size: var(--lumo-font-size-s);
    line-height: var(--lumo-line-height-s);
  }

  [theme~='font-size-xs'] {
    font-size: var(--lumo-font-size-xs);
    line-height: var(--lumo-line-height-xs);
  }

  :where(h1, h2, h3, h4, h5, h6) {
    font-weight: 600;
    line-height: var(--lumo-line-height-xs);
    margin-block: 0;
  }

  :where(h1) {
    font-size: var(--lumo-font-size-xxxl);
  }

  :where(h2) {
    font-size: var(--lumo-font-size-xxl);
  }

  :where(h3) {
    font-size: var(--lumo-font-size-xl);
  }

  :where(h4) {
    font-size: var(--lumo-font-size-l);
  }

  :where(h5) {
    font-size: var(--lumo-font-size-m);
  }

  :where(h6) {
    font-size: var(--lumo-font-size-xs);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  p,
  blockquote {
    margin-top: 0.5em;
    margin-bottom: 0.75em;
  }

  a {
    text-decoration: none;
  }

  a:where(:any-link):hover {
    text-decoration: underline;
  }

  hr {
    display: block;
    align-self: stretch;
    height: 1px;
    border: 0;
    padding: 0;
    margin: var(--lumo-space-s) calc(var(--lumo-border-radius-m) / 2);
    background-color: var(--lumo-contrast-10pct);
  }

  blockquote {
    border-left: 2px solid var(--lumo-contrast-30pct);
  }

  b,
  strong {
    font-weight: 600;
  }

  /* RTL specific styles */
  blockquote[dir='rtl'] {
    border-left: none;
    border-right: 2px solid var(--lumo-contrast-30pct);
  }
`;
Ze("", Fh, { moduleId: "lumo-typography" });
Ui("typography-props", zh);
Ze(
  "vaadin-grid",
  Ee`
    :host {
      font-family: var(--lumo-font-family);
      font-size: var(--lumo-font-size-m);
      line-height: var(--lumo-line-height-s);
      color: var(--lumo-body-text-color);
      background-color: var(--lumo-base-color);
      box-sizing: border-box;
      -webkit-text-size-adjust: 100%;
      -webkit-tap-highlight-color: transparent;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      --_focus-ring-color: var(--vaadin-focus-ring-color, var(--lumo-primary-color-50pct));
      --_focus-ring-width: var(--vaadin-focus-ring-width, 2px);
      /* For internal use only */
      --_lumo-grid-border-color: var(--lumo-contrast-20pct);
      --_lumo-grid-secondary-border-color: var(--lumo-contrast-10pct);
      --_lumo-grid-border-width: 1px;
      --_lumo-grid-selected-row-color: var(--lumo-primary-color-10pct);
    }

    /* No (outer) border */

    :host(:not([theme~='no-border'])) {
      border: var(--_lumo-grid-border-width) solid var(--_lumo-grid-border-color);
    }

    :host([disabled]) {
      opacity: 0.7;
    }

    /* Cell styles */

    [part~='cell'] {
      min-height: var(--lumo-size-m);
      background-color: var(--vaadin-grid-cell-background, var(--lumo-base-color));
      cursor: default;
      --_cell-padding: var(--vaadin-grid-cell-padding, var(--_cell-default-padding));
      --_cell-default-padding: var(--lumo-space-xs) var(--lumo-space-m);
    }

    [part~='cell'] ::slotted(vaadin-grid-cell-content) {
      cursor: inherit;
      padding: var(--_cell-padding);
    }

    /* Apply row borders by default and introduce the "no-row-borders" variant */
    :host(:not([theme~='no-row-borders'])) [part~='cell']:not([part~='details-cell']) {
      border-top: var(--_lumo-grid-border-width) solid var(--_lumo-grid-secondary-border-color);
    }

    /* Hide first body row top border */
    :host(:not([theme~='no-row-borders'])) [part~='first-row'] [part~='cell']:not([part~='details-cell']) {
      border-top: 0;
      min-height: calc(var(--lumo-size-m) - var(--_lumo-grid-border-width));
    }

    /* Focus-ring */

    [part~='row'] {
      position: relative;
    }

    [part~='row']:focus,
    [part~='focused-cell']:focus {
      outline: none;
    }

    :host([navigating]) [part~='row']:focus::before,
    :host([navigating]) [part~='focused-cell']:focus::before {
      content: '';
      position: absolute;
      inset: 0;
      pointer-events: none;
      box-shadow: inset 0 0 0 var(--_focus-ring-width) var(--_focus-ring-color);
    }

    :host([navigating]) [part~='row']:focus::before {
      transform: translateX(calc(-1 * var(--_grid-horizontal-scroll-position)));
      z-index: 3;
    }

    /* Empty state */
    [part~='empty-state'] {
      padding: var(--lumo-space-m);
      color: var(--lumo-secondary-text-color);
    }

    /* Drag and Drop styles */
    :host([dragover])::after {
      content: '';
      position: absolute;
      z-index: 100;
      inset: 0;
      pointer-events: none;
      box-shadow: inset 0 0 0 var(--_focus-ring-width) var(--_focus-ring-color);
    }

    [part~='row'][dragover] {
      z-index: 100 !important;
    }

    [part~='row'][dragover] [part~='cell'] {
      overflow: visible;
    }

    [part~='row'][dragover] [part~='cell']::after {
      content: '';
      position: absolute;
      inset: 0;
      height: calc(var(--_lumo-grid-border-width) + 2px);
      pointer-events: none;
      background: var(--lumo-primary-color-50pct);
    }

    [part~='row'][dragover] [part~='cell'][last-frozen]::after {
      right: -1px;
    }

    :host([theme~='no-row-borders']) [dragover] [part~='cell']::after {
      height: 2px;
    }

    [part~='row'][dragover='below'] [part~='cell']::after {
      top: 100%;
      bottom: auto;
      margin-top: -1px;
    }

    :host([all-rows-visible]) [part~='last-row'][dragover='below'] [part~='cell']::after {
      height: 1px;
    }

    [part~='row'][dragover='above'] [part~='cell']::after {
      top: auto;
      bottom: 100%;
      margin-bottom: -1px;
    }

    [part~='row'][details-opened][dragover='below'] [part~='cell']:not([part~='details-cell'])::after,
    [part~='row'][details-opened][dragover='above'] [part~='details-cell']::after {
      display: none;
    }

    [part~='row'][dragover][dragover='on-top'] [part~='cell']::after {
      height: 100%;
      opacity: 0.5;
    }

    [part~='row'][dragstart] [part~='cell'] {
      border: none !important;
      box-shadow: none !important;
    }

    [part~='row'][dragstart] [part~='cell'][last-column] {
      border-radius: 0 var(--lumo-border-radius-s) var(--lumo-border-radius-s) 0;
    }

    [part~='row'][dragstart] [part~='cell'][first-column] {
      border-radius: var(--lumo-border-radius-s) 0 0 var(--lumo-border-radius-s);
    }

    #scroller [part~='row'][dragstart]:not([dragstart=''])::after {
      display: block;
      position: absolute;
      left: var(--_grid-drag-start-x);
      top: var(--_grid-drag-start-y);
      z-index: 100;
      content: attr(dragstart);
      align-items: center;
      justify-content: center;
      box-sizing: border-box;
      padding: calc(var(--lumo-space-xs) * 0.8);
      color: var(--lumo-error-contrast-color);
      background-color: var(--lumo-error-color);
      border-radius: var(--lumo-border-radius-m);
      font-family: var(--lumo-font-family);
      font-size: var(--lumo-font-size-xxs);
      line-height: 1;
      font-weight: 500;
      text-transform: initial;
      letter-spacing: initial;
      min-width: calc(var(--lumo-size-s) * 0.7);
      text-align: center;
    }

    /* Headers and footers */

    [part~='header-cell'],
    [part~='footer-cell'],
    [part~='reorder-ghost'] {
      font-size: var(--lumo-font-size-s);
      font-weight: 500;
    }

    [part~='footer-cell'] {
      font-weight: 400;
    }

    [part~='row']:only-child [part~='header-cell'] {
      min-height: var(--lumo-size-xl);
    }

    /* Header borders */

    /* Hide first header row top border */
    :host(:not([theme~='no-row-borders'])) [part~='row']:first-child [part~='header-cell'] {
      border-top: 0;
    }

    /* Hide header row top border if previous row is hidden */
    [part~='row'][hidden] + [part~='row'] [part~='header-cell'] {
      border-top: 0;
    }

    [part~='row']:last-child [part~='header-cell'] {
      border-bottom: var(--_lumo-grid-border-width) solid transparent;
    }

    :host(:not([theme~='no-row-borders'])) [part~='row']:last-child [part~='header-cell'] {
      border-bottom-color: var(--_lumo-grid-secondary-border-color);
    }

    /* Overflow uses a stronger border color */
    :host([overflow~='top']) [part~='row']:last-child [part~='header-cell'] {
      border-bottom-color: var(--_lumo-grid-border-color);
    }

    /* Footer borders */

    [part~='row']:first-child [part~='footer-cell'] {
      border-top: var(--_lumo-grid-border-width) solid transparent;
    }

    :host(:not([theme~='no-row-borders'])) [part~='row']:first-child [part~='footer-cell'] {
      border-top-color: var(--_lumo-grid-secondary-border-color);
    }

    /* Overflow uses a stronger border color */
    :host([overflow~='bottom']) [part~='row']:first-child [part~='footer-cell'] {
      border-top-color: var(--_lumo-grid-border-color);
    }

    /* Column reordering */

    :host([reordering]) [part~='cell'] {
      background: linear-gradient(var(--lumo-shade-20pct), var(--lumo-shade-20pct)) var(--lumo-base-color);
    }

    :host([reordering]) [part~='cell'][reorder-status='allowed'] {
      background: var(--lumo-base-color);
    }

    :host([reordering]) [part~='cell'][reorder-status='dragging'] {
      background: linear-gradient(var(--lumo-contrast-5pct), var(--lumo-contrast-5pct)) var(--lumo-base-color);
    }

    [part~='reorder-ghost'] {
      opacity: 0.85;
      box-shadow: var(--lumo-box-shadow-s);
      /* TODO Use the same styles as for the cell element (reorder-ghost copies styles from the cell element) */
      padding: var(--lumo-space-s) var(--lumo-space-m) !important;
    }

    /* Column resizing */

    [part='resize-handle'] {
      --_resize-handle-width: 3px;
      width: var(--_resize-handle-width);
      background-color: var(--lumo-primary-color-50pct);
      opacity: 0;
      transition: opacity 0.2s;
    }

    [part='resize-handle']::before {
      transform: translateX(calc(-50% + var(--_resize-handle-width) / 2));
      width: var(--lumo-size-s);
    }

    :host(:not([reordering])) *:not([column-resizing]) [part~='cell']:hover [part='resize-handle'],
    [part='resize-handle']:active {
      opacity: 1;
      transition-delay: 0.15s;
    }

    /* Column borders */

    :host([theme~='column-borders']) [part~='cell']:not([last-column]):not([part~='details-cell']) {
      border-right: var(--_lumo-grid-border-width) solid var(--_lumo-grid-secondary-border-color);
    }

    /* Frozen columns */

    [last-frozen] {
      border-right: var(--_lumo-grid-border-width) solid transparent;
      overflow: hidden;
    }

    :host([overflow~='start']) [part~='cell'][last-frozen]:not([part~='details-cell']) {
      border-right-color: var(--_lumo-grid-border-color);
    }

    [first-frozen-to-end] {
      border-left: var(--_lumo-grid-border-width) solid transparent;
    }

    :host([overflow~='end']) [part~='cell'][first-frozen-to-end]:not([part~='details-cell']) {
      border-left-color: var(--_lumo-grid-border-color);
    }

    /* Row stripes */

    :host([theme~='row-stripes']) [part~='even-row'] [part~='body-cell'],
    :host([theme~='row-stripes']) [part~='even-row'] [part~='details-cell'] {
      background-image: linear-gradient(var(--lumo-contrast-5pct), var(--lumo-contrast-5pct));
      background-repeat: repeat-x;
    }

    /* Selected row */

    /* Raise the selected rows above unselected rows (so that box-shadow can cover unselected rows) */
    :host(:not([reordering])) [part~='row'][selected] {
      z-index: 1;
    }

    :host(:not([reordering])) [part~='row'][selected] [part~='body-cell']:not([part~='details-cell']) {
      background-image: linear-gradient(var(--_lumo-grid-selected-row-color), var(--_lumo-grid-selected-row-color));
      background-repeat: repeat;
    }

    /* Cover the border of an unselected row */
    :host(:not([theme~='no-row-borders'])) [part~='row'][selected] [part~='cell']:not([part~='details-cell']) {
      box-shadow: 0 var(--_lumo-grid-border-width) 0 0 var(--_lumo-grid-selected-row-color);
    }

    /* Compact */

    :host([theme~='compact']) [part~='row']:only-child [part~='header-cell'] {
      min-height: var(--lumo-size-m);
    }

    :host([theme~='compact']) [part~='cell'] {
      min-height: var(--lumo-size-s);
      --_cell-default-padding: var(--lumo-space-xs) var(--lumo-space-s);
    }

    :host([theme~='compact']) [part~='first-row'] [part~='cell']:not([part~='details-cell']) {
      min-height: calc(var(--lumo-size-s) - var(--_lumo-grid-border-width));
    }

    :host([theme~='compact']) [part~='empty-state'] {
      padding: var(--lumo-space-s);
    }

    /* Wrap cell contents */

    :host([theme~='wrap-cell-content']) [part~='cell'] ::slotted(vaadin-grid-cell-content) {
      white-space: normal;
    }

    /* RTL specific styles */

    :host([dir='rtl']) [part~='row'][dragstart] [part~='cell'][last-column] {
      border-radius: var(--lumo-border-radius-s) 0 0 var(--lumo-border-radius-s);
    }

    :host([dir='rtl']) [part~='row'][dragstart] [part~='cell'][first-column] {
      border-radius: 0 var(--lumo-border-radius-s) var(--lumo-border-radius-s) 0;
    }

    :host([dir='rtl'][theme~='column-borders']) [part~='cell']:not([last-column]):not([part~='details-cell']) {
      border-right: none;
      border-left: var(--_lumo-grid-border-width) solid var(--_lumo-grid-secondary-border-color);
    }

    :host([dir='rtl']) [last-frozen] {
      border-right: none;
      border-left: var(--_lumo-grid-border-width) solid transparent;
    }

    :host([dir='rtl']) [first-frozen-to-end] {
      border-left: none;
      border-right: var(--_lumo-grid-border-width) solid transparent;
    }

    :host([dir='rtl'][overflow~='start']) [part~='cell'][last-frozen]:not([part~='details-cell']) {
      border-left-color: var(--_lumo-grid-border-color);
    }

    :host([dir='rtl'][overflow~='end']) [part~='cell'][first-frozen-to-end]:not([part~='details-cell']) {
      border-right-color: var(--_lumo-grid-border-color);
    }
  `,
  { moduleId: "lumo-grid" }
);
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
window.JSCompiler_renameProperty = function(s, n) {
  return s;
};
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
let Lh = /(url\()([^)]*)(\))/g, Mh = /(^\/[^\/])|(^#)|(^[\w-\d]*:)/, Xr, ut;
function dr(s, n) {
  if (s && Mh.test(s) || s === "//")
    return s;
  if (Xr === void 0) {
    Xr = !1;
    try {
      const e = new URL("b", "http://a");
      e.pathname = "c%20d", Xr = e.href === "http://a/c%20d";
    } catch {
    }
  }
  if (n || (n = document.baseURI || window.location.href), Xr)
    try {
      return new URL(s, n).href;
    } catch {
      return s;
    }
  return ut || (ut = document.implementation.createHTMLDocument("temp"), ut.base = ut.createElement("base"), ut.head.appendChild(ut.base), ut.anchor = ut.createElement("a"), ut.body.appendChild(ut.anchor)), ut.base.href = n, ut.anchor.href = s, ut.anchor.href || s;
}
function as(s, n) {
  return s.replace(Lh, function(e, t, i, r) {
    return t + "'" + dr(i.replace(/["']/g, ""), n) + "'" + r;
  });
}
function ls(s) {
  return s.substring(0, s.lastIndexOf("/") + 1);
}
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const Nh = !window.ShadyDOM || !window.ShadyDOM.inUse;
!window.ShadyCSS || window.ShadyCSS.nativeCss;
const Bh = Nh && "adoptedStyleSheets" in Document.prototype && "replaceSync" in CSSStyleSheet.prototype && // Since spec may change, feature detect exact API we need
(() => {
  try {
    const s = new CSSStyleSheet();
    s.replaceSync("");
    const n = document.createElement("div");
    return n.attachShadow({ mode: "open" }), n.shadowRoot.adoptedStyleSheets = [s], n.shadowRoot.adoptedStyleSheets[0] === s;
  } catch {
    return !1;
  }
})();
let Hh = window.Polymer && window.Polymer.rootPath || ls(document.baseURI || window.location.href), bn = window.Polymer && window.Polymer.sanitizeDOMValue || void 0;
window.Polymer && window.Polymer.setPassiveTouchGestures;
let Lo = window.Polymer && window.Polymer.strictTemplatePolicy || !1, Vh = window.Polymer && window.Polymer.allowTemplateFromDomModule || !1, Uh = window.Polymer && window.Polymer.legacyOptimizations || !1, Wh = window.Polymer && window.Polymer.legacyWarnings || !1, jh = window.Polymer && window.Polymer.syncInitialRender || !1, Mo = window.Polymer && window.Polymer.legacyUndefined || !1, Gh = window.Polymer && window.Polymer.orderedComputed || !1, ma = window.Polymer && window.Polymer.removeNestedTemplates || !1, qh = window.Polymer && window.Polymer.fastDomIf || !1;
window.Polymer && window.Polymer.suppressTemplateNotifications;
window.Polymer && window.Polymer.legacyNoObservedAttributes;
let Kh = window.Polymer && window.Polymer.useAdoptedStyleSheetsWithBuiltCSS || !1;
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
let Zh = 0;
const Je = function(s) {
  let n = (
    /** @type {!MixinFunction} */
    s.__mixinApplications
  );
  n || (n = /* @__PURE__ */ new WeakMap(), s.__mixinApplications = n);
  let e = Zh++;
  function t(i) {
    let r = (
      /** @type {!MixinFunction} */
      i.__mixinSet
    );
    if (r && r[e])
      return i;
    let o = n, l = o.get(i);
    if (!l) {
      l = /** @type {!Function} */
      s(i), o.set(i, l);
      let a = Object.create(
        /** @type {!MixinFunction} */
        l.__mixinSet || r || null
      );
      a[e] = !0, l.__mixinSet = a;
    }
    return l;
  }
  return t;
};
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
let cs = {}, ml = {};
function _a(s, n) {
  cs[s] = ml[s.toLowerCase()] = n;
}
function va(s) {
  return cs[s] || ml[s.toLowerCase()];
}
function Yh(s) {
  s.querySelector("style");
}
class mr extends HTMLElement {
  /** @override */
  static get observedAttributes() {
    return ["id"];
  }
  /**
   * Retrieves the element specified by the css `selector` in the module
   * registered by `id`. For example, this.import('foo', 'img');
   * @param {string} id The id of the dom-module in which to search.
   * @param {string=} selector The css selector by which to find the element.
   * @return {Element} Returns the element which matches `selector` in the
   * module registered at the specified `id`.
   *
   * @export
   * @nocollapse Referred to indirectly in style-gather.js
   */
  static import(n, e) {
    if (n) {
      let t = va(n);
      return t && e ? t.querySelector(e) : t;
    }
    return null;
  }
  /* eslint-disable no-unused-vars */
  /**
   * @param {string} name Name of attribute.
   * @param {?string} old Old value of attribute.
   * @param {?string} value Current value of attribute.
   * @param {?string} namespace Attribute namespace.
   * @return {void}
   * @override
   */
  attributeChangedCallback(n, e, t, i) {
    e !== t && this.register();
  }
  /* eslint-enable no-unused-args */
  /**
   * The absolute URL of the original location of this `dom-module`.
   *
   * This value will differ from this element's `ownerDocument` in the
   * following ways:
   * - Takes into account any `assetpath` attribute added during bundling
   *   to indicate the original location relative to the bundled location
   * - Uses the HTMLImports polyfill's `importForElement` API to ensure
   *   the path is relative to the import document's location since
   *   `ownerDocument` is not currently polyfilled
   */
  get assetpath() {
    if (!this.__assetpath) {
      const n = window.HTMLImports && HTMLImports.importForElement ? HTMLImports.importForElement(this) || document : this.ownerDocument, e = dr(
        this.getAttribute("assetpath") || "",
        n.baseURI
      );
      this.__assetpath = ls(e);
    }
    return this.__assetpath;
  }
  /**
   * Registers the dom-module at a given id. This method should only be called
   * when a dom-module is imperatively created. For
   * example, `document.createElement('dom-module').register('foo')`.
   * @param {string=} id The id at which to register the dom-module.
   * @return {void}
   */
  register(n) {
    if (n = n || this.id, n) {
      if (Lo && va(n) !== void 0)
        throw _a(n, null), new Error(`strictTemplatePolicy: dom-module ${n} re-registered`);
      this.id = n, _a(n, this), Yh(this);
    }
  }
}
mr.prototype.modules = cs;
customElements.define("dom-module", mr);
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const Xh = "link[rel=import][type~=css]", Qh = "include", ya = "shady-unscoped";
function _l(s) {
  return (
    /** @type {?DomModule} */
    mr.import(s)
  );
}
function ba(s) {
  let n = s.body ? s.body : s;
  const e = as(
    n.textContent,
    s.baseURI
  ), t = document.createElement("style");
  return t.textContent = e, t;
}
function Jh(s) {
  const n = s.trim().split(/\s+/), e = [];
  for (let t = 0; t < n.length; t++)
    e.push(...$h(n[t]));
  return e;
}
function $h(s) {
  const n = _l(s);
  if (!n)
    return [];
  if (n._styles === void 0) {
    const e = [];
    e.push(...yl(n));
    const t = (
      /** @type {?HTMLTemplateElement} */
      n.querySelector("template")
    );
    t && e.push(...vl(
      t,
      /** @type {templateWithAssetPath} */
      n.assetpath
    )), n._styles = e;
  }
  return n._styles;
}
function vl(s, n) {
  if (!s._styles) {
    const e = [], t = s.content.querySelectorAll("style");
    for (let i = 0; i < t.length; i++) {
      let r = t[i], o = r.getAttribute(Qh);
      o && e.push(...Jh(o).filter(function(l, a, c) {
        return c.indexOf(l) === a;
      })), n && (r.textContent = as(
        r.textContent,
        /** @type {string} */
        n
      )), e.push(r);
    }
    s._styles = e;
  }
  return s._styles;
}
function ed(s) {
  let n = _l(s);
  return n ? yl(n) : [];
}
function yl(s) {
  const n = [], e = s.querySelectorAll(Xh);
  for (let t = 0; t < e.length; t++) {
    let i = e[t];
    if (i.import) {
      const r = i.import, o = i.hasAttribute(ya);
      if (o && !r._unscopedStyle) {
        const l = ba(r);
        l.setAttribute(ya, ""), r._unscopedStyle = l;
      } else r._style || (r._style = ba(r));
      n.push(o ? r._unscopedStyle : r._style);
    }
  }
  return n;
}
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const ui = window.ShadyDOM && window.ShadyDOM.noPatch && window.ShadyDOM.wrap ? window.ShadyDOM.wrap : window.ShadyDOM ? (s) => ShadyDOM.patch(s) : (s) => s;
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
function No(s) {
  return s.indexOf(".") >= 0;
}
function vi(s) {
  let n = s.indexOf(".");
  return n === -1 ? s : s.slice(0, n);
}
function td(s, n) {
  return s.indexOf(n + ".") === 0;
}
function wn(s, n) {
  return n.indexOf(s + ".") === 0;
}
function xn(s, n, e) {
  return n + e.slice(s.length);
}
function ar(s) {
  if (Array.isArray(s)) {
    let n = [];
    for (let e = 0; e < s.length; e++) {
      let t = s[e].toString().split(".");
      for (let i = 0; i < t.length; i++)
        n.push(t[i]);
    }
    return n.join(".");
  } else
    return s;
}
function bl(s) {
  return Array.isArray(s) ? ar(s).split(".") : s.toString().split(".");
}
function st(s, n, e) {
  let t = s, i = bl(n);
  for (let r = 0; r < i.length; r++) {
    if (!t)
      return;
    let o = i[r];
    t = t[o];
  }
  return e && (e.path = i.join(".")), t;
}
function wa(s, n, e) {
  let t = s, i = bl(n), r = i[i.length - 1];
  if (i.length > 1) {
    for (let o = 0; o < i.length - 1; o++) {
      let l = i[o];
      if (t = t[l], !t)
        return;
    }
    t[r] = e;
  } else
    t[n] = e;
  return i.join(".");
}
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const Cn = {}, id = /-[a-z]/g, rd = /([A-Z])/g;
function wl(s) {
  return Cn[s] || (Cn[s] = s.indexOf("-") < 0 ? s : s.replace(
    id,
    (n) => n[1].toUpperCase()
  ));
}
function Dn(s) {
  return Cn[s] || (Cn[s] = s.replace(rd, "-$1").toLowerCase());
}
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
let nd = 0, xl = 0, zi = [], od = 0, Bo = !1, Cl = document.createTextNode("");
new window.MutationObserver(sd).observe(Cl, { characterData: !0 });
function sd() {
  Bo = !1;
  const s = zi.length;
  for (let n = 0; n < s; n++) {
    let e = zi[n];
    if (e)
      try {
        e();
      } catch (t) {
        setTimeout(() => {
          throw t;
        });
      }
  }
  zi.splice(0, s), xl += s;
}
const ad = {
  /**
   * Enqueues a function called at microtask timing.
   *
   * @memberof microTask
   * @param {!Function=} callback Callback to run
   * @return {number} Handle used for canceling task
   */
  run(s) {
    return Bo || (Bo = !0, Cl.textContent = od++), zi.push(s), nd++;
  },
  /**
   * Cancels a previously enqueued `microTask` callback.
   *
   * @memberof microTask
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(s) {
    const n = s - xl;
    if (n >= 0) {
      if (!zi[n])
        throw new Error("invalid async handle: " + s);
      zi[n] = null;
    }
  }
};
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const ld = ad, Tl = Je(
  /**
   * @template T
   * @param {function(new:T)} superClass Class to apply mixin to.
   * @return {function(new:T)} superClass with mixin applied.
   */
  (s) => {
    class n extends s {
      /**
       * Creates property accessors for the given property names.
       * @param {!Object} props Object whose keys are names of accessors.
       * @return {void}
       * @protected
       * @nocollapse
       */
      static createProperties(t) {
        const i = this.prototype;
        for (let r in t)
          r in i || i._createPropertyAccessor(r);
      }
      /**
       * Returns an attribute name that corresponds to the given property.
       * The attribute name is the lowercased property name. Override to
       * customize this mapping.
       * @param {string} property Property to convert
       * @return {string} Attribute name corresponding to the given property.
       *
       * @protected
       * @nocollapse
       */
      static attributeNameForProperty(t) {
        return t.toLowerCase();
      }
      /**
       * Override point to provide a type to which to deserialize a value to
       * a given property.
       * @param {string} name Name of property
       *
       * @protected
       * @nocollapse
       */
      static typeForProperty(t) {
      }
      //eslint-disable-line no-unused-vars
      /**
       * Creates a setter/getter pair for the named property with its own
       * local storage.  The getter returns the value in the local storage,
       * and the setter calls `_setProperty`, which updates the local storage
       * for the property and enqueues a `_propertiesChanged` callback.
       *
       * This method may be called on a prototype or an instance.  Calling
       * this method may overwrite a property value that already exists on
       * the prototype/instance by creating the accessor.
       *
       * @param {string} property Name of the property
       * @param {boolean=} readOnly When true, no setter is created; the
       *   protected `_setProperty` function must be used to set the property
       * @return {void}
       * @protected
       * @override
       */
      _createPropertyAccessor(t, i) {
        this._addPropertyToAttributeMap(t), this.hasOwnProperty(JSCompiler_renameProperty("__dataHasAccessor", this)) || (this.__dataHasAccessor = Object.assign({}, this.__dataHasAccessor)), this.__dataHasAccessor[t] || (this.__dataHasAccessor[t] = !0, this._definePropertyAccessor(t, i));
      }
      /**
       * Adds the given `property` to a map matching attribute names
       * to property names, using `attributeNameForProperty`. This map is
       * used when deserializing attribute values to properties.
       *
       * @param {string} property Name of the property
       * @override
       */
      _addPropertyToAttributeMap(t) {
        this.hasOwnProperty(JSCompiler_renameProperty("__dataAttributes", this)) || (this.__dataAttributes = Object.assign({}, this.__dataAttributes));
        let i = this.__dataAttributes[t];
        return i || (i = this.constructor.attributeNameForProperty(t), this.__dataAttributes[i] = t), i;
      }
      /**
       * Defines a property accessor for the given property.
       * @param {string} property Name of the property
       * @param {boolean=} readOnly When true, no setter is created
       * @return {void}
       * @override
       */
      _definePropertyAccessor(t, i) {
        Object.defineProperty(this, t, {
          /* eslint-disable valid-jsdoc */
          /** @this {PropertiesChanged} */
          get() {
            return this.__data[t];
          },
          /** @this {PropertiesChanged} */
          set: i ? function() {
          } : function(r) {
            this._setPendingProperty(t, r, !0) && this._invalidateProperties();
          }
          /* eslint-enable */
        });
      }
      constructor() {
        super(), this.__dataEnabled = !1, this.__dataReady = !1, this.__dataInvalid = !1, this.__data = {}, this.__dataPending = null, this.__dataOld = null, this.__dataInstanceProps = null, this.__dataCounter = 0, this.__serializing = !1, this._initializeProperties();
      }
      /**
       * Lifecycle callback called when properties are enabled via
       * `_enableProperties`.
       *
       * Users may override this function to implement behavior that is
       * dependent on the element having its property data initialized, e.g.
       * from defaults (initialized from `constructor`, `_initializeProperties`),
       * `attributeChangedCallback`, or values propagated from host e.g. via
       * bindings.  `super.ready()` must be called to ensure the data system
       * becomes enabled.
       *
       * @return {void}
       * @public
       * @override
       */
      ready() {
        this.__dataReady = !0, this._flushProperties();
      }
      /**
       * Initializes the local storage for property accessors.
       *
       * Provided as an override point for performing any setup work prior
       * to initializing the property accessor system.
       *
       * @return {void}
       * @protected
       * @override
       */
      _initializeProperties() {
        for (let t in this.__dataHasAccessor)
          this.hasOwnProperty(t) && (this.__dataInstanceProps = this.__dataInstanceProps || {}, this.__dataInstanceProps[t] = this[t], delete this[t]);
      }
      /**
       * Called at ready time with bag of instance properties that overwrote
       * accessors when the element upgraded.
       *
       * The default implementation sets these properties back into the
       * setter at ready time.  This method is provided as an override
       * point for customizing or providing more efficient initialization.
       *
       * @param {Object} props Bag of property values that were overwritten
       *   when creating property accessors.
       * @return {void}
       * @protected
       * @override
       */
      _initializeInstanceProperties(t) {
        Object.assign(this, t);
      }
      /**
       * Updates the local storage for a property (via `_setPendingProperty`)
       * and enqueues a `_proeprtiesChanged` callback.
       *
       * @param {string} property Name of the property
       * @param {*} value Value to set
       * @return {void}
       * @protected
       * @override
       */
      _setProperty(t, i) {
        this._setPendingProperty(t, i) && this._invalidateProperties();
      }
      /**
       * Returns the value for the given property.
       * @param {string} property Name of property
       * @return {*} Value for the given property
       * @protected
       * @override
       */
      _getProperty(t) {
        return this.__data[t];
      }
      /* eslint-disable no-unused-vars */
      /**
       * Updates the local storage for a property, records the previous value,
       * and adds it to the set of "pending changes" that will be passed to the
       * `_propertiesChanged` callback.  This method does not enqueue the
       * `_propertiesChanged` callback.
       *
       * @param {string} property Name of the property
       * @param {*} value Value to set
       * @param {boolean=} ext Not used here; affordance for closure
       * @return {boolean} Returns true if the property changed
       * @protected
       * @override
       */
      _setPendingProperty(t, i, r) {
        let o = this.__data[t], l = this._shouldPropertyChange(t, i, o);
        return l && (this.__dataPending || (this.__dataPending = {}, this.__dataOld = {}), this.__dataOld && !(t in this.__dataOld) && (this.__dataOld[t] = o), this.__data[t] = i, this.__dataPending[t] = i), l;
      }
      /* eslint-enable */
      /**
       * @param {string} property Name of the property
       * @return {boolean} Returns true if the property is pending.
       */
      _isPropertyPending(t) {
        return !!(this.__dataPending && this.__dataPending.hasOwnProperty(t));
      }
      /**
       * Marks the properties as invalid, and enqueues an async
       * `_propertiesChanged` callback.
       *
       * @return {void}
       * @protected
       * @override
       */
      _invalidateProperties() {
        !this.__dataInvalid && this.__dataReady && (this.__dataInvalid = !0, ld.run(() => {
          this.__dataInvalid && (this.__dataInvalid = !1, this._flushProperties());
        }));
      }
      /**
       * Call to enable property accessor processing. Before this method is
       * called accessor values will be set but side effects are
       * queued. When called, any pending side effects occur immediately.
       * For elements, generally `connectedCallback` is a normal spot to do so.
       * It is safe to call this method multiple times as it only turns on
       * property accessors once.
       *
       * @return {void}
       * @protected
       * @override
       */
      _enableProperties() {
        this.__dataEnabled || (this.__dataEnabled = !0, this.__dataInstanceProps && (this._initializeInstanceProperties(this.__dataInstanceProps), this.__dataInstanceProps = null), this.ready());
      }
      /**
       * Calls the `_propertiesChanged` callback with the current set of
       * pending changes (and old values recorded when pending changes were
       * set), and resets the pending set of changes. Generally, this method
       * should not be called in user code.
       *
       * @return {void}
       * @protected
       * @override
       */
      _flushProperties() {
        this.__dataCounter++;
        const t = this.__data, i = this.__dataPending, r = this.__dataOld;
        this._shouldPropertiesChange(t, i, r) && (this.__dataPending = null, this.__dataOld = null, this._propertiesChanged(t, i, r)), this.__dataCounter--;
      }
      /**
       * Called in `_flushProperties` to determine if `_propertiesChanged`
       * should be called. The default implementation returns true if
       * properties are pending. Override to customize when
       * `_propertiesChanged` is called.
       * @param {!Object} currentProps Bag of all current accessor values
       * @param {?Object} changedProps Bag of properties changed since the last
       *   call to `_propertiesChanged`
       * @param {?Object} oldProps Bag of previous values for each property
       *   in `changedProps`
       * @return {boolean} true if changedProps is truthy
       * @override
       */
      _shouldPropertiesChange(t, i, r) {
        return !!i;
      }
      /**
       * Callback called when any properties with accessors created via
       * `_createPropertyAccessor` have been set.
       *
       * @param {!Object} currentProps Bag of all current accessor values
       * @param {?Object} changedProps Bag of properties changed since the last
       *   call to `_propertiesChanged`
       * @param {?Object} oldProps Bag of previous values for each property
       *   in `changedProps`
       * @return {void}
       * @protected
       * @override
       */
      _propertiesChanged(t, i, r) {
      }
      /**
       * Method called to determine whether a property value should be
       * considered as a change and cause the `_propertiesChanged` callback
       * to be enqueued.
       *
       * The default implementation returns `true` if a strict equality
       * check fails. The method always returns false for `NaN`.
       *
       * Override this method to e.g. provide stricter checking for
       * Objects/Arrays when using immutable patterns.
       *
       * @param {string} property Property name
       * @param {*} value New property value
       * @param {*} old Previous property value
       * @return {boolean} Whether the property should be considered a change
       *   and enqueue a `_proeprtiesChanged` callback
       * @protected
       * @override
       */
      _shouldPropertyChange(t, i, r) {
        return (
          // Strict equality check
          r !== i && // This ensures (old==NaN, value==NaN) always returns false
          (r === r || i === i)
        );
      }
      /**
       * Implements native Custom Elements `attributeChangedCallback` to
       * set an attribute value to a property via `_attributeToProperty`.
       *
       * @param {string} name Name of attribute that changed
       * @param {?string} old Old attribute value
       * @param {?string} value New attribute value
       * @param {?string} namespace Attribute namespace.
       * @return {void}
       * @suppress {missingProperties} Super may or may not implement the callback
       * @override
       */
      attributeChangedCallback(t, i, r, o) {
        i !== r && this._attributeToProperty(t, r), super.attributeChangedCallback && super.attributeChangedCallback(t, i, r, o);
      }
      /**
       * Deserializes an attribute to its associated property.
       *
       * This method calls the `_deserializeValue` method to convert the string to
       * a typed value.
       *
       * @param {string} attribute Name of attribute to deserialize.
       * @param {?string} value of the attribute.
       * @param {*=} type type to deserialize to, defaults to the value
       * returned from `typeForProperty`
       * @return {void}
       * @override
       */
      _attributeToProperty(t, i, r) {
        if (!this.__serializing) {
          const o = this.__dataAttributes, l = o && o[t] || t;
          this[l] = this._deserializeValue(i, r || this.constructor.typeForProperty(l));
        }
      }
      /**
       * Serializes a property to its associated attribute.
       *
       * @suppress {invalidCasts} Closure can't figure out `this` is an element.
       *
       * @param {string} property Property name to reflect.
       * @param {string=} attribute Attribute name to reflect to.
       * @param {*=} value Property value to refect.
       * @return {void}
       * @override
       */
      _propertyToAttribute(t, i, r) {
        this.__serializing = !0, r = arguments.length < 3 ? this[t] : r, this._valueToNodeAttribute(
          /** @type {!HTMLElement} */
          this,
          r,
          i || this.constructor.attributeNameForProperty(t)
        ), this.__serializing = !1;
      }
      /**
       * Sets a typed value to an HTML attribute on a node.
       *
       * This method calls the `_serializeValue` method to convert the typed
       * value to a string.  If the `_serializeValue` method returns `undefined`,
       * the attribute will be removed (this is the default for boolean
       * type `false`).
       *
       * @param {Element} node Element to set attribute to.
       * @param {*} value Value to serialize.
       * @param {string} attribute Attribute name to serialize to.
       * @return {void}
       * @override
       */
      _valueToNodeAttribute(t, i, r) {
        const o = this._serializeValue(i);
        (r === "class" || r === "name" || r === "slot") && (t = /** @type {?Element} */
        ui(t)), o === void 0 ? t.removeAttribute(r) : t.setAttribute(
          r,
          // Closure's type for `setAttribute`'s second parameter incorrectly
          // excludes `TrustedScript`.
          o === "" && window.trustedTypes ? (
            /** @type {?} */
            window.trustedTypes.emptyScript
          ) : o
        );
      }
      /**
       * Converts a typed JavaScript value to a string.
       *
       * This method is called when setting JS property values to
       * HTML attributes.  Users may override this method to provide
       * serialization for custom types.
       *
       * @param {*} value Property value to serialize.
       * @return {string | undefined} String serialized from the provided
       * property  value.
       * @override
       */
      _serializeValue(t) {
        switch (typeof t) {
          case "boolean":
            return t ? "" : void 0;
          default:
            return t?.toString();
        }
      }
      /**
       * Converts a string to a typed JavaScript value.
       *
       * This method is called when reading HTML attribute values to
       * JS properties.  Users may override this method to provide
       * deserialization for custom `type`s. Types for `Boolean`, `String`,
       * and `Number` convert attributes to the expected types.
       *
       * @param {?string} value Value to deserialize.
       * @param {*=} type Type to deserialize the string to.
       * @return {*} Typed value deserialized from the provided string.
       * @override
       */
      _deserializeValue(t, i) {
        switch (i) {
          case Boolean:
            return t !== null;
          case Number:
            return Number(t);
          default:
            return t;
        }
      }
    }
    return n;
  }
);
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const El = {};
let Qr = HTMLElement.prototype;
for (; Qr; ) {
  let s = Object.getOwnPropertyNames(Qr);
  for (let n = 0; n < s.length; n++)
    El[s[n]] = !0;
  Qr = Object.getPrototypeOf(Qr);
}
const cd = window.trustedTypes ? (s) => trustedTypes.isHTML(s) || trustedTypes.isScript(s) || trustedTypes.isScriptURL(s) : () => !1;
function hd(s, n) {
  if (!El[n]) {
    let e = s[n];
    e !== void 0 && (s.__data ? s._setPendingProperty(n, e) : (s.__dataProto ? s.hasOwnProperty(JSCompiler_renameProperty("__dataProto", s)) || (s.__dataProto = Object.create(s.__dataProto)) : s.__dataProto = {}, s.__dataProto[n] = e));
  }
}
const dd = Je((s) => {
  const n = Tl(s);
  class e extends n {
    /**
     * Generates property accessors for all attributes in the standard
     * static `observedAttributes` array.
     *
     * Attribute names are mapped to property names using the `dash-case` to
     * `camelCase` convention
     *
     * @return {void}
     * @nocollapse
     */
    static createPropertiesForAttributes() {
      let i = (
        /** @type {?} */
        this.observedAttributes
      );
      for (let r = 0; r < i.length; r++)
        this.prototype._createPropertyAccessor(wl(i[r]));
    }
    /**
     * Returns an attribute name that corresponds to the given property.
     * By default, converts camel to dash case, e.g. `fooBar` to `foo-bar`.
     * @param {string} property Property to convert
     * @return {string} Attribute name corresponding to the given property.
     *
     * @protected
     * @nocollapse
     */
    static attributeNameForProperty(i) {
      return Dn(i);
    }
    /**
     * Overrides PropertiesChanged implementation to initialize values for
     * accessors created for values that already existed on the element
     * prototype.
     *
     * @return {void}
     * @protected
     * @override
     */
    _initializeProperties() {
      this.__dataProto && (this._initializeProtoProperties(this.__dataProto), this.__dataProto = null), super._initializeProperties();
    }
    /**
     * Called at instance time with bag of properties that were overwritten
     * by accessors on the prototype when accessors were created.
     *
     * The default implementation sets these properties back into the
     * setter at instance time.  This method is provided as an override
     * point for customizing or providing more efficient initialization.
     *
     * @param {Object} props Bag of property values that were overwritten
     *   when creating property accessors.
     * @return {void}
     * @protected
     * @override
     */
    _initializeProtoProperties(i) {
      for (let r in i)
        this._setProperty(r, i[r]);
    }
    /**
     * Ensures the element has the given attribute. If it does not,
     * assigns the given value to the attribute.
     *
     * @suppress {invalidCasts} Closure can't figure out `this` is infact an
     *     element
     *
     * @param {string} attribute Name of attribute to ensure is set.
     * @param {string} value of the attribute.
     * @return {void}
     * @override
     */
    _ensureAttribute(i, r) {
      const o = (
        /** @type {!HTMLElement} */
        this
      );
      o.hasAttribute(i) || this._valueToNodeAttribute(o, r, i);
    }
    /**
     * Overrides PropertiesChanged implemention to serialize objects as JSON.
     *
     * @param {*} value Property value to serialize.
     * @return {string | undefined} String serialized from the provided property
     *     value.
     * @override
     */
    _serializeValue(i) {
      switch (typeof i) {
        case "object":
          if (i instanceof Date)
            return i.toString();
          if (i) {
            if (cd(i))
              return (
                /** @type {?} */
                i
              );
            try {
              return JSON.stringify(i);
            } catch {
              return "";
            }
          }
        default:
          return super._serializeValue(i);
      }
    }
    /**
     * Converts a string to a typed JavaScript value.
     *
     * This method is called by Polymer when reading HTML attribute values to
     * JS properties.  Users may override this method on Polymer element
     * prototypes to provide deserialization for custom `type`s.  Note,
     * the `type` argument is the value of the `type` field provided in the
     * `properties` configuration object for a given property, and is
     * by convention the constructor for the type to deserialize.
     *
     *
     * @param {?string} value Attribute value to deserialize.
     * @param {*=} type Type to deserialize the string to.
     * @return {*} Typed value deserialized from the provided string.
     * @override
     */
    _deserializeValue(i, r) {
      let o;
      switch (r) {
        case Object:
          try {
            o = JSON.parse(
              /** @type {string} */
              i
            );
          } catch {
            o = i;
          }
          break;
        case Array:
          try {
            o = JSON.parse(
              /** @type {string} */
              i
            );
          } catch {
            o = null;
          }
          break;
        case Date:
          o = isNaN(i) ? String(i) : Number(i), o = new Date(o);
          break;
        default:
          o = super._deserializeValue(i, r);
          break;
      }
      return o;
    }
    /* eslint-enable no-fallthrough */
    /**
     * Overrides PropertiesChanged implementation to save existing prototype
     * property value so that it can be reset.
     * @param {string} property Name of the property
     * @param {boolean=} readOnly When true, no setter is created
     *
     * When calling on a prototype, any overwritten values are saved in
     * `__dataProto`, and it is up to the subclasser to decide how/when
     * to set those properties back into the accessor.  When calling on an
     * instance, the overwritten value is set via `_setPendingProperty`,
     * and the user should call `_invalidateProperties` or `_flushProperties`
     * for the values to take effect.
     * @protected
     * @return {void}
     * @override
     */
    _definePropertyAccessor(i, r) {
      hd(this, i), super._definePropertyAccessor(i, r);
    }
    /**
     * Returns true if this library created an accessor for the given property.
     *
     * @param {string} property Property name
     * @return {boolean} True if an accessor was created
     * @override
     */
    _hasAccessor(i) {
      return this.__dataHasAccessor && this.__dataHasAccessor[i];
    }
    /**
     * Returns true if the specified property has a pending change.
     *
     * @param {string} prop Property name
     * @return {boolean} True if property has a pending change
     * @protected
     * @override
     */
    _isPropertyPending(i) {
      return !!(this.__dataPending && i in this.__dataPending);
    }
  }
  return e;
});
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const ud = {
  "dom-if": !0,
  "dom-repeat": !0
};
let xa = !1, Ca = !1;
function fd() {
  if (!xa) {
    xa = !0;
    const s = document.createElement("textarea");
    s.placeholder = "a", Ca = s.placeholder === s.textContent;
  }
  return Ca;
}
function pd(s) {
  fd() && s.localName === "textarea" && s.placeholder && s.placeholder === s.textContent && (s.textContent = null);
}
const gd = (() => {
  const s = window.trustedTypes && window.trustedTypes.createPolicy(
    "polymer-template-event-attribute-policy",
    {
      createScript: (n) => n
    }
  );
  return (n, e, t) => {
    const i = e.getAttribute(t);
    if (s && t.startsWith("on-")) {
      n.setAttribute(
        t,
        s.createScript(i, t)
      );
      return;
    }
    n.setAttribute(t, i);
  };
})();
function md(s) {
  let n = s.getAttribute("is");
  if (n && ud[n]) {
    let e = s;
    for (e.removeAttribute("is"), s = e.ownerDocument.createElement(n), e.parentNode.replaceChild(s, e), s.appendChild(e); e.attributes.length; ) {
      const { name: t } = e.attributes[0];
      gd(s, e, t), e.removeAttribute(t);
    }
  }
  return s;
}
function Pl(s, n) {
  let e = n.parentInfo && Pl(s, n.parentInfo);
  if (e) {
    for (let t = e.firstChild, i = 0; t; t = t.nextSibling)
      if (n.parentIndex === i++)
        return t;
  } else
    return s;
}
function _d(s, n, e, t) {
  t.id && (n[t.id] = e);
}
function vd(s, n, e) {
  if (e.events && e.events.length)
    for (let t = 0, i = e.events, r; t < i.length && (r = i[t]); t++)
      s._addMethodEventListenerToNode(n, r.name, r.value, s);
}
function yd(s, n, e, t) {
  e.templateInfo && (n._templateInfo = e.templateInfo, n._parentTemplateInfo = t);
}
function bd(s, n, e) {
  return s = s._methodHost || s, function(i) {
    s[e] && s[e](i, i.detail);
  };
}
const wd = Je(
  /**
   * @template T
   * @param {function(new:T)} superClass Class to apply mixin to.
   * @return {function(new:T)} superClass with mixin applied.
   */
  (s) => {
    class n extends s {
      /**
       * Scans a template to produce template metadata.
       *
       * Template-specific metadata are stored in the object returned, and node-
       * specific metadata are stored in objects in its flattened `nodeInfoList`
       * array.  Only nodes in the template that were parsed as nodes of
       * interest contain an object in `nodeInfoList`.  Each `nodeInfo` object
       * contains an `index` (`childNodes` index in parent) and optionally
       * `parent`, which points to node info of its parent (including its index).
       *
       * The template metadata object returned from this method has the following
       * structure (many fields optional):
       *
       * ```js
       *   {
       *     // Flattened list of node metadata (for nodes that generated metadata)
       *     nodeInfoList: [
       *       {
       *         // `id` attribute for any nodes with id's for generating `$` map
       *         id: {string},
       *         // `on-event="handler"` metadata
       *         events: [
       *           {
       *             name: {string},   // event name
       *             value: {string},  // handler method name
       *           }, ...
       *         ],
       *         // Notes when the template contained a `<slot>` for shady DOM
       *         // optimization purposes
       *         hasInsertionPoint: {boolean},
       *         // For nested `<template>`` nodes, nested template metadata
       *         templateInfo: {object}, // nested template metadata
       *         // Metadata to allow efficient retrieval of instanced node
       *         // corresponding to this metadata
       *         parentInfo: {number},   // reference to parent nodeInfo>
       *         parentIndex: {number},  // index in parent's `childNodes` collection
       *         infoIndex: {number},    // index of this `nodeInfo` in `templateInfo.nodeInfoList`
       *       },
       *       ...
       *     ],
       *     // When true, the template had the `strip-whitespace` attribute
       *     // or was nested in a template with that setting
       *     stripWhitespace: {boolean},
       *     // For nested templates, nested template content is moved into
       *     // a document fragment stored here; this is an optimization to
       *     // avoid the cost of nested template cloning
       *     content: {DocumentFragment}
       *   }
       * ```
       *
       * This method kicks off a recursive treewalk as follows:
       *
       * ```
       *    _parseTemplate <---------------------+
       *      _parseTemplateContent              |
       *        _parseTemplateNode  <------------|--+
       *          _parseTemplateNestedTemplate --+  |
       *          _parseTemplateChildNodes ---------+
       *          _parseTemplateNodeAttributes
       *            _parseTemplateNodeAttribute
       *
       * ```
       *
       * These methods may be overridden to add custom metadata about templates
       * to either `templateInfo` or `nodeInfo`.
       *
       * Note that this method may be destructive to the template, in that
       * e.g. event annotations may be removed after being noted in the
       * template metadata.
       *
       * @param {!HTMLTemplateElement} template Template to parse
       * @param {TemplateInfo=} outerTemplateInfo Template metadata from the outer
       *   template, for parsing nested templates
       * @return {!TemplateInfo} Parsed template metadata
       * @nocollapse
       */
      static _parseTemplate(t, i) {
        if (!t._templateInfo) {
          let r = t._templateInfo = {};
          r.nodeInfoList = [], r.nestedTemplate = !!i, r.stripWhiteSpace = i && i.stripWhiteSpace || t.hasAttribute && t.hasAttribute("strip-whitespace"), this._parseTemplateContent(
            t,
            r,
            /** @type {?} */
            { parent: null }
          );
        }
        return t._templateInfo;
      }
      /**
       * See docs for _parseTemplateNode.
       *
       * @param {!HTMLTemplateElement} template .
       * @param {!TemplateInfo} templateInfo .
       * @param {!NodeInfo} nodeInfo .
       * @return {boolean} .
       * @nocollapse
       */
      static _parseTemplateContent(t, i, r) {
        return this._parseTemplateNode(t.content, i, r);
      }
      /**
       * Parses template node and adds template and node metadata based on
       * the current node, and its `childNodes` and `attributes`.
       *
       * This method may be overridden to add custom node or template specific
       * metadata based on this node.
       *
       * @param {Node} node Node to parse
       * @param {!TemplateInfo} templateInfo Template metadata for current template
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @return {boolean} `true` if the visited node added node-specific
       *   metadata to `nodeInfo`
       * @nocollapse
       */
      static _parseTemplateNode(t, i, r) {
        let o = !1, l = (
          /** @type {!HTMLTemplateElement} */
          t
        );
        return l.localName == "template" && !l.hasAttribute("preserve-content") ? o = this._parseTemplateNestedTemplate(l, i, r) || o : l.localName === "slot" && (i.hasInsertionPoint = !0), pd(l), l.firstChild && this._parseTemplateChildNodes(l, i, r), l.hasAttributes && l.hasAttributes() && (o = this._parseTemplateNodeAttributes(l, i, r) || o), o || r.noted;
      }
      /**
       * Parses template child nodes for the given root node.
       *
       * This method also wraps whitelisted legacy template extensions
       * (`is="dom-if"` and `is="dom-repeat"`) with their equivalent element
       * wrappers, collapses text nodes, and strips whitespace from the template
       * if the `templateInfo.stripWhitespace` setting was provided.
       *
       * @param {Node} root Root node whose `childNodes` will be parsed
       * @param {!TemplateInfo} templateInfo Template metadata for current template
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @return {void}
       */
      static _parseTemplateChildNodes(t, i, r) {
        if (!(t.localName === "script" || t.localName === "style"))
          for (let o = t.firstChild, l = 0, a; o; o = a) {
            if (o.localName == "template" && (o = md(o)), a = o.nextSibling, o.nodeType === Node.TEXT_NODE) {
              let d = a;
              for (; d && d.nodeType === Node.TEXT_NODE; )
                o.textContent += d.textContent, a = d.nextSibling, t.removeChild(d), d = a;
              if (i.stripWhiteSpace && !o.textContent.trim()) {
                t.removeChild(o);
                continue;
              }
            }
            let c = (
              /** @type {!NodeInfo} */
              { parentIndex: l, parentInfo: r }
            );
            this._parseTemplateNode(o, i, c) && (c.infoIndex = i.nodeInfoList.push(c) - 1), o.parentNode && l++;
          }
      }
      /**
       * Parses template content for the given nested `<template>`.
       *
       * Nested template info is stored as `templateInfo` in the current node's
       * `nodeInfo`. `template.content` is removed and stored in `templateInfo`.
       * It will then be the responsibility of the host to set it back to the
       * template and for users stamping nested templates to use the
       * `_contentForTemplate` method to retrieve the content for this template
       * (an optimization to avoid the cost of cloning nested template content).
       *
       * @param {HTMLTemplateElement} node Node to parse (a <template>)
       * @param {TemplateInfo} outerTemplateInfo Template metadata for current template
       *   that includes the template `node`
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @return {boolean} `true` if the visited node added node-specific
       *   metadata to `nodeInfo`
       * @nocollapse
       */
      static _parseTemplateNestedTemplate(t, i, r) {
        let o = (
          /** @type {!HTMLTemplateElement} */
          t
        ), l = this._parseTemplate(o, i);
        return (l.content = o.content.ownerDocument.createDocumentFragment()).appendChild(o.content), r.templateInfo = l, !0;
      }
      /**
       * Parses template node attributes and adds node metadata to `nodeInfo`
       * for nodes of interest.
       *
       * @param {Element} node Node to parse
       * @param {!TemplateInfo} templateInfo Template metadata for current
       *     template
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @return {boolean} `true` if the visited node added node-specific
       *   metadata to `nodeInfo`
       * @nocollapse
       */
      static _parseTemplateNodeAttributes(t, i, r) {
        let o = !1, l = Array.from(t.attributes);
        for (let a = l.length - 1, c; c = l[a]; a--)
          o = this._parseTemplateNodeAttribute(t, i, r, c.name, c.value) || o;
        return o;
      }
      /**
       * Parses a single template node attribute and adds node metadata to
       * `nodeInfo` for attributes of interest.
       *
       * This implementation adds metadata for `on-event="handler"` attributes
       * and `id` attributes.
       *
       * @param {Element} node Node to parse
       * @param {!TemplateInfo} templateInfo Template metadata for current template
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @param {string} name Attribute name
       * @param {string} value Attribute value
       * @return {boolean} `true` if the visited node added node-specific
       *   metadata to `nodeInfo`
       * @nocollapse
       */
      static _parseTemplateNodeAttribute(t, i, r, o, l) {
        return o.slice(0, 3) === "on-" ? (t.removeAttribute(o), r.events = r.events || [], r.events.push({
          name: o.slice(3),
          value: l
        }), !0) : o === "id" ? (r.id = l, !0) : !1;
      }
      /**
       * Returns the `content` document fragment for a given template.
       *
       * For nested templates, Polymer performs an optimization to cache nested
       * template content to avoid the cost of cloning deeply nested templates.
       * This method retrieves the cached content for a given template.
       *
       * @param {HTMLTemplateElement} template Template to retrieve `content` for
       * @return {DocumentFragment} Content fragment
       * @nocollapse
       */
      static _contentForTemplate(t) {
        let i = (
          /** @type {HTMLTemplateElementWithInfo} */
          t._templateInfo
        );
        return i && i.content || t.content;
      }
      /**
       * Clones the provided template content and returns a document fragment
       * containing the cloned dom.
       *
       * The template is parsed (once and memoized) using this library's
       * template parsing features, and provides the following value-added
       * features:
       * * Adds declarative event listeners for `on-event="handler"` attributes
       * * Generates an "id map" for all nodes with id's under `$` on returned
       *   document fragment
       * * Passes template info including `content` back to templates as
       *   `_templateInfo` (a performance optimization to avoid deep template
       *   cloning)
       *
       * Note that the memoized template parsing process is destructive to the
       * template: attributes for bindings and declarative event listeners are
       * removed after being noted in notes, and any nested `<template>.content`
       * is removed and stored in notes as well.
       *
       * @param {!HTMLTemplateElement} template Template to stamp
       * @param {TemplateInfo=} templateInfo Optional template info associated
       *   with the template to be stamped; if omitted the template will be
       *   automatically parsed.
       * @return {!StampedTemplate} Cloned template content
       * @override
       */
      _stampTemplate(t, i) {
        t && !t.content && window.HTMLTemplateElement && HTMLTemplateElement.decorate && HTMLTemplateElement.decorate(t), i = i || this.constructor._parseTemplate(t);
        let r = i.nodeInfoList, o = i.content || t.content, l = (
          /** @type {DocumentFragment} */
          document.importNode(o, !0)
        );
        l.__noInsertionPoint = !i.hasInsertionPoint;
        let a = l.nodeList = new Array(r.length);
        l.$ = {};
        for (let c = 0, d = r.length, f; c < d && (f = r[c]); c++) {
          let p = a[c] = Pl(l, f);
          _d(this, l.$, p, f), yd(this, p, f, i), vd(this, p, f);
        }
        return l = /** @type {!StampedTemplate} */
        l, l;
      }
      /**
       * Adds an event listener by method name for the event provided.
       *
       * This method generates a handler function that looks up the method
       * name at handling time.
       *
       * @param {!EventTarget} node Node to add listener on
       * @param {string} eventName Name of event
       * @param {string} methodName Name of method
       * @param {*=} context Context the method will be called on (defaults
       *   to `node`)
       * @return {Function} Generated handler function
       * @override
       */
      _addMethodEventListenerToNode(t, i, r, o) {
        o = o || t;
        let l = bd(o, i, r);
        return this._addEventListenerToNode(t, i, l), l;
      }
      /**
       * Override point for adding custom or simulated event handling.
       *
       * @param {!EventTarget} node Node to add event listener to
       * @param {string} eventName Name of event
       * @param {function(!Event):void} handler Listener function to add
       * @return {void}
       * @override
       */
      _addEventListenerToNode(t, i, r) {
        t.addEventListener(i, r);
      }
      /**
       * Override point for adding custom or simulated event handling.
       *
       * @param {!EventTarget} node Node to remove event listener from
       * @param {string} eventName Name of event
       * @param {function(!Event):void} handler Listener function to remove
       * @return {void}
       * @override
       */
      _removeEventListenerFromNode(t, i, r) {
        t.removeEventListener(i, r);
      }
    }
    return n;
  }
);
/**
 * @fileoverview
 * @suppress {checkPrototypalTypes}
 * @license Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
 * This code may only be used under the BSD style license found at
 * http://polymer.github.io/LICENSE.txt The complete set of authors may be found
 * at http://polymer.github.io/AUTHORS.txt The complete set of contributors may
 * be found at http://polymer.github.io/CONTRIBUTORS.txt Code distributed by
 * Google as part of the polymer project is also subject to an additional IP
 * rights grant found at http://polymer.github.io/PATENTS.txt
 */
let _r = 0;
const vr = [], Re = {
  COMPUTE: "__computeEffects",
  REFLECT: "__reflectEffects",
  NOTIFY: "__notifyEffects",
  PROPAGATE: "__propagateEffects",
  OBSERVE: "__observeEffects",
  READ_ONLY: "__readOnly"
}, Sl = "__computeInfo", xd = /[A-Z]/;
function _o(s, n, e) {
  let t = s[n];
  if (!t)
    t = s[n] = {};
  else if (!s.hasOwnProperty(n) && (t = s[n] = Object.create(s[n]), e))
    for (let i in t) {
      let r = t[i], o = t[i] = Array(r.length);
      for (let l = 0; l < r.length; l++)
        o[l] = r[l];
    }
  return t;
}
function lr(s, n, e, t, i, r) {
  if (n) {
    let o = !1;
    const l = _r++;
    for (let a in e) {
      let c = i ? vi(a) : a, d = n[c];
      if (d)
        for (let f = 0, p = d.length, b; f < p && (b = d[f]); f++)
          (!b.info || b.info.lastRun !== l) && (!i || hs(a, b.trigger)) && (b.info && (b.info.lastRun = l), b.fn(s, a, e, t, b.info, i, r), o = !0);
    }
    return o;
  }
  return !1;
}
function Cd(s, n, e, t, i, r, o, l) {
  let a = !1, c = o ? vi(t) : t, d = n[c];
  if (d)
    for (let f = 0, p = d.length, b; f < p && (b = d[f]); f++)
      (!b.info || b.info.lastRun !== e) && (!o || hs(t, b.trigger)) && (b.info && (b.info.lastRun = e), b.fn(s, t, i, r, b.info, o, l), a = !0);
  return a;
}
function hs(s, n) {
  if (n) {
    let e = (
      /** @type {string} */
      n.name
    );
    return e == s || !!(n.structured && td(e, s)) || !!(n.wildcard && wn(e, s));
  } else
    return !0;
}
function Ta(s, n, e, t, i) {
  let r = typeof i.method == "string" ? s[i.method] : i.method, o = i.property;
  r ? r.call(s, s.__data[o], t[o]) : i.dynamicFn;
}
function Td(s, n, e, t, i) {
  let r = s[Re.NOTIFY], o, l = _r++;
  for (let c in n)
    n[c] && (r && Cd(s, r, l, c, e, t, i) || i && Ed(s, c, e)) && (o = !0);
  let a;
  o && (a = s.__dataHost) && a._invalidateProperties && a._invalidateProperties();
}
function Ed(s, n, e) {
  let t = vi(n);
  if (t !== n) {
    let i = Dn(t) + "-changed";
    return Rl(s, i, e[n], n), !0;
  }
  return !1;
}
function Rl(s, n, e, t) {
  let i = {
    value: e,
    queueProperty: !0
  };
  t && (i.path = t), ui(
    /** @type {!HTMLElement} */
    s
  ).dispatchEvent(new CustomEvent(n, { detail: i }));
}
function Pd(s, n, e, t, i, r) {
  let l = (r ? vi(n) : n) != n ? n : null, a = l ? st(s, l) : s.__data[n];
  l && a === void 0 && (a = e[n]), Rl(s, i.eventName, a, l);
}
function Sd(s, n, e, t, i) {
  let r, o = (
    /** @type {Object} */
    s.detail
  ), l = o && o.path;
  l ? (t = xn(e, t, l), r = o && o.value) : r = s.currentTarget[e], r = i ? !r : r, (!n[Re.READ_ONLY] || !n[Re.READ_ONLY][t]) && n._setPendingPropertyOrPath(t, r, !0, !!l) && (!o || !o.queueProperty) && n._invalidateProperties();
}
function Rd(s, n, e, t, i) {
  let r = s.__data[n];
  bn && (r = bn(
    r,
    i.attrName,
    "attribute",
    /** @type {Node} */
    s
  )), s._propertyToAttribute(n, i.attrName, r);
}
function Ad(s, n, e, t) {
  let i = s[Re.COMPUTE];
  if (i)
    if (Gh) {
      _r++;
      const r = Id(s), o = [];
      for (let a in n)
        Ea(a, i, o, r, t);
      let l;
      for (; l = o.shift(); )
        Al(s, "", n, e, l) && Ea(l.methodInfo, i, o, r, t);
      Object.assign(
        /** @type {!Object} */
        e,
        s.__dataOld
      ), Object.assign(
        /** @type {!Object} */
        n,
        s.__dataPending
      ), s.__dataPending = null;
    } else {
      let r = n;
      for (; lr(s, i, r, e, t); )
        Object.assign(
          /** @type {!Object} */
          e,
          s.__dataOld
        ), Object.assign(
          /** @type {!Object} */
          n,
          s.__dataPending
        ), r = s.__dataPending, s.__dataPending = null;
    }
}
const kd = (s, n, e) => {
  let t = 0, i = n.length - 1, r = -1;
  for (; t <= i; ) {
    const o = t + i >> 1, l = e.get(n[o].methodInfo) - e.get(s.methodInfo);
    if (l < 0)
      t = o + 1;
    else if (l > 0)
      i = o - 1;
    else {
      r = o;
      break;
    }
  }
  r < 0 && (r = i + 1), n.splice(r, 0, s);
}, Ea = (s, n, e, t, i) => {
  const r = i ? vi(s) : s, o = n[r];
  if (o)
    for (let l = 0; l < o.length; l++) {
      const a = o[l];
      a.info.lastRun !== _r && (!i || hs(s, a.trigger)) && (a.info.lastRun = _r, kd(a.info, e, t));
    }
};
function Id(s) {
  let n = s.constructor.__orderedComputedDeps;
  if (!n) {
    n = /* @__PURE__ */ new Map();
    const e = s[Re.COMPUTE];
    let { counts: t, ready: i, total: r } = Dd(s), o;
    for (; o = i.shift(); ) {
      n.set(o, n.size);
      const l = e[o];
      l && l.forEach((a) => {
        const c = a.info.methodInfo;
        --r, --t[c] === 0 && i.push(c);
      });
    }
    if (r !== 0) {
      const l = (
        /** @type {HTMLElement} */
        s
      );
    }
    s.constructor.__orderedComputedDeps = n;
  }
  return n;
}
function Dd(s) {
  const n = s[Sl], e = {}, t = s[Re.COMPUTE], i = [];
  let r = 0;
  for (let o in n) {
    const l = n[o];
    r += e[o] = l.args.filter((a) => !a.literal).length + (l.dynamicFn ? 1 : 0);
  }
  for (let o in t)
    n[o] || i.push(o);
  return { counts: e, ready: i, total: r };
}
function Al(s, n, e, t, i) {
  let r = Ho(s, n, e, t, i);
  if (r === vr)
    return !1;
  let o = i.methodInfo;
  return s.__dataHasAccessor && s.__dataHasAccessor[o] ? s._setPendingProperty(o, r, !0) : (s[o] = r, !1);
}
function Od(s, n, e) {
  let t = s.__dataLinkedPaths;
  if (t) {
    let i;
    for (let r in t) {
      let o = t[r];
      wn(r, n) ? (i = xn(r, o, n), s._setPendingPropertyOrPath(i, e, !0, !0)) : wn(o, n) && (i = xn(o, r, n), s._setPendingPropertyOrPath(i, e, !0, !0));
    }
  }
}
function vo(s, n, e, t, i, r, o) {
  e.bindings = e.bindings || [];
  let l = { kind: t, target: i, parts: r, literal: o, isCompound: r.length !== 1 };
  if (e.bindings.push(l), Nd(l)) {
    let { event: c, negate: d } = l.parts[0];
    l.listenerEvent = c || Dn(i) + "-changed", l.listenerNegate = d;
  }
  let a = n.nodeInfoList.length;
  for (let c = 0; c < l.parts.length; c++) {
    let d = l.parts[c];
    d.compoundIndex = c, zd(s, n, l, d, a);
  }
}
function zd(s, n, e, t, i) {
  if (!t.literal && !(e.kind === "attribute" && e.target[0] === "-")) {
    let r = t.dependencies, o = { index: i, binding: e, part: t, evaluator: s };
    for (let l = 0; l < r.length; l++) {
      let a = r[l];
      typeof a == "string" && (a = Il(a), a.wildcard = !0), s._addTemplatePropertyEffect(n, a.rootProperty, {
        fn: Fd,
        info: o,
        trigger: a
      });
    }
  }
}
function Fd(s, n, e, t, i, r, o) {
  let l = o[i.index], a = i.binding, c = i.part;
  if (r && c.source && n.length > c.source.length && a.kind == "property" && !a.isCompound && l.__isPropertyEffectsClient && l.__dataHasAccessor && l.__dataHasAccessor[a.target]) {
    let d = e[n];
    n = xn(c.source, a.target, n), l._setPendingPropertyOrPath(n, d, !1, !0) && s._enqueueClient(l);
  } else {
    let d = i.evaluator._evaluateBinding(s, c, n, e, t, r);
    d !== vr && Ld(s, l, a, c, d);
  }
}
function Ld(s, n, e, t, i) {
  if (i = Md(n, i, e, t), bn && (i = bn(i, e.target, e.kind, n)), e.kind == "attribute")
    s._valueToNodeAttribute(
      /** @type {Element} */
      n,
      i,
      e.target
    );
  else {
    let r = e.target;
    n.__isPropertyEffectsClient && n.__dataHasAccessor && n.__dataHasAccessor[r] ? (!n[Re.READ_ONLY] || !n[Re.READ_ONLY][r]) && n._setPendingProperty(r, i) && s._enqueueClient(n) : s._setUnmanagedPropertyToNode(n, r, i);
  }
}
function Md(s, n, e, t) {
  if (e.isCompound) {
    let i = s.__dataCompoundStorage[e.target];
    i[t.compoundIndex] = n, n = i.join("");
  }
  return e.kind !== "attribute" && (e.target === "textContent" || e.target === "value" && (s.localName === "input" || s.localName === "textarea")) && (n = n ?? ""), n;
}
function Nd(s) {
  return !!s.target && s.kind != "attribute" && s.kind != "text" && !s.isCompound && s.parts[0].mode === "{";
}
function Bd(s, n) {
  let { nodeList: e, nodeInfoList: t } = n;
  if (t.length)
    for (let i = 0; i < t.length; i++) {
      let r = t[i], o = e[i], l = r.bindings;
      if (l)
        for (let a = 0; a < l.length; a++) {
          let c = l[a];
          Hd(o, c), Vd(o, s, c);
        }
      o.__dataHost = s;
    }
}
function Hd(s, n) {
  if (n.isCompound) {
    let e = s.__dataCompoundStorage || (s.__dataCompoundStorage = {}), t = n.parts, i = new Array(t.length);
    for (let o = 0; o < t.length; o++)
      i[o] = t[o].literal;
    let r = n.target;
    e[r] = i, n.literal && n.kind == "property" && (r === "className" && (s = ui(s)), s[r] = n.literal);
  }
}
function Vd(s, n, e) {
  if (e.listenerEvent) {
    let t = e.parts[0];
    s.addEventListener(e.listenerEvent, function(i) {
      Sd(i, n, e.target, t.source, t.negate);
    });
  }
}
function Pa(s, n, e, t, i, r) {
  r = n.static || r && (typeof r != "object" || r[n.methodName]);
  let o = {
    methodName: n.methodName,
    args: n.args,
    methodInfo: i,
    dynamicFn: r
  };
  for (let l = 0, a; l < n.args.length && (a = n.args[l]); l++)
    a.literal || s._addPropertyEffect(a.rootProperty, e, {
      fn: t,
      info: o,
      trigger: a
    });
  return r && s._addPropertyEffect(n.methodName, e, {
    fn: t,
    info: o
  }), o;
}
function Ho(s, n, e, t, i) {
  let r = s._methodHost || s, o = r[i.methodName];
  if (o) {
    let l = s._marshalArgs(i.args, n, e);
    return l === vr ? vr : o.apply(r, l);
  } else i.dynamicFn;
}
const Ud = [], kl = "(?:[a-zA-Z_$][\\w.:$\\-*]*)", Wd = "(?:[-+]?[0-9]*\\.?[0-9]+(?:[eE][-+]?[0-9]+)?)", jd = "(?:'(?:[^'\\\\]|\\\\.)*')", Gd = '(?:"(?:[^"\\\\]|\\\\.)*")', qd = "(?:" + jd + "|" + Gd + ")", Sa = "(?:(" + kl + "|" + Wd + "|" + qd + ")\\s*)", Kd = "(?:" + Sa + "(?:,\\s*" + Sa + ")*)", Zd = "(?:\\(\\s*(?:" + Kd + "?)\\)\\s*)", Yd = "(" + kl + "\\s*" + Zd + "?)", Xd = "(\\[\\[|{{)\\s*", Qd = "(?:]]|}})", Jd = "(?:(!)\\s*)?", $d = Xd + Jd + Yd + Qd, Ra = new RegExp($d, "g");
function Aa(s) {
  let n = "";
  for (let e = 0; e < s.length; e++) {
    let t = s[e].literal;
    n += t || "";
  }
  return n;
}
function yo(s) {
  let n = s.match(/([^\s]+?)\(([\s\S]*)\)/);
  if (n) {
    let t = { methodName: n[1], static: !0, args: Ud };
    if (n[2].trim()) {
      let i = n[2].replace(/\\,/g, "&comma;").split(",");
      return eu(i, t);
    } else
      return t;
  }
  return null;
}
function eu(s, n) {
  return n.args = s.map(function(e) {
    let t = Il(e);
    return t.literal || (n.static = !1), t;
  }, this), n;
}
function Il(s) {
  let n = s.trim().replace(/&comma;/g, ",").replace(/\\(.)/g, "$1"), e = {
    name: n,
    value: "",
    literal: !1
  }, t = n[0];
  switch (t === "-" && (t = n[1]), t >= "0" && t <= "9" && (t = "#"), t) {
    case "'":
    case '"':
      e.value = n.slice(1, -1), e.literal = !0;
      break;
    case "#":
      e.value = Number(n), e.literal = !0;
      break;
  }
  return e.literal || (e.rootProperty = vi(n), e.structured = No(n), e.structured && (e.wildcard = n.slice(-2) == ".*", e.wildcard && (e.name = n.slice(0, -2)))), e;
}
function ka(s, n, e) {
  let t = st(s, e);
  return t === void 0 && (t = n[e]), t;
}
function Dl(s, n, e, t) {
  const i = { indexSplices: t };
  Mo && !s._overrideLegacyUndefined && (n.splices = i), s.notifyPath(e + ".splices", i), s.notifyPath(e + ".length", n.length), Mo && !s._overrideLegacyUndefined && (i.indexSplices = []);
}
function rr(s, n, e, t, i, r) {
  Dl(s, n, e, [{
    index: t,
    addedCount: i,
    removed: r,
    object: n,
    type: "splice"
  }]);
}
function tu(s) {
  return s[0].toUpperCase() + s.substring(1);
}
const iu = Je((s) => {
  const n = wd(dd(s));
  class e extends n {
    constructor() {
      super(), this.__isPropertyEffectsClient = !0, this.__dataClientsReady, this.__dataPendingClients, this.__dataToNotify, this.__dataLinkedPaths, this.__dataHasPaths, this.__dataCompoundStorage, this.__dataHost, this.__dataTemp, this.__dataClientsInitialized, this.__data, this.__dataPending, this.__dataOld, this.__computeEffects, this.__computeInfo, this.__reflectEffects, this.__notifyEffects, this.__propagateEffects, this.__observeEffects, this.__readOnly, this.__templateInfo, this._overrideLegacyUndefined;
    }
    get PROPERTY_EFFECT_TYPES() {
      return Re;
    }
    /**
     * @override
     * @return {void}
     */
    _initializeProperties() {
      super._initializeProperties(), this._registerHost(), this.__dataClientsReady = !1, this.__dataPendingClients = null, this.__dataToNotify = null, this.__dataLinkedPaths = null, this.__dataHasPaths = !1, this.__dataCompoundStorage = this.__dataCompoundStorage || null, this.__dataHost = this.__dataHost || null, this.__dataTemp = {}, this.__dataClientsInitialized = !1;
    }
    _registerHost() {
      if (nr.length) {
        let i = nr[nr.length - 1];
        i._enqueueClient(this), this.__dataHost = i;
      }
    }
    /**
     * Overrides `PropertyAccessors` implementation to provide a
     * more efficient implementation of initializing properties from
     * the prototype on the instance.
     *
     * @override
     * @param {Object} props Properties to initialize on the prototype
     * @return {void}
     */
    _initializeProtoProperties(i) {
      this.__data = Object.create(i), this.__dataPending = Object.create(i), this.__dataOld = {};
    }
    /**
     * Overrides `PropertyAccessors` implementation to avoid setting
     * `_setProperty`'s `shouldNotify: true`.
     *
     * @override
     * @param {Object} props Properties to initialize on the instance
     * @return {void}
     */
    _initializeInstanceProperties(i) {
      let r = this[Re.READ_ONLY];
      for (let o in i)
        (!r || !r[o]) && (this.__dataPending = this.__dataPending || {}, this.__dataOld = this.__dataOld || {}, this.__data[o] = this.__dataPending[o] = i[o]);
    }
    // Prototype setup ----------------------------------------
    /**
     * Equivalent to static `addPropertyEffect` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property that should trigger the effect
     * @param {string} type Effect type, from this.PROPERTY_EFFECT_TYPES
     * @param {Object=} effect Effect metadata object
     * @return {void}
     * @protected
     */
    _addPropertyEffect(i, r, o) {
      this._createPropertyAccessor(i, r == Re.READ_ONLY);
      let l = _o(this, r, !0)[i];
      l || (l = this[r][i] = []), l.push(o);
    }
    /**
     * Removes the given property effect.
     *
     * @override
     * @param {string} property Property the effect was associated with
     * @param {string} type Effect type, from this.PROPERTY_EFFECT_TYPES
     * @param {Object=} effect Effect metadata object to remove
     * @return {void}
     */
    _removePropertyEffect(i, r, o) {
      let l = _o(this, r, !0)[i], a = l.indexOf(o);
      a >= 0 && l.splice(a, 1);
    }
    /**
     * Returns whether the current prototype/instance has a property effect
     * of a certain type.
     *
     * @override
     * @param {string} property Property name
     * @param {string=} type Effect type, from this.PROPERTY_EFFECT_TYPES
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasPropertyEffect(i, r) {
      let o = this[r];
      return !!(o && o[i]);
    }
    /**
     * Returns whether the current prototype/instance has a "read only"
     * accessor for the given property.
     *
     * @override
     * @param {string} property Property name
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasReadOnlyEffect(i) {
      return this._hasPropertyEffect(i, Re.READ_ONLY);
    }
    /**
     * Returns whether the current prototype/instance has a "notify"
     * property effect for the given property.
     *
     * @override
     * @param {string} property Property name
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasNotifyEffect(i) {
      return this._hasPropertyEffect(i, Re.NOTIFY);
    }
    /**
     * Returns whether the current prototype/instance has a "reflect to
     * attribute" property effect for the given property.
     *
     * @override
     * @param {string} property Property name
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasReflectEffect(i) {
      return this._hasPropertyEffect(i, Re.REFLECT);
    }
    /**
     * Returns whether the current prototype/instance has a "computed"
     * property effect for the given property.
     *
     * @override
     * @param {string} property Property name
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasComputedEffect(i) {
      return this._hasPropertyEffect(i, Re.COMPUTE);
    }
    // Runtime ----------------------------------------
    /**
     * Sets a pending property or path.  If the root property of the path in
     * question had no accessor, the path is set, otherwise it is enqueued
     * via `_setPendingProperty`.
     *
     * This function isolates relatively expensive functionality necessary
     * for the public API (`set`, `setProperties`, `notifyPath`, and property
     * change listeners via {{...}} bindings), such that it is only done
     * when paths enter the system, and not at every propagation step.  It
     * also sets a `__dataHasPaths` flag on the instance which is used to
     * fast-path slower path-matching code in the property effects host paths.
     *
     * `path` can be a path string or array of path parts as accepted by the
     * public API.
     *
     * @override
     * @param {string | !Array<number|string>} path Path to set
     * @param {*} value Value to set
     * @param {boolean=} shouldNotify Set to true if this change should
     *  cause a property notification event dispatch
     * @param {boolean=} isPathNotification If the path being set is a path
     *   notification of an already changed value, as opposed to a request
     *   to set and notify the change.  In the latter `false` case, a dirty
     *   check is performed and then the value is set to the path before
     *   enqueuing the pending property change.
     * @return {boolean} Returns true if the property/path was enqueued in
     *   the pending changes bag.
     * @protected
     */
    _setPendingPropertyOrPath(i, r, o, l) {
      if (l || vi(Array.isArray(i) ? i[0] : i) !== i) {
        if (!l) {
          let a = st(this, i);
          if (i = /** @type {string} */
          wa(this, i, r), !i || !super._shouldPropertyChange(i, r, a))
            return !1;
        }
        if (this.__dataHasPaths = !0, this._setPendingProperty(
          /**@type{string}*/
          i,
          r,
          o
        ))
          return Od(
            this,
            /**@type{string}*/
            i,
            r
          ), !0;
      } else {
        if (this.__dataHasAccessor && this.__dataHasAccessor[i])
          return this._setPendingProperty(
            /**@type{string}*/
            i,
            r,
            o
          );
        this[i] = r;
      }
      return !1;
    }
    /**
     * Applies a value to a non-Polymer element/node's property.
     *
     * The implementation makes a best-effort at binding interop:
     * Some native element properties have side-effects when
     * re-setting the same value (e.g. setting `<input>.value` resets the
     * cursor position), so we do a dirty-check before setting the value.
     * However, for better interop with non-Polymer custom elements that
     * accept objects, we explicitly re-set object changes coming from the
     * Polymer world (which may include deep object changes without the
     * top reference changing), erring on the side of providing more
     * information.
     *
     * Users may override this method to provide alternate approaches.
     *
     * @override
     * @param {!Node} node The node to set a property on
     * @param {string} prop The property to set
     * @param {*} value The value to set
     * @return {void}
     * @protected
     */
    _setUnmanagedPropertyToNode(i, r, o) {
      (o !== i[r] || typeof o == "object") && (r === "className" && (i = /** @type {!Node} */
      ui(i)), i[r] = o);
    }
    /**
     * Overrides the `PropertiesChanged` implementation to introduce special
     * dirty check logic depending on the property & value being set:
     *
     * 1. Any value set to a path (e.g. 'obj.prop': 42 or 'obj.prop': {...})
     *    Stored in `__dataTemp`, dirty checked against `__dataTemp`
     * 2. Object set to simple property (e.g. 'prop': {...})
     *    Stored in `__dataTemp` and `__data`, dirty checked against
     *    `__dataTemp` by default implementation of `_shouldPropertyChange`
     * 3. Primitive value set to simple property (e.g. 'prop': 42)
     *    Stored in `__data`, dirty checked against `__data`
     *
     * The dirty-check is important to prevent cycles due to two-way
     * notification, but paths and objects are only dirty checked against any
     * previous value set during this turn via a "temporary cache" that is
     * cleared when the last `_propertiesChanged` exits. This is so:
     * a. any cached array paths (e.g. 'array.3.prop') may be invalidated
     *    due to array mutations like shift/unshift/splice; this is fine
     *    since path changes are dirty-checked at user entry points like `set`
     * b. dirty-checking for objects only lasts one turn to allow the user
     *    to mutate the object in-place and re-set it with the same identity
     *    and have all sub-properties re-propagated in a subsequent turn.
     *
     * The temp cache is not necessarily sufficient to prevent invalid array
     * paths, since a splice can happen during the same turn (with pathological
     * user code); we could introduce a "fixup" for temporarily cached array
     * paths if needed: https://github.com/Polymer/polymer/issues/4227
     *
     * @override
     * @param {string} property Name of the property
     * @param {*} value Value to set
     * @param {boolean=} shouldNotify True if property should fire notification
     *   event (applies only for `notify: true` properties)
     * @return {boolean} Returns true if the property changed
     */
    _setPendingProperty(i, r, o) {
      let l = this.__dataHasPaths && No(i), a = l ? this.__dataTemp : this.__data;
      return this._shouldPropertyChange(i, r, a[i]) ? (this.__dataPending || (this.__dataPending = {}, this.__dataOld = {}), i in this.__dataOld || (this.__dataOld[i] = this.__data[i]), l ? this.__dataTemp[i] = r : this.__data[i] = r, this.__dataPending[i] = r, (l || this[Re.NOTIFY] && this[Re.NOTIFY][i]) && (this.__dataToNotify = this.__dataToNotify || {}, this.__dataToNotify[i] = o), !0) : !1;
    }
    /**
     * Overrides base implementation to ensure all accessors set `shouldNotify`
     * to true, for per-property notification tracking.
     *
     * @override
     * @param {string} property Name of the property
     * @param {*} value Value to set
     * @return {void}
     */
    _setProperty(i, r) {
      this._setPendingProperty(i, r, !0) && this._invalidateProperties();
    }
    /**
     * Overrides `PropertyAccessor`'s default async queuing of
     * `_propertiesChanged`: if `__dataReady` is false (has not yet been
     * manually flushed), the function no-ops; otherwise flushes
     * `_propertiesChanged` synchronously.
     *
     * @override
     * @return {void}
     */
    _invalidateProperties() {
      this.__dataReady && this._flushProperties();
    }
    /**
     * Enqueues the given client on a list of pending clients, whose
     * pending property changes can later be flushed via a call to
     * `_flushClients`.
     *
     * @override
     * @param {Object} client PropertyEffects client to enqueue
     * @return {void}
     * @protected
     */
    _enqueueClient(i) {
      this.__dataPendingClients = this.__dataPendingClients || [], i !== this && this.__dataPendingClients.push(i);
    }
    /**
     * Flushes any clients previously enqueued via `_enqueueClient`, causing
     * their `_flushProperties` method to run.
     *
     * @override
     * @return {void}
     * @protected
     */
    _flushClients() {
      this.__dataClientsReady ? this.__enableOrFlushClients() : (this.__dataClientsReady = !0, this._readyClients(), this.__dataReady = !0);
    }
    // NOTE: We ensure clients either enable or flush as appropriate. This
    // handles two corner cases:
    // (1) clients flush properly when connected/enabled before the host
    // enables; e.g.
    //   (a) Templatize stamps with no properties and does not flush and
    //   (b) the instance is inserted into dom and
    //   (c) then the instance flushes.
    // (2) clients enable properly when not connected/enabled when the host
    // flushes; e.g.
    //   (a) a template is runtime stamped and not yet connected/enabled
    //   (b) a host sets a property, causing stamped dom to flush
    //   (c) the stamped dom enables.
    __enableOrFlushClients() {
      let i = this.__dataPendingClients;
      if (i) {
        this.__dataPendingClients = null;
        for (let r = 0; r < i.length; r++) {
          let o = i[r];
          o.__dataEnabled ? o.__dataPending && o._flushProperties() : o._enableProperties();
        }
      }
    }
    /**
     * Perform any initial setup on client dom. Called before the first
     * `_flushProperties` call on client dom and before any element
     * observers are called.
     *
     * @override
     * @return {void}
     * @protected
     */
    _readyClients() {
      this.__enableOrFlushClients();
    }
    /**
     * Sets a bag of property changes to this instance, and
     * synchronously processes all effects of the properties as a batch.
     *
     * Property names must be simple properties, not paths.  Batched
     * path propagation is not supported.
     *
     * @override
     * @param {Object} props Bag of one or more key-value pairs whose key is
     *   a property and value is the new value to set for that property.
     * @param {boolean=} setReadOnly When true, any private values set in
     *   `props` will be set. By default, `setProperties` will not set
     *   `readOnly: true` root properties.
     * @return {void}
     * @public
     */
    setProperties(i, r) {
      for (let o in i)
        (r || !this[Re.READ_ONLY] || !this[Re.READ_ONLY][o]) && this._setPendingPropertyOrPath(o, i[o], !0);
      this._invalidateProperties();
    }
    /**
     * Overrides `PropertyAccessors` so that property accessor
     * side effects are not enabled until after client dom is fully ready.
     * Also calls `_flushClients` callback to ensure client dom is enabled
     * that was not enabled as a result of flushing properties.
     *
     * @override
     * @return {void}
     */
    ready() {
      this._flushProperties(), this.__dataClientsReady || this._flushClients(), this.__dataPending && this._flushProperties();
    }
    /**
     * Implements `PropertyAccessors`'s properties changed callback.
     *
     * Runs each class of effects for the batch of changed properties in
     * a specific order (compute, propagate, reflect, observe, notify).
     *
     * @override
     * @param {!Object} currentProps Bag of all current accessor values
     * @param {?Object} changedProps Bag of properties changed since the last
     *   call to `_propertiesChanged`
     * @param {?Object} oldProps Bag of previous values for each property
     *   in `changedProps`
     * @return {void}
     */
    _propertiesChanged(i, r, o) {
      let l = this.__dataHasPaths;
      this.__dataHasPaths = !1;
      let a;
      Ad(this, r, o, l), a = this.__dataToNotify, this.__dataToNotify = null, this._propagatePropertyChanges(r, o, l), this._flushClients(), lr(this, this[Re.REFLECT], r, o, l), lr(this, this[Re.OBSERVE], r, o, l), a && Td(this, a, r, o, l), this.__dataCounter == 1 && (this.__dataTemp = {});
    }
    /**
     * Called to propagate any property changes to stamped template nodes
     * managed by this element.
     *
     * @override
     * @param {Object} changedProps Bag of changed properties
     * @param {Object} oldProps Bag of previous values for changed properties
     * @param {boolean} hasPaths True with `props` contains one or more paths
     * @return {void}
     * @protected
     */
    _propagatePropertyChanges(i, r, o) {
      this[Re.PROPAGATE] && lr(this, this[Re.PROPAGATE], i, r, o), this.__templateInfo && this._runEffectsForTemplate(this.__templateInfo, i, r, o);
    }
    _runEffectsForTemplate(i, r, o, l) {
      const a = (c, d) => {
        lr(
          this,
          i.propertyEffects,
          c,
          o,
          d,
          i.nodeList
        );
        for (let f = i.firstChild; f; f = f.nextSibling)
          this._runEffectsForTemplate(f, c, o, d);
      };
      i.runEffects ? i.runEffects(a, r, l) : a(r, l);
    }
    /**
     * Aliases one data path as another, such that path notifications from one
     * are routed to the other.
     *
     * @override
     * @param {string | !Array<string|number>} to Target path to link.
     * @param {string | !Array<string|number>} from Source path to link.
     * @return {void}
     * @public
     */
    linkPaths(i, r) {
      i = ar(i), r = ar(r), this.__dataLinkedPaths = this.__dataLinkedPaths || {}, this.__dataLinkedPaths[i] = r;
    }
    /**
     * Removes a data path alias previously established with `_linkPaths`.
     *
     * Note, the path to unlink should be the target (`to`) used when
     * linking the paths.
     *
     * @override
     * @param {string | !Array<string|number>} path Target path to unlink.
     * @return {void}
     * @public
     */
    unlinkPaths(i) {
      i = ar(i), this.__dataLinkedPaths && delete this.__dataLinkedPaths[i];
    }
    /**
     * Notify that an array has changed.
     *
     * Example:
     *
     *     this.items = [ {name: 'Jim'}, {name: 'Todd'}, {name: 'Bill'} ];
     *     ...
     *     this.items.splice(1, 1, {name: 'Sam'});
     *     this.items.push({name: 'Bob'});
     *     this.notifySplices('items', [
     *       { index: 1, removed: [{name: 'Todd'}], addedCount: 1,
     *         object: this.items, type: 'splice' },
     *       { index: 3, removed: [], addedCount: 1,
     *         object: this.items, type: 'splice'}
     *     ]);
     *
     * @param {string} path Path that should be notified.
     * @param {Array} splices Array of splice records indicating ordered
     *   changes that occurred to the array. Each record should have the
     *   following fields:
     *    * index: index at which the change occurred
     *    * removed: array of items that were removed from this index
     *    * addedCount: number of new items added at this index
     *    * object: a reference to the array in question
     *    * type: the string literal 'splice'
     *
     *   Note that splice records _must_ be normalized such that they are
     *   reported in index order (raw results from `Object.observe` are not
     *   ordered and must be normalized/merged before notifying).
     *
     * @override
     * @return {void}
     * @public
     */
    notifySplices(i, r) {
      let o = { path: "" }, l = (
        /** @type {Array} */
        st(this, i, o)
      );
      Dl(this, l, o.path, r);
    }
    /**
     * Convenience method for reading a value from a path.
     *
     * Note, if any part in the path is undefined, this method returns
     * `undefined` (this method does not throw when dereferencing undefined
     * paths).
     *
     * @override
     * @param {(string|!Array<(string|number)>)} path Path to the value
     *   to read.  The path may be specified as a string (e.g. `foo.bar.baz`)
     *   or an array of path parts (e.g. `['foo.bar', 'baz']`).  Note that
     *   bracketed expressions are not supported; string-based path parts
     *   *must* be separated by dots.  Note that when dereferencing array
     *   indices, the index may be used as a dotted part directly
     *   (e.g. `users.12.name` or `['users', 12, 'name']`).
     * @param {Object=} root Root object from which the path is evaluated.
     * @return {*} Value at the path, or `undefined` if any part of the path
     *   is undefined.
     * @public
     */
    get(i, r) {
      return st(r || this, i);
    }
    /**
     * Convenience method for setting a value to a path and notifying any
     * elements bound to the same path.
     *
     * Note, if any part in the path except for the last is undefined,
     * this method does nothing (this method does not throw when
     * dereferencing undefined paths).
     *
     * @override
     * @param {(string|!Array<(string|number)>)} path Path to the value
     *   to write.  The path may be specified as a string (e.g. `'foo.bar.baz'`)
     *   or an array of path parts (e.g. `['foo.bar', 'baz']`).  Note that
     *   bracketed expressions are not supported; string-based path parts
     *   *must* be separated by dots.  Note that when dereferencing array
     *   indices, the index may be used as a dotted part directly
     *   (e.g. `'users.12.name'` or `['users', 12, 'name']`).
     * @param {*} value Value to set at the specified path.
     * @param {Object=} root Root object from which the path is evaluated.
     *   When specified, no notification will occur.
     * @return {void}
     * @public
     */
    set(i, r, o) {
      o ? wa(o, i, r) : (!this[Re.READ_ONLY] || !this[Re.READ_ONLY][
        /** @type {string} */
        i
      ]) && this._setPendingPropertyOrPath(i, r, !0) && this._invalidateProperties();
    }
    /**
     * Adds items onto the end of the array at the path specified.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.push`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @param {...*} items Items to push onto array
     * @return {number} New length of the array.
     * @public
     */
    push(i, ...r) {
      let o = { path: "" }, l = (
        /** @type {Array}*/
        st(this, i, o)
      ), a = l.length, c = l.push(...r);
      return r.length && rr(this, l, o.path, a, r.length, []), c;
    }
    /**
     * Removes an item from the end of array at the path specified.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.pop`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @return {*} Item that was removed.
     * @public
     */
    pop(i) {
      let r = { path: "" }, o = (
        /** @type {Array} */
        st(this, i, r)
      ), l = !!o.length, a = o.pop();
      return l && rr(this, o, r.path, o.length, 0, [a]), a;
    }
    /**
     * Starting from the start index specified, removes 0 or more items
     * from the array and inserts 0 or more new items in their place.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.splice`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @param {number} start Index from which to start removing/inserting.
     * @param {number=} deleteCount Number of items to remove.
     * @param {...*} items Items to insert into array.
     * @return {!Array} Array of removed items.
     * @public
     */
    splice(i, r, o, ...l) {
      let a = { path: "" }, c = (
        /** @type {Array} */
        st(this, i, a)
      );
      r < 0 ? r = c.length - Math.floor(-r) : r && (r = Math.floor(r));
      let d;
      return arguments.length === 2 ? d = c.splice(r) : d = c.splice(r, o, ...l), (l.length || d.length) && rr(this, c, a.path, r, l.length, d), d;
    }
    /**
     * Removes an item from the beginning of array at the path specified.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.pop`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @return {*} Item that was removed.
     * @public
     */
    shift(i) {
      let r = { path: "" }, o = (
        /** @type {Array} */
        st(this, i, r)
      ), l = !!o.length, a = o.shift();
      return l && rr(this, o, r.path, 0, 0, [a]), a;
    }
    /**
     * Adds items onto the beginning of the array at the path specified.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.push`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @param {...*} items Items to insert info array
     * @return {number} New length of the array.
     * @public
     */
    unshift(i, ...r) {
      let o = { path: "" }, l = (
        /** @type {Array} */
        st(this, i, o)
      ), a = l.unshift(...r);
      return r.length && rr(this, l, o.path, 0, r.length, []), a;
    }
    /**
     * Notify that a path has changed.
     *
     * Example:
     *
     *     this.item.user.name = 'Bob';
     *     this.notifyPath('item.user.name');
     *
     * @override
     * @param {string} path Path that should be notified.
     * @param {*=} value Value at the path (optional).
     * @return {void}
     * @public
     */
    notifyPath(i, r) {
      let o;
      if (arguments.length == 1) {
        let l = { path: "" };
        r = st(this, i, l), o = l.path;
      } else Array.isArray(i) ? o = ar(i) : o = /** @type{string} */
      i;
      this._setPendingPropertyOrPath(o, r, !0, !0) && this._invalidateProperties();
    }
    /**
     * Equivalent to static `createReadOnlyProperty` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property name
     * @param {boolean=} protectedSetter Creates a custom protected setter
     *   when `true`.
     * @return {void}
     * @protected
     */
    _createReadOnlyProperty(i, r) {
      this._addPropertyEffect(i, Re.READ_ONLY), r && (this["_set" + tu(i)] = /** @this {PropertyEffects} */
      function(o) {
        this._setProperty(i, o);
      });
    }
    /**
     * Equivalent to static `createPropertyObserver` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property name
     * @param {string|function(*,*)} method Function or name of observer method
     *     to call
     * @param {boolean=} dynamicFn Whether the method name should be included as
     *   a dependency to the effect.
     * @return {void}
     * @protected
     */
    _createPropertyObserver(i, r, o) {
      let l = { property: i, method: r, dynamicFn: !!o };
      this._addPropertyEffect(i, Re.OBSERVE, {
        fn: Ta,
        info: l,
        trigger: { name: i }
      }), o && this._addPropertyEffect(
        /** @type {string} */
        r,
        Re.OBSERVE,
        {
          fn: Ta,
          info: l,
          trigger: { name: r }
        }
      );
    }
    /**
     * Equivalent to static `createMethodObserver` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} expression Method expression
     * @param {boolean|Object=} dynamicFn Boolean or object map indicating
     *   whether method names should be included as a dependency to the effect.
     * @return {void}
     * @protected
     */
    _createMethodObserver(i, r) {
      let o = yo(i);
      if (!o)
        throw new Error("Malformed observer expression '" + i + "'");
      Pa(this, o, Re.OBSERVE, Ho, null, r);
    }
    /**
     * Equivalent to static `createNotifyingProperty` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property name
     * @return {void}
     * @protected
     */
    _createNotifyingProperty(i) {
      this._addPropertyEffect(i, Re.NOTIFY, {
        fn: Pd,
        info: {
          eventName: Dn(i) + "-changed",
          property: i
        }
      });
    }
    /**
     * Equivalent to static `createReflectedProperty` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property name
     * @return {void}
     * @protected
     * @suppress {missingProperties} go/missingfnprops
     */
    _createReflectedProperty(i) {
      let r = this.constructor.attributeNameForProperty(i);
      r[0] === "-" || this._addPropertyEffect(i, Re.REFLECT, {
        fn: Rd,
        info: {
          attrName: r
        }
      });
    }
    /**
     * Equivalent to static `createComputedProperty` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Name of computed property to set
     * @param {string} expression Method expression
     * @param {boolean|Object=} dynamicFn Boolean or object map indicating
     *   whether method names should be included as a dependency to the effect.
     * @return {void}
     * @protected
     */
    _createComputedProperty(i, r, o) {
      let l = yo(r);
      if (!l)
        throw new Error("Malformed computed expression '" + r + "'");
      const a = Pa(this, l, Re.COMPUTE, Al, i, o);
      _o(this, Sl)[i] = a;
    }
    /**
     * Gather the argument values for a method specified in the provided array
     * of argument metadata.
     *
     * The `path` and `value` arguments are used to fill in wildcard descriptor
     * when the method is being called as a result of a path notification.
     *
     * @param {!Array<!MethodArg>} args Array of argument metadata
     * @param {string} path Property/path name that triggered the method effect
     * @param {Object} props Bag of current property changes
     * @return {!Array<*>} Array of argument values
     * @private
     */
    _marshalArgs(i, r, o) {
      const l = this.__data, a = [];
      for (let c = 0, d = i.length; c < d; c++) {
        let { name: f, structured: p, wildcard: b, value: x, literal: T } = i[c];
        if (!T)
          if (b) {
            const I = wn(f, r), z = ka(l, o, I ? r : f);
            x = {
              path: I ? r : f,
              value: z,
              base: I ? st(l, f) : z
            };
          } else
            x = p ? ka(l, o, f) : l[f];
        if (Mo && !this._overrideLegacyUndefined && x === void 0 && i.length > 1)
          return vr;
        a[c] = x;
      }
      return a;
    }
    // -- static class methods ------------
    /**
     * Ensures an accessor exists for the specified property, and adds
     * to a list of "property effects" that will run when the accessor for
     * the specified property is set.  Effects are grouped by "type", which
     * roughly corresponds to a phase in effect processing.  The effect
     * metadata should be in the following form:
     *
     *     {
     *       fn: effectFunction, // Reference to function to call to perform effect
     *       info: { ... }       // Effect metadata passed to function
     *       trigger: {          // Optional triggering metadata; if not provided
     *         name: string      // the property is treated as a wildcard
     *         structured: boolean
     *         wildcard: boolean
     *       }
     *     }
     *
     * Effects are called from `_propertiesChanged` in the following order by
     * type:
     *
     * 1. COMPUTE
     * 2. PROPAGATE
     * 3. REFLECT
     * 4. OBSERVE
     * 5. NOTIFY
     *
     * Effect functions are called with the following signature:
     *
     *     effectFunction(inst, path, props, oldProps, info, hasPaths)
     *
     * @param {string} property Property that should trigger the effect
     * @param {string} type Effect type, from this.PROPERTY_EFFECT_TYPES
     * @param {Object=} effect Effect metadata object
     * @return {void}
     * @protected
     * @nocollapse
     */
    static addPropertyEffect(i, r, o) {
      this.prototype._addPropertyEffect(i, r, o);
    }
    /**
     * Creates a single-property observer for the given property.
     *
     * @param {string} property Property name
     * @param {string|function(*,*)} method Function or name of observer method to call
     * @param {boolean=} dynamicFn Whether the method name should be included as
     *   a dependency to the effect.
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createPropertyObserver(i, r, o) {
      this.prototype._createPropertyObserver(i, r, o);
    }
    /**
     * Creates a multi-property "method observer" based on the provided
     * expression, which should be a string in the form of a normal JavaScript
     * function signature: `'methodName(arg1, [..., argn])'`.  Each argument
     * should correspond to a property or path in the context of this
     * prototype (or instance), or may be a literal string or number.
     *
     * @param {string} expression Method expression
     * @param {boolean|Object=} dynamicFn Boolean or object map indicating
     * @return {void}
     *   whether method names should be included as a dependency to the effect.
     * @protected
     * @nocollapse
     */
    static createMethodObserver(i, r) {
      this.prototype._createMethodObserver(i, r);
    }
    /**
     * Causes the setter for the given property to dispatch `<property>-changed`
     * events to notify of changes to the property.
     *
     * @param {string} property Property name
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createNotifyingProperty(i) {
      this.prototype._createNotifyingProperty(i);
    }
    /**
     * Creates a read-only accessor for the given property.
     *
     * To set the property, use the protected `_setProperty` API.
     * To create a custom protected setter (e.g. `_setMyProp()` for
     * property `myProp`), pass `true` for `protectedSetter`.
     *
     * Note, if the property will have other property effects, this method
     * should be called first, before adding other effects.
     *
     * @param {string} property Property name
     * @param {boolean=} protectedSetter Creates a custom protected setter
     *   when `true`.
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createReadOnlyProperty(i, r) {
      this.prototype._createReadOnlyProperty(i, r);
    }
    /**
     * Causes the setter for the given property to reflect the property value
     * to a (dash-cased) attribute of the same name.
     *
     * @param {string} property Property name
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createReflectedProperty(i) {
      this.prototype._createReflectedProperty(i);
    }
    /**
     * Creates a computed property whose value is set to the result of the
     * method described by the given `expression` each time one or more
     * arguments to the method changes.  The expression should be a string
     * in the form of a normal JavaScript function signature:
     * `'methodName(arg1, [..., argn])'`
     *
     * @param {string} property Name of computed property to set
     * @param {string} expression Method expression
     * @param {boolean|Object=} dynamicFn Boolean or object map indicating whether
     *   method names should be included as a dependency to the effect.
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createComputedProperty(i, r, o) {
      this.prototype._createComputedProperty(i, r, o);
    }
    /**
     * Parses the provided template to ensure binding effects are created
     * for them, and then ensures property accessors are created for any
     * dependent properties in the template.  Binding effects for bound
     * templates are stored in a linked list on the instance so that
     * templates can be efficiently stamped and unstamped.
     *
     * @param {!HTMLTemplateElement} template Template containing binding
     *   bindings
     * @return {!TemplateInfo} Template metadata object
     * @protected
     * @nocollapse
     */
    static bindTemplate(i) {
      return this.prototype._bindTemplate(i);
    }
    // -- binding ----------------------------------------------
    /*
     * Overview of binding flow:
     *
     * During finalization (`instanceBinding==false`, `wasPreBound==false`):
     *  `_bindTemplate(t, false)` called directly during finalization - parses
     *  the template (for the first time), and then assigns that _prototypical_
     *  template info to `__preboundTemplateInfo` _on the prototype_; note in
     *  this case `wasPreBound` is false; this is the first time we're binding
     *  it, thus we create accessors.
     *
     * During first stamping (`instanceBinding==true`, `wasPreBound==true`):
     *   `_stampTemplate` calls `_bindTemplate(t, true)`: the `templateInfo`
     *   returned matches the prebound one, and so this is `wasPreBound == true`
     *   state; thus we _skip_ creating accessors, but _do_ create an instance
     *   of the template info to serve as the start of our linked list (needs to
     *   be an instance, not the prototypical one, so that we can add `nodeList`
     *   to it to contain the `nodeInfo`-ordered list of instance nodes for
     *   bindings, and so we can chain runtime-stamped template infos off of
     *   it). At this point, the call to `_stampTemplate` calls
     *   `applyTemplateInfo` for each nested `<template>` found during parsing
     *   to hand prototypical `_templateInfo` to them; we also pass the _parent_
     *   `templateInfo` to the `<template>` so that we have the instance-time
     *   parent to link the `templateInfo` under in the case it was
     *   runtime-stamped.
     *
     * During subsequent runtime stamping (`instanceBinding==true`,
     *   `wasPreBound==false`): `_stampTemplate` calls `_bindTemplate(t, true)`
     *   - here `templateInfo` is guaranteed to _not_ match the prebound one,
     *   because it was either a different template altogether, or even if it
     *   was the same template, the step above created a instance of the info;
     *   in this case `wasPreBound == false`, so we _do_ create accessors, _and_
     *   link a instance into the linked list.
     */
    /**
     * Equivalent to static `bindTemplate` API but can be called on an instance
     * to add effects at runtime.  See that method for full API docs.
     *
     * This method may be called on the prototype (for prototypical template
     * binding, to avoid creating accessors every instance) once per prototype,
     * and will be called with `runtimeBinding: true` by `_stampTemplate` to
     * create and link an instance of the template metadata associated with a
     * particular stamping.
     *
     * @override
     * @param {!HTMLTemplateElement} template Template containing binding
     * bindings
     * @param {boolean=} instanceBinding When false (default), performs
     * "prototypical" binding of the template and overwrites any previously
     * bound template for the class. When true (as passed from
     * `_stampTemplate`), the template info is instanced and linked into the
     * list of bound templates.
     * @return {!TemplateInfo} Template metadata object; for `runtimeBinding`,
     * this is an instance of the prototypical template info
     * @protected
     * @suppress {missingProperties} go/missingfnprops
     */
    _bindTemplate(i, r) {
      let o = this.constructor._parseTemplate(i), l = this.__preBoundTemplateInfo == o;
      if (!l)
        for (let a in o.propertyEffects)
          this._createPropertyAccessor(a);
      if (r)
        if (o = /** @type {!TemplateInfo} */
        Object.create(o), o.wasPreBound = l, !this.__templateInfo)
          this.__templateInfo = o;
        else {
          const a = i._parentTemplateInfo || this.__templateInfo, c = a.lastChild;
          o.parent = a, a.lastChild = o, o.previousSibling = c, c ? c.nextSibling = o : a.firstChild = o;
        }
      else
        this.__preBoundTemplateInfo = o;
      return o;
    }
    /**
     * Adds a property effect to the given template metadata, which is run
     * at the "propagate" stage of `_propertiesChanged` when the template
     * has been bound to the element via `_bindTemplate`.
     *
     * The `effect` object should match the format in `_addPropertyEffect`.
     *
     * @param {Object} templateInfo Template metadata to add effect to
     * @param {string} prop Property that should trigger the effect
     * @param {Object=} effect Effect metadata object
     * @return {void}
     * @protected
     * @nocollapse
     */
    static _addTemplatePropertyEffect(i, r, o) {
      let l = i.hostProps = i.hostProps || {};
      l[r] = !0;
      let a = i.propertyEffects = i.propertyEffects || {};
      (a[r] = a[r] || []).push(o);
    }
    /**
     * Stamps the provided template and performs instance-time setup for
     * Polymer template features, including data bindings, declarative event
     * listeners, and the `this.$` map of `id`'s to nodes.  A document fragment
     * is returned containing the stamped DOM, ready for insertion into the
     * DOM.
     *
     * This method may be called more than once; however note that due to
     * `shadycss` polyfill limitations, only styles from templates prepared
     * using `ShadyCSS.prepareTemplate` will be correctly polyfilled (scoped
     * to the shadow root and support CSS custom properties), and note that
     * `ShadyCSS.prepareTemplate` may only be called once per element. As such,
     * any styles required by in runtime-stamped templates must be included
     * in the main element template.
     *
     * @param {!HTMLTemplateElement} template Template to stamp
     * @param {TemplateInfo=} templateInfo Optional bound template info associated
     *   with the template to be stamped; if omitted the template will be
     *   automatically bound.
     * @return {!StampedTemplate} Cloned template content
     * @override
     * @protected
     */
    _stampTemplate(i, r) {
      r = r || /** @type {!TemplateInfo} */
      this._bindTemplate(i, !0), nr.push(this);
      let o = super._stampTemplate(i, r);
      if (nr.pop(), r.nodeList = o.nodeList, !r.wasPreBound) {
        let l = r.childNodes = [];
        for (let a = o.firstChild; a; a = a.nextSibling)
          l.push(a);
      }
      return o.templateInfo = r, Bd(this, r), this.__dataClientsReady && (this._runEffectsForTemplate(r, this.__data, null, !1), this._flushClients()), o;
    }
    /**
     * Removes and unbinds the nodes previously contained in the provided
     * DocumentFragment returned from `_stampTemplate`.
     *
     * @override
     * @param {!StampedTemplate} dom DocumentFragment previously returned
     *   from `_stampTemplate` associated with the nodes to be removed
     * @return {void}
     * @protected
     */
    _removeBoundDom(i) {
      const r = i.templateInfo, { previousSibling: o, nextSibling: l, parent: a } = r;
      o ? o.nextSibling = l : a && (a.firstChild = l), l ? l.previousSibling = o : a && (a.lastChild = o), r.nextSibling = r.previousSibling = null;
      let c = r.childNodes;
      for (let d = 0; d < c.length; d++) {
        let f = c[d];
        ui(ui(f).parentNode).removeChild(f);
      }
    }
    /**
     * Overrides default `TemplateStamp` implementation to add support for
     * parsing bindings from `TextNode`'s' `textContent`.  A `bindings`
     * array is added to `nodeInfo` and populated with binding metadata
     * with information capturing the binding target, and a `parts` array
     * with one or more metadata objects capturing the source(s) of the
     * binding.
     *
     * @param {Node} node Node to parse
     * @param {TemplateInfo} templateInfo Template metadata for current template
     * @param {NodeInfo} nodeInfo Node metadata for current template node
     * @return {boolean} `true` if the visited node added node-specific
     *   metadata to `nodeInfo`
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _parseTemplateNode(i, r, o) {
      let l = n._parseTemplateNode.call(
        this,
        i,
        r,
        o
      );
      if (i.nodeType === Node.TEXT_NODE) {
        let a = this._parseBindings(i.textContent, r);
        a && (i.textContent = Aa(a) || " ", vo(this, r, o, "text", "textContent", a), l = !0);
      }
      return l;
    }
    /**
     * Overrides default `TemplateStamp` implementation to add support for
     * parsing bindings from attributes.  A `bindings`
     * array is added to `nodeInfo` and populated with binding metadata
     * with information capturing the binding target, and a `parts` array
     * with one or more metadata objects capturing the source(s) of the
     * binding.
     *
     * @param {Element} node Node to parse
     * @param {TemplateInfo} templateInfo Template metadata for current template
     * @param {NodeInfo} nodeInfo Node metadata for current template node
     * @param {string} name Attribute name
     * @param {string} value Attribute value
     * @return {boolean} `true` if the visited node added node-specific
     *   metadata to `nodeInfo`
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _parseTemplateNodeAttribute(i, r, o, l, a) {
      let c = this._parseBindings(a, r);
      if (c) {
        let d = l, f = "property";
        xd.test(l) ? f = "attribute" : l[l.length - 1] == "$" && (l = l.slice(0, -1), f = "attribute");
        let p = Aa(c);
        return p && f == "attribute" && (l == "class" && i.hasAttribute("class") && (p += " " + i.getAttribute(l)), i.setAttribute(l, p)), f == "attribute" && d == "disable-upgrade$" && i.setAttribute(l, ""), i.localName === "input" && d === "value" && i.setAttribute(d, ""), i.removeAttribute(d), f === "property" && (l = wl(l)), vo(this, r, o, f, l, c, p), !0;
      } else
        return n._parseTemplateNodeAttribute.call(
          this,
          i,
          r,
          o,
          l,
          a
        );
    }
    /**
     * Overrides default `TemplateStamp` implementation to add support for
     * binding the properties that a nested template depends on to the template
     * as `_host_<property>`.
     *
     * @param {Node} node Node to parse
     * @param {TemplateInfo} templateInfo Template metadata for current template
     * @param {NodeInfo} nodeInfo Node metadata for current template node
     * @return {boolean} `true` if the visited node added node-specific
     *   metadata to `nodeInfo`
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _parseTemplateNestedTemplate(i, r, o) {
      let l = n._parseTemplateNestedTemplate.call(
        this,
        i,
        r,
        o
      );
      const a = i.parentNode, c = o.templateInfo, d = a.localName === "dom-if", f = a.localName === "dom-repeat";
      ma && (d || f) && (a.removeChild(i), o = o.parentInfo, o.templateInfo = c, o.noted = !0, l = !1);
      let p = c.hostProps;
      if (qh && d)
        p && (r.hostProps = Object.assign(r.hostProps || {}, p), ma || (o.parentInfo.noted = !0));
      else {
        let b = "{";
        for (let x in p) {
          let T = [{ mode: b, source: x, dependencies: [x], hostProp: !0 }];
          vo(this, r, o, "property", "_host_" + x, T);
        }
      }
      return l;
    }
    /**
     * Called to parse text in a template (either attribute values or
     * textContent) into binding metadata.
     *
     * Any overrides of this method should return an array of binding part
     * metadata  representing one or more bindings found in the provided text
     * and any "literal" text in between.  Any non-literal parts will be passed
     * to `_evaluateBinding` when any dependencies change.  The only required
     * fields of each "part" in the returned array are as follows:
     *
     * - `dependencies` - Array containing trigger metadata for each property
     *   that should trigger the binding to update
     * - `literal` - String containing text if the part represents a literal;
     *   in this case no `dependencies` are needed
     *
     * Additional metadata for use by `_evaluateBinding` may be provided in
     * each part object as needed.
     *
     * The default implementation handles the following types of bindings
     * (one or more may be intermixed with literal strings):
     * - Property binding: `[[prop]]`
     * - Path binding: `[[object.prop]]`
     * - Negated property or path bindings: `[[!prop]]` or `[[!object.prop]]`
     * - Two-way property or path bindings (supports negation):
     *   `{{prop}}`, `{{object.prop}}`, `{{!prop}}` or `{{!object.prop}}`
     * - Inline computed method (supports negation):
     *   `[[compute(a, 'literal', b)]]`, `[[!compute(a, 'literal', b)]]`
     *
     * The default implementation uses a regular expression for best
     * performance. However, the regular expression uses a white-list of
     * allowed characters in a data-binding, which causes problems for
     * data-bindings that do use characters not in this white-list.
     *
     * Instead of updating the white-list with all allowed characters,
     * there is a StrictBindingParser (see lib/mixins/strict-binding-parser)
     * that uses a state machine instead. This state machine is able to handle
     * all characters. However, it is slightly less performant, therefore we
     * extracted it into a separate optional mixin.
     *
     * @param {string} text Text to parse from attribute or textContent
     * @param {Object} templateInfo Current template metadata
     * @return {Array<!BindingPart>} Array of binding part metadata
     * @protected
     * @nocollapse
     */
    static _parseBindings(i, r) {
      let o = [], l = 0, a;
      for (; (a = Ra.exec(i)) !== null; ) {
        a.index > l && o.push({ literal: i.slice(l, a.index) });
        let c = a[1][0], d = !!a[2], f = a[3].trim(), p = !1, b = "", x = -1;
        c == "{" && (x = f.indexOf("::")) > 0 && (b = f.substring(x + 2), f = f.substring(0, x), p = !0);
        let T = yo(f), I = [];
        if (T) {
          let { args: z, methodName: W } = T;
          for (let oe = 0; oe < z.length; oe++) {
            let ae = z[oe];
            ae.literal || I.push(ae);
          }
          let j = r.dynamicFns;
          (j && j[W] || T.static) && (I.push(W), T.dynamicFn = !0);
        } else
          I.push(f);
        o.push({
          source: f,
          mode: c,
          negate: d,
          customEvent: p,
          signature: T,
          dependencies: I,
          event: b
        }), l = Ra.lastIndex;
      }
      if (l && l < i.length) {
        let c = i.substring(l);
        c && o.push({
          literal: c
        });
      }
      return o.length ? o : null;
    }
    /**
     * Called to evaluate a previously parsed binding part based on a set of
     * one or more changed dependencies.
     *
     * @param {!Polymer_PropertyEffects} inst Element that should be used as
     *     scope for binding dependencies
     * @param {BindingPart} part Binding part metadata
     * @param {string} path Property/path that triggered this effect
     * @param {Object} props Bag of current property changes
     * @param {Object} oldProps Bag of previous values for changed properties
     * @param {boolean} hasPaths True with `props` contains one or more paths
     * @return {*} Value the binding part evaluated to
     * @protected
     * @nocollapse
     */
    static _evaluateBinding(i, r, o, l, a, c) {
      let d;
      return r.signature ? d = Ho(i, o, l, a, r.signature) : o != r.source ? d = st(i, r.source) : c && No(o) ? d = st(i, o) : d = i.__data[o], r.negate && (d = !d), d;
    }
  }
  return e;
}), nr = [];
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
function ru(s) {
  const n = {};
  for (let e in s) {
    const t = s[e];
    n[e] = typeof t == "function" ? { type: t } : t;
  }
  return n;
}
const nu = Je((s) => {
  const n = Tl(s);
  function e(r) {
    const o = Object.getPrototypeOf(r);
    return o.prototype instanceof i ? (
      /** @type {!PropertiesMixinConstructor} */
      o
    ) : null;
  }
  function t(r) {
    if (!r.hasOwnProperty(JSCompiler_renameProperty("__ownProperties", r))) {
      let o = null;
      if (r.hasOwnProperty(JSCompiler_renameProperty("properties", r))) {
        const l = r.properties;
        l && (o = ru(l));
      }
      r.__ownProperties = o;
    }
    return r.__ownProperties;
  }
  class i extends n {
    /**
     * Implements standard custom elements getter to observes the attributes
     * listed in `properties`.
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static get observedAttributes() {
      if (!this.hasOwnProperty(JSCompiler_renameProperty("__observedAttributes", this))) {
        this.prototype;
        const o = this._properties;
        this.__observedAttributes = o ? Object.keys(o).map((l) => this.prototype._addPropertyToAttributeMap(l)) : [];
      }
      return this.__observedAttributes;
    }
    /**
     * Finalizes an element definition, including ensuring any super classes
     * are also finalized. This includes ensuring property
     * accessors exist on the element prototype. This method calls
     * `_finalizeClass` to finalize each constructor in the prototype chain.
     * @return {void}
     * @nocollapse
     */
    static finalize() {
      if (!this.hasOwnProperty(JSCompiler_renameProperty("__finalized", this))) {
        const o = e(
          /** @type {!PropertiesMixinConstructor} */
          this
        );
        o && o.finalize(), this.__finalized = !0, this._finalizeClass();
      }
    }
    /**
     * Finalize an element class. This includes ensuring property
     * accessors exist on the element prototype. This method is called by
     * `finalize` and finalizes the class constructor.
     *
     * @protected
     * @nocollapse
     */
    static _finalizeClass() {
      const o = t(
        /** @type {!PropertiesMixinConstructor} */
        this
      );
      o && this.createProperties(o);
    }
    /**
     * Returns a memoized version of all properties, including those inherited
     * from super classes. Properties not in object format are converted to
     * at least {type}.
     *
     * @return {Object} Object containing properties for this class
     * @protected
     * @nocollapse
     */
    static get _properties() {
      if (!this.hasOwnProperty(
        JSCompiler_renameProperty("__properties", this)
      )) {
        const o = e(
          /** @type {!PropertiesMixinConstructor} */
          this
        );
        this.__properties = Object.assign(
          {},
          o && o._properties,
          t(
            /** @type {PropertiesMixinConstructor} */
            this
          )
        );
      }
      return this.__properties;
    }
    /**
     * Overrides `PropertiesChanged` method to return type specified in the
     * static `properties` object for the given property.
     * @param {string} name Name of property
     * @return {*} Type to which to deserialize attribute
     *
     * @protected
     * @nocollapse
     */
    static typeForProperty(o) {
      const l = this._properties[o];
      return l && l.type;
    }
    /**
     * Overrides `PropertiesChanged` method and adds a call to
     * `finalize` which lazily configures the element's property accessors.
     * @override
     * @return {void}
     */
    _initializeProperties() {
      this.constructor.finalize(), super._initializeProperties();
    }
    /**
     * Called when the element is added to a document.
     * Calls `_enableProperties` to turn on property system from
     * `PropertiesChanged`.
     * @suppress {missingProperties} Super may or may not implement the callback
     * @return {void}
     * @override
     */
    connectedCallback() {
      super.connectedCallback && super.connectedCallback(), this._enableProperties();
    }
    /**
     * Called when the element is removed from a document
     * @suppress {missingProperties} Super may or may not implement the callback
     * @return {void}
     * @override
     */
    disconnectedCallback() {
      super.disconnectedCallback && super.disconnectedCallback();
    }
  }
  return i;
});
/**
 * @fileoverview
 * @suppress {checkPrototypalTypes}
 * @license Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
 * This code may only be used under the BSD style license found at
 * http://polymer.github.io/LICENSE.txt The complete set of authors may be found
 * at http://polymer.github.io/AUTHORS.txt The complete set of contributors may
 * be found at http://polymer.github.io/CONTRIBUTORS.txt Code distributed by
 * Google as part of the polymer project is also subject to an additional IP
 * rights grant found at http://polymer.github.io/PATENTS.txt
 */
const ou = "3.5.1", Ia = window.ShadyCSS && window.ShadyCSS.cssBuild, su = Je((s) => {
  const n = nu(iu(s));
  function e(a) {
    if (!a.hasOwnProperty(
      JSCompiler_renameProperty("__propertyDefaults", a)
    )) {
      a.__propertyDefaults = null;
      let c = a._properties;
      for (let d in c) {
        let f = c[d];
        "value" in f && (a.__propertyDefaults = a.__propertyDefaults || {}, a.__propertyDefaults[d] = f);
      }
    }
    return a.__propertyDefaults;
  }
  function t(a) {
    return a.hasOwnProperty(
      JSCompiler_renameProperty("__ownObservers", a)
    ) || (a.__ownObservers = a.hasOwnProperty(
      JSCompiler_renameProperty("observers", a)
    ) ? (
      /** @type {PolymerElementConstructor} */
      a.observers
    ) : null), a.__ownObservers;
  }
  function i(a, c, d, f) {
    d.computed && (d.readOnly = !0), d.computed && (a._hasReadOnlyEffect(c) || a._createComputedProperty(c, d.computed, f)), d.readOnly && !a._hasReadOnlyEffect(c) ? a._createReadOnlyProperty(c, !d.computed) : d.readOnly === !1 && a._hasReadOnlyEffect(c), d.reflectToAttribute && !a._hasReflectEffect(c) ? a._createReflectedProperty(c) : d.reflectToAttribute === !1 && a._hasReflectEffect(c), d.notify && !a._hasNotifyEffect(c) ? a._createNotifyingProperty(c) : d.notify === !1 && a._hasNotifyEffect(c), d.observer && a._createPropertyObserver(c, d.observer, f[d.observer]), a._addPropertyToAttributeMap(c);
  }
  function r(a, c, d, f) {
    if (!Ia) {
      const p = c.content.querySelectorAll("style"), b = vl(c), x = ed(d), T = c.content.firstElementChild;
      for (let z = 0; z < x.length; z++) {
        let W = x[z];
        W.textContent = a._processStyleText(W.textContent, f), c.content.insertBefore(W, T);
      }
      let I = 0;
      for (let z = 0; z < b.length; z++) {
        let W = b[z], j = p[I];
        j !== W ? (W = W.cloneNode(!0), j.parentNode.insertBefore(W, j)) : I++, W.textContent = a._processStyleText(W.textContent, f);
      }
    }
    if (window.ShadyCSS && window.ShadyCSS.prepareTemplate(c, d), Kh && Ia && Bh) {
      const p = c.content.querySelectorAll("style");
      if (p) {
        let b = "";
        Array.from(p).forEach((x) => {
          b += x.textContent, x.parentNode.removeChild(x);
        }), a._styleSheet = new CSSStyleSheet(), a._styleSheet.replaceSync(b);
      }
    }
  }
  function o(a) {
    let c = null;
    if (a && (!Lo || Vh) && (c = /** @type {?HTMLTemplateElement} */
    mr.import(a, "template"), Lo && !c))
      throw new Error(`strictTemplatePolicy: expecting dom-module or null template for ${a}`);
    return c;
  }
  class l extends n {
    /**
     * Current Polymer version in Semver notation.
     * @type {string} Semver notation of the current version of Polymer.
     * @nocollapse
     */
    static get polymerElementVersion() {
      return ou;
    }
    /**
     * Override of PropertiesMixin _finalizeClass to create observers and
     * find the template.
     * @return {void}
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _finalizeClass() {
      n._finalizeClass.call(this);
      const c = t(this);
      c && this.createObservers(c, this._properties), this._prepareTemplate();
    }
    /** @nocollapse */
    static _prepareTemplate() {
      let c = (
        /** @type {PolymerElementConstructor} */
        this.template
      );
      c && (typeof c == "string" ? c = null : Uh || (c = c.cloneNode(!0))), this.prototype._template = c;
    }
    /**
     * Override of PropertiesChanged createProperties to create accessors
     * and property effects for all of the properties.
     * @param {!Object} props .
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createProperties(c) {
      for (let d in c)
        i(
          /** @type {?} */
          this.prototype,
          d,
          c[d],
          c
        );
    }
    /**
     * Creates observers for the given `observers` array.
     * Leverages `PropertyEffects` to create observers.
     * @param {Object} observers Array of observer descriptors for
     *   this class
     * @param {Object} dynamicFns Object containing keys for any properties
     *   that are functions and should trigger the effect when the function
     *   reference is changed
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createObservers(c, d) {
      const f = this.prototype;
      for (let p = 0; p < c.length; p++)
        f._createMethodObserver(c[p], d);
    }
    /**
     * Returns the template that will be stamped into this element's shadow root.
     *
     * If a `static get is()` getter is defined, the default implementation will
     * return the first `<template>` in a `dom-module` whose `id` matches this
     * element's `is` (note that a `_template` property on the class prototype
     * takes precedence over the `dom-module` template, to maintain legacy
     * element semantics; a subclass will subsequently fall back to its super
     * class template if neither a `prototype._template` or a `dom-module` for
     * the class's `is` was found).
     *
     * Users may override this getter to return an arbitrary template
     * (in which case the `is` getter is unnecessary). The template returned
     * must be an `HTMLTemplateElement`.
     *
     * Note that when subclassing, if the super class overrode the default
     * implementation and the subclass would like to provide an alternate
     * template via a `dom-module`, it should override this getter and
     * return `DomModule.import(this.is, 'template')`.
     *
     * If a subclass would like to modify the super class template, it should
     * clone it rather than modify it in place.  If the getter does expensive
     * work such as cloning/modifying a template, it should memoize the
     * template for maximum performance:
     *
     *   let memoizedTemplate;
     *   class MySubClass extends MySuperClass {
     *     static get template() {
     *       if (!memoizedTemplate) {
     *         memoizedTemplate = super.template.cloneNode(true);
     *         let subContent = document.createElement('div');
     *         subContent.textContent = 'This came from MySubClass';
     *         memoizedTemplate.content.appendChild(subContent);
     *       }
     *       return memoizedTemplate;
     *     }
     *   }
     *
     * @return {!HTMLTemplateElement|string} Template to be stamped
     * @nocollapse
     */
    static get template() {
      if (!this.hasOwnProperty(JSCompiler_renameProperty("_template", this))) {
        let c = this.prototype.hasOwnProperty(
          JSCompiler_renameProperty("_template", this.prototype)
        ) ? this.prototype._template : void 0;
        typeof c == "function" && (c = c()), this._template = // If user has put template on prototype (e.g. in legacy via registered
        // callback or info object), prefer that first. Note that `null` is
        // used as a sentinel to indicate "no template" and can be used to
        // override a super template, whereas `undefined` is used as a
        // sentinel to mean "fall-back to default template lookup" via
        // dom-module and/or super.template.
        c !== void 0 ? c : (
          // Look in dom-module associated with this element's is
          this.hasOwnProperty(JSCompiler_renameProperty("is", this)) && o(
            /** @type {PolymerElementConstructor}*/
            this.is
          ) || // Next look for superclass template (call the super impl this
          // way so that `this` points to the superclass)
          Object.getPrototypeOf(
            /** @type {PolymerElementConstructor}*/
            this.prototype
          ).constructor.template
        );
      }
      return this._template;
    }
    /**
     * Set the template.
     *
     * @param {!HTMLTemplateElement|string} value Template to set.
     * @nocollapse
     */
    static set template(c) {
      this._template = c;
    }
    /**
     * Path matching the url from which the element was imported.
     *
     * This path is used to resolve url's in template style cssText.
     * The `importPath` property is also set on element instances and can be
     * used to create bindings relative to the import path.
     *
     * For elements defined in ES modules, users should implement
     * `static get importMeta() { return import.meta; }`, and the default
     * implementation of `importPath` will  return `import.meta.url`'s path.
     * For elements defined in HTML imports, this getter will return the path
     * to the document containing a `dom-module` element matching this
     * element's static `is` property.
     *
     * Note, this path should contain a trailing `/`.
     *
     * @return {string} The import path for this element class
     * @suppress {missingProperties}
     * @nocollapse
     */
    static get importPath() {
      if (!this.hasOwnProperty(JSCompiler_renameProperty("_importPath", this))) {
        const c = this.importMeta;
        if (c)
          this._importPath = ls(c.url);
        else {
          const d = mr.import(
            /** @type {PolymerElementConstructor} */
            this.is
          );
          this._importPath = d && d.assetpath || Object.getPrototypeOf(
            /** @type {PolymerElementConstructor}*/
            this.prototype
          ).constructor.importPath;
        }
      }
      return this._importPath;
    }
    constructor() {
      super(), this._template, this._importPath, this.rootPath, this.importPath, this.root, this.$;
    }
    /**
     * Overrides the default `PropertyAccessors` to ensure class
     * metaprogramming related to property accessors and effects has
     * completed (calls `finalize`).
     *
     * It also initializes any property defaults provided via `value` in
     * `properties` metadata.
     *
     * @return {void}
     * @override
     * @suppress {invalidCasts,missingProperties} go/missingfnprops
     */
    _initializeProperties() {
      this.constructor.finalize(), this.constructor._finalizeTemplate(
        /** @type {!HTMLElement} */
        this.localName
      ), super._initializeProperties(), this.rootPath = Hh, this.importPath = this.constructor.importPath;
      let c = e(this.constructor);
      if (c)
        for (let d in c) {
          let f = c[d];
          if (this._canApplyPropertyDefault(d)) {
            let p = typeof f.value == "function" ? f.value.call(this) : f.value;
            this._hasAccessor(d) ? this._setPendingProperty(d, p, !0) : this[d] = p;
          }
        }
    }
    /**
     * Determines if a property dfeault can be applied. For example, this
     * prevents a default from being applied when a property that has no
     * accessor is overridden by its host before upgrade (e.g. via a binding).
     * @override
     * @param {string} property Name of the property
     * @return {boolean} Returns true if the property default can be applied.
     */
    _canApplyPropertyDefault(c) {
      return !this.hasOwnProperty(c);
    }
    /**
     * Gather style text for a style element in the template.
     *
     * @param {string} cssText Text containing styling to process
     * @param {string} baseURI Base URI to rebase CSS paths against
     * @return {string} The processed CSS text
     * @protected
     * @nocollapse
     */
    static _processStyleText(c, d) {
      return as(c, d);
    }
    /**
    * Configures an element `proto` to function with a given `template`.
    * The element name `is` and extends `ext` must be specified for ShadyCSS
    * style scoping.
    *
    * @param {string} is Tag name (or type extension name) for this element
    * @return {void}
    * @protected
    * @nocollapse
    */
    static _finalizeTemplate(c) {
      const d = this.prototype._template;
      if (d && !d.__polymerFinalized) {
        d.__polymerFinalized = !0;
        const f = this.importPath, p = f ? dr(f) : "";
        r(this, d, c, p), this.prototype._bindTemplate(d);
      }
    }
    /**
     * Provides a default implementation of the standard Custom Elements
     * `connectedCallback`.
     *
     * The default implementation enables the property effects system and
     * flushes any pending properties, and updates shimmed CSS properties
     * when using the ShadyCSS scoping/custom properties polyfill.
     *
     * @override
     * @suppress {missingProperties, invalidCasts} Super may or may not
     *     implement the callback
     * @return {void}
     */
    connectedCallback() {
      window.ShadyCSS && this._template && window.ShadyCSS.styleElement(
        /** @type {!HTMLElement} */
        this
      ), super.connectedCallback();
    }
    /**
     * Stamps the element template.
     *
     * @return {void}
     * @override
     */
    ready() {
      this._template && (this.root = this._stampTemplate(this._template), this.$ = this.root.$), super.ready();
    }
    /**
     * Implements `PropertyEffects`'s `_readyClients` call. Attaches
     * element dom by calling `_attachDom` with the dom stamped from the
     * element's template via `_stampTemplate`. Note that this allows
     * client dom to be attached to the element prior to any observers
     * running.
     *
     * @return {void}
     * @override
     */
    _readyClients() {
      this._template && (this.root = this._attachDom(
        /** @type {StampedTemplate} */
        this.root
      )), super._readyClients();
    }
    /**
     * Attaches an element's stamped dom to itself. By default,
     * this method creates a `shadowRoot` and adds the dom to it.
     * However, this method may be overridden to allow an element
     * to put its dom in another location.
     *
     * @override
     * @throws {Error}
     * @suppress {missingReturn}
     * @param {StampedTemplate} dom to attach to the element.
     * @return {ShadowRoot} node to which the dom has been attached.
     */
    _attachDom(c) {
      const d = ui(this);
      if (d.attachShadow)
        return c ? (d.shadowRoot || (d.attachShadow({ mode: "open", shadyUpgradeFragment: c }), d.shadowRoot.appendChild(c), this.constructor._styleSheet && (d.shadowRoot.adoptedStyleSheets = [this.constructor._styleSheet])), jh && window.ShadyDOM && window.ShadyDOM.flushInitial(d.shadowRoot), d.shadowRoot) : null;
      throw new Error("ShadowDOM not available. PolymerElement can create dom as children instead of in ShadowDOM by setting `this.root = this;` before `ready`.");
    }
    /**
     * When using the ShadyCSS scoping and custom property shim, causes all
     * shimmed styles in this element (and its subtree) to be updated
     * based on current custom property values.
     *
     * The optional parameter overrides inline custom property styles with an
     * object of properties where the keys are CSS properties, and the values
     * are strings.
     *
     * Example: `this.updateStyles({'--color': 'blue'})`
     *
     * These properties are retained unless a value of `null` is set.
     *
     * Note: This function does not support updating CSS mixins.
     * You can not dynamically change the value of an `@apply`.
     *
     * @override
     * @param {Object=} properties Bag of custom property key/values to
     *   apply to this element.
     * @return {void}
     * @suppress {invalidCasts}
     */
    updateStyles(c) {
      window.ShadyCSS && window.ShadyCSS.styleSubtree(
        /** @type {!HTMLElement} */
        this,
        c
      );
    }
    /**
     * Rewrites a given URL relative to a base URL. The base URL defaults to
     * the original location of the document containing the `dom-module` for
     * this element. This method will return the same URL before and after
     * bundling.
     *
     * Note that this function performs no resolution for URLs that start
     * with `/` (absolute URLs) or `#` (hash identifiers).  For general purpose
     * URL resolution, use `window.URL`.
     *
     * @override
     * @param {string} url URL to resolve.
     * @param {string=} base Optional base URL to resolve against, defaults
     * to the element's `importPath`
     * @return {string} Rewritten URL relative to base
     */
    resolveUrl(c, d) {
      return !d && this.importPath && (d = dr(this.importPath)), dr(c, d);
    }
    /**
     * Overrides `PropertyEffects` to add map of dynamic functions on
     * template info, for consumption by `PropertyEffects` template binding
     * code. This map determines which method templates should have accessors
     * created for them.
     *
     * @param {!HTMLTemplateElement} template Template
     * @param {!TemplateInfo} templateInfo Template metadata for current template
     * @param {!NodeInfo} nodeInfo Node metadata for current template.
     * @return {boolean} .
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _parseTemplateContent(c, d, f) {
      return d.dynamicFns = d.dynamicFns || this._properties, n._parseTemplateContent.call(
        this,
        c,
        d,
        f
      );
    }
    /**
     * Overrides `PropertyEffects` to warn on use of undeclared properties in
     * template.
     *
     * @param {Object} templateInfo Template metadata to add effect to
     * @param {string} prop Property that should trigger the effect
     * @param {Object=} effect Effect metadata object
     * @return {void}
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _addTemplatePropertyEffect(c, d, f) {
      return Wh && !(d in this._properties) && // Methods used in templates with no dependencies (or only literal
      // dependencies) become accessors with template effects; ignore these
      !(f.info.part.signature && f.info.part.signature.static) && // Warnings for bindings added to nested templates are handled by
      // templatizer so ignore both the host-to-template bindings
      // (`hostProp`) and TemplateInstance-to-child bindings
      // (`nestedTemplate`)
      !f.info.part.hostProp && c.nestedTemplate, n._addTemplatePropertyEffect.call(
        this,
        c,
        d,
        f
      );
    }
  }
  return l;
});
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const Da = window.trustedTypes && trustedTypes.createPolicy("polymer-html-literal", { createHTML: (s) => s });
class Ol {
  /**
   * @param {!ITemplateArray} strings Constant parts of tagged template literal
   * @param {!Array<*>} values Variable parts of tagged template literal
   */
  constructor(n, e) {
    Fl(n, e);
    const t = e.reduce(
      (i, r, o) => i + zl(r) + n[o + 1],
      n[0]
    );
    this.value = t.toString();
  }
  /**
   * @return {string} LiteralString string value
   * @override
   */
  toString() {
    return this.value;
  }
}
function zl(s) {
  if (s instanceof Ol)
    return (
      /** @type {!LiteralString} */
      s.value
    );
  throw new Error(
    `non-literal value passed to Polymer's htmlLiteral function: ${s}`
  );
}
function au(s) {
  if (s instanceof HTMLTemplateElement)
    return (
      /** @type {!HTMLTemplateElement } */
      s.innerHTML
    );
  if (s instanceof Ol)
    return zl(s);
  throw new Error(
    `non-template value passed to Polymer's html function: ${s}`
  );
}
const Wi = function(n, ...e) {
  Fl(n, e);
  const t = (
    /** @type {!HTMLTemplateElement} */
    document.createElement("template")
  );
  let i = e.reduce(
    (r, o, l) => r + au(o) + n[l + 1],
    n[0]
  );
  return Da && (i = Da.createHTML(i)), t.innerHTML = i, t;
}, Fl = (s, n) => {
  if (!Array.isArray(s) || !Array.isArray(s.raw) || n.length !== s.length - 1)
    throw new TypeError("Invalid call to the html template tag");
};
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const yi = su(HTMLElement);
/**
 * @license
 * Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
 * This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
 * The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
 * The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
 * Code distributed by Google as part of the polymer project is also
 * subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
 */
let Oa = 0, Ll = 0;
const Fi = [];
let Vo = !1;
function lu() {
  Vo = !1;
  const s = Fi.length;
  for (let n = 0; n < s; n++) {
    const e = Fi[n];
    if (e)
      try {
        e();
      } catch (t) {
        setTimeout(() => {
          throw t;
        });
      }
  }
  Fi.splice(0, s), Ll += s;
}
const Nt = {
  /**
   * Returns a sub-module with the async interface providing the provided
   * delay.
   *
   * @memberof timeOut
   * @param {number=} delay Time to wait before calling callbacks in ms
   * @return {!AsyncInterface} An async timeout interface
   */
  after(s) {
    return {
      run(n) {
        return window.setTimeout(n, s);
      },
      cancel(n) {
        window.clearTimeout(n);
      }
    };
  },
  /**
   * Enqueues a function called in the next task.
   *
   * @memberof timeOut
   * @param {!Function} fn Callback to run
   * @param {number=} delay Delay in milliseconds
   * @return {number} Handle used for canceling task
   */
  run(s, n) {
    return window.setTimeout(s, n);
  },
  /**
   * Cancels a previously enqueued `timeOut` callback.
   *
   * @memberof timeOut
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(s) {
    window.clearTimeout(s);
  }
}, Xt = {
  /**
   * Enqueues a function called at `requestAnimationFrame` timing.
   *
   * @memberof animationFrame
   * @param {function(number):void} fn Callback to run
   * @return {number} Handle used for canceling task
   */
  run(s) {
    return window.requestAnimationFrame(s);
  },
  /**
   * Cancels a previously enqueued `animationFrame` callback.
   *
   * @memberof animationFrame
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(s) {
    window.cancelAnimationFrame(s);
  }
}, Ml = {
  /**
   * Enqueues a function called at `requestIdleCallback` timing.
   *
   * @memberof idlePeriod
   * @param {function(!IdleDeadline):void} fn Callback to run
   * @return {number} Handle used for canceling task
   */
  run(s) {
    return window.requestIdleCallback ? window.requestIdleCallback(s) : window.setTimeout(s, 16);
  },
  /**
   * Cancels a previously enqueued `idlePeriod` callback.
   *
   * @memberof idlePeriod
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(s) {
    window.cancelIdleCallback ? window.cancelIdleCallback(s) : window.clearTimeout(s);
  }
}, yt = {
  /**
   * Enqueues a function called at microtask timing.
   *
   * @memberof microTask
   * @param {!Function=} callback Callback to run
   * @return {number} Handle used for canceling task
   */
  run(s) {
    Vo || (Vo = !0, queueMicrotask(() => lu())), Fi.push(s);
    const n = Oa;
    return Oa += 1, n;
  },
  /**
   * Cancels a previously enqueued `microTask` callback.
   *
   * @memberof microTask
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(s) {
    const n = s - Ll;
    if (n >= 0) {
      if (!Fi[n])
        throw new Error(`invalid async handle: ${s}`);
      Fi[n] = null;
    }
  }
};
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const yr = /* @__PURE__ */ new Set();
class ze {
  /**
   * Creates a debouncer if no debouncer is passed as a parameter
   * or it cancels an active debouncer otherwise. The following
   * example shows how a debouncer can be called multiple times within a
   * microtask and "debounced" such that the provided callback function is
   * called once. Add this method to a custom element:
   *
   * ```js
   * import {microTask} from '@vaadin/component-base/src/async.js';
   * import {Debouncer} from '@vaadin/component-base/src/debounce.js';
   * // ...
   *
   * _debounceWork() {
   *   this._debounceJob = Debouncer.debounce(this._debounceJob,
   *       microTask, () => this._doWork());
   * }
   * ```
   *
   * If the `_debounceWork` method is called multiple times within the same
   * microtask, the `_doWork` function will be called only once at the next
   * microtask checkpoint.
   *
   * Note: In testing it is often convenient to avoid asynchrony. To accomplish
   * this with a debouncer, you can use `enqueueDebouncer` and
   * `flush`. For example, extend the above example by adding
   * `enqueueDebouncer(this._debounceJob)` at the end of the
   * `_debounceWork` method. Then in a test, call `flush` to ensure
   * the debouncer has completed.
   *
   * @param {Debouncer?} debouncer Debouncer object.
   * @param {!AsyncInterface} asyncModule Object with Async interface
   * @param {function()} callback Callback to run.
   * @return {!Debouncer} Returns a debouncer object.
   */
  static debounce(n, e, t) {
    return n instanceof ze ? n._cancelAsync() : n = new ze(), n.setConfig(e, t), n;
  }
  constructor() {
    this._asyncModule = null, this._callback = null, this._timer = null;
  }
  /**
   * Sets the scheduler; that is, a module with the Async interface,
   * a callback and optional arguments to be passed to the run function
   * from the async module.
   *
   * @param {!AsyncInterface} asyncModule Object with Async interface.
   * @param {function()} callback Callback to run.
   * @return {void}
   */
  setConfig(n, e) {
    this._asyncModule = n, this._callback = e, this._timer = this._asyncModule.run(() => {
      this._timer = null, yr.delete(this), this._callback();
    });
  }
  /**
   * Cancels an active debouncer and returns a reference to itself.
   *
   * @return {void}
   */
  cancel() {
    this.isActive() && (this._cancelAsync(), yr.delete(this));
  }
  /**
   * Cancels a debouncer's async callback.
   *
   * @return {void}
   */
  _cancelAsync() {
    this.isActive() && (this._asyncModule.cancel(
      /** @type {number} */
      this._timer
    ), this._timer = null);
  }
  /**
   * Flushes an active debouncer and returns a reference to itself.
   *
   * @return {void}
   */
  flush() {
    this.isActive() && (this.cancel(), this._callback());
  }
  /**
   * Returns true if the debouncer is active.
   *
   * @return {boolean} True if active.
   */
  isActive() {
    return this._timer != null;
  }
}
function Nl(s) {
  yr.add(s);
}
function cu() {
  const s = !!yr.size;
  return yr.forEach((n) => {
    try {
      n.flush();
    } catch (e) {
      setTimeout(() => {
        throw e;
      });
    }
  }), s;
}
const cr = () => {
  let s;
  do
    s = cu();
  while (s);
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ft = [];
function Uo(s, n, e = s.getAttribute("dir")) {
  n ? s.setAttribute("dir", n) : e != null && s.removeAttribute("dir");
}
function Wo() {
  return document.documentElement.getAttribute("dir");
}
function hu() {
  const s = Wo();
  Ft.forEach((n) => {
    Uo(n, s);
  });
}
const du = new MutationObserver(hu);
du.observe(document.documentElement, { attributes: !0, attributeFilter: ["dir"] });
const Tr = (s) => class extends s {
  static get properties() {
    return {
      /**
       * @protected
       */
      dir: {
        type: String,
        value: "",
        reflectToAttribute: !0,
        converter: {
          fromAttribute: (e) => e || "",
          toAttribute: (e) => e === "" ? null : e
        }
      }
    };
  }
  /**
   * @return {boolean}
   * @protected
   */
  get __isRTL() {
    return this.getAttribute("dir") === "rtl";
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback(), (!this.hasAttribute("dir") || this.__restoreSubscription) && (this.__subscribe(), Uo(this, Wo(), null));
  }
  /** @protected */
  attributeChangedCallback(e, t, i) {
    if (super.attributeChangedCallback(e, t, i), e !== "dir")
      return;
    const r = Wo(), o = i === r && Ft.indexOf(this) === -1, l = !i && t && Ft.indexOf(this) === -1;
    o || l ? (this.__subscribe(), Uo(this, r, i)) : i !== r && t === r && this.__unsubscribe();
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback(), this.__restoreSubscription = Ft.includes(this), this.__unsubscribe();
  }
  /** @protected */
  _valueToNodeAttribute(e, t, i) {
    i === "dir" && t === "" && !e.hasAttribute("dir") || super._valueToNodeAttribute(e, t, i);
  }
  /** @protected */
  _attributeToProperty(e, t, i) {
    e === "dir" && !t ? this.dir = "" : super._attributeToProperty(e, t, i);
  }
  /** @private */
  __subscribe() {
    Ft.includes(this) || Ft.push(this);
  }
  /** @private */
  __unsubscribe() {
    Ft.includes(this) && Ft.splice(Ft.indexOf(this), 1);
  }
};
/**
 * @license
 * Copyright (c) 2023 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function _i(s, n) {
  return s.split(".").reduce((e, t) => e ? e[t] : void 0, n);
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function ds(s) {
  if (window.Vaadin && window.Vaadin.templateRendererCallback) {
    window.Vaadin.templateRendererCallback(s);
    return;
  }
  s.querySelector("template");
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function uu(s) {
  const n = [];
  for (; s; ) {
    if (s.nodeType === Node.DOCUMENT_NODE) {
      n.push(s);
      break;
    }
    if (s.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
      n.push(s), s = s.host;
      continue;
    }
    if (s.assignedSlot) {
      s = s.assignedSlot;
      continue;
    }
    s = s.parentNode;
  }
  return n;
}
function Bl(s, n) {
  return n ? n.closest(s) || Bl(s, n.getRootNode().host) : null;
}
function us(s) {
  return s ? new Set(s.split(" ")) : /* @__PURE__ */ new Set();
}
function On(s) {
  return s ? [...s].join(" ") : "";
}
function zn(s, n, e) {
  const t = us(s.getAttribute(n));
  t.add(e), s.setAttribute(n, On(t));
}
function fs(s, n, e) {
  const t = us(s.getAttribute(n));
  if (t.delete(e), t.size === 0) {
    s.removeAttribute(n);
    return;
  }
  s.setAttribute(n, On(t));
}
function fu(s) {
  return s.nodeType === Node.TEXT_NODE && s.textContent.trim() === "";
}
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function Bi(s) {
  return s.__cells || Array.from(s.querySelectorAll('[part~="cell"]:not([part~="details-cell"])'));
}
function at(s, n) {
  [...s.children].forEach(n);
}
function Hi(s, n) {
  Bi(s).forEach(n), s.__detailsCell && n(s.__detailsCell);
}
function pu(s, n, e) {
  let t = 1;
  s.forEach((i) => {
    t % 10 === 0 && (t += 1), i._order = e + t * n, t += 1;
  });
}
function Fn(s, n, e) {
  switch (typeof e) {
    case "boolean":
      s.toggleAttribute(n, e);
      break;
    case "string":
      s.setAttribute(n, e);
      break;
    default:
      s.removeAttribute(n);
      break;
  }
}
function Qt(s, n, e) {
  n || n === "" ? zn(s, "part", e) : fs(s, "part", e);
}
function Zt(s, n, e) {
  s.forEach((t) => {
    Qt(t, e, n);
  });
}
function Di(s, n) {
  const e = Bi(s);
  Object.entries(n).forEach(([t, i]) => {
    Fn(s, t, i);
    const r = `${t}-row`;
    Qt(s, i, r), Zt(e, `${r}-cell`, i);
  });
}
function za(s, n) {
  const e = Bi(s);
  Object.entries(n).forEach(([t, i]) => {
    const r = s.getAttribute(t);
    if (Fn(s, t, i), r) {
      const o = `${t}-${r}-row`;
      Qt(s, !1, o), Zt(e, `${o}-cell`, !1);
    }
    if (i) {
      const o = `${t}-${i}-row`;
      Qt(s, i, o), Zt(e, `${o}-cell`, i);
    }
  });
}
function ci(s, n, e, t, i) {
  Fn(s, n, e), i && Qt(s, !1, i), Qt(s, e, t || `${n}-cell`);
}
class Li {
  constructor(n, e) {
    this.__host = n, this.__callback = e, this.__currentSlots = [], this.__onMutation = this.__onMutation.bind(this), this.__observer = new MutationObserver(this.__onMutation), this.__observer.observe(n, {
      childList: !0
    }), this.__initialCallDebouncer = ze.debounce(this.__initialCallDebouncer, yt, () => this.__onMutation());
  }
  disconnect() {
    this.__observer.disconnect(), this.__initialCallDebouncer.cancel(), this.__toggleSlotChangeListeners(!1);
  }
  flush() {
    this.__onMutation();
  }
  __toggleSlotChangeListeners(n) {
    this.__currentSlots.forEach((e) => {
      n ? e.addEventListener("slotchange", this.__onMutation) : e.removeEventListener("slotchange", this.__onMutation);
    });
  }
  __onMutation() {
    const n = !this.__currentColumns;
    this.__currentColumns ||= [];
    const e = Li.getColumns(this.__host), t = e.filter((l) => !this.__currentColumns.includes(l)), i = this.__currentColumns.filter((l) => !e.includes(l)), r = this.__currentColumns.some((l, a) => l !== e[a]);
    this.__currentColumns = e, this.__toggleSlotChangeListeners(!1), this.__currentSlots = [...this.__host.children].filter((l) => l instanceof HTMLSlotElement), this.__toggleSlotChangeListeners(!0), (n || t.length || i.length || r) && this.__callback(t, i);
  }
  /**
   * Default filter for column elements.
   */
  static __isColumnElement(n) {
    return n.nodeType === Node.ELEMENT_NODE && /\bcolumn\b/u.test(n.localName);
  }
  static getColumns(n) {
    const e = [], t = n._isColumnElement || Li.__isColumnElement;
    return [...n.children].forEach((i) => {
      t(i) ? e.push(i) : i instanceof HTMLSlotElement && [...i.assignedElements({ flatten: !0 })].filter((r) => t(r)).forEach((r) => e.push(r));
    }), e;
  }
}
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const gu = (s) => class extends s {
  static get properties() {
    return {
      /**
       * When set to true, the column is user-resizable.
       * @default false
       */
      resizable: {
        type: Boolean,
        sync: !0,
        value() {
          if (this.localName === "vaadin-grid-column-group")
            return;
          const e = this.parentNode;
          return e && e.localName === "vaadin-grid-column-group" && e.resizable || !1;
        }
      },
      /**
       * When true, the column is frozen. When a column inside of a column group is frozen,
       * all of the sibling columns inside the group will get frozen also.
       * @type {boolean}
       */
      frozen: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /**
       * When true, the column is frozen to end of grid.
       *
       * When a column inside of a column group is frozen to end, all of the sibling columns
       * inside the group will get frozen to end also.
       *
       * Column can not be set as `frozen` and `frozenToEnd` at the same time.
       * @attr {boolean} frozen-to-end
       * @type {boolean}
       */
      frozenToEnd: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /**
       * When true, the cells for this column will be rendered with the `role` attribute
       * set as `rowheader`, instead of the `gridcell` role value used by default.
       *
       * When a column is set as row header, its cells will be announced by screen readers
       * while navigating to help user identify the current row as uniquely as possible.
       *
       * @attr {boolean} row-header
       * @type {boolean}
       */
      rowHeader: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /**
       * When set to true, the cells for this column are hidden.
       */
      hidden: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /**
       * Text content to display in the header cell of the column.
       */
      header: {
        type: String,
        sync: !0
      },
      /**
       * Aligns the columns cell content horizontally.
       * Supported values: "start", "center" and "end".
       * @attr {start|center|end} text-align
       * @type {GridColumnTextAlign | null | undefined}
       */
      textAlign: {
        type: String,
        sync: !0
      },
      /**
       * Custom part name for the header cell.
       *
       * @attr {string} header-part-name
       */
      headerPartName: {
        type: String,
        sync: !0
      },
      /**
       * Custom part name for the footer cell.
       *
       * @attr {string} footer-part-name
       */
      footerPartName: {
        type: String,
        sync: !0
      },
      /**
       * @type {boolean}
       * @protected
       */
      _lastFrozen: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /**
       * @type {boolean}
       * @protected
       */
      _bodyContentHidden: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /**
       * @type {boolean}
       * @protected
       */
      _firstFrozenToEnd: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /** @protected */
      _order: {
        type: Number,
        sync: !0
      },
      /** @private */
      _reorderStatus: {
        type: Boolean,
        sync: !0
      },
      /**
       * @type {Array<!HTMLElement>}
       * @protected
       */
      _emptyCells: Array,
      /** @private */
      _headerCell: Object,
      /** @private */
      _footerCell: Object,
      /** @protected */
      _grid: Object,
      /**
       * By default, the Polymer doesn't invoke the observer
       * during initialization if all of its dependencies are `undefined`.
       * This internal property can be used to force initial invocation of an observer
       * even the other dependencies of the observer are `undefined`.
       *
       * @private
       */
      __initialized: {
        type: Boolean,
        value: !0
      },
      /**
       * Custom function for rendering the header content.
       * Receives two arguments:
       *
       * - `root` The header cell content DOM element. Append your content to it.
       * - `column` The `<vaadin-grid-column>` element.
       *
       * @type {GridHeaderFooterRenderer | null | undefined}
       */
      headerRenderer: {
        type: Function,
        sync: !0
      },
      /**
       * Represents the final header renderer computed on the set of observable arguments.
       * It is supposed to be used internally when rendering the header cell content.
       *
       * @protected
       * @type {GridHeaderFooterRenderer | undefined}
       */
      _headerRenderer: {
        type: Function,
        computed: "_computeHeaderRenderer(headerRenderer, header, __initialized)",
        sync: !0
      },
      /**
       * Custom function for rendering the footer content.
       * Receives two arguments:
       *
       * - `root` The footer cell content DOM element. Append your content to it.
       * - `column` The `<vaadin-grid-column>` element.
       *
       * @type {GridHeaderFooterRenderer | null | undefined}
       */
      footerRenderer: {
        type: Function,
        sync: !0
      },
      /**
       * Represents the final footer renderer computed on the set of observable arguments.
       * It is supposed to be used internally when rendering the footer cell content.
       *
       * @protected
       * @type {GridHeaderFooterRenderer | undefined}
       */
      _footerRenderer: {
        type: Function,
        computed: "_computeFooterRenderer(footerRenderer, __initialized)",
        sync: !0
      },
      /**
       * An internal property that is mainly used by `vaadin-template-renderer`
       * to identify grid column elements.
       *
       * @private
       */
      __gridColumnElement: {
        type: Boolean,
        value: !0
      }
    };
  }
  static get observers() {
    return [
      "_widthChanged(width, _headerCell, _footerCell, _cells)",
      "_frozenChanged(frozen, _headerCell, _footerCell, _cells)",
      "_frozenToEndChanged(frozenToEnd, _headerCell, _footerCell, _cells)",
      "_flexGrowChanged(flexGrow, _headerCell, _footerCell, _cells)",
      "_textAlignChanged(textAlign, _cells, _headerCell, _footerCell)",
      "_orderChanged(_order, _headerCell, _footerCell, _cells)",
      "_lastFrozenChanged(_lastFrozen)",
      "_firstFrozenToEndChanged(_firstFrozenToEnd)",
      "_onRendererOrBindingChanged(_renderer, _cells, _bodyContentHidden, path)",
      "_onHeaderRendererOrBindingChanged(_headerRenderer, _headerCell, path, header)",
      "_onFooterRendererOrBindingChanged(_footerRenderer, _footerCell)",
      "_resizableChanged(resizable, _headerCell)",
      "_reorderStatusChanged(_reorderStatus, _headerCell, _footerCell, _cells)",
      "_hiddenChanged(hidden, _headerCell, _footerCell, _cells)",
      "_rowHeaderChanged(rowHeader, _cells)",
      "__headerFooterPartNameChanged(_headerCell, _footerCell, headerPartName, footerPartName)"
    ];
  }
  /**
   * @return {!Grid | undefined}
   * @protected
   */
  get _grid() {
    return this._gridValue || (this._gridValue = this._findHostGrid()), this._gridValue;
  }
  /**
   * @return {!Array<!HTMLElement>}
   * @protected
   */
  get _allCells() {
    return [].concat(this._cells || []).concat(this._emptyCells || []).concat(this._headerCell).concat(this._footerCell).filter((e) => e);
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback(), requestAnimationFrame(() => {
      this._grid && this._allCells.forEach((e) => {
        e._content.parentNode || this._grid.appendChild(e._content);
      });
    });
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback(), requestAnimationFrame(() => {
      this._grid || this._allCells.forEach((e) => {
        e._content.parentNode && e._content.parentNode.removeChild(e._content);
      });
    }), this._gridValue = void 0;
  }
  /** @protected */
  ready() {
    super.ready(), ds(this);
  }
  /**
   * @return {!Grid | undefined}
   * @protected
   */
  _findHostGrid() {
    let e = this;
    for (; e && !/^vaadin.*grid(-pro)?$/u.test(e.localName); )
      e = e.assignedSlot ? e.assignedSlot.parentNode : e.parentNode;
    return e || void 0;
  }
  /** @protected */
  _renderHeaderAndFooter() {
    this._renderHeaderCellContent(this._headerRenderer, this._headerCell), this._renderFooterCellContent(this._footerRenderer, this._footerCell);
  }
  /** @private */
  _flexGrowChanged(e) {
    this.parentElement && this.parentElement._columnPropChanged && this.parentElement._columnPropChanged("flexGrow"), this._allCells.forEach((t) => {
      t.style.flexGrow = e;
    });
  }
  /** @private */
  _orderChanged(e) {
    this._allCells.forEach((t) => {
      t.style.order = e;
    });
  }
  /** @private */
  _widthChanged(e) {
    this.parentElement && this.parentElement._columnPropChanged && this.parentElement._columnPropChanged("width"), this._allCells.forEach((t) => {
      t.style.width = e;
    });
  }
  /** @private */
  _frozenChanged(e) {
    this.parentElement && this.parentElement._columnPropChanged && this.parentElement._columnPropChanged("frozen", e), this._allCells.forEach((t) => {
      ci(t, "frozen", e);
    }), this._grid && this._grid._frozenCellsChanged && this._grid._frozenCellsChanged();
  }
  /** @private */
  _frozenToEndChanged(e) {
    this.parentElement && this.parentElement._columnPropChanged && this.parentElement._columnPropChanged("frozenToEnd", e), this._allCells.forEach((t) => {
      this._grid && t.parentElement === this._grid.$.sizer || ci(t, "frozen-to-end", e);
    }), this._grid && this._grid._frozenCellsChanged && this._grid._frozenCellsChanged();
  }
  /** @private */
  _lastFrozenChanged(e) {
    this._allCells.forEach((t) => {
      ci(t, "last-frozen", e);
    }), this.parentElement && this.parentElement._columnPropChanged && (this.parentElement._lastFrozen = e);
  }
  /** @private */
  _firstFrozenToEndChanged(e) {
    this._allCells.forEach((t) => {
      this._grid && t.parentElement === this._grid.$.sizer || ci(t, "first-frozen-to-end", e);
    }), this.parentElement && this.parentElement._columnPropChanged && (this.parentElement._firstFrozenToEnd = e);
  }
  /** @private */
  _rowHeaderChanged(e, t) {
    t && t.forEach((i) => {
      i.setAttribute("role", e ? "rowheader" : "gridcell");
    });
  }
  /**
   * @param {string} path
   * @return {string}
   * @protected
   */
  _generateHeader(e) {
    return e.substr(e.lastIndexOf(".") + 1).replace(/([A-Z])/gu, "-$1").toLowerCase().replace(/-/gu, " ").replace(/^./u, (t) => t.toUpperCase());
  }
  /** @private */
  _reorderStatusChanged(e) {
    const t = this.__previousReorderStatus, i = t ? `reorder-${t}-cell` : "", r = `reorder-${e}-cell`;
    this._allCells.forEach((o) => {
      ci(o, "reorder-status", e, r, i);
    }), this.__previousReorderStatus = e;
  }
  /** @private */
  _resizableChanged(e, t) {
    e === void 0 || t === void 0 || t && [t].concat(this._emptyCells).forEach((i) => {
      if (i) {
        const r = i.querySelector('[part~="resize-handle"]');
        if (r && i.removeChild(r), e) {
          const o = document.createElement("div");
          o.setAttribute("part", "resize-handle"), i.appendChild(o);
        }
      }
    });
  }
  /** @private */
  _textAlignChanged(e) {
    if (e === void 0 || this._grid === void 0 || ["start", "end", "center"].indexOf(e) === -1)
      return;
    let t;
    getComputedStyle(this._grid).direction === "ltr" ? e === "start" ? t = "left" : e === "end" && (t = "right") : e === "start" ? t = "right" : e === "end" && (t = "left"), this._allCells.forEach((i) => {
      i._content.style.textAlign = e, getComputedStyle(i._content).textAlign !== e && (i._content.style.textAlign = t);
    });
  }
  /** @private */
  _hiddenChanged(e) {
    this.parentElement && this.parentElement._columnPropChanged && this.parentElement._columnPropChanged("hidden", e), !!e != !!this._previousHidden && this._grid && (e === !0 && this._allCells.forEach((t) => {
      t._content.parentNode && t._content.parentNode.removeChild(t._content);
    }), this._grid._debouncerHiddenChanged = ze.debounce(
      this._grid._debouncerHiddenChanged,
      Xt,
      () => {
        this._grid && this._grid._renderColumnTree && this._grid._renderColumnTree(this._grid._columnTree);
      }
    ), this._grid._debounceUpdateFrozenColumn && this._grid._debounceUpdateFrozenColumn(), this._grid._resetKeyboardNavigation && this._grid._resetKeyboardNavigation()), this._previousHidden = e;
  }
  /** @protected */
  _runRenderer(e, t, i) {
    const r = i && i.item && !t.parentElement.hidden;
    if (!(r || e === this._headerRenderer || e === this._footerRenderer))
      return;
    const l = [t._content, this];
    r && l.push(i), e.apply(this, l);
  }
  /**
   * Renders the content to the given cells using a renderer.
   *
   * @private
   */
  __renderCellsContent(e, t) {
    this.hidden || !this._grid || t.forEach((i) => {
      if (!i.parentElement)
        return;
      const r = this._grid.__getRowModel(i.parentElement);
      e && (i._renderer !== e && this._clearCellContent(i), i._renderer = e, this._runRenderer(e, i, r));
    });
  }
  /**
   * Clears the content of a cell.
   *
   * @protected
   */
  _clearCellContent(e) {
    e._content.innerHTML = "", delete e._content._$litPart$;
  }
  /**
   * Renders the header cell content using a renderer,
   * and then updates the visibility of the parent row depending on
   * whether all its children cells are empty or not.
   *
   * @protected
   */
  _renderHeaderCellContent(e, t) {
    !t || !e || (this.__renderCellsContent(e, [t]), this._grid && t.parentElement && this._grid.__debounceUpdateHeaderFooterRowVisibility(t.parentElement));
  }
  /** @protected */
  _onHeaderRendererOrBindingChanged(e, t, ...i) {
    this._renderHeaderCellContent(e, t);
  }
  /** @private */
  __headerFooterPartNameChanged(e, t, i, r) {
    [
      { cell: e, partName: i },
      { cell: t, partName: r }
    ].forEach(({ cell: o, partName: l }) => {
      if (o) {
        const a = o.__customParts || [];
        o.part.remove(...a), o.__customParts = l ? l.trim().split(" ") : [], o.part.add(...o.__customParts);
      }
    });
  }
  /**
   * Renders the content of body cells using a renderer.
   *
   * @protected
   */
  _renderBodyCellsContent(e, t) {
    !t || !e || this.__renderCellsContent(e, t);
  }
  /** @protected */
  _onRendererOrBindingChanged(e, t, ...i) {
    this._renderBodyCellsContent(e, t);
  }
  /**
   * Renders the footer cell content using a renderer
   * and then updates the visibility of the parent row depending on
   * whether all its children cells are empty or not.
   *
   * @protected
   */
  _renderFooterCellContent(e, t) {
    !t || !e || (this.__renderCellsContent(e, [t]), this._grid && t.parentElement && this._grid.__debounceUpdateHeaderFooterRowVisibility(t.parentElement));
  }
  /** @protected */
  _onFooterRendererOrBindingChanged(e, t) {
    this._renderFooterCellContent(e, t);
  }
  /** @private */
  __setTextContent(e, t) {
    e.textContent !== t && (e.textContent = t);
  }
  /**
   * Renders the text header to the header cell.
   *
   * @private
   */
  __textHeaderRenderer() {
    this.__setTextContent(this._headerCell._content, this.header);
  }
  /**
   * Computes the property name based on the path and renders it to the header cell.
   * If the path is not defined, then nothing is rendered.
   *
   * @protected
   */
  _defaultHeaderRenderer() {
    this.path && this.__setTextContent(this._headerCell._content, this._generateHeader(this.path));
  }
  /**
   * Computes the item property value based on the path and renders it to the body cell.
   * If the path is not defined, then nothing is rendered.
   *
   * @protected
   */
  _defaultRenderer(e, t, { item: i }) {
    this.path && this.__setTextContent(e, _i(this.path, i));
  }
  /**
   * By default, nothing is rendered to the footer cell.
   *
   * @protected
   */
  _defaultFooterRenderer() {
  }
  /**
   * Computes the final header renderer for the `_headerRenderer` computed property.
   * All the arguments are observable by the Polymer, it re-calls the method
   * once an argument is changed to update the property value.
   *
   * @protected
   * @return {GridHeaderFooterRenderer | undefined}
   */
  _computeHeaderRenderer(e, t) {
    return e || (t != null ? this.__textHeaderRenderer : this._defaultHeaderRenderer);
  }
  /**
   * Computes the final renderer for the `_renderer` property.
   * All the arguments are observable by the Polymer, it re-calls the method
   * once an argument is changed to update the property value.
   *
   * @protected
   * @return {GridBodyRenderer | undefined}
   */
  _computeRenderer(e) {
    return e || this._defaultRenderer;
  }
  /**
   * Computes the final footer renderer for the `_footerRenderer` property.
   * All the arguments are observable by the Polymer, it re-calls the method
   * once an argument is changed to update the property value.
   *
   * @protected
   * @return {GridHeaderFooterRenderer | undefined}
   */
  _computeFooterRenderer(e) {
    return e || this._defaultFooterRenderer;
  }
}, mu = (s) => class extends gu(Tr(s)) {
  static get properties() {
    return {
      /**
       * Width of the cells for this column.
       *
       * Please note that using the `em` length unit is discouraged as
       * it might lead to misalignment issues if the header, body, and footer
       * cells have different font sizes. Instead, use `rem` if you need
       * a length unit relative to the font size.
       */
      width: {
        type: String,
        value: "100px",
        sync: !0
      },
      /**
       * Flex grow ratio for the cell widths. When set to 0, cell width is fixed.
       * @attr {number} flex-grow
       * @type {number}
       */
      flexGrow: {
        type: Number,
        value: 1,
        sync: !0
      },
      /**
       * Custom function for rendering the cell content.
       * Receives three arguments:
       *
       * - `root` The cell content DOM element. Append your content to it.
       * - `column` The `<vaadin-grid-column>` element.
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *   - `model.detailsOpened` Details opened state.
       *
       * @type {GridBodyRenderer | null | undefined}
       */
      renderer: {
        type: Function,
        sync: !0
      },
      /**
       * Represents the final renderer computed on the set of observable arguments.
       * It is supposed to be used internally when rendering the content of a body cell.
       *
       * @protected
       * @type {GridBodyRenderer | undefined}
       */
      _renderer: {
        type: Function,
        computed: "_computeRenderer(renderer, __initialized)",
        sync: !0
      },
      /**
       * Path to an item sub-property whose value gets displayed in the column body cells.
       * The property name is also shown in the column header if an explicit header or renderer isn't defined.
       */
      path: {
        type: String,
        sync: !0
      },
      /**
       * Automatically sets the width of the column based on the column contents when this is set to `true`.
       *
       * For performance reasons the column width is calculated automatically only once when the grid items
       * are rendered for the first time and the calculation only considers the rows which are currently
       * rendered in DOM (a bit more than what is currently visible). If the grid is scrolled, or the cell
       * content changes, the column width might not match the contents anymore.
       *
       * Hidden columns are ignored in the calculation and their widths are not automatically updated when
       * you show a column that was initially hidden.
       *
       * You can manually trigger the auto sizing behavior again by calling `grid.recalculateColumnWidths()`.
       *
       * The column width may still grow larger when `flexGrow` is not 0.
       * @attr {boolean} auto-width
       * @type {boolean}
       */
      autoWidth: {
        type: Boolean,
        value: !1
      },
      /**
       * When true, wraps the cell's slot into an element with role="button", and sets
       * the tabindex attribute on the button element, instead of the cell itself.
       * This is needed to keep focus in sync with VoiceOver cursor when navigating
       * with Control + Option + arrow keys: focusing the `<td>` element does not fire
       * a focus event, but focusing an element with role="button" inside a cell fires it.
       * @protected
       */
      _focusButtonMode: {
        type: Boolean,
        value: !1
      },
      /**
       * @type {Array<!HTMLElement>}
       * @protected
       */
      _cells: {
        type: Array,
        sync: !0
      }
    };
  }
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class _u extends mu(yi) {
  static get is() {
    return "vaadin-grid-column";
  }
}
Jt(_u);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Er = Je((s) => typeof s.prototype.addController == "function" ? s : class extends s {
  constructor() {
    super(), this.__controllers = /* @__PURE__ */ new Set();
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback(), this.__controllers.forEach((e) => {
      e.hostConnected && e.hostConnected();
    });
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback(), this.__controllers.forEach((e) => {
      e.hostDisconnected && e.hostDisconnected();
    });
  }
  /**
   * Registers a controller to participate in the element update cycle.
   *
   * @param {ReactiveController} controller
   * @protected
   */
  addController(e) {
    this.__controllers.add(e), this.$ !== void 0 && this.isConnected && e.hostConnected && e.hostConnected();
  }
  /**
   * Removes a controller from the element.
   *
   * @param {ReactiveController} controller
   * @protected
   */
  removeController(e) {
    this.__controllers.delete(e);
  }
}), vu = /\/\*[\*!]\s+vaadin-dev-mode:start([\s\S]*)vaadin-dev-mode:end\s+\*\*\//i, ln = window.Vaadin && window.Vaadin.Flow && window.Vaadin.Flow.clients;
function yu() {
  function s() {
    return !0;
  }
  return Hl(s);
}
function bu() {
  try {
    return wu() ? !0 : xu() ? ln ? !Cu() : !yu() : !1;
  } catch {
    return !1;
  }
}
function wu() {
  return localStorage.getItem("vaadin.developmentmode.force");
}
function xu() {
  return ["localhost", "127.0.0.1"].indexOf(window.location.hostname) >= 0;
}
function Cu() {
  return !!(ln && Object.keys(ln).map((n) => ln[n]).filter((n) => n.productionMode).length > 0);
}
function Hl(s, n) {
  if (typeof s != "function")
    return;
  const e = vu.exec(s.toString());
  if (e)
    try {
      s = new Function(e[1]);
    } catch {
    }
  return s(n);
}
window.Vaadin = window.Vaadin || {};
const Fa = function(s, n) {
  if (window.Vaadin.developmentMode)
    return Hl(s, n);
};
window.Vaadin.developmentMode === void 0 && (window.Vaadin.developmentMode = bu());
function Tu() {
  /*! vaadin-dev-mode:start
    (function () {
  'use strict';
  
  var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) {
    return typeof obj;
  } : function (obj) {
    return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
  };
  
  var classCallCheck = function (instance, Constructor) {
    if (!(instance instanceof Constructor)) {
      throw new TypeError("Cannot call a class as a function");
    }
  };
  
  var createClass = function () {
    function defineProperties(target, props) {
      for (var i = 0; i < props.length; i++) {
        var descriptor = props[i];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
  
    return function (Constructor, protoProps, staticProps) {
      if (protoProps) defineProperties(Constructor.prototype, protoProps);
      if (staticProps) defineProperties(Constructor, staticProps);
      return Constructor;
    };
  }();
  
  var getPolymerVersion = function getPolymerVersion() {
    return window.Polymer && window.Polymer.version;
  };
  
  var StatisticsGatherer = function () {
    function StatisticsGatherer(logger) {
      classCallCheck(this, StatisticsGatherer);
  
      this.now = new Date().getTime();
      this.logger = logger;
    }
  
    createClass(StatisticsGatherer, [{
      key: 'frameworkVersionDetectors',
      value: function frameworkVersionDetectors() {
        return {
          'Flow': function Flow() {
            if (window.Vaadin && window.Vaadin.Flow && window.Vaadin.Flow.clients) {
              var flowVersions = Object.keys(window.Vaadin.Flow.clients).map(function (key) {
                return window.Vaadin.Flow.clients[key];
              }).filter(function (client) {
                return client.getVersionInfo;
              }).map(function (client) {
                return client.getVersionInfo().flow;
              });
              if (flowVersions.length > 0) {
                return flowVersions[0];
              }
            }
          },
          'Vaadin Framework': function VaadinFramework() {
            if (window.vaadin && window.vaadin.clients) {
              var frameworkVersions = Object.values(window.vaadin.clients).filter(function (client) {
                return client.getVersionInfo;
              }).map(function (client) {
                return client.getVersionInfo().vaadinVersion;
              });
              if (frameworkVersions.length > 0) {
                return frameworkVersions[0];
              }
            }
          },
          'AngularJs': function AngularJs() {
            if (window.angular && window.angular.version && window.angular.version) {
              return window.angular.version.full;
            }
          },
          'Angular': function Angular() {
            if (window.ng) {
              var tags = document.querySelectorAll("[ng-version]");
              if (tags.length > 0) {
                return tags[0].getAttribute("ng-version");
              }
              return "Unknown";
            }
          },
          'Backbone.js': function BackboneJs() {
            if (window.Backbone) {
              return window.Backbone.VERSION;
            }
          },
          'React': function React() {
            var reactSelector = '[data-reactroot], [data-reactid]';
            if (!!document.querySelector(reactSelector)) {
              // React does not publish the version by default
              return "unknown";
            }
          },
          'Ember': function Ember() {
            if (window.Em && window.Em.VERSION) {
              return window.Em.VERSION;
            } else if (window.Ember && window.Ember.VERSION) {
              return window.Ember.VERSION;
            }
          },
          'jQuery': function (_jQuery) {
            function jQuery() {
              return _jQuery.apply(this, arguments);
            }
  
            jQuery.toString = function () {
              return _jQuery.toString();
            };
  
            return jQuery;
          }(function () {
            if (typeof jQuery === 'function' && jQuery.prototype.jquery !== undefined) {
              return jQuery.prototype.jquery;
            }
          }),
          'Polymer': function Polymer() {
            var version = getPolymerVersion();
            if (version) {
              return version;
            }
          },
          'LitElement': function LitElement() {
            var version = window.litElementVersions && window.litElementVersions[0];
            if (version) {
              return version;
            }
          },
          'LitHtml': function LitHtml() {
            var version = window.litHtmlVersions && window.litHtmlVersions[0];
            if (version) {
              return version;
            }
          },
          'Vue.js': function VueJs() {
            if (window.Vue) {
              return window.Vue.version;
            }
          }
        };
      }
    }, {
      key: 'getUsedVaadinElements',
      value: function getUsedVaadinElements(elements) {
        var version = getPolymerVersion();
        var elementClasses = void 0;
        // NOTE: In case you edit the code here, YOU MUST UPDATE any statistics reporting code in Flow.
        // Check all locations calling the method getEntries() in
        // https://github.com/vaadin/flow/blob/master/flow-server/src/main/java/com/vaadin/flow/internal/UsageStatistics.java#L106
        // Currently it is only used by BootstrapHandler.
        if (version && version.indexOf('2') === 0) {
          // Polymer 2: components classes are stored in window.Vaadin
          elementClasses = Object.keys(window.Vaadin).map(function (c) {
            return window.Vaadin[c];
          }).filter(function (c) {
            return c.is;
          });
        } else {
          // Polymer 3: components classes are stored in window.Vaadin.registrations
          elementClasses = window.Vaadin.registrations || [];
        }
        elementClasses.forEach(function (klass) {
          var version = klass.version ? klass.version : "0.0.0";
          elements[klass.is] = { version: version };
        });
      }
    }, {
      key: 'getUsedVaadinThemes',
      value: function getUsedVaadinThemes(themes) {
        ['Lumo', 'Material'].forEach(function (themeName) {
          var theme;
          var version = getPolymerVersion();
          if (version && version.indexOf('2') === 0) {
            // Polymer 2: themes are stored in window.Vaadin
            theme = window.Vaadin[themeName];
          } else {
            // Polymer 3: themes are stored in custom element registry
            theme = customElements.get('vaadin-' + themeName.toLowerCase() + '-styles');
          }
          if (theme && theme.version) {
            themes[themeName] = { version: theme.version };
          }
        });
      }
    }, {
      key: 'getFrameworks',
      value: function getFrameworks(frameworks) {
        var detectors = this.frameworkVersionDetectors();
        Object.keys(detectors).forEach(function (framework) {
          var detector = detectors[framework];
          try {
            var version = detector();
            if (version) {
              frameworks[framework] = { version: version };
            }
          } catch (e) {}
        });
      }
    }, {
      key: 'gather',
      value: function gather(storage) {
        var storedStats = storage.read();
        var gatheredStats = {};
        var types = ["elements", "frameworks", "themes"];
  
        types.forEach(function (type) {
          gatheredStats[type] = {};
          if (!storedStats[type]) {
            storedStats[type] = {};
          }
        });
  
        var previousStats = JSON.stringify(storedStats);
  
        this.getUsedVaadinElements(gatheredStats.elements);
        this.getFrameworks(gatheredStats.frameworks);
        this.getUsedVaadinThemes(gatheredStats.themes);
  
        var now = this.now;
        types.forEach(function (type) {
          var keys = Object.keys(gatheredStats[type]);
          keys.forEach(function (key) {
            if (!storedStats[type][key] || _typeof(storedStats[type][key]) != _typeof({})) {
              storedStats[type][key] = { firstUsed: now };
            }
            // Discards any previously logged version number
            storedStats[type][key].version = gatheredStats[type][key].version;
            storedStats[type][key].lastUsed = now;
          });
        });
  
        var newStats = JSON.stringify(storedStats);
        storage.write(newStats);
        if (newStats != previousStats && Object.keys(storedStats).length > 0) {
          this.logger.debug("New stats: " + newStats);
        }
      }
    }]);
    return StatisticsGatherer;
  }();
  
  var StatisticsStorage = function () {
    function StatisticsStorage(key) {
      classCallCheck(this, StatisticsStorage);
  
      this.key = key;
    }
  
    createClass(StatisticsStorage, [{
      key: 'read',
      value: function read() {
        var localStorageStatsString = localStorage.getItem(this.key);
        try {
          return JSON.parse(localStorageStatsString ? localStorageStatsString : '{}');
        } catch (e) {
          return {};
        }
      }
    }, {
      key: 'write',
      value: function write(data) {
        localStorage.setItem(this.key, data);
      }
    }, {
      key: 'clear',
      value: function clear() {
        localStorage.removeItem(this.key);
      }
    }, {
      key: 'isEmpty',
      value: function isEmpty() {
        var storedStats = this.read();
        var empty = true;
        Object.keys(storedStats).forEach(function (key) {
          if (Object.keys(storedStats[key]).length > 0) {
            empty = false;
          }
        });
  
        return empty;
      }
    }]);
    return StatisticsStorage;
  }();
  
  var StatisticsSender = function () {
    function StatisticsSender(url, logger) {
      classCallCheck(this, StatisticsSender);
  
      this.url = url;
      this.logger = logger;
    }
  
    createClass(StatisticsSender, [{
      key: 'send',
      value: function send(data, errorHandler) {
        var logger = this.logger;
  
        if (navigator.onLine === false) {
          logger.debug("Offline, can't send");
          errorHandler();
          return;
        }
        logger.debug("Sending data to " + this.url);
  
        var req = new XMLHttpRequest();
        req.withCredentials = true;
        req.addEventListener("load", function () {
          // Stats sent, nothing more to do
          logger.debug("Response: " + req.responseText);
        });
        req.addEventListener("error", function () {
          logger.debug("Send failed");
          errorHandler();
        });
        req.addEventListener("abort", function () {
          logger.debug("Send aborted");
          errorHandler();
        });
        req.open("POST", this.url);
        req.setRequestHeader("Content-Type", "application/json");
        req.send(data);
      }
    }]);
    return StatisticsSender;
  }();
  
  var StatisticsLogger = function () {
    function StatisticsLogger(id) {
      classCallCheck(this, StatisticsLogger);
  
      this.id = id;
    }
  
    createClass(StatisticsLogger, [{
      key: '_isDebug',
      value: function _isDebug() {
        return localStorage.getItem("vaadin." + this.id + ".debug");
      }
    }, {
      key: 'debug',
      value: function debug(msg) {
        if (this._isDebug()) {
          console.info(this.id + ": " + msg);
        }
      }
    }]);
    return StatisticsLogger;
  }();
  
  var UsageStatistics = function () {
    function UsageStatistics() {
      classCallCheck(this, UsageStatistics);
  
      this.now = new Date();
      this.timeNow = this.now.getTime();
      this.gatherDelay = 10; // Delay between loading this file and gathering stats
      this.initialDelay = 24 * 60 * 60;
  
      this.logger = new StatisticsLogger("statistics");
      this.storage = new StatisticsStorage("vaadin.statistics.basket");
      this.gatherer = new StatisticsGatherer(this.logger);
      this.sender = new StatisticsSender("https://tools.vaadin.com/usage-stats/submit", this.logger);
    }
  
    createClass(UsageStatistics, [{
      key: 'maybeGatherAndSend',
      value: function maybeGatherAndSend() {
        var _this = this;
  
        if (localStorage.getItem(UsageStatistics.optOutKey)) {
          return;
        }
        this.gatherer.gather(this.storage);
        setTimeout(function () {
          _this.maybeSend();
        }, this.gatherDelay * 1000);
      }
    }, {
      key: 'lottery',
      value: function lottery() {
        return true;
      }
    }, {
      key: 'currentMonth',
      value: function currentMonth() {
        return this.now.getYear() * 12 + this.now.getMonth();
      }
    }, {
      key: 'maybeSend',
      value: function maybeSend() {
        var firstUse = Number(localStorage.getItem(UsageStatistics.firstUseKey));
        var monthProcessed = Number(localStorage.getItem(UsageStatistics.monthProcessedKey));
  
        if (!firstUse) {
          // Use a grace period to avoid interfering with tests, incognito mode etc
          firstUse = this.timeNow;
          localStorage.setItem(UsageStatistics.firstUseKey, firstUse);
        }
  
        if (this.timeNow < firstUse + this.initialDelay * 1000) {
          this.logger.debug("No statistics will be sent until the initial delay of " + this.initialDelay + "s has passed");
          return;
        }
        if (this.currentMonth() <= monthProcessed) {
          this.logger.debug("This month has already been processed");
          return;
        }
        localStorage.setItem(UsageStatistics.monthProcessedKey, this.currentMonth());
        // Use random sampling
        if (this.lottery()) {
          this.logger.debug("Congratulations, we have a winner!");
        } else {
          this.logger.debug("Sorry, no stats from you this time");
          return;
        }
  
        this.send();
      }
    }, {
      key: 'send',
      value: function send() {
        // Ensure we have the latest data
        this.gatherer.gather(this.storage);
  
        // Read, send and clean up
        var data = this.storage.read();
        data["firstUse"] = Number(localStorage.getItem(UsageStatistics.firstUseKey));
        data["usageStatisticsVersion"] = UsageStatistics.version;
        var info = 'This request contains usage statistics gathered from the application running in development mode. \n\nStatistics gathering is automatically disabled and excluded from production builds.\n\nFor details and to opt-out, see https://github.com/vaadin/vaadin-usage-statistics.\n\n\n\n';
        var self = this;
        this.sender.send(info + JSON.stringify(data), function () {
          // Revert the 'month processed' flag
          localStorage.setItem(UsageStatistics.monthProcessedKey, self.currentMonth() - 1);
        });
      }
    }], [{
      key: 'version',
      get: function get$1() {
        return '2.1.2';
      }
    }, {
      key: 'firstUseKey',
      get: function get$1() {
        return 'vaadin.statistics.firstuse';
      }
    }, {
      key: 'monthProcessedKey',
      get: function get$1() {
        return 'vaadin.statistics.monthProcessed';
      }
    }, {
      key: 'optOutKey',
      get: function get$1() {
        return 'vaadin.statistics.optout';
      }
    }]);
    return UsageStatistics;
  }();
  
  try {
    window.Vaadin = window.Vaadin || {};
    window.Vaadin.usageStatsChecker = window.Vaadin.usageStatsChecker || new UsageStatistics();
    window.Vaadin.usageStatsChecker.maybeGatherAndSend();
  } catch (e) {
    // Intentionally ignored as this is not a problem in the app being developed
  }
  
  }());
  
    vaadin-dev-mode:end **/
}
const Eu = function() {
  if (typeof Fa == "function")
    return Fa(Tu);
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
window.Vaadin || (window.Vaadin = {});
window.Vaadin.registrations || (window.Vaadin.registrations = []);
window.Vaadin.developmentModeCallback || (window.Vaadin.developmentModeCallback = {});
window.Vaadin.developmentModeCallback["vaadin-usage-statistics"] = function() {
  Eu();
};
let bo;
const La = /* @__PURE__ */ new Set(), Vl = (s) => class extends Tr(s) {
  /** @protected */
  static finalize() {
    super.finalize();
    const { is: e } = this;
    e && !La.has(e) && (window.Vaadin.registrations.push(this), La.add(e), window.Vaadin.developmentModeCallback && (bo = ze.debounce(bo, Ml, () => {
      window.Vaadin.developmentModeCallback["vaadin-usage-statistics"]();
    }), Nl(bo)));
  }
  constructor() {
    super(), document.doctype;
  }
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
let ps = !1;
window.addEventListener(
  "keydown",
  () => {
    ps = !0;
  },
  { capture: !0 }
);
window.addEventListener(
  "mousedown",
  () => {
    ps = !1;
  },
  { capture: !0 }
);
function jo() {
  let s = document.activeElement || document.body;
  for (; s.shadowRoot && s.shadowRoot.activeElement; )
    s = s.shadowRoot.activeElement;
  return s;
}
function Ln() {
  return ps;
}
function Ul(s) {
  const n = s.style;
  if (n.visibility === "hidden" || n.display === "none")
    return !0;
  const e = window.getComputedStyle(s);
  return e.visibility === "hidden" || e.display === "none";
}
function Pu(s, n) {
  const e = Math.max(s.tabIndex, 0), t = Math.max(n.tabIndex, 0);
  return e === 0 || t === 0 ? t > e : e > t;
}
function Su(s, n) {
  const e = [];
  for (; s.length > 0 && n.length > 0; )
    Pu(s[0], n[0]) ? e.push(n.shift()) : e.push(s.shift());
  return e.concat(s, n);
}
function Go(s) {
  const n = s.length;
  if (n < 2)
    return s;
  const e = Math.ceil(n / 2), t = Go(s.slice(0, e)), i = Go(s.slice(e));
  return Su(t, i);
}
function Tn(s) {
  return s.offsetParent === null && s.clientWidth === 0 && s.clientHeight === 0 ? !0 : Ul(s);
}
function gs(s) {
  return s.matches('[tabindex="-1"]') ? !1 : s.matches("input, select, textarea, button, object") ? s.matches(":not([disabled])") : s.matches("a[href], area[href], iframe, [tabindex], [contentEditable]");
}
function Wl(s) {
  return s.getRootNode().activeElement === s;
}
function Ru(s) {
  if (!gs(s))
    return -1;
  const n = s.getAttribute("tabindex") || 0;
  return Number(n);
}
function jl(s, n) {
  if (s.nodeType !== Node.ELEMENT_NODE || Ul(s))
    return !1;
  const e = (
    /** @type {HTMLElement} */
    s
  ), t = Ru(e);
  let i = t > 0;
  t >= 0 && n.push(e);
  let r = [];
  return e.localName === "slot" ? r = e.assignedNodes({ flatten: !0 }) : r = (e.shadowRoot || e).children, [...r].forEach((o) => {
    i = jl(o, n) || i;
  }), i;
}
function Au(s) {
  const n = [];
  return jl(s, n) ? Go(n) : n;
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Gl = Je(
  (s) => class extends s {
    static get properties() {
      return {
        /**
         * If true, the user cannot interact with this element.
         */
        disabled: {
          type: Boolean,
          value: !1,
          observer: "_disabledChanged",
          reflectToAttribute: !0
        }
      };
    }
    /**
     * @param {boolean} disabled
     * @protected
     */
    _disabledChanged(e) {
      this._setAriaDisabled(e);
    }
    /**
     * @param {boolean} disabled
     * @protected
     */
    _setAriaDisabled(e) {
      e ? this.setAttribute("aria-disabled", "true") : this.removeAttribute("aria-disabled");
    }
    /**
     * Overrides the default element `click` method in order to prevent
     * firing the `click` event when the element is disabled.
     * @protected
     * @override
     */
    click() {
      this.disabled || super.click();
    }
  }
);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const ql = (s) => class extends Gl(s) {
  static get properties() {
    return {
      /**
       * Indicates whether the element can be focused and where it participates in sequential keyboard navigation.
       *
       * @protected
       */
      tabindex: {
        type: Number,
        reflectToAttribute: !0,
        observer: "_tabindexChanged"
      },
      /**
       * Stores the last known tabindex since the element has been disabled.
       *
       * @protected
       */
      _lastTabIndex: {
        type: Number
      }
    };
  }
  /**
   * When the element gets disabled, the observer saves the last known tabindex
   * and makes the element not focusable by setting tabindex to -1.
   * As soon as the element gets enabled, the observer restores the last known tabindex
   * so that the element can be focusable again.
   *
   * @protected
   * @override
   */
  _disabledChanged(e, t) {
    super._disabledChanged(e, t), e ? (this.tabindex !== void 0 && (this._lastTabIndex = this.tabindex), this.tabindex = -1) : t && (this.tabindex = this._lastTabIndex);
  }
  /**
   * When the user has changed tabindex while the element is disabled,
   * the observer reverts tabindex to -1 and rather saves the new tabindex value to apply it later.
   * The new value will be applied as soon as the element becomes enabled.
   *
   * @protected
   */
  _tabindexChanged(e) {
    this.disabled && e !== -1 && (this._lastTabIndex = e, this.tabindex = -1);
  }
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Mn = (s) => s.test(navigator.userAgent), qo = (s) => s.test(navigator.platform), ku = (s) => s.test(navigator.vendor), Ma = Mn(/Android/u), Iu = Mn(/Chrome/u) && ku(/Google Inc/u), Du = Mn(/Firefox/u), Ou = qo(/^iPad/u) || qo(/^Mac/u) && navigator.maxTouchPoints > 1, zu = qo(/^iPhone/u), Ko = zu || Ou, Kl = Mn(/^((?!chrome|android).)*safari/iu), br = (() => {
  try {
    return document.createEvent("TouchEvent"), !0;
  } catch {
    return !1;
  }
})();
/**
 * @license
 * Copyright (c) 2023 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Zl {
  constructor(n, e) {
    this.slot = n, this.callback = e, this._storedNodes = [], this._connected = !1, this._scheduled = !1, this._boundSchedule = () => {
      this._schedule();
    }, this.connect(), this._schedule();
  }
  /**
   * Activates an observer. This method is automatically called when
   * a `SlotObserver` is created. It should only be called to  re-activate
   * an observer that has been deactivated via the `disconnect` method.
   */
  connect() {
    this.slot.addEventListener("slotchange", this._boundSchedule), this._connected = !0;
  }
  /**
   * Deactivates the observer. After calling this method the observer callback
   * will not be called when changes to slotted nodes occur. The `connect` method
   * may be subsequently called to reactivate the observer.
   */
  disconnect() {
    this.slot.removeEventListener("slotchange", this._boundSchedule), this._connected = !1;
  }
  /** @private */
  _schedule() {
    this._scheduled || (this._scheduled = !0, queueMicrotask(() => {
      this.flush();
    }));
  }
  /**
   * Run the observer callback synchronously.
   */
  flush() {
    this._connected && (this._scheduled = !1, this._processNodes());
  }
  /** @private */
  _processNodes() {
    const n = this.slot.assignedNodes({ flatten: !0 });
    let e = [];
    const t = [], i = [];
    n.length && (e = n.filter((r) => !this._storedNodes.includes(r))), this._storedNodes.length && this._storedNodes.forEach((r, o) => {
      const l = n.indexOf(r);
      l === -1 ? t.push(r) : l !== o && i.push(r);
    }), (e.length || t.length || i.length) && this.callback({ addedNodes: e, currentNodes: n, movedNodes: i, removedNodes: t }), this._storedNodes = n;
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
let Fu = 0;
function Yl() {
  return Fu++;
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class ms extends EventTarget {
  /**
   * Ensure that every instance has unique ID.
   *
   * @param {HTMLElement} host
   * @param {string} slotName
   * @return {string}
   * @protected
   */
  static generateId(n, e = "default") {
    return `${e}-${n.localName}-${Yl()}`;
  }
  constructor(n, e, t, i = {}) {
    super();
    const { initializer: r, multiple: o, observe: l, useUniqueId: a, uniqueIdPrefix: c } = i;
    this.host = n, this.slotName = e, this.tagName = t, this.observe = typeof l == "boolean" ? l : !0, this.multiple = typeof o == "boolean" ? o : !1, this.slotInitializer = r, o && (this.nodes = []), a && (this.defaultId = this.constructor.generateId(n, c || e));
  }
  hostConnected() {
    this.initialized || (this.multiple ? this.initMultiple() : this.initSingle(), this.observe && this.observeSlot(), this.initialized = !0);
  }
  /** @protected */
  initSingle() {
    let n = this.getSlotChild();
    n ? (this.node = n, this.initAddedNode(n)) : (n = this.attachDefaultNode(), this.initNode(n));
  }
  /** @protected */
  initMultiple() {
    const n = this.getSlotChildren();
    if (n.length === 0) {
      const e = this.attachDefaultNode();
      e && (this.nodes = [e], this.initNode(e));
    } else
      this.nodes = n, n.forEach((e) => {
        this.initAddedNode(e);
      });
  }
  /**
   * Create and attach default node using the provided tag name, if any.
   * @return {Node | undefined}
   * @protected
   */
  attachDefaultNode() {
    const { host: n, slotName: e, tagName: t } = this;
    let i = this.defaultNode;
    return !i && t && (i = document.createElement(t), i instanceof Element && (e !== "" && i.setAttribute("slot", e), this.defaultNode = i)), i && (this.node = i, n.appendChild(i)), i;
  }
  /**
   * Return the list of nodes matching the slot managed by the controller.
   * @return {Node}
   */
  getSlotChildren() {
    const { slotName: n } = this;
    return Array.from(this.host.childNodes).filter((e) => e.nodeType === Node.ELEMENT_NODE && e.slot === n || e.nodeType === Node.TEXT_NODE && e.textContent.trim() && n === "");
  }
  /**
   * Return a reference to the node managed by the controller.
   * @return {Node}
   */
  getSlotChild() {
    return this.getSlotChildren()[0];
  }
  /**
   * Run `slotInitializer` for the node managed by the controller.
   *
   * @param {Node} node
   * @protected
   */
  initNode(n) {
    const { slotInitializer: e } = this;
    e && e(n, this.host);
  }
  /**
   * Override to initialize the newly added custom node.
   *
   * @param {Node} _node
   * @protected
   */
  initCustomNode(n) {
  }
  /**
   * Override to teardown slotted node when it's removed.
   *
   * @param {Node} _node
   * @protected
   */
  teardownNode(n) {
  }
  /**
   * Run both `initCustomNode` and `initNode` for a custom slotted node.
   *
   * @param {Node} node
   * @protected
   */
  initAddedNode(n) {
    n !== this.defaultNode && (this.initCustomNode(n), this.initNode(n));
  }
  /**
   * Setup the observer to manage slot content changes.
   * @protected
   */
  observeSlot() {
    const { slotName: n } = this, e = n === "" ? "slot:not([name])" : `slot[name=${n}]`, t = this.host.shadowRoot.querySelector(e);
    this.__slotObserver = new Zl(t, ({ addedNodes: i, removedNodes: r }) => {
      const o = this.multiple ? this.nodes : [this.node], l = i.filter((a) => !fu(a) && !o.includes(a));
      r.length && (this.nodes = o.filter((a) => !r.includes(a)), r.forEach((a) => {
        this.teardownNode(a);
      })), l && l.length > 0 && (this.multiple ? (this.defaultNode && this.defaultNode.remove(), this.nodes = [...o, ...l].filter((a) => a !== this.defaultNode), l.forEach((a) => {
        this.initAddedNode(a);
      })) : (this.node && this.node.remove(), this.node = l[0], this.initAddedNode(this.node)));
    });
  }
}
/**
 * @license
 * Copyright (c) 2022 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Xl extends ms {
  constructor(n) {
    super(n, "tooltip"), this.setTarget(n);
  }
  /**
   * Override to initialize the newly added custom tooltip.
   *
   * @param {Node} tooltipNode
   * @protected
   * @override
   */
  initCustomNode(n) {
    n.target = this.target, this.ariaTarget !== void 0 && (n.ariaTarget = this.ariaTarget), this.context !== void 0 && (n.context = this.context), this.manual !== void 0 && (n.manual = this.manual), this.opened !== void 0 && (n.opened = this.opened), this.position !== void 0 && (n._position = this.position), this.shouldShow !== void 0 && (n.shouldShow = this.shouldShow), this.__notifyChange();
  }
  /**
   * Override to notify the host when the tooltip is removed.
   *
   * @param {Node} tooltipNode
   * @protected
   * @override
   */
  teardownNode() {
    this.__notifyChange();
  }
  /**
   * Set an HTML element for linking with the tooltip overlay
   * via `aria-describedby` attribute used by screen readers.
   * @param {HTMLElement} ariaTarget
   */
  setAriaTarget(n) {
    this.ariaTarget = n;
    const e = this.node;
    e && (e.ariaTarget = n);
  }
  /**
   * Set a context object to be used by generator.
   * @param {object} context
   */
  setContext(n) {
    this.context = n;
    const e = this.node;
    e && (e.context = n);
  }
  /**
   * Toggle manual state on the slotted tooltip.
   * @param {boolean} manual
   */
  setManual(n) {
    this.manual = n;
    const e = this.node;
    e && (e.manual = n);
  }
  /**
   * Toggle opened state on the slotted tooltip.
   * @param {boolean} opened
   */
  setOpened(n) {
    this.opened = n;
    const e = this.node;
    e && (e.opened = n);
  }
  /**
   * Set default position for the slotted tooltip.
   * This can be overridden by setting the position
   * using corresponding property or attribute.
   * @param {string} position
   */
  setPosition(n) {
    this.position = n;
    const e = this.node;
    e && (e._position = n);
  }
  /**
   * Set function used to detect whether to show
   * the tooltip based on a condition.
   * @param {Function} shouldShow
   */
  setShouldShow(n) {
    this.shouldShow = n;
    const e = this.node;
    e && (e.shouldShow = n);
  }
  /**
   * Set an HTML element to attach the tooltip to.
   * @param {HTMLElement} target
   */
  setTarget(n) {
    this.target = n;
    const e = this.node;
    e && (e.target = n);
  }
  /** @private */
  __notifyChange() {
    this.dispatchEvent(new CustomEvent("tooltip-changed", { detail: { node: this.node } }));
  }
}
/**
 * @license
 * Copyright (c) 2016 The Polymer Project Authors. All rights reserved.
 * This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
 * The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
 * The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
 * Code distributed by Google as part of the polymer project is also
 * subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
 */
const Na = navigator.userAgent.match(/iP(?:hone|ad;(?: U;)? CPU) OS (\d+)/u), Lu = Na && Na[1] >= 8, Ba = 3, Mu = {
  /**
   * The ratio of hidden tiles that should remain in the scroll direction.
   * Recommended value ~0.5, so it will distribute tiles evenly in both
   * directions.
   */
  _ratio: 0.5,
  /**
   * The padding-top value for the list.
   */
  _scrollerPaddingTop: 0,
  /**
   * This value is a cached value of `scrollTop` from the last `scroll` event.
   */
  _scrollPosition: 0,
  /**
   * The sum of the heights of all the tiles in the DOM.
   */
  _physicalSize: 0,
  /**
   * The average `offsetHeight` of the tiles observed till now.
   */
  _physicalAverage: 0,
  /**
   * The number of tiles which `offsetHeight` > 0 observed until now.
   */
  _physicalAverageCount: 0,
  /**
   * The Y position of the item rendered in the `_physicalStart`
   * tile relative to the scrolling list.
   */
  _physicalTop: 0,
  /**
   * The number of items in the list.
   */
  _virtualCount: 0,
  /**
   * The estimated scroll height based on `_physicalAverage`
   */
  _estScrollHeight: 0,
  /**
   * The scroll height of the dom node
   */
  _scrollHeight: 0,
  /**
   * The height of the list. This is referred as the viewport in the context of
   * list.
   */
  _viewportHeight: 0,
  /**
   * The width of the list. This is referred as the viewport in the context of
   * list.
   */
  _viewportWidth: 0,
  /**
   * An array of DOM nodes that are currently in the tree
   * @type {?Array<!HTMLElement>}
   */
  _physicalItems: null,
  /**
   * An array of heights for each item in `_physicalItems`
   * @type {?Array<number>}
   */
  _physicalSizes: null,
  /**
   * A cached value for the first visible index.
   * See `firstVisibleIndex`
   * @type {?number}
   */
  _firstVisibleIndexVal: null,
  /**
   * A cached value for the last visible index.
   * See `lastVisibleIndex`
   * @type {?number}
   */
  _lastVisibleIndexVal: null,
  /**
   * The max number of pages to render. One page is equivalent to the height of
   * the list.
   */
  _maxPages: 2,
  /**
   * The cost of stamping a template in ms.
   */
  _templateCost: 0,
  /**
   * The bottom of the physical content.
   */
  get _physicalBottom() {
    return this._physicalTop + this._physicalSize;
  },
  /**
   * The bottom of the scroll.
   */
  get _scrollBottom() {
    return this._scrollPosition + this._viewportHeight;
  },
  /**
   * The n-th item rendered in the last physical item.
   */
  get _virtualEnd() {
    return this._virtualStart + this._physicalCount - 1;
  },
  /**
   * The height of the physical content that isn't on the screen.
   */
  get _hiddenContentSize() {
    return this._physicalSize - this._viewportHeight;
  },
  /**
   * The maximum scroll top value.
   */
  get _maxScrollTop() {
    return this._estScrollHeight - this._viewportHeight + this._scrollOffset;
  },
  /**
   * The largest n-th value for an item such that it can be rendered in
   * `_physicalStart`.
   */
  get _maxVirtualStart() {
    const s = this._virtualCount;
    return Math.max(0, s - this._physicalCount);
  },
  get _virtualStart() {
    return this._virtualStartVal || 0;
  },
  set _virtualStart(s) {
    s = this._clamp(s, 0, this._maxVirtualStart), this._virtualStartVal = s;
  },
  get _physicalStart() {
    return this._physicalStartVal || 0;
  },
  /**
   * The k-th tile that is at the top of the scrolling list.
   */
  set _physicalStart(s) {
    s %= this._physicalCount, s < 0 && (s = this._physicalCount + s), this._physicalStartVal = s;
  },
  /**
   * The k-th tile that is at the bottom of the scrolling list.
   */
  get _physicalEnd() {
    return (this._physicalStart + this._physicalCount - 1) % this._physicalCount;
  },
  get _physicalCount() {
    return this._physicalCountVal || 0;
  },
  set _physicalCount(s) {
    this._physicalCountVal = s;
  },
  /**
   * An optimal physical size such that we will have enough physical items
   * to fill up the viewport and recycle when the user scrolls.
   *
   * This default value assumes that we will at least have the equivalent
   * to a viewport of physical items above and below the user's viewport.
   */
  get _optPhysicalSize() {
    return this._viewportHeight === 0 ? 1 / 0 : this._viewportHeight * this._maxPages;
  },
  /**
   * True if the current list is visible.
   */
  get _isVisible() {
    return !!(this.offsetWidth || this.offsetHeight);
  },
  /**
   * Gets the index of the first visible item in the viewport.
   *
   * @type {number}
   */
  get firstVisibleIndex() {
    let s = this._firstVisibleIndexVal;
    if (s == null) {
      let n = this._physicalTop + this._scrollOffset;
      s = this._iterateItems((e, t) => {
        if (n += this._getPhysicalSizeIncrement(e), n > this._scrollPosition)
          return t;
      }) || 0, this._firstVisibleIndexVal = s;
    }
    return s;
  },
  /**
   * Gets the index of the last visible item in the viewport.
   *
   * @type {number}
   */
  get lastVisibleIndex() {
    let s = this._lastVisibleIndexVal;
    if (s == null) {
      let n = this._physicalTop + this._scrollOffset;
      this._iterateItems((e, t) => {
        n < this._scrollBottom && (s = t), n += this._getPhysicalSizeIncrement(e);
      }), this._lastVisibleIndexVal = s;
    }
    return s;
  },
  get _scrollOffset() {
    return this._scrollerPaddingTop + this.scrollOffset;
  },
  /**
   * Recycles the physical items when needed.
   */
  _scrollHandler() {
    const s = Math.max(0, Math.min(this._maxScrollTop, this._scrollTop));
    let n = s - this._scrollPosition;
    const e = n >= 0;
    if (this._scrollPosition = s, this._firstVisibleIndexVal = null, this._lastVisibleIndexVal = null, Math.abs(n) > this._physicalSize && this._physicalSize > 0) {
      n -= this._scrollOffset;
      const t = Math.round(n / this._physicalAverage);
      this._virtualStart += t, this._physicalStart += t, this._physicalTop = Math.min(Math.floor(this._virtualStart) * this._physicalAverage, this._scrollPosition), this._update();
    } else if (this._physicalCount > 0) {
      const t = this._getReusables(e);
      e ? (this._physicalTop = t.physicalTop, this._virtualStart += t.indexes.length, this._physicalStart += t.indexes.length) : (this._virtualStart -= t.indexes.length, this._physicalStart -= t.indexes.length), this._update(t.indexes, e ? null : t.indexes), this._debounce("_increasePoolIfNeeded", this._increasePoolIfNeeded.bind(this, 0), yt);
    }
  },
  /**
   * Returns an object that contains the indexes of the physical items
   * that might be reused and the physicalTop.
   *
   * @param {boolean} fromTop If the potential reusable items are above the scrolling region.
   */
  _getReusables(s) {
    let n, e, t;
    const i = [], r = this._hiddenContentSize * this._ratio, o = this._virtualStart, l = this._virtualEnd, a = this._physicalCount;
    let c = this._physicalTop + this._scrollOffset;
    const d = this._physicalBottom + this._scrollOffset, f = this._scrollPosition, p = this._scrollBottom;
    for (s ? (n = this._physicalStart, e = f - c) : (n = this._physicalEnd, e = d - p); t = this._getPhysicalSizeIncrement(n), e -= t, !(i.length >= a || e <= r); )
      if (s) {
        if (l + i.length + 1 >= this._virtualCount || c + t >= f - this._scrollOffset)
          break;
        i.push(n), c += t, n = (n + 1) % a;
      } else {
        if (o - i.length <= 0 || c + this._physicalSize - t <= p)
          break;
        i.push(n), c -= t, n = n === 0 ? a - 1 : n - 1;
      }
    return { indexes: i, physicalTop: c - this._scrollOffset };
  },
  /**
   * Update the list of items, starting from the `_virtualStart` item.
   * @param {!Array<number>=} itemSet
   * @param {!Array<number>=} movingUp
   */
  _update(s, n) {
    if (!(s && s.length === 0 || this._physicalCount === 0)) {
      if (this._assignModels(s), this._updateMetrics(s), n)
        for (; n.length; ) {
          const e = n.pop();
          this._physicalTop -= this._getPhysicalSizeIncrement(e);
        }
      this._positionItems(), this._updateScrollerSize();
    }
  },
  _isClientFull() {
    return this._scrollBottom !== 0 && this._physicalBottom - 1 >= this._scrollBottom && this._physicalTop <= this._scrollPosition;
  },
  /**
   * Increases the pool size.
   */
  _increasePoolIfNeeded(s) {
    const e = this._clamp(
      this._physicalCount + s,
      Ba,
      this._virtualCount - this._virtualStart
    ) - this._physicalCount;
    let t = Math.round(this._physicalCount * 0.5);
    if (!(e < 0)) {
      if (e > 0) {
        const i = window.performance.now();
        [].push.apply(this._physicalItems, this._createPool(e));
        for (let r = 0; r < e; r++)
          this._physicalSizes.push(0);
        this._physicalCount += e, this._physicalStart > this._physicalEnd && this._isIndexRendered(this._focusedVirtualIndex) && this._getPhysicalIndex(this._focusedVirtualIndex) < this._physicalEnd && (this._physicalStart += e), this._update(), this._templateCost = (window.performance.now() - i) / e, t = Math.round(this._physicalCount * 0.5);
      }
      this._virtualEnd >= this._virtualCount - 1 || t === 0 || (this._isClientFull() ? this._physicalSize < this._optPhysicalSize && this._debounce(
        "_increasePoolIfNeeded",
        this._increasePoolIfNeeded.bind(this, this._clamp(Math.round(50 / this._templateCost), 1, t)),
        Ml
      ) : this._debounce("_increasePoolIfNeeded", this._increasePoolIfNeeded.bind(this, t), yt));
    }
  },
  /**
   * Renders the a new list.
   */
  _render() {
    if (!(!this.isAttached || !this._isVisible))
      if (this._physicalCount !== 0) {
        const s = this._getReusables(!0);
        this._physicalTop = s.physicalTop, this._virtualStart += s.indexes.length, this._physicalStart += s.indexes.length, this._update(s.indexes), this._update(), this._increasePoolIfNeeded(0);
      } else this._virtualCount > 0 && (this.updateViewportBoundaries(), this._increasePoolIfNeeded(Ba));
  },
  /**
   * Called when the items have changed. That is, reassignments
   * to `items`, splices or updates to a single item.
   */
  _itemsChanged(s) {
    s.path === "items" && (this._virtualStart = 0, this._physicalTop = 0, this._virtualCount = this.items ? this.items.length : 0, this._physicalIndexForKey = {}, this._firstVisibleIndexVal = null, this._lastVisibleIndexVal = null, this._physicalItems || (this._physicalItems = []), this._physicalSizes || (this._physicalSizes = []), this._physicalStart = 0, this._scrollTop > this._scrollOffset && this._resetScrollPosition(0), this._debounce("_render", this._render, Xt));
  },
  /**
   * Executes a provided function per every physical index in `itemSet`
   * `itemSet` default value is equivalent to the entire set of physical
   * indexes.
   *
   * @param {!function(number, number)} fn
   * @param {!Array<number>=} itemSet
   */
  _iterateItems(s, n) {
    let e, t, i, r;
    if (arguments.length === 2 && n) {
      for (r = 0; r < n.length; r++)
        if (e = n[r], t = this._computeVidx(e), (i = s.call(this, e, t)) != null)
          return i;
    } else {
      for (e = this._physicalStart, t = this._virtualStart; e < this._physicalCount; e++, t++)
        if ((i = s.call(this, e, t)) != null)
          return i;
      for (e = 0; e < this._physicalStart; e++, t++)
        if ((i = s.call(this, e, t)) != null)
          return i;
    }
  },
  /**
   * Returns the virtual index for a given physical index
   *
   * @param {number} pidx Physical index
   * @return {number}
   */
  _computeVidx(s) {
    return s >= this._physicalStart ? this._virtualStart + (s - this._physicalStart) : this._virtualStart + (this._physicalCount - this._physicalStart) + s;
  },
  /**
   * Updates the position of the physical items.
   */
  _positionItems() {
    this._adjustScrollPosition();
    let s = this._physicalTop;
    this._iterateItems((n) => {
      this.translate3d(0, `${s}px`, 0, this._physicalItems[n]), s += this._physicalSizes[n];
    });
  },
  _getPhysicalSizeIncrement(s) {
    return this._physicalSizes[s];
  },
  /**
   * Adjusts the scroll position when it was overestimated.
   */
  _adjustScrollPosition() {
    const s = this._virtualStart === 0 ? this._physicalTop : Math.min(this._scrollPosition + this._physicalTop, 0);
    if (s !== 0) {
      this._physicalTop -= s;
      const n = this._scrollPosition;
      !Lu && n > 0 && this._resetScrollPosition(n - s);
    }
  },
  /**
   * Sets the position of the scroll.
   */
  _resetScrollPosition(s) {
    this.scrollTarget && s >= 0 && (this._scrollTop = s, this._scrollPosition = this._scrollTop);
  },
  /**
   * Sets the scroll height, that's the height of the content,
   *
   * @param {boolean=} forceUpdate If true, updates the height no matter what.
   */
  _updateScrollerSize(s) {
    const n = this._physicalBottom + Math.max(this._virtualCount - this._physicalCount - this._virtualStart, 0) * this._physicalAverage;
    this._estScrollHeight = n, (s || this._scrollHeight === 0 || this._scrollPosition >= n - this._physicalSize || Math.abs(n - this._scrollHeight) >= this._viewportHeight) && (this.$.items.style.height = `${n}px`, this._scrollHeight = n);
  },
  /**
   * Scroll to a specific index in the virtual list regardless
   * of the physical items in the DOM tree.
   *
   * @method scrollToIndex
   * @param {number} idx The index of the item
   */
  scrollToIndex(s) {
    if (typeof s != "number" || s < 0 || s > this.items.length - 1 || (cr(), this._physicalCount === 0))
      return;
    s = this._clamp(s, 0, this._virtualCount - 1), (!this._isIndexRendered(s) || s >= this._maxVirtualStart) && (this._virtualStart = s - 1), this._assignModels(), this._updateMetrics(), this._physicalTop = this._virtualStart * this._physicalAverage;
    let n = this._physicalStart, e = this._virtualStart, t = 0;
    const i = this._hiddenContentSize;
    for (; e < s && t <= i; )
      t += this._getPhysicalSizeIncrement(n), n = (n + 1) % this._physicalCount, e += 1;
    this._updateScrollerSize(!0), this._positionItems(), this._resetScrollPosition(this._physicalTop + this._scrollOffset + t), this._increasePoolIfNeeded(0), this._firstVisibleIndexVal = null, this._lastVisibleIndexVal = null;
  },
  /**
   * Reset the physical average and the average count.
   */
  _resetAverage() {
    this._physicalAverage = 0, this._physicalAverageCount = 0;
  },
  /**
   * A handler for the `iron-resize` event triggered by `IronResizableBehavior`
   * when the element is resized.
   */
  _resizeHandler() {
    this._debounce(
      "_render",
      () => {
        this._firstVisibleIndexVal = null, this._lastVisibleIndexVal = null, this._isVisible ? (this.updateViewportBoundaries(), this.toggleScrollListener(!0), this._resetAverage(), this._render()) : this.toggleScrollListener(!1);
      },
      Xt
    );
  },
  _isIndexRendered(s) {
    return s >= this._virtualStart && s <= this._virtualEnd;
  },
  _getPhysicalIndex(s) {
    return (this._physicalStart + (s - this._virtualStart)) % this._physicalCount;
  },
  _clamp(s, n, e) {
    return Math.min(e, Math.max(n, s));
  },
  _debounce(s, n, e) {
    this._debouncers || (this._debouncers = {}), this._debouncers[s] = ze.debounce(this._debouncers[s], e, n.bind(this)), Nl(this._debouncers[s]);
  }
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Nu = 1e5, wo = 1e3;
class Ql {
  constructor({ createElements: n, updateElement: e, scrollTarget: t, scrollContainer: i, elementsContainer: r, reorderElements: o }) {
    this.isAttached = !0, this._vidxOffset = 0, this.createElements = n, this.updateElement = e, this.scrollTarget = t, this.scrollContainer = i, this.elementsContainer = r || i, this.reorderElements = o, this._maxPages = 1.3, this.__placeholderHeight = 200, this.__elementHeightQueue = Array(10), this.timeouts = {
      SCROLL_REORDER: 500,
      IGNORE_WHEEL: 500,
      FIX_INVALID_ITEM_POSITIONING: 100
    }, this.__resizeObserver = new ResizeObserver(() => this._resizeHandler()), getComputedStyle(this.scrollTarget).overflow === "visible" && (this.scrollTarget.style.overflow = "auto"), getComputedStyle(this.scrollContainer).position === "static" && (this.scrollContainer.style.position = "relative"), this.__resizeObserver.observe(this.scrollTarget), this.scrollTarget.addEventListener("scroll", () => this._scrollHandler()), this._scrollLineHeight = this._getScrollLineHeight(), this.scrollTarget.addEventListener("wheel", (l) => this.__onWheel(l)), this.scrollTarget.addEventListener("virtualizer-element-focused", (l) => this.__onElementFocused(l)), this.elementsContainer.addEventListener("focusin", () => {
      this.scrollTarget.dispatchEvent(
        new CustomEvent("virtualizer-element-focused", { detail: { element: this.__getFocusedElement() } })
      );
    }), this.reorderElements && (this.scrollTarget.addEventListener("mousedown", () => {
      this.__mouseDown = !0;
    }), this.scrollTarget.addEventListener("mouseup", () => {
      this.__mouseDown = !1, this.__pendingReorder && this.__reorderElements();
    }));
  }
  get scrollOffset() {
    return 0;
  }
  get adjustedFirstVisibleIndex() {
    return this.firstVisibleIndex + this._vidxOffset;
  }
  get adjustedLastVisibleIndex() {
    return this.lastVisibleIndex + this._vidxOffset;
  }
  get _maxVirtualIndexOffset() {
    return this.size - this._virtualCount;
  }
  __hasPlaceholders() {
    return this.__getVisibleElements().some((n) => n.__virtualizerPlaceholder);
  }
  scrollToIndex(n) {
    if (typeof n != "number" || isNaN(n) || this.size === 0 || !this.scrollTarget.offsetHeight)
      return;
    delete this.__pendingScrollToIndex, this._physicalCount <= 3 && this.flush(), n = this._clamp(n, 0, this.size - 1);
    const e = this.__getVisibleElements().length;
    let t = Math.floor(n / this.size * this._virtualCount);
    this._virtualCount - t < e ? (t = this._virtualCount - (this.size - n), this._vidxOffset = this._maxVirtualIndexOffset) : t < e ? n < wo ? (t = n, this._vidxOffset = 0) : (t = wo, this._vidxOffset = n - t) : this._vidxOffset = n - t, this.__skipNextVirtualIndexAdjust = !0, super.scrollToIndex(t), this.adjustedFirstVisibleIndex !== n && this._scrollTop < this._maxScrollTop && !this.grid && (this._scrollTop -= this.__getIndexScrollOffset(n) || 0), this._scrollHandler(), this.__hasPlaceholders() && (this.__pendingScrollToIndex = n);
  }
  flush() {
    this.scrollTarget.offsetHeight !== 0 && (this._resizeHandler(), cr(), this._scrollHandler(), this.__fixInvalidItemPositioningDebouncer && this.__fixInvalidItemPositioningDebouncer.flush(), this.__scrollReorderDebouncer && this.__scrollReorderDebouncer.flush(), this.__debouncerWheelAnimationFrame && this.__debouncerWheelAnimationFrame.flush());
  }
  update(n = 0, e = this.size - 1) {
    const t = [];
    this.__getVisibleElements().forEach((i) => {
      i.__virtualIndex >= n && i.__virtualIndex <= e && (this.__updateElement(i, i.__virtualIndex, !0), t.push(i));
    }), this.__afterElementsUpdated(t);
  }
  /**
   * Updates the height for a given set of items.
   *
   * @param {!Array<number>=} itemSet
   */
  _updateMetrics(n) {
    cr();
    let e = 0, t = 0;
    const i = this._physicalAverageCount, r = this._physicalAverage;
    this._iterateItems((o, l) => {
      t += this._physicalSizes[o], this._physicalSizes[o] = Math.ceil(this.__getBorderBoxHeight(this._physicalItems[o])), e += this._physicalSizes[o], this._physicalAverageCount += this._physicalSizes[o] ? 1 : 0;
    }, n), this._physicalSize = this._physicalSize + e - t, this._physicalAverageCount !== i && (this._physicalAverage = Math.round(
      (r * i + e) / this._physicalAverageCount
    ));
  }
  __getBorderBoxHeight(n) {
    const e = getComputedStyle(n), t = parseFloat(e.height) || 0;
    if (e.boxSizing === "border-box")
      return t;
    const i = parseFloat(e.paddingBottom) || 0, r = parseFloat(e.paddingTop) || 0, o = parseFloat(e.borderBottomWidth) || 0, l = parseFloat(e.borderTopWidth) || 0;
    return t + i + r + o + l;
  }
  __updateElement(n, e, t) {
    n.__virtualizerPlaceholder && (n.style.paddingTop = "", n.style.opacity = "", n.__virtualizerPlaceholder = !1), !this.__preventElementUpdates && (n.__lastUpdatedIndex !== e || t) && (this.updateElement(n, e), n.__lastUpdatedIndex = e);
  }
  /**
   * Called synchronously right after elements have been updated.
   * This is a good place to do any post-update work.
   *
   * @param {!Array<!HTMLElement>} updatedElements
   */
  __afterElementsUpdated(n) {
    n.forEach((e) => {
      const t = e.offsetHeight;
      if (t === 0)
        e.style.paddingTop = `${this.__placeholderHeight}px`, e.style.opacity = "0", e.__virtualizerPlaceholder = !0, this.__placeholderClearDebouncer = ze.debounce(
          this.__placeholderClearDebouncer,
          Xt,
          () => this._resizeHandler()
        );
      else {
        this.__elementHeightQueue.push(t), this.__elementHeightQueue.shift();
        const i = this.__elementHeightQueue.filter((r) => r !== void 0);
        this.__placeholderHeight = Math.round(i.reduce((r, o) => r + o, 0) / i.length);
      }
    }), this.__pendingScrollToIndex !== void 0 && !this.__hasPlaceholders() && this.scrollToIndex(this.__pendingScrollToIndex);
  }
  __getIndexScrollOffset(n) {
    const e = this.__getVisibleElements().find((t) => t.__virtualIndex === n);
    return e ? this.scrollTarget.getBoundingClientRect().top - e.getBoundingClientRect().top : void 0;
  }
  get size() {
    return this.__size;
  }
  set size(n) {
    if (n === this.size)
      return;
    this.__fixInvalidItemPositioningDebouncer && this.__fixInvalidItemPositioningDebouncer.cancel(), this._debouncers && this._debouncers._increasePoolIfNeeded && this._debouncers._increasePoolIfNeeded.cancel(), this.__preventElementUpdates = !0;
    let e, t;
    if (n > 0 && (e = this.adjustedFirstVisibleIndex, t = this.__getIndexScrollOffset(e)), this.__size = n, this._itemsChanged({
      path: "items"
    }), cr(), n > 0) {
      e = Math.min(e, n - 1), this.scrollToIndex(e);
      const i = this.__getIndexScrollOffset(e);
      t !== void 0 && i !== void 0 && (this._scrollTop += t - i);
    }
    this.__preventElementUpdates = !1, this._isVisible || this._assignModels(), this.elementsContainer.children.length || requestAnimationFrame(() => this._resizeHandler()), this._resizeHandler(), cr(), this._debounce("_update", this._update, yt);
  }
  /** @private */
  get _scrollTop() {
    return this.scrollTarget.scrollTop;
  }
  /** @private */
  set _scrollTop(n) {
    this.scrollTarget.scrollTop = n;
  }
  /** @private */
  get items() {
    return {
      length: Math.min(this.size, Nu)
    };
  }
  /** @private */
  get offsetHeight() {
    return this.scrollTarget.offsetHeight;
  }
  /** @private */
  get $() {
    return {
      items: this.scrollContainer
    };
  }
  /** @private */
  updateViewportBoundaries() {
    const n = window.getComputedStyle(this.scrollTarget);
    this._scrollerPaddingTop = this.scrollTarget === this ? 0 : parseInt(n["padding-top"], 10), this._isRTL = n.direction === "rtl", this._viewportWidth = this.elementsContainer.offsetWidth, this._viewportHeight = this.scrollTarget.offsetHeight, this._scrollPageHeight = this._viewportHeight - this._scrollLineHeight, this.grid && this._updateGridMetrics();
  }
  /** @private */
  setAttribute() {
  }
  /** @private */
  _createPool(n) {
    const e = this.createElements(n), t = document.createDocumentFragment();
    return e.forEach((i) => {
      i.style.position = "absolute", t.appendChild(i), this.__resizeObserver.observe(i);
    }), this.elementsContainer.appendChild(t), e;
  }
  /** @private */
  _assignModels(n) {
    const e = [];
    this._iterateItems((t, i) => {
      const r = this._physicalItems[t];
      r.hidden = i >= this.size, r.hidden ? delete r.__lastUpdatedIndex : (r.__virtualIndex = i + (this._vidxOffset || 0), this.__updateElement(r, r.__virtualIndex), e.push(r));
    }, n), this.__afterElementsUpdated(e);
  }
  /** @private */
  _isClientFull() {
    return setTimeout(() => {
      this.__clientFull = !0;
    }), this.__clientFull || super._isClientFull();
  }
  /** @private */
  translate3d(n, e, t, i) {
    i.style.transform = `translateY(${e})`;
  }
  /** @private */
  toggleScrollListener() {
  }
  /** @private */
  __getFocusedElement(n = this.__getVisibleElements()) {
    return n.find(
      (e) => e.contains(this.elementsContainer.getRootNode().activeElement) || e.contains(this.scrollTarget.getRootNode().activeElement)
    );
  }
  /** @private */
  __nextFocusableSiblingMissing(n, e) {
    return (
      // Check if focused element is the last visible DOM element
      e.indexOf(n) === e.length - 1 && // ...while there are more items available
      this.size > n.__virtualIndex + 1
    );
  }
  /** @private */
  __previousFocusableSiblingMissing(n, e) {
    return (
      // Check if focused element is the first visible DOM element
      e.indexOf(n) === 0 && // ...while there are preceding items available
      n.__virtualIndex > 0
    );
  }
  /** @private */
  __onElementFocused(n) {
    if (!this.reorderElements)
      return;
    const e = n.detail.element;
    if (!e)
      return;
    const t = this.__getVisibleElements();
    (this.__previousFocusableSiblingMissing(e, t) || this.__nextFocusableSiblingMissing(e, t)) && this.flush();
    const i = this.__getVisibleElements();
    this.__nextFocusableSiblingMissing(e, i) ? (this._scrollTop += Math.ceil(e.getBoundingClientRect().bottom) - Math.floor(this.scrollTarget.getBoundingClientRect().bottom - 1), this.flush()) : this.__previousFocusableSiblingMissing(e, i) && (this._scrollTop -= Math.ceil(this.scrollTarget.getBoundingClientRect().top + 1) - Math.floor(e.getBoundingClientRect().top), this.flush());
  }
  _scrollHandler() {
    if (this.scrollTarget.offsetHeight === 0)
      return;
    this._adjustVirtualIndexOffset(this._scrollTop - (this.__previousScrollTop || 0));
    const n = this.scrollTarget.scrollTop - this._scrollPosition;
    if (super._scrollHandler(), this._physicalCount !== 0) {
      const e = n >= 0, t = this._getReusables(!e);
      t.indexes.length && (this._physicalTop = t.physicalTop, e ? (this._virtualStart -= t.indexes.length, this._physicalStart -= t.indexes.length) : (this._virtualStart += t.indexes.length, this._physicalStart += t.indexes.length), this._resizeHandler());
    }
    n && (this.__fixInvalidItemPositioningDebouncer = ze.debounce(
      this.__fixInvalidItemPositioningDebouncer,
      Nt.after(this.timeouts.FIX_INVALID_ITEM_POSITIONING),
      () => this.__fixInvalidItemPositioning()
    )), this.reorderElements && (this.__scrollReorderDebouncer = ze.debounce(
      this.__scrollReorderDebouncer,
      Nt.after(this.timeouts.SCROLL_REORDER),
      () => this.__reorderElements()
    )), this.__previousScrollTop = this._scrollTop, this._scrollTop === 0 && this.firstVisibleIndex !== 0 && Math.abs(n) > 0 && this.scrollToIndex(0);
  }
  /**
   * Work around an iron-list issue with invalid item positioning.
   * See https://github.com/vaadin/flow-components/issues/4306
   * @private
   */
  __fixInvalidItemPositioning() {
    if (!this.scrollTarget.isConnected)
      return;
    const n = this._physicalTop > this._scrollTop, e = this._physicalBottom < this._scrollBottom, t = this.adjustedFirstVisibleIndex === 0, i = this.adjustedLastVisibleIndex === this.size - 1;
    if (n && !t || e && !i) {
      const r = e, o = this._ratio;
      this._ratio = 0, this._scrollPosition = this._scrollTop + (r ? -1 : 1), this._scrollHandler(), this._ratio = o;
    }
  }
  /** @private */
  __onWheel(n) {
    if (n.ctrlKey || this._hasScrolledAncestor(n.target, n.deltaX, n.deltaY))
      return;
    let e = n.deltaY;
    if (n.deltaMode === WheelEvent.DOM_DELTA_LINE ? e *= this._scrollLineHeight : n.deltaMode === WheelEvent.DOM_DELTA_PAGE && (e *= this._scrollPageHeight), this._deltaYAcc || (this._deltaYAcc = 0), this._wheelAnimationFrame) {
      this._deltaYAcc += e, n.preventDefault();
      return;
    }
    e += this._deltaYAcc, this._deltaYAcc = 0, this._wheelAnimationFrame = !0, this.__debouncerWheelAnimationFrame = ze.debounce(
      this.__debouncerWheelAnimationFrame,
      Xt,
      () => {
        this._wheelAnimationFrame = !1;
      }
    );
    const t = Math.abs(n.deltaX) + Math.abs(e);
    this._canScroll(this.scrollTarget, n.deltaX, e) ? (n.preventDefault(), this.scrollTarget.scrollTop += e, this.scrollTarget.scrollLeft += n.deltaX, this._hasResidualMomentum = !0, this._ignoreNewWheel = !0, this._debouncerIgnoreNewWheel = ze.debounce(
      this._debouncerIgnoreNewWheel,
      Nt.after(this.timeouts.IGNORE_WHEEL),
      () => {
        this._ignoreNewWheel = !1;
      }
    )) : this._hasResidualMomentum && t <= this._previousMomentum || this._ignoreNewWheel ? n.preventDefault() : t > this._previousMomentum && (this._hasResidualMomentum = !1), this._previousMomentum = t;
  }
  /**
   * Determines if the element has an ancestor that handles the scroll delta prior to this
   *
   * @private
   */
  _hasScrolledAncestor(n, e, t) {
    if (n === this.scrollTarget || n === this.scrollTarget.getRootNode().host)
      return !1;
    if (this._canScroll(n, e, t) && ["auto", "scroll"].indexOf(getComputedStyle(n).overflow) !== -1)
      return !0;
    if (n !== this && n.parentElement)
      return this._hasScrolledAncestor(n.parentElement, e, t);
  }
  _canScroll(n, e, t) {
    return t > 0 && n.scrollTop < n.scrollHeight - n.offsetHeight || t < 0 && n.scrollTop > 0 || e > 0 && n.scrollLeft < n.scrollWidth - n.offsetWidth || e < 0 && n.scrollLeft > 0;
  }
  /**
   * Increases the pool size.
   * @override
   */
  _increasePoolIfNeeded(n) {
    if (this._physicalCount > 2 && n) {
      const t = Math.ceil(this._optPhysicalSize / this._physicalAverage) - this._physicalCount;
      super._increasePoolIfNeeded(Math.max(n, Math.min(100, t)));
    } else
      super._increasePoolIfNeeded(n);
  }
  /**
   * An optimal physical size such that we will have enough physical items
   * to fill up the viewport and recycle when the user scrolls.
   *
   * This default value assumes that we will at least have the equivalent
   * to a viewport of physical items above and below the user's viewport.
   * @override
   */
  get _optPhysicalSize() {
    const n = super._optPhysicalSize;
    return n <= 0 || this.__hasPlaceholders() ? n : n + this.__getItemHeightBuffer();
  }
  /**
   * Extra item height buffer used when calculating optimal physical size.
   *
   * The iron list core uses the optimal physical size when determining whether to increase the item pool.
   * For the cases where some items are much larger than the average, the iron list core might not increase item pool.
   * This can lead to the large item not being rendered.
   *
   * @returns {Number} - Extra item height buffer
   * @private
   */
  __getItemHeightBuffer() {
    if (this._physicalCount === 0)
      return 0;
    const n = Math.ceil(this._viewportHeight * (this._maxPages - 1) / 2), e = Math.max(...this._physicalSizes);
    return e > Math.min(...this._physicalSizes) ? Math.max(0, e - n) : 0;
  }
  /**
   * @returns {Number|undefined} - The browser's default font-size in pixels
   * @private
   */
  _getScrollLineHeight() {
    const n = document.createElement("div");
    n.style.fontSize = "initial", n.style.display = "none", document.body.appendChild(n);
    const e = window.getComputedStyle(n).fontSize;
    return document.body.removeChild(n), e ? window.parseInt(e) : void 0;
  }
  __getVisibleElements() {
    return Array.from(this.elementsContainer.children).filter((n) => !n.hidden);
  }
  /** @private */
  __reorderElements() {
    if (this.__mouseDown) {
      this.__pendingReorder = !0;
      return;
    }
    this.__pendingReorder = !1;
    const n = this._virtualStart + (this._vidxOffset || 0), e = this.__getVisibleElements(), t = this.__getFocusedElement(e) || e[0];
    if (!t)
      return;
    const i = t.__virtualIndex - n, r = e.indexOf(t) - i;
    if (r > 0)
      for (let o = 0; o < r; o++)
        this.elementsContainer.appendChild(e[o]);
    else if (r < 0)
      for (let o = e.length + r; o < e.length; o++)
        this.elementsContainer.insertBefore(e[o], e[0]);
    if (Kl) {
      const { transform: o } = this.scrollTarget.style;
      this.scrollTarget.style.transform = "translateZ(0)", setTimeout(() => {
        this.scrollTarget.style.transform = o;
      });
    }
  }
  /** @private */
  _adjustVirtualIndexOffset(n) {
    const e = this._maxVirtualIndexOffset;
    if (this._virtualCount >= this.size)
      this._vidxOffset = 0;
    else if (this.__skipNextVirtualIndexAdjust)
      this.__skipNextVirtualIndexAdjust = !1;
    else if (Math.abs(n) > 1e4) {
      const t = this._scrollTop / (this.scrollTarget.scrollHeight - this.scrollTarget.clientHeight);
      this._vidxOffset = Math.round(t * e);
    } else {
      const t = this._vidxOffset, i = wo, r = 100;
      this._scrollTop === 0 ? (this._vidxOffset = 0, t !== this._vidxOffset && super.scrollToIndex(0)) : this.firstVisibleIndex < i && this._vidxOffset > 0 && (this._vidxOffset -= Math.min(this._vidxOffset, r), super.scrollToIndex(this.firstVisibleIndex + (t - this._vidxOffset))), this._scrollTop >= this._maxScrollTop && this._maxScrollTop > 0 ? (this._vidxOffset = e, t !== this._vidxOffset && super.scrollToIndex(this._virtualCount - 1)) : this.firstVisibleIndex > this._virtualCount - i && this._vidxOffset < e && (this._vidxOffset += Math.min(e - this._vidxOffset, r), super.scrollToIndex(this.firstVisibleIndex - (this._vidxOffset - t)));
    }
  }
}
Object.setPrototypeOf(Ql.prototype, Mu);
class Jl {
  /**
   * @typedef {Object} VirtualizerConfig
   * @property {Function} createElements Function that returns the given number of new elements
   * @property {Function} updateElement Function that updates the element at a specific index
   * @property {HTMLElement} scrollTarget Reference to the scrolling element
   * @property {HTMLElement} scrollContainer Reference to a wrapper for the item elements (or a slot) inside the scrollTarget
   * @property {HTMLElement | undefined} elementsContainer Reference to the container in which the item elements are placed, defaults to scrollContainer
   * @property {boolean | undefined} reorderElements Determines whether the physical item elements should be kept in order in the DOM
   * @param {VirtualizerConfig} config Configuration for the virtualizer
   */
  constructor(n) {
    this.__adapter = new Ql(n);
  }
  /**
   * Gets the index of the first visible item in the viewport.
   *
   * @return {number}
   */
  get firstVisibleIndex() {
    return this.__adapter.adjustedFirstVisibleIndex;
  }
  /**
   * Gets the index of the last visible item in the viewport.
   *
   * @return {number}
   */
  get lastVisibleIndex() {
    return this.__adapter.adjustedLastVisibleIndex;
  }
  /**
   * The size of the virtualizer
   * @return {number | undefined} The size of the virtualizer
   */
  get size() {
    return this.__adapter.size;
  }
  /**
   * The size of the virtualizer
   * @param {number} size The size of the virtualizer
   */
  set size(n) {
    this.__adapter.size = n;
  }
  /**
   * Scroll to a specific index in the virtual list
   *
   * @method scrollToIndex
   * @param {number} index The index of the item
   */
  scrollToIndex(n) {
    this.__adapter.scrollToIndex(n);
  }
  /**
   * Requests the virtualizer to re-render the item elements on an index range, if currently in the DOM
   *
   * @method update
   * @param {number | undefined} startIndex The start index of the range
   * @param {number | undefined} endIndex The end index of the range
   */
  update(n = 0, e = this.size - 1) {
    this.__adapter.update(n, e);
  }
  /**
   * Flushes active asynchronous tasks so that the component and the DOM end up in a stable state
   *
   * @method update
   * @param {number | undefined} startIndex The start index of the range
   * @param {number | undefined} endIndex The end index of the range
   */
  flush() {
    this.__adapter.flush();
  }
}
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Bu = (s) => class extends s {
  static get observers() {
    return ["_a11yUpdateGridSize(size, _columnTree)"];
  }
  /** @private */
  _a11yGetHeaderRowCount(e) {
    return e.filter(
      (t) => t.some((i) => i.headerRenderer || i.path && i.header !== null || i.header)
    ).length;
  }
  /** @private */
  _a11yGetFooterRowCount(e) {
    return e.filter((t) => t.some((i) => i.headerRenderer)).length;
  }
  /** @private */
  _a11yUpdateGridSize(e, t) {
    if (e === void 0 || t === void 0)
      return;
    const i = t[t.length - 1];
    this.$.table.setAttribute(
      "aria-rowcount",
      e + this._a11yGetHeaderRowCount(t) + this._a11yGetFooterRowCount(t)
    ), this.$.table.setAttribute("aria-colcount", i && i.length || 0), this._a11yUpdateHeaderRows(), this._a11yUpdateFooterRows();
  }
  /** @protected */
  _a11yUpdateHeaderRows() {
    at(this.$.header, (e, t) => {
      e.setAttribute("aria-rowindex", t + 1);
    });
  }
  /** @protected */
  _a11yUpdateFooterRows() {
    at(this.$.footer, (e, t) => {
      e.setAttribute("aria-rowindex", this._a11yGetHeaderRowCount(this._columnTree) + this.size + t + 1);
    });
  }
  /**
   * @param {!HTMLElement} row
   * @param {number} index
   * @protected
   */
  _a11yUpdateRowRowindex(e, t) {
    e.setAttribute("aria-rowindex", t + this._a11yGetHeaderRowCount(this._columnTree) + 1);
  }
  /**
   * @param {!HTMLElement} row
   * @param {boolean} selected
   * @protected
   */
  _a11yUpdateRowSelected(e, t) {
    e.setAttribute("aria-selected", !!t), Hi(e, (i) => {
      i.setAttribute("aria-selected", !!t);
    });
  }
  /**
   * @param {!HTMLElement} row
   * @protected
   */
  _a11yUpdateRowExpanded(e) {
    this.__isRowExpandable(e) ? e.setAttribute("aria-expanded", "false") : this.__isRowCollapsible(e) ? e.setAttribute("aria-expanded", "true") : e.removeAttribute("aria-expanded");
  }
  /**
   * @param {!HTMLElement} row
   * @param {number} level
   * @protected
   */
  _a11yUpdateRowLevel(e, t) {
    t > 0 || this.__isRowCollapsible(e) || this.__isRowExpandable(e) ? e.setAttribute("aria-level", t + 1) : e.removeAttribute("aria-level");
  }
  /**
   * @param {!HTMLElement} row
   * @param {!HTMLElement} detailsCell
   * @protected
   */
  _a11ySetRowDetailsCell(e, t) {
    Hi(e, (i) => {
      i !== t && i.setAttribute("aria-controls", t.id);
    });
  }
  /**
   * @param {!HTMLElement} row
   * @param {number} colspan
   * @protected
   */
  _a11yUpdateCellColspan(e, t) {
    e.setAttribute("aria-colspan", Number(t));
  }
  /** @protected */
  _a11yUpdateSorters() {
    Array.from(this.querySelectorAll("vaadin-grid-sorter")).forEach((e) => {
      let t = e.parentNode;
      for (; t && t.localName !== "vaadin-grid-cell-content"; )
        t = t.parentNode;
      t && t.assignedSlot && t.assignedSlot.parentNode.setAttribute(
        "aria-sort",
        {
          asc: "ascending",
          desc: "descending"
        }[String(e.direction)] || "none"
      );
    });
  }
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Hu = (s) => s.offsetParent && !s.part.contains("body-cell") && gs(s) && getComputedStyle(s).visibility !== "hidden", Vu = (s) => class extends s {
  static get properties() {
    return {
      /**
       * The item user has last interacted with. Turns to `null` after user deactivates
       * the item by re-interacting with the currently active item.
       * @type {GridItem}
       */
      activeItem: {
        type: Object,
        notify: !0,
        value: null,
        sync: !0
      }
    };
  }
  /** @protected */
  ready() {
    super.ready(), this.$.scroller.addEventListener("click", this._onClick.bind(this)), this.addEventListener("cell-activate", this._activateItem.bind(this)), this.addEventListener("row-activate", this._activateItem.bind(this));
  }
  /** @private */
  _activateItem(e) {
    const t = e.detail.model, i = t ? t.item : null;
    i && (this.activeItem = this._itemsEqual(this.activeItem, i) ? null : i);
  }
  /**
   * Checks whether the click event should not activate the cell on which it occurred.
   *
   * @protected
   */
  _shouldPreventCellActivationOnClick(e) {
    const { cell: t } = this._getGridEventLocation(e);
    return (
      // Something has handled this click already, e. g., <vaadin-grid-sorter>
      e.defaultPrevented || // No clicked cell available
      !t || // Cell is a details cell
      t.getAttribute("part").includes("details-cell") || // Cell is the empty state cell
      t === this.$.emptystatecell || // Cell content is focused
      t._content.contains(this.getRootNode().activeElement) || // Clicked on a focusable element
      this._isFocusable(e.target) || // Clicked on a label element
      e.target instanceof HTMLLabelElement
    );
  }
  /**
   * @param {!MouseEvent} e
   * @protected
   */
  _onClick(e) {
    if (this._shouldPreventCellActivationOnClick(e))
      return;
    const { cell: t } = this._getGridEventLocation(e);
    t && this.dispatchEvent(
      new CustomEvent("cell-activate", {
        detail: {
          model: this.__getRowModel(t.parentElement)
        }
      })
    );
  }
  /**
   * @param {!Element} target
   * @return {boolean}
   * @protected
   */
  _isFocusable(e) {
    return Hu(e);
  }
  /**
   * Fired when the `activeItem` property changes.
   *
   * @event active-item-changed
   */
  /**
   * Fired when the cell is activated with click or keyboard.
   *
   * @event cell-activate
   */
};
function Oi(s, n) {
  return s.split(".").reduce((e, t) => e[t], n);
}
function Ha(s, n, e) {
  if (e.length === 0)
    return !1;
  let t = !0;
  return s.forEach(({ path: i }) => {
    if (!i || i.indexOf(".") === -1)
      return;
    const r = i.replace(/\.[^.]*$/u, "");
    Oi(r, e[0]) === void 0 && (t = !1);
  }), t;
}
function En(s) {
  return [void 0, null].indexOf(s) >= 0 ? "" : isNaN(s) ? s.toString() : s;
}
function Va(s, n) {
  return s = En(s), n = En(n), s < n ? -1 : s > n ? 1 : 0;
}
function Uu(s, n) {
  return s.sort((e, t) => n.map((i) => i.direction === "asc" ? Va(Oi(i.path, e), Oi(i.path, t)) : i.direction === "desc" ? Va(Oi(i.path, t), Oi(i.path, e)) : 0).reduce((i, r) => i !== 0 ? i : r, 0));
}
function Wu(s, n) {
  return s.filter((e) => n.every((t) => {
    const i = En(Oi(t.path, e)), r = En(t.value).toString().toLowerCase();
    return i.toString().toLowerCase().includes(r);
  }));
}
const ju = (s) => (n, e) => {
  let t = s ? [...s] : [];
  n.filters && Ha(n.filters, "filtering", t) && (t = Wu(t, n.filters)), Array.isArray(n.sortOrders) && n.sortOrders.length && Ha(n.sortOrders, "sorting", t) && (t = Uu(t, n.sortOrders));
  const i = Math.min(t.length, n.pageSize), r = n.page * i, o = r + i, l = t.slice(r, o);
  e(l, t.length);
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Gu = (s) => class extends s {
  static get properties() {
    return {
      /**
       * An array containing the items which will be passed to renderer functions.
       *
       * @type {Array<!GridItem> | undefined}
       */
      items: {
        type: Array,
        sync: !0
      }
    };
  }
  static get observers() {
    return ["__dataProviderOrItemsChanged(dataProvider, items, isAttached, items.*)"];
  }
  /** @private */
  __setArrayDataProvider(e) {
    const t = ju(this.items);
    t.__items = e, this._arrayDataProvider = t, this.size = e.length, this.dataProvider = t;
  }
  /**
   * @override
   * @protected
   */
  _onDataProviderPageReceived() {
    super._onDataProviderPageReceived(), this._arrayDataProvider && (this.size = this._flatSize);
  }
  /** @private */
  __dataProviderOrItemsChanged(e, t, i) {
    i && (this._arrayDataProvider ? e !== this._arrayDataProvider ? (this._arrayDataProvider = void 0, this.items = void 0) : t ? this._arrayDataProvider.__items === t ? this.clearCache() : this.__setArrayDataProvider(t) : (this._arrayDataProvider = void 0, this.dataProvider = void 0, this.size = 0, this.clearCache()) : t && this.__setArrayDataProvider(t));
  }
};
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
const qu = (s) => s, $l = typeof document.head.style.touchAction == "string", Zo = "__polymerGestures", xo = "__polymerGesturesHandled", Yo = "__polymerGesturesTouchAction", Ua = 25, Wa = 5, Ku = 2, Zu = ["mousedown", "mousemove", "mouseup", "click"], Yu = [0, 1, 4, 2], Xu = (function() {
  try {
    return new MouseEvent("test", { buttons: 1 }).buttons === 1;
  } catch {
    return !1;
  }
})();
function _s(s) {
  return Zu.indexOf(s) > -1;
}
let Qu = !1;
(function() {
  try {
    const s = Object.defineProperty({}, "passive", {
      // eslint-disable-next-line getter-return
      get() {
        Qu = !0;
      }
    });
    window.addEventListener("test", null, s), window.removeEventListener("test", null, s);
  } catch {
  }
})();
function Ju(s) {
  _s(s);
}
const $u = navigator.userAgent.match(/iP(?:[oa]d|hone)|Android/u), ef = {
  button: !0,
  command: !0,
  fieldset: !0,
  input: !0,
  keygen: !0,
  optgroup: !0,
  option: !0,
  select: !0,
  textarea: !0
};
function fi(s) {
  const n = s.type;
  if (!_s(n))
    return !1;
  if (n === "mousemove") {
    let t = s.buttons === void 0 ? 1 : s.buttons;
    return s instanceof window.MouseEvent && !Xu && (t = Yu[s.which] || 0), !!(t & 1);
  }
  return (s.button === void 0 ? 0 : s.button) === 0;
}
function tf(s) {
  if (s.type === "click") {
    if (s.detail === 0)
      return !0;
    const n = Yt(s);
    if (!n.nodeType || /** @type {Element} */
    n.nodeType !== Node.ELEMENT_NODE)
      return !0;
    const e = (
      /** @type {Element} */
      n.getBoundingClientRect()
    ), t = s.pageX, i = s.pageY;
    return !(t >= e.left && t <= e.right && i >= e.top && i <= e.bottom);
  }
  return !1;
}
const Lt = {
  touch: {
    x: 0,
    y: 0,
    id: -1,
    scrollDecided: !1
  }
};
function rf(s) {
  let n = "auto";
  const e = tc(s);
  for (let t = 0, i; t < e.length; t++)
    if (i = e[t], i[Yo]) {
      n = i[Yo];
      break;
    }
  return n;
}
function ec(s, n, e) {
  s.movefn = n, s.upfn = e, document.addEventListener("mousemove", n), document.addEventListener("mouseup", e);
}
function Mi(s) {
  document.removeEventListener("mousemove", s.movefn), document.removeEventListener("mouseup", s.upfn), s.movefn = null, s.upfn = null;
}
const tc = window.ShadyDOM && window.ShadyDOM.noPatch ? window.ShadyDOM.composedPath : (s) => s.composedPath && s.composedPath() || [], vs = {}, di = [];
function nf(s, n) {
  let e = document.elementFromPoint(s, n), t = e;
  for (; t && t.shadowRoot && !window.ShadyDOM; ) {
    const i = t;
    if (t = t.shadowRoot.elementFromPoint(s, n), i === t)
      break;
    t && (e = t);
  }
  return e;
}
function Yt(s) {
  const n = tc(
    /** @type {?Event} */
    s
  );
  return n.length > 0 ? n[0] : s.target;
}
function of(s) {
  const n = s.type, t = s.currentTarget[Zo];
  if (!t)
    return;
  const i = t[n];
  if (!i)
    return;
  if (!s[xo] && (s[xo] = {}, n.startsWith("touch"))) {
    const o = s.changedTouches[0];
    if (n === "touchstart" && s.touches.length === 1 && (Lt.touch.id = o.identifier), Lt.touch.id !== o.identifier)
      return;
    $l || (n === "touchstart" || n === "touchmove") && sf(s);
  }
  const r = s[xo];
  if (!r.skip) {
    for (let o = 0, l; o < di.length; o++)
      l = di[o], i[l.name] && !r[l.name] && l.flow && l.flow.start.indexOf(s.type) > -1 && l.reset && l.reset();
    for (let o = 0, l; o < di.length; o++)
      l = di[o], i[l.name] && !r[l.name] && (r[l.name] = !0, l[n](s));
  }
}
function sf(s) {
  const n = s.changedTouches[0], e = s.type;
  if (e === "touchstart")
    Lt.touch.x = n.clientX, Lt.touch.y = n.clientY, Lt.touch.scrollDecided = !1;
  else if (e === "touchmove") {
    if (Lt.touch.scrollDecided)
      return;
    Lt.touch.scrollDecided = !0;
    const t = rf(s);
    let i = !1;
    const r = Math.abs(Lt.touch.x - n.clientX), o = Math.abs(Lt.touch.y - n.clientY);
    s.cancelable && (t === "none" ? i = !0 : t === "pan-x" ? i = o > r : t === "pan-y" && (i = r > o)), i ? s.preventDefault() : Pn("track");
  }
}
function ic(s, n, e) {
  return vs[n] ? (af(s, n, e), !0) : !1;
}
function af(s, n, e) {
  const t = vs[n], i = t.deps, r = t.name;
  let o = s[Zo];
  o || (s[Zo] = o = {});
  for (let l = 0, a, c; l < i.length; l++)
    a = i[l], !($u && _s(a) && a !== "click") && (c = o[a], c || (o[a] = c = { _count: 0 }), c._count === 0 && s.addEventListener(a, of, Ju(a)), c[r] = (c[r] || 0) + 1, c._count = (c._count || 0) + 1);
  s.addEventListener(n, e), t.touchAction && cf(s, t.touchAction);
}
function ys(s) {
  di.push(s), s.emits.forEach((n) => {
    vs[n] = s;
  });
}
function lf(s) {
  for (let n = 0, e; n < di.length; n++) {
    e = di[n];
    for (let t = 0, i; t < e.emits.length; t++)
      if (i = e.emits[t], i === s)
        return e;
  }
  return null;
}
function cf(s, n) {
  $l && s instanceof HTMLElement && yt.run(() => {
    s.style.touchAction = n;
  }), s[Yo] = n;
}
function bs(s, n, e) {
  const t = new Event(n, { bubbles: !0, cancelable: !0, composed: !0 });
  if (t.detail = e, qu(
    /** @type {!Node} */
    s
  ).dispatchEvent(t), t.defaultPrevented) {
    const i = e.preventer || e.sourceEvent;
    i && i.preventDefault && i.preventDefault();
  }
}
function Pn(s) {
  const n = lf(s);
  n.info && (n.info.prevent = !0);
}
ys({
  name: "downup",
  deps: ["mousedown", "touchstart", "touchend"],
  flow: {
    start: ["mousedown", "touchstart"],
    end: ["mouseup", "touchend"]
  },
  emits: ["down", "up"],
  info: {
    movefn: null,
    upfn: null
  },
  /**
   * @this {GestureRecognizer}
   * @return {void}
   */
  reset() {
    Mi(this.info);
  },
  /**
   * @this {GestureRecognizer}
   * @param {MouseEvent} e
   * @return {void}
   */
  mousedown(s) {
    if (!fi(s))
      return;
    const n = Yt(s), e = this, t = (r) => {
      fi(r) || (or("up", n, r), Mi(e.info));
    }, i = (r) => {
      fi(r) && or("up", n, r), Mi(e.info);
    };
    ec(this.info, t, i), or("down", n, s);
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchstart(s) {
    or("down", Yt(s), s.changedTouches[0], s);
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchend(s) {
    or("up", Yt(s), s.changedTouches[0], s);
  }
});
function or(s, n, e, t) {
  n && bs(n, s, {
    x: e.clientX,
    y: e.clientY,
    sourceEvent: e,
    preventer: t,
    prevent(i) {
      return Pn(i);
    }
  });
}
ys({
  name: "track",
  touchAction: "none",
  deps: ["mousedown", "touchstart", "touchmove", "touchend"],
  flow: {
    start: ["mousedown", "touchstart"],
    end: ["mouseup", "touchend"]
  },
  emits: ["track"],
  info: {
    x: 0,
    y: 0,
    state: "start",
    started: !1,
    moves: [],
    /** @this {GestureInfo} */
    addMove(s) {
      this.moves.length > Ku && this.moves.shift(), this.moves.push(s);
    },
    movefn: null,
    upfn: null,
    prevent: !1
  },
  /**
   * @this {GestureRecognizer}
   * @return {void}
   */
  reset() {
    this.info.state = "start", this.info.started = !1, this.info.moves = [], this.info.x = 0, this.info.y = 0, this.info.prevent = !1, Mi(this.info);
  },
  /**
   * @this {GestureRecognizer}
   * @param {MouseEvent} e
   * @return {void}
   */
  mousedown(s) {
    if (!fi(s))
      return;
    const n = Yt(s), e = this, t = (r) => {
      const o = r.clientX, l = r.clientY;
      ja(e.info, o, l) && (e.info.state = e.info.started ? r.type === "mouseup" ? "end" : "track" : "start", e.info.state === "start" && Pn("tap"), e.info.addMove({ x: o, y: l }), fi(r) || (e.info.state = "end", Mi(e.info)), n && Co(e.info, n, r), e.info.started = !0);
    }, i = (r) => {
      e.info.started && t(r), Mi(e.info);
    };
    ec(this.info, t, i), this.info.x = s.clientX, this.info.y = s.clientY;
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchstart(s) {
    const n = s.changedTouches[0];
    this.info.x = n.clientX, this.info.y = n.clientY;
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchmove(s) {
    const n = Yt(s), e = s.changedTouches[0], t = e.clientX, i = e.clientY;
    ja(this.info, t, i) && (this.info.state === "start" && Pn("tap"), this.info.addMove({ x: t, y: i }), Co(this.info, n, e), this.info.state = "track", this.info.started = !0);
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchend(s) {
    const n = Yt(s), e = s.changedTouches[0];
    this.info.started && (this.info.state = "end", this.info.addMove({ x: e.clientX, y: e.clientY }), Co(this.info, n, e));
  }
});
function ja(s, n, e) {
  if (s.prevent)
    return !1;
  if (s.started)
    return !0;
  const t = Math.abs(s.x - n), i = Math.abs(s.y - e);
  return t >= Wa || i >= Wa;
}
function Co(s, n, e) {
  if (!n)
    return;
  const t = s.moves[s.moves.length - 2], i = s.moves[s.moves.length - 1], r = i.x - s.x, o = i.y - s.y;
  let l, a = 0;
  t && (l = i.x - t.x, a = i.y - t.y), bs(n, "track", {
    state: s.state,
    x: e.clientX,
    y: e.clientY,
    dx: r,
    dy: o,
    ddx: l,
    ddy: a,
    sourceEvent: e,
    hover() {
      return nf(e.clientX, e.clientY);
    }
  });
}
ys({
  name: "tap",
  deps: ["mousedown", "click", "touchstart", "touchend"],
  flow: {
    start: ["mousedown", "touchstart"],
    end: ["click", "touchend"]
  },
  emits: ["tap"],
  info: {
    x: NaN,
    y: NaN,
    prevent: !1
  },
  /**
   * @this {GestureRecognizer}
   * @return {void}
   */
  reset() {
    this.info.x = NaN, this.info.y = NaN, this.info.prevent = !1;
  },
  /**
   * @this {GestureRecognizer}
   * @param {MouseEvent} e
   * @return {void}
   */
  mousedown(s) {
    fi(s) && (this.info.x = s.clientX, this.info.y = s.clientY);
  },
  /**
   * @this {GestureRecognizer}
   * @param {MouseEvent} e
   * @return {void}
   */
  click(s) {
    fi(s) && Ga(this.info, s);
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchstart(s) {
    const n = s.changedTouches[0];
    this.info.x = n.clientX, this.info.y = n.clientY;
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchend(s) {
    Ga(this.info, s.changedTouches[0], s);
  }
});
function Ga(s, n, e) {
  const t = Math.abs(n.clientX - s.x), i = Math.abs(n.clientY - s.y), r = Yt(e || n);
  !r || ef[
    /** @type {!HTMLElement} */
    r.localName
  ] && r.hasAttribute("disabled") || (isNaN(t) || isNaN(i) || t <= Ua && i <= Ua || tf(n)) && (s.prevent || bs(r, "tap", {
    x: n.clientX,
    y: n.clientY,
    sourceEvent: n,
    preventer: e
  }));
}
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const hf = (s) => class extends s {
  static get properties() {
    return {
      /**
       * Set to true to allow column reordering.
       * @attr {boolean} column-reordering-allowed
       * @type {boolean}
       */
      columnReorderingAllowed: {
        type: Boolean,
        value: !1
      },
      /** @private */
      _orderBaseScope: {
        type: Number,
        value: 1e7
      }
    };
  }
  static get observers() {
    return ["_updateOrders(_columnTree)"];
  }
  /** @protected */
  ready() {
    super.ready(), ic(this, "track", this._onTrackEvent), this._reorderGhost = this.shadowRoot.querySelector('[part="reorder-ghost"]'), this.addEventListener("touchstart", this._onTouchStart.bind(this)), this.addEventListener("touchmove", this._onTouchMove.bind(this)), this.addEventListener("touchend", this._onTouchEnd.bind(this)), this.addEventListener("contextmenu", this._onContextMenu.bind(this));
  }
  /** @private */
  _onContextMenu(e) {
    this.hasAttribute("reordering") && (e.preventDefault(), br || this._onTrackEnd());
  }
  /** @private */
  _onTouchStart(e) {
    this._startTouchReorderTimeout = setTimeout(() => {
      this._onTrackStart({
        detail: {
          x: e.touches[0].clientX,
          y: e.touches[0].clientY
        }
      });
    }, 100);
  }
  /** @private */
  _onTouchMove(e) {
    this._draggedColumn && e.preventDefault(), clearTimeout(this._startTouchReorderTimeout);
  }
  /** @private */
  _onTouchEnd() {
    clearTimeout(this._startTouchReorderTimeout), this._onTrackEnd();
  }
  /** @private */
  _onTrackEvent(e) {
    if (e.detail.state === "start") {
      const t = e.composedPath(), i = t[t.indexOf(this.$.header) - 2];
      if (!i || !i._content || i._content.contains(this.getRootNode().activeElement) || this.$.scroller.hasAttribute("column-resizing"))
        return;
      this._touchDevice || this._onTrackStart(e);
    } else e.detail.state === "track" ? this._onTrack(e) : e.detail.state === "end" && this._onTrackEnd(e);
  }
  /** @private */
  _onTrackStart(e) {
    if (!this.columnReorderingAllowed)
      return;
    const t = e.composedPath && e.composedPath();
    if (t && t.some((r) => r.hasAttribute && r.hasAttribute("draggable")))
      return;
    const i = this._cellFromPoint(e.detail.x, e.detail.y);
    if (!(!i || !i.getAttribute("part").includes("header-cell"))) {
      for (this.toggleAttribute("reordering", !0), this._draggedColumn = i._column; this._draggedColumn.parentElement.childElementCount === 1; )
        this._draggedColumn = this._draggedColumn.parentElement;
      this._setSiblingsReorderStatus(this._draggedColumn, "allowed"), this._draggedColumn._reorderStatus = "dragging", this._updateGhost(i), this._reorderGhost.style.visibility = "visible", this._updateGhostPosition(e.detail.x, this._touchDevice ? e.detail.y - 50 : e.detail.y), this._autoScroller();
    }
  }
  /** @private */
  _onTrack(e) {
    if (!this._draggedColumn)
      return;
    const t = this._cellFromPoint(e.detail.x, e.detail.y);
    if (!t)
      return;
    const i = this._getTargetColumn(t, this._draggedColumn);
    if (this._isSwapAllowed(this._draggedColumn, i) && this._isSwappableByPosition(i, e.detail.x)) {
      const r = this._columnTree.findIndex((d) => d.includes(i)), o = this._getColumnsInOrder(r), l = o.indexOf(this._draggedColumn), a = o.indexOf(i), c = l < a ? 1 : -1;
      for (let d = l; d !== a; d += c)
        this._swapColumnOrders(this._draggedColumn, o[d + c]);
    }
    this._updateGhostPosition(e.detail.x, this._touchDevice ? e.detail.y - 50 : e.detail.y), this._lastDragClientX = e.detail.x;
  }
  /** @private */
  _onTrackEnd() {
    this._draggedColumn && (this.toggleAttribute("reordering", !1), this._draggedColumn._reorderStatus = "", this._setSiblingsReorderStatus(this._draggedColumn, ""), this._draggedColumn = null, this._lastDragClientX = null, this._reorderGhost.style.visibility = "hidden", this.dispatchEvent(
      new CustomEvent("column-reorder", {
        detail: {
          columns: this._getColumnsInOrder()
        }
      })
    ));
  }
  /**
   * Returns the columns (or column groups) on the specified header level in visual order.
   * By default, the bottom level is used.
   *
   * @return {!Array<!GridColumn>}
   * @protected
   */
  _getColumnsInOrder(e = this._columnTree.length - 1) {
    return this._columnTree[e].filter((t) => !t.hidden).sort((t, i) => t._order - i._order);
  }
  /**
   * @param {number} x
   * @param {number} y
   * @return {HTMLElement | undefined}
   * @protected
   */
  _cellFromPoint(e = 0, t = 0) {
    this._draggedColumn || this.$.scroller.toggleAttribute("no-content-pointer-events", !0);
    const i = this.shadowRoot.elementFromPoint(e, t);
    return this.$.scroller.toggleAttribute("no-content-pointer-events", !1), this._getCellFromElement(i);
  }
  /** @private */
  _getCellFromElement(e) {
    if (e) {
      if (e._column)
        return e;
      const { parentElement: t } = e;
      if (t && t._focusButton === e)
        return t;
    }
    return null;
  }
  /**
   * @param {number} eventClientX
   * @param {number} eventClientY
   * @protected
   */
  _updateGhostPosition(e, t) {
    const i = this._reorderGhost.getBoundingClientRect(), r = e - i.width / 2, o = t - i.height / 2, l = parseInt(this._reorderGhost._left || 0), a = parseInt(this._reorderGhost._top || 0);
    this._reorderGhost._left = l - (i.left - r), this._reorderGhost._top = a - (i.top - o), this._reorderGhost.style.transform = `translate(${this._reorderGhost._left}px, ${this._reorderGhost._top}px)`;
  }
  /**
   * @param {!HTMLElement} cell
   * @return {!HTMLElement}
   * @protected
   */
  _updateGhost(e) {
    const t = this._reorderGhost;
    t.textContent = e._content.innerText;
    const i = window.getComputedStyle(e);
    return [
      "boxSizing",
      "display",
      "width",
      "height",
      "background",
      "alignItems",
      "padding",
      "border",
      "flex-direction",
      "overflow"
    ].forEach((r) => {
      t.style[r] = i[r];
    }), t;
  }
  /** @private */
  _updateOrders(e) {
    e !== void 0 && (e[0].forEach((t) => {
      t._order = 0;
    }), pu(e[0], this._orderBaseScope, 0));
  }
  /**
   * @param {!GridColumn} column
   * @param {string} status
   * @protected
   */
  _setSiblingsReorderStatus(e, t) {
    at(e.parentNode, (i) => {
      /column/u.test(i.localName) && this._isSwapAllowed(i, e) && (i._reorderStatus = t);
    });
  }
  /** @protected */
  _autoScroller() {
    if (this._lastDragClientX) {
      const e = this._lastDragClientX - this.getBoundingClientRect().right + 50, t = this.getBoundingClientRect().left - this._lastDragClientX + 50;
      e > 0 ? this.$.table.scrollLeft += e / 10 : t > 0 && (this.$.table.scrollLeft -= t / 10);
    }
    this._draggedColumn && setTimeout(() => this._autoScroller(), 10);
  }
  /**
   * @param {GridColumn | undefined} column1
   * @param {GridColumn | undefined} column2
   * @return {boolean | undefined}
   * @protected
   */
  _isSwapAllowed(e, t) {
    if (e && t) {
      const i = e !== t, r = e.parentElement === t.parentElement, o = e.frozen && t.frozen || // Both columns are frozen
      e.frozenToEnd && t.frozenToEnd || // Both columns are frozen to end
      !e.frozen && !e.frozenToEnd && !t.frozen && !t.frozenToEnd;
      return i && r && o;
    }
  }
  /**
   * @param {!GridColumn} targetColumn
   * @param {number} clientX
   * @return {boolean}
   * @protected
   */
  _isSwappableByPosition(e, t) {
    const i = Array.from(this.$.header.querySelectorAll('tr:not([hidden]) [part~="cell"]')).find(
      (l) => e.contains(l._column)
    ), r = this.$.header.querySelector("tr:not([hidden]) [reorder-status=dragging]").getBoundingClientRect(), o = i.getBoundingClientRect();
    return o.left > r.left ? t > o.right - r.width : t < o.left + r.width;
  }
  /**
   * @param {!GridColumn} column1
   * @param {!GridColumn} column2
   * @protected
   */
  _swapColumnOrders(e, t) {
    [e._order, t._order] = [t._order, e._order], this._debounceUpdateFrozenColumn(), this._updateFirstAndLastColumn();
  }
  /**
   * @param {HTMLElement | undefined} targetCell
   * @param {GridColumn} draggedColumn
   * @return {GridColumn | undefined}
   * @protected
   */
  _getTargetColumn(e, t) {
    if (e && t) {
      let i = e._column;
      for (; i.parentElement !== t.parentElement && i !== this; )
        i = i.parentElement;
      return i.parentElement === t.parentElement ? i : e._column;
    }
  }
  /**
   * Fired when the columns in the grid are reordered.
   *
   * @event column-reorder
   * @param {Object} detail
   * @param {Object} detail.columns the columns in the new order
   */
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const df = (s) => class extends s {
  /** @protected */
  ready() {
    super.ready();
    const e = this.$.scroller;
    ic(e, "track", this._onHeaderTrack.bind(this)), e.addEventListener("touchmove", (t) => e.hasAttribute("column-resizing") && t.preventDefault()), e.addEventListener(
      "contextmenu",
      (t) => t.target.getAttribute("part") === "resize-handle" && t.preventDefault()
    ), e.addEventListener(
      "mousedown",
      (t) => t.target.getAttribute("part") === "resize-handle" && t.preventDefault()
    );
  }
  /** @private */
  _onHeaderTrack(e) {
    const t = e.target;
    if (t.getAttribute("part") === "resize-handle") {
      let r = t.parentElement._column;
      for (this.$.scroller.toggleAttribute("column-resizing", !0); r.localName === "vaadin-grid-column-group"; )
        r = r._childColumns.slice(0).sort((f, p) => f._order - p._order).filter((f) => !f.hidden).pop();
      const o = this.__isRTL, l = e.detail.x, a = Array.from(this.$.header.querySelectorAll('[part~="row"]:last-child [part~="cell"]')), c = a.find((f) => f._column === r);
      if (c.offsetWidth) {
        const f = getComputedStyle(c._content), p = 10 + parseInt(f.paddingLeft) + parseInt(f.paddingRight) + parseInt(f.borderLeftWidth) + parseInt(f.borderRightWidth) + parseInt(f.marginLeft) + parseInt(f.marginRight);
        let b;
        const x = c.offsetWidth, T = c.getBoundingClientRect();
        c.hasAttribute("frozen-to-end") ? b = x + (o ? l - T.right : T.left - l) : b = x + (o ? T.left - l : l - T.right), r.width = `${Math.max(p, b)}px`, r.flexGrow = 0;
      }
      a.sort((f, p) => f._column._order - p._column._order).forEach((f, p, b) => {
        p < b.indexOf(c) && (f._column.width = `${f.offsetWidth}px`, f._column.flexGrow = 0);
      });
      const d = this._frozenToEndCells[0];
      if (d && this.$.table.scrollWidth > this.$.table.offsetWidth) {
        const f = d.getBoundingClientRect(), p = l - (o ? f.right : f.left);
        (o && p <= 0 || !o && p >= 0) && (this.$.table.scrollLeft += p);
      }
      e.detail.state === "end" && (this.$.scroller.toggleAttribute("column-resizing", !1), this.dispatchEvent(
        new CustomEvent("column-resize", {
          detail: { resizedColumn: r }
        })
      )), this._resizeHandler();
    }
  }
  /**
   * Fired when a column in the grid is resized by the user.
   *
   * @event column-resize
   * @param {Object} detail
   * @param {Object} detail.resizedColumn the column that was resized
   */
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function Sn(s, n, e = 0) {
  let t = n;
  for (const i of s.subCaches) {
    const r = i.parentCacheIndex;
    if (t <= r)
      break;
    if (t <= r + i.flatSize)
      return Sn(i, t - r - 1, e + 1);
    t -= i.flatSize;
  }
  return {
    cache: s,
    item: s.items[t],
    index: t,
    page: Math.floor(t / s.pageSize),
    level: e
  };
}
function rc({ getItemId: s }, n, e, t = 0, i = 0) {
  for (let r = 0; r < n.items.length; r++) {
    const o = n.items[r];
    if (o && s(o) === s(e))
      return {
        cache: n,
        level: t,
        item: o,
        index: r,
        page: Math.floor(r / n.pageSize),
        subCache: n.getSubCache(r),
        flatIndex: i + n.getFlatIndex(r)
      };
  }
  for (const r of n.subCaches) {
    const o = i + n.getFlatIndex(r.parentCacheIndex), l = rc({ getItemId: s }, r, e, t + 1, o + 1);
    if (l)
      return l;
  }
}
function nc(s, [n, ...e], t = 0) {
  n === 1 / 0 && (n = s.size - 1);
  const i = s.getFlatIndex(n), r = s.getSubCache(n);
  return r && r.flatSize > 0 && e.length ? nc(r, e, t + i + 1) : t + i;
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class ws {
  /**
   * A context object.
   *
   * @type {{ isExpanded: (item: unknown) => boolean }}
   */
  context;
  /**
   * The number of items to display per page.
   *
   * @type {number}
   */
  pageSize;
  /**
   * An array of cached items.
   *
   * @type {object[]}
   */
  items = [];
  /**
   * A map where the key is a requested page and the value is a callback
   * that will be called with data once the request is complete.
   *
   * @type {Record<number, Function>}
   */
  pendingRequests = {};
  /**
   * A map where the key is the index of an item in the `items` array
   * and the value is a sub-cache associated with that item.
   *
   * Note, it's intentionally defined as an object instead of a Map
   * to ensure that Object.entries() returns an array with keys sorted
   * in alphabetical order, rather than the order they were added.
   *
   * @type {Record<number, Cache>}
   * @private
   */
  __subCacheByIndex = {};
  /**
   * The number of items.
   *
   * @type {number}
   * @private
   */
  __size = 0;
  /**
   * The total number of items, including items from expanded sub-caches.
   *
   * @type {number}
   * @private
   */
  __flatSize = 0;
  /**
   * @param {Cache['context']} context
   * @param {number} pageSize
   * @param {number | undefined} size
   * @param {Cache | undefined} parentCache
   * @param {number | undefined} parentCacheIndex
   */
  constructor(n, e, t, i, r) {
    this.context = n, this.pageSize = e, this.size = t, this.parentCache = i, this.parentCacheIndex = r, this.__flatSize = t || 0;
  }
  /**
   * An item in the parent cache that the current cache is associated with.
   *
   * @return {object | undefined}
   */
  get parentItem() {
    return this.parentCache && this.parentCache.items[this.parentCacheIndex];
  }
  /**
   * An array of sub-caches sorted in the same order as their associated items
   * appear in the `items` array.
   *
   * @return {Cache[]}
   */
  get subCaches() {
    return Object.values(this.__subCacheByIndex);
  }
  /**
   * Whether the cache or any of its descendant caches have pending requests.
   *
   * @return {boolean}
   */
  get isLoading() {
    return Object.keys(this.pendingRequests).length > 0 ? !0 : this.subCaches.some((n) => n.isLoading);
  }
  /**
   * The total number of items, including items from expanded sub-caches.
   *
   * @return {number}
   */
  get flatSize() {
    return this.__flatSize;
  }
  /**
   * The total number of items, including items from expanded sub-caches.
   *
   * @protected
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get effectiveSize() {
    return this.flatSize;
  }
  /**
   * The number of items.
   *
   * @return {number}
   */
  get size() {
    return this.__size;
  }
  /**
   * Sets the number of items.
   *
   * @param {number} size
   */
  set size(n) {
    if (this.__size !== n) {
      if (this.__size = n, this.context.placeholder !== void 0) {
        this.items.length = n || 0;
        for (let t = 0; t < n; t++)
          this.items[t] ||= this.context.placeholder;
      }
      Object.keys(this.pendingRequests).forEach((t) => {
        parseInt(t) * this.pageSize >= this.size && delete this.pendingRequests[t];
      });
    }
  }
  /**
   * Recalculates the flattened size for the cache and its descendant caches recursively.
   */
  recalculateFlatSize() {
    this.__flatSize = !this.parentItem || this.context.isExpanded(this.parentItem) ? this.size + this.subCaches.reduce((n, e) => (e.recalculateFlatSize(), n + e.flatSize), 0) : 0;
  }
  /**
   * Adds an array of items corresponding to the given page
   * to the `items` array.
   *
   * @param {number} page
   * @param {object[]} items
   */
  setPage(n, e) {
    const t = n * this.pageSize;
    e.forEach((i, r) => {
      const o = t + r;
      (this.size === void 0 || o < this.size) && (this.items[o] = i);
    });
  }
  /**
   * Retrieves the sub-cache associated with the item at the given index
   * in the `items` array.
   *
   * @param {number} index
   * @return {Cache | undefined}
   */
  getSubCache(n) {
    return this.__subCacheByIndex[n];
  }
  /**
   * Removes the sub-cache associated with the item at the given index
   * in the `items` array.
   *
   * @param {number} index
   */
  removeSubCache(n) {
    delete this.__subCacheByIndex[n];
  }
  /**
   * Removes all sub-caches.
   */
  removeSubCaches() {
    this.__subCacheByIndex = {};
  }
  /**
   * Creates and associates a sub-cache for the item at the given index
   * in the `items` array.
   *
   * @param {number} index
   * @return {Cache}
   */
  createSubCache(n) {
    const e = new ws(this.context, this.pageSize, 0, this, n);
    return this.__subCacheByIndex[n] = e, e;
  }
  /**
   * Retrieves the flattened index corresponding to the given index
   * of an item in the `items` array.
   *
   * @param {number} index
   * @return {number}
   */
  getFlatIndex(n) {
    const e = Math.max(0, Math.min(this.size - 1, n));
    return this.subCaches.reduce((t, i) => {
      const r = i.parentCacheIndex;
      return e > r ? t + i.flatSize : t;
    }, e);
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  getItemForIndex(n) {
    const { item: e } = Sn(this, n);
    return e;
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  getCacheAndIndex(n) {
    const { cache: e, index: t } = Sn(this, n);
    return { cache: e, scaledIndex: t };
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  updateSize() {
    this.recalculateFlatSize();
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  ensureSubCacheForScaledIndex(n) {
    if (!this.getSubCache(n)) {
      const e = this.createSubCache(n);
      this.context.__controller.__loadCachePage(e, 0);
    }
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get grid() {
    return this.context.__controller.host;
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get itemCaches() {
    return this.__subCacheByIndex;
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class oc extends EventTarget {
  /**
   * The controller host element.
   *
   * @param {HTMLElement}
   */
  host;
  /**
   * A callback that returns data based on the passed params such as
   * `page`, `pageSize`, `parentItem`, etc.
   */
  dataProvider;
  /**
   * A callback that returns additional params that need to be passed
   * to the data provider callback with every request.
   */
  dataProviderParams;
  /**
   * A number of items to display per page.
   *
   * @type {number}
   */
  pageSize;
  /**
   * A callback that returns whether the given item is expanded.
   *
   * @type {(item: unknown) => boolean}
   */
  isExpanded;
  /**
   * A callback that returns the id for the given item and that
   * is used when checking object items for equality.
   *
   * @type { (item: unknown) => unknown}
   */
  getItemId;
  /**
   * A reference to the root cache instance.
   *
   * @param {Cache}
   */
  rootCache;
  /**
   * A placeholder item that is used to indicate that the item is not loaded yet.
   *
   * @type {unknown}
   */
  placeholder;
  /**
   * A callback that returns whether the given item is a placeholder.
   *
   * @type {(item: unknown) => boolean}
   */
  isPlaceholder;
  constructor(n, { size: e, pageSize: t, isExpanded: i, getItemId: r, isPlaceholder: o, placeholder: l, dataProvider: a, dataProviderParams: c }) {
    super(), this.host = n, this.pageSize = t, this.getItemId = r, this.isExpanded = i, this.placeholder = l, this.isPlaceholder = o, this.dataProvider = a, this.dataProviderParams = c, this.rootCache = this.__createRootCache(e);
  }
  /**
   * The total number of items, including items from expanded sub-caches.
   */
  get flatSize() {
    return this.rootCache.flatSize;
  }
  /** @private */
  get __cacheContext() {
    return {
      isExpanded: this.isExpanded,
      placeholder: this.placeholder,
      // The controller instance is needed to ensure deprecated cache methods work.
      __controller: this
    };
  }
  /**
   * Whether the root cache or any of its decendant caches have pending requests.
   *
   * @return {boolean}
   */
  isLoading() {
    return this.rootCache.isLoading;
  }
  /**
   * Sets the page size and clears the cache.
   *
   * @param {number} pageSize
   */
  setPageSize(n) {
    this.pageSize = n, this.clearCache();
  }
  /**
   * Sets the data provider callback and clears the cache.
   *
   * @type {Function}
   */
  setDataProvider(n) {
    this.dataProvider = n, this.clearCache();
  }
  /**
   * Recalculates the flattened size.
   */
  recalculateFlatSize() {
    this.rootCache.recalculateFlatSize();
  }
  /**
   * Clears the cache.
   */
  clearCache() {
    this.rootCache = this.__createRootCache(this.rootCache.size);
  }
  /**
   * Returns context for the given flattened index, including:
   * - the corresponding cache
   * - the cache level
   * - the corresponding item (if loaded)
   * - the item's index in the cache's items array
   * - the page containing the item
   *
   * @param {number} flatIndex
   */
  getFlatIndexContext(n) {
    return Sn(this.rootCache, n);
  }
  /**
   * Returns context for the given item, including:
   * - the cache containing the item
   * - the cache level
   * - the item
   * - the item's index in the cache's items array
   * - the item's flattened index
   * - the item's sub-cache (if exists)
   * - the page containing the item
   *
   * If the item isn't found, the method returns undefined.
   */
  getItemContext(n) {
    return rc({ getItemId: this.getItemId }, this.rootCache, n);
  }
  /**
   * Returns the flattened index for the item that the given indexes point to.
   * Each index in the path array points to a sub-item of the previous index.
   * Using `Infinity` as an index will point to the last item on the level.
   *
   * @param {number[]} path
   * @return {number}
   */
  getFlatIndexByPath(n) {
    return nc(this.rootCache, n);
  }
  /**
   * Requests the data provider to load the page with the item corresponding
   * to the given flattened index. If the item is already loaded, the method
   * returns immediatelly.
   *
   * @param {number} flatIndex
   */
  ensureFlatIndexLoaded(n) {
    const { cache: e, page: t, item: i } = this.getFlatIndexContext(n);
    this.__isItemLoaded(i) || this.__loadCachePage(e, t);
  }
  /**
   * Creates a sub-cache for the item corresponding to the given flattened index and
   * requests the data provider to load the first page into the created sub-cache.
   * If the sub-cache already exists, the method returns immediatelly.
   *
   * @param {number} flatIndex
   */
  ensureFlatIndexHierarchy(n) {
    const { cache: e, item: t, index: i } = this.getFlatIndexContext(n);
    if (this.__isItemLoaded(t) && this.isExpanded(t) && !e.getSubCache(i)) {
      const r = e.createSubCache(i);
      this.__loadCachePage(r, 0);
    }
  }
  /**
   * Loads the first page into the root cache.
   */
  loadFirstPage() {
    this.__loadCachePage(this.rootCache, 0);
  }
  /** @private */
  __createRootCache(n) {
    return new ws(this.__cacheContext, this.pageSize, n);
  }
  /** @private */
  __loadCachePage(n, e) {
    if (!this.dataProvider || n.pendingRequests[e])
      return;
    let t = {
      page: e,
      pageSize: this.pageSize,
      parentItem: n.parentItem
    };
    this.dataProviderParams && (t = { ...t, ...this.dataProviderParams() });
    const i = (r, o) => {
      n.pendingRequests[e] === i && (o !== void 0 ? n.size = o : t.parentItem && (n.size = r.length), n.setPage(e, r), this.recalculateFlatSize(), this.dispatchEvent(new CustomEvent("page-received")), delete n.pendingRequests[e], this.dispatchEvent(new CustomEvent("page-loaded")));
    };
    n.pendingRequests[e] = i, this.dispatchEvent(new CustomEvent("page-requested")), this.dataProvider(t, i);
  }
  /** @private */
  __isItemLoaded(n) {
    return this.isPlaceholder ? !this.isPlaceholder(n) : this.placeholder ? n !== this.placeholder : !!n;
  }
}
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const uf = (s) => class extends s {
  static get properties() {
    return {
      /**
       * The number of root-level items in the grid.
       * @attr {number} size
       * @type {number}
       */
      size: {
        type: Number,
        notify: !0,
        sync: !0
      },
      /**
       * @type {number}
       * @protected
       */
      _flatSize: {
        type: Number,
        sync: !0
      },
      /**
       * Number of items fetched at a time from the dataprovider.
       * @attr {number} page-size
       * @type {number}
       */
      pageSize: {
        type: Number,
        value: 50,
        observer: "_pageSizeChanged",
        sync: !0
      },
      /**
       * Function that provides items lazily. Receives arguments `params`, `callback`
       *
       * `params.page` Requested page index
       *
       * `params.pageSize` Current page size
       *
       * `params.filters` Currently applied filters
       *
       * `params.sortOrders` Currently applied sorting orders
       *
       * `params.parentItem` When tree is used, and sublevel items
       * are requested, reference to parent item of the requested sublevel.
       * Otherwise `undefined`.
       *
       * `callback(items, size)` Callback function with arguments:
       *   - `items` Current page of items
       *   - `size` Total number of items. When tree sublevel items
       *     are requested, total number of items in the requested sublevel.
       *     Optional when tree is not used, required for tree.
       *
       * @type {GridDataProvider | null | undefined}
       */
      dataProvider: {
        type: Object,
        notify: !0,
        observer: "_dataProviderChanged",
        sync: !0
      },
      /**
       * `true` while data is being requested from the data provider.
       */
      loading: {
        type: Boolean,
        notify: !0,
        readOnly: !0,
        reflectToAttribute: !0
      },
      /**
       * @protected
       */
      _hasData: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /**
       * Path to an item sub-property that indicates whether the item has child items.
       * @attr {string} item-has-children-path
       */
      itemHasChildrenPath: {
        type: String,
        value: "children",
        observer: "__itemHasChildrenPathChanged",
        sync: !0
      },
      /**
       * Path to an item sub-property that identifies the item.
       * @attr {string} item-id-path
       */
      itemIdPath: {
        type: String,
        value: null,
        sync: !0
      },
      /**
       * An array that contains the expanded items.
       * @type {!Array<!GridItem>}
       */
      expandedItems: {
        type: Object,
        notify: !0,
        value: () => [],
        sync: !0
      },
      /**
       * @private
       */
      __expandedKeys: {
        type: Object,
        computed: "__computeExpandedKeys(itemIdPath, expandedItems)"
      }
    };
  }
  static get observers() {
    return ["_sizeChanged(size)", "_expandedItemsChanged(expandedItems)"];
  }
  constructor() {
    super(), this._dataProviderController = new oc(this, {
      size: this.size || 0,
      pageSize: this.pageSize,
      getItemId: this.getItemId.bind(this),
      isExpanded: this._isExpanded.bind(this),
      dataProvider: this.dataProvider ? this.dataProvider.bind(this) : null,
      dataProviderParams: () => ({
        sortOrders: this._mapSorters(),
        filters: this._mapFilters()
      })
    }), this._dataProviderController.addEventListener("page-requested", this._onDataProviderPageRequested.bind(this)), this._dataProviderController.addEventListener("page-received", this._onDataProviderPageReceived.bind(this)), this._dataProviderController.addEventListener("page-loaded", this._onDataProviderPageLoaded.bind(this));
  }
  /**
   * @protected
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get _cache() {
    return this._dataProviderController.rootCache;
  }
  /**
   * @protected
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get _effectiveSize() {
    return this._flatSize;
  }
  /** @private */
  _sizeChanged(e) {
    this._dataProviderController.rootCache.size = e, this._dataProviderController.recalculateFlatSize(), this._flatSize = this._dataProviderController.flatSize;
  }
  /** @private */
  __itemHasChildrenPathChanged(e, t) {
    !t && e === "children" || this.requestContentUpdate();
  }
  /**
   * @param {number} index
   * @param {HTMLElement} el
   * @protected
   */
  _getItem(e, t) {
    t.index = e;
    const { item: i } = this._dataProviderController.getFlatIndexContext(e);
    i ? (this.__updateLoading(t, !1), this._updateItem(t, i), this._isExpanded(i) && this._dataProviderController.ensureFlatIndexHierarchy(e)) : (this.__updateLoading(t, !0), this._dataProviderController.ensureFlatIndexLoaded(e));
  }
  /**
   * @param {!HTMLElement} row
   * @param {boolean} loading
   * @private
   */
  __updateLoading(e, t) {
    const i = Bi(e);
    Fn(e, "loading", t), Zt(i, "loading-row-cell", t), t && (this._generateCellClassNames(e), this._generateCellPartNames(e));
  }
  /**
   * Returns a value that identifies the item. Uses `itemIdPath` if available.
   * Can be customized by overriding.
   * @param {!GridItem} item
   * @return {!GridItem | !unknown}
   */
  getItemId(e) {
    return this.itemIdPath ? _i(this.itemIdPath, e) : e;
  }
  /**
   * @param {!GridItem} item
   * @return {boolean}
   * @protected
   */
  _isExpanded(e) {
    return this.__expandedKeys && this.__expandedKeys.has(this.getItemId(e));
  }
  /** @private */
  _expandedItemsChanged() {
    this._dataProviderController.recalculateFlatSize(), this._flatSize = this._dataProviderController.flatSize, this.__updateVisibleRows();
  }
  /** @private */
  __computeExpandedKeys(e, t) {
    const i = t || [], r = /* @__PURE__ */ new Set();
    return i.forEach((o) => {
      r.add(this.getItemId(o));
    }), r;
  }
  /**
   * Expands the given item tree.
   * @param {!GridItem} item
   */
  expandItem(e) {
    this._isExpanded(e) || (this.expandedItems = [...this.expandedItems, e]);
  }
  /**
   * Collapses the given item tree.
   * @param {!GridItem} item
   */
  collapseItem(e) {
    this._isExpanded(e) && (this.expandedItems = this.expandedItems.filter((t) => !this._itemsEqual(t, e)));
  }
  /**
   * @param {number} index
   * @return {number}
   * @protected
   */
  _getIndexLevel(e = 0) {
    const { level: t } = this._dataProviderController.getFlatIndexContext(e);
    return t;
  }
  /**
   * @param {number} page
   * @param {ItemCache} cache
   * @protected
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  _loadPage(e, t) {
    this._dataProviderController.__loadCachePage(t, e);
  }
  /** @protected */
  _onDataProviderPageRequested() {
    this._setLoading(!0);
  }
  /** @protected */
  _onDataProviderPageReceived() {
    this._flatSize !== this._dataProviderController.flatSize && (this._shouldUpdateAllRenderedRowsAfterPageLoad = !0, this._flatSize = this._dataProviderController.flatSize), this._getRenderedRows().forEach((e) => {
      this._dataProviderController.ensureFlatIndexHierarchy(e.index);
    }), this._hasData = !0;
  }
  /** @protected */
  _onDataProviderPageLoaded() {
    this._debouncerApplyCachedData = ze.debounce(this._debouncerApplyCachedData, Nt.after(0), () => {
      this._setLoading(!1);
      const e = this._shouldUpdateAllRenderedRowsAfterPageLoad;
      this._shouldUpdateAllRenderedRowsAfterPageLoad = !1, this._getRenderedRows().forEach((t) => {
        const { item: i } = this._dataProviderController.getFlatIndexContext(t.index);
        (i || e) && this._getItem(t.index, t);
      }), this.__scrollToPendingIndexes(), this.__dispatchPendingBodyCellFocus();
    }), this._dataProviderController.isLoading() || this._debouncerApplyCachedData.flush();
  }
  /** @private */
  __debounceClearCache() {
    this.__clearCacheDebouncer = ze.debounce(this.__clearCacheDebouncer, yt, () => this.clearCache());
  }
  /**
   * Clears the cached pages and reloads data from dataprovider when needed.
   */
  clearCache() {
    this._dataProviderController.clearCache(), this._dataProviderController.rootCache.size = this.size, this._dataProviderController.recalculateFlatSize(), this._hasData = !1, this.__updateVisibleRows(), (!this.__virtualizer || !this.__virtualizer.size) && this._dataProviderController.loadFirstPage();
  }
  /** @private */
  _pageSizeChanged(e, t) {
    this._dataProviderController.setPageSize(e), t !== void 0 && e !== t && this.clearCache();
  }
  /** @protected */
  _checkSize() {
    this.size === void 0 && this._flatSize;
  }
  /** @private */
  _dataProviderChanged(e, t) {
    this._dataProviderController.setDataProvider(e ? e.bind(this) : null), t !== void 0 && this.clearCache(), this._ensureFirstPageLoaded(), this._debouncerCheckSize = ze.debounce(
      this._debouncerCheckSize,
      Nt.after(2e3),
      this._checkSize.bind(this)
    );
  }
  /** @protected */
  _ensureFirstPageLoaded() {
    this._hasData || this._dataProviderController.loadFirstPage();
  }
  /**
   * @param {!GridItem} item1
   * @param {!GridItem} item2
   * @return {boolean}
   * @protected
   */
  _itemsEqual(e, t) {
    return this.getItemId(e) === this.getItemId(t);
  }
  /**
   * @param {!GridItem} item
   * @param {!Array<!GridItem>} array
   * @return {number}
   * @protected
   */
  _getItemIndexInArray(e, t) {
    let i = -1;
    return t.forEach((r, o) => {
      this._itemsEqual(r, e) && (i = o);
    }), i;
  }
  /**
   * Scroll to a specific row index in the virtual list. Note that the row index is
   * not always the same for any particular item. For example, sorting or filtering
   * items can affect the row index related to an item.
   *
   * The `indexes` parameter can be either a single number or multiple numbers.
   * The grid will first try to scroll to the item at the first index on the top level.
   * In case the item at the first index is expanded, the grid will then try scroll to the
   * item at the second index within the children of the expanded first item, and so on.
   * Each given index points to a child of the item at the previous index.
   *
   * Using `Infinity` as an index will point to the last item on the level.
   *
   * @param indexes {...number} Row indexes to scroll to
   */
  scrollToIndex(...e) {
    let t;
    for (; t !== (t = this._dataProviderController.getFlatIndexByPath(e)); )
      this._scrollToFlatIndex(t);
    (this._dataProviderController.isLoading() || !this.clientHeight) && (this.__pendingScrollToIndexes = e);
  }
  /** @private */
  __scrollToPendingIndexes() {
    if (this.__pendingScrollToIndexes && this.$.items.children.length) {
      const e = this.__pendingScrollToIndexes;
      delete this.__pendingScrollToIndexes, this.scrollToIndex(...e);
    }
  }
  /**
   * Fired when the `expandedItems` property changes.
   *
   * @event expanded-items-changed
   */
  /**
   * Fired when the `loading` property changes.
   *
   * @event loading-changed
   */
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const sr = {
  BETWEEN: "between",
  ON_TOP_OR_BETWEEN: "on-top-or-between",
  ON_GRID: "on-grid"
}, qt = {
  ON_TOP: "on-top",
  ABOVE: "above",
  BELOW: "below",
  EMPTY: "empty"
}, ff = !("draggable" in document.createElement("div")), pf = (s) => class extends s {
  static get properties() {
    return {
      /**
       * Defines the locations within the Grid row where an element can be dropped.
       *
       * Possible values are:
       * - `between`: The drop event can happen between Grid rows.
       * - `on-top`: The drop event can happen on top of Grid rows.
       * - `on-top-or-between`: The drop event can happen either on top of or between Grid rows.
       * - `on-grid`: The drop event will not happen on any specific row, it will show the drop target outline around the whole grid.
       * @attr {between|on-top|on-top-or-between|on-grid} drop-mode
       * @type {GridDropMode | null | undefined}
       */
      dropMode: {
        type: String,
        sync: !0
      },
      /**
       * Marks the grid's rows to be available for dragging.
       * @attr {boolean} rows-draggable
       */
      rowsDraggable: {
        type: Boolean,
        sync: !0
      },
      /**
       * A function that filters dragging of specific grid rows. The return value should be false
       * if dragging of the row should be disabled.
       *
       * Receives one argument:
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *
       * @type {GridDragAndDropFilter | null | undefined}
       */
      dragFilter: {
        type: Function,
        sync: !0
      },
      /**
       * A function that filters dropping on specific grid rows. The return value should be false
       * if dropping on the row should be disabled.
       *
       * Receives one argument:
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *
       * @type {GridDragAndDropFilter | null | undefined}
       */
      dropFilter: {
        type: Function,
        sync: !0
      },
      /** @private */
      __dndAutoScrollThreshold: {
        value: 50
      },
      /** @private  */
      __draggedItems: {
        value: () => []
      }
    };
  }
  static get observers() {
    return ["_dragDropAccessChanged(rowsDraggable, dropMode, dragFilter, dropFilter, loading)"];
  }
  constructor() {
    super(), this.__onDocumentDragStart = this.__onDocumentDragStart.bind(this);
  }
  /** @protected */
  ready() {
    super.ready(), this.$.table.addEventListener("dragstart", this._onDragStart.bind(this)), this.$.table.addEventListener("dragend", this._onDragEnd.bind(this)), this.$.table.addEventListener("dragover", this._onDragOver.bind(this)), this.$.table.addEventListener("dragleave", this._onDragLeave.bind(this)), this.$.table.addEventListener("drop", this._onDrop.bind(this)), this.$.table.addEventListener("dragenter", (e) => {
      this.dropMode && (e.preventDefault(), e.stopPropagation());
    });
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback(), document.addEventListener("dragstart", this.__onDocumentDragStart, { capture: !0 });
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback(), document.removeEventListener("dragstart", this.__onDocumentDragStart, { capture: !0 });
  }
  /** @private */
  _onDragStart(e) {
    if (this.rowsDraggable) {
      let t = e.target;
      if (t.localName === "vaadin-grid-cell-content" && (t = t.assignedSlot.parentNode.parentNode), t.parentNode !== this.$.items)
        return;
      if (e.stopPropagation(), this.toggleAttribute("dragging-rows", !0), this._safari) {
        const l = t.style.transform;
        t.style.top = /translateY\((.*)\)/u.exec(l)[1], t.style.transform = "none", requestAnimationFrame(() => {
          t.style.top = "", t.style.transform = l;
        });
      }
      const i = t.getBoundingClientRect();
      ff ? e.dataTransfer.setDragImage(t) : e.dataTransfer.setDragImage(t, e.clientX - i.left, e.clientY - i.top);
      let r = [t];
      this._isSelected(t._item) && (r = this.__getViewportRows().filter((l) => this._isSelected(l._item)).filter((l) => !this.dragFilter || this.dragFilter(this.__getRowModel(l)))), this.__draggedItems = r.map((l) => l._item), e.dataTransfer.setData("text", this.__formatDefaultTransferData(r)), Di(t, { dragstart: r.length > 1 ? `${r.length}` : "" }), this.style.setProperty("--_grid-drag-start-x", `${e.clientX - i.left + 20}px`), this.style.setProperty("--_grid-drag-start-y", `${e.clientY - i.top + 10}px`), requestAnimationFrame(() => {
        Di(t, { dragstart: !1 }), this.style.setProperty("--_grid-drag-start-x", ""), this.style.setProperty("--_grid-drag-start-y", ""), this.requestContentUpdate();
      });
      const o = new CustomEvent("grid-dragstart", {
        detail: {
          draggedItems: [...this.__draggedItems],
          setDragData: (l, a) => e.dataTransfer.setData(l, a),
          setDraggedItemsCount: (l) => t.setAttribute("dragstart", l)
        }
      });
      o.originalEvent = e, this.dispatchEvent(o);
    }
  }
  /** @private */
  _onDragEnd(e) {
    this.toggleAttribute("dragging-rows", !1), e.stopPropagation();
    const t = new CustomEvent("grid-dragend");
    t.originalEvent = e, this.dispatchEvent(t), this.__draggedItems = [], this.requestContentUpdate();
  }
  /** @private */
  _onDragLeave(e) {
    e.stopPropagation(), this._clearDragStyles();
  }
  /** @private */
  _onDragOver(e) {
    if (this.dropMode) {
      if (this._dropLocation = void 0, this._dragOverItem = void 0, this.__dndAutoScroll(e.clientY)) {
        this._clearDragStyles();
        return;
      }
      let t = e.composedPath().find((i) => i.localName === "tr");
      if (!this._flatSize || this.dropMode === sr.ON_GRID)
        this._dropLocation = qt.EMPTY;
      else if (!t || t.parentNode !== this.$.items) {
        if (t)
          return;
        if (this.dropMode === sr.BETWEEN || this.dropMode === sr.ON_TOP_OR_BETWEEN)
          t = Array.from(this.$.items.children).filter((i) => !i.hidden).pop(), this._dropLocation = qt.BELOW;
        else
          return;
      } else {
        const i = t.getBoundingClientRect();
        if (this._dropLocation = qt.ON_TOP, this.dropMode === sr.BETWEEN) {
          const r = e.clientY - i.top < i.bottom - e.clientY;
          this._dropLocation = r ? qt.ABOVE : qt.BELOW;
        } else this.dropMode === sr.ON_TOP_OR_BETWEEN && (e.clientY - i.top < i.height / 3 ? this._dropLocation = qt.ABOVE : e.clientY - i.top > i.height / 3 * 2 && (this._dropLocation = qt.BELOW));
      }
      if (t && t.hasAttribute("drop-disabled")) {
        this._dropLocation = void 0;
        return;
      }
      e.stopPropagation(), e.preventDefault(), this._dropLocation === qt.EMPTY ? this.toggleAttribute("dragover", !0) : t ? (this._dragOverItem = t._item, t.getAttribute("dragover") !== this._dropLocation && za(t, { dragover: this._dropLocation })) : this._clearDragStyles();
    }
  }
  /** @private */
  __onDocumentDragStart(e) {
    if (e.target.contains(this) && this.$.items.offsetHeight > 2e4) {
      const t = this.$.items.style.maxHeight, i = this.$.table.style.overflow;
      this.$.items.style.maxHeight = "0", this.$.table.style.overflow = "hidden", requestAnimationFrame(() => {
        this.$.items.style.maxHeight = t, this.$.table.style.overflow = i;
      });
    }
  }
  /** @private */
  __dndAutoScroll(e) {
    if (this.__dndAutoScrolling)
      return !0;
    const t = this.$.header.getBoundingClientRect().bottom, i = this.$.footer.getBoundingClientRect().top, r = t - e + this.__dndAutoScrollThreshold, o = e - i + this.__dndAutoScrollThreshold;
    let l = 0;
    if (o > 0 ? l = o * 2 : r > 0 && (l = -r * 2), l) {
      const a = this.$.table.scrollTop;
      if (this.$.table.scrollTop += l, a !== this.$.table.scrollTop)
        return this.__dndAutoScrolling = !0, setTimeout(() => {
          this.__dndAutoScrolling = !1;
        }, 20), !0;
    }
  }
  /** @private */
  __getViewportRows() {
    const e = this.$.header.getBoundingClientRect().bottom, t = this.$.footer.getBoundingClientRect().top;
    return Array.from(this.$.items.children).filter((i) => {
      const r = i.getBoundingClientRect();
      return r.bottom > e && r.top < t;
    });
  }
  /** @protected */
  _clearDragStyles() {
    this.removeAttribute("dragover"), at(this.$.items, (e) => {
      za(e, { dragover: null });
    });
  }
  /** @private */
  __updateDragSourceParts(e, t) {
    Di(e, { "drag-source": this.__draggedItems.includes(t.item) });
  }
  /** @private */
  _onDrop(e) {
    if (this.dropMode) {
      e.stopPropagation(), e.preventDefault();
      const t = e.dataTransfer.types && Array.from(e.dataTransfer.types).map((r) => ({
        type: r,
        data: e.dataTransfer.getData(r)
      }));
      this._clearDragStyles();
      const i = new CustomEvent("grid-drop", {
        bubbles: e.bubbles,
        cancelable: e.cancelable,
        detail: {
          dropTargetItem: this._dragOverItem,
          dropLocation: this._dropLocation,
          dragData: t
        }
      });
      i.originalEvent = e, this.dispatchEvent(i);
    }
  }
  /** @private */
  __formatDefaultTransferData(e) {
    return e.map((t) => Array.from(t.children).filter((i) => !i.hidden && i.getAttribute("part").indexOf("details-cell") === -1).sort((i, r) => i._column._order > r._column._order ? 1 : -1).map((i) => i._content.textContent.trim()).filter((i) => i).join("	")).join(`
`);
  }
  /** @private */
  _dragDropAccessChanged() {
    this.filterDragAndDrop();
  }
  /**
   * Runs the `dragFilter` and `dropFilter` hooks for the visible cells.
   * If the filter depends on varying conditions, you may need to
   * call this function manually in order to update the draggability when
   * the conditions change.
   */
  filterDragAndDrop() {
    at(this.$.items, (e) => {
      e.hidden || this._filterDragAndDrop(e, this.__getRowModel(e));
    });
  }
  /**
   * @param {!HTMLElement} row
   * @param {!GridItemModel} model
   * @protected
   */
  _filterDragAndDrop(e, t) {
    const i = this.loading || e.hasAttribute("loading"), r = !this.rowsDraggable || i || this.dragFilter && !this.dragFilter(t), o = !this.dropMode || i || this.dropFilter && !this.dropFilter(t);
    Hi(e, (l) => {
      r ? l._content.removeAttribute("draggable") : l._content.setAttribute("draggable", !0);
    }), Di(e, {
      "drag-disabled": !!r,
      "drop-disabled": !!o
    });
  }
  /**
   * Fired when starting to drag grid rows.
   *
   * @event grid-dragstart
   * @param {Object} originalEvent The native dragstart event
   * @param {Object} detail
   * @param {Object} detail.draggedItems the items in the visible viewport that are dragged
   * @param {Function} detail.setDraggedItemsCount Overrides the default number shown in the drag image on multi row drag.
   * Parameter is of type number.
   * @param {Function} detail.setDragData Sets dataTransfer data for the drag operation.
   * Note that "text" is the only data type supported by all the browsers the grid currently supports (including IE11).
   * The function takes two parameters:
   * - type:string The type of the data
   * - data:string The data
   */
  /**
   * Fired when the dragging of the rows ends.
   *
   * @event grid-dragend
   * @param {Object} originalEvent The native dragend event
   */
  /**
   * Fired when a drop occurs on top of the grid.
   *
   * @event grid-drop
   * @param {Object} originalEvent The native drop event
   * @param {Object} detail
   * @param {Object} detail.dropTargetItem The item of the grid row on which the drop occurred.
   * @param {string} detail.dropLocation The position at which the drop event took place relative to a row.
   * Depending on the dropMode value, the drop location can be one of the following
   * - `on-top`: when the drop occurred on top of the row
   * - `above`: when the drop occurred above the row
   * - `below`: when the drop occurred below the row
   * - `empty`: when the drop occurred over the grid, not relative to any specific row
   * @param {string} detail.dragData An array of items with the payload as a string representation as the
   * `data` property and the type of the data as `type` property.
   */
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function sc(s, n) {
  if (!s || !n || s.length !== n.length)
    return !1;
  for (let e = 0, t = s.length; e < t; e++)
    if (s[e] instanceof Array && n[e] instanceof Array) {
      if (!sc(s[e], n[e]))
        return !1;
    } else if (s[e] !== n[e])
      return !1;
  return !0;
}
const gf = (s) => class extends s {
  static get properties() {
    return {
      /**
       * @protected
       */
      _columnTree: Object
    };
  }
  /** @protected */
  ready() {
    super.ready(), this._addNodeObserver();
  }
  /** @private */
  _hasColumnGroups(e) {
    return e.some((t) => t.localName === "vaadin-grid-column-group");
  }
  /**
   * @param {!GridColumnGroup} el
   * @return {!Array<!GridColumn>}
   * @protected
   */
  _getChildColumns(e) {
    return Li.getColumns(e);
  }
  /** @private */
  _flattenColumnGroups(e) {
    return e.map((t) => t.localName === "vaadin-grid-column-group" ? this._getChildColumns(t) : [t]).reduce((t, i) => t.concat(i), []);
  }
  /** @private */
  _getColumnTree() {
    const e = Li.getColumns(this), t = [e];
    let i = e;
    for (; this._hasColumnGroups(i); )
      i = this._flattenColumnGroups(i), t.push(i);
    return t;
  }
  /** @protected */
  _debounceUpdateColumnTree() {
    this.__updateColumnTreeDebouncer = ze.debounce(
      this.__updateColumnTreeDebouncer,
      yt,
      () => this._updateColumnTree()
    );
  }
  /** @protected */
  _updateColumnTree() {
    const e = this._getColumnTree();
    sc(e, this._columnTree) || (e.forEach((t) => {
      t.forEach((i) => {
        i.performUpdate && i.performUpdate();
      });
    }), this._columnTree = e);
  }
  /** @private */
  _addNodeObserver() {
    this._observer = new Li(this, (e, t) => {
      const i = t.flatMap((o) => o._allCells), r = (o) => i.filter((l) => l && l._content.contains(o)).length;
      this.__removeSorters(this._sorters.filter(r)), this.__removeFilters(this._filters.filter(r)), this._debounceUpdateColumnTree(), this._debouncerCheckImports = ze.debounce(
        this._debouncerCheckImports,
        Nt.after(2e3),
        this._checkImports.bind(this)
      ), this._ensureFirstPageLoaded();
    });
  }
  /** @protected */
  _checkImports() {
    [
      "vaadin-grid-column-group",
      "vaadin-grid-filter",
      "vaadin-grid-filter-column",
      "vaadin-grid-tree-toggle",
      "vaadin-grid-selection-column",
      "vaadin-grid-sort-column",
      "vaadin-grid-sorter"
    ].forEach((e) => {
      this.querySelector(e) && customElements.get(e);
    });
  }
  /** @protected */
  _updateFirstAndLastColumn() {
    Array.from(this.shadowRoot.querySelectorAll("tr")).forEach((e) => this._updateFirstAndLastColumnForRow(e));
  }
  /**
   * @param {!HTMLElement} row
   * @protected
   */
  _updateFirstAndLastColumnForRow(e) {
    Array.from(e.querySelectorAll('[part~="cell"]:not([part~="details-cell"])')).sort((t, i) => t._column._order - i._column._order).forEach((t, i, r) => {
      ci(t, "first-column", i === 0), ci(t, "last-column", i === r.length - 1);
    });
  }
  /**
   * @param {!Node} node
   * @return {boolean}
   * @protected
   */
  _isColumnElement(e) {
    return e.nodeType === Node.ELEMENT_NODE && /\bcolumn\b/u.test(e.localName);
  }
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const mf = (s) => class extends s {
  /**
   * Returns an object with context information about the event target:
   * - `item`: the data object corresponding to the targeted row (not specified when targeting header or footer)
   * - `column`: the column element corresponding to the targeted cell (not specified when targeting row details)
   * - `section`: whether the event targeted the body, header, footer or details of the grid
   *
   * These additional properties are included when `item` is specified:
   * - `index`: the index of the item
   * - `selected`: the selected state of the item
   * - `detailsOpened`: whether the row details are open for the item
   * - `expanded`: the expanded state of the tree toggle
   * - `level`: the tree hierarchy level
   *
   * The returned object is populated only when a grid cell, header, footer or row details is found in `event.composedPath()`.
   * This means mostly mouse and keyboard events. If such a grid part is not found in the path, an empty object is returned.
   * This may be the case eg. if the event is fired on the `<vaadin-grid>` element and not any deeper in the DOM, or if
   * the event targets the empty part of the grid body.
   *
   * @param {!Event} event
   * @return {GridEventContext}
   */
  getEventContext(e) {
    const t = {}, { cell: i } = this._getGridEventLocation(e);
    return i && (t.section = ["body", "header", "footer", "details"].find(
      (r) => i.getAttribute("part").indexOf(r) > -1
    ), i._column && (t.column = i._column), (t.section === "body" || t.section === "details") && Object.assign(t, this.__getRowModel(i.parentElement))), t;
  }
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const _f = (s) => class extends s {
  static get properties() {
    return {
      /** @private */
      _filters: {
        type: Array,
        value: () => []
      }
    };
  }
  constructor() {
    super(), this._filterChanged = this._filterChanged.bind(this), this.addEventListener("filter-changed", this._filterChanged);
  }
  /** @private */
  _filterChanged(e) {
    e.stopPropagation(), this.__addFilter(e.target), this.__applyFilters();
  }
  /** @private */
  __removeFilters(e) {
    e.length !== 0 && (this._filters = this._filters.filter((t) => e.indexOf(t) < 0), this.__applyFilters());
  }
  /** @private */
  __addFilter(e) {
    this._filters.indexOf(e) === -1 && this._filters.push(e);
  }
  /** @private */
  __applyFilters() {
    this.dataProvider && this.isAttached && this.clearCache();
  }
  /**
   * @return {!Array<!GridFilterDefinition>}
   * @protected
   */
  _mapFilters() {
    return this._filters.map((e) => ({
      path: e.path,
      value: e.value
    }));
  }
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function Jr(s) {
  return s instanceof HTMLTableRowElement;
}
function $r(s) {
  return s instanceof HTMLTableCellElement;
}
function li(s) {
  return s.matches('[part~="details-cell"]');
}
const vf = (s) => class extends s {
  static get properties() {
    return {
      /** @private */
      _headerFocusable: {
        type: Object,
        observer: "_focusableChanged",
        sync: !0
      },
      /**
       * @type {!HTMLElement | undefined}
       * @protected
       */
      _itemsFocusable: {
        type: Object,
        observer: "_focusableChanged",
        sync: !0
      },
      /** @private */
      _footerFocusable: {
        type: Object,
        observer: "_focusableChanged",
        sync: !0
      },
      /** @private */
      _navigatingIsHidden: Boolean,
      /**
       * @type {number}
       * @protected
       */
      _focusedItemIndex: {
        type: Number,
        value: 0
      },
      /** @private */
      _focusedColumnOrder: Number,
      /** @private */
      _focusedCell: {
        type: Object,
        observer: "_focusedCellChanged",
        sync: !0
      },
      /**
       * Indicates whether the grid is currently in interaction mode.
       * In interaction mode the user is currently interacting with a control,
       * such as an input or a select, within a cell.
       * In interaction mode keyboard navigation between cells is disabled.
       * Interaction mode also prevents the focus target cell of that section of
       * the grid from receiving focus, allowing the user to switch focus to
       * controls in adjacent cells, rather than focussing the outer cell
       * itself.
       * @type {boolean}
       * @private
       */
      interacting: {
        type: Boolean,
        value: !1,
        reflectToAttribute: !0,
        readOnly: !0,
        observer: "_interactingChanged"
      }
    };
  }
  /** @private */
  get __rowFocusMode() {
    return [this._headerFocusable, this._itemsFocusable, this._footerFocusable].some(Jr);
  }
  set __rowFocusMode(e) {
    ["_itemsFocusable", "_footerFocusable", "_headerFocusable"].forEach((t) => {
      const i = this[t];
      if (e) {
        const r = i && i.parentElement;
        $r(i) ? this[t] = r : $r(r) && (this[t] = r.parentElement);
      } else if (!e && Jr(i)) {
        const r = i.firstElementChild;
        this[t] = r._focusButton || r;
      }
    });
  }
  /** @private */
  get _visibleItemsCount() {
    return this._lastVisibleIndex - this._firstVisibleIndex - 1;
  }
  /** @protected */
  ready() {
    super.ready(), !(this._ios || this._android) && (this.addEventListener("keydown", this._onKeyDown), this.addEventListener("keyup", this._onKeyUp), this.addEventListener("focusin", this._onFocusIn), this.addEventListener("focusout", this._onFocusOut), this.$.table.addEventListener("focusin", this._onContentFocusIn.bind(this)), this.addEventListener("mousedown", () => {
      this.toggleAttribute("navigating", !1), this._isMousedown = !0, this._focusedColumnOrder = void 0;
    }), this.addEventListener("mouseup", () => {
      this._isMousedown = !1;
    }));
  }
  /** @private */
  _focusableChanged(e, t) {
    t && t.setAttribute("tabindex", "-1"), e && this._updateGridSectionFocusTarget(e);
  }
  /** @private */
  _focusedCellChanged(e, t) {
    t && fs(t, "part", "focused-cell"), e && zn(e, "part", "focused-cell");
  }
  /** @private */
  _interactingChanged() {
    this._updateGridSectionFocusTarget(this._headerFocusable), this._updateGridSectionFocusTarget(this._itemsFocusable), this._updateGridSectionFocusTarget(this._footerFocusable);
  }
  /**
   * Since the focused cell/row state is stored as an element reference, the reference may get
   * out of sync when the virtual indexes for elements update due to effective size change.
   * This function updates the reference to the correct element after a possible index change.
   * @private
   */
  __updateItemsFocusable() {
    if (!this._itemsFocusable)
      return;
    const e = this.shadowRoot.activeElement === this._itemsFocusable;
    this._getRenderedRows().forEach((t) => {
      if (t.index === this._focusedItemIndex)
        if (this.__rowFocusMode)
          this._itemsFocusable = t;
        else {
          let i = this._itemsFocusable.parentElement, r = this._itemsFocusable;
          if (i) {
            $r(i) && (r = i, i = i.parentElement);
            const o = [...i.children].indexOf(r);
            this._itemsFocusable = this.__getFocusable(t, t.children[o]);
          }
        }
    }), e && this._itemsFocusable.focus();
  }
  /**
   * @param {!KeyboardEvent} e
   * @protected
   */
  _onKeyDown(e) {
    const t = e.key;
    let i;
    switch (t) {
      case "ArrowUp":
      case "ArrowDown":
      case "ArrowLeft":
      case "ArrowRight":
      case "PageUp":
      case "PageDown":
      case "Home":
      case "End":
        i = "Navigation";
        break;
      case "Enter":
      case "Escape":
      case "F2":
        i = "Interaction";
        break;
      case "Tab":
        i = "Tab";
        break;
      case " ":
        i = "Space";
        break;
    }
    this._detectInteracting(e), this.interacting && i !== "Interaction" && (i = void 0), i && this[`_on${i}KeyDown`](e, t);
  }
  /** @private */
  __ensureFlatIndexInViewport(e) {
    [...this.$.items.children].find((i) => i.index === e) ? this.__scrollIntoViewport(e) : this._scrollToFlatIndex(e);
  }
  /** @private */
  __isRowExpandable(e) {
    if (this.itemHasChildrenPath) {
      const t = e._item;
      return !!(t && _i(this.itemHasChildrenPath, t) && !this._isExpanded(t));
    }
  }
  /** @private */
  __isRowCollapsible(e) {
    return this._isExpanded(e._item);
  }
  /** @private */
  _onNavigationKeyDown(e, t) {
    e.preventDefault();
    const i = this.__isRTL, r = e.composedPath().find(Jr), o = e.composedPath().find($r);
    let l = 0, a = 0;
    switch (t) {
      case "ArrowRight":
        l = i ? -1 : 1;
        break;
      case "ArrowLeft":
        l = i ? 1 : -1;
        break;
      case "Home":
        this.__rowFocusMode || e.ctrlKey ? a = -1 / 0 : l = -1 / 0;
        break;
      case "End":
        this.__rowFocusMode || e.ctrlKey ? a = 1 / 0 : l = 1 / 0;
        break;
      case "ArrowDown":
        a = 1;
        break;
      case "ArrowUp":
        a = -1;
        break;
      case "PageDown":
        if (this.$.items.contains(r)) {
          const f = this.__getIndexInGroup(r, this._focusedItemIndex);
          this._scrollToFlatIndex(f);
        }
        a = this._visibleItemsCount;
        break;
      case "PageUp":
        a = -this._visibleItemsCount;
        break;
    }
    if (this.__rowFocusMode && !r || !this.__rowFocusMode && !o)
      return;
    const c = i ? "ArrowLeft" : "ArrowRight", d = i ? "ArrowRight" : "ArrowLeft";
    if (t === c) {
      if (this.__rowFocusMode) {
        if (this.__isRowExpandable(r)) {
          this.expandItem(r._item);
          return;
        }
        this.__rowFocusMode = !1, this._onCellNavigation(r.firstElementChild, 0, 0);
        return;
      }
    } else if (t === d)
      if (this.__rowFocusMode) {
        if (this.__isRowCollapsible(r)) {
          this.collapseItem(r._item);
          return;
        }
      } else {
        const f = [...r.children].sort((p, b) => p._order - b._order);
        if (o === f[0] || li(o)) {
          this.__rowFocusMode = !0, this._onRowNavigation(r, 0);
          return;
        }
      }
    this.__rowFocusMode ? this._onRowNavigation(r, a) : this._onCellNavigation(o, l, a);
  }
  /**
   * Focuses the target row after navigating by the given dy offset.
   * If the row is not in the viewport, it is first scrolled to.
   * @private
   */
  _onRowNavigation(e, t) {
    const { dstRow: i } = this.__navigateRows(t, e);
    i && i.focus();
  }
  /** @private */
  __getIndexInGroup(e, t) {
    const i = e.parentNode;
    return i === this.$.items ? t !== void 0 ? t : e.index : [...i.children].indexOf(e);
  }
  /**
   * Returns the target row after navigating by the given dy offset.
   * Also returns information whether the details cell should be the target on the target row.
   * If the row is not in the viewport, it is first scrolled to.
   * @private
   */
  __navigateRows(e, t, i) {
    const r = this.__getIndexInGroup(t, this._focusedItemIndex), o = t.parentNode, l = (o === this.$.items ? this._flatSize : o.children.length) - 1;
    let a = Math.max(0, Math.min(r + e, l));
    if (o !== this.$.items) {
      if (a > r)
        for (; a < l && o.children[a].hidden; )
          a += 1;
      else if (a < r)
        for (; a > 0 && o.children[a].hidden; )
          a -= 1;
      return this.toggleAttribute("navigating", !0), { dstRow: o.children[a] };
    }
    let c = !1;
    if (i) {
      const d = li(i);
      if (o === this.$.items) {
        const f = t._item, { item: p } = this._dataProviderController.getFlatIndexContext(a);
        d ? c = e === 0 : c = e === 1 && this._isDetailsOpened(f) || e === -1 && a !== r && this._isDetailsOpened(p), c !== d && (e === 1 && c || e === -1 && !c) && (a = r);
      }
    }
    return this.__ensureFlatIndexInViewport(a), this._focusedItemIndex = a, this.toggleAttribute("navigating", !0), {
      dstRow: [...o.children].find((d) => !d.hidden && d.index === a),
      dstIsRowDetails: c
    };
  }
  /**
   * Focuses the target cell after navigating by the given dx and dy offset.
   * If the cell is not in the viewport, it is first scrolled to.
   * @private
   */
  _onCellNavigation(e, t, i) {
    const r = e.parentNode, { dstRow: o, dstIsRowDetails: l } = this.__navigateRows(i, r, e);
    if (!o)
      return;
    let a = [...r.children].indexOf(e);
    this.$.items.contains(e) && (a = [...this.$.sizer.children].findIndex((p) => p._column === e._column));
    const c = li(e), d = r.parentNode, f = this.__getIndexInGroup(r, this._focusedItemIndex);
    if (this._focusedColumnOrder === void 0 && (c ? this._focusedColumnOrder = 0 : this._focusedColumnOrder = this._getColumns(d, f).filter((p) => !p.hidden)[a]._order), l)
      [...o.children].find(li).focus();
    else {
      const p = this.__getIndexInGroup(o, this._focusedItemIndex), b = this._getColumns(d, p).filter((ae) => !ae.hidden), x = b.map((ae) => ae._order).sort((ae, se) => ae - se), T = x.length - 1, I = x.indexOf(
        x.slice(0).sort((ae, se) => Math.abs(ae - this._focusedColumnOrder) - Math.abs(se - this._focusedColumnOrder))[0]
      ), z = i === 0 && c ? I : Math.max(0, Math.min(I + t, T));
      z !== I && (this._focusedColumnOrder = void 0);
      const j = b.reduce((ae, se, fe) => (ae[se._order] = fe, ae), {})[x[z]];
      let oe;
      if (this.$.items.contains(e)) {
        const ae = this.$.sizer.children[j];
        this._lazyColumns && (this.__isColumnInViewport(ae._column) || ae.scrollIntoView(), this.__updateColumnsBodyContentHidden(), this.__updateHorizontalScrollPosition()), oe = [...o.children].find((se) => se._column === ae._column), this._scrollHorizontallyToCell(oe);
      } else
        oe = o.children[j], this._scrollHorizontallyToCell(oe);
      oe.focus();
    }
  }
  /** @private */
  _onInteractionKeyDown(e, t) {
    const i = e.composedPath()[0], r = i.localName === "input" && !/^(button|checkbox|color|file|image|radio|range|reset|submit)$/iu.test(i.type);
    let o;
    switch (t) {
      case "Enter":
        o = this.interacting ? !r : !0;
        break;
      case "Escape":
        o = !1;
        break;
      case "F2":
        o = !this.interacting;
        break;
    }
    const { cell: l } = this._getGridEventLocation(e);
    if (this.interacting !== o && l !== null)
      if (o) {
        const a = l._content.querySelector("[focus-target]") || // If a child element hasn't been explicitly marked as a focus target,
        // fall back to any focusable element inside the cell.
        [...l._content.querySelectorAll("*")].find((c) => this._isFocusable(c));
        a && (e.preventDefault(), a.focus(), this._setInteracting(!0), this.toggleAttribute("navigating", !1));
      } else
        e.preventDefault(), this._focusedColumnOrder = void 0, l.focus(), this._setInteracting(!1), this.toggleAttribute("navigating", !0);
    t === "Escape" && this._hideTooltip(!0);
  }
  /** @private */
  _predictFocusStepTarget(e, t) {
    const i = [
      this.$.table,
      this._headerFocusable,
      this.__emptyState ? this.$.emptystatecell : this._itemsFocusable,
      this._footerFocusable,
      this.$.focusexit
    ];
    let r = i.indexOf(e);
    for (r += t; r >= 0 && r <= i.length - 1; ) {
      let l = i[r];
      if (l && !this.__rowFocusMode && (l = i[r].parentNode), !l || l.hidden)
        r += t;
      else
        break;
    }
    let o = i[r];
    if (o && !this.__isHorizontallyInViewport(o)) {
      const l = this._getColumnsInOrder().find((a) => this.__isColumnInViewport(a));
      if (l)
        if (o === this._headerFocusable)
          o = l._headerCell;
        else if (o === this._itemsFocusable) {
          const a = o._column._cells.indexOf(o);
          o = l._cells[a];
        } else o === this._footerFocusable && (o = l._footerCell);
    }
    return o;
  }
  /** @private */
  _onTabKeyDown(e) {
    const t = this._predictFocusStepTarget(e.composedPath()[0], e.shiftKey ? -1 : 1);
    t && (e.stopPropagation(), t === this.$.table ? this.$.table.focus() : t === this.$.focusexit ? this.$.focusexit.focus() : t === this._itemsFocusable ? (this.__ensureFlatIndexInViewport(this._focusedItemIndex), this.__updateItemsFocusable(), e.preventDefault(), this._itemsFocusable.focus()) : (e.preventDefault(), t.focus()), this.toggleAttribute("navigating", !0));
  }
  /** @private */
  _onSpaceKeyDown(e) {
    e.preventDefault();
    const t = e.composedPath()[0], i = Jr(t);
    (i || !t._content || !t._content.firstElementChild) && this.dispatchEvent(
      new CustomEvent(i ? "row-activate" : "cell-activate", {
        detail: {
          model: this.__getRowModel(i ? t : t.parentElement)
        }
      })
    );
  }
  /** @private */
  _onKeyUp(e) {
    if (!/^( |SpaceBar)$/u.test(e.key) || this.interacting)
      return;
    e.preventDefault();
    const t = e.composedPath()[0];
    if (t._content && t._content.firstElementChild) {
      const i = this.hasAttribute("navigating");
      t._content.firstElementChild.dispatchEvent(
        new MouseEvent("click", {
          shiftKey: e.shiftKey,
          bubbles: !0,
          composed: !0,
          cancelable: !0
        })
      ), this.toggleAttribute("navigating", i);
    }
  }
  /**
   * @param {!FocusEvent} e
   * @protected
   */
  _onFocusIn(e) {
    this._isMousedown || this.toggleAttribute("navigating", !0);
    const t = e.composedPath()[0];
    t === this.$.table || t === this.$.focusexit ? (this._isMousedown || this._predictFocusStepTarget(t, t === this.$.table ? 1 : -1).focus(), this._setInteracting(!1)) : this._detectInteracting(e);
  }
  /**
   * @param {!FocusEvent} e
   * @protected
   */
  _onFocusOut(e) {
    this.toggleAttribute("navigating", !1), this._detectInteracting(e), this._hideTooltip(), this._focusedCell = null;
  }
  /** @private */
  _onContentFocusIn(e) {
    const { section: t, cell: i, row: r } = this._getGridEventLocation(e);
    if (!(!i && !this.__rowFocusMode) && (this._detectInteracting(e), t && (i || r)))
      if (this._activeRowGroup = t, t === this.$.header ? this._headerFocusable = this.__getFocusable(r, i) : t === this.$.items ? (this._itemsFocusable = this.__getFocusable(r, i), this._focusedItemIndex = r.index) : t === this.$.footer && (this._footerFocusable = this.__getFocusable(r, i)), i) {
        const o = this.getEventContext(e);
        this.__pendingBodyCellFocus = this.loading && o.section === "body", !this.__pendingBodyCellFocus && i !== this.$.emptystatecell && i.dispatchEvent(new CustomEvent("cell-focus", { bubbles: !0, composed: !0, detail: { context: o } })), this._focusedCell = i._focusButton || i, Ln() && e.target === i && this._showTooltip(e);
      } else
        this._focusedCell = null;
  }
  /**
   * @private
   */
  __dispatchPendingBodyCellFocus() {
    this.__pendingBodyCellFocus && this.shadowRoot.activeElement === this._itemsFocusable && this._itemsFocusable.dispatchEvent(new Event("focusin", { bubbles: !0, composed: !0 }));
  }
  /**
   * Get the focusable element depending on the current focus mode.
   * It can be a row, a cell, or a focusable div inside a cell.
   *
   * @param {HTMLElement} row
   * @param {HTMLElement} cell
   * @return {HTMLElement}
   * @private
   */
  __getFocusable(e, t) {
    return this.__rowFocusMode ? e : t._focusButton || t;
  }
  /**
   * Enables interaction mode if a cells descendant receives focus or keyboard
   * input. Disables it if the event is not related to cell content.
   * @param {!KeyboardEvent|!FocusEvent} e
   * @private
   */
  _detectInteracting(e) {
    const t = e.composedPath().some((i) => i.localName === "slot" && this.shadowRoot.contains(i));
    this._setInteracting(t), this.__updateHorizontalScrollPosition();
  }
  /**
   * Enables or disables the focus target of the containing section of the
   * grid from receiving focus, based on whether the user is interacting with
   * that section of the grid.
   * @param {HTMLElement} focusTarget
   * @private
   */
  _updateGridSectionFocusTarget(e) {
    if (!e)
      return;
    const t = this._getGridSectionFromFocusTarget(e), i = this.interacting && t === this._activeRowGroup;
    e.tabIndex = i ? -1 : 0;
  }
  /** @protected */
  _preventScrollerRotatingCellFocus() {
    this._activeRowGroup === this.$.items && (this.__preventScrollerRotatingCellFocusDebouncer = ze.debounce(
      this.__preventScrollerRotatingCellFocusDebouncer,
      Xt,
      () => {
        const e = this._activeRowGroup === this.$.items;
        this._getRenderedRows().some((i) => i.index === this._focusedItemIndex) ? (this.__updateItemsFocusable(), e && !this.__rowFocusMode && (this._focusedCell = this._itemsFocusable), this._navigatingIsHidden && (this.toggleAttribute("navigating", !0), this._navigatingIsHidden = !1)) : e && (this._focusedCell = null, this.hasAttribute("navigating") && (this._navigatingIsHidden = !0, this.toggleAttribute("navigating", !1)));
      }
    ));
  }
  /**
   * @param {HTMLTableSectionElement=} rowGroup
   * @param {number=} rowIndex
   * @return {!Array<!GridColumn>}
   * @protected
   */
  _getColumns(e, t) {
    let i = this._columnTree.length - 1;
    return e === this.$.header ? i = t : e === this.$.footer && (i = this._columnTree.length - 1 - t), this._columnTree[i];
  }
  /** @private */
  __isValidFocusable(e) {
    return this.$.table.contains(e) && e.offsetHeight;
  }
  /** @protected */
  _resetKeyboardNavigation() {
    if (!this.$ && this.performUpdate && this.performUpdate(), ["header", "footer"].forEach((e) => {
      if (!this.__isValidFocusable(this[`_${e}Focusable`])) {
        const t = [...this.$[e].children].find((r) => r.offsetHeight), i = t ? [...t.children].find((r) => !r.hidden) : null;
        t && i && (this[`_${e}Focusable`] = this.__getFocusable(t, i));
      }
    }), !this.__isValidFocusable(this._itemsFocusable) && this.$.items.firstElementChild) {
      const e = this.__getFirstVisibleItem(), t = e ? [...e.children].find((i) => !i.hidden) : null;
      t && e && (this._focusedColumnOrder = void 0, this._itemsFocusable = this.__getFocusable(e, t));
    } else
      this.__updateItemsFocusable();
  }
  /**
   * @param {!HTMLElement} dstCell
   * @protected
   */
  _scrollHorizontallyToCell(e) {
    if (e.hasAttribute("frozen") || e.hasAttribute("frozen-to-end") || li(e))
      return;
    const t = e.getBoundingClientRect(), i = e.parentNode, r = Array.from(i.children).indexOf(e), o = this.$.table.getBoundingClientRect();
    let l = o.left, a = o.right;
    for (let c = r - 1; c >= 0; c--) {
      const d = i.children[c];
      if (!(d.hasAttribute("hidden") || li(d)) && (d.hasAttribute("frozen") || d.hasAttribute("frozen-to-end"))) {
        l = d.getBoundingClientRect().right;
        break;
      }
    }
    for (let c = r + 1; c < i.children.length; c++) {
      const d = i.children[c];
      if (!(d.hasAttribute("hidden") || li(d)) && (d.hasAttribute("frozen") || d.hasAttribute("frozen-to-end"))) {
        a = d.getBoundingClientRect().left;
        break;
      }
    }
    t.left < l && (this.$.table.scrollLeft += Math.round(t.left - l)), t.right > a && (this.$.table.scrollLeft += Math.round(t.right - a));
  }
  /**
   * @typedef {Object} GridEventLocation
   * @property {HTMLTableSectionElement | null} section - The table section element that the event occurred in (header, body, or footer), or null if the event did not occur in a section
   * @property {HTMLTableRowElement | null} row - The row element that the event occurred in, or null if the event did not occur in a row
   * @property {HTMLTableCellElement | null} cell - The cell element that the event occurred in, or null if the event did not occur in a cell
   * @private
   */
  /**
   * Takes an event and returns a location object describing in which part of the grid the event occurred.
   * The event may either target table section, a row, a cell or contents of a cell.
   * @param {Event} e
   * @returns {GridEventLocation}
   * @protected
   */
  _getGridEventLocation(e) {
    const t = e.__composedPath || e.composedPath(), i = t.indexOf(this.$.table), r = i >= 1 ? t[i - 1] : null, o = i >= 2 ? t[i - 2] : null, l = i >= 3 ? t[i - 3] : null;
    return {
      section: r,
      row: o,
      cell: l
    };
  }
  /**
   * Helper method that maps a focus target cell to the containing grid section
   * @param {HTMLElement} focusTarget
   * @returns {HTMLTableSectionElement | null}
   * @private
   */
  _getGridSectionFromFocusTarget(e) {
    return e === this._headerFocusable ? this.$.header : e === this._itemsFocusable ? this.$.items : e === this._footerFocusable ? this.$.footer : null;
  }
  /**
   * Fired when a cell is focused with click or keyboard navigation.
   *
   * Use context property of @see {@link GridCellFocusEvent} to get detail information about the event.
   *
   * @event cell-focus
   */
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const yf = (s) => class extends s {
  static get properties() {
    return {
      /**
       * An array containing references to items with open row details.
       * @type {!Array<!GridItem>}
       */
      detailsOpenedItems: {
        type: Array,
        value: () => [],
        sync: !0
      },
      /**
       * Custom function for rendering the content of the row details.
       * Receives three arguments:
       *
       * - `root` The row details content DOM element. Append your content to it.
       * - `grid` The `<vaadin-grid>` element.
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.level` The number of the item's tree sublevel, starts from 0.
       *   - `model.expanded` True if the item's tree sublevel is expanded.
       *   - `model.selected` True if the item is selected.
       *
       * @type {GridRowDetailsRenderer | null | undefined}
       */
      rowDetailsRenderer: {
        type: Function,
        sync: !0
      },
      /**
       * @type {!Array<!HTMLElement> | undefined}
       * @protected
       */
      _detailsCells: {
        type: Array
      }
    };
  }
  static get observers() {
    return [
      "_detailsOpenedItemsChanged(detailsOpenedItems, rowDetailsRenderer)",
      "_rowDetailsRendererChanged(rowDetailsRenderer)"
    ];
  }
  /** @protected */
  ready() {
    super.ready(), this._detailsCellResizeObserver = new ResizeObserver((e) => {
      e.forEach(({ target: t }) => {
        this._updateDetailsCellHeight(t.parentElement);
      }), this.__virtualizer.__adapter._resizeHandler();
    });
  }
  /** @private */
  _rowDetailsRendererChanged(e) {
    e && this._columnTree && at(this.$.items, (t) => {
      if (!t.querySelector("[part~=details-cell]")) {
        this._updateRow(t, this._columnTree[this._columnTree.length - 1]);
        const i = this._isDetailsOpened(t._item);
        this._toggleDetailsCell(t, i);
      }
    });
  }
  /** @private */
  _detailsOpenedItemsChanged(e, t) {
    at(this.$.items, (i) => {
      if (i.hasAttribute("details-opened")) {
        this._updateItem(i, i._item);
        return;
      }
      t && this._isDetailsOpened(i._item) && this._updateItem(i, i._item);
    });
  }
  /**
   * @param {!HTMLElement} cell
   * @protected
   */
  _configureDetailsCell(e) {
    e.setAttribute("part", "cell details-cell"), e.toggleAttribute("frozen", !0), this._detailsCellResizeObserver.observe(e);
  }
  /**
   * @param {!HTMLElement} row
   * @param {!GridItem} item
   * @protected
   */
  _toggleDetailsCell(e, t) {
    const i = e.querySelector('[part~="details-cell"]');
    i && (i.hidden = !t, !i.hidden && this.rowDetailsRenderer && (i._renderer = this.rowDetailsRenderer));
  }
  /** @protected */
  _updateDetailsCellHeight(e) {
    const t = e.querySelector('[part~="details-cell"]');
    t && (this.__updateDetailsRowPadding(e, t), requestAnimationFrame(() => this.__updateDetailsRowPadding(e, t)));
  }
  /** @private */
  __updateDetailsRowPadding(e, t) {
    t.hidden ? e.style.removeProperty("padding-bottom") : e.style.setProperty("padding-bottom", `${t.offsetHeight}px`);
  }
  /** @protected */
  _updateDetailsCellHeights() {
    at(this.$.items, (e) => {
      this._updateDetailsCellHeight(e);
    });
  }
  /**
   * @param {!GridItem} item
   * @return {boolean}
   * @protected
   */
  _isDetailsOpened(e) {
    return this.detailsOpenedItems && this._getItemIndexInArray(e, this.detailsOpenedItems) !== -1;
  }
  /**
   * Open the details row of a given item.
   * @param {!GridItem} item
   */
  openItemDetails(e) {
    this._isDetailsOpened(e) || (this.detailsOpenedItems = [...this.detailsOpenedItems, e]);
  }
  /**
   * Close the details row of a given item.
   * @param {!GridItem} item
   */
  closeItemDetails(e) {
    this._isDetailsOpened(e) && (this.detailsOpenedItems = this.detailsOpenedItems.filter((t) => !this._itemsEqual(t, e)));
  }
};
/**
 * @license
 * Copyright (c) 2022 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Nn = document.createElement("div");
Nn.style.position = "fixed";
Nn.style.clip = "rect(0px, 0px, 0px, 0px)";
Nn.setAttribute("aria-live", "polite");
document.body.appendChild(Nn);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const xs = Je(
  (s) => class extends s {
    /** @protected */
    ready() {
      super.ready(), this.addEventListener("keydown", (e) => {
        this._onKeyDown(e);
      }), this.addEventListener("keyup", (e) => {
        this._onKeyUp(e);
      });
    }
    /**
     * A handler for the `keydown` event. By default, it calls
     * separate methods for handling "Enter" and "Escape" keys.
     * Override the method to implement your own behavior.
     *
     * @param {KeyboardEvent} event
     * @protected
     */
    _onKeyDown(e) {
      switch (e.key) {
        case "Enter":
          this._onEnter(e);
          break;
        case "Escape":
          this._onEscape(e);
          break;
      }
    }
    /**
     * A handler for the `keyup` event. By default, it does nothing.
     * Override the method to implement your own behavior.
     *
     * @param {KeyboardEvent} _event
     * @protected
     */
    _onKeyUp(e) {
    }
    /**
     * A handler for the "Enter" key. By default, it does nothing.
     * Override the method to implement your own behavior.
     *
     * @param {KeyboardEvent} _event
     * @protected
     */
    _onEnter(e) {
    }
    /**
     * A handler for the "Escape" key. By default, it does nothing.
     * Override the method to implement your own behavior.
     *
     * @param {KeyboardEvent} _event
     * @protected
     */
    _onEscape(e) {
    }
  }
);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const ac = Je(
  (s) => class extends s {
    /**
     * @protected
     * @return {boolean}
     */
    get _keyboardActive() {
      return Ln();
    }
    /** @protected */
    ready() {
      this.addEventListener("focusin", (e) => {
        this._shouldSetFocus(e) && this._setFocused(!0);
      }), this.addEventListener("focusout", (e) => {
        this._shouldRemoveFocus(e) && this._setFocused(!1);
      }), super.ready();
    }
    /** @protected */
    disconnectedCallback() {
      super.disconnectedCallback(), this.hasAttribute("focused") && this._setFocused(!1);
    }
    /**
     * Override to change how focused and focus-ring attributes are set.
     *
     * @param {boolean} focused
     * @protected
     */
    _setFocused(e) {
      this.toggleAttribute("focused", e), this.toggleAttribute("focus-ring", e && this._keyboardActive);
    }
    /**
     * Override to define if the field receives focus based on the event.
     *
     * @param {FocusEvent} _event
     * @return {boolean}
     * @protected
     */
    _shouldSetFocus(e) {
      return !0;
    }
    /**
     * Override to define if the field loses focus based on the event.
     *
     * @param {FocusEvent} _event
     * @return {boolean}
     * @protected
     */
    _shouldRemoveFocus(e) {
      return !0;
    }
  }
);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const bf = Je(
  (s) => class extends ac(ql(s)) {
    static get properties() {
      return {
        /**
         * Specify that this control should have input focus when the page loads.
         */
        autofocus: {
          type: Boolean
        },
        /**
         * A reference to the focusable element controlled by the mixin.
         * It can be an input, textarea, button or any element with tabindex > -1.
         *
         * Any component implementing this mixin is expected to provide it
         * by using `this._setFocusElement(input)` Polymer API.
         *
         * Toggling `tabindex` attribute on the host element propagates its value to `focusElement`.
         *
         * @protected
         * @type {!HTMLElement}
         */
        focusElement: {
          type: Object,
          readOnly: !0,
          observer: "_focusElementChanged"
        },
        /**
         * Override the property from `TabIndexMixin`
         * to ensure the `tabindex` attribute of the focus element
         * will be restored to `0` after re-enabling the element.
         *
         * @protected
         * @override
         */
        _lastTabIndex: {
          value: 0
        }
      };
    }
    constructor() {
      super(), this._boundOnBlur = this._onBlur.bind(this), this._boundOnFocus = this._onFocus.bind(this);
    }
    /** @protected */
    ready() {
      super.ready(), this.autofocus && !this.disabled && requestAnimationFrame(() => {
        this.focus(), this.setAttribute("focus-ring", "");
      });
    }
    /**
     * @protected
     * @override
     */
    focus() {
      this.focusElement && !this.disabled && this.focusElement.focus();
    }
    /**
     * @protected
     * @override
     */
    blur() {
      this.focusElement && this.focusElement.blur();
    }
    /**
     * @protected
     * @override
     */
    click() {
      this.focusElement && !this.disabled && this.focusElement.click();
    }
    /** @protected */
    _focusElementChanged(e, t) {
      e ? (e.disabled = this.disabled, this._addFocusListeners(e), this.__forwardTabIndex(this.tabindex)) : t && this._removeFocusListeners(t);
    }
    /**
     * @param {HTMLElement} element
     * @protected
     */
    _addFocusListeners(e) {
      e.addEventListener("blur", this._boundOnBlur), e.addEventListener("focus", this._boundOnFocus);
    }
    /**
     * @param {HTMLElement} element
     * @protected
     */
    _removeFocusListeners(e) {
      e.removeEventListener("blur", this._boundOnBlur), e.removeEventListener("focus", this._boundOnFocus);
    }
    /**
     * Focus event does not bubble, so we dispatch it manually
     * on the host element to support adding focus listeners
     * when the focusable element is placed in light DOM.
     * @param {FocusEvent} event
     * @protected
     */
    _onFocus(e) {
      e.stopPropagation(), this.dispatchEvent(new Event("focus"));
    }
    /**
     * Blur event does not bubble, so we dispatch it manually
     * on the host element to support adding blur listeners
     * when the focusable element is placed in light DOM.
     * @param {FocusEvent} event
     * @protected
     */
    _onBlur(e) {
      e.stopPropagation(), this.dispatchEvent(new Event("blur"));
    }
    /**
     * @param {FocusEvent} event
     * @return {boolean}
     * @protected
     * @override
     */
    _shouldSetFocus(e) {
      return e.target === this.focusElement;
    }
    /**
     * @param {FocusEvent} event
     * @return {boolean}
     * @protected
     * @override
     */
    _shouldRemoveFocus(e) {
      return e.target === this.focusElement;
    }
    /**
     * @param {boolean} disabled
     * @param {boolean} oldDisabled
     * @protected
     * @override
     */
    _disabledChanged(e, t) {
      super._disabledChanged(e, t), this.focusElement && (this.focusElement.disabled = e), e && this.blur();
    }
    /**
     * Override an observer from `TabindexMixin`.
     * Do not call super to remove tabindex attribute
     * from the host after it has been forwarded.
     * @param {string} tabindex
     * @protected
     * @override
     */
    _tabindexChanged(e) {
      this.__forwardTabIndex(e);
    }
    /** @private */
    __forwardTabIndex(e) {
      e !== void 0 && this.focusElement && (this.focusElement.tabIndex = e, e !== -1 && (this.tabindex = void 0)), this.disabled && e && (e !== -1 && (this._lastTabIndex = e), this.tabindex = void 0);
    }
  }
);
/**
 * @license
 * Copyright (c) 2023 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const To = /* @__PURE__ */ new Map();
function Cs(s) {
  return To.has(s) || To.set(s, /* @__PURE__ */ new WeakMap()), To.get(s);
}
function lc(s, n) {
  s && s.removeAttribute(n);
}
function cc(s, n) {
  if (!s || !n)
    return;
  const e = Cs(n);
  if (e.has(s))
    return;
  const t = us(s.getAttribute(n));
  e.set(s, new Set(t));
}
function wf(s, n) {
  if (!s || !n)
    return;
  const e = Cs(n), t = e.get(s);
  !t || t.size === 0 ? s.removeAttribute(n) : zn(s, n, On(t)), e.delete(s);
}
function Eo(s, n, e = { newId: null, oldId: null, fromUser: !1 }) {
  if (!s || !n)
    return;
  const { newId: t, oldId: i, fromUser: r } = e, o = Cs(n), l = o.get(s);
  if (!r && l) {
    i && l.delete(i), t && l.add(t);
    return;
  }
  r && (l ? t || o.delete(s) : cc(s, n), lc(s, n)), fs(s, n, i);
  const a = t || On(l);
  a && zn(s, n, a);
}
function xf(s, n) {
  cc(s, n), lc(s, n);
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Cf {
  constructor(n) {
    this.host = n, this.__required = !1;
  }
  /**
   * Sets a target element to which ARIA attributes are added.
   *
   * @param {HTMLElement} target
   */
  setTarget(n) {
    this.__target = n, this.__setAriaRequiredAttribute(this.__required), this.__setLabelIdToAriaAttribute(this.__labelId, this.__labelId), this.__labelIdFromUser != null && this.__setLabelIdToAriaAttribute(this.__labelIdFromUser, this.__labelIdFromUser, !0), this.__setErrorIdToAriaAttribute(this.__errorId), this.__setHelperIdToAriaAttribute(this.__helperId), this.setAriaLabel(this.__label);
  }
  /**
   * Toggles the `aria-required` attribute on the target element
   * if the target is the host component (e.g. a field group).
   * Otherwise, it does nothing.
   *
   * @param {boolean} required
   */
  setRequired(n) {
    this.__setAriaRequiredAttribute(n), this.__required = n;
  }
  /**
   * Defines the `aria-label` attribute of the target element.
   *
   * To remove the attribute, pass `null` as `label`.
   *
   * @param {string | null | undefined} label
   */
  setAriaLabel(n) {
    this.__setAriaLabelToAttribute(n), this.__label = n;
  }
  /**
   * Links the target element with a slotted label element
   * via the target's attribute `aria-labelledby`.
   *
   * To unlink the previous slotted label element, pass `null` as `labelId`.
   *
   * @param {string | null} labelId
   */
  setLabelId(n, e = !1) {
    const t = e ? this.__labelIdFromUser : this.__labelId;
    this.__setLabelIdToAriaAttribute(n, t, e), e ? this.__labelIdFromUser = n : this.__labelId = n;
  }
  /**
   * Links the target element with a slotted error element via the target's attribute:
   * - `aria-labelledby` if the target is the host component (e.g a field group).
   * - `aria-describedby` otherwise.
   *
   * To unlink the previous slotted error element, pass `null` as `errorId`.
   *
   * @param {string | null} errorId
   */
  setErrorId(n) {
    this.__setErrorIdToAriaAttribute(n, this.__errorId), this.__errorId = n;
  }
  /**
   * Links the target element with a slotted helper element via the target's attribute:
   * - `aria-labelledby` if the target is the host component (e.g a field group).
   * - `aria-describedby` otherwise.
   *
   * To unlink the previous slotted helper element, pass `null` as `helperId`.
   *
   * @param {string | null} helperId
   */
  setHelperId(n) {
    this.__setHelperIdToAriaAttribute(n, this.__helperId), this.__helperId = n;
  }
  /**
   * @param {string | null | undefined} label
   * @private
   * */
  __setAriaLabelToAttribute(n) {
    this.__target && (n ? (xf(this.__target, "aria-labelledby"), this.__target.setAttribute("aria-label", n)) : this.__label && (wf(this.__target, "aria-labelledby"), this.__target.removeAttribute("aria-label")));
  }
  /**
   * @param {string | null | undefined} labelId
   * @param {string | null | undefined} oldLabelId
   * @param {boolean | null | undefined} fromUser
   * @private
   */
  __setLabelIdToAriaAttribute(n, e, t) {
    Eo(this.__target, "aria-labelledby", { newId: n, oldId: e, fromUser: t });
  }
  /**
   * @param {string | null | undefined} errorId
   * @param {string | null | undefined} oldErrorId
   * @private
   */
  __setErrorIdToAriaAttribute(n, e) {
    Eo(this.__target, "aria-describedby", { newId: n, oldId: e, fromUser: !1 });
  }
  /**
   * @param {string | null | undefined} helperId
   * @param {string | null | undefined} oldHelperId
   * @private
   */
  __setHelperIdToAriaAttribute(n, e) {
    Eo(this.__target, "aria-describedby", { newId: n, oldId: e, fromUser: !1 });
  }
  /**
   * @param {boolean} required
   * @private
   */
  __setAriaRequiredAttribute(n) {
    this.__target && (["input", "textarea"].includes(this.__target.localName) || (n ? this.__target.setAttribute("aria-required", "true") : this.__target.removeAttribute("aria-required")));
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Po = [];
class Tf {
  /**
   * @param {HTMLElement} host
   */
  constructor(n) {
    this.host = n, this.__trapNode = null, this.__onKeyDown = this.__onKeyDown.bind(this);
  }
  /**
   * An array of tab-ordered focusable elements inside the trap node.
   *
   * @return {HTMLElement[]}
   * @private
   */
  get __focusableElements() {
    return Au(this.__trapNode);
  }
  /**
   * The index of the element inside the trap node that currently has focus.
   *
   * @return {HTMLElement | undefined}
   * @private
   */
  get __focusedElementIndex() {
    const n = this.__focusableElements;
    return n.indexOf(n.filter(Wl).pop());
  }
  hostConnected() {
    document.addEventListener("keydown", this.__onKeyDown);
  }
  hostDisconnected() {
    document.removeEventListener("keydown", this.__onKeyDown);
  }
  /**
   * Activates a focus trap for a DOM node that will prevent focus from escaping the node.
   * The trap can be deactivated with the `.releaseFocus()` method.
   *
   * If focus is initially outside the trap, the method will move focus inside,
   * on the first focusable element of the trap in the tab order.
   * The first focusable element can be the trap node itself if it is focusable
   * and comes first in the tab order.
   *
   * If there are no focusable elements, the method will throw an exception
   * and the trap will not be set.
   *
   * @param {HTMLElement} trapNode
   */
  trapFocus(n) {
    if (this.__trapNode = n, this.__focusableElements.length === 0)
      throw this.__trapNode = null, new Error("The trap node should have at least one focusable descendant or be focusable itself.");
    Po.push(this), this.__focusedElementIndex === -1 && this.__focusableElements[0].focus();
  }
  /**
   * Deactivates the focus trap set with the `.trapFocus()` method
   * so that it becomes possible to tab outside the trap node.
   */
  releaseFocus() {
    this.__trapNode = null, Po.pop();
  }
  /**
   * A `keydown` event handler that manages tabbing navigation when the trap is enabled.
   *
   * - Moves focus to the next focusable element of the trap on `Tab` press.
   * When no next element to focus, the method moves focus to the first focusable element.
   * - Moves focus to the prev focusable element of the trap on `Shift+Tab` press.
   * When no prev element to focus, the method moves focus to the last focusable element.
   *
   * @param {KeyboardEvent} event
   * @private
   */
  __onKeyDown(n) {
    if (this.__trapNode && this === Array.from(Po).pop() && n.key === "Tab") {
      n.preventDefault();
      const e = n.shiftKey;
      this.__focusNextElement(e);
    }
  }
  /**
   * - Moves focus to the next focusable element if `backward === false`.
   * When no next element to focus, the method moves focus to the first focusable element.
   * - Moves focus to the prev focusable element if `backward === true`.
   * When no prev element to focus the method moves focus to the last focusable element.
   *
   * If no focusable elements, the method returns immediately.
   *
   * @param {boolean} backward
   * @private
   */
  __focusNextElement(n = !1) {
    const e = this.__focusableElements, t = n ? -1 : 1, i = this.__focusedElementIndex, r = (e.length + i + t) % e.length, o = e[r];
    o.focus(), o.localName === "input" && o.select();
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Ef {
  /**
   * Saves the given node as a target for restoring focus to
   * when `restoreFocus()` is called. If no node is provided,
   * the currently focused node in the DOM is saved as a target.
   *
   * @param {Node | null | undefined} focusNode
   */
  saveFocus(n) {
    this.focusNode = n || jo();
  }
  /**
   * Restores focus to the target node that was saved previously with `saveFocus()`.
   */
  restoreFocus(n) {
    const e = this.focusNode;
    if (!e)
      return;
    const t = n ? n.preventScroll : !1;
    jo() === document.body ? setTimeout(() => e.focus({ preventScroll: t })) : e.focus({ preventScroll: t }), this.focusNode = null;
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function qa(s, n) {
  const { scrollLeft: e } = s;
  return n !== "rtl" ? e : s.scrollWidth - s.clientWidth + e;
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const en = new ResizeObserver((s) => {
  setTimeout(() => {
    s.forEach((n) => {
      n.target.resizables ? n.target.resizables.forEach((e) => {
        e._onResize(n.contentRect);
      }) : n.target._onResize(n.contentRect);
    });
  });
}), Pf = Je(
  (s) => class extends s {
    /**
     * When true, the parent element resize will be also observed.
     * Override this getter and return `true` to enable this.
     *
     * @protected
     */
    get _observeParent() {
      return !1;
    }
    /** @protected */
    connectedCallback() {
      if (super.connectedCallback(), en.observe(this), this._observeParent) {
        const e = this.parentNode instanceof ShadowRoot ? this.parentNode.host : this.parentNode;
        e.resizables || (e.resizables = /* @__PURE__ */ new Set(), en.observe(e)), e.resizables.add(this), this.__parent = e;
      }
    }
    /** @protected */
    disconnectedCallback() {
      super.disconnectedCallback(), en.unobserve(this);
      const e = this.__parent;
      if (this._observeParent && e) {
        const t = e.resizables;
        t && (t.delete(this), t.size === 0 && en.unobserve(e)), this.__parent = null;
      }
    }
    /**
     * A handler invoked on host resize. By default, it does nothing.
     * Override the method to implement your own behavior.
     *
     * @protected
     */
    _onResize(e) {
    }
  }
);
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ka = {
  SCROLLING: 500,
  UPDATE_CONTENT_VISIBILITY: 100
}, Sf = (s) => class extends Pf(s) {
  static get properties() {
    return {
      /**
       * Allows you to choose between modes for rendering columns in the grid:
       *
       * "eager" (default): All columns are rendered upfront, regardless of their visibility within the viewport.
       * This mode should generally be preferred, as it avoids the limitations imposed by the "lazy" mode.
       * Use this mode unless the grid has a large number of columns and performance outweighs the limitations
       * in priority.
       *
       * "lazy": Optimizes the rendering of cells when there are multiple columns in the grid by virtualizing
       * horizontal scrolling. In this mode, body cells are rendered only when their corresponding columns are
       * inside the visible viewport.
       *
       * Using "lazy" rendering should be used only if you're dealing with a large number of columns and performance
       * is your highest priority. For most use cases, the default "eager" mode is recommended due to the
       * limitations imposed by the "lazy" mode.
       *
       * When using the "lazy" mode, keep the following limitations in mind:
       *
       * - Row Height: When only a number of columns are visible at once, the height of a row can only be that of
       * the highest cell currently visible on that row. Make sure each cell on a single row has the same height
       * as all other cells on that row. If row cells have different heights, users may experience jumpiness when
       * scrolling the grid horizontally as lazily rendered cells with different heights are scrolled into view.
       *
       * - Auto-width Columns: For the columns that are initially outside the visible viewport but still use auto-width,
       * only the header content is taken into account when calculating the column width because the body cells
       * of the columns outside the viewport are not initially rendered.
       *
       * - Screen Reader Compatibility: Screen readers may not be able to associate the focused cells with the correct
       * headers when only a subset of the body cells on a row is rendered.
       *
       * - Keyboard Navigation: Tabbing through focusable elements inside the grid body may not work as expected because
       * some of the columns that would include focusable elements in the body cells may be outside the visible viewport
       * and thus not rendered.
       *
       * @attr {eager|lazy} column-rendering
       * @type {!ColumnRendering}
       */
      columnRendering: {
        type: String,
        value: "eager",
        sync: !0
      },
      /**
       * Cached array of frozen cells
       * @private
       */
      _frozenCells: {
        type: Array,
        value: () => []
      },
      /**
       * Cached array of cells frozen to end
       * @private
       */
      _frozenToEndCells: {
        type: Array,
        value: () => []
      },
      /** @private */
      _rowWithFocusedElement: Element
    };
  }
  static get observers() {
    return ["__columnRenderingChanged(_columnTree, columnRendering)"];
  }
  /** @private */
  get _scrollLeft() {
    return this.$.table.scrollLeft;
  }
  /** @private */
  get _scrollTop() {
    return this.$.table.scrollTop;
  }
  /**
   * Override (from iron-scroll-target-behavior) to avoid document scroll
   * @private
   */
  set _scrollTop(e) {
    this.$.table.scrollTop = e;
  }
  /** @protected */
  get _lazyColumns() {
    return this.columnRendering === "lazy";
  }
  /** @protected */
  ready() {
    super.ready(), this.scrollTarget = this.$.table, this.$.items.addEventListener("focusin", (e) => {
      const t = e.composedPath().indexOf(this.$.items);
      this._rowWithFocusedElement = e.composedPath()[t - 1], this._rowWithFocusedElement && (this._isMousedown || this.__scrollIntoViewport(this._rowWithFocusedElement.index), this.$.table.contains(e.relatedTarget) || this.$.table.dispatchEvent(
        new CustomEvent("virtualizer-element-focused", { detail: { element: this._rowWithFocusedElement } })
      ));
    }), this.$.items.addEventListener("focusout", () => {
      this._rowWithFocusedElement = void 0;
    }), this.$.table.addEventListener("scroll", () => this._afterScroll());
  }
  /**
   * @protected
   * @override
   */
  _onResize() {
    if (this._updateOverflow(), this.__updateHorizontalScrollPosition(), this._firefox) {
      const e = !Tn(this);
      e && this.__previousVisible === !1 && (this._scrollTop = this.__memorizedScrollTop || 0), this.__previousVisible = e;
    }
  }
  /**
   * Scroll to a flat index in the grid. The method doesn't take into account
   * the hierarchy of the items.
   *
   * @param {number} index Row index to scroll to
   * @protected
   */
  _scrollToFlatIndex(e) {
    e = Math.min(this._flatSize - 1, Math.max(0, e)), this.__virtualizer.scrollToIndex(e), this.__scrollIntoViewport(e);
  }
  /**
   * Makes sure the row with the given index (if found in the DOM) is fully
   * inside the visible viewport, taking header/footer into account.
   * @private
   */
  __scrollIntoViewport(e) {
    const t = [...this.$.items.children].find((i) => i.index === e);
    if (t) {
      const i = t.getBoundingClientRect(), r = this.$.footer.getBoundingClientRect().top, o = this.$.header.getBoundingClientRect().bottom;
      i.bottom > r ? this.$.table.scrollTop += i.bottom - r : i.top < o && (this.$.table.scrollTop -= o - i.top);
    }
  }
  /** @private */
  _scheduleScrolling() {
    this._scrollingFrame || (this._scrollingFrame = requestAnimationFrame(() => this.$.scroller.toggleAttribute("scrolling", !0))), this._debounceScrolling = ze.debounce(this._debounceScrolling, Nt.after(Ka.SCROLLING), () => {
      cancelAnimationFrame(this._scrollingFrame), delete this._scrollingFrame, this.$.scroller.toggleAttribute("scrolling", !1);
    });
  }
  /** @private */
  _afterScroll() {
    this.__updateHorizontalScrollPosition(), this.hasAttribute("reordering") || this._scheduleScrolling(), this.hasAttribute("navigating") || this._hideTooltip(!0), this._updateOverflow(), this._debounceColumnContentVisibility = ze.debounce(
      this._debounceColumnContentVisibility,
      Nt.after(Ka.UPDATE_CONTENT_VISIBILITY),
      () => {
        this._lazyColumns && this.__cachedScrollLeft !== this._scrollLeft && (this.__cachedScrollLeft = this._scrollLeft, this.__updateColumnsBodyContentHidden());
      }
    ), this._firefox && !Tn(this) && this.__previousVisible !== !1 && (this.__memorizedScrollTop = this._scrollTop);
  }
  /** @private */
  __updateColumnsBodyContentHidden() {
    if (!this._columnTree || !this._areSizerCellsAssigned())
      return;
    const e = this._getColumnsInOrder();
    let t = !1;
    if (e.forEach((i) => {
      const r = this._lazyColumns && !this.__isColumnInViewport(i);
      i._bodyContentHidden !== r && (t = !0, i._cells.forEach((o) => {
        if (o !== i._sizerCell) {
          if (r)
            o.remove();
          else if (o.__parentRow) {
            const l = [...o.__parentRow.children].find(
              (a) => e.indexOf(a._column) > e.indexOf(i)
            );
            o.__parentRow.insertBefore(o, l);
          }
        }
      })), i._bodyContentHidden = r;
    }), t && this._frozenCellsChanged(), this._lazyColumns) {
      const i = [...e].reverse().find((l) => l.frozen), r = this.__getColumnEnd(i), o = e.find((l) => !l.frozen && !l._bodyContentHidden);
      this.__lazyColumnsStart = this.__getColumnStart(o) - r, this.$.items.style.setProperty("--_grid-lazy-columns-start", `${this.__lazyColumnsStart}px`), this._resetKeyboardNavigation();
    }
  }
  /** @private */
  __getColumnEnd(e) {
    return e ? e._sizerCell.offsetLeft + (this.__isRTL ? 0 : e._sizerCell.offsetWidth) : this.__isRTL ? this.$.table.clientWidth : 0;
  }
  /** @private */
  __getColumnStart(e) {
    return e ? e._sizerCell.offsetLeft + (this.__isRTL ? e._sizerCell.offsetWidth : 0) : this.__isRTL ? this.$.table.clientWidth : 0;
  }
  /**
   * Returns true if the given column is horizontally inside the viewport.
   * @private
   */
  __isColumnInViewport(e) {
    return e.frozen || e.frozenToEnd ? !0 : this.__isHorizontallyInViewport(e._sizerCell);
  }
  /** @private */
  __isHorizontallyInViewport(e) {
    return e.offsetLeft + e.offsetWidth >= this._scrollLeft && e.offsetLeft <= this._scrollLeft + this.clientWidth;
  }
  /** @private */
  __columnRenderingChanged(e, t) {
    t === "eager" ? this.$.scroller.removeAttribute("column-rendering") : this.$.scroller.setAttribute("column-rendering", t), this.__updateColumnsBodyContentHidden();
  }
  /** @private */
  _updateOverflow() {
    this._debounceOverflow = ze.debounce(this._debounceOverflow, Xt, () => {
      this.__doUpdateOverflow();
    });
  }
  /** @private */
  __doUpdateOverflow() {
    let e = "";
    const t = this.$.table;
    t.scrollTop < t.scrollHeight - t.clientHeight && (e += " bottom"), t.scrollTop > 0 && (e += " top");
    const i = qa(t, this.getAttribute("dir"));
    i > 0 && (e += " start"), i < t.scrollWidth - t.clientWidth && (e += " end"), this.__isRTL && (e = e.replace(/start|end/giu, (o) => o === "start" ? "end" : "start")), t.scrollLeft < t.scrollWidth - t.clientWidth && (e += " right"), t.scrollLeft > 0 && (e += " left");
    const r = e.trim();
    r.length > 0 && this.getAttribute("overflow") !== r ? this.setAttribute("overflow", r) : r.length === 0 && this.hasAttribute("overflow") && this.removeAttribute("overflow");
  }
  /** @protected */
  _frozenCellsChanged() {
    this._debouncerCacheElements = ze.debounce(this._debouncerCacheElements, yt, () => {
      Array.from(this.shadowRoot.querySelectorAll('[part~="cell"]')).forEach((e) => {
        e.style.transform = "";
      }), this._frozenCells = Array.prototype.slice.call(this.$.table.querySelectorAll("[frozen]")), this._frozenToEndCells = Array.prototype.slice.call(this.$.table.querySelectorAll("[frozen-to-end]")), this.__updateHorizontalScrollPosition();
    }), this._debounceUpdateFrozenColumn();
  }
  /** @protected */
  _debounceUpdateFrozenColumn() {
    this.__debounceUpdateFrozenColumn = ze.debounce(
      this.__debounceUpdateFrozenColumn,
      yt,
      () => this._updateFrozenColumn()
    );
  }
  /** @private */
  _updateFrozenColumn() {
    if (!this._columnTree)
      return;
    const e = this._columnTree[this._columnTree.length - 1].slice(0);
    e.sort((r, o) => r._order - o._order);
    let t, i;
    for (let r = 0; r < e.length; r++) {
      const o = e[r];
      o._lastFrozen = !1, o._firstFrozenToEnd = !1, i === void 0 && o.frozenToEnd && !o.hidden && (i = r), o.frozen && !o.hidden && (t = r);
    }
    t !== void 0 && (e[t]._lastFrozen = !0), i !== void 0 && (e[i]._firstFrozenToEnd = !0), this.__updateColumnsBodyContentHidden();
  }
  /** @private */
  __updateHorizontalScrollPosition() {
    if (!this._columnTree)
      return;
    const e = this.$.table.scrollWidth, t = this.$.table.clientWidth, i = Math.max(0, this.$.table.scrollLeft), r = qa(this.$.table, this.getAttribute("dir")), o = `translate(${-i}px, 0)`;
    this.$.header.style.transform = o, this.$.footer.style.transform = o, this.$.items.style.transform = o;
    const l = this.__isRTL ? r + t - e : i, a = `translate(${l}px, 0)`;
    this._frozenCells.forEach((p) => {
      p.style.transform = a;
    });
    const c = this.__isRTL ? r : i + t - e, d = `translate(${c}px, 0)`;
    let f = d;
    if (this._lazyColumns && this._areSizerCellsAssigned()) {
      const p = this._getColumnsInOrder(), b = [...p].reverse().find((W) => !W.frozenToEnd && !W._bodyContentHidden), x = this.__getColumnEnd(b), T = p.find((W) => W.frozenToEnd), I = this.__getColumnStart(T);
      f = `translate(${c + (I - x) + this.__lazyColumnsStart}px, 0)`;
    }
    this._frozenToEndCells.forEach((p) => {
      this.$.items.contains(p) ? p.style.transform = f : p.style.transform = d;
    }), this.hasAttribute("navigating") && this.__rowFocusMode && this.$.table.style.setProperty("--_grid-horizontal-scroll-position", `${-l}px`);
  }
  /** @private */
  _areSizerCellsAssigned() {
    return this._getColumnsInOrder().every((e) => e._sizerCell);
  }
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Rf = (s) => class extends s {
  static get properties() {
    return {
      /**
       * An array that contains the selected items.
       * @type {!Array<!GridItem>}
       */
      selectedItems: {
        type: Object,
        notify: !0,
        value: () => [],
        sync: !0
      },
      /**
       * Set of selected item ids
       * @private
       */
      __selectedKeys: {
        type: Object,
        computed: "__computeSelectedKeys(itemIdPath, selectedItems)"
      }
    };
  }
  static get observers() {
    return ["__selectedItemsChanged(itemIdPath, selectedItems)"];
  }
  /**
   * @param {!GridItem} item
   * @return {boolean}
   * @protected
   */
  _isSelected(e) {
    return this.__selectedKeys.has(this.getItemId(e));
  }
  /**
   * Selects the given item.
   *
   * @method selectItem
   * @param {!GridItem} item The item object
   */
  selectItem(e) {
    this._isSelected(e) || (this.selectedItems = [...this.selectedItems, e]);
  }
  /**
   * Deselects the given item if it is already selected.
   *
   * @method deselect
   * @param {!GridItem} item The item object
   */
  deselectItem(e) {
    this._isSelected(e) && (this.selectedItems = this.selectedItems.filter((t) => !this._itemsEqual(t, e)));
  }
  /**
   * Toggles the selected state of the given item.
   *
   * @method toggle
   * @param {!GridItem} item The item object
   * @protected
   */
  _toggleItem(e) {
    this._isSelected(e) ? this.deselectItem(e) : this.selectItem(e);
  }
  /** @private */
  __selectedItemsChanged() {
    this.requestContentUpdate();
  }
  /** @private */
  __computeSelectedKeys(e, t) {
    const i = t || [], r = /* @__PURE__ */ new Set();
    return i.forEach((o) => {
      r.add(this.getItemId(o));
    }), r;
  }
  /**
   * Fired when the `selectedItems` property changes.
   *
   * @event selected-items-changed
   */
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
let Za = "prepend";
const Af = (s) => class extends s {
  static get properties() {
    return {
      /**
       * When `true`, all `<vaadin-grid-sorter>` are applied for sorting.
       * @attr {boolean} multi-sort
       * @type {boolean}
       */
      multiSort: {
        type: Boolean,
        value: !1
      },
      /**
       * Controls how columns are added to the sort order when using multi-sort.
       * The sort order is visually indicated by numbers in grid sorters placed in column headers.
       *
       * By default, whenever an unsorted column is sorted, or the sort-direction of a column is
       * changed, that column gets sort priority 1, thus affecting the priority for all the other
       * sorted columns. This is identical to using `multi-sort-priority="prepend"`.
       *
       * Using this property allows to change this behavior so that sorting an unsorted column
       * would add it to the "end" of the sort, and changing column's sort direction would retain
       * it's previous priority. To set this, use `multi-sort-priority="append"`.
       *
       * @attr {string} multi-sort-priority
       */
      multiSortPriority: {
        type: String,
        value: () => Za
      },
      /**
       * When `true`, Shift-clicking an unsorted column's sorter adds it to the multi-sort.
       * Shift + Space does the same action via keyboard. This property has precedence over the
       * `multiSort` property. If `multiSortOnShiftClick` is true, the multiSort property is effectively ignored.
       *
       * @attr {boolean} multi-sort-on-shift-click
       * @type {boolean}
       */
      multiSortOnShiftClick: {
        type: Boolean,
        value: !1
      },
      /**
       * @type {!Array<!GridSorterDefinition>}
       * @protected
       */
      _sorters: {
        type: Array,
        value: () => []
      },
      /** @private */
      _previousSorters: {
        type: Array,
        value: () => []
      }
    };
  }
  /**
   * Sets the default multi-sort priority to use for all grid instances.
   * This method should be called before creating any grid instances.
   * Changing this setting does not affect the default for existing grids.
   *
   * @param {string} priority
   */
  static setDefaultMultiSortPriority(e) {
    Za = ["append", "prepend"].includes(e) ? e : "prepend";
  }
  /** @protected */
  ready() {
    super.ready(), this.addEventListener("sorter-changed", this._onSorterChanged);
  }
  /** @private */
  _onSorterChanged(e) {
    const t = e.target;
    e.stopPropagation(), t._grid = this, this.__updateSorter(t, e.detail.shiftClick, e.detail.fromSorterClick), this.__applySorters();
  }
  /** @private */
  __removeSorters(e) {
    e.length !== 0 && (this._sorters = this._sorters.filter((t) => !e.includes(t)), this.__applySorters());
  }
  /** @private */
  __updateSortOrders() {
    this._sorters.forEach((t) => {
      t._order = null;
    });
    const e = this._getActiveSorters();
    e.length > 1 && e.forEach((t, i) => {
      t._order = i;
    });
  }
  /** @private */
  __updateSorter(e, t, i) {
    if (!e.direction && !this._sorters.includes(e))
      return;
    e._order = null;
    const r = this._sorters.filter((o) => o !== e);
    this.multiSort && (!this.multiSortOnShiftClick || !i) || this.multiSortOnShiftClick && t ? this.multiSortPriority === "append" ? this._sorters = [...r, e] : this._sorters = [e, ...r] : (e.direction || this.multiSortOnShiftClick) && (this._sorters = e.direction ? [e] : [], r.forEach((o) => {
      o._order = null, o.direction = null;
    }));
  }
  /** @private */
  __applySorters() {
    this.__updateSortOrders(), this.dataProvider && // No need to clear cache if sorters didn't change and grid is attached
    this.isAttached && JSON.stringify(this._previousSorters) !== JSON.stringify(this._mapSorters()) && this.__debounceClearCache(), this._a11yUpdateSorters(), this._previousSorters = this._mapSorters();
  }
  /**
   * @type {GridSorterDefinition[]}
   * @protected
   */
  _getActiveSorters() {
    return this._sorters.filter((e) => e.direction && e.isConnected);
  }
  /**
   * @return {!Array<!GridSorterDefinition>}
   * @protected
   */
  _mapSorters() {
    return this._getActiveSorters().map((e) => ({
      path: e.path,
      direction: e.direction
    }));
  }
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const kf = (s) => class extends s {
  static get properties() {
    return {
      /**
       * A function that allows generating CSS class names for grid cells
       * based on their row and column. The return value should be the generated
       * class name as a string, or multiple class names separated by whitespace
       * characters.
       *
       * Receives two arguments:
       * - `column` The `<vaadin-grid-column>` element (`undefined` for details-cell).
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *
       * @type {GridCellClassNameGenerator | null | undefined}
       * @deprecated Use `cellPartNameGenerator` instead.
       */
      cellClassNameGenerator: {
        type: Function,
        sync: !0
      },
      /**
       * A function that allows generating CSS `part` names for grid cells in Shadow DOM based
       * on their row and column, for styling from outside using the `::part()` selector.
       *
       * The return value should be the generated part name as a string, or multiple part names
       * separated by whitespace characters.
       *
       * Receives two arguments:
       * - `column` The `<vaadin-grid-column>` element (`undefined` for details-cell).
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *
       * @type {GridCellPartNameGenerator | null | undefined}
       */
      cellPartNameGenerator: {
        type: Function,
        sync: !0
      }
    };
  }
  static get observers() {
    return [
      "__cellClassNameGeneratorChanged(cellClassNameGenerator)",
      "__cellPartNameGeneratorChanged(cellPartNameGenerator)"
    ];
  }
  /** @private */
  __cellClassNameGeneratorChanged() {
    this.generateCellClassNames();
  }
  /** @private */
  __cellPartNameGeneratorChanged() {
    this.generateCellPartNames();
  }
  /**
   * Runs the `cellClassNameGenerator` for the visible cells.
   * If the generator depends on varying conditions, you need to
   * call this function manually in order to update the styles when
   * the conditions change.
   *
   * @deprecated Use `cellPartNameGenerator` and `generateCellPartNames()` instead.
   */
  generateCellClassNames() {
    at(this.$.items, (e) => {
      e.hidden || this._generateCellClassNames(e, this.__getRowModel(e));
    });
  }
  /**
   * Runs the `cellPartNameGenerator` for the visible cells.
   * If the generator depends on varying conditions, you need to
   * call this function manually in order to update the styles when
   * the conditions change.
   */
  generateCellPartNames() {
    at(this.$.items, (e) => {
      e.hidden || this._generateCellPartNames(e, this.__getRowModel(e));
    });
  }
  /** @private */
  _generateCellClassNames(e, t) {
    Hi(e, (i) => {
      if (i.__generatedClasses && i.__generatedClasses.forEach((r) => i.classList.remove(r)), this.cellClassNameGenerator && !e.hasAttribute("loading")) {
        const r = this.cellClassNameGenerator(i._column, t);
        i.__generatedClasses = r && r.split(" ").filter((o) => o.length > 0), i.__generatedClasses && i.__generatedClasses.forEach((o) => i.classList.add(o));
      }
    });
  }
  /** @private */
  _generateCellPartNames(e, t) {
    Hi(e, (i) => {
      if (i.__generatedParts && i.__generatedParts.forEach((r) => {
        Qt(i, null, r);
      }), this.cellPartNameGenerator && !e.hasAttribute("loading")) {
        const r = this.cellPartNameGenerator(i._column, t);
        i.__generatedParts = r && r.split(" ").filter((o) => o.length > 0), i.__generatedParts && i.__generatedParts.forEach((o) => {
          Qt(i, !0, o);
        });
      }
    });
  }
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const If = (s) => class extends Gu(
  uf(
    gf(
      Vu(
        Sf(
          Rf(
            Af(
              yf(
                vf(
                  Bu(
                    _f(
                      hf(
                        df(
                          mf(pf(kf(ql(s))))
                        )
                      )
                    )
                  )
                )
              )
            )
          )
        )
      )
    )
  )
) {
  static get observers() {
    return ["_columnTreeChanged(_columnTree)", "_flatSizeChanged(_flatSize, __virtualizer, _hasData, _columnTree)"];
  }
  static get properties() {
    return {
      /** @private */
      _safari: {
        type: Boolean,
        value: Kl
      },
      /** @private */
      _ios: {
        type: Boolean,
        value: Ko
      },
      /** @private */
      _firefox: {
        type: Boolean,
        value: Du
      },
      /** @private */
      _android: {
        type: Boolean,
        value: Ma
      },
      /** @private */
      _touchDevice: {
        type: Boolean,
        value: br
      },
      /**
       * If true, the grid's height is defined by its rows.
       *
       * Effectively, this disables the grid's virtual scrolling so that all the rows are rendered in the DOM at once.
       * If the grid has a large number of items, using the feature is discouraged to avoid performance issues.
       * @attr {boolean} all-rows-visible
       * @type {boolean}
       */
      allRowsVisible: {
        type: Boolean,
        value: !1,
        reflectToAttribute: !0
      },
      /** @private */
      __pendingRecalculateColumnWidths: {
        type: Boolean,
        value: !0
      },
      /** @private */
      isAttached: {
        value: !1
      },
      /**
       * An internal property that is mainly used by `vaadin-template-renderer`
       * to identify grid elements.
       *
       * @private
       */
      __gridElement: {
        type: Boolean,
        value: !0
      },
      /** @private */
      __hasEmptyStateContent: {
        type: Boolean,
        value: !1
      },
      /** @private */
      __emptyState: {
        type: Boolean,
        computed: "__computeEmptyState(_flatSize, __hasEmptyStateContent)"
      }
    };
  }
  constructor() {
    super(), this.addEventListener("animationend", this._onAnimationEnd);
  }
  /** @private */
  get _firstVisibleIndex() {
    const n = this.__getFirstVisibleItem();
    return n ? n.index : void 0;
  }
  /** @private */
  get _lastVisibleIndex() {
    const n = this.__getLastVisibleItem();
    return n ? n.index : void 0;
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback(), this.isAttached = !0, this.recalculateColumnWidths();
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback(), this.isAttached = !1, this._hideTooltip(!0);
  }
  /** @private */
  __getFirstVisibleItem() {
    return this._getRenderedRows().find((n) => this._isInViewport(n));
  }
  /** @private */
  __getLastVisibleItem() {
    return this._getRenderedRows().reverse().find((n) => this._isInViewport(n));
  }
  /** @private */
  _isInViewport(n) {
    const e = this.$.table.getBoundingClientRect(), t = n.getBoundingClientRect(), i = this.$.header.getBoundingClientRect().height, r = this.$.footer.getBoundingClientRect().height;
    return t.bottom > e.top + i && t.top < e.bottom - r;
  }
  /** @private */
  _getRenderedRows() {
    return Array.from(this.$.items.children).filter((n) => !n.hidden).sort((n, e) => n.index - e.index);
  }
  /** @protected */
  _getRowContainingNode(n) {
    const e = Bl("vaadin-grid-cell-content", n);
    return e ? e.assignedSlot.parentElement.parentElement : void 0;
  }
  /** @protected */
  _isItemAssignedToRow(n, e) {
    const t = this.__getRowModel(e);
    return this.getItemId(n) === this.getItemId(t.item);
  }
  /** @protected */
  ready() {
    super.ready(), this.__virtualizer = new Jl({
      createElements: this._createScrollerRows.bind(this),
      updateElement: this._updateScrollerItem.bind(this),
      scrollContainer: this.$.items,
      scrollTarget: this.$.table,
      reorderElements: !0
    }), new ResizeObserver(
      () => setTimeout(() => {
        this.__updateColumnsBodyContentHidden(), this.__tryToRecalculateColumnWidthsIfPending();
      })
    ).observe(this.$.table), ds(this), this._tooltipController = new Xl(this), this.addController(this._tooltipController), this._tooltipController.setManual(!0), this.__emptyStateContentObserver = new Zl(this.$.emptystateslot, ({ currentNodes: n }) => {
      this.$.emptystatecell._content = n[0], this.__hasEmptyStateContent = !!this.$.emptystatecell._content;
    });
  }
  /** @private */
  __getBodyCellCoordinates(n) {
    if (this.$.items.contains(n) && n.localName === "td")
      return {
        item: n.parentElement._item,
        column: n._column
      };
  }
  /** @private */
  __focusBodyCell({ item: n, column: e }) {
    const t = this._getRenderedRows().find((r) => r._item === n), i = t && [...t.children].find((r) => r._column === e);
    i && i.focus();
  }
  /** @protected */
  _focusFirstVisibleRow() {
    const n = this.__getFirstVisibleItem();
    this.__rowFocusMode = !0, n.focus();
  }
  /** @private */
  _flatSizeChanged(n, e, t, i) {
    if (e && t && i) {
      const r = this.shadowRoot.activeElement, o = this.__getBodyCellCoordinates(r), l = e.size || 0;
      e.size = n, e.update(l - 1, l - 1), n < l && e.update(n - 1, n - 1), o && r.parentElement.hidden && this.__focusBodyCell(o), this._resetKeyboardNavigation();
    }
  }
  /** @private */
  __getIntrinsicWidth(n) {
    return this.__intrinsicWidthCache.has(n) || this.__calculateAndCacheIntrinsicWidths([n]), this.__intrinsicWidthCache.get(n);
  }
  /** @private */
  __getDistributedWidth(n, e) {
    if (n == null || n === this)
      return 0;
    const t = Math.max(
      this.__getIntrinsicWidth(n),
      this.__getDistributedWidth((n.assignedSlot || n).parentElement, n)
    );
    if (!e)
      return t;
    const i = n, r = t, o = i._visibleChildColumns.map((d) => this.__getIntrinsicWidth(d)).reduce((d, f) => d + f, 0), l = Math.max(0, r - o), c = this.__getIntrinsicWidth(e) / o * l;
    return this.__getIntrinsicWidth(e) + c;
  }
  /**
   * @param {!Array<!GridColumn>} cols the columns to auto size based on their content width
   * @private
   */
  _recalculateColumnWidths(n) {
    this.__virtualizer.flush(), [...this.$.header.children, ...this.$.footer.children].forEach((i) => {
      i.__debounceUpdateHeaderFooterRowVisibility && i.__debounceUpdateHeaderFooterRowVisibility.flush();
    }), this._debouncerHiddenChanged && this._debouncerHiddenChanged.flush(), this.__intrinsicWidthCache = /* @__PURE__ */ new Map();
    const e = this._firstVisibleIndex, t = this._lastVisibleIndex;
    this.__viewportRowsCache = this._getRenderedRows().filter((i) => i.index >= e && i.index <= t), this.__calculateAndCacheIntrinsicWidths(n), n.forEach((i) => {
      i.width = `${this.__getDistributedWidth(i)}px`;
    });
  }
  /**
   * Toggles the cell content for the given column to use or not use auto width.
   *
   * While content for all the column cells uses auto width (instead of the default 100%),
   * their offsetWidth can be used to calculate the collective intrinsic width of the column.
   *
   * @private
   */
  __setVisibleCellContentAutoWidth(n, e) {
    n._allCells.filter((t) => this.$.items.contains(t) ? this.__viewportRowsCache.includes(t.parentElement) : !0).forEach((t) => {
      t.__measuringAutoWidth = e, t.__measuringAutoWidth ? (t.__originalWidth = t.style.width, t.style.width = "auto", t.style.position = "absolute") : (t.style.width = t.__originalWidth, delete t.__originalWidth, t.style.position = "");
    }), e ? this.$.scroller.setAttribute("measuring-auto-width", "") : this.$.scroller.removeAttribute("measuring-auto-width");
  }
  /**
   * Returns the maximum intrinsic width of the cell content in the given column.
   * Only cells which are marked for measuring auto width are considered.
   *
   * @private
   */
  __getAutoWidthCellsMaxWidth(n) {
    return n._allCells.reduce((e, t) => t.__measuringAutoWidth ? Math.max(e, t.offsetWidth + 1) : e, 0);
  }
  /**
   * Calculates and caches the intrinsic width of each given column.
   *
   * @private
   */
  __calculateAndCacheIntrinsicWidths(n) {
    n.forEach((e) => this.__setVisibleCellContentAutoWidth(e, !0)), n.forEach((e) => {
      const t = this.__getAutoWidthCellsMaxWidth(e);
      this.__intrinsicWidthCache.set(e, t);
    }), n.forEach((e) => this.__setVisibleCellContentAutoWidth(e, !1));
  }
  /**
   * Updates the `width` of all columns which have `autoWidth` set to `true`.
   */
  recalculateColumnWidths() {
    if (!this._columnTree)
      return;
    if (Tn(this) || this._dataProviderController.isLoading()) {
      this.__pendingRecalculateColumnWidths = !0;
      return;
    }
    const n = this._getColumns().filter((t) => !t.hidden && t.autoWidth), e = n.filter((t) => !customElements.get(t.localName));
    e.length ? Promise.all(e.map((t) => customElements.whenDefined(t.localName))).then(() => {
      this._recalculateColumnWidths(n);
    }) : this._recalculateColumnWidths(n);
  }
  /** @private */
  __tryToRecalculateColumnWidthsIfPending() {
    if (!this.__pendingRecalculateColumnWidths || Tn(this) || this._dataProviderController.isLoading() || [...this.$.items.children].some((t) => t.index === void 0))
      return;
    [...this.$.items.children].some((t) => t.clientHeight > 0) && (this.__pendingRecalculateColumnWidths = !1, this.recalculateColumnWidths());
  }
  /**
   * @protected
   * @override
   */
  _onDataProviderPageLoaded() {
    super._onDataProviderPageLoaded(), this.__tryToRecalculateColumnWidthsIfPending();
  }
  /** @private */
  _createScrollerRows(n) {
    const e = [];
    for (let t = 0; t < n; t++) {
      const i = document.createElement("tr");
      i.setAttribute("part", "row body-row"), i.setAttribute("role", "row"), i.setAttribute("tabindex", "-1"), this._columnTree && this._updateRow(i, this._columnTree[this._columnTree.length - 1], "body", !1, !0), e.push(i);
    }
    return this._columnTree && this._columnTree[this._columnTree.length - 1].forEach((t) => {
      t.isConnected && t._cells && (t._cells = [...t._cells]);
    }), this.__afterCreateScrollerRowsDebouncer = ze.debounce(
      this.__afterCreateScrollerRowsDebouncer,
      Xt,
      () => {
        this._afterScroll(), this.__tryToRecalculateColumnWidthsIfPending();
      }
    ), e;
  }
  /** @private */
  _createCell(n, e) {
    const i = `vaadin-grid-cell-content-${this._contentIndex = this._contentIndex + 1 || 0}`, r = document.createElement("vaadin-grid-cell-content");
    r.setAttribute("slot", i);
    const o = document.createElement(n);
    o.id = i.replace("-content-", "-"), o.setAttribute("role", n === "td" ? "gridcell" : "columnheader"), !Ma && !Ko && (o.addEventListener("mouseenter", (a) => {
      this.$.scroller.hasAttribute("scrolling") || this._showTooltip(a);
    }), o.addEventListener("mouseleave", () => {
      this._hideTooltip();
    }), o.addEventListener("mousedown", () => {
      this._hideTooltip(!0);
    }));
    const l = document.createElement("slot");
    if (l.setAttribute("name", i), e && e._focusButtonMode) {
      const a = document.createElement("div");
      a.setAttribute("role", "button"), a.setAttribute("tabindex", "-1"), o.appendChild(a), o._focusButton = a, o.focus = function(c) {
        o._focusButton.focus(c);
      }, a.appendChild(l);
    } else
      o.setAttribute("tabindex", "-1"), o.appendChild(l);
    return o._content = r, r.addEventListener("mousedown", () => {
      if (Iu) {
        const a = (c) => {
          const d = r.contains(this.getRootNode().activeElement), f = c.composedPath().includes(r);
          !d && f && o.focus({ preventScroll: !0 }), document.removeEventListener("mouseup", a, !0);
        };
        document.addEventListener("mouseup", a, !0);
      } else
        setTimeout(() => {
          r.contains(this.getRootNode().activeElement) || o.focus({ preventScroll: !0 });
        });
    }), o;
  }
  /**
   * @param {!HTMLTableRowElement} row
   * @param {!Array<!GridColumn>} columns
   * @param {?string} section
   * @param {boolean} isColumnRow
   * @param {boolean} noNotify
   * @protected
   */
  _updateRow(n, e, t = "body", i = !1, r = !1) {
    const o = document.createDocumentFragment();
    Hi(n, (l) => {
      l._vacant = !0;
    }), n.innerHTML = "", t === "body" && (n.__cells = [], n.__detailsCell = null), e.filter((l) => !l.hidden).forEach((l, a, c) => {
      let d;
      if (t === "body") {
        l._cells || (l._cells = []), d = l._cells.find((p) => p._vacant), d || (d = this._createCell("td", l), l._onCellKeyDown && d.addEventListener("keydown", l._onCellKeyDown.bind(l)), l._cells.push(d)), d.setAttribute("part", "cell body-cell"), d.__parentRow = n, n.__cells.push(d);
        const f = n === this.$.sizer;
        if ((!l._bodyContentHidden || f) && n.appendChild(d), f && (l._sizerCell = d), a === c.length - 1 && this.rowDetailsRenderer) {
          this._detailsCells || (this._detailsCells = []);
          const p = this._detailsCells.find((b) => b._vacant) || this._createCell("td");
          this._detailsCells.indexOf(p) === -1 && this._detailsCells.push(p), p._content.parentElement || o.appendChild(p._content), this._configureDetailsCell(p), n.appendChild(p), n.__detailsCell = p, this._a11ySetRowDetailsCell(n, p), p._vacant = !1;
        }
        r || (l._cells = [...l._cells]);
      } else {
        const f = t === "header" ? "th" : "td";
        i || l.localName === "vaadin-grid-column-group" ? (d = l[`_${t}Cell`], d || (d = this._createCell(f), l._onCellKeyDown && d.addEventListener("keydown", l._onCellKeyDown.bind(l))), d._column = l, n.appendChild(d), l[`_${t}Cell`] = d) : (l._emptyCells || (l._emptyCells = []), d = l._emptyCells.find((p) => p._vacant) || this._createCell(f), d._column = l, n.appendChild(d), l._emptyCells.indexOf(d) === -1 && l._emptyCells.push(d)), d.part.add("cell", `${t}-cell`);
      }
      d._content.parentElement || o.appendChild(d._content), d._vacant = !1, d._column = l;
    }), t !== "body" && this.__debounceUpdateHeaderFooterRowVisibility(n), this.appendChild(o), this._frozenCellsChanged(), this._updateFirstAndLastColumnForRow(n);
  }
  /**
   * @param {HTMLTableRowElement} row
   * @protected
   */
  __debounceUpdateHeaderFooterRowVisibility(n) {
    n.__debounceUpdateHeaderFooterRowVisibility = ze.debounce(
      n.__debounceUpdateHeaderFooterRowVisibility,
      yt,
      () => this.__updateHeaderFooterRowVisibility(n)
    );
  }
  /**
   * @param {HTMLTableRowElement} row
   * @protected
   */
  __updateHeaderFooterRowVisibility(n) {
    if (!n)
      return;
    const e = Array.from(n.children).filter((t) => {
      const i = t._column;
      if (i._emptyCells && i._emptyCells.indexOf(t) > -1)
        return !1;
      if (n.parentElement === this.$.header) {
        if (i.headerRenderer)
          return !0;
        if (i.header === null)
          return !1;
        if (i.path || i.header !== void 0)
          return !0;
      } else if (i.footerRenderer)
        return !0;
      return !1;
    });
    n.hidden !== !e.length && (n.hidden = !e.length), this._resetKeyboardNavigation();
  }
  /** @private */
  _updateScrollerItem(n, e) {
    this._preventScrollerRotatingCellFocus(n, e), this._columnTree && (this._updateRowOrderParts(n, e), this._a11yUpdateRowRowindex(n, e), this._getItem(e, n));
  }
  /** @private */
  _columnTreeChanged(n) {
    this._renderColumnTree(n), this.recalculateColumnWidths(), this.__updateColumnsBodyContentHidden();
  }
  /** @private */
  _updateRowOrderParts(n, e = n.index) {
    Di(n, {
      first: e === 0,
      last: e === this._flatSize - 1,
      odd: e % 2 !== 0,
      even: e % 2 === 0
    });
  }
  /** @private */
  _updateRowStateParts(n, { expanded: e, selected: t, detailsOpened: i }) {
    Di(n, {
      expanded: e,
      collapsed: this.__isRowExpandable(n),
      selected: t,
      "details-opened": i
    });
  }
  /** @private */
  __computeEmptyState(n, e) {
    return n === 0 && e;
  }
  /**
   * @param {!Array<!GridColumn>} columnTree
   * @protected
   */
  _renderColumnTree(n) {
    for (at(this.$.items, (e) => {
      this._updateRow(e, n[n.length - 1], "body", !1, !0);
      const t = this.__getRowModel(e);
      this._updateRowOrderParts(e), this._updateRowStateParts(e, t), this._filterDragAndDrop(e, t);
    }); this.$.header.children.length < n.length; ) {
      const e = document.createElement("tr");
      e.setAttribute("part", "row"), e.setAttribute("role", "row"), e.setAttribute("tabindex", "-1"), this.$.header.appendChild(e);
      const t = document.createElement("tr");
      t.setAttribute("part", "row"), t.setAttribute("role", "row"), t.setAttribute("tabindex", "-1"), this.$.footer.appendChild(t);
    }
    for (; this.$.header.children.length > n.length; )
      this.$.header.removeChild(this.$.header.firstElementChild), this.$.footer.removeChild(this.$.footer.firstElementChild);
    at(this.$.header, (e, t, i) => {
      this._updateRow(e, n[t], "header", t === n.length - 1);
      const r = Bi(e);
      Zt(r, "first-header-row-cell", t === 0), Zt(r, "last-header-row-cell", t === i.length - 1);
    }), at(this.$.footer, (e, t, i) => {
      this._updateRow(e, n[n.length - 1 - t], "footer", t === 0);
      const r = Bi(e);
      Zt(r, "first-footer-row-cell", t === 0), Zt(r, "last-footer-row-cell", t === i.length - 1);
    }), this._updateRow(this.$.sizer, n[n.length - 1]), this._resizeHandler(), this._frozenCellsChanged(), this._updateFirstAndLastColumn(), this._resetKeyboardNavigation(), this._a11yUpdateHeaderRows(), this._a11yUpdateFooterRows(), this.generateCellClassNames(), this.generateCellPartNames(), this.__updateHeaderAndFooter();
  }
  /**
   * @param {!HTMLElement} row
   * @param {GridItem} item
   * @protected
   */
  _updateItem(n, e) {
    n._item = e;
    const t = this.__getRowModel(n);
    this._toggleDetailsCell(n, t.detailsOpened), this._a11yUpdateRowLevel(n, t.level), this._a11yUpdateRowSelected(n, t.selected), this._updateRowStateParts(n, t), this._generateCellClassNames(n, t), this._generateCellPartNames(n, t), this._filterDragAndDrop(n, t), this.__updateDragSourceParts(n, t), at(n, (i) => {
      if (!(i._column && !i._column.isConnected) && i._renderer) {
        const r = i._column || this;
        i._renderer.call(r, i._content, r, t);
      }
    }), this._updateDetailsCellHeight(n), this._a11yUpdateRowExpanded(n, t.expanded);
  }
  /** @private */
  _resizeHandler() {
    this._updateDetailsCellHeights(), this.__updateHorizontalScrollPosition();
  }
  /** @private */
  _onAnimationEnd(n) {
    n.animationName.indexOf("vaadin-grid-appear") === 0 && (n.stopPropagation(), this.__tryToRecalculateColumnWidthsIfPending(), this._resetKeyboardNavigation(), requestAnimationFrame(() => {
      this.__scrollToPendingIndexes();
    }));
  }
  /**
   * @param {!HTMLTableRowElement} row
   * @return {!GridItemModel}
   * @protected
   */
  __getRowModel(n) {
    return {
      index: n.index,
      item: n._item,
      level: this._getIndexLevel(n.index),
      expanded: this._isExpanded(n._item),
      selected: this._isSelected(n._item),
      detailsOpened: !!this.rowDetailsRenderer && this._isDetailsOpened(n._item)
    };
  }
  /**
   * @param {Event} event
   * @protected
   */
  _showTooltip(n) {
    const e = this._tooltipController.node;
    if (e && e.isConnected) {
      const t = n.target;
      if (!this.__isCellFullyVisible(t))
        return;
      this._tooltipController.setTarget(t), this._tooltipController.setContext(this.getEventContext(n)), e._stateController.open({
        focus: n.type === "focusin",
        hover: n.type === "mouseenter"
      });
    }
  }
  /** @private */
  __isCellFullyVisible(n) {
    if (n.hasAttribute("frozen") || n.hasAttribute("frozen-to-end"))
      return !0;
    let { left: e, right: t } = this.getBoundingClientRect();
    const i = [...n.parentNode.children].find((l) => l.hasAttribute("last-frozen"));
    if (i) {
      const l = i.getBoundingClientRect();
      e = this.__isRTL ? e : l.right, t = this.__isRTL ? l.left : t;
    }
    const r = [...n.parentNode.children].find((l) => l.hasAttribute("first-frozen-to-end"));
    if (r) {
      const l = r.getBoundingClientRect();
      e = this.__isRTL ? l.right : e, t = this.__isRTL ? t : l.left;
    }
    const o = n.getBoundingClientRect();
    return o.left >= e && o.right <= t;
  }
  /** @protected */
  _hideTooltip(n) {
    const e = this._tooltipController && this._tooltipController.node;
    e && e._stateController.close(n);
  }
  /**
   * Requests an update for the content of cells.
   *
   * While performing the update, the following renderers are invoked:
   * - `Grid.rowDetailsRenderer`
   * - `GridColumn.renderer`
   * - `GridColumn.headerRenderer`
   * - `GridColumn.footerRenderer`
   *
   * It is not guaranteed that the update happens immediately (synchronously) after it is requested.
   */
  requestContentUpdate() {
    this.__updateHeaderAndFooter(), this.__updateVisibleRows();
  }
  /** @private */
  __updateHeaderAndFooter() {
    (this._columnTree || []).forEach((n) => {
      n.forEach((e) => {
        e._renderHeaderAndFooter && e._renderHeaderAndFooter();
      });
    });
  }
  /** @protected */
  __updateVisibleRows(n, e) {
    this.__virtualizer && this.__virtualizer.update(n, e);
  }
};
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Df = Ee`
  @keyframes vaadin-grid-appear {
    to {
      opacity: 1;
    }
  }

  :host {
    display: flex;
    flex-direction: column;
    animation: 1ms vaadin-grid-appear;
    height: 400px;
    flex: 1 1 auto;
    align-self: stretch;
    position: relative;
  }

  :host([hidden]) {
    display: none !important;
  }

  :host([disabled]) {
    pointer-events: none;
  }

  #scroller {
    display: flex;
    flex-direction: column;
    min-height: 100%;
    transform: translateY(0);
    width: auto;
    height: auto;
    position: absolute;
    inset: 0;
  }

  :host([all-rows-visible]) {
    height: auto;
    align-self: flex-start;
    flex-grow: 0;
    width: 100%;
  }

  :host([all-rows-visible]) #scroller {
    width: 100%;
    height: 100%;
    position: relative;
  }

  :host([all-rows-visible]) #items {
    min-height: 1px;
  }

  #table {
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100%;
    overflow: auto;
    position: relative;
    outline: none;
    /* Workaround for a Desktop Safari bug: new stacking context here prevents the scrollbar from getting hidden */
    z-index: 0;
  }

  #header,
  #footer {
    display: block;
    position: -webkit-sticky;
    position: sticky;
    left: 0;
    overflow: visible;
    width: 100%;
    z-index: 1;
  }

  #header {
    top: 0;
  }

  th {
    text-align: inherit;
  }

  /* Safari doesn't work with "inherit" */
  [safari] th {
    text-align: initial;
  }

  #footer {
    bottom: 0;
  }

  #items {
    flex-grow: 1;
    flex-shrink: 0;
    display: block;
    position: -webkit-sticky;
    position: sticky;
    width: 100%;
    left: 0;
    overflow: visible;
  }

  [part~='row'] {
    display: flex;
    width: 100%;
    box-sizing: border-box;
    margin: 0;
  }

  [part~='row'][loading] [part~='body-cell'] ::slotted(vaadin-grid-cell-content) {
    visibility: hidden;
  }

  [column-rendering='lazy'] [part~='body-cell']:not([frozen]):not([frozen-to-end]) {
    transform: translateX(var(--_grid-lazy-columns-start));
  }

  #items [part~='row'] {
    position: absolute;
  }

  #items [part~='row']:empty {
    height: 100%;
  }

  [part~='cell']:not([part~='details-cell']) {
    flex-shrink: 0;
    flex-grow: 1;
    box-sizing: border-box;
    display: flex;
    width: 100%;
    position: relative;
    align-items: center;
    padding: 0;
    white-space: nowrap;
  }

  [part~='cell'] {
    outline: none;
  }

  [part~='cell'] > [tabindex] {
    display: flex;
    align-items: inherit;
    outline: none;
    position: absolute;
    inset: 0;
  }

  /* Switch the focusButtonMode wrapping element to "position: static" temporarily
     when measuring real width of the cells in the auto-width columns. */
  [measuring-auto-width] [part~='cell'] > [tabindex] {
    position: static;
  }

  [part~='details-cell'] {
    position: absolute;
    bottom: 0;
    width: 100%;
    box-sizing: border-box;
    padding: 0;
  }

  [part~='cell'] ::slotted(vaadin-grid-cell-content) {
    display: block;
    width: 100%;
    box-sizing: border-box;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  [hidden] {
    display: none !important;
  }

  [frozen],
  [frozen-to-end] {
    z-index: 2;
    will-change: transform;
  }

  [no-scrollbars][safari] #table,
  [no-scrollbars][firefox] #table {
    overflow: hidden;
  }

  /* Empty state */

  #scroller:not([empty-state]) #emptystatebody,
  #scroller[empty-state] #items {
    display: none;
  }

  #emptystatebody {
    display: flex;
    position: sticky;
    inset: 0;
    flex: 1;
    overflow: hidden;
  }

  #emptystaterow {
    display: flex;
    flex: 1;
  }

  #emptystatecell {
    display: block;
    flex: 1;
    overflow: auto;
  }

  /* Reordering styles */
  :host([reordering]) [part~='cell'] ::slotted(vaadin-grid-cell-content),
  :host([reordering]) [part~='resize-handle'],
  #scroller[no-content-pointer-events] [part~='cell'] ::slotted(vaadin-grid-cell-content) {
    pointer-events: none;
  }

  [part~='reorder-ghost'] {
    visibility: hidden;
    position: fixed;
    pointer-events: none;
    opacity: 0.5;

    /* Prevent overflowing the grid in Firefox */
    top: 0;
    left: 0;
  }

  :host([reordering]) {
    -moz-user-select: none;
    -webkit-user-select: none;
    user-select: none;
  }

  /* Resizing styles */
  [part~='resize-handle'] {
    position: absolute;
    top: 0;
    right: 0;
    height: 100%;
    cursor: col-resize;
    z-index: 1;
  }

  [part~='resize-handle']::before {
    position: absolute;
    content: '';
    height: 100%;
    width: 35px;
    transform: translateX(-50%);
  }

  [last-column] [part~='resize-handle']::before,
  [last-frozen] [part~='resize-handle']::before {
    width: 18px;
    transform: none;
    right: 0;
  }

  [frozen-to-end] [part~='resize-handle'] {
    left: 0;
    right: auto;
  }

  [frozen-to-end] [part~='resize-handle']::before {
    left: 0;
    right: auto;
  }

  [first-frozen-to-end] [part~='resize-handle']::before {
    width: 18px;
    transform: none;
  }

  [first-frozen-to-end] {
    margin-inline-start: auto;
  }

  /* Hide resize handle if scrolled to end */
  :host(:not([overflow~='end'])) [first-frozen-to-end] [part~='resize-handle'] {
    display: none;
  }

  #scroller[column-resizing] {
    -ms-user-select: none;
    -moz-user-select: none;
    -webkit-user-select: none;
    user-select: none;
  }

  /* Sizer styles */
  #sizer {
    display: flex;
    position: absolute;
    visibility: hidden;
  }

  #sizer [part~='details-cell'] {
    display: none !important;
  }

  #sizer [part~='cell'][hidden] {
    display: none !important;
  }

  #sizer [part~='cell'] {
    display: block;
    flex-shrink: 0;
    line-height: 0;
    height: 0 !important;
    min-height: 0 !important;
    max-height: 0 !important;
    padding: 0 !important;
    border: none !important;
  }

  #sizer [part~='cell']::before {
    content: '-';
  }

  #sizer [part~='cell'] ::slotted(vaadin-grid-cell-content) {
    display: none !important;
  }

  /* RTL specific styles */

  :host([dir='rtl']) #items,
  :host([dir='rtl']) #header,
  :host([dir='rtl']) #footer {
    left: auto;
  }

  :host([dir='rtl']) [part~='reorder-ghost'] {
    left: auto;
    right: 0;
  }

  :host([dir='rtl']) [part~='resize-handle'] {
    left: 0;
    right: auto;
  }

  :host([dir='rtl']) [part~='resize-handle']::before {
    transform: translateX(50%);
  }

  :host([dir='rtl']) [last-column] [part~='resize-handle']::before,
  :host([dir='rtl']) [last-frozen] [part~='resize-handle']::before {
    left: 0;
    right: auto;
  }

  :host([dir='rtl']) [frozen-to-end] [part~='resize-handle'] {
    right: 0;
    left: auto;
  }

  :host([dir='rtl']) [frozen-to-end] [part~='resize-handle']::before {
    right: 0;
    left: auto;
  }

  @media (forced-colors: active) {
    [part~='selected-row'] [part~='first-column-cell']::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      bottom: 0;
      border: 2px solid;
    }

    [part~='focused-cell']::before {
      outline: 2px solid !important;
      outline-offset: -1px;
    }
  }
`;
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
Ze("vaadin-grid", Df, { moduleId: "vaadin-grid-styles" });
class Of extends If(Vl(Cr(Er(yi)))) {
  static get template() {
    return Wi`
      <div
        id="scroller"
        safari$="[[_safari]]"
        ios$="[[_ios]]"
        loading$="[[loading]]"
        column-reordering-allowed$="[[columnReorderingAllowed]]"
        empty-state$="[[__emptyState]]"
      >
        <table id="table" role="treegrid" aria-multiselectable="true" tabindex="0">
          <caption id="sizer" part="row"></caption>
          <thead id="header" role="rowgroup"></thead>
          <tbody id="items" role="rowgroup"></tbody>
          <tbody id="emptystatebody">
            <tr id="emptystaterow">
              <td part="empty-state" id="emptystatecell" tabindex="0">
                <slot name="empty-state" id="emptystateslot"></slot>
              </td>
            </tr>
          </tbody>
          <tfoot id="footer" role="rowgroup"></tfoot>
        </table>

        <div part="reorder-ghost"></div>
      </div>

      <slot name="tooltip"></slot>

      <div id="focusexit" tabindex="0"></div>
    `;
  }
  static get is() {
    return "vaadin-grid";
  }
}
Jt(Of);
const hc = Ee`
  :host {
    display: flex;
    align-items: center;
    box-sizing: border-box;
    font-family: var(--lumo-font-family);
    font-size: var(--lumo-font-size-m);
    line-height: var(--lumo-line-height-xs);
    padding: 0.5em calc(var(--lumo-space-l) + var(--lumo-border-radius-m) / 4) 0.5em
      var(--_lumo-list-box-item-padding-left, calc(var(--lumo-border-radius-m) / 4));
    min-height: var(--lumo-size-m);
    outline: none;
    border-radius: var(--lumo-border-radius-m);
    cursor: var(--lumo-clickable-cursor);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    -webkit-tap-highlight-color: var(--lumo-primary-color-10pct);
    --_focus-ring-color: var(--vaadin-focus-ring-color, var(--lumo-primary-color-50pct));
    --_focus-ring-width: var(--vaadin-focus-ring-width, 2px);
    --_selection-color-text: var(--vaadin-selection-color-text, var(--lumo-primary-text-color));
  }

  /* Checkmark */
  [part='checkmark']::before {
    display: var(--_lumo-item-selected-icon-display, none);
    content: var(--lumo-icons-checkmark);
    font-family: lumo-icons;
    font-size: var(--lumo-icon-size-m);
    line-height: 1;
    font-weight: normal;
    width: 1em;
    height: 1em;
    margin: calc((1 - var(--lumo-line-height-xs)) * var(--lumo-font-size-m) / 2) 0;
    color: var(--_selection-color-text);
    flex: none;
    opacity: 0;
    transition:
      transform 0.2s cubic-bezier(0.12, 0.32, 0.54, 2),
      opacity 0.1s;
  }

  :host([selected]) [part='checkmark']::before {
    opacity: 1;
  }

  :host([active]:not([selected])) [part='checkmark']::before {
    transform: scale(0.8);
    opacity: 0;
    transition-duration: 0s;
  }

  [part='content'] {
    flex: auto;
  }

  /* Disabled */
  :host([disabled]) {
    color: var(--lumo-disabled-text-color);
    cursor: default;
    pointer-events: none;
  }

  /* TODO a workaround until we have "focus-follows-mouse". After that, use the hover style for focus-ring as well */
  @media (any-hover: hover) {
    :host(:hover:not([disabled])) {
      background-color: var(--lumo-primary-color-10pct);
    }
  }

  :host([focus-ring]:not([disabled])) {
    box-shadow: inset 0 0 0 var(--_focus-ring-width) var(--_focus-ring-color);
  }

  /* RTL specific styles */
  :host([dir='rtl']) {
    padding-left: calc(var(--lumo-space-l) + var(--lumo-border-radius-m) / 4);
    padding-right: var(--_lumo-list-box-item-padding-left, calc(var(--lumo-border-radius-m) / 4));
  }

  /* Slotted icons */
  :host ::slotted(vaadin-icon) {
    width: var(--lumo-icon-size-m);
    height: var(--lumo-icon-size-m);
  }
`;
Ze("vaadin-item", hc, { moduleId: "lumo-item" });
const zf = Ee`
  :host {
    transition: background-color 100ms;
    overflow: hidden;
    --_lumo-item-selected-icon-display: block;
    --_focus-ring-color: var(--vaadin-focus-ring-color, var(--lumo-primary-color-50pct));
    --_focus-ring-width: var(--vaadin-focus-ring-width, 2px);
  }

  :host([focused]:not([disabled])) {
    box-shadow: inset 0 0 0 var(--_focus-ring-width) var(--_focus-ring-color);
  }
`;
Ze("vaadin-combo-box-item", [hc, zf], {
  moduleId: "lumo-combo-box-item"
});
/**
 * @license
 * Copyright (c) 2022 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ff = Ee`
  [part~='loader'] {
    box-sizing: border-box;
    width: var(--lumo-icon-size-s);
    height: var(--lumo-icon-size-s);
    border: 2px solid transparent;
    border-color: var(--lumo-primary-color-10pct) var(--lumo-primary-color-10pct) var(--lumo-primary-color)
      var(--lumo-primary-color);
    border-radius: calc(0.5 * var(--lumo-icon-size-s));
    opacity: 0;
    pointer-events: none;
  }

  :host(:not([loading])) [part~='loader'] {
    display: none;
  }

  :host([loading]) [part~='loader'] {
    animation:
      1s linear infinite lumo-loader-rotate,
      0.3s 0.1s lumo-loader-fade-in both;
  }

  @keyframes lumo-loader-fade-in {
    0% {
      opacity: 0;
    }

    100% {
      opacity: 1;
    }
  }

  @keyframes lumo-loader-rotate {
    0% {
      transform: rotate(0deg);
    }

    100% {
      transform: rotate(360deg);
    }
  }
`;
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ts = Ee`
  :host {
    top: var(--lumo-space-m);
    right: var(--lumo-space-m);
    bottom: var(--lumo-space-m);
    left: var(--lumo-space-m);
    /* Workaround for Edge issue (only on Surface), where an overflowing vaadin-list-box inside vaadin-select-overlay makes the overlay transparent */
    /* stylelint-disable-next-line */
    outline: 0px solid transparent;
  }

  [part='overlay'] {
    background-color: var(--lumo-base-color);
    background-image: linear-gradient(var(--lumo-tint-5pct), var(--lumo-tint-5pct));
    border-radius: var(--lumo-border-radius-m);
    box-shadow:
      0 0 0 1px var(--lumo-shade-5pct),
      var(--lumo-box-shadow-m);
    color: var(--lumo-body-text-color);
    font-family: var(--lumo-font-family);
    font-size: var(--lumo-font-size-m);
    font-weight: 400;
    line-height: var(--lumo-line-height-m);
    letter-spacing: 0;
    text-transform: none;
    -webkit-text-size-adjust: 100%;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  [part='content'] {
    padding: var(--lumo-space-xs);
  }

  [part='backdrop'] {
    background-color: var(--lumo-shade-20pct);
    animation: 0.2s lumo-overlay-backdrop-enter both;
    will-change: opacity;
  }

  @keyframes lumo-overlay-backdrop-enter {
    0% {
      opacity: 0;
    }
  }

  :host([closing]) [part='backdrop'] {
    animation: 0.2s lumo-overlay-backdrop-exit both;
  }

  @keyframes lumo-overlay-backdrop-exit {
    100% {
      opacity: 0;
    }
  }

  @keyframes lumo-overlay-dummy-animation {
    0% {
      opacity: 1;
    }

    100% {
      opacity: 1;
    }
  }
`;
Ze("", Ts, { moduleId: "lumo-overlay" });
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Es = Ee`
  :host([opening]),
  :host([closing]) {
    animation: 0.14s lumo-overlay-dummy-animation;
  }

  [part='overlay'] {
    will-change: opacity, transform;
  }

  :host([opening]) [part='overlay'] {
    animation: 0.1s lumo-menu-overlay-enter ease-out both;
  }

  @keyframes lumo-menu-overlay-enter {
    0% {
      opacity: 0;
      transform: translateY(-4px);
    }
  }

  :host([closing]) [part='overlay'] {
    animation: 0.1s lumo-menu-overlay-exit both;
  }

  @keyframes lumo-menu-overlay-exit {
    100% {
      opacity: 0;
    }
  }
`;
Ze("", Es, { moduleId: "lumo-menu-overlay-core" });
const Lf = Ee`
  /* Small viewport (bottom sheet) styles */
  /* Use direct media queries instead of the state attributes ([phone] and [fullscreen]) provided by the elements */
  @media (max-width: 420px), (max-height: 420px) {
    :host {
      top: 0 !important;
      right: 0 !important;
      bottom: var(--vaadin-overlay-viewport-bottom, 0) !important;
      left: 0 !important;
      align-items: stretch !important;
      justify-content: flex-end !important;
    }

    [part='overlay'] {
      max-height: 50vh;
      width: 100vw;
      border-radius: 0;
      box-shadow: var(--lumo-box-shadow-xl);
    }

    /* The content part scrolls instead of the overlay part, because of the gradient fade-out */
    [part='content'] {
      padding: 30px var(--lumo-space-m);
      max-height: inherit;
      box-sizing: border-box;
      -webkit-overflow-scrolling: touch;
      overflow: auto;
      -webkit-mask-image: linear-gradient(transparent, #000 40px, #000 calc(100% - 40px), transparent);
      mask-image: linear-gradient(transparent, #000 40px, #000 calc(100% - 40px), transparent);
    }

    [part='backdrop'] {
      display: block;
    }

    /* Animations */

    :host([opening]) [part='overlay'] {
      animation: 0.2s lumo-mobile-menu-overlay-enter cubic-bezier(0.215, 0.61, 0.355, 1) both;
    }

    :host([closing]),
    :host([closing]) [part='backdrop'] {
      animation-delay: 0.14s;
    }

    :host([closing]) [part='overlay'] {
      animation: 0.14s 0.14s lumo-mobile-menu-overlay-exit cubic-bezier(0.55, 0.055, 0.675, 0.19) both;
    }
  }

  @keyframes lumo-mobile-menu-overlay-enter {
    0% {
      transform: translateY(150%);
    }
  }

  @keyframes lumo-mobile-menu-overlay-exit {
    100% {
      transform: translateY(150%);
    }
  }
`, Mf = [Ts, Es, Lf];
Ze("", Mf, { moduleId: "lumo-menu-overlay" });
const Nf = Ee`
  [part='content'] {
    padding: 0;
  }

  /* When items are empty, the spinner needs some room */
  :host(:not([closing])) [part~='content'] {
    min-height: calc(2 * var(--lumo-space-s) + var(--lumo-icon-size-s));
  }

  [part~='overlay'] {
    position: relative;
  }

  :host([top-aligned]) [part~='overlay'] {
    margin-top: var(--lumo-space-xs);
  }

  :host([bottom-aligned]) [part~='overlay'] {
    margin-bottom: var(--lumo-space-xs);
  }
`, Bf = Ee`
  [part~='loader'] {
    position: absolute;
    z-index: 1;
    inset-inline: var(--lumo-space-s);
    top: var(--lumo-space-s);
    margin-inline: auto 0;
  }
`;
Ze(
  "vaadin-combo-box-overlay",
  [
    Ts,
    Es,
    Nf,
    Ff,
    Bf,
    Ee`
      :host {
        --_vaadin-combo-box-items-container-border-width: var(--lumo-space-xs);
        --_vaadin-combo-box-items-container-border-style: solid;
      }
    `
  ],
  { moduleId: "lumo-combo-box-overlay" }
);
Ze(
  "vaadin-input-container",
  Ee`
    :host {
      background: var(--_background);
      padding: 0 calc(0.375em + var(--_input-container-radius) / 4 - 1px);
      font-weight: var(--vaadin-input-field-value-font-weight, 500);
      line-height: 1;
      position: relative;
      cursor: text;
      box-sizing: border-box;
      border-radius:
        /* See https://developer.mozilla.org/en-US/docs/Web/CSS/border-radius#syntax */
        var(--vaadin-input-field-top-start-radius, var(--_input-container-radius))
        var(--vaadin-input-field-top-end-radius, var(--_input-container-radius))
        var(--vaadin-input-field-bottom-end-radius, var(--_input-container-radius))
        var(--vaadin-input-field-bottom-start-radius, var(--_input-container-radius));
      /* Fallback */
      --_input-container-radius: var(--vaadin-input-field-border-radius, var(--lumo-border-radius-m));
      --_input-height: var(--lumo-text-field-size, var(--lumo-size-m));
      /* Default values */
      --_background: var(--vaadin-input-field-background, var(--lumo-contrast-10pct));
      --_hover-highlight: var(--vaadin-input-field-hover-highlight, var(--lumo-contrast-50pct));
      --_input-border-color: var(--vaadin-input-field-border-color, var(--lumo-contrast-50pct));
      --_icon-color: var(--vaadin-input-field-icon-color, var(--lumo-contrast-60pct));
      --_icon-size: var(--vaadin-input-field-icon-size, var(--lumo-icon-size-m));
      --_invalid-background: var(--vaadin-input-field-invalid-background, var(--lumo-error-color-10pct));
      --_invalid-hover-highlight: var(--vaadin-input-field-invalid-hover-highlight, var(--lumo-error-color-50pct));
      --_disabled-background: var(--vaadin-input-field-disabled-background, var(--lumo-contrast-5pct));
      --_disabled-value-color: var(--vaadin-input-field-disabled-value-color, var(--lumo-disabled-text-color));
    }

    :host([dir='rtl']) {
      border-radius:
        /* Don't use logical props, see https://github.com/vaadin/vaadin-time-picker/issues/145 */
        var(--vaadin-input-field-top-end-radius, var(--_input-container-radius))
        var(--vaadin-input-field-top-start-radius, var(--_input-container-radius))
        var(--vaadin-input-field-bottom-start-radius, var(--_input-container-radius))
        var(--vaadin-input-field-bottom-end-radius, var(--_input-container-radius));
    }

    /* Used for hover and activation effects */
    :host::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--_hover-highlight);
      opacity: 0;
      transition:
        transform 0.15s,
        opacity 0.2s;
      transform-origin: 100% 0;
    }

    ::slotted(:not([slot$='fix'])) {
      cursor: inherit;
      min-height: var(--vaadin-input-field-height, var(--_input-height));
      padding: 0 0.25em;
      --_lumo-text-field-overflow-mask-image: linear-gradient(to left, transparent, #000 1.25em);
      -webkit-mask-image: var(--_lumo-text-field-overflow-mask-image);
      mask-image: var(--_lumo-text-field-overflow-mask-image);
    }

    /* Read-only */
    :host([readonly]) {
      color: var(--lumo-secondary-text-color);
      background-color: transparent;
      cursor: default;
    }

    :host([readonly])::after {
      background-color: transparent;
      opacity: 1;
      border: var(--vaadin-input-field-readonly-border, 1px dashed var(--lumo-contrast-30pct));
    }

    /* Disabled */
    :host([disabled]) {
      background: var(--_disabled-background);
    }

    :host([disabled]) ::slotted(:not([slot$='fix'])) {
      -webkit-text-fill-color: var(--_disabled-value-color);
      color: var(--_disabled-value-color);
    }

    /* Invalid */
    :host([invalid]) {
      background: var(--_invalid-background);
    }

    :host([invalid]:not([readonly]))::after {
      background: var(--_invalid-hover-highlight);
    }

    /* Slotted icons */
    ::slotted(vaadin-icon) {
      color: var(--_icon-color);
      width: var(--_icon-size);
      height: var(--_icon-size);
    }

    /* Vaadin icons are based on a 16x16 grid (unlike Lumo and Material icons with 24x24), so they look too big by default */
    ::slotted(vaadin-icon[icon^='vaadin:']) {
      padding: 0.25em;
      box-sizing: border-box !important;
    }

    /* Text align */
    :host([dir='rtl']) ::slotted(:not([slot$='fix'])) {
      --_lumo-text-field-overflow-mask-image: linear-gradient(to right, transparent, #000 1.25em);
    }

    @-moz-document url-prefix() {
      :host([dir='rtl']) ::slotted(:not([slot$='fix'])) {
        mask-image: var(--_lumo-text-field-overflow-mask-image);
      }
    }

    :host([theme~='align-left']) ::slotted(:not([slot$='fix'])) {
      text-align: start;
      --_lumo-text-field-overflow-mask-image: none;
    }

    :host([theme~='align-center']) ::slotted(:not([slot$='fix'])) {
      text-align: center;
      --_lumo-text-field-overflow-mask-image: none;
    }

    :host([theme~='align-right']) ::slotted(:not([slot$='fix'])) {
      text-align: end;
      --_lumo-text-field-overflow-mask-image: none;
    }

    @-moz-document url-prefix() {
      /* Firefox is smart enough to align overflowing text to right */
      :host([theme~='align-right']) ::slotted(:not([slot$='fix'])) {
        --_lumo-text-field-overflow-mask-image: linear-gradient(to right, transparent 0.25em, #000 1.5em);
      }
    }

    @-moz-document url-prefix() {
      /* Firefox is smart enough to align overflowing text to right */
      :host([theme~='align-left']) ::slotted(:not([slot$='fix'])) {
        --_lumo-text-field-overflow-mask-image: linear-gradient(to left, transparent 0.25em, #000 1.5em);
      }
    }

    /* RTL specific styles */
    :host([dir='rtl'])::after {
      transform-origin: 0% 0;
    }

    :host([theme~='align-left'][dir='rtl']) ::slotted(:not([slot$='fix'])) {
      --_lumo-text-field-overflow-mask-image: none;
    }

    :host([theme~='align-center'][dir='rtl']) ::slotted(:not([slot$='fix'])) {
      --_lumo-text-field-overflow-mask-image: none;
    }

    :host([theme~='align-right'][dir='rtl']) ::slotted(:not([slot$='fix'])) {
      --_lumo-text-field-overflow-mask-image: none;
    }

    @-moz-document url-prefix() {
      /* Firefox is smart enough to align overflowing text to right */
      :host([theme~='align-right'][dir='rtl']) ::slotted(:not([slot$='fix'])) {
        --_lumo-text-field-overflow-mask-image: linear-gradient(to right, transparent 0.25em, #000 1.5em);
      }
    }

    @-moz-document url-prefix() {
      /* Firefox is smart enough to align overflowing text to right */
      :host([theme~='align-left'][dir='rtl']) ::slotted(:not([slot$='fix'])) {
        --_lumo-text-field-overflow-mask-image: linear-gradient(to left, transparent 0.25em, #000 1.5em);
      }
    }
  `,
  { moduleId: "lumo-input-container" }
);
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const dc = Ee`
  [part$='button'] {
    flex: none;
    width: 1em;
    height: 1em;
    line-height: 1;
    font-size: var(--lumo-icon-size-m);
    text-align: center;
    color: var(--lumo-contrast-60pct);
    transition: 0.2s color;
    cursor: var(--lumo-clickable-cursor);
  }

  [part$='button']:hover {
    color: var(--lumo-contrast-90pct);
  }

  :host([disabled]) [part$='button'],
  :host([readonly]) [part$='button'] {
    color: var(--lumo-contrast-20pct);
    cursor: default;
  }

  [part$='button']::before {
    font-family: 'lumo-icons';
    display: block;
  }
`;
Ze("", dc, { moduleId: "lumo-field-button" });
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Hf = Ee`
  :host {
    --_helper-spacing: var(--vaadin-input-field-helper-spacing, 0.4em);
  }

  :host([has-helper]) [part='helper-text']::before {
    content: '';
    display: block;
    height: var(--_helper-spacing);
  }

  [part='helper-text'] {
    display: block;
    color: var(--vaadin-input-field-helper-color, var(--lumo-secondary-text-color));
    font-size: var(--vaadin-input-field-helper-font-size, var(--lumo-font-size-xs));
    line-height: var(--lumo-line-height-xs);
    font-weight: var(--vaadin-input-field-helper-font-weight, 400);
    margin-left: calc(var(--lumo-border-radius-m) / 4);
    transition: color 0.2s;
  }

  :host(:hover:not([readonly])) [part='helper-text'] {
    color: var(--lumo-body-text-color);
  }

  :host([disabled]) [part='helper-text'] {
    color: var(--lumo-disabled-text-color);
    -webkit-text-fill-color: var(--lumo-disabled-text-color);
  }

  :host([has-helper][theme~='helper-above-field']) [part='helper-text']::before {
    display: none;
  }

  :host([has-helper][theme~='helper-above-field']) [part='helper-text']::after {
    content: '';
    display: block;
    height: var(--_helper-spacing);
  }

  :host([has-helper][theme~='helper-above-field']) [part='label'] {
    order: 0;
    padding-bottom: var(--_helper-spacing);
  }

  :host([has-helper][theme~='helper-above-field']) [part='helper-text'] {
    order: 1;
  }

  :host([has-helper][theme~='helper-above-field']) [part='label'] + * {
    order: 2;
  }

  :host([has-helper][theme~='helper-above-field']) [part='error-message'] {
    order: 3;
  }
`;
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const uc = Ee`
  [part='label'] {
    align-self: flex-start;
    color: var(--vaadin-input-field-label-color, var(--lumo-secondary-text-color));
    font-weight: var(--vaadin-input-field-label-font-weight, 500);
    font-size: var(--vaadin-input-field-label-font-size, var(--lumo-font-size-s));
    margin-left: calc(var(--lumo-border-radius-m) / 4);
    transition: color 0.2s;
    line-height: 1;
    padding-right: 1em;
    padding-bottom: 0.5em;
    /* As a workaround for diacritics being cut off, add a top padding and a
    negative margin to compensate */
    padding-top: 0.25em;
    margin-top: -0.25em;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
    position: relative;
    max-width: 100%;
    box-sizing: border-box;
  }

  :host([focused]:not([readonly])) [part='label'] {
    color: var(--vaadin-input-field-focused-label-color, var(--lumo-primary-text-color));
  }

  :host(:hover:not([readonly]):not([focused])) [part='label'] {
    color: var(--vaadin-input-field-hovered-label-color, var(--lumo-body-text-color));
  }

  /* Touch device adjustment */
  @media (pointer: coarse) {
    :host(:hover:not([readonly]):not([focused])) [part='label'] {
      color: var(--vaadin-input-field-label-color, var(--lumo-secondary-text-color));
    }
  }

  :host([has-label])::before {
    margin-top: calc(var(--lumo-font-size-s) * 1.5);
  }

  :host([has-label][theme~='small'])::before {
    margin-top: calc(var(--lumo-font-size-xs) * 1.5);
  }

  :host([has-label]) {
    padding-top: var(--lumo-space-m);
  }

  :host([has-label]) ::slotted([slot='tooltip']) {
    --vaadin-tooltip-offset-bottom: calc((var(--lumo-space-m) - var(--lumo-space-xs)) * -1);
  }

  :host([required]) [part='required-indicator']::after {
    content: var(--lumo-required-field-indicator, '\\2022');
    transition: opacity 0.2s;
    color: var(--lumo-required-field-indicator-color, var(--lumo-primary-text-color));
    position: absolute;
    right: 0;
    width: 1em;
    text-align: center;
  }

  :host([invalid]) [part='required-indicator']::after {
    color: var(--lumo-required-field-indicator-color, var(--lumo-error-text-color));
  }

  [part='error-message'] {
    margin-left: calc(var(--lumo-border-radius-m) / 4);
    font-size: var(--vaadin-input-field-error-font-size, var(--lumo-font-size-xs));
    line-height: var(--lumo-line-height-xs);
    font-weight: var(--vaadin-input-field-error-font-weight, 400);
    color: var(--vaadin-input-field-error-color, var(--lumo-error-text-color));
    will-change: max-height;
    transition: 0.4s max-height;
    max-height: 5em;
  }

  :host([has-error-message]) [part='error-message']::before,
  :host([has-error-message]) [part='error-message']::after {
    content: '';
    display: block;
    height: 0.4em;
  }

  :host(:not([invalid])) [part='error-message'] {
    max-height: 0;
    overflow: hidden;
  }

  /* RTL specific styles */

  :host([dir='rtl']) [part='label'] {
    margin-left: 0;
    margin-right: calc(var(--lumo-border-radius-m) / 4);
  }

  :host([dir='rtl']) [part='label'] {
    padding-left: 1em;
    padding-right: 0;
  }

  :host([dir='rtl']) [part='required-indicator']::after {
    right: auto;
    left: 0;
  }

  :host([dir='rtl']) [part='error-message'] {
    margin-left: 0;
    margin-right: calc(var(--lumo-border-radius-m) / 4);
  }
`;
Ze("", uc, { moduleId: "lumo-required-field" });
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Vf = Ee`
  :host {
    --lumo-text-field-size: var(--lumo-size-m);
    color: var(--vaadin-input-field-value-color, var(--lumo-body-text-color));
    font-size: var(--vaadin-input-field-value-font-size, var(--lumo-font-size-m));
    font-family: var(--lumo-font-family);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    -webkit-tap-highlight-color: transparent;
    padding: var(--lumo-space-xs) 0;
    --_focus-ring-color: var(--vaadin-focus-ring-color, var(--lumo-primary-color-50pct));
    --_focus-ring-width: var(--vaadin-focus-ring-width, 2px);
    --_input-height: var(--vaadin-input-field-height, var(--lumo-text-field-size));
    --_disabled-value-color: var(--vaadin-input-field-disabled-value-color, var(--lumo-disabled-text-color));
  }

  :host::before {
    height: var(--_input-height);
    box-sizing: border-box;
    display: inline-flex;
    align-items: center;
  }

  :host([focused]) [part='input-field'] ::slotted(:is(input, textarea)) {
    -webkit-mask-image: none;
    mask-image: none;
  }

  ::slotted(:is(input, textarea):placeholder-shown) {
    color: var(--vaadin-input-field-placeholder-color, var(--lumo-secondary-text-color));
  }

  /* Hover */
  :host(:hover:not([readonly]):not([focused])) [part='input-field']::after {
    opacity: var(--vaadin-input-field-hover-highlight-opacity, 0.1);
  }

  /* Touch device adjustment */
  @media (pointer: coarse) {
    :host(:hover:not([readonly]):not([focused])) [part='input-field']::after {
      opacity: 0;
    }

    :host(:active:not([readonly]):not([focused])) [part='input-field']::after {
      opacity: 0.2;
    }
  }

  /* Trigger when not focusing using the keyboard */
  :host([focused]:not([focus-ring]):not([readonly])) [part='input-field']::after {
    transform: scaleX(0);
    transition-duration: 0.15s, 1s;
  }

  /* Focus-ring */
  :host([focus-ring]) [part='input-field'] {
    box-shadow: 0 0 0 var(--_focus-ring-width) var(--_focus-ring-color);
  }

  /* Read-only and disabled */
  :host(:is([readonly], [disabled])) ::slotted(:is(input, textarea):placeholder-shown) {
    opacity: 0;
  }

  /* Read-only style */
  :host([readonly]) {
    --vaadin-input-field-border-color: transparent;
  }

  /* Disabled style */
  :host([disabled]) {
    pointer-events: none;
    --vaadin-input-field-border-color: var(--lumo-contrast-20pct);
  }

  :host([disabled]) [part='label'],
  :host([disabled]) [part='input-field'] ::slotted([slot$='fix']) {
    color: var(--lumo-disabled-text-color);
    -webkit-text-fill-color: var(--lumo-disabled-text-color);
  }

  :host([disabled]) [part='input-field'] ::slotted(:not([slot$='fix'])) {
    color: var(--_disabled-value-color);
    -webkit-text-fill-color: var(--_disabled-value-color);
  }

  /* Invalid style */
  :host([invalid]) {
    --vaadin-input-field-border-color: var(--lumo-error-color);
  }

  :host([invalid][focus-ring]) [part='input-field'] {
    box-shadow: 0 0 0 2px var(--lumo-error-color-50pct);
  }

  :host([input-prevented]) [part='input-field'] {
    animation: shake 0.15s infinite;
  }

  @keyframes shake {
    25% {
      transform: translateX(4px);
    }
    75% {
      transform: translateX(-4px);
    }
  }

  /* Small theme */
  :host([theme~='small']) {
    font-size: var(--lumo-font-size-s);
    --lumo-text-field-size: var(--lumo-size-s);
  }

  :host([theme~='small']) [part='label'] {
    font-size: var(--lumo-font-size-xs);
  }

  :host([theme~='small']) [part='error-message'] {
    font-size: var(--lumo-font-size-xxs);
  }

  /* Slotted content */
  [part='input-field'] ::slotted(:not(vaadin-icon):not(input):not(textarea)) {
    color: var(--lumo-secondary-text-color);
    font-weight: 400;
  }

  [part='clear-button']::before {
    content: var(--lumo-icons-cross);
  }
`, fc = [uc, dc, Hf, Vf];
Ze("", fc, {
  moduleId: "lumo-input-field-shared-styles"
});
const Uf = Ee`
  [part='toggle-button']::before {
    content: var(--lumo-icons-dropdown);
  }
`;
Ze("vaadin-combo-box", [fc, Uf], { moduleId: "lumo-combo-box" });
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Wf = (s) => class extends s {
  static get properties() {
    return {
      /**
       * If true, the user cannot interact with this element.
       */
      disabled: {
        type: Boolean,
        reflectToAttribute: !0
      },
      /**
       * Set to true to make this element read-only.
       */
      readonly: {
        type: Boolean,
        reflectToAttribute: !0
      },
      /**
       * Set to true when the element is invalid.
       */
      invalid: {
        type: Boolean,
        reflectToAttribute: !0
      }
    };
  }
  /** @protected */
  ready() {
    super.ready(), this.addEventListener("pointerdown", (e) => {
      e.target === this && e.preventDefault();
    }), this.addEventListener("click", (e) => {
      e.target === this && this.shadowRoot.querySelector("slot:not([name])").assignedNodes({ flatten: !0 }).forEach((t) => t.focus && t.focus());
    });
  }
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const jf = Ee`
  :host {
    display: flex;
    align-items: center;
    flex: 0 1 auto;
    border-radius:
            /* See https://developer.mozilla.org/en-US/docs/Web/CSS/border-radius */
      var(--vaadin-input-field-top-start-radius, var(--__border-radius))
      var(--vaadin-input-field-top-end-radius, var(--__border-radius))
      var(--vaadin-input-field-bottom-end-radius, var(--__border-radius))
      var(--vaadin-input-field-bottom-start-radius, var(--__border-radius));
    --_border-radius: var(--vaadin-input-field-border-radius, 0);
    --_input-border-width: var(--vaadin-input-field-border-width, 0);
    --_input-border-color: var(--vaadin-input-field-border-color, transparent);
    box-shadow: inset 0 0 0 var(--_input-border-width, 0) var(--_input-border-color);
  }

  :host([dir='rtl']) {
    border-radius:
            /* Don't use logical props, see https://github.com/vaadin/vaadin-time-picker/issues/145 */
      var(--vaadin-input-field-top-end-radius, var(--_border-radius))
      var(--vaadin-input-field-top-start-radius, var(--_border-radius))
      var(--vaadin-input-field-bottom-start-radius, var(--_border-radius))
      var(--vaadin-input-field-bottom-end-radius, var(--_border-radius));
  }

  :host([hidden]) {
    display: none !important;
  }

  /* Reset the native input styles */
  ::slotted(input) {
    -webkit-appearance: none;
    -moz-appearance: none;
    flex: auto;
    white-space: nowrap;
    overflow: hidden;
    width: 100%;
    height: 100%;
    outline: none;
    margin: 0;
    padding: 0;
    border: 0;
    border-radius: 0;
    min-width: 0;
    font: inherit;
    line-height: normal;
    color: inherit;
    background-color: transparent;
    /* Disable default invalid style in Firefox */
    box-shadow: none;
  }

  ::slotted(*) {
    flex: none;
  }

  ::slotted(:is(input, textarea))::placeholder {
    /* Use ::slotted(input:placeholder-shown) in themes to style the placeholder. */
    /* because ::slotted(...)::placeholder does not work in Safari. */
    font: inherit;
    color: inherit;
    /* Override default opacity in Firefox */
    opacity: 1;
  }
`;
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
Ze("vaadin-input-container", jf, { moduleId: "vaadin-input-container-styles" });
class Gf extends Wf(Cr(Tr(yi))) {
  static get is() {
    return "vaadin-input-container";
  }
  static get template() {
    return Wi`
      <slot name="prefix"></slot>
      <slot></slot>
      <slot name="suffix"></slot>
    `;
  }
}
Jt(Gf);
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const qf = (s) => class extends s {
  static get properties() {
    return {
      /**
       * The index of the item.
       */
      index: {
        type: Number
      },
      /**
       * The item to render.
       */
      item: {
        type: Object
      },
      /**
       * The text to render in the item.
       */
      label: {
        type: String
      },
      /**
       * True when item is selected.
       */
      selected: {
        type: Boolean,
        value: !1,
        reflectToAttribute: !0
      },
      /**
       * True when item is focused.
       */
      focused: {
        type: Boolean,
        value: !1,
        reflectToAttribute: !0
      },
      /**
       * Custom function for rendering the item content.
       */
      renderer: {
        type: Function
      }
    };
  }
  static get observers() {
    return ["__rendererOrItemChanged(renderer, index, item, selected, focused)", "__updateLabel(label, renderer)"];
  }
  static get observedAttributes() {
    return [...super.observedAttributes, "hidden"];
  }
  attributeChangedCallback(e, t, i) {
    e === "hidden" && i !== null ? this.index = void 0 : super.attributeChangedCallback(e, t, i);
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback(), this._owner = this.parentNode.owner;
    const e = this._owner.getAttribute("dir");
    e && this.setAttribute("dir", e);
  }
  /**
   * Requests an update for the content of the item.
   * While performing the update, it invokes the renderer passed in the `renderer` property.
   *
   * It is not guaranteed that the update happens immediately (synchronously) after it is requested.
   */
  requestContentUpdate() {
    if (!this.renderer || this.hidden)
      return;
    const e = {
      index: this.index,
      item: this.item,
      focused: this.focused,
      selected: this.selected
    };
    this.renderer(this, this._owner, e);
  }
  /** @private */
  __rendererOrItemChanged(e, t, i) {
    i === void 0 || t === void 0 || (this._oldRenderer !== e && (this.innerHTML = "", delete this._$litPart$), e && (this._oldRenderer = e, this.requestContentUpdate()));
  }
  /** @private */
  __updateLabel(e, t) {
    t || (this.textContent = e);
  }
};
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Kf extends qf(Cr(Tr(yi))) {
  static get template() {
    return Wi`
      <style>
        :host {
          display: block;
        }

        :host([hidden]) {
          display: none;
        }
      </style>
      <span part="checkmark" aria-hidden="true"></span>
      <div part="content">
        <slot></slot>
      </div>
    `;
  }
  static get is() {
    return "vaadin-combo-box-item";
  }
}
Jt(Kf);
/**
@license
Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
Code distributed by Google as part of the polymer project is also
subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
*/
let Xo = !1, Zf = [], pc = [];
function Yf() {
  Xo = !0, requestAnimationFrame(function() {
    Xo = !1, Xf(Zf), setTimeout(function() {
      Qf(pc);
    });
  });
}
function Xf(s) {
  for (; s.length; )
    gc(s.shift());
}
function Qf(s) {
  for (let n = 0, e = s.length; n < e; n++)
    gc(s.shift());
}
function gc(s) {
  const n = s[0], e = s[1], t = s[2];
  try {
    e.apply(n, t);
  } catch (i) {
    setTimeout(() => {
      throw i;
    });
  }
}
function Jf(s, n, e) {
  Xo || Yf(), pc.push([s, n, e]);
}
/**
 * @license
 * Copyright (c) 2017 Anton Korzunov
 * SPDX-License-Identifier: MIT
 */
let ki = /* @__PURE__ */ new WeakMap(), tn = /* @__PURE__ */ new WeakMap(), rn = {}, So = 0;
const Ya = (s) => s && s.nodeType === Node.ELEMENT_NODE, Ro = (...s) => {
}, $f = (s, n) => Ya(s) ? n.map((e) => {
  if (!Ya(e))
    return Ro(e, "is not a valid element"), null;
  let t = e;
  for (; t && t !== s; ) {
    if (s.contains(t))
      return e;
    t = t.getRootNode().host;
  }
  return Ro(e, "is not contained inside", s), null;
}).filter((e) => !!e) : (Ro(s, "is not a valid element"), []), ep = (s, n, e, t) => {
  const i = $f(n, Array.isArray(s) ? s : [s]);
  rn[e] || (rn[e] = /* @__PURE__ */ new WeakMap());
  const r = rn[e], o = [], l = /* @__PURE__ */ new Set(), a = new Set(i), c = (f) => {
    if (!f || l.has(f))
      return;
    l.add(f);
    const p = f.assignedSlot;
    p && c(p), c(f.parentNode || f.host);
  };
  i.forEach(c);
  const d = (f) => {
    if (!f || a.has(f))
      return;
    const p = f.shadowRoot;
    (p ? [...f.children, ...p.children] : [...f.children]).forEach((x) => {
      if (!["template", "script", "style"].includes(x.localName))
        if (l.has(x))
          d(x);
        else {
          const T = x.getAttribute(t), I = T !== null && T !== "false", z = (ki.get(x) || 0) + 1, W = (r.get(x) || 0) + 1;
          ki.set(x, z), r.set(x, W), o.push(x), z === 1 && I && tn.set(x, !0), W === 1 && x.setAttribute(e, "true"), I || x.setAttribute(t, "true");
        }
    });
  };
  return d(n), l.clear(), So += 1, () => {
    o.forEach((f) => {
      const p = ki.get(f) - 1, b = r.get(f) - 1;
      ki.set(f, p), r.set(f, b), p || (tn.has(f) ? tn.delete(f) : f.removeAttribute(t)), b || f.removeAttribute(e);
    }), So -= 1, So || (ki = /* @__PURE__ */ new WeakMap(), ki = /* @__PURE__ */ new WeakMap(), tn = /* @__PURE__ */ new WeakMap(), rn = {});
  };
}, tp = (s, n = document.body, e = "data-aria-hidden") => {
  const t = Array.from(Array.isArray(s) ? s : [s]);
  return n && t.push(...Array.from(n.querySelectorAll("[aria-live]"))), ep(t, n, e, "aria-hidden");
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class ip {
  /**
   * @param {HTMLElement} host
   */
  constructor(n, e) {
    this.host = n, this.callback = typeof e == "function" ? e : () => n;
  }
  /**
   * Make the controller host modal by hiding other elements from screen readers
   * using `aria-hidden` attribute (can be replaced with `inert` in the future).
   *
   * The method name is chosen to align with the one provided by native `<dialog>`:
   * https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/showModal
   */
  showModal() {
    const n = this.callback();
    this.__showOthers = tp(n);
  }
  /**
   * Remove `aria-hidden` from other elements unless there are any other
   * controller hosts on the page activated by using `showModal()` call.
   */
  close() {
    this.__showOthers && (this.__showOthers(), this.__showOthers = null);
  }
}
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const rp = (s) => class extends Er(s) {
  static get properties() {
    return {
      /**
       * When true, opening the overlay moves focus to the first focusable child,
       * or to the overlay part with tabindex if there are no focusable children.
       * @attr {boolean} focus-trap
       */
      focusTrap: {
        type: Boolean,
        value: !1
      },
      /**
       * Set to true to enable restoring of focus when overlay is closed.
       * @attr {boolean} restore-focus-on-close
       */
      restoreFocusOnClose: {
        type: Boolean,
        value: !1
      },
      /**
       * Set to specify the element which should be focused on overlay close,
       * if `restoreFocusOnClose` is set to true.
       * @type {HTMLElement}
       */
      restoreFocusNode: {
        type: HTMLElement
      }
    };
  }
  constructor() {
    super(), this.__ariaModalController = new ip(this), this.__focusTrapController = new Tf(this), this.__focusRestorationController = new Ef();
  }
  /** @protected */
  ready() {
    super.ready(), this.addController(this.__ariaModalController), this.addController(this.__focusTrapController), this.addController(this.__focusRestorationController);
  }
  /**
   * Release focus and restore focus after the overlay is closed.
   *
   * @protected
   */
  _resetFocus() {
    if (this.focusTrap && (this.__ariaModalController.close(), this.__focusTrapController.releaseFocus()), this.restoreFocusOnClose && this._shouldRestoreFocus()) {
      const e = !Ln();
      this.__focusRestorationController.restoreFocus({ preventScroll: e });
    }
  }
  /**
   * Save the previously focused node when the overlay starts to open.
   *
   * @protected
   */
  _saveFocus() {
    this.restoreFocusOnClose && this.__focusRestorationController.saveFocus(this.restoreFocusNode);
  }
  /**
   * Trap focus within the overlay after opening has completed.
   *
   * @protected
   */
  _trapFocus() {
    this.focusTrap && (this.__ariaModalController.showModal(), this.__focusTrapController.trapFocus(this.$.overlay));
  }
  /**
   * Returns true if focus is still inside the overlay or on the body element,
   * otherwise false.
   *
   * Focus shouldn't be restored if it's been moved elsewhere by another
   * component or as a result of a user interaction e.g. the user clicked
   * on a button outside the overlay while the overlay was open.
   *
   * @protected
   * @return {boolean}
   */
  _shouldRestoreFocus() {
    const e = jo();
    return e === document.body || this._deepContains(e);
  }
  /**
   * Returns true if the overlay contains the given node,
   * including those within shadow DOM trees.
   *
   * @param {Node} node
   * @return {boolean}
   * @protected
   */
  _deepContains(e) {
    if (this.contains(e))
      return !0;
    let t = e;
    const i = e.ownerDocument;
    for (; t && t !== i && t !== this; )
      t = t.parentNode || t.host;
    return t === this;
  }
};
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const cn = () => Array.from(document.body.children).filter((s) => s instanceof HTMLElement && s._hasOverlayStackMixin && !s.hasAttribute("closing")).sort((s, n) => s.__zIndex - n.__zIndex || 0), np = (s) => s === cn().pop(), op = (s) => class extends s {
  constructor() {
    super(), this._hasOverlayStackMixin = !0;
  }
  /**
   * Returns true if this is the last one in the opened overlays stack.
   *
   * @return {boolean}
   * @protected
   */
  get _last() {
    return np(this);
  }
  /**
   * Brings the overlay as visually the frontmost one.
   */
  bringToFront() {
    let e = "";
    const t = cn().filter((i) => i !== this).pop();
    t && (e = t.__zIndex + 1), this.style.zIndex = e, this.__zIndex = e || parseFloat(getComputedStyle(this).zIndex);
  }
  /** @protected */
  _enterModalState() {
    document.body.style.pointerEvents !== "none" && (this._previousDocumentPointerEvents = document.body.style.pointerEvents, document.body.style.pointerEvents = "none"), cn().forEach((e) => {
      e !== this && e.$.overlay && (e.$.overlay.style.pointerEvents = "none");
    });
  }
  /** @protected */
  _exitModalState() {
    this._previousDocumentPointerEvents !== void 0 && (document.body.style.pointerEvents = this._previousDocumentPointerEvents, delete this._previousDocumentPointerEvents);
    const e = cn();
    let t;
    for (; (t = e.pop()) && !(t !== this && (t.$.overlay && t.$.overlay.style.removeProperty("pointer-events"), !t.modeless)); )
      ;
  }
};
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const sp = (s) => class extends rp(op(s)) {
  static get properties() {
    return {
      /**
       * When true, the overlay is visible and attached to body.
       */
      opened: {
        type: Boolean,
        notify: !0,
        observer: "_openedChanged",
        reflectToAttribute: !0
      },
      /**
       * Owner element passed with renderer function
       * @type {HTMLElement}
       */
      owner: {
        type: Object
      },
      /**
       * Object with properties that is passed to `renderer` function
       */
      model: {
        type: Object
      },
      /**
       * Custom function for rendering the content of the overlay.
       * Receives three arguments:
       *
       * - `root` The root container DOM element. Append your content to it.
       * - `owner` The host element of the renderer function.
       * - `model` The object with the properties related with rendering.
       * @type {OverlayRenderer | null | undefined}
       */
      renderer: {
        type: Object
      },
      /**
       * When true the overlay won't disable the main content, showing
       * it doesn't change the functionality of the user interface.
       * @type {boolean}
       */
      modeless: {
        type: Boolean,
        value: !1,
        reflectToAttribute: !0,
        observer: "_modelessChanged"
      },
      /**
       * When set to true, the overlay is hidden. This also closes the overlay
       * immediately in case there is a closing animation in progress.
       * @type {boolean}
       */
      hidden: {
        type: Boolean,
        reflectToAttribute: !0,
        observer: "_hiddenChanged"
      },
      /**
       * When true the overlay has backdrop on top of content when opened.
       * @type {boolean}
       */
      withBackdrop: {
        type: Boolean,
        value: !1,
        reflectToAttribute: !0
      }
    };
  }
  static get observers() {
    return ["_rendererOrDataChanged(renderer, owner, model, opened)"];
  }
  constructor() {
    super(), this._boundMouseDownListener = this._mouseDownListener.bind(this), this._boundMouseUpListener = this._mouseUpListener.bind(this), this._boundOutsideClickListener = this._outsideClickListener.bind(this), this._boundKeydownListener = this._keydownListener.bind(this), Ko && (this._boundIosResizeListener = () => this._detectIosNavbar());
  }
  /** @protected */
  ready() {
    super.ready(), this.addEventListener("click", () => {
    }), this.$.backdrop.addEventListener("click", () => {
    }), this.addEventListener("mouseup", () => {
      document.activeElement === document.body && this.$.overlay.getAttribute("tabindex") === "0" && this.$.overlay.focus();
    });
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback(), this._boundIosResizeListener && (this._detectIosNavbar(), window.addEventListener("resize", this._boundIosResizeListener));
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback(), this._boundIosResizeListener && window.removeEventListener("resize", this._boundIosResizeListener);
  }
  /**
   * Requests an update for the content of the overlay.
   * While performing the update, it invokes the renderer passed in the `renderer` property.
   *
   * It is not guaranteed that the update happens immediately (synchronously) after it is requested.
   */
  requestContentUpdate() {
    this.renderer && this.renderer.call(this.owner, this, this.owner, this.model);
  }
  /**
   * @param {Event=} sourceEvent
   */
  close(e) {
    const t = new CustomEvent("vaadin-overlay-close", {
      bubbles: !0,
      cancelable: !0,
      detail: { sourceEvent: e }
    });
    this.dispatchEvent(t), t.defaultPrevented || (this.opened = !1);
  }
  /** @private */
  _detectIosNavbar() {
    if (!this.opened)
      return;
    const e = window.innerHeight, i = window.innerWidth > e, r = document.documentElement.clientHeight;
    i && r > e ? this.style.setProperty("--vaadin-overlay-viewport-bottom", `${r - e}px`) : this.style.setProperty("--vaadin-overlay-viewport-bottom", "0");
  }
  /** @private */
  _addGlobalListeners() {
    document.addEventListener("mousedown", this._boundMouseDownListener), document.addEventListener("mouseup", this._boundMouseUpListener), document.documentElement.addEventListener("click", this._boundOutsideClickListener, !0);
  }
  /** @private */
  _removeGlobalListeners() {
    document.removeEventListener("mousedown", this._boundMouseDownListener), document.removeEventListener("mouseup", this._boundMouseUpListener), document.documentElement.removeEventListener("click", this._boundOutsideClickListener, !0);
  }
  /** @private */
  _rendererOrDataChanged(e, t, i, r) {
    const o = this._oldOwner !== t || this._oldModel !== i;
    this._oldModel = i, this._oldOwner = t;
    const l = this._oldRenderer !== e, a = this._oldRenderer !== void 0;
    this._oldRenderer = e;
    const c = this._oldOpened !== r;
    this._oldOpened = r, l && a && (this.innerHTML = "", delete this._$litPart$), r && e && (l || c || o) && this.requestContentUpdate();
  }
  /** @private */
  _modelessChanged(e) {
    e ? (this._removeGlobalListeners(), this._exitModalState()) : this.opened && (this._addGlobalListeners(), this._enterModalState());
  }
  /** @private */
  _openedChanged(e, t) {
    e ? (this._saveFocus(), this._animatedOpening(), Jf(this, () => {
      this._trapFocus();
      const i = new CustomEvent("vaadin-overlay-open", { bubbles: !0 });
      this.dispatchEvent(i);
    }), document.addEventListener("keydown", this._boundKeydownListener), this.modeless || this._addGlobalListeners()) : t && (this._resetFocus(), this._animatedClosing(), document.removeEventListener("keydown", this._boundKeydownListener), this.modeless || this._removeGlobalListeners());
  }
  /** @private */
  _hiddenChanged(e) {
    e && this.hasAttribute("closing") && this._flushAnimation("closing");
  }
  /**
   * @return {boolean}
   * @private
   */
  _shouldAnimate() {
    const e = getComputedStyle(this), t = e.getPropertyValue("animation-name");
    return !(e.getPropertyValue("display") === "none") && t && t !== "none";
  }
  /**
   * @param {string} type
   * @param {Function} callback
   * @private
   */
  _enqueueAnimation(e, t) {
    const i = `__${e}Handler`, r = (o) => {
      o && o.target !== this || (t(), this.removeEventListener("animationend", r), delete this[i]);
    };
    this[i] = r, this.addEventListener("animationend", r);
  }
  /**
   * @param {string} type
   * @protected
   */
  _flushAnimation(e) {
    const t = `__${e}Handler`;
    typeof this[t] == "function" && this[t]();
  }
  /** @private */
  _animatedOpening() {
    this.parentNode === document.body && this.hasAttribute("closing") && this._flushAnimation("closing"), this._attachOverlay(), this.modeless || this._enterModalState(), this.setAttribute("opening", ""), this._shouldAnimate() ? this._enqueueAnimation("opening", () => {
      this._finishOpening();
    }) : this._finishOpening();
  }
  /** @private */
  _attachOverlay() {
    this._placeholder = document.createComment("vaadin-overlay-placeholder"), this.parentNode.insertBefore(this._placeholder, this), document.body.appendChild(this), this.bringToFront();
  }
  /** @private */
  _finishOpening() {
    this.removeAttribute("opening");
  }
  /** @private */
  _finishClosing() {
    this._detachOverlay(), this.$.overlay.style.removeProperty("pointer-events"), this.removeAttribute("closing"), this.dispatchEvent(new CustomEvent("vaadin-overlay-closed"));
  }
  /** @private */
  _animatedClosing() {
    this.hasAttribute("opening") && this._flushAnimation("opening"), this._placeholder && (this._exitModalState(), this.setAttribute("closing", ""), this.dispatchEvent(new CustomEvent("vaadin-overlay-closing")), this._shouldAnimate() ? this._enqueueAnimation("closing", () => {
      this._finishClosing();
    }) : this._finishClosing());
  }
  /** @private */
  _detachOverlay() {
    this._placeholder.parentNode.insertBefore(this, this._placeholder), this._placeholder.parentNode.removeChild(this._placeholder);
  }
  /** @private */
  _mouseDownListener(e) {
    this._mouseDownInside = e.composedPath().indexOf(this.$.overlay) >= 0;
  }
  /** @private */
  _mouseUpListener(e) {
    this._mouseUpInside = e.composedPath().indexOf(this.$.overlay) >= 0;
  }
  /**
   * Whether to close the overlay on outside click or not.
   * Override this method to customize the closing logic.
   *
   * @param {Event} _event
   * @return {boolean}
   * @protected
   */
  _shouldCloseOnOutsideClick(e) {
    return this._last;
  }
  /**
   * Outside click listener used in capture phase to close the overlay before
   * propagating the event to the listener on the element that triggered it.
   * Otherwise, calling `open()` would result in closing and re-opening.
   *
   * @private
   */
  _outsideClickListener(e) {
    if (e.composedPath().includes(this.$.overlay) || this._mouseDownInside || this._mouseUpInside) {
      this._mouseDownInside = !1, this._mouseUpInside = !1;
      return;
    }
    if (!this._shouldCloseOnOutsideClick(e))
      return;
    const t = new CustomEvent("vaadin-overlay-outside-click", {
      bubbles: !0,
      cancelable: !0,
      detail: { sourceEvent: e }
    });
    this.dispatchEvent(t), this.opened && !t.defaultPrevented && this.close(e);
  }
  /**
   * Listener used to close whe overlay on Escape press, if it is the last one.
   * @private
   */
  _keydownListener(e) {
    if (this._last && !(this.modeless && !e.composedPath().includes(this.$.overlay)) && e.key === "Escape") {
      const t = new CustomEvent("vaadin-overlay-escape-press", {
        bubbles: !0,
        cancelable: !0,
        detail: { sourceEvent: e }
      });
      this.dispatchEvent(t), this.opened && !t.defaultPrevented && this.close(e);
    }
  }
};
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const ap = Ee`
  :host {
    z-index: 200;
    position: fixed;

    /* Despite of what the names say, <vaadin-overlay> is just a container
          for position/sizing/alignment. The actual overlay is the overlay part. */

    /* Default position constraints: the entire viewport. Note: themes can
          override this to introduce gaps between the overlay and the viewport. */
    inset: 0;
    bottom: var(--vaadin-overlay-viewport-bottom);

    /* Use flexbox alignment for the overlay part. */
    display: flex;
    flex-direction: column; /* makes dropdowns sizing easier */
    /* Align to center by default. */
    align-items: center;
    justify-content: center;

    /* Allow centering when max-width/max-height applies. */
    margin: auto;

    /* The host is not clickable, only the overlay part is. */
    pointer-events: none;

    /* Remove tap highlight on touch devices. */
    -webkit-tap-highlight-color: transparent;

    /* CSS API for host */
    --vaadin-overlay-viewport-bottom: 0;
  }

  :host([hidden]),
  :host(:not([opened]):not([closing])),
  :host(:not([opened]):not([closing])) [part='overlay'] {
    display: none !important;
  }

  [part='overlay'] {
    -webkit-overflow-scrolling: touch;
    overflow: auto;
    pointer-events: auto;

    /* Prevent overflowing the host */
    max-width: 100%;
    box-sizing: border-box;

    -webkit-tap-highlight-color: initial; /* reenable tap highlight inside */
  }

  [part='backdrop'] {
    z-index: -1;
    content: '';
    background: rgba(0, 0, 0, 0.5);
    position: fixed;
    inset: 0;
    pointer-events: auto;
  }
`;
/**
 * @license
 * Copyright (c) 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function lp(s, n) {
  let e = null;
  const t = document.documentElement;
  function i() {
    e && e.disconnect(), e = null;
  }
  function r(o = !1, l = 1) {
    i();
    const { left: a, top: c, width: d, height: f } = s.getBoundingClientRect();
    if (o || n(), !d || !f)
      return;
    const p = Math.floor(c), b = Math.floor(t.clientWidth - (a + d)), x = Math.floor(t.clientHeight - (c + f)), T = Math.floor(a), z = {
      rootMargin: `${-p}px ${-b}px ${-x}px ${-T}px`,
      threshold: Math.max(0, Math.min(1, l)) || 1
    };
    let W = !0;
    function j(oe) {
      let ae = oe[0].intersectionRatio;
      if (ae !== l) {
        if (!W)
          return r();
        ae === 0 && (ae = 1e-7), r(!1, ae);
      }
      W = !1;
    }
    e = new IntersectionObserver(j, z), e.observe(s);
  }
  return r(!0), i;
}
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ao = {
  start: "top",
  end: "bottom"
}, ko = {
  start: "left",
  end: "right"
}, Xa = new ResizeObserver((s) => {
  setTimeout(() => {
    s.forEach((n) => {
      n.target.__overlay && n.target.__overlay._updatePosition();
    });
  });
}), cp = (s) => class extends s {
  static get properties() {
    return {
      /**
       * The element next to which this overlay should be aligned.
       * The position of the overlay relative to the positionTarget can be adjusted
       * with properties `horizontalAlign`, `verticalAlign`, `noHorizontalOverlap`
       * and `noVerticalOverlap`.
       */
      positionTarget: {
        type: Object,
        value: null,
        sync: !0
      },
      /**
       * When `positionTarget` is set, this property defines whether to align the overlay's
       * left or right side to the target element by default.
       * Possible values are `start` and `end`.
       * RTL is taken into account when interpreting the value.
       * The overlay is automatically flipped to the opposite side when it doesn't fit into
       * the default side defined by this property.
       *
       * @attr {start|end} horizontal-align
       */
      horizontalAlign: {
        type: String,
        value: "start",
        sync: !0
      },
      /**
       * When `positionTarget` is set, this property defines whether to align the overlay's
       * top or bottom side to the target element by default.
       * Possible values are `top` and `bottom`.
       * The overlay is automatically flipped to the opposite side when it doesn't fit into
       * the default side defined by this property.
       *
       * @attr {top|bottom} vertical-align
       */
      verticalAlign: {
        type: String,
        value: "top",
        sync: !0
      },
      /**
       * When `positionTarget` is set, this property defines whether the overlay should overlap
       * the target element in the x-axis, or be positioned right next to it.
       *
       * @attr {boolean} no-horizontal-overlap
       */
      noHorizontalOverlap: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /**
       * When `positionTarget` is set, this property defines whether the overlay should overlap
       * the target element in the y-axis, or be positioned right above/below it.
       *
       * @attr {boolean} no-vertical-overlap
       */
      noVerticalOverlap: {
        type: Boolean,
        value: !1,
        sync: !0
      },
      /**
       * If the overlay content has no intrinsic height, this property can be used to set
       * the minimum vertical space (in pixels) required by the overlay. Setting a value to
       * the property effectively disables the content measurement in favor of using this
       * fixed value for determining the open direction.
       *
       * @attr {number} required-vertical-space
       */
      requiredVerticalSpace: {
        type: Number,
        value: 0,
        sync: !0
      }
    };
  }
  static get observers() {
    return [
      "__positionSettingsChanged(horizontalAlign, verticalAlign, noHorizontalOverlap, noVerticalOverlap, requiredVerticalSpace)",
      "__overlayOpenedChanged(opened, positionTarget)"
    ];
  }
  constructor() {
    super(), this.__onScroll = this.__onScroll.bind(this), this._updatePosition = this._updatePosition.bind(this);
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback(), this.opened && this.__addUpdatePositionEventListeners();
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback(), this.__removeUpdatePositionEventListeners();
  }
  /** @private */
  __addUpdatePositionEventListeners() {
    window.visualViewport.addEventListener("resize", this._updatePosition), window.visualViewport.addEventListener("scroll", this.__onScroll, !0), this.__positionTargetAncestorRootNodes = uu(this.positionTarget), this.__positionTargetAncestorRootNodes.forEach((e) => {
      e.addEventListener("scroll", this.__onScroll, !0);
    }), this.positionTarget && (this.__observePositionTargetMove = lp(this.positionTarget, () => {
      this._updatePosition();
    }));
  }
  /** @private */
  __removeUpdatePositionEventListeners() {
    window.visualViewport.removeEventListener("resize", this._updatePosition), window.visualViewport.removeEventListener("scroll", this.__onScroll, !0), this.__positionTargetAncestorRootNodes && (this.__positionTargetAncestorRootNodes.forEach((e) => {
      e.removeEventListener("scroll", this.__onScroll, !0);
    }), this.__positionTargetAncestorRootNodes = null), this.__observePositionTargetMove && (this.__observePositionTargetMove(), this.__observePositionTargetMove = null);
  }
  /** @private */
  __overlayOpenedChanged(e, t) {
    if (this.__removeUpdatePositionEventListeners(), t && (t.__overlay = null, Xa.unobserve(t), e && (this.__addUpdatePositionEventListeners(), t.__overlay = this, Xa.observe(t))), e) {
      const i = getComputedStyle(this);
      this.__margins || (this.__margins = {}, ["top", "bottom", "left", "right"].forEach((r) => {
        this.__margins[r] = parseInt(i[r], 10);
      })), this._updatePosition(), requestAnimationFrame(() => this._updatePosition());
    }
  }
  __positionSettingsChanged() {
    this._updatePosition();
  }
  /** @private */
  __onScroll(e) {
    e.target instanceof Node && this.contains(e.target) || this._updatePosition();
  }
  _updatePosition() {
    if (!this.positionTarget || !this.opened || !this.__margins)
      return;
    const e = this.positionTarget.getBoundingClientRect();
    if (e.width === 0 && e.height === 0 && this.opened) {
      this.opened = !1;
      return;
    }
    const t = this.__shouldAlignStartVertically(e);
    this.style.justifyContent = t ? "flex-start" : "flex-end";
    const i = this.__isRTL, r = this.__shouldAlignStartHorizontally(e, i), o = !i && r || i && !r;
    this.style.alignItems = o ? "flex-start" : "flex-end";
    const l = this.getBoundingClientRect(), a = this.__calculatePositionInOneDimension(
      e,
      l,
      this.noVerticalOverlap,
      Ao,
      this,
      t
    ), c = this.__calculatePositionInOneDimension(
      e,
      l,
      this.noHorizontalOverlap,
      ko,
      this,
      r
    );
    Object.assign(this.style, a, c), this.toggleAttribute("bottom-aligned", !t), this.toggleAttribute("top-aligned", t), this.toggleAttribute("end-aligned", !o), this.toggleAttribute("start-aligned", o);
  }
  __shouldAlignStartHorizontally(e, t) {
    const i = Math.max(this.__oldContentWidth || 0, this.$.overlay.offsetWidth);
    this.__oldContentWidth = this.$.overlay.offsetWidth;
    const r = Math.min(window.innerWidth, document.documentElement.clientWidth), o = !t && this.horizontalAlign === "start" || t && this.horizontalAlign === "end";
    return this.__shouldAlignStart(
      e,
      i,
      r,
      this.__margins,
      o,
      this.noHorizontalOverlap,
      ko
    );
  }
  __shouldAlignStartVertically(e) {
    const t = this.requiredVerticalSpace || Math.max(this.__oldContentHeight || 0, this.$.overlay.offsetHeight);
    this.__oldContentHeight = this.$.overlay.offsetHeight;
    const i = Math.min(window.innerHeight, document.documentElement.clientHeight), r = this.verticalAlign === "top";
    return this.__shouldAlignStart(
      e,
      t,
      i,
      this.__margins,
      r,
      this.noVerticalOverlap,
      Ao
    );
  }
  // eslint-disable-next-line @typescript-eslint/max-params
  __shouldAlignStart(e, t, i, r, o, l, a) {
    const c = i - e[l ? a.end : a.start] - r[a.end], d = e[l ? a.start : a.end] - r[a.start], f = o ? c : d, b = f > (o ? d : c) || f > t;
    return o === b;
  }
  /**
   * Returns an adjusted value after resizing the browser window,
   * to avoid wrong calculations when e.g. previously set `bottom`
   * CSS property value is larger than the updated viewport height.
   * See https://github.com/vaadin/web-components/issues/4604
   */
  __adjustBottomProperty(e, t, i) {
    let r;
    if (e === t.end) {
      if (t.end === Ao.end) {
        const o = Math.min(window.innerHeight, document.documentElement.clientHeight);
        if (i > o && this.__oldViewportHeight) {
          const l = this.__oldViewportHeight - o;
          r = i - l;
        }
        this.__oldViewportHeight = o;
      }
      if (t.end === ko.end) {
        const o = Math.min(window.innerWidth, document.documentElement.clientWidth);
        if (i > o && this.__oldViewportWidth) {
          const l = this.__oldViewportWidth - o;
          r = i - l;
        }
        this.__oldViewportWidth = o;
      }
    }
    return r;
  }
  /**
   * Returns an object with CSS position properties to set,
   * e.g. { top: "100px" }
   */
  // eslint-disable-next-line @typescript-eslint/max-params
  __calculatePositionInOneDimension(e, t, i, r, o, l) {
    const a = l ? r.start : r.end, c = l ? r.end : r.start, d = parseFloat(o.style[a] || getComputedStyle(o)[a]), f = this.__adjustBottomProperty(a, r, d), p = t[l ? r.start : r.end] - e[i === l ? r.end : r.start], b = f ? `${f}px` : `${d + p * (l ? -1 : 1)}px`;
    return {
      [a]: b,
      [c]: ""
    };
  }
};
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const hp = (s) => class extends cp(s) {
  static get observers() {
    return ["_setOverlayWidth(positionTarget, opened)"];
  }
  constructor() {
    super(), this.requiredVerticalSpace = 200;
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    const e = this._comboBox, t = e && e.getAttribute("dir");
    t && this.setAttribute("dir", t);
  }
  /**
   * Override method inherited from `Overlay`
   * to not close on position target click.
   *
   * @param {Event} event
   * @return {boolean}
   * @protected
   */
  _shouldCloseOnOutsideClick(e) {
    const t = e.composedPath();
    return !t.includes(this.positionTarget) && !t.includes(this);
  }
  /**
   * @protected
   * @override
   */
  _mouseDownListener(e) {
    super._mouseDownListener(e), this._shouldCloseOnOutsideClick(e) && !gs(e.composedPath()[0]) && e.preventDefault();
  }
  /** @protected */
  _updateOverlayWidth() {
    const e = this.localName;
    this.style.setProperty(`--_${e}-default-width`, `${this.positionTarget.clientWidth}px`);
    const t = getComputedStyle(this._comboBox).getPropertyValue(`--${e}-width`);
    t === "" ? this.style.removeProperty(`--${e}-width`) : this.style.setProperty(`--${e}-width`, t);
  }
  /** @private */
  _setOverlayWidth(e, t) {
    e && t && (this._updateOverlayWidth(), this._updatePosition());
  }
};
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const dp = Ee`
  #overlay {
    width: var(--vaadin-combo-box-overlay-width, var(--_vaadin-combo-box-overlay-default-width, auto));
  }

  [part='content'] {
    display: flex;
    flex-direction: column;
    height: 100%;
  }
`;
Ze("vaadin-combo-box-overlay", [ap, dp], {
  moduleId: "vaadin-combo-box-overlay-styles"
});
class up extends hp(sp(Tr(Cr(yi)))) {
  static get is() {
    return "vaadin-combo-box-overlay";
  }
  static get template() {
    return Wi`
      <div id="backdrop" part="backdrop" hidden></div>
      <div part="overlay" id="overlay">
        <div part="loader"></div>
        <div part="content" id="content"><slot></slot></div>
      </div>
    `;
  }
}
Jt(up);
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Vi = class {
  toString() {
    return "";
  }
};
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const fp = (s) => class extends s {
  static get properties() {
    return {
      /**
       * A full set of items to filter the visible options from.
       * Set to an empty array when combo-box is not opened.
       */
      items: {
        type: Array,
        sync: !0,
        observer: "__itemsChanged"
      },
      /**
       * Index of an item that has focus outline and is scrolled into view.
       * The actual focus still remains in the input field.
       */
      focusedIndex: {
        type: Number,
        sync: !0,
        observer: "__focusedIndexChanged"
      },
      /**
       * Set to true while combo-box fetches new page from the data provider.
       */
      loading: {
        type: Boolean,
        sync: !0,
        observer: "__loadingChanged"
      },
      /**
       * Whether the combo-box is currently opened or not. If set to false,
       * calling `scrollIntoView` does not have any effect.
       */
      opened: {
        type: Boolean,
        sync: !0,
        observer: "__openedChanged"
      },
      /**
       * The selected item from the `items` array.
       */
      selectedItem: {
        type: Object,
        sync: !0,
        observer: "__selectedItemChanged"
      },
      /**
       * A function used to generate CSS class names for dropdown
       * items based on the item. The return value should be the
       * generated class name as a string, or multiple class names
       * separated by whitespace characters.
       */
      itemClassNameGenerator: {
        type: Object,
        observer: "__itemClassNameGeneratorChanged"
      },
      /**
       * Path for the id of the item, used to detect whether the item is selected.
       */
      itemIdPath: {
        type: String
      },
      /**
       * Reference to the owner (combo-box owner), used by the item elements.
       */
      owner: {
        type: Object
      },
      /**
       * Function used to set a label for every combo-box item.
       */
      getItemLabel: {
        type: Object
      },
      /**
       * Function used to render the content of every combo-box item.
       */
      renderer: {
        type: Object,
        sync: !0,
        observer: "__rendererChanged"
      },
      /**
       * Used to propagate the `theme` attribute from the host element.
       */
      theme: {
        type: String
      }
    };
  }
  constructor() {
    super(), this.__boundOnItemClick = this.__onItemClick.bind(this);
  }
  /** @private */
  get _viewportTotalPaddingBottom() {
    if (this._cachedViewportTotalPaddingBottom === void 0) {
      const e = window.getComputedStyle(this.$.selector);
      this._cachedViewportTotalPaddingBottom = [e.paddingBottom, e.borderBottomWidth].map((t) => parseInt(t, 10)).reduce((t, i) => t + i);
    }
    return this._cachedViewportTotalPaddingBottom;
  }
  /** @protected */
  ready() {
    super.ready(), this.setAttribute("role", "listbox"), this.id = `${this.localName}-${Yl()}`, this.__hostTagName = this.constructor.is.replace("-scroller", ""), this.addEventListener("click", (e) => e.stopPropagation()), this.__patchWheelOverScrolling();
  }
  /**
   * Updates the virtualizer's size and items.
   */
  requestContentUpdate() {
    this.__virtualizer && (this.items && (this.__virtualizer.size = this.items.length), this.opened && this.__virtualizer.update());
  }
  /**
   * Scrolls an item at given index into view and adjusts `scrollTop`
   * so that the element gets fully visible on Arrow Down key press.
   * @param {number} index
   */
  scrollIntoView(e) {
    if (!this.__virtualizer || !(this.opened && e >= 0))
      return;
    const t = this._visibleItemsCount();
    let i = e;
    e > this.__virtualizer.lastVisibleIndex - 1 ? (this.__virtualizer.scrollToIndex(e), i = e - t + 1) : e > this.__virtualizer.firstVisibleIndex && (i = this.__virtualizer.firstVisibleIndex), this.__virtualizer.scrollToIndex(Math.max(0, i));
    const r = [...this.children].find(
      (c) => !c.hidden && c.index === this.__virtualizer.lastVisibleIndex
    );
    if (!r || e !== r.index)
      return;
    const o = r.getBoundingClientRect(), l = this.getBoundingClientRect(), a = o.bottom - l.bottom + this._viewportTotalPaddingBottom;
    a > 0 && (this.scrollTop += a);
  }
  /**
   * @param {string | object} item
   * @param {string | object} selectedItem
   * @param {string} itemIdPath
   * @protected
   */
  _isItemSelected(e, t, i) {
    return e instanceof Vi ? !1 : i && e !== void 0 && t !== void 0 ? _i(i, e) === _i(i, t) : e === t;
  }
  /** @private */
  __initVirtualizer() {
    this.__virtualizer = new Jl({
      createElements: this.__createElements.bind(this),
      updateElement: this._updateElement.bind(this),
      elementsContainer: this,
      scrollTarget: this,
      scrollContainer: this.$.selector,
      reorderElements: !0
    });
  }
  /** @private */
  __itemsChanged(e) {
    e && this.__virtualizer && this.requestContentUpdate();
  }
  /** @private */
  __loadingChanged() {
    this.requestContentUpdate();
  }
  /** @private */
  __openedChanged(e) {
    e && (this.__virtualizer || this.__initVirtualizer(), this.requestContentUpdate());
  }
  /** @private */
  __selectedItemChanged() {
    this.requestContentUpdate();
  }
  /** @private */
  __itemClassNameGeneratorChanged(e, t) {
    (e || t) && this.requestContentUpdate();
  }
  /** @private */
  __focusedIndexChanged(e, t) {
    e !== t && this.requestContentUpdate(), e >= 0 && !this.loading && this.scrollIntoView(e);
  }
  /** @private */
  __rendererChanged(e, t) {
    (e || t) && this.requestContentUpdate();
  }
  /** @private */
  __createElements(e) {
    return [...Array(e)].map(() => {
      const t = document.createElement(`${this.__hostTagName}-item`);
      return t.addEventListener("click", this.__boundOnItemClick), t.tabIndex = "-1", t.style.width = "100%", t;
    });
  }
  /**
   * @param {HTMLElement} el
   * @param {number} index
   * @protected
   */
  _updateElement(e, t) {
    const i = this.items[t], r = this.focusedIndex, o = this._isItemSelected(i, this.selectedItem, this.itemIdPath);
    e.setProperties({
      item: i,
      index: t,
      label: this.getItemLabel(i),
      selected: o,
      renderer: this.renderer,
      focused: !this.loading && r === t
    }), typeof this.itemClassNameGenerator == "function" ? e.className = this.itemClassNameGenerator(i) : e.className !== "" && (e.className = ""), e.performUpdate && !e.hasUpdated && e.performUpdate(), e.id = `${this.__hostTagName}-item-${t}`, e.setAttribute("role", t !== void 0 ? "option" : !1), e.setAttribute("aria-selected", o.toString()), e.setAttribute("aria-posinset", t + 1), e.setAttribute("aria-setsize", this.items.length), this.theme ? e.setAttribute("theme", this.theme) : e.removeAttribute("theme"), i instanceof Vi && this.__requestItemByIndex(t);
  }
  /** @private */
  __onItemClick(e) {
    this.dispatchEvent(new CustomEvent("selection-changed", { detail: { item: e.currentTarget.item } }));
  }
  /**
   * We want to prevent the kinetic scrolling energy from being transferred from the overlay contents over to the parent.
   * Further improvement ideas: after the contents have been scrolled to the top or bottom and scrolling has stopped, it could allow
   * scrolling the parent similarly to touch scrolling.
   * @private
   */
  __patchWheelOverScrolling() {
    this.$.selector.addEventListener("wheel", (e) => {
      const t = this.scrollTop === 0, i = this.scrollHeight - this.scrollTop - this.clientHeight <= 1;
      (t && e.deltaY < 0 || i && e.deltaY > 0) && e.preventDefault();
    });
  }
  /**
   * Dispatches an `index-requested` event for the given index to notify
   * the data provider that it should start loading the page containing the requested index.
   *
   * The event is dispatched asynchronously to prevent an immediate page request and therefore
   * a possible infinite recursion in case the data provider implements page request cancelation logic
   * by invoking data provider page callbacks with an empty array.
   * The infinite recursion may occur otherwise since invoking a data provider page callback with an empty array
   * triggers a synchronous scroller update and, if the callback corresponds to the currently visible page,
   * the scroller will synchronously request the page again which may lead to looping in the end.
   * That was the case for the Flow counterpart:
   * https://github.com/vaadin/flow-components/issues/3553#issuecomment-1239344828
   * @private
   */
  __requestItemByIndex(e) {
    requestAnimationFrame(() => {
      this.dispatchEvent(
        new CustomEvent("index-requested", {
          detail: {
            index: e
          }
        })
      );
    });
  }
  /** @private */
  _visibleItemsCount() {
    return this.__virtualizer.scrollToIndex(this.__virtualizer.firstVisibleIndex), this.__virtualizer.size > 0 ? this.__virtualizer.lastVisibleIndex - this.__virtualizer.firstVisibleIndex + 1 : 0;
  }
};
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class pp extends fp(yi) {
  static get is() {
    return "vaadin-combo-box-scroller";
  }
  static get template() {
    return Wi`
      <style>
        :host {
          display: block;
          min-height: 1px;
          overflow: auto;

          /* Fixes item background from getting on top of scrollbars on Safari */
          transform: translate3d(0, 0, 0);

          /* Enable momentum scrolling on iOS */
          -webkit-overflow-scrolling: touch;

          /* Fixes scrollbar disappearing when 'Show scroll bars: Always' enabled in Safari */
          box-shadow: 0 0 0 white;
        }

        #selector {
          border-width: var(--_vaadin-combo-box-items-container-border-width);
          border-style: var(--_vaadin-combo-box-items-container-border-style);
          border-color: var(--_vaadin-combo-box-items-container-border-color, transparent);
          position: relative;
        }
      </style>
      <div id="selector">
        <slot></slot>
      </div>
    `;
  }
}
Jt(pp);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Io = /* @__PURE__ */ new WeakMap();
function gp(s) {
  return Io.has(s) || Io.set(s, /* @__PURE__ */ new Set()), Io.get(s);
}
function mp(s, n) {
  const e = document.createElement("style");
  e.textContent = s, n === document ? document.head.appendChild(e) : n.insertBefore(e, n.firstChild);
}
const _p = Je(
  (s) => class extends s {
    /**
     * List of styles to insert into root.
     * @protected
     */
    get slotStyles() {
      return {};
    }
    /** @protected */
    connectedCallback() {
      super.connectedCallback(), this.__applySlotStyles();
    }
    /** @private */
    __applySlotStyles() {
      const e = this.getRootNode(), t = gp(e);
      this.slotStyles.forEach((i) => {
        t.has(i) || (mp(i, e), t.add(i));
      });
    }
  }
);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ps = Je(
  (s) => class extends s {
    static get properties() {
      return {
        /**
         * A reference to the input element controlled by the mixin.
         * Any component implementing this mixin is expected to provide it
         * by using `this._setInputElement(input)` Polymer API.
         *
         * A typical case is using `InputController` that does this automatically.
         * However, the input element does not have to always be native <input>:
         * as an example, <vaadin-combo-box-light> accepts other components.
         *
         * @protected
         * @type {!HTMLElement}
         */
        inputElement: {
          type: Object,
          readOnly: !0,
          observer: "_inputElementChanged"
        },
        /**
         * String used to define input type.
         * @protected
         */
        type: {
          type: String,
          readOnly: !0
        },
        /**
         * The value of the field.
         */
        value: {
          type: String,
          value: "",
          observer: "_valueChanged",
          notify: !0,
          sync: !0
        },
        /**
         * Whether the input element has a non-empty value.
         *
         * @protected
         */
        _hasInputValue: {
          type: Boolean,
          value: !1,
          observer: "_hasInputValueChanged"
        }
      };
    }
    constructor() {
      super(), this._boundOnInput = this.__onInput.bind(this), this._boundOnChange = this._onChange.bind(this);
    }
    /**
     * Indicates whether the value is different from the default one.
     * Override if the `value` property has a type other than `string`.
     *
     * @protected
     */
    get _hasValue() {
      return this.value != null && this.value !== "";
    }
    /**
     * A property for accessing the input element's value.
     *
     * Override this getter if the property is different from the default `value` one.
     *
     * @protected
     * @return {string}
     */
    get _inputElementValueProperty() {
      return "value";
    }
    /**
     * The input element's value.
     *
     * @protected
     * @return {string}
     */
    get _inputElementValue() {
      return this.inputElement ? this.inputElement[this._inputElementValueProperty] : void 0;
    }
    /**
     * The input element's value.
     *
     * @protected
     */
    set _inputElementValue(e) {
      this.inputElement && (this.inputElement[this._inputElementValueProperty] = e);
    }
    /**
     * Clear the value of the field.
     */
    clear() {
      this._hasInputValue = !1, this.value = "", this._inputElementValue = "";
    }
    /**
     * Add event listeners to the input element instance.
     * Override this method to add custom listeners.
     * @param {!HTMLElement} input
     * @protected
     */
    _addInputListeners(e) {
      e.addEventListener("input", this._boundOnInput), e.addEventListener("change", this._boundOnChange);
    }
    /**
     * Remove event listeners from the input element instance.
     * @param {!HTMLElement} input
     * @protected
     */
    _removeInputListeners(e) {
      e.removeEventListener("input", this._boundOnInput), e.removeEventListener("change", this._boundOnChange);
    }
    /**
     * A method to forward the value property set on the field
     * programmatically back to the input element value.
     * Override this method to perform additional checks,
     * for example to skip this in certain conditions.
     * @param {string} value
     * @protected
     */
    _forwardInputValue(e) {
      this.inputElement && (this._inputElementValue = e ?? "");
    }
    /**
     * @param {HTMLElement | undefined} input
     * @param {HTMLElement | undefined} oldInput
     * @protected
     */
    _inputElementChanged(e, t) {
      e ? this._addInputListeners(e) : t && this._removeInputListeners(t);
    }
    /**
     * Observer to notify about the change of private property.
     *
     * @private
     */
    _hasInputValueChanged(e, t) {
      (e || t) && this.dispatchEvent(new CustomEvent("has-input-value-changed"));
    }
    /**
     * An input event listener used to update `_hasInputValue` property.
     * Do not override this method.
     *
     * @param {Event} event
     * @private
     */
    __onInput(e) {
      this._setHasInputValue(e), this._onInput(e);
    }
    /**
     * An input event listener used to update the field value.
     *
     * @param {Event} event
     * @protected
     */
    _onInput(e) {
      const t = e.composedPath()[0];
      this.__userInput = e.isTrusted, this.value = t.value, this.__userInput = !1;
    }
    /**
     * A change event listener.
     * Override this method with an actual implementation.
     * @param {Event} _event
     * @protected
     */
    _onChange(e) {
    }
    /**
     * Toggle the has-value attribute based on the value property.
     *
     * @param {boolean} hasValue
     * @protected
     */
    _toggleHasValue(e) {
      this.toggleAttribute("has-value", e);
    }
    /**
     * Observer called when a value property changes.
     * @param {string | undefined} newVal
     * @param {string | undefined} oldVal
     * @protected
     */
    _valueChanged(e, t) {
      this._toggleHasValue(this._hasValue), !(e === "" && t === void 0) && (this.__userInput || this._forwardInputValue(e));
    }
    /**
     * Sets the `_hasInputValue` property based on the `input` event.
     *
     * @param {InputEvent} event
     * @protected
     */
    _setHasInputValue(e) {
      const t = e.composedPath()[0];
      this._hasInputValue = t.value.length > 0;
    }
  }
);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const vp = (s) => class extends Ps(xs(s)) {
  static get properties() {
    return {
      /**
       * Set to true to display the clear icon which clears the input.
       *
       * It is up to the component to choose where to place the clear icon:
       * in the Shadow DOM or in the light DOM. In any way, a reference to
       * the clear icon element should be provided via the `clearElement` getter.
       *
       * @attr {boolean} clear-button-visible
       */
      clearButtonVisible: {
        type: Boolean,
        reflectToAttribute: !0,
        value: !1
      }
    };
  }
  /**
   * Any element extending this mixin is required to implement this getter.
   * It returns the reference to the clear button element.
   *
   * @protected
   * @return {Element | null | undefined}
   */
  get clearElement() {
    return null;
  }
  /** @protected */
  ready() {
    super.ready(), this.clearElement && (this.clearElement.addEventListener("mousedown", (e) => this._onClearButtonMouseDown(e)), this.clearElement.addEventListener("click", (e) => this._onClearButtonClick(e)));
  }
  /**
   * @param {Event} event
   * @protected
   */
  _onClearButtonClick(e) {
    e.preventDefault(), this._onClearAction();
  }
  /**
   * @param {MouseEvent} event
   * @protected
   */
  _onClearButtonMouseDown(e) {
    e.preventDefault(), br || this.inputElement.focus();
  }
  /**
   * Override an event listener inherited from `KeydownMixin` to clear on Esc.
   * Components that extend this mixin can prevent this behavior by overriding
   * this method without calling `super._onEscape` to provide custom logic.
   *
   * @param {KeyboardEvent} event
   * @protected
   * @override
   */
  _onEscape(e) {
    super._onEscape(e), this.clearButtonVisible && this.value && (e.stopPropagation(), this._onClearAction());
  }
  /**
   * Clears the value and dispatches `input` and `change` events
   * on the input element. This method should be called
   * when the clear action originates from the user.
   *
   * @protected
   */
  _onClearAction() {
    this._inputElementValue = "", this.inputElement.dispatchEvent(new Event("input", { bubbles: !0, composed: !0 })), this.inputElement.dispatchEvent(new Event("change", { bubbles: !0 }));
  }
};
/**
 * @license
 * Copyright (c) 2022 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Ss extends ms {
  constructor(n, e, t, i = {}) {
    super(n, e, t, { ...i, useUniqueId: !0 });
  }
  /**
   * Override to initialize the newly added custom node.
   *
   * @param {Node} node
   * @protected
   * @override
   */
  initCustomNode(n) {
    this.__updateNodeId(n), this.__notifyChange(n);
  }
  /**
   * Override to notify the controller host about removal of
   * the custom node, and to apply the default one if needed.
   *
   * @param {Node} _node
   * @protected
   * @override
   */
  teardownNode(n) {
    const e = this.getSlotChild();
    e && e !== this.defaultNode ? this.__notifyChange(e) : (this.restoreDefaultNode(), this.updateDefaultNode(this.node));
  }
  /**
   * Override method inherited from `SlotMixin`
   * to set ID attribute on the default node.
   *
   * @return {Node}
   * @protected
   * @override
   */
  attachDefaultNode() {
    const n = super.attachDefaultNode();
    return n && this.__updateNodeId(n), n;
  }
  /**
   * Override to restore default node when a custom one is removed.
   *
   * @protected
   */
  restoreDefaultNode() {
  }
  /**
   * Override to update default node text on property change.
   *
   * @param {Node} node
   * @protected
   */
  updateDefaultNode(n) {
    this.__notifyChange(n);
  }
  /**
   * Setup the mutation observer on the node to update ID and notify host.
   * Node doesn't get observed automatically until this method is called.
   *
   * @param {Node} node
   * @protected
   */
  observeNode(n) {
    this.__nodeObserver && this.__nodeObserver.disconnect(), this.__nodeObserver = new MutationObserver((e) => {
      e.forEach((t) => {
        const i = t.target, r = i === this.node;
        t.type === "attributes" ? r && this.__updateNodeId(i) : (r || i.parentElement === this.node) && this.__notifyChange(this.node);
      });
    }), this.__nodeObserver.observe(n, {
      attributes: !0,
      attributeFilter: ["id"],
      childList: !0,
      subtree: !0,
      characterData: !0
    });
  }
  /**
   * Returns true if a node is an HTML element with children,
   * or is a defined custom element, or has non-empty text.
   *
   * @param {Node} node
   * @return {boolean}
   * @private
   */
  __hasContent(n) {
    return n ? n.nodeType === Node.ELEMENT_NODE && (customElements.get(n.localName) || n.children.length > 0) || n.textContent && n.textContent.trim() !== "" : !1;
  }
  /**
   * Fire an event to notify the controller host about node changes.
   *
   * @param {Node} node
   * @private
   */
  __notifyChange(n) {
    this.dispatchEvent(
      new CustomEvent("slot-content-changed", {
        detail: { hasContent: this.__hasContent(n), node: n }
      })
    );
  }
  /**
   * Set default ID on the node in case it is an HTML element.
   *
   * @param {Node} node
   * @private
   */
  __updateNodeId(n) {
    const e = !this.nodes || n === this.nodes[0];
    n.nodeType === Node.ELEMENT_NODE && (!this.multiple || e) && !n.id && (n.id = this.defaultId);
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class yp extends Ss {
  constructor(n) {
    super(n, "error-message", "div");
  }
  /**
   * Set the error message element text content.
   *
   * @param {string} errorMessage
   */
  setErrorMessage(n) {
    this.errorMessage = n, this.updateDefaultNode(this.node);
  }
  /**
   * Set invalid state for detecting whether to show error message.
   *
   * @param {boolean} invalid
   */
  setInvalid(n) {
    this.invalid = n, this.updateDefaultNode(this.node);
  }
  /**
   * Override method inherited from `SlotController` to not run
   * initializer on the custom slotted node unnecessarily.
   *
   * @param {Node} node
   * @protected
   * @override
   */
  initAddedNode(n) {
    n !== this.defaultNode && this.initCustomNode(n);
  }
  /**
   * Override to initialize the newly added default error message.
   *
   * @param {Node} errorNode
   * @protected
   * @override
   */
  initNode(n) {
    this.updateDefaultNode(n);
  }
  /**
   * Override to initialize the newly added custom error message.
   *
   * @param {Node} errorNode
   * @protected
   * @override
   */
  initCustomNode(n) {
    n.textContent && !this.errorMessage && (this.errorMessage = n.textContent.trim()), super.initCustomNode(n);
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to restore the default error message element.
   *
   * @protected
   * @override
   */
  restoreDefaultNode() {
    this.attachDefaultNode();
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to update the error message text and hidden state.
   *
   * Note: unlike with other controllers, this method is
   * called for both default and custom error message.
   *
   * @param {Node | undefined} node
   * @protected
   * @override
   */
  updateDefaultNode(n) {
    const { errorMessage: e, invalid: t } = this, i = !!(t && e && e.trim() !== "");
    n && (n.textContent = i ? e : "", n.hidden = !i, i ? n.setAttribute("role", "alert") : n.removeAttribute("role")), super.updateDefaultNode(n);
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class bp extends Ss {
  constructor(n) {
    super(n, "helper", null);
  }
  /**
   * Set helper text based on corresponding host property.
   *
   * @param {string} helperText
   */
  setHelperText(n) {
    this.helperText = n, this.getSlotChild() || this.restoreDefaultNode(), this.node === this.defaultNode && this.updateDefaultNode(this.node);
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to create the default helper element lazily as needed.
   *
   * @param {Node | undefined} node
   * @protected
   * @override
   */
  restoreDefaultNode() {
    const { helperText: n } = this;
    if (n && n.trim() !== "") {
      this.tagName = "div";
      const e = this.attachDefaultNode();
      this.observeNode(e);
    }
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to update the default helper element text content.
   *
   * @param {Node | undefined} node
   * @protected
   * @override
   */
  updateDefaultNode(n) {
    n && (n.textContent = this.helperText), super.updateDefaultNode(n);
  }
  /**
   * Override to observe the newly added custom node.
   *
   * @param {Node} node
   * @protected
   * @override
   */
  initCustomNode(n) {
    super.initCustomNode(n), this.observeNode(n);
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class wp extends Ss {
  constructor(n) {
    super(n, "label", "label");
  }
  /**
   * Set label based on corresponding host property.
   *
   * @param {string} label
   */
  setLabel(n) {
    this.label = n, this.getSlotChild() || this.restoreDefaultNode(), this.node === this.defaultNode && this.updateDefaultNode(this.node);
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to restore and observe the default label element.
   *
   * @protected
   * @override
   */
  restoreDefaultNode() {
    const { label: n } = this;
    if (n && n.trim() !== "") {
      const e = this.attachDefaultNode();
      this.observeNode(e);
    }
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to update the default label element text content.
   *
   * @param {Node | undefined} node
   * @protected
   * @override
   */
  updateDefaultNode(n) {
    n && (n.textContent = this.label), super.updateDefaultNode(n);
  }
  /**
   * Override to observe the newly added custom node.
   *
   * @param {Node} node
   * @protected
   * @override
   */
  initCustomNode(n) {
    super.initCustomNode(n), this.observeNode(n);
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const xp = Je(
  (s) => class extends Er(s) {
    static get properties() {
      return {
        /**
         * The label text for the input node.
         * When no light dom defined via [slot=label], this value will be used.
         */
        label: {
          type: String,
          observer: "_labelChanged"
        }
      };
    }
    constructor() {
      super(), this._labelController = new wp(this), this._labelController.addEventListener("slot-content-changed", (e) => {
        this.toggleAttribute("has-label", e.detail.hasContent);
      });
    }
    /** @protected */
    get _labelId() {
      const e = this._labelNode;
      return e && e.id;
    }
    /** @protected */
    get _labelNode() {
      return this._labelController.node;
    }
    /** @protected */
    ready() {
      super.ready(), this.addController(this._labelController);
    }
    /** @protected */
    _labelChanged(e) {
      this._labelController.setLabel(e);
    }
  }
);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Rs = Je(
  (s) => class extends s {
    static get properties() {
      return {
        /**
         * Set to true when the field is invalid.
         */
        invalid: {
          type: Boolean,
          reflectToAttribute: !0,
          notify: !0,
          value: !1
        },
        /**
         * Specifies that the user must fill in a value.
         */
        required: {
          type: Boolean,
          reflectToAttribute: !0
        }
      };
    }
    /**
     * Validates the field and sets the `invalid` property based on the result.
     *
     * The method fires a `validated` event with the result of the validation.
     *
     * @return {boolean} True if the value is valid.
     */
    validate() {
      const e = this.checkValidity();
      return this._setInvalid(!e), this.dispatchEvent(new CustomEvent("validated", { detail: { valid: e } })), e;
    }
    /**
     * Returns true if the field value satisfies all constraints (if any).
     *
     * @return {boolean}
     */
    checkValidity() {
      return !this.required || !!this.value;
    }
    /**
     * @param {boolean} invalid
     * @protected
     */
    _setInvalid(e) {
      this._shouldSetInvalid(e) && (this.invalid = e);
    }
    /**
     * Override this method to define whether the given `invalid` state should be set.
     *
     * @param {boolean} _invalid
     * @return {boolean}
     * @protected
     */
    _shouldSetInvalid(e) {
      return !0;
    }
    /**
     * Fired whenever the field is validated.
     *
     * @event validated
     * @param {Object} detail
     * @param {boolean} detail.valid the result of the validation.
     */
  }
);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Cp = (s) => class extends Rs(xp(Er(s))) {
  static get properties() {
    return {
      /**
       * A target element to which ARIA attributes are set.
       * @protected
       */
      ariaTarget: {
        type: Object,
        observer: "_ariaTargetChanged"
      },
      /**
       * Error to show when the field is invalid.
       *
       * @attr {string} error-message
       */
      errorMessage: {
        type: String,
        observer: "_errorMessageChanged"
      },
      /**
       * String used for the helper text.
       * @attr {string} helper-text
       */
      helperText: {
        type: String,
        observer: "_helperTextChanged"
      },
      /**
       * String used to label the component to screen reader users.
       * @attr {string} accessible-name
       */
      accessibleName: {
        type: String,
        observer: "_accessibleNameChanged"
      },
      /**
       * Id of the element used as label of the component to screen reader users.
       * @attr {string} accessible-name-ref
       */
      accessibleNameRef: {
        type: String,
        observer: "_accessibleNameRefChanged"
      }
    };
  }
  static get observers() {
    return ["_invalidChanged(invalid)", "_requiredChanged(required)"];
  }
  constructor() {
    super(), this._fieldAriaController = new Cf(this), this._helperController = new bp(this), this._errorController = new yp(this), this._errorController.addEventListener("slot-content-changed", (e) => {
      this.toggleAttribute("has-error-message", e.detail.hasContent);
    }), this._labelController.addEventListener("slot-content-changed", (e) => {
      const { hasContent: t, node: i } = e.detail;
      this.__labelChanged(t, i);
    }), this._helperController.addEventListener("slot-content-changed", (e) => {
      const { hasContent: t, node: i } = e.detail;
      this.toggleAttribute("has-helper", t), this.__helperChanged(t, i);
    });
  }
  /**
   * @protected
   * @return {HTMLElement}
   */
  get _errorNode() {
    return this._errorController.node;
  }
  /**
   * @protected
   * @return {HTMLElement}
   */
  get _helperNode() {
    return this._helperController.node;
  }
  /** @protected */
  ready() {
    super.ready(), this.addController(this._fieldAriaController), this.addController(this._helperController), this.addController(this._errorController);
  }
  /** @private */
  __helperChanged(e, t) {
    e ? this._fieldAriaController.setHelperId(t.id) : this._fieldAriaController.setHelperId(null);
  }
  /** @protected */
  _accessibleNameChanged(e) {
    this._fieldAriaController.setAriaLabel(e);
  }
  /** @protected */
  _accessibleNameRefChanged(e) {
    this._fieldAriaController.setLabelId(e, !0);
  }
  /** @private */
  __labelChanged(e, t) {
    e ? this._fieldAriaController.setLabelId(t.id) : this._fieldAriaController.setLabelId(null);
  }
  /**
   * @param {string | null | undefined} errorMessage
   * @protected
   */
  _errorMessageChanged(e) {
    this._errorController.setErrorMessage(e);
  }
  /**
   * @param {string} helperText
   * @protected
   */
  _helperTextChanged(e) {
    this._helperController.setHelperText(e);
  }
  /**
   * @param {HTMLElement | null | undefined} target
   * @protected
   */
  _ariaTargetChanged(e) {
    e && this._fieldAriaController.setTarget(e);
  }
  /**
   * @param {boolean} required
   * @protected
   */
  _requiredChanged(e) {
    this._fieldAriaController.setRequired(e);
  }
  /**
   * @param {boolean} invalid
   * @protected
   */
  _invalidChanged(e) {
    this._errorController.setInvalid(e), setTimeout(() => {
      if (e) {
        const t = this._errorNode;
        this._fieldAriaController.setErrorId(t && t.id);
      } else
        this._fieldAriaController.setErrorId(null);
    });
  }
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Tp = Je(
  (s) => class extends s {
    static get properties() {
      return {
        /**
         * A target element to which attributes and properties are delegated.
         * @protected
         */
        stateTarget: {
          type: Object,
          observer: "_stateTargetChanged"
        }
      };
    }
    /**
     * An array of the host attributes to delegate to the target element.
     */
    static get delegateAttrs() {
      return [];
    }
    /**
     * An array of the host properties to delegate to the target element.
     */
    static get delegateProps() {
      return [];
    }
    /** @protected */
    ready() {
      super.ready(), this._createDelegateAttrsObserver(), this._createDelegatePropsObserver();
    }
    /** @protected */
    _stateTargetChanged(e) {
      e && (this._ensureAttrsDelegated(), this._ensurePropsDelegated());
    }
    /** @protected */
    _createDelegateAttrsObserver() {
      this._createMethodObserver(`_delegateAttrsChanged(${this.constructor.delegateAttrs.join(", ")})`);
    }
    /** @protected */
    _createDelegatePropsObserver() {
      this._createMethodObserver(`_delegatePropsChanged(${this.constructor.delegateProps.join(", ")})`);
    }
    /** @protected */
    _ensureAttrsDelegated() {
      this.constructor.delegateAttrs.forEach((e) => {
        this._delegateAttribute(e, this[e]);
      });
    }
    /** @protected */
    _ensurePropsDelegated() {
      this.constructor.delegateProps.forEach((e) => {
        this._delegateProperty(e, this[e]);
      });
    }
    /** @protected */
    _delegateAttrsChanged(...e) {
      this.constructor.delegateAttrs.forEach((t, i) => {
        this._delegateAttribute(t, e[i]);
      });
    }
    /** @protected */
    _delegatePropsChanged(...e) {
      this.constructor.delegateProps.forEach((t, i) => {
        this._delegateProperty(t, e[i]);
      });
    }
    /** @protected */
    _delegateAttribute(e, t) {
      this.stateTarget && (e === "invalid" && this._delegateAttribute("aria-invalid", t ? "true" : !1), typeof t == "boolean" ? this.stateTarget.toggleAttribute(e, t) : t ? this.stateTarget.setAttribute(e, t) : this.stateTarget.removeAttribute(e));
    }
    /** @protected */
    _delegateProperty(e, t) {
      this.stateTarget && (this.stateTarget[e] = t);
    }
  }
);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const mc = Je(
  (s) => class extends Tp(Rs(Ps(s))) {
    /**
     * An array of attributes which participate in the input validation.
     * Changing these attributes will cause the input to re-validate.
     *
     * IMPORTANT: The attributes should be properly delegated to the input element
     * from the host using `delegateAttrs` getter (see `DelegateStateMixin`).
     * The `required` attribute is already delegated.
     */
    static get constraints() {
      return ["required"];
    }
    static get delegateAttrs() {
      return [...super.delegateAttrs, "required"];
    }
    /** @protected */
    ready() {
      super.ready(), this._createConstraintsObserver();
    }
    /**
     * Returns true if the current input value satisfies all constraints (if any).
     * @return {boolean}
     */
    checkValidity() {
      return this.inputElement && this._hasValidConstraints(this.constructor.constraints.map((e) => this[e])) ? this.inputElement.checkValidity() : !this.invalid;
    }
    /**
     * Returns true if some of the provided set of constraints are valid.
     * @param {Array} constraints
     * @return {boolean}
     * @protected
     */
    _hasValidConstraints(e) {
      return e.some((t) => this.__isValidConstraint(t));
    }
    /**
     * Override this method to customize setting up constraints observer.
     * @protected
     */
    _createConstraintsObserver() {
      this._createMethodObserver(`_constraintsChanged(stateTarget, ${this.constructor.constraints.join(", ")})`);
    }
    /**
     * Override this method to implement custom validation constraints.
     * @param {HTMLElement | undefined} stateTarget
     * @param {unknown[]} constraints
     * @protected
     */
    _constraintsChanged(e, ...t) {
      if (!e)
        return;
      const i = this._hasValidConstraints(t), r = this.__previousHasConstraints && !i;
      (this._hasValue || this.invalid) && i ? this.validate() : r && this._setInvalid(!1), this.__previousHasConstraints = i;
    }
    /**
     * Override an event listener inherited from `InputMixin`
     * to capture native `change` event and make sure that
     * a new one is dispatched after validation runs.
     * @param {Event} event
     * @protected
     * @override
     */
    _onChange(e) {
      e.stopPropagation(), this.validate(), this.dispatchEvent(
        new CustomEvent("change", {
          detail: {
            sourceEvent: e
          },
          bubbles: e.bubbles,
          cancelable: e.cancelable
        })
      );
    }
    /** @private */
    __isValidConstraint(e) {
      return !!e || e === 0;
    }
  }
);
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ep = (s) => class extends _p(
  bf(mc(Cp(vp(xs(s)))))
) {
  static get properties() {
    return {
      /**
       * A pattern matched against individual characters the user inputs.
       *
       * When set, the field will prevent:
       * - `keydown` events if the entered key doesn't match `/^allowedCharPattern$/`
       * - `paste` events if the pasted text doesn't match `/^allowedCharPattern*$/`
       * - `drop` events if the dropped text doesn't match `/^allowedCharPattern*$/`
       *
       * For example, to allow entering only numbers and minus signs, use:
       * `allowedCharPattern = "[\\d-]"`
       * @attr {string} allowed-char-pattern
       */
      allowedCharPattern: {
        type: String,
        observer: "_allowedCharPatternChanged"
      },
      /**
       * If true, the input text gets fully selected when the field is focused using click or touch / tap.
       */
      autoselect: {
        type: Boolean,
        value: !1
      },
      /**
       * The name of this field.
       */
      name: {
        type: String,
        reflectToAttribute: !0
      },
      /**
       * A hint to the user of what can be entered in the field.
       */
      placeholder: {
        type: String,
        reflectToAttribute: !0
      },
      /**
       * When present, it specifies that the field is read-only.
       */
      readonly: {
        type: Boolean,
        value: !1,
        reflectToAttribute: !0
      },
      /**
       * The text usually displayed in a tooltip popup when the mouse is over the field.
       */
      title: {
        type: String,
        reflectToAttribute: !0
      }
    };
  }
  static get delegateAttrs() {
    return [...super.delegateAttrs, "name", "type", "placeholder", "readonly", "invalid", "title"];
  }
  constructor() {
    super(), this._boundOnPaste = this._onPaste.bind(this), this._boundOnDrop = this._onDrop.bind(this), this._boundOnBeforeInput = this._onBeforeInput.bind(this);
  }
  /** @protected */
  get slotStyles() {
    return [
      `
          :is(input[slot='input'], textarea[slot='textarea'])::placeholder {
            font: inherit;
            color: inherit;
          }
        `
    ];
  }
  /**
   * Override an event listener from `DelegateFocusMixin`.
   * @param {FocusEvent} event
   * @protected
   * @override
   */
  _onFocus(e) {
    super._onFocus(e), this.autoselect && this.inputElement && this.inputElement.select();
  }
  /**
   * Override an event listener inherited from `InputMixin`
   * to capture native `change` event and make sure that
   * a new one is dispatched after validation runs.
   * @param {Event} event
   * @protected
   * @override
   */
  _onChange(e) {
    e.stopPropagation(), this.validate(), this.dispatchEvent(
      new CustomEvent("change", {
        detail: {
          sourceEvent: e
        },
        bubbles: e.bubbles,
        cancelable: e.cancelable
      })
    );
  }
  /**
   * Override a method from `InputMixin`.
   * @param {!HTMLElement} input
   * @protected
   * @override
   */
  _addInputListeners(e) {
    super._addInputListeners(e), e.addEventListener("paste", this._boundOnPaste), e.addEventListener("drop", this._boundOnDrop), e.addEventListener("beforeinput", this._boundOnBeforeInput);
  }
  /**
   * Override a method from `InputMixin`.
   * @param {!HTMLElement} input
   * @protected
   * @override
   */
  _removeInputListeners(e) {
    super._removeInputListeners(e), e.removeEventListener("paste", this._boundOnPaste), e.removeEventListener("drop", this._boundOnDrop), e.removeEventListener("beforeinput", this._boundOnBeforeInput);
  }
  /**
   * Override an event listener from `KeyboardMixin`.
   * @param {!KeyboardEvent} event
   * @protected
   * @override
   */
  _onKeyDown(e) {
    super._onKeyDown(e), this.allowedCharPattern && !this.__shouldAcceptKey(e) && e.target === this.inputElement && (e.preventDefault(), this._markInputPrevented());
  }
  /** @protected */
  _markInputPrevented() {
    this.setAttribute("input-prevented", ""), this._preventInputDebouncer = ze.debounce(this._preventInputDebouncer, Nt.after(200), () => {
      this.removeAttribute("input-prevented");
    });
  }
  /** @private */
  __shouldAcceptKey(e) {
    return e.metaKey || e.ctrlKey || !e.key || // Allow typing anything if event.key is not supported
    e.key.length !== 1 || // Allow "Backspace", "ArrowLeft" etc.
    this.__allowedCharRegExp.test(e.key);
  }
  /** @private */
  _onPaste(e) {
    if (this.allowedCharPattern) {
      const t = e.clipboardData.getData("text");
      this.__allowedTextRegExp.test(t) || (e.preventDefault(), this._markInputPrevented());
    }
  }
  /** @private */
  _onDrop(e) {
    if (this.allowedCharPattern) {
      const t = e.dataTransfer.getData("text");
      this.__allowedTextRegExp.test(t) || (e.preventDefault(), this._markInputPrevented());
    }
  }
  /** @private */
  _onBeforeInput(e) {
    this.allowedCharPattern && e.data && !this.__allowedTextRegExp.test(e.data) && (e.preventDefault(), this._markInputPrevented());
  }
  /** @private */
  _allowedCharPatternChanged(e) {
    if (e)
      try {
        this.__allowedCharRegExp = new RegExp(`^${e}$`, "u"), this.__allowedTextRegExp = new RegExp(`^${e}*$`, "u");
      } catch {
      }
  }
  /**
   * Fired when the user commits a value change.
   *
   * @event change
   */
  /**
   * Fired when the value is changed by the user: on every typing keystroke,
   * and the value is cleared using the clear button.
   *
   * @event input
   */
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Pp extends ms {
  constructor(n, e, t = {}) {
    const { uniqueIdPrefix: i } = t;
    super(n, "input", "input", {
      initializer: (r, o) => {
        o.value && (r.value = o.value), o.type && r.setAttribute("type", o.type), r.id = this.defaultId, typeof e == "function" && e(r);
      },
      useUniqueId: !0,
      uniqueIdPrefix: i
    });
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Sp {
  constructor(n, e) {
    this.input = n, this.__preventDuplicateLabelClick = this.__preventDuplicateLabelClick.bind(this), e.addEventListener("slot-content-changed", (t) => {
      this.__initLabel(t.detail.node);
    }), this.__initLabel(e.node);
  }
  /**
   * @param {HTMLElement} label
   * @private
   */
  __initLabel(n) {
    n && (n.addEventListener("click", this.__preventDuplicateLabelClick), this.input && n.setAttribute("for", this.input.id));
  }
  /**
   * The native platform fires an event for both the click on the label, and also
   * the subsequent click on the native input element caused by label click.
   * This results in two click events arriving at the host, but we only want one.
   * This method prevents the duplicate click and ensures the correct isTrusted event
   * with the correct event.target arrives at the host.
   * @private
   */
  __preventDuplicateLabelClick() {
    const n = (e) => {
      e.stopImmediatePropagation(), this.input.removeEventListener("click", n);
    };
    this.input.addEventListener("click", n);
  }
}
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Rp = (s) => class extends mc(s) {
  static get properties() {
    return {
      /**
       * A regular expression that the value is checked against.
       * The pattern must match the entire value, not just some subset.
       */
      pattern: {
        type: String
      }
    };
  }
  static get delegateAttrs() {
    return [...super.delegateAttrs, "pattern"];
  }
  static get constraints() {
    return [...super.constraints, "pattern"];
  }
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd..
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ap = Ee`
  [part='clear-button'] {
    display: none;
    cursor: default;
  }

  [part='clear-button']::before {
    content: '\\2715';
  }

  :host([clear-button-visible][has-value]:not([disabled]):not([readonly])) [part='clear-button'] {
    display: block;
  }
`;
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd..
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const kp = Ee`
  :host {
    display: inline-flex;
    outline: none;
  }

  :host::before {
    content: '\\2003';
    width: 0;
    display: inline-block;
    /* Size and position this element on the same vertical position as the input-field element
          to make vertical align for the host element work as expected */
  }

  :host([hidden]) {
    display: none !important;
  }

  :host(:not([has-label])) [part='label'] {
    display: none;
  }

  @media (forced-colors: active) {
    :host(:not([readonly])) [part='input-field'] {
      outline: 1px solid;
      outline-offset: -1px;
    }
    :host([focused]) [part='input-field'] {
      outline-width: 2px;
    }
    :host([disabled]) [part='input-field'] {
      outline-color: GrayText;
    }
  }
`;
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd..
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Ip = Ee`
  [class$='container'] {
    display: flex;
    flex-direction: column;
    min-width: 100%;
    max-width: 100%;
    width: var(--vaadin-field-default-width, 12em);
  }
`;
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd..
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Dp = [kp, Ip, Ap];
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const Op = (s) => class extends s {
  static get properties() {
    return {
      /**
       * Number of items fetched at a time from the dataprovider.
       * @attr {number} page-size
       * @type {number}
       */
      pageSize: {
        type: Number,
        value: 50,
        observer: "_pageSizeChanged",
        sync: !0
      },
      /**
       * Total number of items.
       * @type {number | undefined}
       */
      size: {
        type: Number,
        observer: "_sizeChanged",
        sync: !0
      },
      /**
       * Function that provides items lazily. Receives arguments `params`, `callback`
       *
       * `params.page` Requested page index
       *
       * `params.pageSize` Current page size
       *
       * `params.filter` Currently applied filter
       *
       * `callback(items, size)` Callback function with arguments:
       *   - `items` Current page of items
       *   - `size` Total number of items.
       * @type {ComboBoxDataProvider | undefined}
       */
      dataProvider: {
        type: Object,
        observer: "_dataProviderChanged",
        sync: !0
      },
      /** @private */
      __dataProviderInitialized: {
        type: Boolean,
        value: !1
      },
      /** @private */
      __previousDataProviderFilter: {
        type: String
      }
    };
  }
  static get observers() {
    return [
      "_dataProviderFilterChanged(filter)",
      "_warnDataProviderValue(dataProvider, value)",
      "_ensureFirstPage(opened)"
    ];
  }
  constructor() {
    super(), this.__dataProviderController = new oc(this, {
      placeholder: new Vi(),
      isPlaceholder: (e) => e instanceof Vi,
      dataProviderParams: () => ({ filter: this.filter })
    }), this.__dataProviderController.addEventListener("page-requested", this.__onDataProviderPageRequested.bind(this)), this.__dataProviderController.addEventListener("page-loaded", this.__onDataProviderPageLoaded.bind(this));
  }
  /** @protected */
  ready() {
    super.ready(), this._scroller.addEventListener("index-requested", (e) => {
      if (!this._shouldFetchData())
        return;
      const t = e.detail.index;
      t !== void 0 && this.__dataProviderController.ensureFlatIndexLoaded(t);
    }), this.__dataProviderInitialized = !0, this.dataProvider && this.__synchronizeControllerState();
  }
  /** @private */
  _dataProviderFilterChanged(e) {
    if (this.__previousDataProviderFilter === void 0 && e === "") {
      this.__previousDataProviderFilter = e;
      return;
    }
    this.__previousDataProviderFilter !== e && (this.__previousDataProviderFilter = e, this.__keepOverlayOpened = !0, this.size = void 0, this.clearCache(), this.__keepOverlayOpened = !1);
  }
  /** @protected */
  _shouldFetchData() {
    return this.dataProvider ? this.opened || this.filter && this.filter.length : !1;
  }
  /** @private */
  _ensureFirstPage(e) {
    !this._shouldFetchData() || !e || (this._forceNextRequest || this.size === void 0 ? (this._forceNextRequest = !1, this.__dataProviderController.loadFirstPage()) : this.size > 0 && this.__dataProviderController.ensureFlatIndexLoaded(0));
  }
  /** @private */
  __onDataProviderPageRequested() {
    this.loading = !0;
  }
  /** @private */
  __onDataProviderPageLoaded() {
    const { rootCache: e } = this.__dataProviderController;
    e.items = [...e.items], this.__synchronizeControllerState(), !this.opened && !this._isInputFocused() && this._commitValue();
  }
  /**
   * Clears the cached pages and reloads data from dataprovider when needed.
   */
  clearCache() {
    this.dataProvider && (this.__dataProviderController.clearCache(), this.__synchronizeControllerState(), this._shouldFetchData() ? (this._forceNextRequest = !1, this.__dataProviderController.loadFirstPage()) : this._forceNextRequest = !0);
  }
  /**
   * When the size change originates externally, synchronizes the new size with
   * the controller and request a content update to re-render the scroller.
   *
   * @private
   */
  _sizeChanged(e) {
    const { rootCache: t } = this.__dataProviderController;
    t.size !== e && (t.size = e, t.items = [...t.items], this.__synchronizeControllerState());
  }
  /**
   * When the items change originates externally, synchronizes the new items with
   * the controller and requests a content update to re-render the scroller.
   *
   * @private
   * @override
   */
  _filteredItemsChanged(e) {
    if (super._filteredItemsChanged(e), this.dataProvider && e) {
      const { rootCache: t } = this.__dataProviderController;
      t.items !== e && (t.items = e, this.__synchronizeControllerState());
    }
  }
  /**
   * Synchronizes the controller's state with the component, which can be
   * out of sync after the controller receives new data from the data provider
   * or if the state in the controller is directly manipulated.
   *
   * @private
   */
  __synchronizeControllerState() {
    if (this.__dataProviderInitialized && this.dataProvider) {
      const { rootCache: e } = this.__dataProviderController;
      this.size = e.size, this.filteredItems = e.items, this.loading = this.__dataProviderController.isLoading();
    }
  }
  /** @private */
  _pageSizeChanged(e, t) {
    if (Math.floor(e) !== e || e < 1)
      throw this.pageSize = t, new Error("`pageSize` value must be an integer > 0");
    this.__dataProviderController.setPageSize(e), this.clearCache();
  }
  /** @private */
  _dataProviderChanged(e, t) {
    this._ensureItemsOrDataProvider(() => {
      this.dataProvider = t;
    }), this.__dataProviderController.setDataProvider(e), this.clearCache();
  }
  /** @private */
  _ensureItemsOrDataProvider(e) {
    if (this.items !== void 0 && this.dataProvider !== void 0)
      throw e(), new Error("Using `items` and `dataProvider` together is not supported");
  }
  /** @private */
  _warnDataProviderValue(e, t) {
    if (e && t !== "" && (this.selectedItem === void 0 || this.selectedItem === null)) {
      const i = this.__getItemIndexByValue(this.filteredItems, t);
      i < 0 || this._getItemLabel(this.filteredItems[i]);
    }
  }
};
/**
 * @license
 * Copyright (c) 2023 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const zp = (s) => class extends s {
  static get properties() {
    return {
      /**
       * A space-delimited list of CSS class names to set on the overlay element.
       * This property does not affect other CSS class names set manually via JS.
       *
       * Note, if the CSS class name was set with this property, clearing it will
       * remove it from the overlay, even if the same class name was also added
       * manually, e.g. by using `classList.add()` in the `renderer` function.
       *
       * @attr {string} overlay-class
       */
      overlayClass: {
        type: String
      },
      /**
       * An overlay element on which CSS class names are set.
       *
       * @protected
       */
      _overlayElement: {
        type: Object
      }
    };
  }
  static get observers() {
    return ["__updateOverlayClassNames(overlayClass, _overlayElement)"];
  }
  /** @private */
  __updateOverlayClassNames(e, t) {
    if (!t || e === void 0)
      return;
    const { classList: i } = t;
    if (this.__initialClasses || (this.__initialClasses = new Set(i)), Array.isArray(this.__previousClasses)) {
      const o = this.__previousClasses.filter((l) => !this.__initialClasses.has(l));
      o.length > 0 && i.remove(...o);
    }
    const r = typeof e == "string" ? e.split(" ").filter(Boolean) : [];
    r.length > 0 && i.add(...r), this.__previousClasses = r;
  }
};
/**
 * @license
 * Copyright (c) 2021 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class Fp {
  /**
   * @param {{ inputElement?: HTMLElement; opened: boolean } & HTMLElement} host
   */
  constructor(n) {
    this.host = n, n.addEventListener("opened-changed", () => {
      n.opened || this.__setVirtualKeyboardEnabled(!1);
    }), n.addEventListener("blur", () => this.__setVirtualKeyboardEnabled(!0)), n.addEventListener("touchstart", () => this.__setVirtualKeyboardEnabled(!0));
  }
  /** @private */
  __setVirtualKeyboardEnabled(n) {
    this.host.inputElement && (this.host.inputElement.inputMode = n ? "" : "none");
  }
}
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
function Qa(s) {
  return s != null;
}
function Ja(s, n) {
  return s.findIndex((e) => e instanceof Vi ? !1 : n(e));
}
const Lp = (s) => class extends zp(
  Er(Rs(ac(xs(Ps(Gl(s))))))
) {
  static get properties() {
    return {
      /**
       * True if the dropdown is open, false otherwise.
       * @type {boolean}
       */
      opened: {
        type: Boolean,
        notify: !0,
        value: !1,
        reflectToAttribute: !0,
        sync: !0,
        observer: "_openedChanged"
      },
      /**
       * Set true to prevent the overlay from opening automatically.
       * @attr {boolean} auto-open-disabled
       */
      autoOpenDisabled: {
        type: Boolean,
        sync: !0
      },
      /**
       * When present, it specifies that the field is read-only.
       * @type {boolean}
       */
      readonly: {
        type: Boolean,
        value: !1,
        reflectToAttribute: !0
      },
      /**
       * Custom function for rendering the content of every item.
       * Receives three arguments:
       *
       * - `root` The `<vaadin-combo-box-item>` internal container DOM element.
       * - `comboBox` The reference to the `<vaadin-combo-box>` element.
       * - `model` The object with the properties related with the rendered
       *   item, contains:
       *   - `model.index` The index of the rendered item.
       *   - `model.item` The item.
       * @type {ComboBoxRenderer | undefined}
       */
      renderer: {
        type: Object,
        sync: !0
      },
      /**
       * A full set of items to filter the visible options from.
       * The items can be of either `String` or `Object` type.
       * @type {!Array<!ComboBoxItem | string> | undefined}
       */
      items: {
        type: Array,
        sync: !0,
        observer: "_itemsChanged"
      },
      /**
       * If `true`, the user can input a value that is not present in the items list.
       * `value` property will be set to the input value in this case.
       * Also, when `value` is set programmatically, the input value will be set
       * to reflect that value.
       * @attr {boolean} allow-custom-value
       * @type {boolean}
       */
      allowCustomValue: {
        type: Boolean,
        value: !1
      },
      /**
       * A subset of items, filtered based on the user input. Filtered items
       * can be assigned directly to omit the internal filtering functionality.
       * The items can be of either `String` or `Object` type.
       * @type {!Array<!ComboBoxItem | string> | undefined}
       */
      filteredItems: {
        type: Array,
        observer: "_filteredItemsChanged",
        sync: !0
      },
      /**
       * Used to detect user value changes and fire `change` events.
       * @private
       */
      _lastCommittedValue: String,
      /**
       * When set to `true`, "loading" attribute is added to host and the overlay element.
       * @type {boolean}
       */
      loading: {
        type: Boolean,
        value: !1,
        reflectToAttribute: !0,
        sync: !0
      },
      /**
       * @type {number}
       * @protected
       */
      _focusedIndex: {
        type: Number,
        observer: "_focusedIndexChanged",
        value: -1,
        sync: !0
      },
      /**
       * Filtering string the user has typed into the input field.
       * @type {string}
       */
      filter: {
        type: String,
        value: "",
        notify: !0,
        sync: !0
      },
      /**
       * The selected item from the `items` array.
       * @type {ComboBoxItem | string | undefined}
       */
      selectedItem: {
        type: Object,
        notify: !0,
        sync: !0
      },
      /**
       * A function used to generate CSS class names for dropdown
       * items based on the item. The return value should be the
       * generated class name as a string, or multiple class names
       * separated by whitespace characters.
       */
      itemClassNameGenerator: {
        type: Object
      },
      /**
       * Path for label of the item. If `items` is an array of objects, the
       * `itemLabelPath` is used to fetch the displayed string label for each
       * item.
       *
       * The item label is also used for matching items when processing user
       * input, i.e., for filtering and selecting items.
       * @attr {string} item-label-path
       * @type {string}
       */
      itemLabelPath: {
        type: String,
        value: "label",
        observer: "_itemLabelPathChanged",
        sync: !0
      },
      /**
       * Path for the value of the item. If `items` is an array of objects, the
       * `itemValuePath:` is used to fetch the string value for the selected
       * item.
       *
       * The item value is used in the `value` property of the combo box,
       * to provide the form value.
       * @attr {string} item-value-path
       * @type {string}
       */
      itemValuePath: {
        type: String,
        value: "value",
        sync: !0
      },
      /**
       * Path for the id of the item. If `items` is an array of objects,
       * the `itemIdPath` is used to compare and identify the same item
       * in `selectedItem` and `filteredItems` (items given by the
       * `dataProvider` callback).
       * @attr {string} item-id-path
       */
      itemIdPath: {
        type: String,
        sync: !0
      },
      /**
       * @type {!HTMLElement | undefined}
       * @protected
       */
      _toggleElement: {
        type: Object,
        observer: "_toggleElementChanged"
      },
      /**
       * Set of items to be rendered in the dropdown.
       * @protected
       */
      _dropdownItems: {
        type: Array,
        sync: !0
      },
      /** @private */
      _closeOnBlurIsPrevented: Boolean,
      /** @private */
      _scroller: {
        type: Object,
        sync: !0
      },
      /** @private */
      _overlayOpened: {
        type: Boolean,
        sync: !0,
        observer: "_overlayOpenedChanged"
      },
      /** @private */
      __keepOverlayOpened: {
        type: Boolean,
        sync: !0
      }
    };
  }
  static get observers() {
    return [
      "_selectedItemChanged(selectedItem, itemValuePath, itemLabelPath)",
      "_openedOrItemsChanged(opened, _dropdownItems, loading, __keepOverlayOpened)",
      "_updateScroller(_scroller, _dropdownItems, opened, loading, selectedItem, itemIdPath, _focusedIndex, renderer, _theme, itemClassNameGenerator)"
    ];
  }
  constructor() {
    super(), this._boundOverlaySelectedItemChanged = this._overlaySelectedItemChanged.bind(this), this._boundOnClearButtonMouseDown = this.__onClearButtonMouseDown.bind(this), this._boundOnClick = this._onClick.bind(this), this._boundOnOverlayTouchAction = this._onOverlayTouchAction.bind(this), this._boundOnTouchend = this._onTouchend.bind(this);
  }
  /**
   * Tag name prefix used by scroller and items.
   * @protected
   * @return {string}
   */
  get _tagNamePrefix() {
    return "vaadin-combo-box";
  }
  /**
   * Get a reference to the native `<input>` element.
   * Override to provide a custom input.
   * @protected
   * @return {HTMLInputElement | undefined}
   */
  get _nativeInput() {
    return this.inputElement;
  }
  /**
   * Override method inherited from `InputMixin`
   * to customize the input element.
   * @protected
   * @override
   */
  _inputElementChanged(e) {
    super._inputElementChanged(e);
    const t = this._nativeInput;
    t && (t.autocomplete = "off", t.autocapitalize = "off", t.setAttribute("role", "combobox"), t.setAttribute("aria-autocomplete", "list"), t.setAttribute("aria-expanded", !!this.opened), t.setAttribute("spellcheck", "false"), t.setAttribute("autocorrect", "off"), this._revertInputValueToValue(), this.clearElement && this.clearElement.addEventListener("mousedown", this._boundOnClearButtonMouseDown));
  }
  /** @protected */
  ready() {
    super.ready(), this._initOverlay(), this._initScroller(), this._lastCommittedValue = this.value, this.addEventListener("click", this._boundOnClick), this.addEventListener("touchend", this._boundOnTouchend);
    const e = () => {
      requestAnimationFrame(() => {
        this._overlayElement.bringToFront();
      });
    };
    this.addEventListener("mousedown", e), this.addEventListener("touchstart", e), ds(this), this.addController(new Fp(this));
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback(), this.close();
  }
  /**
   * Requests an update for the content of items.
   * While performing the update, it invokes the renderer (passed in the `renderer` property) once an item.
   *
   * It is not guaranteed that the update happens immediately (synchronously) after it is requested.
   */
  requestContentUpdate() {
    this._scroller && (this._scroller.requestContentUpdate(), this._getItemElements().forEach((e) => {
      e.requestContentUpdate();
    }));
  }
  /**
   * Opens the dropdown list.
   */
  open() {
    !this.disabled && !this.readonly && (this.opened = !0);
  }
  /**
   * Closes the dropdown list.
   */
  close() {
    this.opened = !1;
  }
  /**
   * Override Polymer lifecycle callback to handle `filter` property change after
   * the observer for `opened` property is triggered. This is needed when opening
   * combo-box on user input to ensure the focused index is set correctly.
   *
   * @param {!Object} currentProps Current accessor values
   * @param {?Object} changedProps Properties changed since the last call
   * @param {?Object} oldProps Previous values for each changed property
   * @protected
   * @override
   */
  _propertiesChanged(e, t, i) {
    super._propertiesChanged(e, t, i), t.filter !== void 0 && this._filterChanged(t.filter);
  }
  /**
   * Override LitElement lifecycle callback to handle filter property change.
   * @param {Object} props
   */
  updated(e) {
    super.updated(e), e.has("filter") && this._filterChanged(this.filter);
  }
  /** @private */
  _initOverlay() {
    const e = this.$.overlay;
    e._comboBox = this, e.addEventListener("touchend", this._boundOnOverlayTouchAction), e.addEventListener("touchmove", this._boundOnOverlayTouchAction), e.addEventListener("mousedown", (t) => t.preventDefault()), e.addEventListener("opened-changed", (t) => {
      this._overlayOpened = t.detail.value;
    }), this._overlayElement = e;
  }
  /**
   * Create and initialize the scroller element.
   * Override to provide custom host reference.
   *
   * @protected
   */
  _initScroller(e) {
    const t = document.createElement(`${this._tagNamePrefix}-scroller`);
    t.owner = e || this, t.getItemLabel = this._getItemLabel.bind(this), t.addEventListener("selection-changed", this._boundOverlaySelectedItemChanged);
    const i = this._overlayElement;
    i.renderer = (r) => {
      r.innerHTML || r.appendChild(t);
    }, i.requestContentUpdate(), this._scroller = t;
  }
  /** @private */
  // eslint-disable-next-line @typescript-eslint/max-params
  _updateScroller(e, t, i, r, o, l, a, c, d, f) {
    if (e && (i && (e.style.maxHeight = getComputedStyle(this).getPropertyValue(`--${this._tagNamePrefix}-overlay-max-height`) || "65vh"), e.setProperties({
      items: i ? t : [],
      opened: i,
      loading: r,
      selectedItem: o,
      itemIdPath: l,
      focusedIndex: a,
      renderer: c,
      theme: d,
      itemClassNameGenerator: f
    }), e.performUpdate && !e.hasUpdated))
      try {
        e.performUpdate();
      } catch {
      }
  }
  /** @private */
  _openedOrItemsChanged(e, t, i, r) {
    this._overlayOpened = e && (r || i || !!(t && t.length));
  }
  /** @private */
  _overlayOpenedChanged(e, t) {
    e ? (this.dispatchEvent(new CustomEvent("vaadin-combo-box-dropdown-opened", { bubbles: !0, composed: !0 })), this._onOpened()) : t && this._dropdownItems && this._dropdownItems.length && (this.close(), this.dispatchEvent(new CustomEvent("vaadin-combo-box-dropdown-closed", { bubbles: !0, composed: !0 })));
  }
  /** @private */
  _focusedIndexChanged(e, t) {
    t !== void 0 && this._updateActiveDescendant(e);
  }
  /** @protected */
  _isInputFocused() {
    return this.inputElement && Wl(this.inputElement);
  }
  /** @private */
  _updateActiveDescendant(e) {
    const t = this._nativeInput;
    if (!t)
      return;
    const i = this._getItemElements().find((r) => r.index === e);
    i ? t.setAttribute("aria-activedescendant", i.id) : t.removeAttribute("aria-activedescendant");
  }
  /** @private */
  _openedChanged(e, t) {
    if (t === void 0)
      return;
    e ? !this._isInputFocused() && !br && this.inputElement && this.inputElement.focus() : this._onClosed();
    const i = this._nativeInput;
    i && (i.setAttribute("aria-expanded", !!e), e ? i.setAttribute("aria-controls", this._scroller.id) : i.removeAttribute("aria-controls"));
  }
  /** @private */
  _onOverlayTouchAction() {
    this._closeOnBlurIsPrevented = !0, this.inputElement.blur(), this._closeOnBlurIsPrevented = !1;
  }
  /** @protected */
  _isClearButton(e) {
    return e.composedPath()[0] === this.clearElement;
  }
  /** @private */
  __onClearButtonMouseDown(e) {
    e.preventDefault(), this.inputElement.focus();
  }
  /**
   * @param {Event} event
   * @protected
   */
  _onClearButtonClick(e) {
    e.preventDefault(), this._onClearAction(), this.opened && this.requestContentUpdate();
  }
  /**
   * @param {Event} event
   * @private
   */
  _onToggleButtonClick(e) {
    e.preventDefault(), this.opened ? this.close() : this.open();
  }
  /**
   * @param {Event} event
   * @protected
   */
  _onHostClick(e) {
    this.autoOpenDisabled || (e.preventDefault(), this.open());
  }
  /** @private */
  _onClick(e) {
    this._isClearButton(e) ? this._onClearButtonClick(e) : e.composedPath().includes(this._toggleElement) ? this._onToggleButtonClick(e) : this._onHostClick(e);
  }
  /**
   * Override an event listener from `KeyboardMixin`.
   *
   * @param {KeyboardEvent} e
   * @protected
   * @override
   */
  _onKeyDown(e) {
    super._onKeyDown(e), e.key === "ArrowDown" ? (this._onArrowDown(), e.preventDefault()) : e.key === "ArrowUp" && (this._onArrowUp(), e.preventDefault());
  }
  /** @private */
  _getItemLabel(e) {
    let t = e && this.itemLabelPath ? _i(this.itemLabelPath, e) : void 0;
    return t == null && (t = e ? e.toString() : ""), t;
  }
  /** @private */
  _getItemValue(e) {
    let t = e && this.itemValuePath ? _i(this.itemValuePath, e) : void 0;
    return t === void 0 && (t = e ? e.toString() : ""), t;
  }
  /** @private */
  _onArrowDown() {
    if (this.opened) {
      const e = this._dropdownItems;
      e && (this._focusedIndex = Math.min(e.length - 1, this._focusedIndex + 1), this._prefillFocusedItemLabel());
    } else
      this.open();
  }
  /** @private */
  _onArrowUp() {
    if (this.opened) {
      if (this._focusedIndex > -1)
        this._focusedIndex = Math.max(0, this._focusedIndex - 1);
      else {
        const e = this._dropdownItems;
        e && (this._focusedIndex = e.length - 1);
      }
      this._prefillFocusedItemLabel();
    } else
      this.open();
  }
  /** @private */
  _prefillFocusedItemLabel() {
    if (this._focusedIndex > -1) {
      const e = this._dropdownItems[this._focusedIndex];
      this._inputElementValue = this._getItemLabel(e), this._markAllSelectionRange();
    }
  }
  /** @private */
  _setSelectionRange(e, t) {
    this._isInputFocused() && this.inputElement.setSelectionRange && this.inputElement.setSelectionRange(e, t);
  }
  /** @private */
  _markAllSelectionRange() {
    this._inputElementValue !== void 0 && this._setSelectionRange(0, this._inputElementValue.length);
  }
  /** @private */
  _clearSelectionRange() {
    if (this._inputElementValue !== void 0) {
      const e = this._inputElementValue ? this._inputElementValue.length : 0;
      this._setSelectionRange(e, e);
    }
  }
  /** @private */
  _closeOrCommit() {
    !this.opened && !this.loading ? this._commitValue() : this.close();
  }
  /**
   * Override an event listener from `KeyboardMixin`.
   *
   * @param {KeyboardEvent} e
   * @protected
   * @override
   */
  _onEnter(e) {
    if (!this._hasValidInputValue()) {
      e.preventDefault(), e.stopPropagation();
      return;
    }
    this.opened && (e.preventDefault(), e.stopPropagation()), this._closeOrCommit();
  }
  /**
   * @protected
   */
  _hasValidInputValue() {
    const e = this._focusedIndex < 0 && this._inputElementValue !== "" && this._getItemLabel(this.selectedItem) !== this._inputElementValue;
    return this.allowCustomValue || !e;
  }
  /**
   * Override an event listener from `KeyboardMixin`.
   * Do not call `super` in order to override clear
   * button logic defined in `InputControlMixin`.
   *
   * @param {!KeyboardEvent} e
   * @protected
   * @override
   */
  _onEscape(e) {
    this.autoOpenDisabled ? this.opened || this.value !== this._inputElementValue && this._inputElementValue.length > 0 ? (e.stopPropagation(), this._focusedIndex = -1, this.cancel()) : this.clearButtonVisible && !this.opened && this.value && (e.stopPropagation(), this._onClearAction()) : this.opened ? (e.stopPropagation(), this._focusedIndex > -1 ? (this._focusedIndex = -1, this._revertInputValue()) : this.cancel()) : this.clearButtonVisible && this.value && (e.stopPropagation(), this._onClearAction());
  }
  /** @private */
  _toggleElementChanged(e) {
    e && (e.addEventListener("mousedown", (t) => t.preventDefault()), e.addEventListener("click", () => {
      br && !this._isInputFocused() && document.activeElement.blur();
    }));
  }
  /**
   * Clears the current value.
   * @protected
   */
  _onClearAction() {
    this.selectedItem = null, this.allowCustomValue && (this.value = ""), this._detectAndDispatchChange();
  }
  /**
   * Clears the current filter. Should be used instead of setting the property
   * directly in order to allow overriding this in multi-select combo box.
   * @protected
   */
  _clearFilter() {
    this.filter = "";
  }
  /**
   * Reverts back to original value.
   */
  cancel() {
    this._revertInputValueToValue(), this._lastCommittedValue = this.value, this._closeOrCommit();
  }
  /** @private */
  _onOpened() {
    this._lastCommittedValue = this.value;
  }
  /** @private */
  _onClosed() {
    (!this.loading || this.allowCustomValue) && this._commitValue();
  }
  /** @private */
  _commitValue() {
    if (this._focusedIndex > -1) {
      const e = this._dropdownItems[this._focusedIndex];
      this.selectedItem !== e && (this.selectedItem = e), this._inputElementValue = this._getItemLabel(this.selectedItem), this._focusedIndex = -1;
    } else if (this._inputElementValue === "" || this._inputElementValue === void 0)
      this.selectedItem = null, this.allowCustomValue && (this.value = "");
    else {
      const e = [this.selectedItem, ...this._dropdownItems || []], t = e[this.__getItemIndexByLabel(e, this._inputElementValue)];
      if (this.allowCustomValue && // To prevent a repetitive input value being saved after pressing ESC and Tab.
      !t) {
        const i = this._inputElementValue;
        this._lastCustomValue = i;
        const r = new CustomEvent("custom-value-set", {
          detail: i,
          composed: !0,
          cancelable: !0,
          bubbles: !0
        });
        this.dispatchEvent(r), r.defaultPrevented || (this.value = i);
      } else !this.allowCustomValue && !this.opened && t ? this.value = this._getItemValue(t) : this._revertInputValueToValue();
    }
    this._detectAndDispatchChange(), this._clearSelectionRange(), this._clearFilter();
  }
  /**
   * Override an event listener from `InputMixin`.
   * @param {!Event} event
   * @protected
   * @override
   */
  _onInput(e) {
    const t = this._inputElementValue, i = {};
    this.filter === t ? this._filterChanged(this.filter) : i.filter = t, !this.opened && !this._isClearButton(e) && !this.autoOpenDisabled && (i.opened = !0), this.setProperties(i);
  }
  /**
   * Override an event listener from `InputMixin`.
   * @param {!Event} event
   * @protected
   * @override
   */
  _onChange(e) {
    e.stopPropagation();
  }
  /** @private */
  _itemLabelPathChanged(e) {
  }
  /** @private */
  _filterChanged(e) {
    this._scrollIntoView(0), this._focusedIndex = -1, this.items ? this.filteredItems = this._filterItems(this.items, e) : this._filteredItemsChanged(this.filteredItems);
  }
  /** @protected */
  _revertInputValue() {
    this.filter !== "" ? this._inputElementValue = this.filter : this._revertInputValueToValue(), this._clearSelectionRange();
  }
  /** @private */
  _revertInputValueToValue() {
    this.allowCustomValue && !this.selectedItem ? this._inputElementValue = this.value : this._inputElementValue = this._getItemLabel(this.selectedItem);
  }
  /** @private */
  _selectedItemChanged(e) {
    if (e == null)
      this.filteredItems && (this.allowCustomValue || (this.value = ""), this._toggleHasValue(this._hasValue), this._inputElementValue = this.value);
    else {
      const t = this._getItemValue(e);
      if (this.value !== t && (this.value = t, this.value !== t))
        return;
      this._toggleHasValue(!0), this._inputElementValue = this._getItemLabel(e);
    }
  }
  /**
   * Override an observer from `InputMixin`.
   * @protected
   * @override
   */
  _valueChanged(e, t) {
    e === "" && t === void 0 || (Qa(e) ? (this._getItemValue(this.selectedItem) !== e && this._selectItemForValue(e), !this.selectedItem && this.allowCustomValue && (this._inputElementValue = e), this._toggleHasValue(this._hasValue)) : this.selectedItem = null, this._clearFilter(), this._lastCommittedValue = void 0);
  }
  /** @private */
  _detectAndDispatchChange() {
    document.hasFocus() && this.validate(), this.value !== this._lastCommittedValue && (this.dispatchEvent(new CustomEvent("change", { bubbles: !0 })), this._lastCommittedValue = this.value);
  }
  /** @private */
  _itemsChanged(e, t) {
    this._ensureItemsOrDataProvider(() => {
      this.items = t;
    }), e ? this.filteredItems = e.slice(0) : t && (this.filteredItems = null);
  }
  /** @private */
  _filteredItemsChanged(e) {
    this._setDropdownItems(e);
  }
  /** @private */
  _filterItems(e, t) {
    return e && e.filter((r) => (t = t ? t.toString().toLowerCase() : "", this._getItemLabel(r).toString().toLowerCase().indexOf(t) > -1));
  }
  /** @private */
  _selectItemForValue(e) {
    const t = this.__getItemIndexByValue(this.filteredItems, e), i = this.selectedItem;
    t >= 0 ? this.selectedItem = this.filteredItems[t] : this.dataProvider && this.selectedItem === void 0 ? this.selectedItem = void 0 : this.selectedItem = null, this.selectedItem === null && i === null && this._selectedItemChanged(this.selectedItem);
  }
  /**
   * Provide items to be rendered in the dropdown.
   * Override this method to show custom items.
   *
   * @protected
   */
  _setDropdownItems(e) {
    const t = this._dropdownItems;
    this._dropdownItems = e;
    const i = t ? t[this._focusedIndex] : null, r = this.__getItemIndexByValue(e, this.value);
    (this.selectedItem === null || this.selectedItem === void 0) && r >= 0 && (this.selectedItem = e[r]);
    const o = this.__getItemIndexByValue(e, this._getItemValue(i));
    o > -1 ? this._focusedIndex = o : this._focusedIndex = this.__getItemIndexByLabel(e, this.filter);
  }
  /** @private */
  _getItemElements() {
    return Array.from(this._scroller.querySelectorAll(`${this._tagNamePrefix}-item`));
  }
  /** @private */
  _scrollIntoView(e) {
    this._scroller && this._scroller.scrollIntoView(e);
  }
  /**
   * Returns the first item that matches the provided value.
   *
   * @private
   */
  __getItemIndexByValue(e, t) {
    return !e || !Qa(t) ? -1 : Ja(e, (i) => this._getItemValue(i) === t);
  }
  /**
   * Returns the first item that matches the provided label.
   * Labels are matched against each other case insensitively.
   *
   * @private
   */
  __getItemIndexByLabel(e, t) {
    return !e || !t ? -1 : Ja(e, (i) => this._getItemLabel(i).toString().toLowerCase() === t.toString().toLowerCase());
  }
  /** @private */
  _overlaySelectedItemChanged(e) {
    e.stopPropagation(), !(e.detail.item instanceof Vi) && this.opened && (this._focusedIndex = this.filteredItems.indexOf(e.detail.item), this.close());
  }
  /**
   * Override method inherited from `FocusMixin`
   * to close the overlay on blur and commit the value.
   *
   * @param {boolean} focused
   * @protected
   * @override
   */
  _setFocused(e) {
    if (super._setFocused(e), !e && !this.readonly && !this._closeOnBlurIsPrevented) {
      if (!this.opened && this.allowCustomValue && this._inputElementValue === this._lastCustomValue) {
        delete this._lastCustomValue;
        return;
      }
      if (Ln()) {
        this._closeOrCommit();
        return;
      }
      this.opened ? this._overlayOpened || this.close() : this._commitValue();
    }
  }
  /**
   * Override method inherited from `FocusMixin` to not remove focused
   * state when focus moves to the overlay.
   *
   * @param {FocusEvent} event
   * @return {boolean}
   * @protected
   * @override
   */
  _shouldRemoveFocus(e) {
    return e.relatedTarget && e.relatedTarget.localName === `${this._tagNamePrefix}-item` ? !1 : e.relatedTarget === this._overlayElement ? (e.composedPath()[0].focus(), !1) : !0;
  }
  /** @private */
  _onTouchend(e) {
    !this.clearElement || e.composedPath()[0] !== this.clearElement || (e.preventDefault(), this._onClearAction());
  }
  /**
   * Fired when the value changes.
   *
   * @event value-changed
   * @param {Object} detail
   * @param {String} detail.value the combobox value
   */
  /**
   * Fired when selected item changes.
   *
   * @event selected-item-changed
   * @param {Object} detail
   * @param {Object|String} detail.value the selected item. Type is the same as the type of `items`.
   */
  /**
   * Fired when the user sets a custom value.
   * @event custom-value-set
   * @param {String} detail the custom value
   */
  /**
   * Fired when value changes.
   * To comply with https://developer.mozilla.org/en-US/docs/Web/Events/change
   * @event change
   */
  /**
   * Fired after the `vaadin-combo-box-overlay` opens.
   *
   * @event vaadin-combo-box-dropdown-opened
   */
  /**
   * Fired after the `vaadin-combo-box-overlay` closes.
   *
   * @event vaadin-combo-box-dropdown-closed
   */
};
/**
 * @license
 * Copyright (c) 2015 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
Ze("vaadin-combo-box", Dp, { moduleId: "vaadin-combo-box-styles" });
class Mp extends Op(
  Lp(Rp(Ep(Cr(Vl(yi)))))
) {
  static get is() {
    return "vaadin-combo-box";
  }
  static get template() {
    return Wi`
      <style>
        :host([opened]) {
          pointer-events: auto;
        }
      </style>

      <div class="vaadin-combo-box-container">
        <div part="label">
          <slot name="label"></slot>
          <span part="required-indicator" aria-hidden="true" on-click="focus"></span>
        </div>

        <vaadin-input-container
          part="input-field"
          readonly="[[readonly]]"
          disabled="[[disabled]]"
          invalid="[[invalid]]"
          theme$="[[_theme]]"
        >
          <slot name="prefix" slot="prefix"></slot>
          <slot name="input"></slot>
          <div id="clearButton" part="clear-button" slot="suffix" aria-hidden="true"></div>
          <div id="toggleButton" part="toggle-button" slot="suffix" aria-hidden="true"></div>
        </vaadin-input-container>

        <div part="helper-text">
          <slot name="helper"></slot>
        </div>

        <div part="error-message">
          <slot name="error-message"></slot>
        </div>
      </div>

      <vaadin-combo-box-overlay
        id="overlay"
        opened="[[_overlayOpened]]"
        loading$="[[loading]]"
        theme$="[[_theme]]"
        position-target="[[_positionTarget]]"
        no-vertical-overlap
      ></vaadin-combo-box-overlay>

      <slot name="tooltip"></slot>
    `;
  }
  static get properties() {
    return {
      /**
       * @protected
       */
      _positionTarget: {
        type: Object
      }
    };
  }
  /**
   * Used by `InputControlMixin` as a reference to the clear button element.
   * @protected
   * @return {!HTMLElement}
   */
  get clearElement() {
    return this.$.clearButton;
  }
  /** @protected */
  ready() {
    super.ready(), this.addController(
      new Pp(this, (n) => {
        this._setInputElement(n), this._setFocusElement(n), this.stateTarget = n, this.ariaTarget = n;
      })
    ), this.addController(new Sp(this.inputElement, this._labelController)), this._tooltipController = new Xl(this), this.addController(this._tooltipController), this._tooltipController.setPosition("top"), this._tooltipController.setAriaTarget(this.inputElement), this._tooltipController.setShouldShow((n) => !n.opened), this._positionTarget = this.shadowRoot.querySelector('[part="input-field"]'), this._toggleElement = this.$.toggleButton;
  }
  /**
   * Override the method from `InputControlMixin`
   * to stop event propagation to prevent `ComboBoxMixin`
   * from handling this click event also on its own.
   *
   * @param {Event} event
   * @protected
   * @override
   */
  _onClearButtonClick(n) {
    n.stopPropagation(), super._onClearButtonClick(n);
  }
  /**
   * @param {Event} event
   * @protected
   */
  _onHostClick(n) {
    const e = n.composedPath();
    (e.includes(this._labelNode) || e.includes(this._positionTarget)) && super._onHostClick(n);
  }
}
Jt(Mp);
const Np = '*{padding:0;margin:0;border:0px;-webkit-user-select:none;user-select:none}p,div{-webkit-user-select:text;user-select:text}select{-webkit-user-select:none;user-select:none}.col-bg-body-lighter{background-color:var(--col-bg-body-lighter)}.col-bg-body{background-color:var(--col-bg-body)}.col-bg-body-darker{background-color:var(--col-bg-body-darker)}.col-bg-1-lighter{background-color:var(--col-bg-1-lighter)}.col-bg-1{background-color:var(--col-bg-1)}.col-bg-1-darker{background-color:var(--col-bg-1-darker)}.col-bg-1-input{background-color:var(--col-bg-1-input)}.col-primary-bg-1{background-color:var(--col-primary-bg-1)}.col-accent-bg-1{background-color:var(--col-accent-bg-1)}.col-success-bg-1{background-color:var(--col-success-bg-1)}.col-warning-bg-1{background-color:var(--col-warning-bg-1)}.col-error-bg-1{background-color:var(--col-error-bg-1)}.col-bg-2-lighter{background-color:var(--col-bg-2-lighter)}.col-bg-2{background-color:var(--col-bg-2)}.col-bg-2-darker{background-color:var(--col-bg-2-darker)}.col-bg-2-button{background-color:var(--col-bg-2-button)}.col-bg-2-input{background-color:var(--col-bg-2-input)}.col-primary-bg-2{background-color:var(--col-primary-bg-2)}.col-accent-bg-2{background-color:var(--col-accent-bg-2)}.col-success-bg-2{background-color:var(--col-success-bg-2)}.col-warning-bg-2{background-color:var(--col-warning-bg-2)}.col-error-bg-2{background-color:var(--col-error-bg-2)}.col-bg-3-lighter{background-color:var(--col-bg-3-lighter)}.col-bg-3{background-color:var(--col-bg-3)}.col-bg-3-darker{background-color:var(--col-bg-3-darker)}.col-bg-3-button{background-color:var(--col-bg-3-button)}.col-bg-3-input{background-color:var(--col-bg-3-input)}.col-primary-bg-3{background-color:var(--col-primary-bg-3)}.col-accent-bg-3{background-color:var(--col-accent-bg-3)}.col-success-bg-3{background-color:var(--col-success-bg-3)}.col-warning-bg-3{background-color:var(--col-warning-bg-3)}.col-error-bg-3{background-color:var(--col-error-bg-3)}.col-bg-ack-lighter{background-color:var(--col-bg-ack-lighter)}.col-bg-ack{background-color:var(--col-bg-ack)}.col-bg-ack-darker{background-color:var(--col-bg-ack-darker)}.col-primary-bg-ack{background-color:var(--col-primary-bg-ack)}.col-accent-bg-ack{background-color:var(--col-accent-bg-ack)}.col-bg-att-lighter{background-color:var(--col-bg-att-lighter)}.col-bg-att{background-color:var(--col-bg-att)}.col-bg-att-darker{background-color:var(--col-bg-att-darker)}.col-primary-bg-att{background-color:var(--col-primary-bg-att)}.col-accent-bg-att{background-color:var(--col-accent-bg-att)}.col-bg-alert-lighter{background-color:var(--col-bg-alert-lighter)}.col-bg-alert{background-color:var(--col-bg-alert)}.col-bg-alert-darker{background-color:var(--col-bg-alert-darker)}.col-primary-bg-alert{background-color:var(--col-primary-bg-alert)}.col-accent-bg-alert{background-color:var(--col-accent-bg-alert)}.col-bg-btn-lighter{background-color:var(--col-bg-btn-lighter)}.col-bg-btn{background-color:var(--col-bg-btn)}.col-bg-btn-darker{background-color:var(--col-bg-btn-darker)}.col-primary-bg-btn{background-color:var(--col-primary-bg-btn)}.col-accent-bg-btn{background-color:var(--col-accent-bg-btn)}.pattern-diagonal-stripes-sm{background:repeating-linear-gradient(45deg,transparent,transparent 10px,currentColor 10px,currentColor 20px)}.pattern-dots-lg{background-image:radial-gradient(currentColor 1.5px,transparent 1.5px);background-size:15px 15px}.pattern-dots-md{background-image:radial-gradient(currentColor 1px,transparent 1px);background-size:8px 8px}.pattern-dots-medium-dense{background-image:radial-gradient(currentColor .5px,transparent .5px);background-size:3px 3px}.pattern-dots-sm{background-image:radial-gradient(currentColor .5px,transparent .5px);background-size:5px 5px}.pattern-cross-dots-md,.imagelist-background{background-image:radial-gradient(currentColor .7px,transparent 1px),radial-gradient(currentColor .7px,transparent 1px);background-size:6.5px 6.5px;background-position:0 0,3.5px 3.5px}.imagelist-background{background-color:#fff9;background-blend-mode:overlay}label{font-family:var(--monospace-font);font-size:.9em}input,select{-webkit-user-select:text;-moz-user-select:text;-ms-user-select:text;box-sizing:border-box;border-radius:0;user-select:text;font-family:var(--standard-text-font);background-color:var(--col-bg-1-input);border:none;color:var(--col-primary-bg-1);padding:2px;margin-bottom:2px;outline:none}textarea{outline:none}input:not([type=radio],[type=checkbox]),select,textarea{width:100%}input:not([type=radio],[type=checkbox]):focus,select:focus,textarea:focus{margin-bottom:0;border-bottom:2px solid rgb(0,0,0)}h1{font-size:var(--font-size-h1)}h2{font-size:var(--font-size-h2)}h3{font-size:var(--font-size-h3)}h4{font-size:var(--font-size-h4)}small,.font-small{font-size:var(--font-size-small)}button{width:60px;height:60px;border-radius:35px;border-style:solid;border-width:2px;background-color:var(--col-bg-btn);color:var(--col-primary-bg-btn);border-color:var(--col-bg-btn-darker);font-family:var(--headline-text-font);font-size:var(--font-size-h4);font-weight:700}button:hover,button:focus{outline:none;background-color:var(--col-bg-btn-lighter)}button:active{padding-top:3px;color:var(--col-primary-bg-btn);background-color:var(--col-bg-btn-darker);border-color:var(--col-bg-btn-darker)}.kiosk-btn-128{width:128px;height:128px}.kiosk-btn-32{width:32px;height:32px}.kiosk-dropdown-btn{box-sizing:content-box;border-radius:0;background-color:var(--col-bg-btn);border-color:var(--col-bg-btn-darker);color:var(--col-primary-bg-btn);height:30px;width:30px}.kiosk-dropdown-btn .drop-down-btn{float:left}.kiosk-dropdown-btn .caret{color:var(--col-primary-bg-btn);display:inline-block;width:0;height:0;margin-left:2px;vertical-align:middle;border-color:var(--col-primary-bg-btn);border-top:4px solid;border-right:4px solid transparent;border-left:4px solid transparent}button:disabled{opacity:.3}.modal-round-button,.modal-delete,.modal-close,.modal-cancel,.modal-back,.modal-next,.modal-ok,.modal-button{box-sizing:border-box;margin-left:25px;background-image:none;display:inline-block;font-style:normal;font-variant:normal;text-rendering:auto;-webkit-font-smoothing:antialiased;font-family:"Font Awesome 6 Free";font-weight:900;font-size:24px;line-height:24px;vertical-align:center;text-align:center}.modal-ok:after{content:var(--icon-bt-ok)}.modal-next:after{padding-left:2px;content:var(--icon-bt-next)}.modal-back:after{padding-right:2px;content:var(--icon-bt-back)}.modal-cancel:after{content:var(--icon-bt-cancel)}.modal-close:before{content:var(--icon-bt-close)}.modal-delete{background-color:var(--col-bg-alert);color:var(--col-primary-bg-alert)}.modal-delete:before{content:var(--icon-bt-trash)}.modal-delete:hover,.modal-delete:focus{border-color:var(--col-bg-alert);background-color:var(--col-bg-alert-lighter);background-size:75%}.modal-delete:active{border-color:var(--col-bg-alert);background-color:var(--col-bg-alert-darker);color:var(--col-primary-bg-alert)}.kiosk-rounded{border-radius:15px}.kiosk-shaded{box-shadow:2px 2px 10px #2e380394}.kiosk-margin-bottom{margin-bottom:1em}.flex-line-break{width:100%;height:0px}.kiosk-align-flex-end{align-items:flex-end}.kiosk-align-flex-start{align-items:flex-start}.full-background-bg-1{background:linear-gradient(to right bottom,var(--col-bg-1-darker),var(--col-bg-1-lighter));height:100%;width:100%}.small-list-button i{border-radius:25px;box-shadow:2px 2px 5px #2e380394}.small-list-button:hover{color:var(--col-bg-att)}.small-list-button:hover i{box-shadow:2px 2px 5px #2e380394}.small-list-button:active,.small-list-button:focus{color:var(--col-bg-ack);transform:translateY(5px)}.small-list-button:active i,.small-list-button:focus i{box-shadow:none}.dialog-radio-div{display:flex;flex-direction:row}.dialog-radio-div input[type=radio]{margin-right:.5em}.rainbow-loading{display:flex;justify-content:center;align-items:center;height:5px;width:100%;background-color:#000}.rainbow-loading.to-top{position:fixed;top:0;z-index:1000}.rainbow-loading-progress{height:5px;width:100%;border-radius:3px;background:linear-gradient(90deg,red,#ff0,#0f0 30%,#0ff 50%,#00f,#f0f 80%,red);background-size:200%;animation:move-gradient 2s ease-in infinite}@keyframes move-gradient{0%{background-position:0% 0%}to{background-position:-200% 0%}}.fa,.fas{font-family:"Font Awesome 6 Free";font-weight:900;font-style:normal}.fa-trash:before{content:""}.fa-view-grid:before{content:""}.fa-reload:before{content:""}.fa-view-list:before{content:""}.fa-camera:before{content:""}.fa-view-image:before{content:""}.fa-check:before{content:""}.fa-bug:before{content:""}.fa-lightbulb:before{content:""}.fa-query:before{content:""}.fa-footsteps{transform:rotate(270deg)}.fa-footsteps:before{content:""}.fa-next:before{content:""}.fa-prev:before{content:""}.fa-hide:before{content:""}.fa-circle-half-stroke:before{content:""}i{font-family:"Font Awesome 6 Free";font-style:normal;font-variant:normal;text-rendering:auto;-webkit-font-smoothing:antialiased;font-weight:900;font-size:.9em}div,p{font-family:var(--standard-text-font)}:host{--lumo-font-size-s: $font-size-standard;--_lumo-grid-secondary-border-color: $col-bg-2-lighter;--_lumo-grid-border-width: 5px;--lumo-font-family: $standard-text;--lumo-size-m: $font-size-standard}vaadin-grid{margin-top:.5em;background-color:var(--col-bg-2);--_lumo-grid-selected-row-color: var(--col-bg-2-darker)}vaadin-grid::part(cell){padding-left:.5em;padding-top:3px;padding-bottom:3px;background-color:var(--col-bg-2)}vaadin-grid::part(header-cell){min-height:auto;padding-top:.2em;padding-bottom:.2em;border-bottom-color:var(--col-bg-2-darker);border-bottom-width:2px;background-color:var(--col-bg-2-darker)}vaadin-grid::part(row){background-color:var(--col-bg-2);color:var(--col-primary-bg-2)}vaadin-grid::part(selected-row){color:var(--col-accent-bg-2)}';
/**
 * @license
 * Copyright 2017 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const Bn = (s) => (n, e) => {
  e !== void 0 ? e.addInitializer((() => {
    customElements.define(s, n);
  })) : customElements.define(s, n);
};
/**
 * @license
 * Copyright 2017 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const Bp = { attribute: !0, type: String, converter: _n, reflect: !1, hasChanged: is }, Hp = (s = Bp, n, e) => {
  const { kind: t, metadata: i } = e;
  let r = globalThis.litPropertyMetadata.get(i);
  if (r === void 0 && globalThis.litPropertyMetadata.set(i, r = /* @__PURE__ */ new Map()), t === "setter" && ((s = Object.create(s)).wrapped = !0), r.set(e.name, s), t === "accessor") {
    const { name: o } = e;
    return { set(l) {
      const a = n.get.call(this);
      n.set.call(this, l), this.requestUpdate(o, a, s);
    }, init(l) {
      return l !== void 0 && this.C(o, void 0, s, l), l;
    } };
  }
  if (t === "setter") {
    const { name: o } = e;
    return function(l) {
      const a = this[o];
      n.call(this, l), this.requestUpdate(o, a, s);
    };
  }
  throw Error("Unsupported decorator location: " + t);
};
function Ge(s) {
  return (n, e) => typeof e == "object" ? Hp(s, n, e) : ((t, i, r) => {
    const o = i.hasOwnProperty(r);
    return i.constructor.createProperty(r, t), o ? Object.getOwnPropertyDescriptor(i, r) : void 0;
  })(s, n, e);
}
/**
 * @license
 * Copyright 2017 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
function At(s) {
  return Ge({ ...s, state: !0, attribute: !1 });
}
var Vp = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function _c(s) {
  return s && s.__esModule && Object.prototype.hasOwnProperty.call(s, "default") ? s.default : s;
}
var hn = { exports: {} }, Up = hn.exports, $a;
function Wp() {
  return $a || ($a = 1, (function(s, n) {
    (function(e, t) {
      s.exports = t();
    })(Up, function() {
      var e = function(h, u) {
        return (e = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(g, _) {
          g.__proto__ = _;
        } || function(g, _) {
          for (var v in _) Object.prototype.hasOwnProperty.call(_, v) && (g[v] = _[v]);
        })(h, u);
      }, t = function() {
        return (t = Object.assign || function(h) {
          for (var u, g = 1, _ = arguments.length; g < _; g++) for (var v in u = arguments[g]) Object.prototype.hasOwnProperty.call(u, v) && (h[v] = u[v]);
          return h;
        }).apply(this, arguments);
      };
      function i(h, u, g) {
        for (var _, v = 0, C = u.length; v < C; v++) !_ && v in u || ((_ = _ || Array.prototype.slice.call(u, 0, v))[v] = u[v]);
        return h.concat(_ || Array.prototype.slice.call(u));
      }
      var r = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : Vp, o = Object.keys, l = Array.isArray;
      function a(h, u) {
        return typeof u != "object" || o(u).forEach(function(g) {
          h[g] = u[g];
        }), h;
      }
      typeof Promise > "u" || r.Promise || (r.Promise = Promise);
      var c = Object.getPrototypeOf, d = {}.hasOwnProperty;
      function f(h, u) {
        return d.call(h, u);
      }
      function p(h, u) {
        typeof u == "function" && (u = u(c(h))), (typeof Reflect > "u" ? o : Reflect.ownKeys)(u).forEach(function(g) {
          x(h, g, u[g]);
        });
      }
      var b = Object.defineProperty;
      function x(h, u, g, _) {
        b(h, u, a(g && f(g, "get") && typeof g.get == "function" ? { get: g.get, set: g.set, configurable: !0 } : { value: g, configurable: !0, writable: !0 }, _));
      }
      function T(h) {
        return { from: function(u) {
          return h.prototype = Object.create(u.prototype), x(h.prototype, "constructor", h), { extend: p.bind(null, h.prototype) };
        } };
      }
      var I = Object.getOwnPropertyDescriptor, z = [].slice;
      function W(h, u, g) {
        return z.call(h, u, g);
      }
      function j(h, u) {
        return u(h);
      }
      function oe(h) {
        if (!h) throw new Error("Assertion Failed");
      }
      function ae(h) {
        r.setImmediate ? setImmediate(h) : setTimeout(h, 0);
      }
      function se(h, u) {
        if (typeof u == "string" && f(h, u)) return h[u];
        if (!u) return h;
        if (typeof u != "string") {
          for (var g = [], _ = 0, v = u.length; _ < v; ++_) {
            var C = se(h, u[_]);
            g.push(C);
          }
          return g;
        }
        var S = u.indexOf(".");
        if (S !== -1) {
          var A = h[u.substr(0, S)];
          return A == null ? void 0 : se(A, u.substr(S + 1));
        }
      }
      function fe(h, u, g) {
        if (h && u !== void 0 && !("isFrozen" in Object && Object.isFrozen(h))) if (typeof u != "string" && "length" in u) {
          oe(typeof g != "string" && "length" in g);
          for (var _ = 0, v = u.length; _ < v; ++_) fe(h, u[_], g[_]);
        } else {
          var C, S, A = u.indexOf(".");
          A !== -1 ? (C = u.substr(0, A), (S = u.substr(A + 1)) === "" ? g === void 0 ? l(h) && !isNaN(parseInt(C)) ? h.splice(C, 1) : delete h[C] : h[C] = g : fe(A = !(A = h[C]) || !f(h, C) ? h[C] = {} : A, S, g)) : g === void 0 ? l(h) && !isNaN(parseInt(u)) ? h.splice(u, 1) : delete h[u] : h[u] = g;
        }
      }
      function Ie(h) {
        var u, g = {};
        for (u in h) f(h, u) && (g[u] = h[u]);
        return g;
      }
      var Ae = [].concat;
      function qe(h) {
        return Ae.apply([], h);
      }
      var Ue = "BigUint64Array,BigInt64Array,Array,Boolean,String,Date,RegExp,Blob,File,FileList,FileSystemFileHandle,FileSystemDirectoryHandle,ArrayBuffer,DataView,Uint8ClampedArray,ImageBitmap,ImageData,Map,Set,CryptoKey".split(",").concat(qe([8, 16, 32, 64].map(function(h) {
        return ["Int", "Uint", "Float"].map(function(u) {
          return u + h + "Array";
        });
      }))).filter(function(h) {
        return r[h];
      }), Ne = new Set(Ue.map(function(h) {
        return r[h];
      })), Fe = null;
      function De(h) {
        return Fe = /* @__PURE__ */ new WeakMap(), h = (function u(g) {
          if (!g || typeof g != "object") return g;
          var _ = Fe.get(g);
          if (_) return _;
          if (l(g)) {
            _ = [], Fe.set(g, _);
            for (var v = 0, C = g.length; v < C; ++v) _.push(u(g[v]));
          } else if (Ne.has(g.constructor)) _ = g;
          else {
            var S, A = c(g);
            for (S in _ = A === Object.prototype ? {} : Object.create(A), Fe.set(g, _), g) f(g, S) && (_[S] = u(g[S]));
          }
          return _;
        })(h), Fe = null, h;
      }
      var wt = {}.toString;
      function lt(h) {
        return wt.call(h).slice(8, -1);
      }
      var ct = typeof Symbol < "u" ? Symbol.iterator : "@@iterator", pt = typeof ct == "symbol" ? function(h) {
        var u;
        return h != null && (u = h[ct]) && u.apply(h);
      } : function() {
        return null;
      };
      function ht(h, u) {
        return u = h.indexOf(u), 0 <= u && h.splice(u, 1), 0 <= u;
      }
      var mt = {};
      function ot(h) {
        var u, g, _, v;
        if (arguments.length === 1) {
          if (l(h)) return h.slice();
          if (this === mt && typeof h == "string") return [h];
          if (v = pt(h)) {
            for (g = []; !(_ = v.next()).done; ) g.push(_.value);
            return g;
          }
          if (h == null) return [h];
          if (typeof (u = h.length) != "number") return [h];
          for (g = new Array(u); u--; ) g[u] = h[u];
          return g;
        }
        for (u = arguments.length, g = new Array(u); u--; ) g[u] = arguments[u];
        return g;
      }
      var Bt = typeof Symbol < "u" ? function(h) {
        return h[Symbol.toStringTag] === "AsyncFunction";
      } : function() {
        return !1;
      }, Oe = ["Unknown", "Constraint", "Data", "TransactionInactive", "ReadOnly", "Version", "NotFound", "InvalidState", "InvalidAccess", "Abort", "Timeout", "QuotaExceeded", "Syntax", "DataClone"], gt = ["Modify", "Bulk", "OpenFailed", "VersionChange", "Schema", "Upgrade", "InvalidTable", "MissingAPI", "NoSuchDatabase", "InvalidArgument", "SubTransaction", "Unsupported", "Internal", "DatabaseClosed", "PrematureCommit", "ForeignAwait"].concat(Oe), Gi = { VersionChanged: "Database version changed by other database connection", DatabaseClosed: "Database has been closed", Abort: "Transaction aborted", TransactionInactive: "Transaction has already completed or failed", MissingAPI: "IndexedDB API missing. Please visit https://tinyurl.com/y2uuvskb" };
      function _t(h, u) {
        this.name = h, this.message = u;
      }
      function $t(h, u) {
        return h + ". Errors: " + Object.keys(u).map(function(g) {
          return u[g].toString();
        }).filter(function(g, _, v) {
          return v.indexOf(g) === _;
        }).join(`
`);
      }
      function kt(h, u, g, _) {
        this.failures = u, this.failedKeys = _, this.successCount = g, this.message = $t(h, u);
      }
      function me(h, u) {
        this.name = "BulkError", this.failures = Object.keys(u).map(function(g) {
          return u[g];
        }), this.failuresByPos = u, this.message = $t(h, this.failures);
      }
      T(_t).from(Error).extend({ toString: function() {
        return this.name + ": " + this.message;
      } }), T(kt).from(_t), T(me).from(_t);
      var xt = gt.reduce(function(h, u) {
        return h[u] = u + "Error", h;
      }, {}), bi = _t, pe = gt.reduce(function(h, u) {
        var g = u + "Error";
        function _(v, C) {
          this.name = g, v ? typeof v == "string" ? (this.message = "".concat(v).concat(C ? `
 ` + C : ""), this.inner = C || null) : typeof v == "object" && (this.message = "".concat(v.name, " ").concat(v.message), this.inner = v) : (this.message = Gi[u] || g, this.inner = null);
        }
        return T(_).from(bi), h[u] = _, h;
      }, {});
      pe.Syntax = SyntaxError, pe.Type = TypeError, pe.Range = RangeError;
      var ei = Oe.reduce(function(h, u) {
        return h[u + "Error"] = pe[u], h;
      }, {}), It = gt.reduce(function(h, u) {
        return ["Syntax", "Type", "Range"].indexOf(u) === -1 && (h[u + "Error"] = pe[u]), h;
      }, {});
      function Pe() {
      }
      function Ct(h) {
        return h;
      }
      function qi(h, u) {
        return h == null || h === Ct ? u : function(g) {
          return u(h(g));
        };
      }
      function m(h, u) {
        return function() {
          h.apply(this, arguments), u.apply(this, arguments);
        };
      }
      function P(h, u) {
        return h === Pe ? u : function() {
          var g = h.apply(this, arguments);
          g !== void 0 && (arguments[0] = g);
          var _ = this.onsuccess, v = this.onerror;
          this.onsuccess = null, this.onerror = null;
          var C = u.apply(this, arguments);
          return _ && (this.onsuccess = this.onsuccess ? m(_, this.onsuccess) : _), v && (this.onerror = this.onerror ? m(v, this.onerror) : v), C !== void 0 ? C : g;
        };
      }
      function U(h, u) {
        return h === Pe ? u : function() {
          h.apply(this, arguments);
          var g = this.onsuccess, _ = this.onerror;
          this.onsuccess = this.onerror = null, u.apply(this, arguments), g && (this.onsuccess = this.onsuccess ? m(g, this.onsuccess) : g), _ && (this.onerror = this.onerror ? m(_, this.onerror) : _);
        };
      }
      function q(h, u) {
        return h === Pe ? u : function(g) {
          var _ = h.apply(this, arguments);
          a(g, _);
          var v = this.onsuccess, C = this.onerror;
          return this.onsuccess = null, this.onerror = null, g = u.apply(this, arguments), v && (this.onsuccess = this.onsuccess ? m(v, this.onsuccess) : v), C && (this.onerror = this.onerror ? m(C, this.onerror) : C), _ === void 0 ? g === void 0 ? void 0 : g : a(_, g);
        };
      }
      function ee(h, u) {
        return h === Pe ? u : function() {
          return u.apply(this, arguments) !== !1 && h.apply(this, arguments);
        };
      }
      function ue(h, u) {
        return h === Pe ? u : function() {
          var g = h.apply(this, arguments);
          if (g && typeof g.then == "function") {
            for (var _ = this, v = arguments.length, C = new Array(v); v--; ) C[v] = arguments[v];
            return g.then(function() {
              return u.apply(_, C);
            });
          }
          return u.apply(this, arguments);
        };
      }
      It.ModifyError = kt, It.DexieError = _t, It.BulkError = me;
      var X = typeof location < "u" && /^(http|https):\/\/(localhost|127\.0\.0\.1)/.test(location.href);
      function le(h) {
        X = h;
      }
      var ke = {}, ge = 100, Ue = typeof Promise > "u" ? [] : (function() {
        var h = Promise.resolve();
        if (typeof crypto > "u" || !crypto.subtle) return [h, c(h), h];
        var u = crypto.subtle.digest("SHA-512", new Uint8Array([0]));
        return [u, c(u), h];
      })(), Oe = Ue[0], gt = Ue[1], Ue = Ue[2], gt = gt && gt.then, ve = Oe && Oe.constructor, Ke = !!Ue, Tt = function(h, u) {
        Ce.push([h, u]), y && (queueMicrotask(Rc), y = !1);
      }, w = !0, y = !0, E = [], R = [], K = Ct, re = { id: "global", global: !0, ref: 0, unhandleds: [], onunhandled: Pe, pgp: !1, env: {}, finalize: Pe }, J = re, Ce = [], He = 0, rt = [];
      function Q(h) {
        if (typeof this != "object") throw new TypeError("Promises must be constructed via new");
        this._listeners = [], this._lib = !1;
        var u = this._PSD = J;
        if (typeof h != "function") {
          if (h !== ke) throw new TypeError("Not a function");
          return this._state = arguments[1], this._value = arguments[2], void (this._state === !1 && Dt(this, this._value));
        }
        this._state = null, this._value = null, ++u.ref, (function g(_, v) {
          try {
            v(function(C) {
              if (_._state === null) {
                if (C === _) throw new TypeError("A promise cannot be resolved with itself.");
                var S = _._lib && xi();
                C && typeof C.then == "function" ? g(_, function(A, D) {
                  C instanceof Q ? C._then(A, D) : C.then(A, D);
                }) : (_._state = !0, _._value = C, Pt(_)), S && Ci();
              }
            }, Dt.bind(null, _));
          } catch (C) {
            Dt(_, C);
          }
        })(this, h);
      }
      var Et = { get: function() {
        var h = J, u = kr;
        function g(_, v) {
          var C = this, S = !h.global && (h !== J || u !== kr), A = S && !Vt(), D = new Q(function(F, M) {
            Hn(C, new wi(Is(_, h, S, A), Is(v, h, S, A), F, M, h));
          });
          return this._consoleTask && (D._consoleTask = this._consoleTask), D;
        }
        return g.prototype = ke, g;
      }, set: function(h) {
        x(this, "then", h && h.prototype === ke ? Et : { get: function() {
          return h;
        }, set: Et.set });
      } };
      function wi(h, u, g, _, v) {
        this.onFulfilled = typeof h == "function" ? h : null, this.onRejected = typeof u == "function" ? u : null, this.resolve = g, this.reject = _, this.psd = v;
      }
      function Dt(h, u) {
        var g, _;
        R.push(u), h._state === null && (g = h._lib && xi(), u = K(u), h._state = !1, h._value = u, _ = h, E.some(function(v) {
          return v._value === _._value;
        }) || E.push(_), Pt(h), g && Ci());
      }
      function Pt(h) {
        var u = h._listeners;
        h._listeners = [];
        for (var g = 0, _ = u.length; g < _; ++g) Hn(h, u[g]);
        var v = h._PSD;
        --v.ref || v.finalize(), He === 0 && (++He, Tt(function() {
          --He == 0 && Vn();
        }, []));
      }
      function Hn(h, u) {
        if (h._state !== null) {
          var g = h._state ? u.onFulfilled : u.onRejected;
          if (g === null) return (h._state ? u.resolve : u.reject)(h._value);
          ++u.psd.ref, ++He, Tt(Sc, [g, h, u]);
        } else h._listeners.push(u);
      }
      function Sc(h, u, g) {
        try {
          var _, v = u._value;
          !u._state && R.length && (R = []), _ = X && u._consoleTask ? u._consoleTask.run(function() {
            return h(v);
          }) : h(v), u._state || R.indexOf(v) !== -1 || (function(C) {
            for (var S = E.length; S; ) if (E[--S]._value === C._value) return E.splice(S, 1);
          })(u), g.resolve(_);
        } catch (C) {
          g.reject(C);
        } finally {
          --He == 0 && Vn(), --g.psd.ref || g.psd.finalize();
        }
      }
      function Rc() {
        ti(re, function() {
          xi() && Ci();
        });
      }
      function xi() {
        var h = w;
        return y = w = !1, h;
      }
      function Ci() {
        var h, u, g;
        do
          for (; 0 < Ce.length; ) for (h = Ce, Ce = [], g = h.length, u = 0; u < g; ++u) {
            var _ = h[u];
            _[0].apply(null, _[1]);
          }
        while (0 < Ce.length);
        y = w = !0;
      }
      function Vn() {
        var h = E;
        E = [], h.forEach(function(_) {
          _._PSD.onunhandled.call(null, _._value, _);
        });
        for (var u = rt.slice(0), g = u.length; g; ) u[--g]();
      }
      function Sr(h) {
        return new Q(ke, !1, h);
      }
      function Me(h, u) {
        var g = J;
        return function() {
          var _ = xi(), v = J;
          try {
            return Ut(g, !0), h.apply(this, arguments);
          } catch (C) {
            u && u(C);
          } finally {
            Ut(v, !1), _ && Ci();
          }
        };
      }
      p(Q.prototype, { then: Et, _then: function(h, u) {
        Hn(this, new wi(null, null, h, u, J));
      }, catch: function(h) {
        if (arguments.length === 1) return this.then(null, h);
        var u = h, g = arguments[1];
        return typeof u == "function" ? this.then(null, function(_) {
          return (_ instanceof u ? g : Sr)(_);
        }) : this.then(null, function(_) {
          return (_ && _.name === u ? g : Sr)(_);
        });
      }, finally: function(h) {
        return this.then(function(u) {
          return Q.resolve(h()).then(function() {
            return u;
          });
        }, function(u) {
          return Q.resolve(h()).then(function() {
            return Sr(u);
          });
        });
      }, timeout: function(h, u) {
        var g = this;
        return h < 1 / 0 ? new Q(function(_, v) {
          var C = setTimeout(function() {
            return v(new pe.Timeout(u));
          }, h);
          g.then(_, v).finally(clearTimeout.bind(null, C));
        }) : this;
      } }), typeof Symbol < "u" && Symbol.toStringTag && x(Q.prototype, Symbol.toStringTag, "Dexie.Promise"), re.env = ks(), p(Q, { all: function() {
        var h = ot.apply(null, arguments).map(Ir);
        return new Q(function(u, g) {
          h.length === 0 && u([]);
          var _ = h.length;
          h.forEach(function(v, C) {
            return Q.resolve(v).then(function(S) {
              h[C] = S, --_ || u(h);
            }, g);
          });
        });
      }, resolve: function(h) {
        return h instanceof Q ? h : h && typeof h.then == "function" ? new Q(function(u, g) {
          h.then(u, g);
        }) : new Q(ke, !0, h);
      }, reject: Sr, race: function() {
        var h = ot.apply(null, arguments).map(Ir);
        return new Q(function(u, g) {
          h.map(function(_) {
            return Q.resolve(_).then(u, g);
          });
        });
      }, PSD: { get: function() {
        return J;
      }, set: function(h) {
        return J = h;
      } }, totalEchoes: { get: function() {
        return kr;
      } }, newPSD: Ht, usePSD: ti, scheduler: { get: function() {
        return Tt;
      }, set: function(h) {
        Tt = h;
      } }, rejectionMapper: { get: function() {
        return K;
      }, set: function(h) {
        K = h;
      } }, follow: function(h, u) {
        return new Q(function(g, _) {
          return Ht(function(v, C) {
            var S = J;
            S.unhandleds = [], S.onunhandled = C, S.finalize = m(function() {
              var A, D = this;
              A = function() {
                D.unhandleds.length === 0 ? v() : C(D.unhandleds[0]);
              }, rt.push(function F() {
                A(), rt.splice(rt.indexOf(F), 1);
              }), ++He, Tt(function() {
                --He == 0 && Vn();
              }, []);
            }, S.finalize), h();
          }, u, g, _);
        });
      } }), ve && (ve.allSettled && x(Q, "allSettled", function() {
        var h = ot.apply(null, arguments).map(Ir);
        return new Q(function(u) {
          h.length === 0 && u([]);
          var g = h.length, _ = new Array(g);
          h.forEach(function(v, C) {
            return Q.resolve(v).then(function(S) {
              return _[C] = { status: "fulfilled", value: S };
            }, function(S) {
              return _[C] = { status: "rejected", reason: S };
            }).then(function() {
              return --g || u(_);
            });
          });
        });
      }), ve.any && typeof AggregateError < "u" && x(Q, "any", function() {
        var h = ot.apply(null, arguments).map(Ir);
        return new Q(function(u, g) {
          h.length === 0 && g(new AggregateError([]));
          var _ = h.length, v = new Array(_);
          h.forEach(function(C, S) {
            return Q.resolve(C).then(function(A) {
              return u(A);
            }, function(A) {
              v[S] = A, --_ || g(new AggregateError(v));
            });
          });
        });
      }), ve.withResolvers && (Q.withResolvers = ve.withResolvers));
      var Ye = { awaits: 0, echoes: 0, id: 0 }, Ac = 0, Rr = [], Ar = 0, kr = 0, kc = 0;
      function Ht(h, u, g, _) {
        var v = J, C = Object.create(v);
        return C.parent = v, C.ref = 0, C.global = !1, C.id = ++kc, re.env, C.env = Ke ? { Promise: Q, PromiseProp: { value: Q, configurable: !0, writable: !0 }, all: Q.all, race: Q.race, allSettled: Q.allSettled, any: Q.any, resolve: Q.resolve, reject: Q.reject } : {}, u && a(C, u), ++v.ref, C.finalize = function() {
          --this.parent.ref || this.parent.finalize();
        }, _ = ti(C, h, g, _), C.ref === 0 && C.finalize(), _;
      }
      function Ti() {
        return Ye.id || (Ye.id = ++Ac), ++Ye.awaits, Ye.echoes += ge, Ye.id;
      }
      function Vt() {
        return !!Ye.awaits && (--Ye.awaits == 0 && (Ye.id = 0), Ye.echoes = Ye.awaits * ge, !0);
      }
      function Ir(h) {
        return Ye.echoes && h && h.constructor === ve ? (Ti(), h.then(function(u) {
          return Vt(), u;
        }, function(u) {
          return Vt(), We(u);
        })) : h;
      }
      function Ic() {
        var h = Rr[Rr.length - 1];
        Rr.pop(), Ut(h, !1);
      }
      function Ut(h, u) {
        var g, _ = J;
        (u ? !Ye.echoes || Ar++ && h === J : !Ar || --Ar && h === J) || queueMicrotask(u ? (function(v) {
          ++kr, Ye.echoes && --Ye.echoes != 0 || (Ye.echoes = Ye.awaits = Ye.id = 0), Rr.push(J), Ut(v, !0);
        }).bind(null, h) : Ic), h !== J && (J = h, _ === re && (re.env = ks()), Ke && (g = re.env.Promise, u = h.env, (_.global || h.global) && (Object.defineProperty(r, "Promise", u.PromiseProp), g.all = u.all, g.race = u.race, g.resolve = u.resolve, g.reject = u.reject, u.allSettled && (g.allSettled = u.allSettled), u.any && (g.any = u.any))));
      }
      function ks() {
        var h = r.Promise;
        return Ke ? { Promise: h, PromiseProp: Object.getOwnPropertyDescriptor(r, "Promise"), all: h.all, race: h.race, allSettled: h.allSettled, any: h.any, resolve: h.resolve, reject: h.reject } : {};
      }
      function ti(h, u, g, _, v) {
        var C = J;
        try {
          return Ut(h, !0), u(g, _, v);
        } finally {
          Ut(C, !1);
        }
      }
      function Is(h, u, g, _) {
        return typeof h != "function" ? h : function() {
          var v = J;
          g && Ti(), Ut(u, !0);
          try {
            return h.apply(this, arguments);
          } finally {
            Ut(v, !1), _ && queueMicrotask(Vt);
          }
        };
      }
      function Un(h) {
        Promise === ve && Ye.echoes === 0 ? Ar === 0 ? h() : enqueueNativeMicroTask(h) : setTimeout(h, 0);
      }
      ("" + gt).indexOf("[native code]") === -1 && (Ti = Vt = Pe);
      var We = Q.reject, ii = "￿", Ot = "Invalid key provided. Keys must be of type string, number, Date or Array<string | number | Date>.", Ds = "String expected.", Ei = [], Dr = "__dbnames", Wn = "readonly", jn = "readwrite";
      function ri(h, u) {
        return h ? u ? function() {
          return h.apply(this, arguments) && u.apply(this, arguments);
        } : h : u;
      }
      var Os = { type: 3, lower: -1 / 0, lowerOpen: !1, upper: [[]], upperOpen: !1 };
      function Or(h) {
        return typeof h != "string" || /\./.test(h) ? function(u) {
          return u;
        } : function(u) {
          return u[h] === void 0 && h in u && delete (u = De(u))[h], u;
        };
      }
      function zs() {
        throw pe.Type("Entity instances must never be new:ed. Instances are generated by the framework bypassing the constructor.");
      }
      function we(h, u) {
        try {
          var g = Fs(h), _ = Fs(u);
          if (g !== _) return g === "Array" ? 1 : _ === "Array" ? -1 : g === "binary" ? 1 : _ === "binary" ? -1 : g === "string" ? 1 : _ === "string" ? -1 : g === "Date" ? 1 : _ !== "Date" ? NaN : -1;
          switch (g) {
            case "number":
            case "Date":
            case "string":
              return u < h ? 1 : h < u ? -1 : 0;
            case "binary":
              return (function(v, C) {
                for (var S = v.length, A = C.length, D = S < A ? S : A, F = 0; F < D; ++F) if (v[F] !== C[F]) return v[F] < C[F] ? -1 : 1;
                return S === A ? 0 : S < A ? -1 : 1;
              })(Ls(h), Ls(u));
            case "Array":
              return (function(v, C) {
                for (var S = v.length, A = C.length, D = S < A ? S : A, F = 0; F < D; ++F) {
                  var M = we(v[F], C[F]);
                  if (M !== 0) return M;
                }
                return S === A ? 0 : S < A ? -1 : 1;
              })(h, u);
          }
        } catch {
        }
        return NaN;
      }
      function Fs(h) {
        var u = typeof h;
        return u != "object" ? u : ArrayBuffer.isView(h) ? "binary" : (h = lt(h), h === "ArrayBuffer" ? "binary" : h);
      }
      function Ls(h) {
        return h instanceof Uint8Array ? h : ArrayBuffer.isView(h) ? new Uint8Array(h.buffer, h.byteOffset, h.byteLength) : new Uint8Array(h);
      }
      function zr(h, u, g) {
        var _ = h.schema.yProps;
        return _ ? (u && 0 < g.numFailures && (u = u.filter(function(v, C) {
          return !g.failures[C];
        })), Promise.all(_.map(function(v) {
          return v = v.updatesTable, u ? h.db.table(v).where("k").anyOf(u).delete() : h.db.table(v).clear();
        })).then(function() {
          return g;
        })) : g;
      }
      var Ms = (Le.prototype._trans = function(h, u, g) {
        var _ = this._tx || J.trans, v = this.name, C = X && typeof console < "u" && console.createTask && void 0;
        function S(F, M, k) {
          if (!k.schema[v]) throw new pe.NotFound("Table " + v + " not part of transaction");
          return u(k.idbtrans, k);
        }
        var A = xi();
        try {
          var D = _ && _.db._novip === this.db._novip ? _ === J.trans ? _._promise(h, S, g) : Ht(function() {
            return _._promise(h, S, g);
          }, { trans: _, transless: J.transless || J }) : (function F(M, k, H, O) {
            if (M.idbdb && (M._state.openComplete || J.letThrough || M._vip)) {
              var L = M._createTransaction(k, H, M._dbSchema);
              try {
                L.create(), M._state.PR1398_maxLoop = 3;
              } catch (B) {
                return B.name === xt.InvalidState && M.isOpen() && 0 < --M._state.PR1398_maxLoop ? (M.close({ disableAutoOpen: !1 }), M.open().then(function() {
                  return F(M, k, H, O);
                })) : We(B);
              }
              return L._promise(k, function(B, N) {
                return Ht(function() {
                  return J.trans = L, O(B, N, L);
                });
              }).then(function(B) {
                if (k === "readwrite") try {
                  L.idbtrans.commit();
                } catch {
                }
                return k === "readonly" ? B : L._completion.then(function() {
                  return B;
                });
              });
            }
            if (M._state.openComplete) return We(new pe.DatabaseClosed(M._state.dbOpenError));
            if (!M._state.isBeingOpened) {
              if (!M._state.autoOpen) return We(new pe.DatabaseClosed());
              M.open().catch(Pe);
            }
            return M._state.dbReadyPromise.then(function() {
              return F(M, k, H, O);
            });
          })(this.db, h, [this.name], S);
          return C && (D._consoleTask = C, D = D.catch(function(F) {
            return We(F);
          })), D;
        } finally {
          A && Ci();
        }
      }, Le.prototype.get = function(h, u) {
        var g = this;
        return h && h.constructor === Object ? this.where(h).first(u) : h == null ? We(new pe.Type("Invalid argument to Table.get()")) : this._trans("readonly", function(_) {
          return g.core.get({ trans: _, key: h }).then(function(v) {
            return g.hook.reading.fire(v);
          });
        }).then(u);
      }, Le.prototype.where = function(h) {
        if (typeof h == "string") return new this.db.WhereClause(this, h);
        if (l(h)) return new this.db.WhereClause(this, "[".concat(h.join("+"), "]"));
        var u = o(h);
        if (u.length === 1) return this.where(u[0]).equals(h[u[0]]);
        var g = this.schema.indexes.concat(this.schema.primKey).filter(function(A) {
          if (A.compound && u.every(function(F) {
            return 0 <= A.keyPath.indexOf(F);
          })) {
            for (var D = 0; D < u.length; ++D) if (u.indexOf(A.keyPath[D]) === -1) return !1;
            return !0;
          }
          return !1;
        }).sort(function(A, D) {
          return A.keyPath.length - D.keyPath.length;
        })[0];
        if (g && this.db._maxKey !== ii) {
          var C = g.keyPath.slice(0, u.length);
          return this.where(C).equals(C.map(function(D) {
            return h[D];
          }));
        }
        var _ = this.schema.idxByName;
        function v(A, D) {
          return we(A, D) === 0;
        }
        var S = u.reduce(function(k, D) {
          var F = k[0], M = k[1], k = _[D], H = h[D];
          return [F || k, F || !k ? ri(M, k && k.multi ? function(O) {
            return O = se(O, D), l(O) && O.some(function(L) {
              return v(H, L);
            });
          } : function(O) {
            return v(H, se(O, D));
          }) : M];
        }, [null, null]), C = S[0], S = S[1];
        return C ? this.where(C.name).equals(h[C.keyPath]).filter(S) : g ? this.filter(S) : this.where(u).equals("");
      }, Le.prototype.filter = function(h) {
        return this.toCollection().and(h);
      }, Le.prototype.count = function(h) {
        return this.toCollection().count(h);
      }, Le.prototype.offset = function(h) {
        return this.toCollection().offset(h);
      }, Le.prototype.limit = function(h) {
        return this.toCollection().limit(h);
      }, Le.prototype.each = function(h) {
        return this.toCollection().each(h);
      }, Le.prototype.toArray = function(h) {
        return this.toCollection().toArray(h);
      }, Le.prototype.toCollection = function() {
        return new this.db.Collection(new this.db.WhereClause(this));
      }, Le.prototype.orderBy = function(h) {
        return new this.db.Collection(new this.db.WhereClause(this, l(h) ? "[".concat(h.join("+"), "]") : h));
      }, Le.prototype.reverse = function() {
        return this.toCollection().reverse();
      }, Le.prototype.mapToClass = function(h) {
        var u, g = this.db, _ = this.name;
        function v() {
          return u !== null && u.apply(this, arguments) || this;
        }
        (this.schema.mappedClass = h).prototype instanceof zs && ((function(D, F) {
          if (typeof F != "function" && F !== null) throw new TypeError("Class extends value " + String(F) + " is not a constructor or null");
          function M() {
            this.constructor = D;
          }
          e(D, F), D.prototype = F === null ? Object.create(F) : (M.prototype = F.prototype, new M());
        })(v, u = h), Object.defineProperty(v.prototype, "db", { get: function() {
          return g;
        }, enumerable: !1, configurable: !0 }), v.prototype.table = function() {
          return _;
        }, h = v);
        for (var C = /* @__PURE__ */ new Set(), S = h.prototype; S; S = c(S)) Object.getOwnPropertyNames(S).forEach(function(D) {
          return C.add(D);
        });
        function A(D) {
          if (!D) return D;
          var F, M = Object.create(h.prototype);
          for (F in D) if (!C.has(F)) try {
            M[F] = D[F];
          } catch {
          }
          return M;
        }
        return this.schema.readHook && this.hook.reading.unsubscribe(this.schema.readHook), this.schema.readHook = A, this.hook("reading", A), h;
      }, Le.prototype.defineClass = function() {
        return this.mapToClass(function(h) {
          a(this, h);
        });
      }, Le.prototype.add = function(h, u) {
        var g = this, _ = this.schema.primKey, v = _.auto, C = _.keyPath, S = h;
        return C && v && (S = Or(C)(h)), this._trans("readwrite", function(A) {
          return g.core.mutate({ trans: A, type: "add", keys: u != null ? [u] : null, values: [S] });
        }).then(function(A) {
          return A.numFailures ? Q.reject(A.failures[0]) : A.lastResult;
        }).then(function(A) {
          if (C) try {
            fe(h, C, A);
          } catch {
          }
          return A;
        });
      }, Le.prototype.update = function(h, u) {
        return typeof h != "object" || l(h) ? this.where(":id").equals(h).modify(u) : (h = se(h, this.schema.primKey.keyPath), h === void 0 ? We(new pe.InvalidArgument("Given object does not contain its primary key")) : this.where(":id").equals(h).modify(u));
      }, Le.prototype.put = function(h, u) {
        var g = this, _ = this.schema.primKey, v = _.auto, C = _.keyPath, S = h;
        return C && v && (S = Or(C)(h)), this._trans("readwrite", function(A) {
          return g.core.mutate({ trans: A, type: "put", values: [S], keys: u != null ? [u] : null });
        }).then(function(A) {
          return A.numFailures ? Q.reject(A.failures[0]) : A.lastResult;
        }).then(function(A) {
          if (C) try {
            fe(h, C, A);
          } catch {
          }
          return A;
        });
      }, Le.prototype.delete = function(h) {
        var u = this;
        return this._trans("readwrite", function(g) {
          return u.core.mutate({ trans: g, type: "delete", keys: [h] }).then(function(_) {
            return zr(u, [h], _);
          }).then(function(_) {
            return _.numFailures ? Q.reject(_.failures[0]) : void 0;
          });
        });
      }, Le.prototype.clear = function() {
        var h = this;
        return this._trans("readwrite", function(u) {
          return h.core.mutate({ trans: u, type: "deleteRange", range: Os }).then(function(g) {
            return zr(h, null, g);
          });
        }).then(function(u) {
          return u.numFailures ? Q.reject(u.failures[0]) : void 0;
        });
      }, Le.prototype.bulkGet = function(h) {
        var u = this;
        return this._trans("readonly", function(g) {
          return u.core.getMany({ keys: h, trans: g }).then(function(_) {
            return _.map(function(v) {
              return u.hook.reading.fire(v);
            });
          });
        });
      }, Le.prototype.bulkAdd = function(h, u, g) {
        var _ = this, v = Array.isArray(u) ? u : void 0, C = (g = g || (v ? void 0 : u)) ? g.allKeys : void 0;
        return this._trans("readwrite", function(S) {
          var F = _.schema.primKey, A = F.auto, F = F.keyPath;
          if (F && v) throw new pe.InvalidArgument("bulkAdd(): keys argument invalid on tables with inbound keys");
          if (v && v.length !== h.length) throw new pe.InvalidArgument("Arguments objects and keys must have the same length");
          var D = h.length, F = F && A ? h.map(Or(F)) : h;
          return _.core.mutate({ trans: S, type: "add", keys: v, values: F, wantResults: C }).then(function(L) {
            var k = L.numFailures, H = L.results, O = L.lastResult, L = L.failures;
            if (k === 0) return C ? H : O;
            throw new me("".concat(_.name, ".bulkAdd(): ").concat(k, " of ").concat(D, " operations failed"), L);
          });
        });
      }, Le.prototype.bulkPut = function(h, u, g) {
        var _ = this, v = Array.isArray(u) ? u : void 0, C = (g = g || (v ? void 0 : u)) ? g.allKeys : void 0;
        return this._trans("readwrite", function(S) {
          var F = _.schema.primKey, A = F.auto, F = F.keyPath;
          if (F && v) throw new pe.InvalidArgument("bulkPut(): keys argument invalid on tables with inbound keys");
          if (v && v.length !== h.length) throw new pe.InvalidArgument("Arguments objects and keys must have the same length");
          var D = h.length, F = F && A ? h.map(Or(F)) : h;
          return _.core.mutate({ trans: S, type: "put", keys: v, values: F, wantResults: C }).then(function(L) {
            var k = L.numFailures, H = L.results, O = L.lastResult, L = L.failures;
            if (k === 0) return C ? H : O;
            throw new me("".concat(_.name, ".bulkPut(): ").concat(k, " of ").concat(D, " operations failed"), L);
          });
        });
      }, Le.prototype.bulkUpdate = function(h) {
        var u = this, g = this.core, _ = h.map(function(S) {
          return S.key;
        }), v = h.map(function(S) {
          return S.changes;
        }), C = [];
        return this._trans("readwrite", function(S) {
          return g.getMany({ trans: S, keys: _, cache: "clone" }).then(function(A) {
            var D = [], F = [];
            h.forEach(function(k, H) {
              var O = k.key, L = k.changes, B = A[H];
              if (B) {
                for (var N = 0, V = Object.keys(L); N < V.length; N++) {
                  var G = V[N], Z = L[G];
                  if (G === u.schema.primKey.keyPath) {
                    if (we(Z, O) !== 0) throw new pe.Constraint("Cannot update primary key in bulkUpdate()");
                  } else fe(B, G, Z);
                }
                C.push(H), D.push(O), F.push(B);
              }
            });
            var M = D.length;
            return g.mutate({ trans: S, type: "put", keys: D, values: F, updates: { keys: _, changeSpecs: v } }).then(function(k) {
              var H = k.numFailures, O = k.failures;
              if (H === 0) return M;
              for (var L = 0, B = Object.keys(O); L < B.length; L++) {
                var N, V = B[L], G = C[Number(V)];
                G != null && (N = O[V], delete O[V], O[G] = N);
              }
              throw new me("".concat(u.name, ".bulkUpdate(): ").concat(H, " of ").concat(M, " operations failed"), O);
            });
          });
        });
      }, Le.prototype.bulkDelete = function(h) {
        var u = this, g = h.length;
        return this._trans("readwrite", function(_) {
          return u.core.mutate({ trans: _, type: "delete", keys: h }).then(function(v) {
            return zr(u, h, v);
          });
        }).then(function(S) {
          var v = S.numFailures, C = S.lastResult, S = S.failures;
          if (v === 0) return C;
          throw new me("".concat(u.name, ".bulkDelete(): ").concat(v, " of ").concat(g, " operations failed"), S);
        });
      }, Le);
      function Le() {
      }
      function Ki(h) {
        function u(S, A) {
          if (A) {
            for (var D = arguments.length, F = new Array(D - 1); --D; ) F[D - 1] = arguments[D];
            return g[S].subscribe.apply(null, F), h;
          }
          if (typeof S == "string") return g[S];
        }
        var g = {};
        u.addEventType = C;
        for (var _ = 1, v = arguments.length; _ < v; ++_) C(arguments[_]);
        return u;
        function C(S, A, D) {
          if (typeof S != "object") {
            var F;
            A = A || ee;
            var M = { subscribers: [], fire: D = D || Pe, subscribe: function(k) {
              M.subscribers.indexOf(k) === -1 && (M.subscribers.push(k), M.fire = A(M.fire, k));
            }, unsubscribe: function(k) {
              M.subscribers = M.subscribers.filter(function(H) {
                return H !== k;
              }), M.fire = M.subscribers.reduce(A, D);
            } };
            return g[S] = u[S] = M;
          }
          o(F = S).forEach(function(k) {
            var H = F[k];
            if (l(H)) C(k, F[k][0], F[k][1]);
            else {
              if (H !== "asap") throw new pe.InvalidArgument("Invalid event config");
              var O = C(k, Ct, function() {
                for (var L = arguments.length, B = new Array(L); L--; ) B[L] = arguments[L];
                O.subscribers.forEach(function(N) {
                  ae(function() {
                    N.apply(null, B);
                  });
                });
              });
            }
          });
        }
      }
      function Zi(h, u) {
        return T(u).from({ prototype: h }), u;
      }
      function Pi(h, u) {
        return !(h.filter || h.algorithm || h.or) && (u ? h.justLimit : !h.replayFilter);
      }
      function Gn(h, u) {
        h.filter = ri(h.filter, u);
      }
      function qn(h, u, g) {
        var _ = h.replayFilter;
        h.replayFilter = _ ? function() {
          return ri(_(), u());
        } : u, h.justLimit = g && !_;
      }
      function Fr(h, u) {
        if (h.isPrimKey) return u.primaryKey;
        var g = u.getIndexByKeyPath(h.index);
        if (!g) throw new pe.Schema("KeyPath " + h.index + " on object store " + u.name + " is not indexed");
        return g;
      }
      function Ns(h, u, g) {
        var _ = Fr(h, u.schema);
        return u.openCursor({ trans: g, values: !h.keysOnly, reverse: h.dir === "prev", unique: !!h.unique, query: { index: _, range: h.range } });
      }
      function Lr(h, u, g, _) {
        var v = h.replayFilter ? ri(h.filter, h.replayFilter()) : h.filter;
        if (h.or) {
          var C = {}, S = function(A, D, F) {
            var M, k;
            v && !v(D, F, function(H) {
              return D.stop(H);
            }, function(H) {
              return D.fail(H);
            }) || ((k = "" + (M = D.primaryKey)) == "[object ArrayBuffer]" && (k = "" + new Uint8Array(M)), f(C, k) || (C[k] = !0, u(A, D, F)));
          };
          return Promise.all([h.or._iterate(S, g), Bs(Ns(h, _, g), h.algorithm, S, !h.keysOnly && h.valueMapper)]);
        }
        return Bs(Ns(h, _, g), ri(h.algorithm, v), u, !h.keysOnly && h.valueMapper);
      }
      function Bs(h, u, g, _) {
        var v = Me(_ ? function(C, S, A) {
          return g(_(C), S, A);
        } : g);
        return h.then(function(C) {
          if (C) return C.start(function() {
            var S = function() {
              return C.continue();
            };
            u && !u(C, function(A) {
              return S = A;
            }, function(A) {
              C.stop(A), S = Pe;
            }, function(A) {
              C.fail(A), S = Pe;
            }) || v(C.value, C, function(A) {
              return S = A;
            }), S();
          });
        });
      }
      var Yi = (Hs.prototype.execute = function(h) {
        var u = this["@@propmod"];
        if (u.add !== void 0) {
          var g = u.add;
          if (l(g)) return i(i([], l(h) ? h : [], !0), g).sort();
          if (typeof g == "number") return (Number(h) || 0) + g;
          if (typeof g == "bigint") try {
            return BigInt(h) + g;
          } catch {
            return BigInt(0) + g;
          }
          throw new TypeError("Invalid term ".concat(g));
        }
        if (u.remove !== void 0) {
          var _ = u.remove;
          if (l(_)) return l(h) ? h.filter(function(v) {
            return !_.includes(v);
          }).sort() : [];
          if (typeof _ == "number") return Number(h) - _;
          if (typeof _ == "bigint") try {
            return BigInt(h) - _;
          } catch {
            return BigInt(0) - _;
          }
          throw new TypeError("Invalid subtrahend ".concat(_));
        }
        return g = (g = u.replacePrefix) === null || g === void 0 ? void 0 : g[0], g && typeof h == "string" && h.startsWith(g) ? u.replacePrefix[1] + h.substring(g.length) : h;
      }, Hs);
      function Hs(h) {
        this["@@propmod"] = h;
      }
      var Dc = (Se.prototype._read = function(h, u) {
        var g = this._ctx;
        return g.error ? g.table._trans(null, We.bind(null, g.error)) : g.table._trans("readonly", h).then(u);
      }, Se.prototype._write = function(h) {
        var u = this._ctx;
        return u.error ? u.table._trans(null, We.bind(null, u.error)) : u.table._trans("readwrite", h, "locked");
      }, Se.prototype._addAlgorithm = function(h) {
        var u = this._ctx;
        u.algorithm = ri(u.algorithm, h);
      }, Se.prototype._iterate = function(h, u) {
        return Lr(this._ctx, h, u, this._ctx.table.core);
      }, Se.prototype.clone = function(h) {
        var u = Object.create(this.constructor.prototype), g = Object.create(this._ctx);
        return h && a(g, h), u._ctx = g, u;
      }, Se.prototype.raw = function() {
        return this._ctx.valueMapper = null, this;
      }, Se.prototype.each = function(h) {
        var u = this._ctx;
        return this._read(function(g) {
          return Lr(u, h, g, u.table.core);
        });
      }, Se.prototype.count = function(h) {
        var u = this;
        return this._read(function(g) {
          var _ = u._ctx, v = _.table.core;
          if (Pi(_, !0)) return v.count({ trans: g, query: { index: Fr(_, v.schema), range: _.range } }).then(function(S) {
            return Math.min(S, _.limit);
          });
          var C = 0;
          return Lr(_, function() {
            return ++C, !1;
          }, g, v).then(function() {
            return C;
          });
        }).then(h);
      }, Se.prototype.sortBy = function(h, u) {
        var g = h.split(".").reverse(), _ = g[0], v = g.length - 1;
        function C(D, F) {
          return F ? C(D[g[F]], F - 1) : D[_];
        }
        var S = this._ctx.dir === "next" ? 1 : -1;
        function A(D, F) {
          return we(C(D, v), C(F, v)) * S;
        }
        return this.toArray(function(D) {
          return D.sort(A);
        }).then(u);
      }, Se.prototype.toArray = function(h) {
        var u = this;
        return this._read(function(g) {
          var _ = u._ctx;
          if (_.dir === "next" && Pi(_, !0) && 0 < _.limit) {
            var v = _.valueMapper, C = Fr(_, _.table.core.schema);
            return _.table.core.query({ trans: g, limit: _.limit, values: !0, query: { index: C, range: _.range } }).then(function(A) {
              return A = A.result, v ? A.map(v) : A;
            });
          }
          var S = [];
          return Lr(_, function(A) {
            return S.push(A);
          }, g, _.table.core).then(function() {
            return S;
          });
        }, h);
      }, Se.prototype.offset = function(h) {
        var u = this._ctx;
        return h <= 0 || (u.offset += h, Pi(u) ? qn(u, function() {
          var g = h;
          return function(_, v) {
            return g === 0 || (g === 1 ? --g : v(function() {
              _.advance(g), g = 0;
            }), !1);
          };
        }) : qn(u, function() {
          var g = h;
          return function() {
            return --g < 0;
          };
        })), this;
      }, Se.prototype.limit = function(h) {
        return this._ctx.limit = Math.min(this._ctx.limit, h), qn(this._ctx, function() {
          var u = h;
          return function(g, _, v) {
            return --u <= 0 && _(v), 0 <= u;
          };
        }, !0), this;
      }, Se.prototype.until = function(h, u) {
        return Gn(this._ctx, function(g, _, v) {
          return !h(g.value) || (_(v), u);
        }), this;
      }, Se.prototype.first = function(h) {
        return this.limit(1).toArray(function(u) {
          return u[0];
        }).then(h);
      }, Se.prototype.last = function(h) {
        return this.reverse().first(h);
      }, Se.prototype.filter = function(h) {
        var u;
        return Gn(this._ctx, function(g) {
          return h(g.value);
        }), (u = this._ctx).isMatch = ri(u.isMatch, h), this;
      }, Se.prototype.and = function(h) {
        return this.filter(h);
      }, Se.prototype.or = function(h) {
        return new this.db.WhereClause(this._ctx.table, h, this);
      }, Se.prototype.reverse = function() {
        return this._ctx.dir = this._ctx.dir === "prev" ? "next" : "prev", this._ondirectionchange && this._ondirectionchange(this._ctx.dir), this;
      }, Se.prototype.desc = function() {
        return this.reverse();
      }, Se.prototype.eachKey = function(h) {
        var u = this._ctx;
        return u.keysOnly = !u.isMatch, this.each(function(g, _) {
          h(_.key, _);
        });
      }, Se.prototype.eachUniqueKey = function(h) {
        return this._ctx.unique = "unique", this.eachKey(h);
      }, Se.prototype.eachPrimaryKey = function(h) {
        var u = this._ctx;
        return u.keysOnly = !u.isMatch, this.each(function(g, _) {
          h(_.primaryKey, _);
        });
      }, Se.prototype.keys = function(h) {
        var u = this._ctx;
        u.keysOnly = !u.isMatch;
        var g = [];
        return this.each(function(_, v) {
          g.push(v.key);
        }).then(function() {
          return g;
        }).then(h);
      }, Se.prototype.primaryKeys = function(h) {
        var u = this._ctx;
        if (u.dir === "next" && Pi(u, !0) && 0 < u.limit) return this._read(function(_) {
          var v = Fr(u, u.table.core.schema);
          return u.table.core.query({ trans: _, values: !1, limit: u.limit, query: { index: v, range: u.range } });
        }).then(function(_) {
          return _.result;
        }).then(h);
        u.keysOnly = !u.isMatch;
        var g = [];
        return this.each(function(_, v) {
          g.push(v.primaryKey);
        }).then(function() {
          return g;
        }).then(h);
      }, Se.prototype.uniqueKeys = function(h) {
        return this._ctx.unique = "unique", this.keys(h);
      }, Se.prototype.firstKey = function(h) {
        return this.limit(1).keys(function(u) {
          return u[0];
        }).then(h);
      }, Se.prototype.lastKey = function(h) {
        return this.reverse().firstKey(h);
      }, Se.prototype.distinct = function() {
        var h = this._ctx, h = h.index && h.table.schema.idxByName[h.index];
        if (!h || !h.multi) return this;
        var u = {};
        return Gn(this._ctx, function(v) {
          var _ = v.primaryKey.toString(), v = f(u, _);
          return u[_] = !0, !v;
        }), this;
      }, Se.prototype.modify = function(h) {
        var u = this, g = this._ctx;
        return this._write(function(_) {
          var v, C, S;
          S = typeof h == "function" ? h : (v = o(h), C = v.length, function(V) {
            for (var G = !1, Z = 0; Z < C; ++Z) {
              var Y = v[Z], $ = h[Y], ne = se(V, Y);
              $ instanceof Yi ? (fe(V, Y, $.execute(ne)), G = !0) : ne !== $ && (fe(V, Y, $), G = !0);
            }
            return G;
          });
          var A = g.table.core, k = A.schema.primaryKey, D = k.outbound, F = k.extractKey, M = 200, k = u.db._options.modifyChunkSize;
          k && (M = typeof k == "object" ? k[A.name] || k["*"] || 200 : k);
          function H(V, Y) {
            var Z = Y.failures, Y = Y.numFailures;
            L += V - Y;
            for (var $ = 0, ne = o(Z); $ < ne.length; $++) {
              var te = ne[$];
              O.push(Z[te]);
            }
          }
          var O = [], L = 0, B = [], N = h === Vs;
          return u.clone().primaryKeys().then(function(V) {
            function G(Y) {
              var $ = Math.min(M, V.length - Y), ne = V.slice(Y, Y + $);
              return (N ? Promise.resolve([]) : A.getMany({ trans: _, keys: ne, cache: "immutable" })).then(function(te) {
                var ie = [], he = [], ce = D ? [] : null, de = N ? ne : [];
                if (!N) for (var be = 0; be < $; ++be) {
                  var xe = te[be], ye = { value: De(xe), primKey: V[Y + be] };
                  S.call(ye, ye.value, ye) !== !1 && (ye.value == null ? de.push(V[Y + be]) : D || we(F(xe), F(ye.value)) === 0 ? (he.push(ye.value), D && ce.push(V[Y + be])) : (de.push(V[Y + be]), ie.push(ye.value)));
                }
                return Promise.resolve(0 < ie.length && A.mutate({ trans: _, type: "add", values: ie }).then(function(Ve) {
                  for (var _e in Ve.failures) de.splice(parseInt(_e), 1);
                  H(ie.length, Ve);
                })).then(function() {
                  return (0 < he.length || Z && typeof h == "object") && A.mutate({ trans: _, type: "put", keys: ce, values: he, criteria: Z, changeSpec: typeof h != "function" && h, isAdditionalChunk: 0 < Y }).then(function(Ve) {
                    return H(he.length, Ve);
                  });
                }).then(function() {
                  return (0 < de.length || Z && N) && A.mutate({ trans: _, type: "delete", keys: de, criteria: Z, isAdditionalChunk: 0 < Y }).then(function(Ve) {
                    return zr(g.table, de, Ve);
                  }).then(function(Ve) {
                    return H(de.length, Ve);
                  });
                }).then(function() {
                  return V.length > Y + $ && G(Y + M);
                });
              });
            }
            var Z = Pi(g) && g.limit === 1 / 0 && (typeof h != "function" || N) && { index: g.index, range: g.range };
            return G(0).then(function() {
              if (0 < O.length) throw new kt("Error modifying one or more objects", O, L, B);
              return V.length;
            });
          });
        });
      }, Se.prototype.delete = function() {
        var h = this._ctx, u = h.range;
        return !Pi(h) || h.table.schema.yProps || !h.isPrimKey && u.type !== 3 ? this.modify(Vs) : this._write(function(g) {
          var _ = h.table.core.schema.primaryKey, v = u;
          return h.table.core.count({ trans: g, query: { index: _, range: v } }).then(function(C) {
            return h.table.core.mutate({ trans: g, type: "deleteRange", range: v }).then(function(D) {
              var A = D.failures, D = D.numFailures;
              if (D) throw new kt("Could not delete some values", Object.keys(A).map(function(F) {
                return A[F];
              }), C - D);
              return C - D;
            });
          });
        });
      }, Se);
      function Se() {
      }
      var Vs = function(h, u) {
        return u.value = null;
      };
      function Oc(h, u) {
        return h < u ? -1 : h === u ? 0 : 1;
      }
      function zc(h, u) {
        return u < h ? -1 : h === u ? 0 : 1;
      }
      function dt(h, u, g) {
        return h = h instanceof Ws ? new h.Collection(h) : h, h._ctx.error = new (g || TypeError)(u), h;
      }
      function Si(h) {
        return new h.Collection(h, function() {
          return Us("");
        }).limit(0);
      }
      function Mr(h, u, g, _) {
        var v, C, S, A, D, F, M, k = g.length;
        if (!g.every(function(L) {
          return typeof L == "string";
        })) return dt(h, Ds);
        function H(L) {
          v = L === "next" ? function(N) {
            return N.toUpperCase();
          } : function(N) {
            return N.toLowerCase();
          }, C = L === "next" ? function(N) {
            return N.toLowerCase();
          } : function(N) {
            return N.toUpperCase();
          }, S = L === "next" ? Oc : zc;
          var B = g.map(function(N) {
            return { lower: C(N), upper: v(N) };
          }).sort(function(N, V) {
            return S(N.lower, V.lower);
          });
          A = B.map(function(N) {
            return N.upper;
          }), D = B.map(function(N) {
            return N.lower;
          }), M = (F = L) === "next" ? "" : _;
        }
        H("next"), h = new h.Collection(h, function() {
          return Wt(A[0], D[k - 1] + _);
        }), h._ondirectionchange = function(L) {
          H(L);
        };
        var O = 0;
        return h._addAlgorithm(function(L, B, N) {
          var V = L.key;
          if (typeof V != "string") return !1;
          var G = C(V);
          if (u(G, D, O)) return !0;
          for (var Z = null, Y = O; Y < k; ++Y) {
            var $ = (function(ne, te, ie, he, ce, de) {
              for (var be = Math.min(ne.length, he.length), xe = -1, ye = 0; ye < be; ++ye) {
                var Ve = te[ye];
                if (Ve !== he[ye]) return ce(ne[ye], ie[ye]) < 0 ? ne.substr(0, ye) + ie[ye] + ie.substr(ye + 1) : ce(ne[ye], he[ye]) < 0 ? ne.substr(0, ye) + he[ye] + ie.substr(ye + 1) : 0 <= xe ? ne.substr(0, xe) + te[xe] + ie.substr(xe + 1) : null;
                ce(ne[ye], Ve) < 0 && (xe = ye);
              }
              return be < he.length && de === "next" ? ne + ie.substr(ne.length) : be < ne.length && de === "prev" ? ne.substr(0, ie.length) : xe < 0 ? null : ne.substr(0, xe) + he[xe] + ie.substr(xe + 1);
            })(V, G, A[Y], D[Y], S, F);
            $ === null && Z === null ? O = Y + 1 : (Z === null || 0 < S(Z, $)) && (Z = $);
          }
          return B(Z !== null ? function() {
            L.continue(Z + M);
          } : N), !1;
        }), h;
      }
      function Wt(h, u, g, _) {
        return { type: 2, lower: h, upper: u, lowerOpen: g, upperOpen: _ };
      }
      function Us(h) {
        return { type: 1, lower: h, upper: h };
      }
      var Ws = (Object.defineProperty(Xe.prototype, "Collection", { get: function() {
        return this._ctx.table.db.Collection;
      }, enumerable: !1, configurable: !0 }), Xe.prototype.between = function(h, u, g, _) {
        g = g !== !1, _ = _ === !0;
        try {
          return 0 < this._cmp(h, u) || this._cmp(h, u) === 0 && (g || _) && (!g || !_) ? Si(this) : new this.Collection(this, function() {
            return Wt(h, u, !g, !_);
          });
        } catch {
          return dt(this, Ot);
        }
      }, Xe.prototype.equals = function(h) {
        return h == null ? dt(this, Ot) : new this.Collection(this, function() {
          return Us(h);
        });
      }, Xe.prototype.above = function(h) {
        return h == null ? dt(this, Ot) : new this.Collection(this, function() {
          return Wt(h, void 0, !0);
        });
      }, Xe.prototype.aboveOrEqual = function(h) {
        return h == null ? dt(this, Ot) : new this.Collection(this, function() {
          return Wt(h, void 0, !1);
        });
      }, Xe.prototype.below = function(h) {
        return h == null ? dt(this, Ot) : new this.Collection(this, function() {
          return Wt(void 0, h, !1, !0);
        });
      }, Xe.prototype.belowOrEqual = function(h) {
        return h == null ? dt(this, Ot) : new this.Collection(this, function() {
          return Wt(void 0, h);
        });
      }, Xe.prototype.startsWith = function(h) {
        return typeof h != "string" ? dt(this, Ds) : this.between(h, h + ii, !0, !0);
      }, Xe.prototype.startsWithIgnoreCase = function(h) {
        return h === "" ? this.startsWith(h) : Mr(this, function(u, g) {
          return u.indexOf(g[0]) === 0;
        }, [h], ii);
      }, Xe.prototype.equalsIgnoreCase = function(h) {
        return Mr(this, function(u, g) {
          return u === g[0];
        }, [h], "");
      }, Xe.prototype.anyOfIgnoreCase = function() {
        var h = ot.apply(mt, arguments);
        return h.length === 0 ? Si(this) : Mr(this, function(u, g) {
          return g.indexOf(u) !== -1;
        }, h, "");
      }, Xe.prototype.startsWithAnyOfIgnoreCase = function() {
        var h = ot.apply(mt, arguments);
        return h.length === 0 ? Si(this) : Mr(this, function(u, g) {
          return g.some(function(_) {
            return u.indexOf(_) === 0;
          });
        }, h, ii);
      }, Xe.prototype.anyOf = function() {
        var h = this, u = ot.apply(mt, arguments), g = this._cmp;
        try {
          u.sort(g);
        } catch {
          return dt(this, Ot);
        }
        if (u.length === 0) return Si(this);
        var _ = new this.Collection(this, function() {
          return Wt(u[0], u[u.length - 1]);
        });
        _._ondirectionchange = function(C) {
          g = C === "next" ? h._ascending : h._descending, u.sort(g);
        };
        var v = 0;
        return _._addAlgorithm(function(C, S, A) {
          for (var D = C.key; 0 < g(D, u[v]); ) if (++v === u.length) return S(A), !1;
          return g(D, u[v]) === 0 || (S(function() {
            C.continue(u[v]);
          }), !1);
        }), _;
      }, Xe.prototype.notEqual = function(h) {
        return this.inAnyRange([[-1 / 0, h], [h, this.db._maxKey]], { includeLowers: !1, includeUppers: !1 });
      }, Xe.prototype.noneOf = function() {
        var h = ot.apply(mt, arguments);
        if (h.length === 0) return new this.Collection(this);
        try {
          h.sort(this._ascending);
        } catch {
          return dt(this, Ot);
        }
        var u = h.reduce(function(g, _) {
          return g ? g.concat([[g[g.length - 1][1], _]]) : [[-1 / 0, _]];
        }, null);
        return u.push([h[h.length - 1], this.db._maxKey]), this.inAnyRange(u, { includeLowers: !1, includeUppers: !1 });
      }, Xe.prototype.inAnyRange = function(V, u) {
        var g = this, _ = this._cmp, v = this._ascending, C = this._descending, S = this._min, A = this._max;
        if (V.length === 0) return Si(this);
        if (!V.every(function(G) {
          return G[0] !== void 0 && G[1] !== void 0 && v(G[0], G[1]) <= 0;
        })) return dt(this, "First argument to inAnyRange() must be an Array of two-value Arrays [lower,upper] where upper must not be lower than lower", pe.InvalidArgument);
        var D = !u || u.includeLowers !== !1, F = u && u.includeUppers === !0, M, k = v;
        function H(G, Z) {
          return k(G[0], Z[0]);
        }
        try {
          (M = V.reduce(function(G, Z) {
            for (var Y = 0, $ = G.length; Y < $; ++Y) {
              var ne = G[Y];
              if (_(Z[0], ne[1]) < 0 && 0 < _(Z[1], ne[0])) {
                ne[0] = S(ne[0], Z[0]), ne[1] = A(ne[1], Z[1]);
                break;
              }
            }
            return Y === $ && G.push(Z), G;
          }, [])).sort(H);
        } catch {
          return dt(this, Ot);
        }
        var O = 0, L = F ? function(G) {
          return 0 < v(G, M[O][1]);
        } : function(G) {
          return 0 <= v(G, M[O][1]);
        }, B = D ? function(G) {
          return 0 < C(G, M[O][0]);
        } : function(G) {
          return 0 <= C(G, M[O][0]);
        }, N = L, V = new this.Collection(this, function() {
          return Wt(M[0][0], M[M.length - 1][1], !D, !F);
        });
        return V._ondirectionchange = function(G) {
          k = G === "next" ? (N = L, v) : (N = B, C), M.sort(H);
        }, V._addAlgorithm(function(G, Z, Y) {
          for (var $, ne = G.key; N(ne); ) if (++O === M.length) return Z(Y), !1;
          return !L($ = ne) && !B($) || (g._cmp(ne, M[O][1]) === 0 || g._cmp(ne, M[O][0]) === 0 || Z(function() {
            k === v ? G.continue(M[O][0]) : G.continue(M[O][1]);
          }), !1);
        }), V;
      }, Xe.prototype.startsWithAnyOf = function() {
        var h = ot.apply(mt, arguments);
        return h.every(function(u) {
          return typeof u == "string";
        }) ? h.length === 0 ? Si(this) : this.inAnyRange(h.map(function(u) {
          return [u, u + ii];
        })) : dt(this, "startsWithAnyOf() only works with strings");
      }, Xe);
      function Xe() {
      }
      function St(h) {
        return Me(function(u) {
          return Xi(u), h(u.target.error), !1;
        });
      }
      function Xi(h) {
        h.stopPropagation && h.stopPropagation(), h.preventDefault && h.preventDefault();
      }
      var Qi = "storagemutated", Kn = "x-storagemutated-1", jt = Ki(null, Qi), Fc = (Rt.prototype._lock = function() {
        return oe(!J.global), ++this._reculock, this._reculock !== 1 || J.global || (J.lockOwnerFor = this), this;
      }, Rt.prototype._unlock = function() {
        if (oe(!J.global), --this._reculock == 0) for (J.global || (J.lockOwnerFor = null); 0 < this._blockedFuncs.length && !this._locked(); ) {
          var h = this._blockedFuncs.shift();
          try {
            ti(h[1], h[0]);
          } catch {
          }
        }
        return this;
      }, Rt.prototype._locked = function() {
        return this._reculock && J.lockOwnerFor !== this;
      }, Rt.prototype.create = function(h) {
        var u = this;
        if (!this.mode) return this;
        var g = this.db.idbdb, _ = this.db._state.dbOpenError;
        if (oe(!this.idbtrans), !h && !g) switch (_ && _.name) {
          case "DatabaseClosedError":
            throw new pe.DatabaseClosed(_);
          case "MissingAPIError":
            throw new pe.MissingAPI(_.message, _);
          default:
            throw new pe.OpenFailed(_);
        }
        if (!this.active) throw new pe.TransactionInactive();
        return oe(this._completion._state === null), (h = this.idbtrans = h || (this.db.core || g).transaction(this.storeNames, this.mode, { durability: this.chromeTransactionDurability })).onerror = Me(function(v) {
          Xi(v), u._reject(h.error);
        }), h.onabort = Me(function(v) {
          Xi(v), u.active && u._reject(new pe.Abort(h.error)), u.active = !1, u.on("abort").fire(v);
        }), h.oncomplete = Me(function() {
          u.active = !1, u._resolve(), "mutatedParts" in h && jt.storagemutated.fire(h.mutatedParts);
        }), this;
      }, Rt.prototype._promise = function(h, u, g) {
        var _ = this;
        if (h === "readwrite" && this.mode !== "readwrite") return We(new pe.ReadOnly("Transaction is readonly"));
        if (!this.active) return We(new pe.TransactionInactive());
        if (this._locked()) return new Q(function(C, S) {
          _._blockedFuncs.push([function() {
            _._promise(h, u, g).then(C, S);
          }, J]);
        });
        if (g) return Ht(function() {
          var C = new Q(function(S, A) {
            _._lock();
            var D = u(S, A, _);
            D && D.then && D.then(S, A);
          });
          return C.finally(function() {
            return _._unlock();
          }), C._lib = !0, C;
        });
        var v = new Q(function(C, S) {
          var A = u(C, S, _);
          A && A.then && A.then(C, S);
        });
        return v._lib = !0, v;
      }, Rt.prototype._root = function() {
        return this.parent ? this.parent._root() : this;
      }, Rt.prototype.waitFor = function(h) {
        var u, g = this._root(), _ = Q.resolve(h);
        g._waitingFor ? g._waitingFor = g._waitingFor.then(function() {
          return _;
        }) : (g._waitingFor = _, g._waitingQueue = [], u = g.idbtrans.objectStore(g.storeNames[0]), (function C() {
          for (++g._spinCount; g._waitingQueue.length; ) g._waitingQueue.shift()();
          g._waitingFor && (u.get(-1 / 0).onsuccess = C);
        })());
        var v = g._waitingFor;
        return new Q(function(C, S) {
          _.then(function(A) {
            return g._waitingQueue.push(Me(C.bind(null, A)));
          }, function(A) {
            return g._waitingQueue.push(Me(S.bind(null, A)));
          }).finally(function() {
            g._waitingFor === v && (g._waitingFor = null);
          });
        });
      }, Rt.prototype.abort = function() {
        this.active && (this.active = !1, this.idbtrans && this.idbtrans.abort(), this._reject(new pe.Abort()));
      }, Rt.prototype.table = function(h) {
        var u = this._memoizedTables || (this._memoizedTables = {});
        if (f(u, h)) return u[h];
        var g = this.schema[h];
        if (!g) throw new pe.NotFound("Table " + h + " not part of transaction");
        return g = new this.db.Table(h, g, this), g.core = this.db.core.table(h), u[h] = g;
      }, Rt);
      function Rt() {
      }
      function Zn(h, u, g, _, v, C, S, A) {
        return { name: h, keyPath: u, unique: g, multi: _, auto: v, compound: C, src: (g && !S ? "&" : "") + (_ ? "*" : "") + (v ? "++" : "") + js(u), type: A };
      }
      function js(h) {
        return typeof h == "string" ? h : h ? "[" + [].join.call(h, "+") + "]" : "";
      }
      function Yn(h, u, g) {
        return { name: h, primKey: u, indexes: g, mappedClass: null, idxByName: (_ = function(v) {
          return [v.name, v];
        }, g.reduce(function(v, C, S) {
          return S = _(C, S), S && (v[S[0]] = S[1]), v;
        }, {})) };
        var _;
      }
      var Ji = function(h) {
        try {
          return h.only([[]]), Ji = function() {
            return [[]];
          }, [[]];
        } catch {
          return Ji = function() {
            return ii;
          }, ii;
        }
      };
      function Xn(h) {
        return h == null ? function() {
        } : typeof h == "string" ? (u = h).split(".").length === 1 ? function(g) {
          return g[u];
        } : function(g) {
          return se(g, u);
        } : function(g) {
          return se(g, h);
        };
        var u;
      }
      function Gs(h) {
        return [].slice.call(h);
      }
      var Lc = 0;
      function $i(h) {
        return h == null ? ":id" : typeof h == "string" ? h : "[".concat(h.join("+"), "]");
      }
      function Mc(h, u, D) {
        function _(N) {
          if (N.type === 3) return null;
          if (N.type === 4) throw new Error("Cannot convert never type to IDBKeyRange");
          var O = N.lower, L = N.upper, B = N.lowerOpen, N = N.upperOpen;
          return O === void 0 ? L === void 0 ? null : u.upperBound(L, !!N) : L === void 0 ? u.lowerBound(O, !!B) : u.bound(O, L, !!B, !!N);
        }
        function v(H) {
          var O, L = H.name;
          return { name: L, schema: H, mutate: function(B) {
            var N = B.trans, V = B.type, G = B.keys, Z = B.values, Y = B.range;
            return new Promise(function($, ne) {
              $ = Me($);
              var te = N.objectStore(L), ie = te.keyPath == null, he = V === "put" || V === "add";
              if (!he && V !== "delete" && V !== "deleteRange") throw new Error("Invalid operation type: " + V);
              var ce, de = (G || Z || { length: 1 }).length;
              if (G && Z && G.length !== Z.length) throw new Error("Given keys array must have same length as given values array.");
              if (de === 0) return $({ numFailures: 0, failures: {}, results: [], lastResult: void 0 });
              function be(nt) {
                ++Ve, Xi(nt);
              }
              var xe = [], ye = [], Ve = 0;
              if (V === "deleteRange") {
                if (Y.type === 4) return $({ numFailures: Ve, failures: ye, results: [], lastResult: void 0 });
                Y.type === 3 ? xe.push(ce = te.clear()) : xe.push(ce = te.delete(_(Y)));
              } else {
                var ie = he ? ie ? [Z, G] : [Z, null] : [G, null], _e = ie[0], et = ie[1];
                if (he) for (var tt = 0; tt < de; ++tt) xe.push(ce = et && et[tt] !== void 0 ? te[V](_e[tt], et[tt]) : te[V](_e[tt])), ce.onerror = be;
                else for (tt = 0; tt < de; ++tt) xe.push(ce = te[V](_e[tt])), ce.onerror = be;
              }
              function Yr(nt) {
                nt = nt.target.result, xe.forEach(function(si, go) {
                  return si.error != null && (ye[go] = si.error);
                }), $({ numFailures: Ve, failures: ye, results: V === "delete" ? G : xe.map(function(si) {
                  return si.result;
                }), lastResult: nt });
              }
              ce.onerror = function(nt) {
                be(nt), Yr(nt);
              }, ce.onsuccess = Yr;
            });
          }, getMany: function(B) {
            var N = B.trans, V = B.keys;
            return new Promise(function(G, Z) {
              G = Me(G);
              for (var Y, $ = N.objectStore(L), ne = V.length, te = new Array(ne), ie = 0, he = 0, ce = function(xe) {
                xe = xe.target, te[xe._pos] = xe.result, ++he === ie && G(te);
              }, de = St(Z), be = 0; be < ne; ++be) V[be] != null && ((Y = $.get(V[be]))._pos = be, Y.onsuccess = ce, Y.onerror = de, ++ie);
              ie === 0 && G(te);
            });
          }, get: function(B) {
            var N = B.trans, V = B.key;
            return new Promise(function(G, Z) {
              G = Me(G);
              var Y = N.objectStore(L).get(V);
              Y.onsuccess = function($) {
                return G($.target.result);
              }, Y.onerror = St(Z);
            });
          }, query: (O = F, function(B) {
            return new Promise(function(N, V) {
              N = Me(N);
              var G, Z, Y, ie = B.trans, $ = B.values, ne = B.limit, ce = B.query, te = ne === 1 / 0 ? void 0 : ne, he = ce.index, ce = ce.range, ie = ie.objectStore(L), he = he.isPrimaryKey ? ie : ie.index(he.name), ce = _(ce);
              if (ne === 0) return N({ result: [] });
              O ? ((te = $ ? he.getAll(ce, te) : he.getAllKeys(ce, te)).onsuccess = function(de) {
                return N({ result: de.target.result });
              }, te.onerror = St(V)) : (G = 0, Z = !$ && "openKeyCursor" in he ? he.openKeyCursor(ce) : he.openCursor(ce), Y = [], Z.onsuccess = function(de) {
                var be = Z.result;
                return be ? (Y.push($ ? be.value : be.primaryKey), ++G === ne ? N({ result: Y }) : void be.continue()) : N({ result: Y });
              }, Z.onerror = St(V));
            });
          }), openCursor: function(B) {
            var N = B.trans, V = B.values, G = B.query, Z = B.reverse, Y = B.unique;
            return new Promise(function($, ne) {
              $ = Me($);
              var he = G.index, te = G.range, ie = N.objectStore(L), ie = he.isPrimaryKey ? ie : ie.index(he.name), he = Z ? Y ? "prevunique" : "prev" : Y ? "nextunique" : "next", ce = !V && "openKeyCursor" in ie ? ie.openKeyCursor(_(te), he) : ie.openCursor(_(te), he);
              ce.onerror = St(ne), ce.onsuccess = Me(function(de) {
                var be, xe, ye, Ve, _e = ce.result;
                _e ? (_e.___id = ++Lc, _e.done = !1, be = _e.continue.bind(_e), xe = (xe = _e.continuePrimaryKey) && xe.bind(_e), ye = _e.advance.bind(_e), Ve = function() {
                  throw new Error("Cursor not stopped");
                }, _e.trans = N, _e.stop = _e.continue = _e.continuePrimaryKey = _e.advance = function() {
                  throw new Error("Cursor not started");
                }, _e.fail = Me(ne), _e.next = function() {
                  var et = this, tt = 1;
                  return this.start(function() {
                    return tt-- ? et.continue() : et.stop();
                  }).then(function() {
                    return et;
                  });
                }, _e.start = function(et) {
                  function tt() {
                    if (ce.result) try {
                      et();
                    } catch (nt) {
                      _e.fail(nt);
                    }
                    else _e.done = !0, _e.start = function() {
                      throw new Error("Cursor behind last entry");
                    }, _e.stop();
                  }
                  var Yr = new Promise(function(nt, si) {
                    nt = Me(nt), ce.onerror = St(si), _e.fail = si, _e.stop = function(go) {
                      _e.stop = _e.continue = _e.continuePrimaryKey = _e.advance = Ve, nt(go);
                    };
                  });
                  return ce.onsuccess = Me(function(nt) {
                    ce.onsuccess = tt, tt();
                  }), _e.continue = be, _e.continuePrimaryKey = xe, _e.advance = ye, tt(), Yr;
                }, $(_e)) : $(null);
              }, ne);
            });
          }, count: function(B) {
            var N = B.query, V = B.trans, G = N.index, Z = N.range;
            return new Promise(function(Y, $) {
              var ne = V.objectStore(L), te = G.isPrimaryKey ? ne : ne.index(G.name), ne = _(Z), te = ne ? te.count(ne) : te.count();
              te.onsuccess = Me(function(ie) {
                return Y(ie.target.result);
              }), te.onerror = St($);
            });
          } };
        }
        var C, S, A, M = (S = D, A = Gs((C = h).objectStoreNames), { schema: { name: C.name, tables: A.map(function(H) {
          return S.objectStore(H);
        }).map(function(H) {
          var O = H.keyPath, N = H.autoIncrement, L = l(O), B = {}, N = { name: H.name, primaryKey: { name: null, isPrimaryKey: !0, outbound: O == null, compound: L, keyPath: O, autoIncrement: N, unique: !0, extractKey: Xn(O) }, indexes: Gs(H.indexNames).map(function(V) {
            return H.index(V);
          }).map(function(Y) {
            var G = Y.name, Z = Y.unique, $ = Y.multiEntry, Y = Y.keyPath, $ = { name: G, compound: l(Y), keyPath: Y, unique: Z, multiEntry: $, extractKey: Xn(Y) };
            return B[$i(Y)] = $;
          }), getIndexByKeyPath: function(V) {
            return B[$i(V)];
          } };
          return B[":id"] = N.primaryKey, O != null && (B[$i(O)] = N.primaryKey), N;
        }) }, hasGetAll: 0 < A.length && "getAll" in S.objectStore(A[0]) && !(typeof navigator < "u" && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604) }), D = M.schema, F = M.hasGetAll, M = D.tables.map(v), k = {};
        return M.forEach(function(H) {
          return k[H.name] = H;
        }), { stack: "dbcore", transaction: h.transaction.bind(h), table: function(H) {
          if (!k[H]) throw new Error("Table '".concat(H, "' not found"));
          return k[H];
        }, MIN_KEY: -1 / 0, MAX_KEY: Ji(u), schema: D };
      }
      function Nc(h, u, g, _) {
        var v = g.IDBKeyRange;
        return g.indexedDB, { dbcore: (_ = Mc(u, v, _), h.dbcore.reduce(function(C, S) {
          return S = S.create, t(t({}, C), S(C));
        }, _)) };
      }
      function Nr(h, _) {
        var g = _.db, _ = Nc(h._middlewares, g, h._deps, _);
        h.core = _.dbcore, h.tables.forEach(function(v) {
          var C = v.name;
          h.core.schema.tables.some(function(S) {
            return S.name === C;
          }) && (v.core = h.core.table(C), h[C] instanceof h.Table && (h[C].core = v.core));
        });
      }
      function Br(h, u, g, _) {
        g.forEach(function(v) {
          var C = _[v];
          u.forEach(function(S) {
            var A = (function D(F, M) {
              return I(F, M) || (F = c(F)) && D(F, M);
            })(S, v);
            (!A || "value" in A && A.value === void 0) && (S === h.Transaction.prototype || S instanceof h.Transaction ? x(S, v, { get: function() {
              return this.table(v);
            }, set: function(D) {
              b(this, v, { value: D, writable: !0, configurable: !0, enumerable: !0 });
            } }) : S[v] = new h.Table(v, C));
          });
        });
      }
      function Qn(h, u) {
        u.forEach(function(g) {
          for (var _ in g) g[_] instanceof h.Table && delete g[_];
        });
      }
      function Bc(h, u) {
        return h._cfg.version - u._cfg.version;
      }
      function Hc(h, u, g, _) {
        var v = h._dbSchema;
        g.objectStoreNames.contains("$meta") && !v.$meta && (v.$meta = Yn("$meta", Ks("")[0], []), h._storeNames.push("$meta"));
        var C = h._createTransaction("readwrite", h._storeNames, v);
        C.create(g), C._completion.catch(_);
        var S = C._reject.bind(C), A = J.transless || J;
        Ht(function() {
          return J.trans = C, J.transless = A, u !== 0 ? (Nr(h, g), F = u, ((D = C).storeNames.includes("$meta") ? D.table("$meta").get("version").then(function(M) {
            return M ?? F;
          }) : Q.resolve(F)).then(function(M) {
            return H = M, O = C, L = g, B = [], M = (k = h)._versions, N = k._dbSchema = Vr(0, k.idbdb, L), (M = M.filter(function(V) {
              return V._cfg.version >= H;
            })).length !== 0 ? (M.forEach(function(V) {
              B.push(function() {
                var G = N, Z = V._cfg.dbschema;
                Ur(k, G, L), Ur(k, Z, L), N = k._dbSchema = Z;
                var Y = Jn(G, Z);
                Y.add.forEach(function(he) {
                  $n(L, he[0], he[1].primKey, he[1].indexes);
                }), Y.change.forEach(function(he) {
                  if (he.recreate) throw new pe.Upgrade("Not yet support for changing primary key");
                  var ce = L.objectStore(he.name);
                  he.add.forEach(function(de) {
                    return Hr(ce, de);
                  }), he.change.forEach(function(de) {
                    ce.deleteIndex(de.name), Hr(ce, de);
                  }), he.del.forEach(function(de) {
                    return ce.deleteIndex(de);
                  });
                });
                var $ = V._cfg.contentUpgrade;
                if ($ && V._cfg.version > H) {
                  Nr(k, L), O._memoizedTables = {};
                  var ne = Ie(Z);
                  Y.del.forEach(function(he) {
                    ne[he] = G[he];
                  }), Qn(k, [k.Transaction.prototype]), Br(k, [k.Transaction.prototype], o(ne), ne), O.schema = ne;
                  var te, ie = Bt($);
                  return ie && Ti(), Y = Q.follow(function() {
                    var he;
                    (te = $(O)) && ie && (he = Vt.bind(null, null), te.then(he, he));
                  }), te && typeof te.then == "function" ? Q.resolve(te) : Y.then(function() {
                    return te;
                  });
                }
              }), B.push(function(G) {
                var Z, Y, $ = V._cfg.dbschema;
                Z = $, Y = G, [].slice.call(Y.db.objectStoreNames).forEach(function(ne) {
                  return Z[ne] == null && Y.db.deleteObjectStore(ne);
                }), Qn(k, [k.Transaction.prototype]), Br(k, [k.Transaction.prototype], k._storeNames, k._dbSchema), O.schema = k._dbSchema;
              }), B.push(function(G) {
                k.idbdb.objectStoreNames.contains("$meta") && (Math.ceil(k.idbdb.version / 10) === V._cfg.version ? (k.idbdb.deleteObjectStore("$meta"), delete k._dbSchema.$meta, k._storeNames = k._storeNames.filter(function(Z) {
                  return Z !== "$meta";
                })) : G.objectStore("$meta").put(V._cfg.version, "version"));
              });
            }), (function V() {
              return B.length ? Q.resolve(B.shift()(O.idbtrans)).then(V) : Q.resolve();
            })().then(function() {
              qs(N, L);
            })) : Q.resolve();
            var k, H, O, L, B, N;
          }).catch(S)) : (o(v).forEach(function(M) {
            $n(g, M, v[M].primKey, v[M].indexes);
          }), Nr(h, g), void Q.follow(function() {
            return h.on.populate.fire(C);
          }).catch(S));
          var D, F;
        });
      }
      function Vc(h, u) {
        qs(h._dbSchema, u), u.db.version % 10 != 0 || u.objectStoreNames.contains("$meta") || u.db.createObjectStore("$meta").add(Math.ceil(u.db.version / 10 - 1), "version");
        var g = Vr(0, h.idbdb, u);
        Ur(h, h._dbSchema, u);
        for (var _ = 0, v = Jn(g, h._dbSchema).change; _ < v.length; _++) {
          var C = (function(S) {
            if (S.change.length || S.recreate) return { value: void 0 };
            var A = u.objectStore(S.name);
            S.add.forEach(function(D) {
              Hr(A, D);
            });
          })(v[_]);
          if (typeof C == "object") return C.value;
        }
      }
      function Jn(h, u) {
        var g, _ = { del: [], add: [], change: [] };
        for (g in h) u[g] || _.del.push(g);
        for (g in u) {
          var v = h[g], C = u[g];
          if (v) {
            var S = { name: g, def: C, recreate: !1, del: [], add: [], change: [] };
            if ("" + (v.primKey.keyPath || "") != "" + (C.primKey.keyPath || "") || v.primKey.auto !== C.primKey.auto) S.recreate = !0, _.change.push(S);
            else {
              var A = v.idxByName, D = C.idxByName, F = void 0;
              for (F in A) D[F] || S.del.push(F);
              for (F in D) {
                var M = A[F], k = D[F];
                M ? M.src !== k.src && S.change.push(k) : S.add.push(k);
              }
              (0 < S.del.length || 0 < S.add.length || 0 < S.change.length) && _.change.push(S);
            }
          } else _.add.push([g, C]);
        }
        return _;
      }
      function $n(h, u, g, _) {
        var v = h.db.createObjectStore(u, g.keyPath ? { keyPath: g.keyPath, autoIncrement: g.auto } : { autoIncrement: g.auto });
        return _.forEach(function(C) {
          return Hr(v, C);
        }), v;
      }
      function qs(h, u) {
        o(h).forEach(function(g) {
          u.db.objectStoreNames.contains(g) || $n(u, g, h[g].primKey, h[g].indexes);
        });
      }
      function Hr(h, u) {
        h.createIndex(u.name, u.keyPath, { unique: u.unique, multiEntry: u.multi });
      }
      function Vr(h, u, g) {
        var _ = {};
        return W(u.objectStoreNames, 0).forEach(function(v) {
          for (var C = g.objectStore(v), S = Zn(js(F = C.keyPath), F || "", !0, !1, !!C.autoIncrement, F && typeof F != "string", !0), A = [], D = 0; D < C.indexNames.length; ++D) {
            var M = C.index(C.indexNames[D]), F = M.keyPath, M = Zn(M.name, F, !!M.unique, !!M.multiEntry, !1, F && typeof F != "string", !1);
            A.push(M);
          }
          _[v] = Yn(v, S, A);
        }), _;
      }
      function Ur(h, u, g) {
        for (var _ = g.db.objectStoreNames, v = 0; v < _.length; ++v) {
          var C = _[v], S = g.objectStore(C);
          h._hasGetAll = "getAll" in S;
          for (var A = 0; A < S.indexNames.length; ++A) {
            var D = S.indexNames[A], F = S.index(D).keyPath, M = typeof F == "string" ? F : "[" + W(F).join("+") + "]";
            !u[C] || (F = u[C].idxByName[M]) && (F.name = D, delete u[C].idxByName[M], u[C].idxByName[D] = F);
          }
        }
        typeof navigator < "u" && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && r.WorkerGlobalScope && r instanceof r.WorkerGlobalScope && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604 && (h._hasGetAll = !1);
      }
      function Ks(h) {
        return h.split(",").map(function(u, g) {
          var C = u.split(":"), _ = (v = C[1]) === null || v === void 0 ? void 0 : v.trim(), v = (u = C[0].trim()).replace(/([&*]|\+\+)/g, ""), C = /^\[/.test(v) ? v.match(/^\[(.*)\]$/)[1].split("+") : v;
          return Zn(v, C || null, /\&/.test(u), /\*/.test(u), /\+\+/.test(u), l(C), g === 0, _);
        });
      }
      var Uc = (Ri.prototype._createTableSchema = Yn, Ri.prototype._parseIndexSyntax = Ks, Ri.prototype._parseStoresSpec = function(h, u) {
        var g = this;
        o(h).forEach(function(_) {
          if (h[_] !== null) {
            var v = g._parseIndexSyntax(h[_]), C = v.shift();
            if (!C) throw new pe.Schema("Invalid schema for table " + _ + ": " + h[_]);
            if (C.unique = !0, C.multi) throw new pe.Schema("Primary key cannot be multiEntry*");
            v.forEach(function(S) {
              if (S.auto) throw new pe.Schema("Only primary key can be marked as autoIncrement (++)");
              if (!S.keyPath) throw new pe.Schema("Index must have a name and cannot be an empty string");
            }), v = g._createTableSchema(_, C, v), u[_] = v;
          }
        });
      }, Ri.prototype.stores = function(g) {
        var u = this.db;
        this._cfg.storesSource = this._cfg.storesSource ? a(this._cfg.storesSource, g) : g;
        var g = u._versions, _ = {}, v = {};
        return g.forEach(function(C) {
          a(_, C._cfg.storesSource), v = C._cfg.dbschema = {}, C._parseStoresSpec(_, v);
        }), u._dbSchema = v, Qn(u, [u._allTables, u, u.Transaction.prototype]), Br(u, [u._allTables, u, u.Transaction.prototype, this._cfg.tables], o(v), v), u._storeNames = o(v), this;
      }, Ri.prototype.upgrade = function(h) {
        return this._cfg.contentUpgrade = ue(this._cfg.contentUpgrade || Pe, h), this;
      }, Ri);
      function Ri() {
      }
      function eo(h, u) {
        var g = h._dbNamesDB;
        return g || (g = h._dbNamesDB = new zt(Dr, { addons: [], indexedDB: h, IDBKeyRange: u })).version(1).stores({ dbnames: "name" }), g.table("dbnames");
      }
      function to(h) {
        return h && typeof h.databases == "function";
      }
      function io(h) {
        return Ht(function() {
          return J.letThrough = !0, h();
        });
      }
      function ro(h) {
        return !("from" in h);
      }
      var $e = function(h, u) {
        if (!this) {
          var g = new $e();
          return h && "d" in h && a(g, h), g;
        }
        a(this, arguments.length ? { d: 1, from: h, to: 1 < arguments.length ? u : h } : { d: 0 });
      };
      function er(h, u, g) {
        var _ = we(u, g);
        if (!isNaN(_)) {
          if (0 < _) throw RangeError();
          if (ro(h)) return a(h, { from: u, to: g, d: 1 });
          var v = h.l, _ = h.r;
          if (we(g, h.from) < 0) return v ? er(v, u, g) : h.l = { from: u, to: g, d: 1, l: null, r: null }, Ys(h);
          if (0 < we(u, h.to)) return _ ? er(_, u, g) : h.r = { from: u, to: g, d: 1, l: null, r: null }, Ys(h);
          we(u, h.from) < 0 && (h.from = u, h.l = null, h.d = _ ? _.d + 1 : 1), 0 < we(g, h.to) && (h.to = g, h.r = null, h.d = h.l ? h.l.d + 1 : 1), g = !h.r, v && !h.l && tr(h, v), _ && g && tr(h, _);
        }
      }
      function tr(h, u) {
        ro(u) || (function g(_, D) {
          var C = D.from, S = D.to, A = D.l, D = D.r;
          er(_, C, S), A && g(_, A), D && g(_, D);
        })(h, u);
      }
      function Zs(h, u) {
        var g = Wr(u), _ = g.next();
        if (_.done) return !1;
        for (var v = _.value, C = Wr(h), S = C.next(v.from), A = S.value; !_.done && !S.done; ) {
          if (we(A.from, v.to) <= 0 && 0 <= we(A.to, v.from)) return !0;
          we(v.from, A.from) < 0 ? v = (_ = g.next(A.from)).value : A = (S = C.next(v.from)).value;
        }
        return !1;
      }
      function Wr(h) {
        var u = ro(h) ? null : { s: 0, n: h };
        return { next: function(g) {
          for (var _ = 0 < arguments.length; u; ) switch (u.s) {
            case 0:
              if (u.s = 1, _) for (; u.n.l && we(g, u.n.from) < 0; ) u = { up: u, n: u.n.l, s: 1 };
              else for (; u.n.l; ) u = { up: u, n: u.n.l, s: 1 };
            case 1:
              if (u.s = 2, !_ || we(g, u.n.to) <= 0) return { value: u.n, done: !1 };
            case 2:
              if (u.n.r) {
                u.s = 3, u = { up: u, n: u.n.r, s: 0 };
                continue;
              }
            case 3:
              u = u.up;
          }
          return { done: !0 };
        } };
      }
      function Ys(h) {
        var u, g, _ = (((u = h.r) === null || u === void 0 ? void 0 : u.d) || 0) - (((g = h.l) === null || g === void 0 ? void 0 : g.d) || 0), v = 1 < _ ? "r" : _ < -1 ? "l" : "";
        v && (u = v == "r" ? "l" : "r", g = t({}, h), _ = h[v], h.from = _.from, h.to = _.to, h[v] = _[v], g[v] = _[u], (h[u] = g).d = Xs(g)), h.d = Xs(h);
      }
      function Xs(g) {
        var u = g.r, g = g.l;
        return (u ? g ? Math.max(u.d, g.d) : u.d : g ? g.d : 0) + 1;
      }
      function jr(h, u) {
        return o(u).forEach(function(g) {
          h[g] ? tr(h[g], u[g]) : h[g] = (function _(v) {
            var C, S, A = {};
            for (C in v) f(v, C) && (S = v[C], A[C] = !S || typeof S != "object" || Ne.has(S.constructor) ? S : _(S));
            return A;
          })(u[g]);
        }), h;
      }
      function no(h, u) {
        return h.all || u.all || Object.keys(h).some(function(g) {
          return u[g] && Zs(u[g], h[g]);
        });
      }
      p($e.prototype, ((gt = { add: function(h) {
        return tr(this, h), this;
      }, addKey: function(h) {
        return er(this, h, h), this;
      }, addKeys: function(h) {
        var u = this;
        return h.forEach(function(g) {
          return er(u, g, g);
        }), this;
      }, hasKey: function(h) {
        var u = Wr(this).next(h).value;
        return u && we(u.from, h) <= 0 && 0 <= we(u.to, h);
      } })[ct] = function() {
        return Wr(this);
      }, gt));
      var ni = {}, oo = {}, so = !1;
      function Gr(h) {
        jr(oo, h), so || (so = !0, setTimeout(function() {
          so = !1, ao(oo, !(oo = {}));
        }, 0));
      }
      function ao(h, u) {
        u === void 0 && (u = !1);
        var g = /* @__PURE__ */ new Set();
        if (h.all) for (var _ = 0, v = Object.values(ni); _ < v.length; _++) Qs(S = v[_], h, g, u);
        else for (var C in h) {
          var S, A = /^idb\:\/\/(.*)\/(.*)\//.exec(C);
          A && (C = A[1], A = A[2], (S = ni["idb://".concat(C, "/").concat(A)]) && Qs(S, h, g, u));
        }
        g.forEach(function(D) {
          return D();
        });
      }
      function Qs(h, u, g, _) {
        for (var v = [], C = 0, S = Object.entries(h.queries.query); C < S.length; C++) {
          for (var A = S[C], D = A[0], F = [], M = 0, k = A[1]; M < k.length; M++) {
            var H = k[M];
            no(u, H.obsSet) ? H.subscribers.forEach(function(N) {
              return g.add(N);
            }) : _ && F.push(H);
          }
          _ && v.push([D, F]);
        }
        if (_) for (var O = 0, L = v; O < L.length; O++) {
          var B = L[O], D = B[0], F = B[1];
          h.queries.query[D] = F;
        }
      }
      function Wc(h) {
        var u = h._state, g = h._deps.indexedDB;
        if (u.isBeingOpened || h.idbdb) return u.dbReadyPromise.then(function() {
          return u.dbOpenError ? We(u.dbOpenError) : h;
        });
        u.isBeingOpened = !0, u.dbOpenError = null, u.openComplete = !1;
        var _ = u.openCanceller, v = Math.round(10 * h.verno), C = !1;
        function S() {
          if (u.openCanceller !== _) throw new pe.DatabaseClosed("db.open() was cancelled");
        }
        function A() {
          return new Q(function(H, O) {
            if (S(), !g) throw new pe.MissingAPI();
            var L = h.name, B = u.autoSchema || !v ? g.open(L) : g.open(L, v);
            if (!B) throw new pe.MissingAPI();
            B.onerror = St(O), B.onblocked = Me(h._fireOnBlocked), B.onupgradeneeded = Me(function(N) {
              var V;
              M = B.transaction, u.autoSchema && !h._options.allowEmptyDB ? (B.onerror = Xi, M.abort(), B.result.close(), (V = g.deleteDatabase(L)).onsuccess = V.onerror = Me(function() {
                O(new pe.NoSuchDatabase("Database ".concat(L, " doesnt exist")));
              })) : (M.onerror = St(O), N = N.oldVersion > Math.pow(2, 62) ? 0 : N.oldVersion, k = N < 1, h.idbdb = B.result, C && Vc(h, M), Hc(h, N / 10, M, O));
            }, O), B.onsuccess = Me(function() {
              M = null;
              var N, V, G, Z, Y, $ = h.idbdb = B.result, ne = W($.objectStoreNames);
              if (0 < ne.length) try {
                var te = $.transaction((Z = ne).length === 1 ? Z[0] : Z, "readonly");
                if (u.autoSchema) V = $, G = te, (N = h).verno = V.version / 10, G = N._dbSchema = Vr(0, V, G), N._storeNames = W(V.objectStoreNames, 0), Br(N, [N._allTables], o(G), G);
                else if (Ur(h, h._dbSchema, te), ((Y = Jn(Vr(0, (Y = h).idbdb, te), Y._dbSchema)).add.length || Y.change.some(function(ie) {
                  return ie.add.length || ie.change.length;
                })) && !C) return $.close(), v = $.version + 1, C = !0, H(A());
                Nr(h, te);
              } catch {
              }
              Ei.push(h), $.onversionchange = Me(function(ie) {
                u.vcFired = !0, h.on("versionchange").fire(ie);
              }), $.onclose = Me(function(ie) {
                h.on("close").fire(ie);
              }), k && (Y = h._deps, te = L, $ = Y.indexedDB, Y = Y.IDBKeyRange, to($) || te === Dr || eo($, Y).put({ name: te }).catch(Pe)), H();
            }, O);
          }).catch(function(H) {
            switch (H?.name) {
              case "UnknownError":
                if (0 < u.PR1398_maxLoop) return u.PR1398_maxLoop--, A();
                break;
              case "VersionError":
                if (0 < v) return v = 0, A();
            }
            return Q.reject(H);
          });
        }
        var D, F = u.dbReadyResolve, M = null, k = !1;
        return Q.race([_, (typeof navigator > "u" ? Q.resolve() : !navigator.userAgentData && /Safari\//.test(navigator.userAgent) && !/Chrom(e|ium)\//.test(navigator.userAgent) && indexedDB.databases ? new Promise(function(H) {
          function O() {
            return indexedDB.databases().finally(H);
          }
          D = setInterval(O, 100), O();
        }).finally(function() {
          return clearInterval(D);
        }) : Promise.resolve()).then(A)]).then(function() {
          return S(), u.onReadyBeingFired = [], Q.resolve(io(function() {
            return h.on.ready.fire(h.vip);
          })).then(function H() {
            if (0 < u.onReadyBeingFired.length) {
              var O = u.onReadyBeingFired.reduce(ue, Pe);
              return u.onReadyBeingFired = [], Q.resolve(io(function() {
                return O(h.vip);
              })).then(H);
            }
          });
        }).finally(function() {
          u.openCanceller === _ && (u.onReadyBeingFired = null, u.isBeingOpened = !1);
        }).catch(function(H) {
          u.dbOpenError = H;
          try {
            M && M.abort();
          } catch {
          }
          return _ === u.openCanceller && h._close(), We(H);
        }).finally(function() {
          u.openComplete = !0, F();
        }).then(function() {
          var H;
          return k && (H = {}, h.tables.forEach(function(O) {
            O.schema.indexes.forEach(function(L) {
              L.name && (H["idb://".concat(h.name, "/").concat(O.name, "/").concat(L.name)] = new $e(-1 / 0, [[[]]]));
            }), H["idb://".concat(h.name, "/").concat(O.name, "/")] = H["idb://".concat(h.name, "/").concat(O.name, "/:dels")] = new $e(-1 / 0, [[[]]]);
          }), jt(Qi).fire(H), ao(H, !0)), h;
        });
      }
      function lo(h) {
        function u(C) {
          return h.next(C);
        }
        var g = v(u), _ = v(function(C) {
          return h.throw(C);
        });
        function v(C) {
          return function(D) {
            var A = C(D), D = A.value;
            return A.done ? D : D && typeof D.then == "function" ? D.then(g, _) : l(D) ? Promise.all(D).then(g, _) : g(D);
          };
        }
        return v(u)();
      }
      function qr(h, u, g) {
        for (var _ = l(h) ? h.slice() : [h], v = 0; v < g; ++v) _.push(u);
        return _;
      }
      var jc = { stack: "dbcore", name: "VirtualIndexMiddleware", level: 1, create: function(h) {
        return t(t({}, h), { table: function(u) {
          var g = h.table(u), _ = g.schema, v = {}, C = [];
          function S(k, H, O) {
            var L = $i(k), B = v[L] = v[L] || [], N = k == null ? 0 : typeof k == "string" ? 1 : k.length, V = 0 < H, V = t(t({}, O), { name: V ? "".concat(L, "(virtual-from:").concat(O.name, ")") : O.name, lowLevelIndex: O, isVirtual: V, keyTail: H, keyLength: N, extractKey: Xn(k), unique: !V && O.unique });
            return B.push(V), V.isPrimaryKey || C.push(V), 1 < N && S(N === 2 ? k[0] : k.slice(0, N - 1), H + 1, O), B.sort(function(G, Z) {
              return G.keyTail - Z.keyTail;
            }), V;
          }
          u = S(_.primaryKey.keyPath, 0, _.primaryKey), v[":id"] = [u];
          for (var A = 0, D = _.indexes; A < D.length; A++) {
            var F = D[A];
            S(F.keyPath, 0, F);
          }
          function M(k) {
            var H, O = k.query.index;
            return O.isVirtual ? t(t({}, k), { query: { index: O.lowLevelIndex, range: (H = k.query.range, O = O.keyTail, { type: H.type === 1 ? 2 : H.type, lower: qr(H.lower, H.lowerOpen ? h.MAX_KEY : h.MIN_KEY, O), lowerOpen: !0, upper: qr(H.upper, H.upperOpen ? h.MIN_KEY : h.MAX_KEY, O), upperOpen: !0 }) } }) : k;
          }
          return t(t({}, g), { schema: t(t({}, _), { primaryKey: u, indexes: C, getIndexByKeyPath: function(k) {
            return (k = v[$i(k)]) && k[0];
          } }), count: function(k) {
            return g.count(M(k));
          }, query: function(k) {
            return g.query(M(k));
          }, openCursor: function(k) {
            var H = k.query.index, O = H.keyTail, L = H.isVirtual, B = H.keyLength;
            return L ? g.openCursor(M(k)).then(function(V) {
              return V && N(V);
            }) : g.openCursor(k);
            function N(V) {
              return Object.create(V, { continue: { value: function(G) {
                G != null ? V.continue(qr(G, k.reverse ? h.MAX_KEY : h.MIN_KEY, O)) : k.unique ? V.continue(V.key.slice(0, B).concat(k.reverse ? h.MIN_KEY : h.MAX_KEY, O)) : V.continue();
              } }, continuePrimaryKey: { value: function(G, Z) {
                V.continuePrimaryKey(qr(G, h.MAX_KEY, O), Z);
              } }, primaryKey: { get: function() {
                return V.primaryKey;
              } }, key: { get: function() {
                var G = V.key;
                return B === 1 ? G[0] : G.slice(0, B);
              } }, value: { get: function() {
                return V.value;
              } } });
            }
          } });
        } });
      } };
      function co(h, u, g, _) {
        return g = g || {}, _ = _ || "", o(h).forEach(function(v) {
          var C, S, A;
          f(u, v) ? (C = h[v], S = u[v], typeof C == "object" && typeof S == "object" && C && S ? (A = lt(C)) !== lt(S) ? g[_ + v] = u[v] : A === "Object" ? co(C, S, g, _ + v + ".") : C !== S && (g[_ + v] = u[v]) : C !== S && (g[_ + v] = u[v])) : g[_ + v] = void 0;
        }), o(u).forEach(function(v) {
          f(h, v) || (g[_ + v] = u[v]);
        }), g;
      }
      function ho(h, u) {
        return u.type === "delete" ? u.keys : u.keys || u.values.map(h.extractKey);
      }
      var Gc = { stack: "dbcore", name: "HooksMiddleware", level: 2, create: function(h) {
        return t(t({}, h), { table: function(u) {
          var g = h.table(u), _ = g.schema.primaryKey;
          return t(t({}, g), { mutate: function(v) {
            var C = J.trans, S = C.table(u).hook, A = S.deleting, D = S.creating, F = S.updating;
            switch (v.type) {
              case "add":
                if (D.fire === Pe) break;
                return C._promise("readwrite", function() {
                  return M(v);
                }, !0);
              case "put":
                if (D.fire === Pe && F.fire === Pe) break;
                return C._promise("readwrite", function() {
                  return M(v);
                }, !0);
              case "delete":
                if (A.fire === Pe) break;
                return C._promise("readwrite", function() {
                  return M(v);
                }, !0);
              case "deleteRange":
                if (A.fire === Pe) break;
                return C._promise("readwrite", function() {
                  return (function k(H, O, L) {
                    return g.query({ trans: H, values: !1, query: { index: _, range: O }, limit: L }).then(function(B) {
                      var N = B.result;
                      return M({ type: "delete", keys: N, trans: H }).then(function(V) {
                        return 0 < V.numFailures ? Promise.reject(V.failures[0]) : N.length < L ? { failures: [], numFailures: 0, lastResult: void 0 } : k(H, t(t({}, O), { lower: N[N.length - 1], lowerOpen: !0 }), L);
                      });
                    });
                  })(v.trans, v.range, 1e4);
                }, !0);
            }
            return g.mutate(v);
            function M(k) {
              var H, O, L, B = J.trans, N = k.keys || ho(_, k);
              if (!N) throw new Error("Keys missing");
              return (k = k.type === "add" || k.type === "put" ? t(t({}, k), { keys: N }) : t({}, k)).type !== "delete" && (k.values = i([], k.values)), k.keys && (k.keys = i([], k.keys)), H = g, L = N, ((O = k).type === "add" ? Promise.resolve([]) : H.getMany({ trans: O.trans, keys: L, cache: "immutable" })).then(function(V) {
                var G = N.map(function(Z, Y) {
                  var $, ne, te, ie = V[Y], he = { onerror: null, onsuccess: null };
                  return k.type === "delete" ? A.fire.call(he, Z, ie, B) : k.type === "add" || ie === void 0 ? ($ = D.fire.call(he, Z, k.values[Y], B), Z == null && $ != null && (k.keys[Y] = Z = $, _.outbound || fe(k.values[Y], _.keyPath, Z))) : ($ = co(ie, k.values[Y]), (ne = F.fire.call(he, $, Z, ie, B)) && (te = k.values[Y], Object.keys(ne).forEach(function(ce) {
                    f(te, ce) ? te[ce] = ne[ce] : fe(te, ce, ne[ce]);
                  }))), he;
                });
                return g.mutate(k).then(function(Z) {
                  for (var Y = Z.failures, $ = Z.results, ne = Z.numFailures, Z = Z.lastResult, te = 0; te < N.length; ++te) {
                    var ie = ($ || N)[te], he = G[te];
                    ie == null ? he.onerror && he.onerror(Y[te]) : he.onsuccess && he.onsuccess(k.type === "put" && V[te] ? k.values[te] : ie);
                  }
                  return { failures: Y, results: $, numFailures: ne, lastResult: Z };
                }).catch(function(Z) {
                  return G.forEach(function(Y) {
                    return Y.onerror && Y.onerror(Z);
                  }), Promise.reject(Z);
                });
              });
            }
          } });
        } });
      } };
      function Js(h, u, g) {
        try {
          if (!u || u.keys.length < h.length) return null;
          for (var _ = [], v = 0, C = 0; v < u.keys.length && C < h.length; ++v) we(u.keys[v], h[C]) === 0 && (_.push(g ? De(u.values[v]) : u.values[v]), ++C);
          return _.length === h.length ? _ : null;
        } catch {
          return null;
        }
      }
      var qc = { stack: "dbcore", level: -1, create: function(h) {
        return { table: function(u) {
          var g = h.table(u);
          return t(t({}, g), { getMany: function(_) {
            if (!_.cache) return g.getMany(_);
            var v = Js(_.keys, _.trans._cache, _.cache === "clone");
            return v ? Q.resolve(v) : g.getMany(_).then(function(C) {
              return _.trans._cache = { keys: _.keys, values: _.cache === "clone" ? De(C) : C }, C;
            });
          }, mutate: function(_) {
            return _.type !== "add" && (_.trans._cache = null), g.mutate(_);
          } });
        } };
      } };
      function $s(h, u) {
        return h.trans.mode === "readonly" && !!h.subscr && !h.trans.explicit && h.trans.db._options.cache !== "disabled" && !u.schema.primaryKey.outbound;
      }
      function ea(h, u) {
        switch (h) {
          case "query":
            return u.values && !u.unique;
          case "get":
          case "getMany":
          case "count":
          case "openCursor":
            return !1;
        }
      }
      var Kc = { stack: "dbcore", level: 0, name: "Observability", create: function(h) {
        var u = h.schema.name, g = new $e(h.MIN_KEY, h.MAX_KEY);
        return t(t({}, h), { transaction: function(_, v, C) {
          if (J.subscr && v !== "readonly") throw new pe.ReadOnly("Readwrite transaction in liveQuery context. Querier source: ".concat(J.querier));
          return h.transaction(_, v, C);
        }, table: function(_) {
          var v = h.table(_), C = v.schema, S = C.primaryKey, k = C.indexes, A = S.extractKey, D = S.outbound, F = S.autoIncrement && k.filter(function(O) {
            return O.compound && O.keyPath.includes(S.keyPath);
          }), M = t(t({}, v), { mutate: function(O) {
            function L(ce) {
              return ce = "idb://".concat(u, "/").concat(_, "/").concat(ce), Z[ce] || (Z[ce] = new $e());
            }
            var B, N, V, G = O.trans, Z = O.mutatedParts || (O.mutatedParts = {}), Y = L(""), $ = L(":dels"), ne = O.type, he = O.type === "deleteRange" ? [O.range] : O.type === "delete" ? [O.keys] : O.values.length < 50 ? [ho(S, O).filter(function(ce) {
              return ce;
            }), O.values] : [], te = he[0], ie = he[1], he = O.trans._cache;
            return l(te) ? (Y.addKeys(te), (he = ne === "delete" || te.length === ie.length ? Js(te, he) : null) || $.addKeys(te), (he || ie) && (B = L, N = he, V = ie, C.indexes.forEach(function(ce) {
              var de = B(ce.name || "");
              function be(ye) {
                return ye != null ? ce.extractKey(ye) : null;
              }
              function xe(ye) {
                return ce.multiEntry && l(ye) ? ye.forEach(function(Ve) {
                  return de.addKey(Ve);
                }) : de.addKey(ye);
              }
              (N || V).forEach(function(ye, et) {
                var _e = N && be(N[et]), et = V && be(V[et]);
                we(_e, et) !== 0 && (_e != null && xe(_e), et != null && xe(et));
              });
            }))) : te ? (ie = { from: (ie = te.lower) !== null && ie !== void 0 ? ie : h.MIN_KEY, to: (ie = te.upper) !== null && ie !== void 0 ? ie : h.MAX_KEY }, $.add(ie), Y.add(ie)) : (Y.add(g), $.add(g), C.indexes.forEach(function(ce) {
              return L(ce.name).add(g);
            })), v.mutate(O).then(function(ce) {
              return !te || O.type !== "add" && O.type !== "put" || (Y.addKeys(ce.results), F && F.forEach(function(de) {
                for (var be = O.values.map(function(_e) {
                  return de.extractKey(_e);
                }), xe = de.keyPath.findIndex(function(_e) {
                  return _e === S.keyPath;
                }), ye = 0, Ve = ce.results.length; ye < Ve; ++ye) be[ye][xe] = ce.results[ye];
                L(de.name).addKeys(be);
              })), G.mutatedParts = jr(G.mutatedParts || {}, Z), ce;
            });
          } }), k = function(L) {
            var B = L.query, L = B.index, B = B.range;
            return [L, new $e((L = B.lower) !== null && L !== void 0 ? L : h.MIN_KEY, (B = B.upper) !== null && B !== void 0 ? B : h.MAX_KEY)];
          }, H = { get: function(O) {
            return [S, new $e(O.key)];
          }, getMany: function(O) {
            return [S, new $e().addKeys(O.keys)];
          }, count: k, query: k, openCursor: k };
          return o(H).forEach(function(O) {
            M[O] = function(L) {
              var B = J.subscr, N = !!B, V = $s(J, v) && ea(O, L) ? L.obsSet = {} : B;
              if (N) {
                var G = function(ie) {
                  return ie = "idb://".concat(u, "/").concat(_, "/").concat(ie), V[ie] || (V[ie] = new $e());
                }, Z = G(""), Y = G(":dels"), B = H[O](L), N = B[0], B = B[1];
                if ((O === "query" && N.isPrimaryKey && !L.values ? Y : G(N.name || "")).add(B), !N.isPrimaryKey) {
                  if (O !== "count") {
                    var $ = O === "query" && D && L.values && v.query(t(t({}, L), { values: !1 }));
                    return v[O].apply(this, arguments).then(function(ie) {
                      if (O === "query") {
                        if (D && L.values) return $.then(function(be) {
                          return be = be.result, Z.addKeys(be), ie;
                        });
                        var he = L.values ? ie.result.map(A) : ie.result;
                        (L.values ? Z : Y).addKeys(he);
                      } else if (O === "openCursor") {
                        var ce = ie, de = L.values;
                        return ce && Object.create(ce, { key: { get: function() {
                          return Y.addKey(ce.primaryKey), ce.key;
                        } }, primaryKey: { get: function() {
                          var be = ce.primaryKey;
                          return Y.addKey(be), be;
                        } }, value: { get: function() {
                          return de && Z.addKey(ce.primaryKey), ce.value;
                        } } });
                      }
                      return ie;
                    });
                  }
                  Y.add(g);
                }
              }
              return v[O].apply(this, arguments);
            };
          }), M;
        } });
      } };
      function ta(h, u, g) {
        if (g.numFailures === 0) return u;
        if (u.type === "deleteRange") return null;
        var _ = u.keys ? u.keys.length : "values" in u && u.values ? u.values.length : 1;
        return g.numFailures === _ ? null : (u = t({}, u), l(u.keys) && (u.keys = u.keys.filter(function(v, C) {
          return !(C in g.failures);
        })), "values" in u && l(u.values) && (u.values = u.values.filter(function(v, C) {
          return !(C in g.failures);
        })), u);
      }
      function uo(h, u) {
        return g = h, ((_ = u).lower === void 0 || (_.lowerOpen ? 0 < we(g, _.lower) : 0 <= we(g, _.lower))) && (h = h, (u = u).upper === void 0 || (u.upperOpen ? we(h, u.upper) < 0 : we(h, u.upper) <= 0));
        var g, _;
      }
      function ia(h, u, H, _, v, C) {
        if (!H || H.length === 0) return h;
        var S = u.query.index, A = S.multiEntry, D = u.query.range, F = _.schema.primaryKey.extractKey, M = S.extractKey, k = (S.lowLevelIndex || S).extractKey, H = H.reduce(function(O, L) {
          var B = O, N = [];
          if (L.type === "add" || L.type === "put") for (var V = new $e(), G = L.values.length - 1; 0 <= G; --G) {
            var Z, Y = L.values[G], $ = F(Y);
            V.hasKey($) || (Z = M(Y), (A && l(Z) ? Z.some(function(ce) {
              return uo(ce, D);
            }) : uo(Z, D)) && (V.addKey($), N.push(Y)));
          }
          switch (L.type) {
            case "add":
              var ne = new $e().addKeys(u.values ? O.map(function(de) {
                return F(de);
              }) : O), B = O.concat(u.values ? N.filter(function(de) {
                return de = F(de), !ne.hasKey(de) && (ne.addKey(de), !0);
              }) : N.map(function(de) {
                return F(de);
              }).filter(function(de) {
                return !ne.hasKey(de) && (ne.addKey(de), !0);
              }));
              break;
            case "put":
              var te = new $e().addKeys(L.values.map(function(de) {
                return F(de);
              }));
              B = O.filter(function(de) {
                return !te.hasKey(u.values ? F(de) : de);
              }).concat(u.values ? N : N.map(function(de) {
                return F(de);
              }));
              break;
            case "delete":
              var ie = new $e().addKeys(L.keys);
              B = O.filter(function(de) {
                return !ie.hasKey(u.values ? F(de) : de);
              });
              break;
            case "deleteRange":
              var he = L.range;
              B = O.filter(function(de) {
                return !uo(F(de), he);
              });
          }
          return B;
        }, h);
        return H === h ? h : (H.sort(function(O, L) {
          return we(k(O), k(L)) || we(F(O), F(L));
        }), u.limit && u.limit < 1 / 0 && (H.length > u.limit ? H.length = u.limit : h.length === u.limit && H.length < u.limit && (v.dirty = !0)), C ? Object.freeze(H) : H);
      }
      function ra(h, u) {
        return we(h.lower, u.lower) === 0 && we(h.upper, u.upper) === 0 && !!h.lowerOpen == !!u.lowerOpen && !!h.upperOpen == !!u.upperOpen;
      }
      function Zc(h, u) {
        return (function(g, _, v, C) {
          if (g === void 0) return _ !== void 0 ? -1 : 0;
          if (_ === void 0) return 1;
          if ((_ = we(g, _)) === 0) {
            if (v && C) return 0;
            if (v) return 1;
            if (C) return -1;
          }
          return _;
        })(h.lower, u.lower, h.lowerOpen, u.lowerOpen) <= 0 && 0 <= (function(g, _, v, C) {
          if (g === void 0) return _ !== void 0 ? 1 : 0;
          if (_ === void 0) return -1;
          if ((_ = we(g, _)) === 0) {
            if (v && C) return 0;
            if (v) return -1;
            if (C) return 1;
          }
          return _;
        })(h.upper, u.upper, h.upperOpen, u.upperOpen);
      }
      function Yc(h, u, g, _) {
        h.subscribers.add(g), _.addEventListener("abort", function() {
          var v, C;
          h.subscribers.delete(g), h.subscribers.size === 0 && (v = h, C = u, setTimeout(function() {
            v.subscribers.size === 0 && ht(C, v);
          }, 3e3));
        });
      }
      var Xc = { stack: "dbcore", level: 0, name: "Cache", create: function(h) {
        var u = h.schema.name;
        return t(t({}, h), { transaction: function(g, _, v) {
          var C, S, A = h.transaction(g, _, v);
          return _ === "readwrite" && (S = (C = new AbortController()).signal, v = function(D) {
            return function() {
              if (C.abort(), _ === "readwrite") {
                for (var F = /* @__PURE__ */ new Set(), M = 0, k = g; M < k.length; M++) {
                  var H = k[M], O = ni["idb://".concat(u, "/").concat(H)];
                  if (O) {
                    var L = h.table(H), B = O.optimisticOps.filter(function(de) {
                      return de.trans === A;
                    });
                    if (A._explicit && D && A.mutatedParts) for (var N = 0, V = Object.values(O.queries.query); N < V.length; N++) for (var G = 0, Z = (ne = V[N]).slice(); G < Z.length; G++) no((te = Z[G]).obsSet, A.mutatedParts) && (ht(ne, te), te.subscribers.forEach(function(de) {
                      return F.add(de);
                    }));
                    else if (0 < B.length) {
                      O.optimisticOps = O.optimisticOps.filter(function(de) {
                        return de.trans !== A;
                      });
                      for (var Y = 0, $ = Object.values(O.queries.query); Y < $.length; Y++) for (var ne, te, ie, he = 0, ce = (ne = $[Y]).slice(); he < ce.length; he++) (te = ce[he]).res != null && A.mutatedParts && (D && !te.dirty ? (ie = Object.isFrozen(te.res), ie = ia(te.res, te.req, B, L, te, ie), te.dirty ? (ht(ne, te), te.subscribers.forEach(function(de) {
                        return F.add(de);
                      })) : ie !== te.res && (te.res = ie, te.promise = Q.resolve({ result: ie }))) : (te.dirty && ht(ne, te), te.subscribers.forEach(function(de) {
                        return F.add(de);
                      })));
                    }
                  }
                }
                F.forEach(function(de) {
                  return de();
                });
              }
            };
          }, A.addEventListener("abort", v(!1), { signal: S }), A.addEventListener("error", v(!1), { signal: S }), A.addEventListener("complete", v(!0), { signal: S })), A;
        }, table: function(g) {
          var _ = h.table(g), v = _.schema.primaryKey;
          return t(t({}, _), { mutate: function(C) {
            var S = J.trans;
            if (v.outbound || S.db._options.cache === "disabled" || S.explicit || S.idbtrans.mode !== "readwrite") return _.mutate(C);
            var A = ni["idb://".concat(u, "/").concat(g)];
            return A ? (S = _.mutate(C), C.type !== "add" && C.type !== "put" || !(50 <= C.values.length || ho(v, C).some(function(D) {
              return D == null;
            })) ? (A.optimisticOps.push(C), C.mutatedParts && Gr(C.mutatedParts), S.then(function(D) {
              0 < D.numFailures && (ht(A.optimisticOps, C), (D = ta(0, C, D)) && A.optimisticOps.push(D), C.mutatedParts && Gr(C.mutatedParts));
            }), S.catch(function() {
              ht(A.optimisticOps, C), C.mutatedParts && Gr(C.mutatedParts);
            })) : S.then(function(D) {
              var F = ta(0, t(t({}, C), { values: C.values.map(function(M, k) {
                var H;
                return D.failures[k] ? M : (M = (H = v.keyPath) !== null && H !== void 0 && H.includes(".") ? De(M) : t({}, M), fe(M, v.keyPath, D.results[k]), M);
              }) }), D);
              A.optimisticOps.push(F), queueMicrotask(function() {
                return C.mutatedParts && Gr(C.mutatedParts);
              });
            }), S) : _.mutate(C);
          }, query: function(C) {
            if (!$s(J, _) || !ea("query", C)) return _.query(C);
            var S = ((F = J.trans) === null || F === void 0 ? void 0 : F.db._options.cache) === "immutable", k = J, A = k.requery, D = k.signal, F = (function(L, B, N, V) {
              var G = ni["idb://".concat(L, "/").concat(B)];
              if (!G) return [];
              if (!(B = G.queries[N])) return [null, !1, G, null];
              var Z = B[(V.query ? V.query.index.name : null) || ""];
              if (!Z) return [null, !1, G, null];
              switch (N) {
                case "query":
                  var Y = Z.find(function($) {
                    return $.req.limit === V.limit && $.req.values === V.values && ra($.req.query.range, V.query.range);
                  });
                  return Y ? [Y, !0, G, Z] : [Z.find(function($) {
                    return ("limit" in $.req ? $.req.limit : 1 / 0) >= V.limit && (!V.values || $.req.values) && Zc($.req.query.range, V.query.range);
                  }), !1, G, Z];
                case "count":
                  return Y = Z.find(function($) {
                    return ra($.req.query.range, V.query.range);
                  }), [Y, !!Y, G, Z];
              }
            })(u, g, "query", C), M = F[0], k = F[1], H = F[2], O = F[3];
            return M && k ? M.obsSet = C.obsSet : (k = _.query(C).then(function(L) {
              var B = L.result;
              if (M && (M.res = B), S) {
                for (var N = 0, V = B.length; N < V; ++N) Object.freeze(B[N]);
                Object.freeze(B);
              } else L.result = De(B);
              return L;
            }).catch(function(L) {
              return O && M && ht(O, M), Promise.reject(L);
            }), M = { obsSet: C.obsSet, promise: k, subscribers: /* @__PURE__ */ new Set(), type: "query", req: C, dirty: !1 }, O ? O.push(M) : (O = [M], (H = H || (ni["idb://".concat(u, "/").concat(g)] = { queries: { query: {}, count: {} }, objs: /* @__PURE__ */ new Map(), optimisticOps: [], unsignaledParts: {} })).queries.query[C.query.index.name || ""] = O)), Yc(M, O, A, D), M.promise.then(function(L) {
              return { result: ia(L.result, C, H?.optimisticOps, _, M, S) };
            });
          } });
        } });
      } };
      function Kr(h, u) {
        return new Proxy(h, { get: function(g, _, v) {
          return _ === "db" ? u : Reflect.get(g, _, v);
        } });
      }
      var zt = (je.prototype.version = function(h) {
        if (isNaN(h) || h < 0.1) throw new pe.Type("Given version is not a positive number");
        if (h = Math.round(10 * h) / 10, this.idbdb || this._state.isBeingOpened) throw new pe.Schema("Cannot add version when database is open");
        this.verno = Math.max(this.verno, h);
        var u = this._versions, g = u.filter(function(_) {
          return _._cfg.version === h;
        })[0];
        return g || (g = new this.Version(h), u.push(g), u.sort(Bc), g.stores({}), this._state.autoSchema = !1, g);
      }, je.prototype._whenReady = function(h) {
        var u = this;
        return this.idbdb && (this._state.openComplete || J.letThrough || this._vip) ? h() : new Q(function(g, _) {
          if (u._state.openComplete) return _(new pe.DatabaseClosed(u._state.dbOpenError));
          if (!u._state.isBeingOpened) {
            if (!u._state.autoOpen) return void _(new pe.DatabaseClosed());
            u.open().catch(Pe);
          }
          u._state.dbReadyPromise.then(g, _);
        }).then(h);
      }, je.prototype.use = function(h) {
        var u = h.stack, g = h.create, _ = h.level, v = h.name;
        return v && this.unuse({ stack: u, name: v }), h = this._middlewares[u] || (this._middlewares[u] = []), h.push({ stack: u, create: g, level: _ ?? 10, name: v }), h.sort(function(C, S) {
          return C.level - S.level;
        }), this;
      }, je.prototype.unuse = function(h) {
        var u = h.stack, g = h.name, _ = h.create;
        return u && this._middlewares[u] && (this._middlewares[u] = this._middlewares[u].filter(function(v) {
          return _ ? v.create !== _ : !!g && v.name !== g;
        })), this;
      }, je.prototype.open = function() {
        var h = this;
        return ti(re, function() {
          return Wc(h);
        });
      }, je.prototype._close = function() {
        this.on.close.fire(new CustomEvent("close"));
        var h = this._state, u = Ei.indexOf(this);
        if (0 <= u && Ei.splice(u, 1), this.idbdb) {
          try {
            this.idbdb.close();
          } catch {
          }
          this.idbdb = null;
        }
        h.isBeingOpened || (h.dbReadyPromise = new Q(function(g) {
          h.dbReadyResolve = g;
        }), h.openCanceller = new Q(function(g, _) {
          h.cancelOpen = _;
        }));
      }, je.prototype.close = function(g) {
        var u = (g === void 0 ? { disableAutoOpen: !0 } : g).disableAutoOpen, g = this._state;
        u ? (g.isBeingOpened && g.cancelOpen(new pe.DatabaseClosed()), this._close(), g.autoOpen = !1, g.dbOpenError = new pe.DatabaseClosed()) : (this._close(), g.autoOpen = this._options.autoOpen || g.isBeingOpened, g.openComplete = !1, g.dbOpenError = null);
      }, je.prototype.delete = function(h) {
        var u = this;
        h === void 0 && (h = { disableAutoOpen: !0 });
        var g = 0 < arguments.length && typeof arguments[0] != "object", _ = this._state;
        return new Q(function(v, C) {
          function S() {
            u.close(h);
            var A = u._deps.indexedDB.deleteDatabase(u.name);
            A.onsuccess = Me(function() {
              var D, F, M;
              D = u._deps, F = u.name, M = D.indexedDB, D = D.IDBKeyRange, to(M) || F === Dr || eo(M, D).delete(F).catch(Pe), v();
            }), A.onerror = St(C), A.onblocked = u._fireOnBlocked;
          }
          if (g) throw new pe.InvalidArgument("Invalid closeOptions argument to db.delete()");
          _.isBeingOpened ? _.dbReadyPromise.then(S) : S();
        });
      }, je.prototype.backendDB = function() {
        return this.idbdb;
      }, je.prototype.isOpen = function() {
        return this.idbdb !== null;
      }, je.prototype.hasBeenClosed = function() {
        var h = this._state.dbOpenError;
        return h && h.name === "DatabaseClosed";
      }, je.prototype.hasFailed = function() {
        return this._state.dbOpenError !== null;
      }, je.prototype.dynamicallyOpened = function() {
        return this._state.autoSchema;
      }, Object.defineProperty(je.prototype, "tables", { get: function() {
        var h = this;
        return o(this._allTables).map(function(u) {
          return h._allTables[u];
        });
      }, enumerable: !1, configurable: !0 }), je.prototype.transaction = function() {
        var h = (function(u, g, _) {
          var v = arguments.length;
          if (v < 2) throw new pe.InvalidArgument("Too few arguments");
          for (var C = new Array(v - 1); --v; ) C[v - 1] = arguments[v];
          return _ = C.pop(), [u, qe(C), _];
        }).apply(this, arguments);
        return this._transaction.apply(this, h);
      }, je.prototype._transaction = function(h, u, g) {
        var _ = this, v = J.trans;
        v && v.db === this && h.indexOf("!") === -1 || (v = null);
        var C, S, A = h.indexOf("?") !== -1;
        h = h.replace("!", "").replace("?", "");
        try {
          if (S = u.map(function(F) {
            if (F = F instanceof _.Table ? F.name : F, typeof F != "string") throw new TypeError("Invalid table argument to Dexie.transaction(). Only Table or String are allowed");
            return F;
          }), h == "r" || h === Wn) C = Wn;
          else {
            if (h != "rw" && h != jn) throw new pe.InvalidArgument("Invalid transaction mode: " + h);
            C = jn;
          }
          if (v) {
            if (v.mode === Wn && C === jn) {
              if (!A) throw new pe.SubTransaction("Cannot enter a sub-transaction with READWRITE mode when parent transaction is READONLY");
              v = null;
            }
            v && S.forEach(function(F) {
              if (v && v.storeNames.indexOf(F) === -1) {
                if (!A) throw new pe.SubTransaction("Table " + F + " not included in parent transaction.");
                v = null;
              }
            }), A && v && !v.active && (v = null);
          }
        } catch (F) {
          return v ? v._promise(null, function(M, k) {
            k(F);
          }) : We(F);
        }
        var D = (function F(M, k, H, O, L) {
          return Q.resolve().then(function() {
            var B = J.transless || J, N = M._createTransaction(k, H, M._dbSchema, O);
            if (N.explicit = !0, B = { trans: N, transless: B }, O) N.idbtrans = O.idbtrans;
            else try {
              N.create(), N.idbtrans._explicit = !0, M._state.PR1398_maxLoop = 3;
            } catch (Z) {
              return Z.name === xt.InvalidState && M.isOpen() && 0 < --M._state.PR1398_maxLoop ? (M.close({ disableAutoOpen: !1 }), M.open().then(function() {
                return F(M, k, H, null, L);
              })) : We(Z);
            }
            var V, G = Bt(L);
            return G && Ti(), B = Q.follow(function() {
              var Z;
              (V = L.call(N, N)) && (G ? (Z = Vt.bind(null, null), V.then(Z, Z)) : typeof V.next == "function" && typeof V.throw == "function" && (V = lo(V)));
            }, B), (V && typeof V.then == "function" ? Q.resolve(V).then(function(Z) {
              return N.active ? Z : We(new pe.PrematureCommit("Transaction committed too early. See http://bit.ly/2kdckMn"));
            }) : B.then(function() {
              return V;
            })).then(function(Z) {
              return O && N._resolve(), N._completion.then(function() {
                return Z;
              });
            }).catch(function(Z) {
              return N._reject(Z), We(Z);
            });
          });
        }).bind(null, this, C, S, v, g);
        return v ? v._promise(C, D, "lock") : J.trans ? ti(J.transless, function() {
          return _._whenReady(D);
        }) : this._whenReady(D);
      }, je.prototype.table = function(h) {
        if (!f(this._allTables, h)) throw new pe.InvalidTable("Table ".concat(h, " does not exist"));
        return this._allTables[h];
      }, je);
      function je(h, u) {
        var g = this;
        this._middlewares = {}, this.verno = 0;
        var _ = je.dependencies;
        this._options = u = t({ addons: je.addons, autoOpen: !0, indexedDB: _.indexedDB, IDBKeyRange: _.IDBKeyRange, cache: "cloned" }, u), this._deps = { indexedDB: u.indexedDB, IDBKeyRange: u.IDBKeyRange }, _ = u.addons, this._dbSchema = {}, this._versions = [], this._storeNames = [], this._allTables = {}, this.idbdb = null, this._novip = this;
        var v, C, S, A, D, F = { dbOpenError: null, isBeingOpened: !1, onReadyBeingFired: null, openComplete: !1, dbReadyResolve: Pe, dbReadyPromise: null, cancelOpen: Pe, openCanceller: null, autoSchema: !0, PR1398_maxLoop: 3, autoOpen: u.autoOpen };
        F.dbReadyPromise = new Q(function(k) {
          F.dbReadyResolve = k;
        }), F.openCanceller = new Q(function(k, H) {
          F.cancelOpen = H;
        }), this._state = F, this.name = h, this.on = Ki(this, "populate", "blocked", "versionchange", "close", { ready: [ue, Pe] }), this.once = function(k, H) {
          var O = function() {
            for (var L = [], B = 0; B < arguments.length; B++) L[B] = arguments[B];
            g.on(k).unsubscribe(O), H.apply(g, L);
          };
          return g.on(k, O);
        }, this.on.ready.subscribe = j(this.on.ready.subscribe, function(k) {
          return function(H, O) {
            je.vip(function() {
              var L, B = g._state;
              B.openComplete ? (B.dbOpenError || Q.resolve().then(H), O && k(H)) : B.onReadyBeingFired ? (B.onReadyBeingFired.push(H), O && k(H)) : (k(H), L = g, O || k(function N() {
                L.on.ready.unsubscribe(H), L.on.ready.unsubscribe(N);
              }));
            });
          };
        }), this.Collection = (v = this, Zi(Dc.prototype, function(V, N) {
          this.db = v;
          var O = Os, L = null;
          if (N) try {
            O = N();
          } catch (G) {
            L = G;
          }
          var B = V._ctx, N = B.table, V = N.hook.reading.fire;
          this._ctx = { table: N, index: B.index, isPrimKey: !B.index || N.schema.primKey.keyPath && B.index === N.schema.primKey.name, range: O, keysOnly: !1, dir: "next", unique: "", algorithm: null, filter: null, replayFilter: null, justLimit: !0, isMatch: null, offset: 0, limit: 1 / 0, error: L, or: B.or, valueMapper: V !== Ct ? V : null };
        })), this.Table = (C = this, Zi(Ms.prototype, function(k, H, O) {
          this.db = C, this._tx = O, this.name = k, this.schema = H, this.hook = C._allTables[k] ? C._allTables[k].hook : Ki(null, { creating: [P, Pe], reading: [qi, Ct], updating: [q, Pe], deleting: [U, Pe] });
        })), this.Transaction = (S = this, Zi(Fc.prototype, function(k, H, O, L, B) {
          var N = this;
          k !== "readonly" && H.forEach(function(V) {
            V = (V = O[V]) === null || V === void 0 ? void 0 : V.yProps, V && (H = H.concat(V.map(function(G) {
              return G.updatesTable;
            })));
          }), this.db = S, this.mode = k, this.storeNames = H, this.schema = O, this.chromeTransactionDurability = L, this.idbtrans = null, this.on = Ki(this, "complete", "error", "abort"), this.parent = B || null, this.active = !0, this._reculock = 0, this._blockedFuncs = [], this._resolve = null, this._reject = null, this._waitingFor = null, this._waitingQueue = null, this._spinCount = 0, this._completion = new Q(function(V, G) {
            N._resolve = V, N._reject = G;
          }), this._completion.then(function() {
            N.active = !1, N.on.complete.fire();
          }, function(V) {
            var G = N.active;
            return N.active = !1, N.on.error.fire(V), N.parent ? N.parent._reject(V) : G && N.idbtrans && N.idbtrans.abort(), We(V);
          });
        })), this.Version = (A = this, Zi(Uc.prototype, function(k) {
          this.db = A, this._cfg = { version: k, storesSource: null, dbschema: {}, tables: {}, contentUpgrade: null };
        })), this.WhereClause = (D = this, Zi(Ws.prototype, function(k, H, O) {
          if (this.db = D, this._ctx = { table: k, index: H === ":id" ? null : H, or: O }, this._cmp = this._ascending = we, this._descending = function(L, B) {
            return we(B, L);
          }, this._max = function(L, B) {
            return 0 < we(L, B) ? L : B;
          }, this._min = function(L, B) {
            return we(L, B) < 0 ? L : B;
          }, this._IDBKeyRange = D._deps.IDBKeyRange, !this._IDBKeyRange) throw new pe.MissingAPI();
        })), this.on("versionchange", function(k) {
          0 < k.newVersion, g.close({ disableAutoOpen: !1 });
        }), this.on("blocked", function(k) {
          !k.newVersion || k.newVersion < k.oldVersion;
        }), this._maxKey = Ji(u.IDBKeyRange), this._createTransaction = function(k, H, O, L) {
          return new g.Transaction(k, H, O, g._options.chromeTransactionDurability, L);
        }, this._fireOnBlocked = function(k) {
          g.on("blocked").fire(k), Ei.filter(function(H) {
            return H.name === g.name && H !== g && !H._state.vcFired;
          }).map(function(H) {
            return H.on("versionchange").fire(k);
          });
        }, this.use(qc), this.use(Xc), this.use(Kc), this.use(jc), this.use(Gc);
        var M = new Proxy(this, { get: function(k, H, O) {
          if (H === "_vip") return !0;
          if (H === "table") return function(B) {
            return Kr(g.table(B), M);
          };
          var L = Reflect.get(k, H, O);
          return L instanceof Ms ? Kr(L, M) : H === "tables" ? L.map(function(B) {
            return Kr(B, M);
          }) : H === "_createTransaction" ? function() {
            return Kr(L.apply(this, arguments), M);
          } : L;
        } });
        this.vip = M, _.forEach(function(k) {
          return k(g);
        });
      }
      var Zr, gt = typeof Symbol < "u" && "observable" in Symbol ? Symbol.observable : "@@observable", Qc = (fo.prototype.subscribe = function(h, u, g) {
        return this._subscribe(h && typeof h != "function" ? h : { next: h, error: u, complete: g });
      }, fo.prototype[gt] = function() {
        return this;
      }, fo);
      function fo(h) {
        this._subscribe = h;
      }
      try {
        Zr = { indexedDB: r.indexedDB || r.mozIndexedDB || r.webkitIndexedDB || r.msIndexedDB, IDBKeyRange: r.IDBKeyRange || r.webkitIDBKeyRange };
      } catch {
        Zr = { indexedDB: null, IDBKeyRange: null };
      }
      function na(h) {
        var u, g = !1, _ = new Qc(function(v) {
          var C = Bt(h), S, A = !1, D = {}, F = {}, M = { get closed() {
            return A;
          }, unsubscribe: function() {
            A || (A = !0, S && S.abort(), k && jt.storagemutated.unsubscribe(O));
          } };
          v.start && v.start(M);
          var k = !1, H = function() {
            return Un(L);
          }, O = function(B) {
            jr(D, B), no(F, D) && H();
          }, L = function() {
            var B, N, V;
            !A && Zr.indexedDB && (D = {}, B = {}, S && S.abort(), S = new AbortController(), V = (function(G) {
              var Z = xi();
              try {
                C && Ti();
                var Y = Ht(h, G);
                return Y = C ? Y.finally(Vt) : Y;
              } finally {
                Z && Ci();
              }
            })(N = { subscr: B, signal: S.signal, requery: H, querier: h, trans: null }), Promise.resolve(V).then(function(G) {
              g = !0, u = G, A || N.signal.aborted || (D = {}, (function(Z) {
                for (var Y in Z) if (f(Z, Y)) return;
                return 1;
              })(F = B) || k || (jt(Qi, O), k = !0), Un(function() {
                return !A && v.next && v.next(G);
              }));
            }, function(G) {
              g = !1, ["DatabaseClosedError", "AbortError"].includes(G?.name) || A || Un(function() {
                A || v.error && v.error(G);
              });
            }));
          };
          return setTimeout(H, 0), M;
        });
        return _.hasValue = function() {
          return g;
        }, _.getValue = function() {
          return u;
        }, _;
      }
      var oi = zt;
      function po(h) {
        var u = Gt;
        try {
          Gt = !0, jt.storagemutated.fire(h), ao(h, !0);
        } finally {
          Gt = u;
        }
      }
      p(oi, t(t({}, It), { delete: function(h) {
        return new oi(h, { addons: [] }).delete();
      }, exists: function(h) {
        return new oi(h, { addons: [] }).open().then(function(u) {
          return u.close(), !0;
        }).catch("NoSuchDatabaseError", function() {
          return !1;
        });
      }, getDatabaseNames: function(h) {
        try {
          return u = oi.dependencies, g = u.indexedDB, u = u.IDBKeyRange, (to(g) ? Promise.resolve(g.databases()).then(function(_) {
            return _.map(function(v) {
              return v.name;
            }).filter(function(v) {
              return v !== Dr;
            });
          }) : eo(g, u).toCollection().primaryKeys()).then(h);
        } catch {
          return We(new pe.MissingAPI());
        }
        var u, g;
      }, defineClass: function() {
        return function(h) {
          a(this, h);
        };
      }, ignoreTransaction: function(h) {
        return J.trans ? ti(J.transless, h) : h();
      }, vip: io, async: function(h) {
        return function() {
          try {
            var u = lo(h.apply(this, arguments));
            return u && typeof u.then == "function" ? u : Q.resolve(u);
          } catch (g) {
            return We(g);
          }
        };
      }, spawn: function(h, u, g) {
        try {
          var _ = lo(h.apply(g, u || []));
          return _ && typeof _.then == "function" ? _ : Q.resolve(_);
        } catch (v) {
          return We(v);
        }
      }, currentTransaction: { get: function() {
        return J.trans || null;
      } }, waitFor: function(h, u) {
        return u = Q.resolve(typeof h == "function" ? oi.ignoreTransaction(h) : h).timeout(u || 6e4), J.trans ? J.trans.waitFor(u) : u;
      }, Promise: Q, debug: { get: function() {
        return X;
      }, set: function(h) {
        le(h);
      } }, derive: T, extend: a, props: p, override: j, Events: Ki, on: jt, liveQuery: na, extendObservabilitySet: jr, getByKeyPath: se, setByKeyPath: fe, delByKeyPath: function(h, u) {
        typeof u == "string" ? fe(h, u, void 0) : "length" in u && [].map.call(u, function(g) {
          fe(h, g, void 0);
        });
      }, shallowClone: Ie, deepClone: De, getObjectDiff: co, cmp: we, asap: ae, minKey: -1 / 0, addons: [], connections: Ei, errnames: xt, dependencies: Zr, cache: ni, semVer: "4.2.0", version: "4.2.0".split(".").map(function(h) {
        return parseInt(h);
      }).reduce(function(h, u, g) {
        return h + u / Math.pow(10, 2 * g);
      }) })), oi.maxKey = Ji(oi.dependencies.IDBKeyRange), typeof dispatchEvent < "u" && typeof addEventListener < "u" && (jt(Qi, function(h) {
        Gt || (h = new CustomEvent(Kn, { detail: h }), Gt = !0, dispatchEvent(h), Gt = !1);
      }), addEventListener(Kn, function(h) {
        h = h.detail, Gt || po(h);
      }));
      var Ai, Gt = !1, oa = function() {
      };
      return typeof BroadcastChannel < "u" && ((oa = function() {
        (Ai = new BroadcastChannel(Kn)).onmessage = function(h) {
          return h.data && po(h.data);
        };
      })(), typeof Ai.unref == "function" && Ai.unref(), jt(Qi, function(h) {
        Gt || Ai.postMessage(h);
      })), typeof addEventListener < "u" && (addEventListener("pagehide", function(h) {
        if (!zt.disableBfCache && h.persisted) {
          Ai?.close();
          for (var u = 0, g = Ei; u < g.length; u++) g[u].close({ disableAutoOpen: !1 });
        }
      }), addEventListener("pageshow", function(h) {
        !zt.disableBfCache && h.persisted && (oa(), po({ all: new $e(-1 / 0, [[]]) }));
      })), Q.rejectionMapper = function(h, u) {
        return !h || h instanceof _t || h instanceof TypeError || h instanceof SyntaxError || !h.name || !ei[h.name] ? h : (u = new ei[h.name](u || h.message, h), "stack" in h && x(u, "stack", { get: function() {
          return this.inner.stack;
        } }), u);
      }, le(X), t(zt, Object.freeze({ __proto__: null, Dexie: zt, liveQuery: na, Entity: zs, cmp: we, PropModification: Yi, replacePrefix: function(h, u) {
        return new Yi({ replacePrefix: [h, u] });
      }, add: function(h) {
        return new Yi({ add: h });
      }, remove: function(h) {
        return new Yi({ remove: h });
      }, default: zt, RangeSet: $e, mergeRanges: tr, rangesOverlap: Zs }), { default: zt }), zt;
    });
  })(hn)), hn.exports;
}
var jp = Wp();
const Qo = /* @__PURE__ */ _c(jp), el = Symbol.for("Dexie"), Rn = globalThis[el] || (globalThis[el] = Qo);
if (Qo.semVer !== Rn.semVer)
  throw new Error(`Two different versions of Dexie loaded in the same app: ${Qo.semVer} and ${Rn.semVer}`);
const {
  liveQuery: zm,
  mergeRanges: Fm,
  rangesOverlap: Lm,
  RangeSet: Mm,
  cmp: Nm,
  Entity: Bm,
  PropModification: Hm,
  replacePrefix: Vm,
  add: Um,
  remove: Wm,
  DexieYProvider: jm
} = Rn, vc = 2, Jo = 3, Gp = class extends Mt {
  constructor() {
    super(), this.kioskBaseUrl = "", this.autoRenderProgress = !0, this.autoRenderErrors = !0, this.appErrors = [], this.apiContext = void 0, this.showProgress = !1;
  }
  onAppMessage(n) {
    this.addAppError(n.detail.headline + "<br>" + n.detail.body);
  }
  firstUpdated(n) {
    super.firstUpdated(n), this.addEventListener("send-message", this.onAppMessage);
  }
  updated(n) {
    n.has("apiContext") && (this.showProgress = !1, this.apiContext && this.apiContext.status === Jo && this.addAppError("Cannot connect to Kiosk API."), !n.apiContext && this.apiContext && this.apiConnected());
  }
  apiConnected() {
  }
  render() {
    let n;
    return this.apiContext && this.apiContext.status === vc ? n = this.apiRender() : this.apiContext && this.apiContext.status === Jo ? n = this.renderApiError() : n = this.renderNoContextYet(), Be`
            <style>
                .system-message {
                    display: flex;
                    flex-direction: row;
                    justify-content: space-between;
                    border-style: solid;
                    border-width: 2px;
                    padding: 2px 1em;
                    position: relative;
                    margin-bottom: 10px;
                    background: linear-gradient(135deg, #882501, #bb3302);
                    color: #fabc02;
                }
                .system-message i {
                    
                }
                .loading {
                    display: flex;
                    justify-content: center;
                    align-items: center;
                    height: 5px;
                    width: 100vw;
                    background-color: black;
                }
                .loading-progress {
                    height: 5px;
                    width: 100%;
                    border-radius: 3px;
                    background: linear-gradient(
                        90deg,
                        red 0%,
                        yellow 15%,
                        lime 30%,
                        cyan 50%,
                        blue 65%,
                        magenta 80%,
                        red 100%
                    );
                    background-size: 200%;
                    animation: move-gradient 2s ease-in infinite;
                }
                @keyframes move-gradient {
                    0% {
                        background-position: 0% 0%;
                    }
                    100% {
                        background-position: -200% 0%;
                    }
                }
            </style>
            <link rel="stylesheet" href="${this.kioskBaseUrl}static/styles.css" />
            ${this.autoRenderProgress ? this.renderProgress() : Te} ${this.autoRenderErrors ? this.renderErrors() : Te} ${n}
        `;
  }
  renderNoContextYet() {
    return Be` <link rel="stylesheet" href="${this.kioskBaseUrl}static/styles.css" /> `;
  }
  renderApiError() {
  }
  renderErrors() {
    return this.appErrors.length > 0 ? Be` ${this.appErrors.map((n) => Be`<div class="system-message" @click="${this.errorClicked}"><span>${n}</span><i>x</i></div>`)} ` : Te;
  }
  errorClicked(n) {
    let e = n.currentTarget.children[0].textContent;
    e && this.deleteError(e);
  }
  renderProgress(n = !1) {
    if (n || this.showProgress)
      return Be` <div class="loading">
                <div class="loading-progress"></div>
            </div>`;
  }
  addAppError(n) {
    this.appErrors.push(n), this.requestUpdate();
  }
  clearAppErrors() {
    this.appErrors = [], this.requestUpdate();
  }
  deleteError(n) {
    let e = -1;
    this.appErrors.find((t, i) => t === n ? (e = i, !0) : !1), e > -1 && (this.appErrors.splice(e, 1), this.appErrors = [...this.appErrors]);
  }
};
Gp.properties = {
  apiContext: { type: Object },
  appErrors: { type: Array },
  showProgress: { type: Boolean },
  kioskBaseUrl: { type: String }
};
var qp = Object.defineProperty, yc = (s, n, e, t) => {
  for (var i = void 0, r = s.length - 1, o; r >= 0; r--)
    (o = s[r]) && (i = o(n, e, i) || i);
  return i && qp(n, e, i), i;
};
const bc = class extends Mt {
  constructor() {
    super(), this.showProgress = !1, this.kioskBaseUrl = "", this.apiContext = void 0;
  }
  updated(n) {
    n.has("apiContext") && (this.showProgress = !1);
  }
  /**
   * dispatches a BeforeEvent and coordinates calling cancelCallback and finishCallback either synchronously or asynchronously, if the
   * consumer of the event used .defer on the event object.
   *
   * The dispatched BeforeEvent has a method "defer" which returns a deferrance object that itself has methods finish and cancel.
   * One of those must be called to complete the event asynchronously.
   *
   * example for an emitter:
   *   tryClose() {
   *       if (!this.emitBeforeEvent("beforeClose", {},
   *           () => {
   *               this.closeDeferred = false
   *           },
   *           () => {
   *               this.open = false
   *           })
   *       ) this.closeDeferred = true
   * }
   *
   * example for an async consumer:
   *   beforeCloseLightbox(e: BeforeEvent) {
   *       const defObj = e.detail.defer(e)
   *       setTimeout(()=>defObj.finish(),1000)
   *   }
   *
   * example for a sync consumer that let's the event succeed:
   *   beforeCloseLightbox(e: BeforeEvent) {
   *       e.stopPropagation()
   *   }
   *
   *  example for a sync consumer that cancels the event:
   *   beforeCloseLightbox(e: BeforeEvent) {
   *       e.stopPropagation()
   *       e.preventDefault()
   *   }
   *
   * @param eventName Name a consumer can listen to
   * @param detail additional data to send to the consumer
   * @param cancelCallback the code to run in case the event gets cancelled (either synchronously or asynchronously)
   * @param finishCallback the code to run in case the event's default behaviour may proceed (either synchronously or asynchronously)
   * @returns false if the consumer of the event asked to defer the result of the event,
   *              otherwise true (in which case the default behaviour will run synchronously)
   */
  emitBeforeEvent(n, e, t, i) {
    let r = {
      // some this math is going on in here: here this points to the beforeEventDetail object. That's why the component's this needed saving
      component: this,
      _defer: !1,
      defer: function(l = void 0) {
        return this._defer = !0, l && (l.stopPropagation(), l.preventDefault()), {
          cancel: () => {
            this.component.updateComplete.then(() => {
              t();
            });
          },
          finish: () => {
            this.component.updateComplete.then(() => {
              i();
            });
          }
        };
      }
    };
    Object.assign(r, e);
    const o = new CustomEvent(n, {
      bubbles: !0,
      composed: !0,
      cancelable: !0,
      detail: r
    });
    if (this.dispatchEvent(o))
      i();
    else {
      if (r._defer)
        return !1;
      t();
    }
    return !0;
  }
  render() {
    let n;
    return this.apiContext && this.apiContext.status === vc ? n = this.apiRender() : this.apiContext && this.apiContext.status === Jo ? n = this.renderApiError() : n = this.renderNoContextYet(), Be`
            <link rel="stylesheet" href="${this.kioskBaseUrl}static/styles.css" />
            ${n}
        `;
  }
  renderNoContextYet() {
    return Be` <link rel="stylesheet" href="${this.kioskBaseUrl}static/styles.css" /> `;
  }
  renderApiError() {
  }
  renderProgress(n = !1) {
    if (n || this.showProgress)
      return Be` <div class="loading">
                <div class="loading-progress"></div>
            </div>`;
  }
};
bc.properties = {
  /**
   * The Api Context
   */
  apiContext: { type: Object }
};
let Pr = bc;
yc([
  At()
], Pr.prototype, "showProgress");
yc([
  Ge()
], Pr.prototype, "kioskBaseUrl");
const Do = "MSG_NETWORK_ERROR";
class Kp {
  constructor(n, e, t = "") {
    this.messageId = n, this.headline = e, this.body = t;
  }
}
function Oo(s, n, e, t = "") {
  let i = new Kp(
    n,
    e,
    t
  );
  s.dispatchEvent(new CustomEvent(
    "send-message",
    { bubbles: !0, composed: !0, detail: i }
  ));
}
function Zp(s, n) {
  return s && n in s ? s[n] : n.replace("_", " ");
}
function Yp(s, n, e = "", t = null) {
  if (e && (e += ": "), n.response) {
    if (n.response.status == 403 || n.response.status == 401) {
      Oo(
        s,
        Do,
        `${e}You are not logged in properly or your session has timed out`,
        '<a href="/logout">Please log in again.</a>'
      );
      return;
    }
    t ? t(n) : Oo(
      s,
      Do,
      `${e}Kiosk server responded with an error.`,
      `(${n.msg}). 
                The server might be down or perhaps you are not logged in properly.`
    );
  } else {
    Oo(
      s,
      Do,
      `${e}Kiosk server responded with a network error.`,
      `(${n}). 
            The server might be down or perhaps you are not logged in properly.`
    );
    return;
  }
}
class Xp {
  constructor(n) {
    this.apiContext = void 0, this.db = void 0, this.hasRefreshedFavourites = !1, this.hasRefreshedAll = !1, this.localCache = /* @__PURE__ */ new Map(), this.apiContext = n, this.db = this.initDb();
  }
  initDb() {
    const n = new Rn("KioskTimeZones");
    return n.version(1).stores({
      kioskTimeZones: "&id, tz_long, tz_IANA, deprecated, version, favourite"
    }), n;
  }
  getLocalCache() {
    return this.localCache;
  }
  async getFavouriteTimeZones(n = !1, e = !1) {
    if (this.db) {
      const t = await this.db.kioskTimeZones.count();
      let i;
      return t == 0 && await this.refreshFavourites(), n ? i = await this.db.kioskTimeZones.where({ favourite: 1 }).toArray() : i = await this.db.kioskTimeZones.where({ deprecated: 0, favourite: 1 }).toArray(), e && this.refreshFavourites().finally(() => {
      }), i;
    } else return [];
  }
  async refreshFavourites() {
    if (!this.db) return [];
    if (!this.hasRefreshedFavourites) {
      const n = await this.fetchFavouriteTimeZones();
      if (n && n.length > 0) {
        await this.db?.kioskTimeZones.where("favourite").equals(1).delete();
        const e = n.map((t) => ({
          id: t.id,
          tz_IANA: t.tz_IANA,
          tz_long: t.tz_long,
          deprecated: t.deprecated ? 1 : 0,
          version: t.version,
          favourite: 1
        }));
        return await this.db?.kioskTimeZones.bulkAdd(e), this.hasRefreshedFavourites = !0, e;
      }
    }
    return [];
  }
  async fetchFavouriteTimeZones() {
    return await this.apiContext?.fetchFromApi(
      "",
      "favouritetimezones",
      {
        method: "GET",
        caller: "app.fetchFavouriteTimeZones"
      }
    ).then((n) => n).catch((n) => []);
  }
  async fetchAllTimeZones(n = 0) {
    const e = new URLSearchParams();
    return e.append("include_deprecated", "true"), n > 0 && e.append("newer_than", `${n}`), await this.apiContext?.fetchFromApi(
      "",
      "timezones",
      {
        method: "GET",
        caller: "app.fetchFavouriteTimeZones"
      },
      "v1",
      e
    ).then((t) => t).catch((t) => []);
  }
  async getAllTimeZones(n = !1, e = !1) {
    return await this.refreshAllTimeZones(e), (await this.db?.kioskTimeZones.toArray())?.filter((t) => t.deprecated == 0 || n);
  }
  async getTimeZoneByIndex(n, e = !1) {
    if (!this.db) return;
    await this.refreshAllTimeZones(e);
    let t = await this.db.kioskTimeZones.where("id").equals(n).toArray();
    return t.length > 0 ? t[0] : void 0;
  }
  /**
   * Asynchronously caches the time zone information locally based on the provided timezone index.
   * Locally cached time zone information can be retrieved synchronously using getTimeZoneInfoFromLocalCache
   * @param tzIndex a Kiosk Time Zone Index
   * @param tryRefresh Flag indicating whether to refresh the timezone information if not found in the cache.
   * @returns A Promise that resolves to the cached TimeZone object if found, otherwise undefined.
   */
  async cacheLocally(n, e = !1) {
    if (!this.db || typeof n != "number") return;
    let t = this.localCache.get(n);
    if (t) return t;
    if (t = await this.getTimeZoneByIndex(n), !t) {
      if (!e) return;
      await this.refreshAllTimeZones(!1), t = await this.getTimeZoneByIndex(n);
    }
    return t && this.localCache.set(n, t), this.localCache.get(n);
  }
  /**
   * Retrieves timezone information from the local cache based on the provided timezone index.
   * Unlike other methods this is synchronous.
   * @param tzIndex - The index of the timezone to retrieve information for.
   * @returns a TimeZone instance if found in the local cache, otherwise undefined.
   */
  getTimeZoneInfoFromLocalCache(n) {
    if (typeof n == "string" && (n = parseInt(n)), !!n)
      return this.localCache.get(n);
  }
  async refreshAllTimeZones(n) {
    let e = [];
    if (this.db && (n || !this.hasRefreshedAll)) {
      let t = 0;
      const i = (await this.getFavouriteTimeZones()).filter((o) => o.favourite == 1).map((o) => o.id);
      if (!n)
        try {
          t = (await this.db.kioskTimeZones.where("favourite").equals(0).reverse().sortBy("version"))[0].version;
        } catch {
        }
      const r = await this.fetchAllTimeZones(t);
      r && r.length > 0 && (await this.db.kioskTimeZones.where("version").above(t).delete(), e = r.map((o) => ({
        id: o.id,
        tz_IANA: o.tz_IANA,
        tz_long: o.tz_long,
        deprecated: o.deprecated ? 1 : 0,
        version: o.version,
        favourite: i.includes(o.id) ? 1 : 0
      })), await this.db.kioskTimeZones.bulkAdd(e)), this.hasRefreshedAll = !0;
    }
  }
}
const Qp = '*{padding:0;margin:0;border:0px;-webkit-user-select:none;user-select:none}p,div{-webkit-user-select:text;user-select:text}select{-webkit-user-select:none;user-select:none}.col-bg-body-lighter{background-color:var(--col-bg-body-lighter)}.col-bg-body{background-color:var(--col-bg-body)}.col-bg-body-darker{background-color:var(--col-bg-body-darker)}.col-bg-1-lighter{background-color:var(--col-bg-1-lighter)}.col-bg-1{background-color:var(--col-bg-1)}.col-bg-1-darker{background-color:var(--col-bg-1-darker)}.col-bg-1-input{background-color:var(--col-bg-1-input)}.col-primary-bg-1{background-color:var(--col-primary-bg-1)}.col-accent-bg-1{background-color:var(--col-accent-bg-1)}.col-success-bg-1{background-color:var(--col-success-bg-1)}.col-warning-bg-1{background-color:var(--col-warning-bg-1)}.col-error-bg-1{background-color:var(--col-error-bg-1)}.col-bg-2-lighter{background-color:var(--col-bg-2-lighter)}.col-bg-2{background-color:var(--col-bg-2)}.col-bg-2-darker{background-color:var(--col-bg-2-darker)}.col-bg-2-button{background-color:var(--col-bg-2-button)}.col-bg-2-input{background-color:var(--col-bg-2-input)}.col-primary-bg-2{background-color:var(--col-primary-bg-2)}.col-accent-bg-2{background-color:var(--col-accent-bg-2)}.col-success-bg-2{background-color:var(--col-success-bg-2)}.col-warning-bg-2{background-color:var(--col-warning-bg-2)}.col-error-bg-2{background-color:var(--col-error-bg-2)}.col-bg-3-lighter{background-color:var(--col-bg-3-lighter)}.col-bg-3{background-color:var(--col-bg-3)}.col-bg-3-darker{background-color:var(--col-bg-3-darker)}.col-bg-3-button{background-color:var(--col-bg-3-button)}.col-bg-3-input{background-color:var(--col-bg-3-input)}.col-primary-bg-3{background-color:var(--col-primary-bg-3)}.col-accent-bg-3{background-color:var(--col-accent-bg-3)}.col-success-bg-3{background-color:var(--col-success-bg-3)}.col-warning-bg-3{background-color:var(--col-warning-bg-3)}.col-error-bg-3{background-color:var(--col-error-bg-3)}.col-bg-ack-lighter{background-color:var(--col-bg-ack-lighter)}.col-bg-ack{background-color:var(--col-bg-ack)}.col-bg-ack-darker{background-color:var(--col-bg-ack-darker)}.col-primary-bg-ack{background-color:var(--col-primary-bg-ack)}.col-accent-bg-ack{background-color:var(--col-accent-bg-ack)}.col-bg-att-lighter{background-color:var(--col-bg-att-lighter)}.col-bg-att{background-color:var(--col-bg-att)}.col-bg-att-darker{background-color:var(--col-bg-att-darker)}.col-primary-bg-att{background-color:var(--col-primary-bg-att)}.col-accent-bg-att{background-color:var(--col-accent-bg-att)}.col-bg-alert-lighter{background-color:var(--col-bg-alert-lighter)}.col-bg-alert{background-color:var(--col-bg-alert)}.col-bg-alert-darker{background-color:var(--col-bg-alert-darker)}.col-primary-bg-alert{background-color:var(--col-primary-bg-alert)}.col-accent-bg-alert{background-color:var(--col-accent-bg-alert)}.col-bg-btn-lighter{background-color:var(--col-bg-btn-lighter)}.col-bg-btn{background-color:var(--col-bg-btn)}.col-bg-btn-darker{background-color:var(--col-bg-btn-darker)}.col-primary-bg-btn{background-color:var(--col-primary-bg-btn)}.col-accent-bg-btn{background-color:var(--col-accent-bg-btn)}.pattern-diagonal-stripes-sm{background:repeating-linear-gradient(45deg,transparent,transparent 10px,currentColor 10px,currentColor 20px)}.pattern-dots-lg{background-image:radial-gradient(currentColor 1.5px,transparent 1.5px);background-size:15px 15px}.pattern-dots-md{background-image:radial-gradient(currentColor 1px,transparent 1px);background-size:8px 8px}.pattern-dots-medium-dense{background-image:radial-gradient(currentColor .5px,transparent .5px);background-size:3px 3px}.pattern-dots-sm{background-image:radial-gradient(currentColor .5px,transparent .5px);background-size:5px 5px}.pattern-cross-dots-md,.imagelist-background{background-image:radial-gradient(currentColor .7px,transparent 1px),radial-gradient(currentColor .7px,transparent 1px);background-size:6.5px 6.5px;background-position:0 0,3.5px 3.5px}.imagelist-background{background-color:#fff9;background-blend-mode:overlay}label{font-family:var(--monospace-font);font-size:.9em}input,select{-webkit-user-select:text;-moz-user-select:text;-ms-user-select:text;box-sizing:border-box;border-radius:0;user-select:text;font-family:var(--standard-text-font);background-color:var(--col-bg-1-input);border:none;color:var(--col-primary-bg-1);padding:2px;margin-bottom:2px;outline:none}textarea{outline:none}input:not([type=radio],[type=checkbox]),select,textarea{width:100%}input:not([type=radio],[type=checkbox]):focus,select:focus,textarea:focus{margin-bottom:0;border-bottom:2px solid rgb(0,0,0)}h1{font-size:var(--font-size-h1)}h2{font-size:var(--font-size-h2)}h3{font-size:var(--font-size-h3)}h4{font-size:var(--font-size-h4)}small,.font-small{font-size:var(--font-size-small)}button{width:60px;height:60px;border-radius:35px;border-style:solid;border-width:2px;background-color:var(--col-bg-btn);color:var(--col-primary-bg-btn);border-color:var(--col-bg-btn-darker);font-family:var(--headline-text-font);font-size:var(--font-size-h4);font-weight:700}button:hover,button:focus{outline:none;background-color:var(--col-bg-btn-lighter)}button:active{padding-top:3px;color:var(--col-primary-bg-btn);background-color:var(--col-bg-btn-darker);border-color:var(--col-bg-btn-darker)}.kiosk-btn-128{width:128px;height:128px}.kiosk-btn-32{width:32px;height:32px}.kiosk-dropdown-btn{box-sizing:content-box;border-radius:0;background-color:var(--col-bg-btn);border-color:var(--col-bg-btn-darker);color:var(--col-primary-bg-btn);height:30px;width:30px}.kiosk-dropdown-btn .drop-down-btn{float:left}.kiosk-dropdown-btn .caret{color:var(--col-primary-bg-btn);display:inline-block;width:0;height:0;margin-left:2px;vertical-align:middle;border-color:var(--col-primary-bg-btn);border-top:4px solid;border-right:4px solid transparent;border-left:4px solid transparent}button:disabled{opacity:.3}.modal-round-button,.modal-delete,.modal-close,.modal-cancel,.modal-back,.modal-next,.modal-ok,.modal-button{box-sizing:border-box;margin-left:25px;background-image:none;display:inline-block;font-style:normal;font-variant:normal;text-rendering:auto;-webkit-font-smoothing:antialiased;font-family:"Font Awesome 6 Free";font-weight:900;font-size:24px;line-height:24px;vertical-align:center;text-align:center}.modal-ok:after{content:var(--icon-bt-ok)}.modal-next:after{padding-left:2px;content:var(--icon-bt-next)}.modal-back:after{padding-right:2px;content:var(--icon-bt-back)}.modal-cancel:after{content:var(--icon-bt-cancel)}.modal-close:before{content:var(--icon-bt-close)}.modal-delete{background-color:var(--col-bg-alert);color:var(--col-primary-bg-alert)}.modal-delete:before{content:var(--icon-bt-trash)}.modal-delete:hover,.modal-delete:focus{border-color:var(--col-bg-alert);background-color:var(--col-bg-alert-lighter);background-size:75%}.modal-delete:active{border-color:var(--col-bg-alert);background-color:var(--col-bg-alert-darker);color:var(--col-primary-bg-alert)}.kiosk-rounded{border-radius:15px}.kiosk-shaded{box-shadow:2px 2px 10px #2e380394}.kiosk-margin-bottom{margin-bottom:1em}.flex-line-break{width:100%;height:0px}.kiosk-align-flex-end{align-items:flex-end}.kiosk-align-flex-start{align-items:flex-start}.full-background-bg-1{background:linear-gradient(to right bottom,var(--col-bg-1-darker),var(--col-bg-1-lighter));height:100%;width:100%}.small-list-button i{border-radius:25px;box-shadow:2px 2px 5px #2e380394}.small-list-button:hover{color:var(--col-bg-att)}.small-list-button:hover i{box-shadow:2px 2px 5px #2e380394}.small-list-button:active,.small-list-button:focus{color:var(--col-bg-ack);transform:translateY(5px)}.small-list-button:active i,.small-list-button:focus i{box-shadow:none}.dialog-radio-div{display:flex;flex-direction:row}.dialog-radio-div input[type=radio]{margin-right:.5em}.rainbow-loading{display:flex;justify-content:center;align-items:center;height:5px;width:100%;background-color:#000}.rainbow-loading.to-top{position:fixed;top:0;z-index:1000}.rainbow-loading-progress{height:5px;width:100%;border-radius:3px;background:linear-gradient(90deg,red,#ff0,#0f0 30%,#0ff 50%,#00f,#f0f 80%,red);background-size:200%;animation:move-gradient 2s ease-in infinite}@keyframes move-gradient{0%{background-position:0% 0%}to{background-position:-200% 0%}}.fa,.fas{font-family:"Font Awesome 6 Free";font-weight:900;font-style:normal}.fa-trash:before{content:""}.fa-view-grid:before{content:""}.fa-reload:before{content:""}.fa-view-list:before{content:""}.fa-camera:before{content:""}.fa-view-image:before{content:""}.fa-check:before{content:""}.fa-bug:before{content:""}.fa-lightbulb:before{content:""}.fa-query:before{content:""}.fa-footsteps{transform:rotate(270deg)}.fa-footsteps:before{content:""}.fa-next:before{content:""}.fa-prev:before{content:""}.fa-hide:before{content:""}.fa-circle-half-stroke:before{content:""}i{font-family:"Font Awesome 6 Free";font-style:normal;font-variant:normal;text-rendering:auto;-webkit-font-smoothing:antialiased;font-weight:900;font-size:.9em}div,p{font-family:var(--standard-text-font)}dialog{margin:auto;width:100%;height:100%;background-color:unset;outline:none;border:none}dialog .dialog-outer-zone{display:grid;background-color:unset;width:100%;height:100%;place-items:center}.dialog-frame{position:relative;background-color:var(--col-bg-1);display:flex;padding:20px 25px 10px;flex-direction:column;justify-content:space-between;border:2px solid var(--col-bg-1)}.dialog-header{height:auto;display:flex;width:100%;flex-direction:row;flex-wrap:nowrap;align-items:center;padding-bottom:.5rem;margin-top:.5em;margin-right:1em;border:0px solid black;border-bottom-width:1px;margin-bottom:1rem}.dialog-image{box-sizing:border-box;padding-right:10px;background-repeat:no-repeat;z-index:0}.dialog-name h3{font-family:var(--monospace-font)}.close-button{-webkit-user-select:none;user-select:none;position:absolute;font-size:var(--font-size-h3);color:var(--col-primary-bg-1);margin-left:auto;right:.5rem;top:.5rem}.close-button :hover{background-color:var(--col-bg-btn);color:var(--col-primary-bg-btn)}.close-button :active{background-color:var(--col-bg-btn);color:var(--col-accent-bg-btn)}';
var Jp = Object.defineProperty, $p = Object.getOwnPropertyDescriptor, eg = Object.getPrototypeOf, tg = Reflect.get, wc = (s, n, e, t) => {
  for (var i = t > 1 ? void 0 : t ? $p(n, e) : n, r = s.length - 1, o; r >= 0; r--)
    (o = s[r]) && (i = (t ? o(n, e, i) : o(i)) || i);
  return t && i && Jp(n, e, i), i;
}, ig = (s, n, e) => tg(eg(s), e, n);
let pi = class extends Mt {
  constructor() {
    super(...arguments), this.heading = "";
  }
  firstUpdated(s) {
    super.firstUpdated(s);
  }
  openDialog() {
    let s = this.shadowRoot?.querySelector("dialog");
    s && s.showModal();
  }
  closeDialog(s) {
    let n = this.shadowRoot?.querySelector("dialog");
    n && n.close(s);
  }
  _overlayClicked(s) {
    s.target == this.shadowRoot?.querySelector(".dialog-outer-zone") && this.closeDialog();
  }
  _onCloseDialog() {
    let s = this.shadowRoot?.querySelector("dialog"), n = new CustomEvent("kiosk-dialog-closed", {
      bubbles: !1,
      cancelable: !1,
      composed: !1,
      detail: s?.returnValue
    });
    this.dispatchEvent(n);
  }
  render() {
    return Be`
            <dialog id="my-dialog" @close="${this._onCloseDialog}">
                <div class="dialog-outer-zone" @click="${this._overlayClicked}">
                    <div class="dialog-frame">
                        <div class="dialog-header">
                            <div class="dialog-image">
                                <slot name="dialog-image"></slot>
                            </div>
                            <div class="dialog-name">
                                <h3 class="dialog-title">${this.heading}</h3>
                            </div>
                            <div class="close-button" @click="${this.closeDialog}">
                                <i class="fa"></i>
                            </div>
                        </div>
                        <slot name="dialog-content">
                        </slot>
                    </div>
                </div>
            </dialog>
        `;
  }
};
pi.styles = wr(Qp);
pi.properties = {
  ...ig(pi, pi, "properties")
};
wc([
  Ge()
], pi.prototype, "heading", 2);
pi = wc([
  Bn("kiosk-dialog")
], pi);
/**
 * @license
 * Copyright 2017 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const As = { ATTRIBUTE: 1, CHILD: 2, ELEMENT: 6 }, xc = (s) => (...n) => ({ _$litDirective$: s, values: n });
class Cc {
  constructor(n) {
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  _$AT(n, e, t) {
    this._$Ct = n, this._$AM = e, this._$Ci = t;
  }
  _$AS(n, e) {
    return this.update(n, e);
  }
  update(n, e) {
    return this.render(...e);
  }
}
/**
 * @license
 * Copyright 2020 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const rg = (s) => s.strings === void 0;
/**
 * @license
 * Copyright 2017 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const ur = (s, n) => {
  const e = s._$AN;
  if (e === void 0) return !1;
  for (const t of e) t._$AO?.(n, !1), ur(t, n);
  return !0;
}, An = (s) => {
  let n, e;
  do {
    if ((n = s._$AM) === void 0) break;
    e = n._$AN, e.delete(s), s = n;
  } while (e?.size === 0);
}, Tc = (s) => {
  for (let n; n = s._$AM; s = n) {
    let e = n._$AN;
    if (e === void 0) n._$AN = e = /* @__PURE__ */ new Set();
    else if (e.has(s)) break;
    e.add(s), sg(n);
  }
};
function ng(s) {
  this._$AN !== void 0 ? (An(this), this._$AM = s, Tc(this)) : this._$AM = s;
}
function og(s, n = !1, e = 0) {
  const t = this._$AH, i = this._$AN;
  if (i !== void 0 && i.size !== 0) if (n) if (Array.isArray(t)) for (let r = e; r < t.length; r++) ur(t[r], !1), An(t[r]);
  else t != null && (ur(t, !1), An(t));
  else ur(this, s);
}
const sg = (s) => {
  s.type == As.CHILD && (s._$AP ??= og, s._$AQ ??= ng);
};
class ag extends Cc {
  constructor() {
    super(...arguments), this._$AN = void 0;
  }
  _$AT(n, e, t) {
    super._$AT(n, e, t), Tc(this), this.isConnected = n._$AU;
  }
  _$AO(n, e = !0) {
    n !== this.isConnected && (this.isConnected = n, n ? this.reconnected?.() : this.disconnected?.()), e && (ur(this, n), An(this));
  }
  setValue(n) {
    if (rg(this._$Ct)) this._$Ct._$AI(n, this);
    else {
      const e = [...this._$Ct._$AH];
      e[this._$Ci] = n, this._$Ct._$AI(e, this, 0);
    }
  }
  disconnected() {
  }
  reconnected() {
  }
}
/**
 * @license
 * Copyright (c) 2016 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const tl = Symbol("valueNotInitialized");
class lg extends ag {
  constructor(n) {
    if (super(n), n.type !== As.ELEMENT)
      throw new Error(`\`${this.constructor.name}\` must be bound to an element.`);
    this.previousValue = tl;
  }
  /** @override */
  render(n, e) {
    return Te;
  }
  /** @override */
  update(n, [e, t]) {
    return this.hasChanged(t) && (this.host = n.options && n.options.host, this.element = n.element, this.renderer = e, this.previousValue === tl ? this.addRenderer() : this.runRenderer(), this.previousValue = Array.isArray(t) ? [...t] : t), Te;
  }
  /** @override */
  reconnected() {
    this.addRenderer();
  }
  /** @override */
  disconnected() {
    this.removeRenderer();
  }
  /** @abstract */
  addRenderer() {
    throw new Error("The `addRenderer` method must be implemented.");
  }
  /** @abstract */
  runRenderer() {
    throw new Error("The `runRenderer` method must be implemented.");
  }
  /** @abstract */
  removeRenderer() {
    throw new Error("The `removeRenderer` method must be implemented.");
  }
  /** @protected */
  renderRenderer(n, ...e) {
    const t = this.renderer.call(this.host, ...e);
    cl(t, n, { host: this.host });
  }
  /** @protected */
  hasChanged(n) {
    return Array.isArray(n) ? !Array.isArray(this.previousValue) || this.previousValue.length !== n.length ? !0 : n.some((e, t) => e !== this.previousValue[t]) : this.previousValue !== n;
  }
}
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
const il = Symbol("contentUpdateDebouncer");
/**
 * @license
 * Copyright (c) 2017 - 2024 Vaadin Ltd.
 * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
 */
class cg extends lg {
  /**
   * A property to that the renderer callback will be assigned.
   *
   * @abstract
   */
  get rendererProperty() {
    throw new Error("The `rendererProperty` getter must be implemented.");
  }
  /**
   * Adds the renderer callback to the grid column.
   */
  addRenderer() {
    this.element[this.rendererProperty] = (n, e) => {
      this.renderRenderer(n, e);
    };
  }
  /**
   * Runs the renderer callback on the grid column.
   */
  runRenderer() {
    const n = this.element._grid;
    n[il] = ze.debounce(n[il], yt, () => {
      n.requestContentUpdate();
    });
  }
  /**
   * Removes the renderer callback from the grid column.
   */
  removeRenderer() {
    this.element[this.rendererProperty] = null;
  }
}
class hg extends cg {
  get rendererProperty() {
    return "renderer";
  }
  addRenderer() {
    this.element[this.rendererProperty] = (n, e, t) => {
      this.renderRenderer(n, t.item, t, e);
    };
  }
}
const dg = xc(hg);
var ug = Object.defineProperty, fg = Object.getOwnPropertyDescriptor, pg = Object.getPrototypeOf, gg = Reflect.get, Ec = (s) => {
  throw TypeError(s);
}, bt = (s, n, e, t) => {
  for (var i = t > 1 ? void 0 : t ? fg(n, e) : n, r = s.length - 1, o; r >= 0; r--)
    (o = s[r]) && (i = (t ? o(n, e, i) : o(i)) || i);
  return t && i && ug(n, e, i), i;
}, Pc = (s, n, e) => n.has(s) || Ec("Cannot " + e), nn = (s, n, e) => (Pc(s, n, "read from private field"), e ? e.call(s) : n.get(s)), on = (s, n, e) => n.has(s) ? Ec("Cannot add the same private member more than once") : n instanceof WeakSet ? n.add(s) : n.set(s, e), sn = (s, n, e, t) => (Pc(s, n, "write to private field"), n.set(s, e), e), mg = (s, n, e) => gg(pg(s), e, n), dn, un, fn, pn;
let it = class extends Pr {
  constructor() {
    super(), this.constants = {}, on(this, dn, []), on(this, un, []), this.searchIdentifier = "", this.heading = "select archaeological entity", this.identifierColumnTitle = "entity", this.initialRecordType = "unit", on(this, fn, []), on(this, pn, {}), this.recordTypeFilter = [], this.cellRenderer = (s) => {
      const n = Zp(this.recordTypeAliases, s.record_type);
      return Be`
            <div>
                ${n}
            </div>`;
    }, Ze("vaadin-grid", Ee`
            :host [part~="header-cell"] ::slotted(vaadin-grid-cell-content), [part~="footer-cell"] ::slotted(vaadin-grid-cell-content), [part~="reorder-ghost"] {
                font-weight: bold
            }
        `);
  }
  get identifiers() {
    return nn(this, dn);
  }
  set identifiers(s) {
    sn(this, dn, s);
  }
  get _identifiers() {
    return nn(this, un);
  }
  set _identifiers(s) {
    sn(this, un, s);
  }
  get selectedItems() {
    return nn(this, fn);
  }
  set selectedItems(s) {
    sn(this, fn, s);
  }
  get recordTypeAliases() {
    return nn(this, pn);
  }
  set recordTypeAliases(s) {
    sn(this, pn, s);
  }
  updated(s) {
    if (super.updated(s), s.has("apiContext") && this.apiConnected(), s.has("selectedItems") && this.selectedItems.length > 0) {
      const n = this.shadowRoot?.querySelector("kiosk-dialog");
      n && setTimeout(() => {
        const e = new CustomEvent("closeSelection", {
          detail: this.selectedItems[0]
        });
        this.dispatchEvent(e), n.closeDialog();
      }, 250);
    }
  }
  willUpdate(s) {
    (s.has("identifiers") || s.has("recordTypeFilter")) && this.prepareIdentifiers();
  }
  apiConnected() {
    (!this.identifiers || this.identifiers.length == 0) && this.fetchIdentifiers();
  }
  fetchIdentifiers() {
    this.apiContext.fetchFromApi(
      "",
      "contexts/full",
      {
        method: "GET",
        caller: "app.fetchIdentifiers"
      }
    ).then((s) => {
      this.identifiers = s.identifiers;
    }).catch((s) => {
      this.showProgress = !1, Yp(this, s, "loadConstants");
    });
  }
  prepareIdentifiers() {
    this.recordTypeFilter.length > 0 ? this._identifiers = this.identifiers.filter((s) => this.recordTypeFilter.includes(s.record_type.toLowerCase())) : this._identifiers = this.identifiers;
  }
  filterIdentifiers() {
    return this.searchIdentifier === "" && this.initialRecordType !== "" ? this._identifiers.filter((s) => s.record_type === this.initialRecordType) : this.searchIdentifier || this._identifiers.length < 50 ? this._identifiers.filter((s) => s.identifier.toLowerCase().startsWith(this.searchIdentifier)) : [];
  }
  openDialog() {
    (this.shadowRoot?.querySelector("kiosk-dialog")).openDialog();
  }
  searchChanged(s) {
    let n = s.target.value;
    this.searchIdentifier = n.toLowerCase();
  }
  activeItemChanged(s) {
    const n = s.detail.value;
    n && (this.selectedItems = [n]);
  }
  renderGrid() {
    return Be`
            <vaadin-grid id="grid" class="selection-grid"
                         .items=${this.filterIdentifiers()}
                         .selectedItems="${this.selectedItems}"
                         @active-item-changed="${this.activeItemChanged}">
                <vaadin-grid-column header="${this.identifierColumnTitle}" path="identifier"></vaadin-grid-column>
                <vaadin-grid-column header="type" ${dg(this.cellRenderer, [])}></vaadin-grid-column>
            </vaadin-grid>
        `;
  }
  apiRender() {
    return Be`
            <kiosk-dialog api heading="${this.heading}">
                <!--svg slot="dialog-image" xmlns="http://www.w3.org/2000/svg" width="auto" height="auto" viewBox="0 0 24 24" style="fill: rgba(0, 0, 0, 1);transform: ;msFilter:;"><path d="M11 19.91 10 22h4l-1-2.09c4-.65 7-5.28 7-9.91a8 8 0 0 0-16 0c0 4.63 3.08 9.26 7 9.91zm1-15.66v1.5A4.26 4.26 0 0 0 7.75 10h-1.5A5.76 5.76 0 0 1 12 4.25z"></path></svg-->
                <div slot="dialog-image">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"
                         style="fill: rgba(0, 0, 0, 1);transform: ;msFilter:;">
                        <path d="M4 22h12v-2H4V8H2v12c0 1.103.897 2 2 2z"></path>
                        <path
                            d="M20 2H8c-1.103 0-2 .897-2 2v12c0 1.103.897 2 2 2h12c1.103 0 2-.897 2-2V4c0-1.103-.897-2-2-2zm-2 9h-3v3h-2v-3h-3V9h3V6h2v3h3v2z"></path>
                    </svg>
                </div>
                <div slot="dialog-content">
                    <label for="identifier">search word expands selection</label>
                    <input id="identifier" type="text" @input="${this.searchChanged}" autofocus>
                    ${this.renderGrid()}
                </div>
            </kiosk-dialog>
        `;
  }
};
dn = /* @__PURE__ */ new WeakMap();
un = /* @__PURE__ */ new WeakMap();
fn = /* @__PURE__ */ new WeakMap();
pn = /* @__PURE__ */ new WeakMap();
it.styles = wr(Np);
it.properties = {
  ...mg(it, it, "properties")
};
bt([
  Ge()
], it.prototype, "constants", 2);
bt([
  Ge()
], it.prototype, "identifiers", 1);
bt([
  At()
], it.prototype, "_identifiers", 1);
bt([
  Ge()
], it.prototype, "searchIdentifier", 2);
bt([
  Ge()
], it.prototype, "heading", 2);
bt([
  Ge()
], it.prototype, "identifierColumnTitle", 2);
bt([
  Ge()
], it.prototype, "initialRecordType", 2);
bt([
  Ge()
], it.prototype, "selectedItems", 1);
bt([
  Ge()
], it.prototype, "recordTypeAliases", 1);
bt([
  Ge()
], it.prototype, "recordTypeFilter", 2);
it = bt([
  Bn("kiosk-context-selector")
], it);
const _g = '*{padding:0;margin:0;border:0px;-webkit-user-select:none;user-select:none}p,div{-webkit-user-select:text;user-select:text}select{-webkit-user-select:none;user-select:none}.col-bg-body-lighter{background-color:var(--col-bg-body-lighter)}.col-bg-body{background-color:var(--col-bg-body)}.col-bg-body-darker{background-color:var(--col-bg-body-darker)}.col-bg-1-lighter{background-color:var(--col-bg-1-lighter)}.col-bg-1{background-color:var(--col-bg-1)}.col-bg-1-darker{background-color:var(--col-bg-1-darker)}.col-bg-1-input{background-color:var(--col-bg-1-input)}.col-primary-bg-1{background-color:var(--col-primary-bg-1)}.col-accent-bg-1{background-color:var(--col-accent-bg-1)}.col-success-bg-1{background-color:var(--col-success-bg-1)}.col-warning-bg-1{background-color:var(--col-warning-bg-1)}.col-error-bg-1{background-color:var(--col-error-bg-1)}.col-bg-2-lighter{background-color:var(--col-bg-2-lighter)}.col-bg-2{background-color:var(--col-bg-2)}.col-bg-2-darker{background-color:var(--col-bg-2-darker)}.col-bg-2-button{background-color:var(--col-bg-2-button)}.col-bg-2-input{background-color:var(--col-bg-2-input)}.col-primary-bg-2{background-color:var(--col-primary-bg-2)}.col-accent-bg-2{background-color:var(--col-accent-bg-2)}.col-success-bg-2{background-color:var(--col-success-bg-2)}.col-warning-bg-2{background-color:var(--col-warning-bg-2)}.col-error-bg-2{background-color:var(--col-error-bg-2)}.col-bg-3-lighter{background-color:var(--col-bg-3-lighter)}.col-bg-3{background-color:var(--col-bg-3)}.col-bg-3-darker{background-color:var(--col-bg-3-darker)}.col-bg-3-button{background-color:var(--col-bg-3-button)}.col-bg-3-input{background-color:var(--col-bg-3-input)}.col-primary-bg-3{background-color:var(--col-primary-bg-3)}.col-accent-bg-3{background-color:var(--col-accent-bg-3)}.col-success-bg-3{background-color:var(--col-success-bg-3)}.col-warning-bg-3{background-color:var(--col-warning-bg-3)}.col-error-bg-3{background-color:var(--col-error-bg-3)}.col-bg-ack-lighter{background-color:var(--col-bg-ack-lighter)}.col-bg-ack{background-color:var(--col-bg-ack)}.col-bg-ack-darker{background-color:var(--col-bg-ack-darker)}.col-primary-bg-ack{background-color:var(--col-primary-bg-ack)}.col-accent-bg-ack{background-color:var(--col-accent-bg-ack)}.col-bg-att-lighter{background-color:var(--col-bg-att-lighter)}.col-bg-att{background-color:var(--col-bg-att)}.col-bg-att-darker{background-color:var(--col-bg-att-darker)}.col-primary-bg-att{background-color:var(--col-primary-bg-att)}.col-accent-bg-att{background-color:var(--col-accent-bg-att)}.col-bg-alert-lighter{background-color:var(--col-bg-alert-lighter)}.col-bg-alert{background-color:var(--col-bg-alert)}.col-bg-alert-darker{background-color:var(--col-bg-alert-darker)}.col-primary-bg-alert{background-color:var(--col-primary-bg-alert)}.col-accent-bg-alert{background-color:var(--col-accent-bg-alert)}.col-bg-btn-lighter{background-color:var(--col-bg-btn-lighter)}.col-bg-btn{background-color:var(--col-bg-btn)}.col-bg-btn-darker{background-color:var(--col-bg-btn-darker)}.col-primary-bg-btn{background-color:var(--col-primary-bg-btn)}.col-accent-bg-btn{background-color:var(--col-accent-bg-btn)}.pattern-diagonal-stripes-sm{background:repeating-linear-gradient(45deg,transparent,transparent 10px,currentColor 10px,currentColor 20px)}.pattern-dots-lg{background-image:radial-gradient(currentColor 1.5px,transparent 1.5px);background-size:15px 15px}.pattern-dots-md{background-image:radial-gradient(currentColor 1px,transparent 1px);background-size:8px 8px}.pattern-dots-medium-dense{background-image:radial-gradient(currentColor .5px,transparent .5px);background-size:3px 3px}.pattern-dots-sm{background-image:radial-gradient(currentColor .5px,transparent .5px);background-size:5px 5px}.pattern-cross-dots-md,.imagelist-background{background-image:radial-gradient(currentColor .7px,transparent 1px),radial-gradient(currentColor .7px,transparent 1px);background-size:6.5px 6.5px;background-position:0 0,3.5px 3.5px}.imagelist-background{background-color:#fff9;background-blend-mode:overlay}label{font-family:var(--monospace-font);font-size:.9em}input,select{-webkit-user-select:text;-moz-user-select:text;-ms-user-select:text;box-sizing:border-box;border-radius:0;user-select:text;font-family:var(--standard-text-font);background-color:var(--col-bg-1-input);border:none;color:var(--col-primary-bg-1);padding:2px;margin-bottom:2px;outline:none}textarea{outline:none}input:not([type=radio],[type=checkbox]),select,textarea{width:100%}input:not([type=radio],[type=checkbox]):focus,select:focus,textarea:focus{margin-bottom:0;border-bottom:2px solid rgb(0,0,0)}h1{font-size:var(--font-size-h1)}h2{font-size:var(--font-size-h2)}h3{font-size:var(--font-size-h3)}h4{font-size:var(--font-size-h4)}small,.font-small{font-size:var(--font-size-small)}button{width:60px;height:60px;border-radius:35px;border-style:solid;border-width:2px;background-color:var(--col-bg-btn);color:var(--col-primary-bg-btn);border-color:var(--col-bg-btn-darker);font-family:var(--headline-text-font);font-size:var(--font-size-h4);font-weight:700}button:hover,button:focus{outline:none;background-color:var(--col-bg-btn-lighter)}button:active{padding-top:3px;color:var(--col-primary-bg-btn);background-color:var(--col-bg-btn-darker);border-color:var(--col-bg-btn-darker)}.kiosk-btn-128{width:128px;height:128px}.kiosk-btn-32{width:32px;height:32px}.kiosk-dropdown-btn{box-sizing:content-box;border-radius:0;background-color:var(--col-bg-btn);border-color:var(--col-bg-btn-darker);color:var(--col-primary-bg-btn);height:30px;width:30px}.kiosk-dropdown-btn .drop-down-btn{float:left}.kiosk-dropdown-btn .caret{color:var(--col-primary-bg-btn);display:inline-block;width:0;height:0;margin-left:2px;vertical-align:middle;border-color:var(--col-primary-bg-btn);border-top:4px solid;border-right:4px solid transparent;border-left:4px solid transparent}button:disabled{opacity:.3}.modal-round-button,.modal-delete,.modal-close,.modal-cancel,.modal-back,.modal-next,.modal-ok,.modal-button{box-sizing:border-box;margin-left:25px;background-image:none;display:inline-block;font-style:normal;font-variant:normal;text-rendering:auto;-webkit-font-smoothing:antialiased;font-family:"Font Awesome 6 Free";font-weight:900;font-size:24px;line-height:24px;vertical-align:center;text-align:center}.modal-ok:after{content:var(--icon-bt-ok)}.modal-next:after{padding-left:2px;content:var(--icon-bt-next)}.modal-back:after{padding-right:2px;content:var(--icon-bt-back)}.modal-cancel:after{content:var(--icon-bt-cancel)}.modal-close:before{content:var(--icon-bt-close)}.modal-delete{background-color:var(--col-bg-alert);color:var(--col-primary-bg-alert)}.modal-delete:before{content:var(--icon-bt-trash)}.modal-delete:hover,.modal-delete:focus{border-color:var(--col-bg-alert);background-color:var(--col-bg-alert-lighter);background-size:75%}.modal-delete:active{border-color:var(--col-bg-alert);background-color:var(--col-bg-alert-darker);color:var(--col-primary-bg-alert)}.kiosk-rounded{border-radius:15px}.kiosk-shaded{box-shadow:2px 2px 10px #2e380394}.kiosk-margin-bottom{margin-bottom:1em}.flex-line-break{width:100%;height:0px}.kiosk-align-flex-end{align-items:flex-end}.kiosk-align-flex-start{align-items:flex-start}.full-background-bg-1{background:linear-gradient(to right bottom,var(--col-bg-1-darker),var(--col-bg-1-lighter));height:100%;width:100%}.small-list-button i{border-radius:25px;box-shadow:2px 2px 5px #2e380394}.small-list-button:hover{color:var(--col-bg-att)}.small-list-button:hover i{box-shadow:2px 2px 5px #2e380394}.small-list-button:active,.small-list-button:focus{color:var(--col-bg-ack);transform:translateY(5px)}.small-list-button:active i,.small-list-button:focus i{box-shadow:none}.dialog-radio-div{display:flex;flex-direction:row}.dialog-radio-div input[type=radio]{margin-right:.5em}.rainbow-loading{display:flex;justify-content:center;align-items:center;height:5px;width:100%;background-color:#000}.rainbow-loading.to-top{position:fixed;top:0;z-index:1000}.rainbow-loading-progress{height:5px;width:100%;border-radius:3px;background:linear-gradient(90deg,red,#ff0,#0f0 30%,#0ff 50%,#00f,#f0f 80%,red);background-size:200%;animation:move-gradient 2s ease-in infinite}@keyframes move-gradient{0%{background-position:0% 0%}to{background-position:-200% 0%}}.fa,.fas{font-family:"Font Awesome 6 Free";font-weight:900;font-style:normal}.fa-trash:before{content:""}.fa-view-grid:before{content:""}.fa-reload:before{content:""}.fa-view-list:before{content:""}.fa-camera:before{content:""}.fa-view-image:before{content:""}.fa-check:before{content:""}.fa-bug:before{content:""}.fa-lightbulb:before{content:""}.fa-query:before{content:""}.fa-footsteps{transform:rotate(270deg)}.fa-footsteps:before{content:""}.fa-next:before{content:""}.fa-prev:before{content:""}.fa-hide:before{content:""}.fa-circle-half-stroke:before{content:""}i{font-family:"Font Awesome 6 Free";font-style:normal;font-variant:normal;text-rendering:auto;-webkit-font-smoothing:antialiased;font-weight:900;font-size:.9em}div,p{font-family:var(--standard-text-font)}:host{--lumo-font-size-s: $font-size-standard;--lumo-font-family: $standard-text;--lumo-size-m: $font-size-standard;--lumo-contrast-10pct: rgba(0,0,0,0);--lumo-contrast-60pct: var(--col-accent-bg-1);--lumo-text-field-size: 1em;--lumo-font-size: var(--font-size-standard);--lumo-border-radius-l: 0;--lumo-border-radius-m: 0;--lumo-border-radius-s: 0}vaadin-combo-box{margin-left:calc(-1 * (.375em + var(--lumo-border-radius-m) / 4 - 1px));margin-right:calc(-1 * (.375em + var(--lumo-border-radius-m) / 4 - 1px));padding-top:0}vaadin-combo-box>input{--_lumo-text-field-overflow-mask-image: white;--lumo-text-field-size: 22px;background-color:#fff;max-height:var(--tz-combobox-max-height, 40);padding:var(--tz-combobox-horizontal-padding, 5px) var(--tz-combobox-vertical-padding, 5px)}vaadin-combo-box>input:disabled{background-color:var(--col-bg-1-lighter);--lumo-disabled-text-color: $col-primary-bg-1}vaadin-combo-box{font-family:var(--standard-text-font);font-size:var(--font-size-standard, 1rem);color:var(--col-primary-bg-1);background-color:var(--col-bg-2);width:100%;padding:0}vaadin-combo-box input:disabled,vaadin-combo-box textarea:disabled{background-color:var(--col-bg-1-lighter);color:var(--col-primary-bg-1);-webkit-text-fill-color:currentcolor;opacity:1}vaadin-combo-box::part(input-field){padding:0}';
var vg = Object.defineProperty, yg = Object.getOwnPropertyDescriptor, bg = Object.getPrototypeOf, wg = Reflect.get, ji = (s, n, e, t) => {
  for (var i = t > 1 ? void 0 : t ? yg(n, e) : n, r = s.length - 1, o; r >= 0; r--)
    (o = s[r]) && (i = (t ? o(n, e, i) : o(i)) || i);
  return t && i && vg(n, e, i), i;
}, xg = (s, n, e) => wg(bg(s), e, n);
let vt = class extends Pr {
  // @property()
  // public timeZone: string;
  constructor() {
    super(), this.timeZones = [], this.value = null, this.text = "", this.disabled = !1, this.includeDeprecated = !1;
  }
  updated(s) {
    if (super.updated(s), s.has("apiContext") && this.apiConnected(), s.has("value")) {
      const n = this.shadowRoot?.querySelector("vaadin-combo-box");
      n && !this.value && (n.value = "");
    }
  }
  // willUpdate(_changedProperties: any) {
  //     // if (_changedProperties.has("identifiers") || _changedProperties.has("recordTypeFilter")) {
  //     //     this.prepareIdentifiers();
  //     // }
  // }
  apiConnected() {
    this.kioskTimeZones = new Xp(this.apiContext), this.fetchFavouriteTimeZones();
  }
  fetchFavouriteTimeZones() {
    this.kioskTimeZones && this.kioskTimeZones.getFavouriteTimeZones(!1, !1).then((s) => {
      this.addTimeZones(s, !0), this.kioskTimeZones && this.kioskTimeZones.getAllTimeZones(this.includeDeprecated).then((n) => {
        n && this.addTimeZones(n, !1);
      });
    });
  }
  addTimeZones(s, n = !1) {
    const e = s.filter((t) => n || t.favourite == 1).map((t) => ({ label: t.tz_long, value: t.id }));
    n || (e.push({ label: "------", value: -1 }), e.push(...s.filter((t) => t.favourite != 1).map((t) => ({ label: t.tz_long, value: t.id })))), this.timeZones = e;
  }
  timeZoneChanged(s) {
    const n = s.target;
    n.selectedItem && n.selectedItem.value > -1 ? (this.value = n.selectedItem.value, this.text = n.selectedItem.label) : (this.value = null, this.text = "", this.requestUpdate("value", -1)), s.preventDefault();
    const e = new CustomEvent("change");
    this.dispatchEvent(e);
  }
  onKeyUp(s) {
    (s.key === "Escape" || s.key === "Enter") && s.stopPropagation();
  }
  apiRender() {
    return Be`
            <vaadin-combo-box id="kiosk-tz-combo-box" ?disabled="${this.disabled || this.timeZones.length == 0}" .value=${this.value && this.value > -1 ? this.value : ""} @change=${this.timeZoneChanged} .items="${this.timeZones}" @keyup="${this.onKeyUp}"></vaadin-combo-box>
        `;
  }
};
vt.styles = wr(_g);
vt.properties = {
  ...xg(vt, vt, "properties")
};
ji([
  At()
], vt.prototype, "timeZones", 2);
ji([
  Ge({ type: Number, reflect: !0 })
], vt.prototype, "value", 2);
ji([
  Ge({ type: String, reflect: !0 })
], vt.prototype, "text", 2);
ji([
  Ge({ attribute: !0, type: Boolean })
], vt.prototype, "disabled", 2);
ji([
  Ge({ attribute: !0, type: Boolean })
], vt.prototype, "includeDeprecated", 2);
vt = ji([
  Bn("kiosk-tz-combo-box")
], vt);
const Cg = '*{padding:0;margin:0;border:0px;-webkit-user-select:none;user-select:none}p,div{-webkit-user-select:text;user-select:text}select{-webkit-user-select:none;user-select:none}.col-bg-body-lighter{background-color:var(--col-bg-body-lighter)}.col-bg-body{background-color:var(--col-bg-body)}.col-bg-body-darker{background-color:var(--col-bg-body-darker)}.col-bg-1-lighter{background-color:var(--col-bg-1-lighter)}.col-bg-1{background-color:var(--col-bg-1)}.col-bg-1-darker{background-color:var(--col-bg-1-darker)}.col-bg-1-input{background-color:var(--col-bg-1-input)}.col-primary-bg-1{background-color:var(--col-primary-bg-1)}.col-accent-bg-1{background-color:var(--col-accent-bg-1)}.col-success-bg-1{background-color:var(--col-success-bg-1)}.col-warning-bg-1{background-color:var(--col-warning-bg-1)}.col-error-bg-1{background-color:var(--col-error-bg-1)}.col-bg-2-lighter{background-color:var(--col-bg-2-lighter)}.col-bg-2{background-color:var(--col-bg-2)}.col-bg-2-darker{background-color:var(--col-bg-2-darker)}.col-bg-2-button{background-color:var(--col-bg-2-button)}.col-bg-2-input{background-color:var(--col-bg-2-input)}.col-primary-bg-2{background-color:var(--col-primary-bg-2)}.col-accent-bg-2{background-color:var(--col-accent-bg-2)}.col-success-bg-2{background-color:var(--col-success-bg-2)}.col-warning-bg-2{background-color:var(--col-warning-bg-2)}.col-error-bg-2{background-color:var(--col-error-bg-2)}.col-bg-3-lighter{background-color:var(--col-bg-3-lighter)}.col-bg-3{background-color:var(--col-bg-3)}.col-bg-3-darker{background-color:var(--col-bg-3-darker)}.col-bg-3-button{background-color:var(--col-bg-3-button)}.col-bg-3-input{background-color:var(--col-bg-3-input)}.col-primary-bg-3{background-color:var(--col-primary-bg-3)}.col-accent-bg-3{background-color:var(--col-accent-bg-3)}.col-success-bg-3{background-color:var(--col-success-bg-3)}.col-warning-bg-3{background-color:var(--col-warning-bg-3)}.col-error-bg-3{background-color:var(--col-error-bg-3)}.col-bg-ack-lighter{background-color:var(--col-bg-ack-lighter)}.col-bg-ack{background-color:var(--col-bg-ack)}.col-bg-ack-darker{background-color:var(--col-bg-ack-darker)}.col-primary-bg-ack{background-color:var(--col-primary-bg-ack)}.col-accent-bg-ack{background-color:var(--col-accent-bg-ack)}.col-bg-att-lighter{background-color:var(--col-bg-att-lighter)}.col-bg-att{background-color:var(--col-bg-att)}.col-bg-att-darker{background-color:var(--col-bg-att-darker)}.col-primary-bg-att{background-color:var(--col-primary-bg-att)}.col-accent-bg-att{background-color:var(--col-accent-bg-att)}.col-bg-alert-lighter{background-color:var(--col-bg-alert-lighter)}.col-bg-alert{background-color:var(--col-bg-alert)}.col-bg-alert-darker{background-color:var(--col-bg-alert-darker)}.col-primary-bg-alert{background-color:var(--col-primary-bg-alert)}.col-accent-bg-alert{background-color:var(--col-accent-bg-alert)}.col-bg-btn-lighter{background-color:var(--col-bg-btn-lighter)}.col-bg-btn{background-color:var(--col-bg-btn)}.col-bg-btn-darker{background-color:var(--col-bg-btn-darker)}.col-primary-bg-btn{background-color:var(--col-primary-bg-btn)}.col-accent-bg-btn{background-color:var(--col-accent-bg-btn)}.pattern-diagonal-stripes-sm{background:repeating-linear-gradient(45deg,transparent,transparent 10px,currentColor 10px,currentColor 20px)}.pattern-dots-lg{background-image:radial-gradient(currentColor 1.5px,transparent 1.5px);background-size:15px 15px}.pattern-dots-md{background-image:radial-gradient(currentColor 1px,transparent 1px);background-size:8px 8px}.pattern-dots-medium-dense{background-image:radial-gradient(currentColor .5px,transparent .5px);background-size:3px 3px}.pattern-dots-sm{background-image:radial-gradient(currentColor .5px,transparent .5px);background-size:5px 5px}.pattern-cross-dots-md,.imagelist-background{background-image:radial-gradient(currentColor .7px,transparent 1px),radial-gradient(currentColor .7px,transparent 1px);background-size:6.5px 6.5px;background-position:0 0,3.5px 3.5px}.imagelist-background{background-color:#fff9;background-blend-mode:overlay}label{font-family:var(--monospace-font);font-size:.9em}input,select{-webkit-user-select:text;-moz-user-select:text;-ms-user-select:text;box-sizing:border-box;border-radius:0;user-select:text;font-family:var(--standard-text-font);background-color:var(--col-bg-1-input);border:none;color:var(--col-primary-bg-1);padding:2px;margin-bottom:2px;outline:none}textarea{outline:none}input:not([type=radio],[type=checkbox]),select,textarea{width:100%}input:not([type=radio],[type=checkbox]):focus,select:focus,textarea:focus{margin-bottom:0;border-bottom:2px solid rgb(0,0,0)}h1{font-size:var(--font-size-h1)}h2{font-size:var(--font-size-h2)}h3{font-size:var(--font-size-h3)}h4{font-size:var(--font-size-h4)}small,.font-small{font-size:var(--font-size-small)}button{width:60px;height:60px;border-radius:35px;border-style:solid;border-width:2px;background-color:var(--col-bg-btn);color:var(--col-primary-bg-btn);border-color:var(--col-bg-btn-darker);font-family:var(--headline-text-font);font-size:var(--font-size-h4);font-weight:700}button:hover,button:focus{outline:none;background-color:var(--col-bg-btn-lighter)}button:active{padding-top:3px;color:var(--col-primary-bg-btn);background-color:var(--col-bg-btn-darker);border-color:var(--col-bg-btn-darker)}.kiosk-btn-128{width:128px;height:128px}.kiosk-btn-32{width:32px;height:32px}.kiosk-dropdown-btn{box-sizing:content-box;border-radius:0;background-color:var(--col-bg-btn);border-color:var(--col-bg-btn-darker);color:var(--col-primary-bg-btn);height:30px;width:30px}.kiosk-dropdown-btn .drop-down-btn{float:left}.kiosk-dropdown-btn .caret{color:var(--col-primary-bg-btn);display:inline-block;width:0;height:0;margin-left:2px;vertical-align:middle;border-color:var(--col-primary-bg-btn);border-top:4px solid;border-right:4px solid transparent;border-left:4px solid transparent}button:disabled{opacity:.3}.modal-round-button,.modal-delete,.modal-close,.modal-cancel,.modal-back,.modal-next,.modal-ok,.modal-button{box-sizing:border-box;margin-left:25px;background-image:none;display:inline-block;font-style:normal;font-variant:normal;text-rendering:auto;-webkit-font-smoothing:antialiased;font-family:"Font Awesome 6 Free";font-weight:900;font-size:24px;line-height:24px;vertical-align:center;text-align:center}.modal-ok:after{content:var(--icon-bt-ok)}.modal-next:after{padding-left:2px;content:var(--icon-bt-next)}.modal-back:after{padding-right:2px;content:var(--icon-bt-back)}.modal-cancel:after{content:var(--icon-bt-cancel)}.modal-close:before{content:var(--icon-bt-close)}.modal-delete{background-color:var(--col-bg-alert);color:var(--col-primary-bg-alert)}.modal-delete:before{content:var(--icon-bt-trash)}.modal-delete:hover,.modal-delete:focus{border-color:var(--col-bg-alert);background-color:var(--col-bg-alert-lighter);background-size:75%}.modal-delete:active{border-color:var(--col-bg-alert);background-color:var(--col-bg-alert-darker);color:var(--col-primary-bg-alert)}.kiosk-rounded{border-radius:15px}.kiosk-shaded{box-shadow:2px 2px 10px #2e380394}.kiosk-margin-bottom{margin-bottom:1em}.flex-line-break{width:100%;height:0px}.kiosk-align-flex-end{align-items:flex-end}.kiosk-align-flex-start{align-items:flex-start}.full-background-bg-1{background:linear-gradient(to right bottom,var(--col-bg-1-darker),var(--col-bg-1-lighter));height:100%;width:100%}.small-list-button i{border-radius:25px;box-shadow:2px 2px 5px #2e380394}.small-list-button:hover{color:var(--col-bg-att)}.small-list-button:hover i{box-shadow:2px 2px 5px #2e380394}.small-list-button:active,.small-list-button:focus{color:var(--col-bg-ack);transform:translateY(5px)}.small-list-button:active i,.small-list-button:focus i{box-shadow:none}.dialog-radio-div{display:flex;flex-direction:row}.dialog-radio-div input[type=radio]{margin-right:.5em}.rainbow-loading{display:flex;justify-content:center;align-items:center;height:5px;width:100%;background-color:#000}.rainbow-loading.to-top{position:fixed;top:0;z-index:1000}.rainbow-loading-progress{height:5px;width:100%;border-radius:3px;background:linear-gradient(90deg,red,#ff0,#0f0 30%,#0ff 50%,#00f,#f0f 80%,red);background-size:200%;animation:move-gradient 2s ease-in infinite}@keyframes move-gradient{0%{background-position:0% 0%}to{background-position:-200% 0%}}.fa,.fas{font-family:"Font Awesome 6 Free";font-weight:900;font-style:normal}.fa-trash:before{content:""}.fa-view-grid:before{content:""}.fa-reload:before{content:""}.fa-view-list:before{content:""}.fa-camera:before{content:""}.fa-view-image:before{content:""}.fa-check:before{content:""}.fa-bug:before{content:""}.fa-lightbulb:before{content:""}.fa-query:before{content:""}.fa-footsteps{transform:rotate(270deg)}.fa-footsteps:before{content:""}.fa-next:before{content:""}.fa-prev:before{content:""}.fa-hide:before{content:""}.fa-circle-half-stroke:before{content:""}i{font-family:"Font Awesome 6 Free";font-style:normal;font-variant:normal;text-rendering:auto;-webkit-font-smoothing:antialiased;font-weight:900;font-size:.9em}:host{position:fixed;top:0;left:0;height:100dvh;max-width:100dvw;z-index:100;overflow-y:auto;overflow-x:hidden;background-color:var(--col-bg-body-dm)}.kiosk-lightbox-error{position:absolute;font-size:var(--font-size-standard);font-family:var(--standard-text) sans-serif;font-weight:700;color:var(--col-primary-bg-alert);display:grid;place-items:center;z-index:100;background:var(--col-bg-alert);top:50%;margin-left:50vw;width:400px;height:10em;transform:translate(-50%) translateY(-50%)}.kiosk-lightbox-outer{top:0;left:0}.kiosk-lightbox-outer.background-dark{background:var(--col-bg-body-dm)}.kiosk-lightbox-outer.background-light{background:var(--col-bg-body)}.kiosk-lightbox-inner{display:block;right:0;left:auto;width:calc(100dvw - 2rem)}.hide-ui{visibility:hidden}.kiosk-lightbox-viewer{width:100cqw}.kiosk-lightbox-viewer.expanded{height:75dvh}.kiosk-lightbox-viewer.collapsed{height:calc(100dvh - 1rem)}.kiosk-lightbox-viewer.collapsed.nodata{height:calc(100dvh - 2rem)}.kiosk-lightbox-data{position:relative;z-index:10;transform:translateY(-25px);background-color:#fff;width:100%;min-height:calc(25dvh - 2rem)}.kiosk-lightbox-splitter{display:flex;place-content:center;width:100%;background-color:var(--col-bg-body-dm);color:var(--col-primary-bg-body-dm)}.kiosk-lightbox-splitter i{font-size:25px}.kiosk-lightbox-buttons{display:flex;position:fixed;right:3rem;left:auto;top:1em;z-index:2}.kiosk-lightbox-button{border-radius:24px;height:2rem;width:2rem;background-color:var(--col-bg-btn);box-shadow:0 0 5px 2px var(--col-bg-att-lighter);color:var(--col-primary-bg-btn);text-align:center}.kiosk-lightbox-button i{line-height:2rem}.kiosk-lightbox-button:hover{background-color:var(--col-bg-att);box-shadow:0 0 5px 2px var(--col-bg-att-darker);color:var(--col-primary-bg-att)}.kiosk-lightbox-button:active{background-color:var(--col-bg-ack);box-shadow:0 0 5px 2px var(--col-bg-ack-darker);color:var(--col-primary-bg-ack)}.kiosk-lightbox-button+.kiosk-lightbox-button{margin-left:1rem}.kiosk-lightbox-button:last-child{margin-left:2rem}.kiosk-lightbox-button:first-child{margin-right:1rem}.nav-button-deactivated{opacity:25%;box-shadow:none}.nav-button-deactivated:hover,.nav-button-deactivated:active{background-color:var(--col-bg-btn);box-shadow:none;color:var(--col-primary-bg-btn)}.resolution-btn{font-family:var(--monospace-font);vertical-align:middle;font-size:12px;line-height:32px}.resolutions{position:absolute;background-color:var(--col-bg-btn);box-shadow:0 0 5px 2px var(--col-bg-att-lighter);border:1px solid;border-radius:5px;text-align:left;color:var(--col-primary-bg-btn);padding:.5rem}.res-item{-webkit-user-select:none;user-select:none;display:flex;flex-direction:row}.res-item:hover,.res-item:active{color:var(--col-accent-bg-btn)}.res-item .res-item-checker{width:1em;text-align:center;margin-right:.5em}';
/**
 * @license
 * Copyright 2018 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const Tg = xc(class extends Cc {
  constructor(s) {
    if (super(s), s.type !== As.ATTRIBUTE || s.name !== "class" || s.strings?.length > 2) throw Error("`classMap()` can only be used in the `class` attribute and must be the only part in the attribute.");
  }
  render(s) {
    return " " + Object.keys(s).filter(((n) => s[n])).join(" ") + " ";
  }
  update(s, [n]) {
    if (this.st === void 0) {
      this.st = /* @__PURE__ */ new Set(), s.strings !== void 0 && (this.nt = new Set(s.strings.join(" ").split(/\s/).filter(((t) => t !== ""))));
      for (const t in n) n[t] && !this.nt?.has(t) && this.st.add(t);
      return this.render(n);
    }
    const e = s.element.classList;
    for (const t of this.st) t in n || (e.remove(t), this.st.delete(t));
    for (const t in n) {
      const i = !!n[t];
      i === this.st.has(t) || this.nt?.has(t) || (i ? (e.add(t), this.st.add(t)) : (e.remove(t), this.st.delete(t)));
    }
    return mi;
  }
});
var gn = { exports: {} }, Eg = gn.exports, rl;
function Pg() {
  return rl || (rl = 1, (function(s) {
    //! openseadragon 5.0.1
    //! Built on 2025-09-25
    //! Git commit: unknown
    //! http://openseadragon.github.io
    //! License: http://openseadragon.github.io/license/
    function n(e) {
      return new n.Viewer(e);
    }
    (function(e) {
      e.version = {
        versionStr: "5.0.1",
        major: parseInt("5", 10),
        minor: parseInt("0", 10),
        revision: parseInt("1", 10)
      };
      const t = {
        "[object Boolean]": "boolean",
        "[object Number]": "number",
        "[object String]": "string",
        "[object Function]": "function",
        "[object AsyncFunction]": "function",
        "[object Promise]": "promise",
        "[object Array]": "array",
        "[object Date]": "date",
        "[object RegExp]": "regexp",
        "[object Object]": "object",
        "[object HTMLUnknownElement]": "dom-node",
        "[object HTMLImageElement]": "image",
        "[object HTMLCanvasElement]": "canvas",
        "[object CanvasRenderingContext2D]": "context2d"
      }, i = Object.prototype.toString, r = Object.prototype.hasOwnProperty;
      e.isFunction = function(o) {
        return e.type(o) === "function";
      }, e.isArray = Array.isArray || function(o) {
        return e.type(o) === "array";
      }, e.isWindow = function(o) {
        return o && typeof o == "object" && "setInterval" in o;
      }, e.type = function(o) {
        return o == null ? String(o) : t[i.call(o)] || "object";
      }, e.isPlainObject = function(o) {
        if (!o || n.type(o) !== "object" || o.nodeType || e.isWindow(o) || o.constructor && !r.call(o, "constructor") && !r.call(o.constructor.prototype, "isPrototypeOf"))
          return !1;
        let l;
        for (const a in o)
          l = a;
        return l === void 0 || r.call(o, l);
      }, e.isEmptyObject = function(o) {
        for (const l in o)
          return !1;
        return !0;
      }, e.freezeObject = function(o) {
        return Object.freeze ? e.freezeObject = Object.freeze : e.freezeObject = function(l) {
          return l;
        }, e.freezeObject(o);
      }, e.supportsCanvas = (function() {
        const o = document.createElement("canvas");
        return !!(e.isFunction(o.getContext) && o.getContext("2d"));
      })(), e.isCanvasTainted = function(o) {
        let l = !1;
        try {
          o.getContext("2d").getImageData(0, 0, 1, 1);
        } catch {
          l = !0;
        }
        return l;
      }, e.supportsAddEventListener = (function() {
        return !!(document.documentElement.addEventListener && document.addEventListener);
      })(), e.supportsRemoveEventListener = (function() {
        return !!(document.documentElement.removeEventListener && document.removeEventListener);
      })(), e.supportsEventListenerOptions = (function() {
        let o = 0;
        if (e.supportsAddEventListener)
          try {
            const l = {
              get capture() {
                return o++, !1;
              },
              get once() {
                return o++, !1;
              },
              get passive() {
                return o++, !1;
              }
            };
            window.addEventListener("test", null, l), window.removeEventListener("test", null, l);
          } catch {
            o = 0;
          }
        return o >= 3;
      })(), e.supportsAsync = !0, e.getCurrentPixelDensityRatio = function() {
        if (e.supportsCanvas) {
          const o = document.createElement("canvas").getContext("2d"), l = window.devicePixelRatio || 1, a = o.webkitBackingStorePixelRatio || o.mozBackingStorePixelRatio || o.msBackingStorePixelRatio || o.oBackingStorePixelRatio || o.backingStorePixelRatio || 1;
          return Math.max(l, 1) / a;
        } else
          return 1;
      }, e.pixelDensityRatio = e.getCurrentPixelDensityRatio();
    })(n), (function(e) {
      e.extend = function() {
        let a, c, d, f, p, b, x = arguments[0] || {};
        const T = arguments.length;
        let I = !1, z = 1;
        for (typeof x == "boolean" && (I = x, x = arguments[1] || {}, z = 2), typeof x != "object" && !n.isFunction(x) && (x = {}), T === z && (x = this, --z); z < T; z++)
          if (a = arguments[z], a !== null || a !== void 0)
            for (c in a) {
              const W = Object.getOwnPropertyDescriptor(a, c);
              if (W !== void 0) {
                if (W.get || W.set) {
                  Object.defineProperty(x, c, W);
                  continue;
                }
                f = W.value;
              } else {
                e.console.warn('Could not copy inherited property "' + c + '".');
                continue;
              }
              x !== f && (I && f && (n.isPlainObject(f) || (p = n.isArray(f))) ? (d = x[c], p ? (p = !1, b = d && n.isArray(d) ? d : []) : b = d && n.isPlainObject(d) ? d : {}, x[c] = n.extend(I, b, f)) : f !== void 0 && (x[c] = f));
            }
        return x;
      };
      const t = function() {
        if (typeof navigator != "object")
          return !1;
        const a = navigator.userAgent;
        return typeof a != "string" ? !1 : a.indexOf("iPhone") !== -1 || a.indexOf("iPad") !== -1 || a.indexOf("iPod") !== -1;
      };
      e.extend(
        e,
        /** @lends OpenSeadragon */
        {
          /**
           * The default values for the optional settings documented at {@link OpenSeadragon.Options}.
           * @static
           * @type {Object}
           */
          DEFAULT_SETTINGS: {
            //DATA SOURCE DETAILS
            xmlPath: null,
            tileSources: null,
            tileHost: null,
            initialPage: 0,
            crossOriginPolicy: !1,
            ajaxWithCredentials: !1,
            loadTilesWithAjax: !1,
            ajaxHeaders: {},
            splitHashDataForPost: !1,
            callTileLoadedWithCachedData: !1,
            //PAN AND ZOOM SETTINGS AND CONSTRAINTS
            panHorizontal: !0,
            panVertical: !0,
            constrainDuringPan: !1,
            wrapHorizontal: !1,
            wrapVertical: !1,
            visibilityRatio: 0.5,
            //-> how much of the viewer can be negative space
            minPixelRatio: 0.5,
            //->closer to 0 draws tiles meant for a higher zoom at this zoom
            defaultZoomLevel: 0,
            minZoomLevel: null,
            maxZoomLevel: null,
            homeFillsViewer: !1,
            //UI RESPONSIVENESS AND FEEL
            clickTimeThreshold: 300,
            clickDistThreshold: 5,
            dblClickTimeThreshold: 300,
            dblClickDistThreshold: 20,
            springStiffness: 6.5,
            animationTime: 1.2,
            loadDestinationTilesOnAnimation: !0,
            gestureSettingsMouse: {
              dragToPan: !0,
              scrollToZoom: !0,
              clickToZoom: !0,
              dblClickToZoom: !1,
              dblClickDragToZoom: !1,
              pinchToZoom: !1,
              zoomToRefPoint: !0,
              flickEnabled: !1,
              flickMinSpeed: 120,
              flickMomentum: 0.25,
              pinchRotate: !1
            },
            gestureSettingsTouch: {
              dragToPan: !0,
              scrollToZoom: !1,
              clickToZoom: !1,
              dblClickToZoom: !0,
              dblClickDragToZoom: !0,
              pinchToZoom: !0,
              zoomToRefPoint: !0,
              flickEnabled: !0,
              flickMinSpeed: 120,
              flickMomentum: 0.25,
              pinchRotate: !1
            },
            gestureSettingsPen: {
              dragToPan: !0,
              scrollToZoom: !1,
              clickToZoom: !0,
              dblClickToZoom: !1,
              dblClickDragToZoom: !1,
              pinchToZoom: !1,
              zoomToRefPoint: !0,
              flickEnabled: !1,
              flickMinSpeed: 120,
              flickMomentum: 0.25,
              pinchRotate: !1
            },
            gestureSettingsUnknown: {
              dragToPan: !0,
              scrollToZoom: !1,
              clickToZoom: !1,
              dblClickToZoom: !0,
              dblClickDragToZoom: !1,
              pinchToZoom: !0,
              zoomToRefPoint: !0,
              flickEnabled: !0,
              flickMinSpeed: 120,
              flickMomentum: 0.25,
              pinchRotate: !1
            },
            zoomPerClick: 2,
            zoomPerScroll: 1.2,
            zoomPerDblClickDrag: 1.2,
            zoomPerSecond: 1,
            blendTime: 0,
            alwaysBlend: !1,
            autoHideControls: !0,
            immediateRender: !1,
            minZoomImageRatio: 0.9,
            //-> closer to 0 allows zoom out to infinity
            maxZoomPixelRatio: 1.1,
            //-> higher allows 'over zoom' into pixels
            smoothTileEdgesMinZoom: 1.1,
            //-> higher than maxZoomPixelRatio disables it
            iOSDevice: t(),
            pixelsPerWheelLine: 40,
            pixelsPerArrowPress: 40,
            autoResize: !0,
            preserveImageSizeOnResize: !1,
            // requires autoResize=true
            minScrollDeltaTime: 50,
            rotationIncrement: 90,
            maxTilesPerFrame: 1,
            //DEFAULT CONTROL SETTINGS
            showSequenceControl: !0,
            //SEQUENCE
            sequenceControlAnchor: null,
            //SEQUENCE
            preserveViewport: !1,
            //SEQUENCE
            preserveOverlays: !1,
            //SEQUENCE
            navPrevNextWrap: !1,
            //SEQUENCE
            showNavigationControl: !0,
            //ZOOM/HOME/FULL/ROTATION
            navigationControlAnchor: null,
            //ZOOM/HOME/FULL/ROTATION
            showZoomControl: !0,
            //ZOOM
            showHomeControl: !0,
            //HOME
            showFullPageControl: !0,
            //FULL
            showRotationControl: !1,
            //ROTATION
            showFlipControl: !1,
            //FLIP
            controlsFadeDelay: 2e3,
            //ZOOM/HOME/FULL/SEQUENCE
            controlsFadeLength: 1500,
            //ZOOM/HOME/FULL/SEQUENCE
            mouseNavEnabled: !0,
            //GENERAL MOUSE INTERACTIVITY
            keyboardNavEnabled: !0,
            //GENERAL KEYBOARD INTERACTIVITY
            //VIEWPORT NAVIGATOR SETTINGS
            showNavigator: !1,
            navigatorElement: null,
            navigatorId: null,
            navigatorPosition: null,
            navigatorSizeRatio: 0.2,
            navigatorMaintainSizeRatio: !1,
            navigatorTop: null,
            navigatorLeft: null,
            navigatorHeight: null,
            navigatorWidth: null,
            navigatorAutoResize: !0,
            navigatorAutoFade: !0,
            navigatorRotate: !0,
            navigatorBackground: "#000",
            navigatorOpacity: 0.8,
            navigatorBorderColor: "#555",
            navigatorDisplayRegionColor: "#900",
            // INITIAL ROTATION
            degrees: 0,
            // INITIAL FLIP STATE
            flipped: !1,
            overlayPreserveContentDirection: !0,
            // APPEARANCE
            opacity: 1,
            // to be passed into each TiledImage
            compositeOperation: null,
            // to be passed into each TiledImage
            // DRAWER SETTINGS
            drawer: ["webgl", "canvas", "html"],
            // prefer using webgl, then canvas (i.e. context2d), then fallback to html
            drawerOptions: {
              webgl: {
                unpackWithPremultipliedAlpha: !1
              },
              canvas: {},
              html: {},
              custom: {}
            },
            // TILED IMAGE SETTINGS
            preload: !1,
            // to be passed into each TiledImage
            imageSmoothingEnabled: !0,
            // to be passed into each TiledImage
            placeholderFillStyle: null,
            // to be passed into each TiledImage
            subPixelRoundingForTransparency: null,
            // to be passed into each TiledImage
            //REFERENCE STRIP SETTINGS
            showReferenceStrip: !1,
            referenceStripScroll: "horizontal",
            referenceStripElement: null,
            referenceStripHeight: null,
            referenceStripWidth: null,
            referenceStripPosition: "BOTTOM_LEFT",
            referenceStripSizeRatio: 0.2,
            //COLLECTION VISUALIZATION SETTINGS
            collectionRows: 3,
            //or columns depending on layout
            collectionColumns: 0,
            //columns in horizontal layout, rows in vertical layout
            collectionLayout: "horizontal",
            //vertical
            collectionMode: !1,
            collectionTileSize: 800,
            collectionTileMargin: 80,
            //PERFORMANCE SETTINGS
            imageLoaderLimit: 0,
            maxImageCacheCount: 200,
            timeout: 3e4,
            tileRetryMax: 0,
            tileRetryDelay: 2500,
            //INTERFACE RESOURCE SETTINGS
            prefixUrl: "/images/",
            navImages: {
              zoomIn: {
                REST: "zoomin_rest.png",
                GROUP: "zoomin_grouphover.png",
                HOVER: "zoomin_hover.png",
                DOWN: "zoomin_pressed.png"
              },
              zoomOut: {
                REST: "zoomout_rest.png",
                GROUP: "zoomout_grouphover.png",
                HOVER: "zoomout_hover.png",
                DOWN: "zoomout_pressed.png"
              },
              home: {
                REST: "home_rest.png",
                GROUP: "home_grouphover.png",
                HOVER: "home_hover.png",
                DOWN: "home_pressed.png"
              },
              fullpage: {
                REST: "fullpage_rest.png",
                GROUP: "fullpage_grouphover.png",
                HOVER: "fullpage_hover.png",
                DOWN: "fullpage_pressed.png"
              },
              rotateleft: {
                REST: "rotateleft_rest.png",
                GROUP: "rotateleft_grouphover.png",
                HOVER: "rotateleft_hover.png",
                DOWN: "rotateleft_pressed.png"
              },
              rotateright: {
                REST: "rotateright_rest.png",
                GROUP: "rotateright_grouphover.png",
                HOVER: "rotateright_hover.png",
                DOWN: "rotateright_pressed.png"
              },
              flip: {
                // Flip icon designed by Yaroslav Samoylov from the Noun Project and modified by Nelson Campos ncampos@criteriamarathon.com, https://thenounproject.com/term/flip/136289/
                REST: "flip_rest.png",
                GROUP: "flip_grouphover.png",
                HOVER: "flip_hover.png",
                DOWN: "flip_pressed.png"
              },
              previous: {
                REST: "previous_rest.png",
                GROUP: "previous_grouphover.png",
                HOVER: "previous_hover.png",
                DOWN: "previous_pressed.png"
              },
              next: {
                REST: "next_rest.png",
                GROUP: "next_grouphover.png",
                HOVER: "next_hover.png",
                DOWN: "next_pressed.png"
              }
            },
            //DEVELOPER SETTINGS
            debugMode: !1,
            debugGridColor: ["#437AB2", "#1B9E77", "#D95F02", "#7570B3", "#E7298A", "#66A61E", "#E6AB02", "#A6761D", "#666666"],
            silenceMultiImageWarnings: !1
          },
          /**
           * Returns a function which invokes the method as if it were a method belonging to the object.
           * @function
           * @param {Object} object
           * @param {Function} method
           * @returns {Function}
           */
          delegate: function(a, c) {
            return function() {
              let d = arguments;
              return d === void 0 && (d = []), c.apply(a, d);
            };
          },
          /**
           * An enumeration of Browser vendors.
           * @static
           * @type {Object}
           * @property {Number} UNKNOWN
           * @property {Number} IE
           * @property {Number} FIREFOX
           * @property {Number} SAFARI
           * @property {Number} CHROME
           * @property {Number} OPERA
           * @property {Number} EDGE
           * @property {Number} CHROMEEDGE
           */
          BROWSERS: {
            UNKNOWN: 0,
            IE: 1,
            FIREFOX: 2,
            SAFARI: 3,
            CHROME: 4,
            OPERA: 5,
            EDGE: 6,
            CHROMEEDGE: 7
          },
          /**
           * An enumeration of when subpixel rounding should occur.
           * @static
           * @type {Object}
           * @property {Number} NEVER Never apply subpixel rounding for transparency.
           * @property {Number} ONLY_AT_REST Do not apply subpixel rounding for transparency during animation (panning, zoom, rotation) and apply it once animation is over.
           * @property {Number} ALWAYS Apply subpixel rounding for transparency during animation and when animation is over.
           */
          SUBPIXEL_ROUNDING_OCCURRENCES: {
            NEVER: 0,
            ONLY_AT_REST: 1,
            ALWAYS: 2
          },
          /**
           * Keep track of which {@link Viewer}s have been created.
           * - Key: {@link Element} to which a Viewer is attached.
           * - Value: {@link Viewer} of the element defined by the key.
           * @private
           * @static
           * @type {Object}
           */
          _viewers: /* @__PURE__ */ new Map(),
          /**
            * Returns the {@link Viewer} attached to a given DOM element. If there is
            * no viewer attached to the provided element, undefined is returned.
            * @function
            * @param {String|Element} element Accepts an id or element.
            * @returns {Viewer} The viewer attached to the given element, or undefined.
            */
          getViewer: function(a) {
            return e._viewers.get(this.getElement(a));
          },
          /**
           * Returns a DOM Element for the given id or element.
           * @function
           * @param {String|Element} element Accepts an id or element.
           * @returns {Element} The element with the given id, null, or the element itself.
           */
          getElement: function(a) {
            return typeof a == "string" && (a = document.getElementById(a)), a;
          },
          /**
           * Determines the position of the upper-left corner of the element.
           * @function
           * @param {Element|String} element - the element we want the position for.
           * @returns {OpenSeadragon.Point} - the position of the upper left corner of the element.
           */
          getElementPosition: function(a) {
            let c = new e.Point(), d, f;
            for (a = e.getElement(a), d = e.getElementStyle(a).position === "fixed", f = l(a, d); f; )
              c.x += a.offsetLeft, c.y += a.offsetTop, d && (c = c.plus(e.getPageScroll())), a = f, d = e.getElementStyle(a).position === "fixed", f = l(a, d);
            return c;
          },
          /**
           * Determines the position of the upper-left corner of the element adjusted for current page and/or element scroll.
           * @function
           * @param {Element|String} element - the element we want the position for.
           * @returns {OpenSeadragon.Point} - the position of the upper left corner of the element adjusted for current page and/or element scroll.
           */
          getElementOffset: function(a) {
            a = e.getElement(a);
            const c = a && a.ownerDocument;
            let d = { top: 0, left: 0 };
            if (!c)
              return new e.Point();
            const f = c.documentElement;
            typeof a.getBoundingClientRect < "u" && (d = a.getBoundingClientRect());
            const p = c === c.window ? c : c.nodeType === 9 ? c.defaultView || c.parentWindow : !1;
            return new e.Point(
              d.left + (p.pageXOffset || f.scrollLeft) - (f.clientLeft || 0),
              d.top + (p.pageYOffset || f.scrollTop) - (f.clientTop || 0)
            );
          },
          /**
           * Determines the height and width of the given element.
           * @function
           * @param {Element|String} element
           * @returns {OpenSeadragon.Point}
           */
          getElementSize: function(a) {
            return a = e.getElement(a), new e.Point(
              a.clientWidth,
              a.clientHeight
            );
          },
          /**
           * Returns the CSSStyle object for the given element.
           * @function
           * @param {Element|String} element
           * @returns {CSSStyle}
           */
          getElementStyle: document.documentElement.currentStyle ? function(a) {
            return a = e.getElement(a), a.currentStyle;
          } : function(a) {
            return a = e.getElement(a), window.getComputedStyle(a, "");
          },
          /**
           * Returns the property with the correct vendor prefix appended.
           * @param {String} property the property name
           * @returns {String} the property with the correct prefix or null if not
           * supported.
           */
          getCssPropertyWithVendorPrefix: function(a) {
            const c = {};
            return e.getCssPropertyWithVendorPrefix = function(d) {
              if (c[d] !== void 0)
                return c[d];
              const f = document.createElement("div").style;
              let p = null;
              if (f[d] !== void 0)
                p = d;
              else {
                const b = [
                  "Webkit",
                  "Moz",
                  "MS",
                  "O",
                  "webkit",
                  "moz",
                  "ms",
                  "o"
                ], x = e.capitalizeFirstLetter(d);
                for (let T = 0; T < b.length; T++) {
                  const I = b[T] + x;
                  if (f[I] !== void 0) {
                    p = I;
                    break;
                  }
                }
              }
              return c[d] = p, p;
            }, e.getCssPropertyWithVendorPrefix(a);
          },
          /**
           * Capitalizes the first letter of a string
           * @param {String} string
           * @returns {String} The string with the first letter capitalized
           */
          capitalizeFirstLetter: function(a) {
            return a.charAt(0).toUpperCase() + a.slice(1);
          },
          /**
           * Compute the modulo of a number but makes sure to always return
           * a positive value (also known as Euclidean modulo).
           * @param {Number} number the number to compute the modulo of
           * @param {Number} modulo the modulo
           * @returns {Number} the result of the modulo of number
           */
          positiveModulo: function(a, c) {
            let d = a % c;
            return d < 0 && (d += c), d;
          },
          /**
           * Determines if a point is within the bounding rectangle of the given element (hit-test).
           * @function
           * @param {Element|String} element
           * @param {OpenSeadragon.Point} point
           * @returns {Boolean}
           */
          pointInElement: function(a, c) {
            a = e.getElement(a);
            const d = e.getElementOffset(a), f = e.getElementSize(a);
            return c.x >= d.x && c.x < d.x + f.x && c.y < d.y + f.y && c.y >= d.y;
          },
          /**
           * Gets the position of the mouse on the screen for a given event.
           * @function
           * @param {Event} [event]
           * @returns {OpenSeadragon.Point}
           */
          getMousePosition: function(a) {
            if (typeof a.pageX == "number")
              e.getMousePosition = function(c) {
                const d = new e.Point();
                return d.x = c.pageX, d.y = c.pageY, d;
              };
            else if (typeof a.clientX == "number")
              e.getMousePosition = function(c) {
                const d = new e.Point();
                return d.x = c.clientX + document.body.scrollLeft + document.documentElement.scrollLeft, d.y = c.clientY + document.body.scrollTop + document.documentElement.scrollTop, d;
              };
            else
              throw new Error(
                "Unknown event mouse position, no known technique."
              );
            return e.getMousePosition(a);
          },
          /**
           * Determines the page's current scroll position.
           * @function
           * @returns {OpenSeadragon.Point}
           */
          getPageScroll: function() {
            const a = document.documentElement || {}, c = document.body || {};
            if (typeof window.pageXOffset == "number")
              e.getPageScroll = function() {
                return new e.Point(
                  window.pageXOffset,
                  window.pageYOffset
                );
              };
            else if (c.scrollLeft || c.scrollTop)
              e.getPageScroll = function() {
                return new e.Point(
                  document.body.scrollLeft,
                  document.body.scrollTop
                );
              };
            else if (a.scrollLeft || a.scrollTop)
              e.getPageScroll = function() {
                return new e.Point(
                  document.documentElement.scrollLeft,
                  document.documentElement.scrollTop
                );
              };
            else
              return new e.Point(0, 0);
            return e.getPageScroll();
          },
          /**
           * Set the page scroll position.
           * @function
           * @returns {OpenSeadragon.Point}
           */
          setPageScroll: function(a) {
            if (typeof window.scrollTo < "u")
              e.setPageScroll = function(c) {
                window.scrollTo(c.x, c.y);
              };
            else {
              const c = e.getPageScroll();
              if (c.x === a.x && c.y === a.y)
                return;
              document.body.scrollLeft = a.x, document.body.scrollTop = a.y;
              let d = e.getPageScroll();
              if (d.x !== c.x && d.y !== c.y) {
                e.setPageScroll = function(f) {
                  document.body.scrollLeft = f.x, document.body.scrollTop = f.y;
                };
                return;
              }
              if (document.documentElement.scrollLeft = a.x, document.documentElement.scrollTop = a.y, d = e.getPageScroll(), d.x !== c.x && d.y !== c.y) {
                e.setPageScroll = function(f) {
                  document.documentElement.scrollLeft = f.x, document.documentElement.scrollTop = f.y;
                };
                return;
              }
              e.setPageScroll = function(f) {
              };
            }
            e.setPageScroll(a);
          },
          /**
           * Determines the size of the browsers window.
           * @function
           * @returns {OpenSeadragon.Point}
           */
          getWindowSize: function() {
            const a = document.documentElement || {}, c = document.body || {};
            if (typeof window.innerWidth == "number")
              e.getWindowSize = function() {
                return new e.Point(
                  window.innerWidth,
                  window.innerHeight
                );
              };
            else if (a.clientWidth || a.clientHeight)
              e.getWindowSize = function() {
                return new e.Point(
                  document.documentElement.clientWidth,
                  document.documentElement.clientHeight
                );
              };
            else if (c.clientWidth || c.clientHeight)
              e.getWindowSize = function() {
                return new e.Point(
                  document.body.clientWidth,
                  document.body.clientHeight
                );
              };
            else
              throw new Error("Unknown window size, no known technique.");
            return e.getWindowSize();
          },
          /**
           * Wraps the given element in a nest of divs so that the element can
           * be easily centered using CSS tables
           * @function
           * @param {Element|String} element
           * @returns {Element} outermost wrapper element
           */
          makeCenteredNode: function(a) {
            a = e.getElement(a);
            const c = [
              e.makeNeutralElement("div"),
              e.makeNeutralElement("div"),
              e.makeNeutralElement("div")
            ];
            return e.extend(c[0].style, {
              display: "table",
              height: "100%",
              width: "100%"
            }), e.extend(c[1].style, {
              display: "table-row"
            }), e.extend(c[2].style, {
              display: "table-cell",
              verticalAlign: "middle",
              textAlign: "center"
            }), c[0].appendChild(c[1]), c[1].appendChild(c[2]), c[2].appendChild(a), c[0];
          },
          /**
           * Creates an easily positionable element of the given type that therefor
           * serves as an excellent container element.
           * @function
           * @param {String} tagName
           * @returns {Element}
           */
          makeNeutralElement: function(a) {
            const c = document.createElement(a), d = c.style;
            return d.background = "transparent none", d.border = "none", d.margin = "0px", d.padding = "0px", d.position = "static", c;
          },
          /**
           * Returns the current milliseconds, using Date.now() if available
           * @function
           */
          now: function() {
            return Date.now ? e.now = Date.now : e.now = function() {
              return (/* @__PURE__ */ new Date()).getTime();
            }, e.now();
          },
          /**
           * Ensures an image is loaded correctly to support alpha transparency.
           * @function
           * @param {String} src
           * @returns {Element}
           */
          makeTransparentImage: function(a) {
            const c = e.makeNeutralElement("img");
            return c.src = a, c;
          },
          /**
           * Sets the opacity of the specified element.
           * @function
           * @param {Element|String} element
           * @param {Number} opacity
           * @param {Boolean} [usesAlpha]
           */
          setElementOpacity: function(a, c, d) {
            let f, p;
            a = e.getElement(a), d && !e.Browser.alpha && (c = Math.round(c)), e.Browser.opacity ? a.style.opacity = c < 1 ? c : "" : c < 1 ? (f = Math.round(100 * c), p = "alpha(opacity=" + f + ")", a.style.filter = p) : a.style.filter = "";
          },
          /**
           * Sets the specified element's touch-action style attribute to 'none'.
           * @function
           * @param {Element|String} element
           */
          setElementTouchActionNone: function(a) {
            a = e.getElement(a), typeof a.style.touchAction < "u" ? a.style.touchAction = "none" : typeof a.style.msTouchAction < "u" && (a.style.msTouchAction = "none");
          },
          /**
           * Sets the specified element's pointer-events style attribute to the passed value.
           * @function
           * @param {Element|String} element
           * @param {String} value
           */
          setElementPointerEvents: function(a, c) {
            a = e.getElement(a), typeof a.style < "u" && typeof a.style.pointerEvents < "u" && (a.style.pointerEvents = c);
          },
          /**
           * Sets the specified element's pointer-events style attribute to 'none'.
           * @function
           * @param {Element|String} element
           */
          setElementPointerEventsNone: function(a) {
            e.setElementPointerEvents(a, "none");
          },
          /**
           * Add the specified CSS class to the element if not present.
           * @function
           * @param {Element|String} element
           * @param {String} className
           */
          addClass: function(a, c) {
            a = e.getElement(a), a.className ? (" " + a.className + " ").indexOf(" " + c + " ") === -1 && (a.className += " " + c) : a.className = c;
          },
          /**
           * Find the first index at which an element is found in an array or -1
           * if not present.
           *
           * Code taken and adapted from
           * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/indexOf#Compatibility
           *
           * @function
           * @param {Array} array The array from which to find the element
           * @param {Object} searchElement The element to find
           * @param {Number} [fromIndex=0] Index to start research.
           * @returns {Number} The index of the element in the array.
           */
          indexOf: function(a, c, d) {
            return Array.prototype.indexOf ? this.indexOf = function(f, p, b) {
              return f.indexOf(p, b);
            } : this.indexOf = function(f, p, b) {
              let x = b || 0;
              if (!f)
                throw new TypeError();
              const T = f.length;
              if (T === 0 || x >= T)
                return -1;
              x < 0 && (x = T - Math.abs(x));
              for (let I = x; I < T; I++)
                if (f[I] === p)
                  return I;
              return -1;
            }, this.indexOf(a, c, d);
          },
          /**
           * Remove the specified CSS class from the element.
           * @function
           * @param {Element|String} element
           * @param {String} className
           */
          removeClass: function(a, c) {
            const d = [];
            a = e.getElement(a);
            const f = a.className.split(/\s+/);
            for (let p = 0; p < f.length; p++)
              f[p] && f[p] !== c && d.push(f[p]);
            a.className = d.join(" ");
          },
          /**
           * Convert passed addEventListener() options to boolean or options object,
           * depending on browser support.
           * @function
           * @param {Boolean|Object} [options] Boolean useCapture, or if [supportsEventListenerOptions]{@link OpenSeadragon.supportsEventListenerOptions}, can be an object
           * @param {Boolean} [options.capture]
           * @param {Boolean} [options.passive]
           * @param {Boolean} [options.once]
           * @returns {String} The protocol (http:, https:, file:, ftp: ...)
           */
          normalizeEventListenerOptions: function(a) {
            let c;
            return typeof a < "u" ? typeof a == "boolean" ? c = e.supportsEventListenerOptions ? { capture: a } : a : c = e.supportsEventListenerOptions ? a : typeof a.capture < "u" ? a.capture : !1 : c = e.supportsEventListenerOptions ? { capture: !1 } : !1, c;
          },
          /**
           * Adds an event listener for the given element, eventName and handler.
           * @function
           * @param {Element|String} element
           * @param {String} eventName
           * @param {Function} handler
           * @param {Boolean|Object} [options] Boolean useCapture, or if [supportsEventListenerOptions]{@link OpenSeadragon.supportsEventListenerOptions}, can be an object
           * @param {Boolean} [options.capture]
           * @param {Boolean} [options.passive]
           * @param {Boolean} [options.once]
           */
          addEvent: (function() {
            if (e.supportsAddEventListener)
              return function(a, c, d, f) {
                f = e.normalizeEventListenerOptions(f), a = e.getElement(a), a.addEventListener(c, d, f);
              };
            if (document.documentElement.attachEvent && document.attachEvent)
              return function(a, c, d) {
                a = e.getElement(a), a.attachEvent("on" + c, d);
              };
            throw new Error("No known event model.");
          })(),
          /**
           * Remove a given event listener for the given element, event type and
           * handler.
           * @function
           * @param {Element|String} element
           * @param {String} eventName
           * @param {Function} handler
           * @param {Boolean|Object} [options] Boolean useCapture, or if [supportsEventListenerOptions]{@link OpenSeadragon.supportsEventListenerOptions}, can be an object
           * @param {Boolean} [options.capture]
           */
          removeEvent: (function() {
            if (e.supportsRemoveEventListener)
              return function(a, c, d, f) {
                f = e.normalizeEventListenerOptions(f), a = e.getElement(a), a.removeEventListener(c, d, f);
              };
            if (document.documentElement.detachEvent && document.detachEvent)
              return function(a, c, d) {
                a = e.getElement(a), a.detachEvent("on" + c, d);
              };
            throw new Error("No known event model.");
          })(),
          /**
           * Cancels the default browser behavior had the event propagated all
           * the way up the DOM to the window object.
           * @function
           * @param {Event} [event]
           */
          cancelEvent: function(a) {
            a.preventDefault();
          },
          /**
           * Returns true if {@link OpenSeadragon.cancelEvent|cancelEvent} has been called on
           * the event, otherwise returns false.
           * @function
           * @param {Event} [event]
           */
          eventIsCanceled: function(a) {
            return a.defaultPrevented;
          },
          /**
           * Stops the propagation of the event through the DOM in the capturing and bubbling phases.
           * @function
           * @param {Event} [event]
           */
          stopEvent: function(a) {
            a.stopPropagation();
          },
          /**
           * Retrieves the value of a url parameter from the window.location string.
           * @function
           * @param {String} key
           * @returns {String} The value of the url parameter or null if no param matches.
           */
          getUrlParameter: function(a) {
            const c = o[a];
            return c || null;
          },
          /**
           * Retrieves the protocol used by the url. The url can either be absolute
           * or relative.
           * @function
           * @private
           * @param {String} url The url to retrieve the protocol from.
           * @returns {String} The protocol (http:, https:, file:, ftp: ...)
           */
          getUrlProtocol: function(a) {
            const c = a.match(/^([a-z]+:)\/\//i);
            return c === null ? window.location.protocol : c[1].toLowerCase();
          },
          /**
           * Create an XHR object
           * @private
           * @param {type} [local] Deprecated. Ignored (IE/ActiveXObject file protocol no longer supported).
           * @returns {XMLHttpRequest}
           */
          createAjaxRequest: function() {
            if (window.XMLHttpRequest)
              return e.createAjaxRequest = function() {
                return new XMLHttpRequest();
              }, new XMLHttpRequest();
            throw new Error("Browser doesn't support XMLHttpRequest.");
          },
          /**
           * Makes an AJAX request.
           * @param {String} url - the url to request
           * @param {Function} onSuccess
           * @param {Function} onError
           * @throws {Error}
           * @returns {XMLHttpRequest}
           * @deprecated deprecated way of calling this function
           */
          /**
          * Makes an AJAX request.
          * @param {Object} options
          * @param {String} options.url - the url to request
          * @param {Function} options.success - a function to call on a successful response
          * @param {Function} options.error - a function to call on when an error occurs
          * @param {Object} options.headers - headers to add to the AJAX request
          * @param {String} options.responseType - the response type of the AJAX request
          * @param {String} options.postData - HTTP POST data (usually but not necessarily in k=v&k2=v2... form,
          *      see TileSource::getTilePostData), GET method used if null
          * @param {Boolean} [options.withCredentials=false] - whether to set the XHR's withCredentials
          * @throws {Error}
          * @returns {XMLHttpRequest}
          */
          makeAjaxRequest: function(a, c, d) {
            let f, p, b, x;
            e.isPlainObject(a) ? (c = a.success, d = a.error, f = a.withCredentials, p = a.headers, b = a.responseType || null, x = a.postData || null, a = a.url) : e.console.warn("OpenSeadragon.makeAjaxRequest() deprecated usage!");
            const T = e.getUrlProtocol(a), I = e.createAjaxRequest();
            if (!e.isFunction(c))
              throw new Error("makeAjaxRequest requires a success callback");
            I.onreadystatechange = function() {
              I.readyState === 4 && (I.onreadystatechange = function() {
              }, I.status >= 200 && I.status < 300 || I.status === 0 && T !== "http:" && T !== "https:" ? c(I) : e.isFunction(d) ? d(I) : e.console.error("AJAX request returned %d: %s", I.status, a));
            };
            const z = x ? "POST" : "GET";
            try {
              if (I.open(z, a, !0), b && (I.responseType = b), p)
                for (const W in p)
                  Object.prototype.hasOwnProperty.call(p, W) && p[W] && I.setRequestHeader(W, p[W]);
              f && (I.withCredentials = !0), I.send(x);
            } catch (W) {
              e.console.error("%s while making AJAX request: %s", W.name, W.message), I.onreadystatechange = function() {
              }, e.isFunction(d) && d(I, W);
            }
            return I;
          },
          /**
           * Taken from jQuery 1.6.1
           * @function
           * @param {Object} options
           * @param {String} options.url
           * @param {Function} options.callback
           * @param {String} [options.param='callback'] The name of the url parameter
           *      to request the jsonp provider with.
           * @param {String} [options.callbackName=] The name of the callback to
           *      request the jsonp provider with.
           */
          jsonp: function(a) {
            let c, d = a.url;
            const f = document.head || document.getElementsByTagName("head")[0] || document.documentElement, p = a.callbackName || "openseadragon" + e.now(), b = window[p], x = "$1" + p + "$2", T = a.param || "callback", I = a.callback;
            d = d.replace(/(=)\?(&|$)|\?\?/i, x), d += (/\?/.test(d) ? "&" : "?") + T + "=" + p, window[p] = function(z) {
              if (b)
                window[p] = b;
              else
                try {
                  delete window[p];
                } catch {
                }
              I && e.isFunction(I) && I(z);
            }, c = document.createElement("script"), (a.async !== void 0 || a.async !== !1) && (c.async = "async"), a.scriptCharset && (c.charset = a.scriptCharset), c.src = d, c.onload = c.onreadystatechange = function(z, W) {
              (W || !c.readyState || /loaded|complete/.test(c.readyState)) && (c.onload = c.onreadystatechange = null, f && c.parentNode && f.removeChild(c), c = void 0);
            }, f.insertBefore(c, f.firstChild);
          },
          /**
           * Fully deprecated. Will throw an error.
           * @function
           * @deprecated use {@link OpenSeadragon.Viewer#open}
           */
          createFromDZI: function() {
            throw "OpenSeadragon.createFromDZI is deprecated, use Viewer.open.";
          },
          /**
           * Parses an XML string into a DOM Document.
           * @function
           * @param {String} string
           * @returns {Document}
           */
          parseXml: function(a) {
            if (window.DOMParser)
              e.parseXml = function(c) {
                let d = null;
                return d = new DOMParser().parseFromString(c, "text/xml"), d;
              };
            else
              throw new Error("Browser doesn't support XML DOM.");
            return e.parseXml(a);
          },
          /**
           * Parses a JSON string into a Javascript object.
           * @function
           * @param {String} string
           * @returns {Object}
           */
          parseJSON: function(a) {
            return e.parseJSON = window.JSON.parse, e.parseJSON(a);
          },
          /**
           * Reports whether the image format is supported for tiling in this
           * version.
           * @function
           * @param {String} [extension]
           * @returns {Boolean}
           */
          imageFormatSupported: function(a) {
            return a = a || "", !!r[a.toLowerCase()];
          },
          /**
           * Updates supported image formats with user-specified values.
           * Preexisting formats that are not being updated are left unchanged.
           * By default, the defined formats are
           * <pre><code>{
           *      avif: true,
           *      bmp:  false,
           *      jpeg: true,
           *      jpg:  true,
           *      png:  true,
           *      tif:  false,
           *      wdp:  false,
           *      webp: true
           * }
           * </code></pre>
           * @function
           * @example
           * // sets bmp as supported and png as unsupported
           * setImageFormatsSupported({bmp: true, png: false});
           * @param {Object} formats An object containing format extensions as
           * keys and booleans as values.
           */
          setImageFormatsSupported: function(a) {
            e.extend(r, a);
          }
        }
      );
      const i = function(a) {
      };
      e.console = window.console || {
        log: i,
        debug: i,
        info: i,
        warn: i,
        error: i,
        assert: i
      }, e.Browser = {
        vendor: e.BROWSERS.UNKNOWN,
        version: 0,
        alpha: !0
      };
      const r = {
        avif: !0,
        bmp: !1,
        jpeg: !0,
        jpg: !0,
        png: !0,
        tif: !1,
        wdp: !1,
        webp: !0
      }, o = {};
      (function() {
        const a = navigator.appVersion, c = navigator.userAgent;
        let d;
        switch (navigator.appName) {
          case "Microsoft Internet Explorer":
            window.attachEvent && window.ActiveXObject && (e.Browser.vendor = e.BROWSERS.IE, e.Browser.version = parseFloat(
              c.substring(
                c.indexOf("MSIE") + 5,
                c.indexOf(";", c.indexOf("MSIE"))
              )
            ));
            break;
          case "Netscape":
            window.addEventListener && (c.indexOf("Edge") >= 0 ? (e.Browser.vendor = e.BROWSERS.EDGE, e.Browser.version = parseFloat(
              c.substring(c.indexOf("Edge") + 5)
            )) : c.indexOf("Edg") >= 0 ? (e.Browser.vendor = e.BROWSERS.CHROMEEDGE, e.Browser.version = parseFloat(
              c.substring(c.indexOf("Edg") + 4)
            )) : c.indexOf("Firefox") >= 0 ? (e.Browser.vendor = e.BROWSERS.FIREFOX, e.Browser.version = parseFloat(
              c.substring(c.indexOf("Firefox") + 8)
            )) : c.indexOf("Safari") >= 0 ? (e.Browser.vendor = c.indexOf("Chrome") >= 0 ? e.BROWSERS.CHROME : e.BROWSERS.SAFARI, e.Browser.version = parseFloat(
              c.substring(
                c.substring(0, c.indexOf("Safari")).lastIndexOf("/") + 1,
                c.indexOf("Safari")
              )
            )) : (d = new RegExp("Trident/.*rv:([0-9]{1,}[.0-9]{0,})"), d.exec(c) !== null && (e.Browser.vendor = e.BROWSERS.IE, e.Browser.version = parseFloat(RegExp.$1))));
            break;
          case "Opera":
            e.Browser.vendor = e.BROWSERS.OPERA, e.Browser.version = parseFloat(a);
            break;
        }
        const p = window.location.search.substring(1).split("&");
        for (let b = 0; b < p.length; b++) {
          const x = p[b], T = x.indexOf("=");
          if (T > 0) {
            const I = x.substring(0, T), z = x.substring(T + 1);
            try {
              o[I] = decodeURIComponent(z);
            } catch {
              e.console.error("Ignoring malformed URL parameter: %s=%s", I, z);
            }
          }
        }
        e.Browser.alpha = !(e.Browser.vendor === e.BROWSERS.CHROME && e.Browser.version < 2), e.Browser.opacity = !0, e.Browser.vendor === e.BROWSERS.IE && e.console.error("Internet Explorer is not supported by OpenSeadragon");
      })(), (function(a) {
        const c = a.requestAnimationFrame || a.mozRequestAnimationFrame || a.webkitRequestAnimationFrame || a.msRequestAnimationFrame, d = a.cancelAnimationFrame || a.mozCancelAnimationFrame || a.webkitCancelAnimationFrame || a.msCancelAnimationFrame;
        if (c && d)
          e.requestAnimationFrame = function() {
            return c.apply(a, arguments);
          }, e.cancelAnimationFrame = function() {
            return d.apply(a, arguments);
          };
        else {
          let f = [], p = [], b, x = 0;
          e.requestAnimationFrame = function(T) {
            return f.push([++x, T]), b || (b = setInterval(function() {
              if (f.length) {
                const I = e.now(), z = p;
                for (p = f, f = z; p.length; )
                  p.shift()[1](I);
              } else
                clearInterval(b), b = void 0;
            }, 1e3 / 50)), x;
          }, e.cancelAnimationFrame = function(T) {
            let I, z;
            for (I = 0, z = f.length; I < z; I += 1)
              if (f[I][0] === T) {
                f.splice(I, 1);
                return;
              }
            for (I = 0, z = p.length; I < z; I += 1)
              if (p[I][0] === T) {
                p.splice(I, 1);
                return;
              }
          };
        }
      })(window);
      function l(a, c) {
        return c && a !== document.body ? document.body : a.offsetParent;
      }
      e.Promise = window.Promise && e.supportsAsync ? window.Promise : class {
        constructor(a) {
          this._error = !1, this.__value = void 0;
          try {
            a(
              (c) => {
                for (; c instanceof e.Promise; )
                  c = c._value;
                this._value = c;
              },
              (c) => {
                for (; c instanceof e.Promise; )
                  c = c._value;
                this._value = c, this._error = !0;
              }
            );
          } catch (c) {
            this._value = c, this._error = !0;
          }
        }
        then(a) {
          if (!this._error)
            try {
              this._value = a(this._value);
            } catch (c) {
              this._value = c, this._error = !0;
            }
          return this;
        }
        catch(a) {
          if (this._error)
            try {
              this._value = a(this._value), this._error = !1;
            } catch (c) {
              this._value = c, this._error = !0;
            }
          return this;
        }
        get _value() {
          return this.__value;
        }
        set _value(a) {
          a && a.constructor === this.constructor && (a = a._value), this.__value = a;
        }
        static resolve(a) {
          return new this((c) => c(a));
        }
        static reject(a) {
          return new this((c, d) => d(a));
        }
        static all(a) {
          return new this((c) => c(a.map((d) => d())));
        }
        static race(a) {
          return a.length < 1 ? this.resolve() : new this((c) => c(a[0]()));
        }
      };
    })(n), (function(e, t) {
      s.exports ? s.exports = t : (e || (e = typeof window == "object" && window, e || t.console.error("OpenSeadragon must run in browser environment!")), e.OpenSeadragon = t);
    })(Eg, n), (function(e) {
      class t {
        constructor(r) {
          r || (r = [
            0,
            0,
            0,
            0,
            0,
            0,
            0,
            0,
            0
          ]), this.values = r;
        }
        /**
         * @function makeIdentity
         * @memberof OpenSeadragon.Mat3
         * @static
         * @returns {OpenSeadragon.Mat3} an identity matrix
         */
        static makeIdentity() {
          return new t([
            1,
            0,
            0,
            0,
            1,
            0,
            0,
            0,
            1
          ]);
        }
        /**
         * @function makeTranslation
         * @memberof OpenSeadragon.Mat3
         * @static
         * @param {Number} tx The x value of the translation
         * @param {Number} ty The y value of the translation
         * @returns {OpenSeadragon.Mat3} A translation matrix
         */
        static makeTranslation(r, o) {
          return new t([
            1,
            0,
            0,
            0,
            1,
            0,
            r,
            o,
            1
          ]);
        }
        /**
         * @function makeRotation
         * @memberof OpenSeadragon.Mat3
         * @static
         * @param {Number} angleInRadians The desired rotation angle, in radians
         * @returns {OpenSeadragon.Mat3} A rotation matrix
         */
        static makeRotation(r) {
          const o = Math.cos(r), l = Math.sin(r);
          return new t([
            o,
            -l,
            0,
            l,
            o,
            0,
            0,
            0,
            1
          ]);
        }
        /**
         * @function makeScaling
         * @memberof OpenSeadragon.Mat3
         * @static
         * @param {Number} sx The x value of the scaling
         * @param {Number} sy The y value of the scaling
         * @returns {OpenSeadragon.Mat3} A scaling matrix
         */
        static makeScaling(r, o) {
          return new t([
            r,
            0,
            0,
            0,
            o,
            0,
            0,
            0,
            1
          ]);
        }
        /**
         * @alias multiply
         * @memberof! OpenSeadragon.Mat3
         * @param {OpenSeadragon.Mat3} other the matrix to multiply with
         * @returns {OpenSeadragon.Mat3} The result of matrix multiplication
         */
        multiply(r) {
          let o = this.values, l = r.values;
          const a = o[0], c = o[1], d = o[2], f = o[3], p = o[4], b = o[5], x = o[6], T = o[7], I = o[8], z = l[0], W = l[1], j = l[2], oe = l[3], ae = l[4], se = l[5], fe = l[6], Ie = l[7], Ae = l[8];
          return new t([
            z * a + W * f + j * x,
            z * c + W * p + j * T,
            z * d + W * b + j * I,
            oe * a + ae * f + se * x,
            oe * c + ae * p + se * T,
            oe * d + ae * b + se * I,
            fe * a + Ie * f + Ae * x,
            fe * c + Ie * p + Ae * T,
            fe * d + Ie * b + Ae * I
          ]);
        }
        /**
         * Sets the values of the matrix.
         * @param a00 top left
         * @param a01 top middle
         * @param a02 top right
         * @param a10 middle left
         * @param a11 middle middle
         * @param a12 middle right
         * @param a20 bottom left
         * @param a21 bottom middle
         * @param a22 bottom right
         */
        setValues(r, o, l, a, c, d, f, p, b) {
          this.values[0] = r, this.values[1] = o, this.values[2] = l, this.values[3] = a, this.values[4] = c, this.values[5] = d, this.values[6] = f, this.values[7] = p, this.values[8] = b;
        }
        /**
         * Scaling & translation only changes certain values, no need to compute full matrix multiplication.
         * @memberof OpenSeadragon.Mat3
         * @returns {OpenSeadragon.Mat3} The result of matrix multiplication
         */
        scaleAndTranslate(r, o, l, a) {
          const c = this.values, d = c[0], f = c[1], p = c[2], b = c[3], x = c[4], T = c[5];
          return new t([
            r * d,
            r * f,
            r * p,
            o * b,
            o * x,
            o * T,
            l * d + a * b,
            l * f + a * x,
            l * p + a * T
          ]);
        }
        /**
         * Scaling & translation only changes certain values, no need to compute full matrix multiplication.
         * Optimization: in case the original matrix can be thrown away, optimize instead by computing in-place.
         * @memberof OpenSeadragon.Mat3
         */
        scaleAndTranslateSelf(r, o, l, a) {
          const c = this.values, d = c[0], f = c[1], p = c[2], b = c[3], x = c[4], T = c[5];
          c[0] = r * d, c[1] = r * f, c[2] = r * p, c[3] = o * b, c[4] = o * x, c[5] = o * T, c[6] = l * d + a * b + c[6], c[7] = l * f + a * x + c[7], c[8] = l * p + a * T + c[8];
        }
        /**
         * Move and translate another matrix by self. 'this' matrix must be scale & translate matrix.
         * Optimization: in case the original matrix can be thrown away, optimize instead by computing in-place.
         * Used for optimization: we have
         * A) THIS matrix, carrying scale and translation,
         * B) OTHER general matrix to scale and translate.
         * Since THIS matrix is unique per tile, we can optimize the operation by:
         *  - move & scale OTHER by THIS, and
         *  - store the result to THIS, since we don't need to keep the scaling and translation, but
         *    we need to keep the original OTHER matrix (for each tile within tiled image).
         * @param {OpenSeadragon.Mat3} other the matrix to scale and translate by this matrix and accept values from
         * @memberof OpenSeadragon.Mat3
         */
        scaleAndTranslateOtherSetSelf(r) {
          const o = r.values, l = this.values, a = l[0], c = l[4], d = l[6], f = l[7];
          l[0] = a * o[0], l[1] = a * o[1], l[2] = a * o[2], l[3] = c * o[3], l[4] = c * o[4], l[5] = c * o[5], l[6] = d * o[0] + f * o[3] + o[6], l[7] = d * o[1] + f * o[4] + o[7], l[8] = d * o[2] + f * o[5] + o[8];
        }
      }
      e.Mat3 = t;
    })(n), (function(e) {
      const t = {
        supportsFullScreen: !1,
        isFullScreen: function() {
          return !1;
        },
        getFullScreenElement: function() {
          return null;
        },
        requestFullScreen: function() {
        },
        exitFullScreen: function() {
        },
        cancelFullScreen: function() {
        },
        fullScreenEventName: "",
        fullScreenErrorEventName: ""
      };
      document.exitFullscreen ? (t.supportsFullScreen = !0, t.getFullScreenElement = function() {
        return document.fullscreenElement;
      }, t.requestFullScreen = function(i) {
        return i.requestFullscreen().catch(function(r) {
          e.console.error("Fullscreen request failed: ", r);
        });
      }, t.exitFullScreen = function() {
        document.exitFullscreen().catch(function(i) {
          e.console.error("Error while exiting fullscreen: ", i);
        });
      }, t.fullScreenEventName = "fullscreenchange", t.fullScreenErrorEventName = "fullscreenerror") : document.msExitFullscreen ? (t.supportsFullScreen = !0, t.getFullScreenElement = function() {
        return document.msFullscreenElement;
      }, t.requestFullScreen = function(i) {
        return i.msRequestFullscreen();
      }, t.exitFullScreen = function() {
        document.msExitFullscreen();
      }, t.fullScreenEventName = "MSFullscreenChange", t.fullScreenErrorEventName = "MSFullscreenError") : document.webkitExitFullscreen ? (t.supportsFullScreen = !0, t.getFullScreenElement = function() {
        return document.webkitFullscreenElement;
      }, t.requestFullScreen = function(i) {
        return i.webkitRequestFullscreen();
      }, t.exitFullScreen = function() {
        document.webkitExitFullscreen();
      }, t.fullScreenEventName = "webkitfullscreenchange", t.fullScreenErrorEventName = "webkitfullscreenerror") : document.webkitCancelFullScreen ? (t.supportsFullScreen = !0, t.getFullScreenElement = function() {
        return document.webkitCurrentFullScreenElement;
      }, t.requestFullScreen = function(i) {
        return i.webkitRequestFullScreen();
      }, t.exitFullScreen = function() {
        document.webkitCancelFullScreen();
      }, t.fullScreenEventName = "webkitfullscreenchange", t.fullScreenErrorEventName = "webkitfullscreenerror") : document.mozCancelFullScreen && (t.supportsFullScreen = !0, t.getFullScreenElement = function() {
        return document.mozFullScreenElement;
      }, t.requestFullScreen = function(i) {
        return i.mozRequestFullScreen();
      }, t.exitFullScreen = function() {
        document.mozCancelFullScreen();
      }, t.fullScreenEventName = "mozfullscreenchange", t.fullScreenErrorEventName = "mozfullscreenerror"), t.isFullScreen = function() {
        return t.getFullScreenElement() !== null;
      }, t.cancelFullScreen = function() {
        e.console.error("cancelFullScreen is deprecated. Use exitFullScreen instead."), t.exitFullScreen();
      }, e.extend(e, t);
    })(n), (function(e) {
      e.EventSource = function() {
        this.events = {}, this._rejectedEventList = {};
      }, e.EventSource.prototype = {
        /**
         * Add an event handler to be triggered only once (or a given number of times)
         * for a given event. It is not removable with removeHandler().
         * @function
         * @param {String} eventName - Name of event to register.
         * @param {OpenSeadragon.EventHandler|OpenSeadragon.AsyncEventHandler} handler - Function to call when event
         * is triggered.
         * @param {Object} [userData=null] - Arbitrary object to be passed unchanged
         * to the handler.
         * @param {Number} [times=1] - The number of times to handle the event
         * before removing it.
         * @param {Number} [priority=0] - Handler priority. By default, all priorities are 0. Higher number = priority.
         * @returns {Boolean} - True if the handler was added, false if it was rejected
         */
        addOnceHandler: function(t, i, r, o, l) {
          const a = this;
          o = o || 1;
          let c = 0;
          const d = function(f) {
            return c++, c === o && a.removeHandler(t, d), i(f);
          };
          return this.addHandler(t, d, r, l);
        },
        /**
         * Add an event handler for a given event.
         * @function
         * @param {String} eventName - Name of event to register.
         * @param {OpenSeadragon.EventHandler|OpenSeadragon.AsyncEventHandler} handler - Function to call when event is triggered.
         * @param {Object} [userData=null] - Arbitrary object to be passed unchanged to the handler.
         * @param {Number} [priority=0] - Handler priority. By default, all priorities are 0. Higher number = priority.
         * @returns {Boolean} - True if the handler was added, false if it was rejected
         */
        addHandler: function(t, i, r, o) {
          if (Object.prototype.hasOwnProperty.call(this._rejectedEventList, t))
            return e.console.error(`Error adding handler for ${t}. ${this._rejectedEventList[t]}`), !1;
          let l = this.events[t];
          if (l || (this.events[t] = l = []), i && e.isFunction(i)) {
            let a = l.length, c = { handler: i, userData: r || null, priority: o || 0 };
            for (l[a] = c; a > 0 && l[a - 1].priority < l[a].priority; )
              l[a] = l[a - 1], l[a - 1] = c, a--;
          }
          return !0;
        },
        /**
         * Remove a specific event handler for a given event.
         * @function
         * @param {String} eventName - Name of event for which the handler is to be removed.
         * @param {OpenSeadragon.EventHandler|OpenSeadragon.AsyncEventHandler} handler - Function to be removed.
         */
        removeHandler: function(t, i) {
          const r = this.events[t], o = [];
          if (r && e.isArray(r)) {
            for (let l = 0; l < r.length; l++)
              r[l].handler !== i && o.push(r[l]);
            this.events[t] = o;
          }
        },
        /**
         * Get the amount of handlers registered for a given event.
         * @param {String} eventName - Name of event to inspect.
         * @returns {number} amount of events
         */
        numberOfHandlers: function(t) {
          const i = this.events[t];
          return i ? i.length : 0;
        },
        /**
         * Remove all event handlers for a given event type. If no type is given all
         * event handlers for every event type are removed.
         * @function
         * @param {String} eventName - Name of event for which all handlers are to be removed.
         */
        removeAllHandlers: function(t) {
          if (t)
            this.events[t] = [];
          else
            for (let i in this.events)
              this.events[i] = [];
        },
        /**
         * Get a function which iterates the list of all handlers registered for a given event, calling the handler for each.
         * @function
         * @param {String} eventName - Name of event to get handlers for.
         */
        getHandler: function(t) {
          let i = this.events[t];
          return !i || !i.length ? null : (i = i.length === 1 ? [i[0]] : Array.apply(null, i), function(r, o) {
            let l = i.length;
            for (let a = 0; a < l; a++)
              i[a] && (o.eventSource = r, o.userData = i[a].userData, i[a].handler(o));
          });
        },
        /**
         * Get a function which iterates the list of all handlers registered for a given event,
         * calling the handler for each and awaiting async ones.
         * @function
         * @param {String} eventName - Name of event to get handlers for.
         * @param {any} bindTarget - Bound target to return with the promise on finish
         */
        getAwaitingHandler: function(t, i) {
          let r = this.events[t];
          return !r || !r.length ? null : (r = r.length === 1 ? [r[0]] : Array.apply(null, r), function(o, l) {
            return new e.Promise((a) => {
              const c = r.length;
              function d(f) {
                if (f >= c || !r[f])
                  return a(i), null;
                l.eventSource = o, l.userData = r[f].userData;
                let p = r[f].handler(l);
                return p = !p || e.type(p) !== "promise" ? e.Promise.resolve() : p, p.then(() => d(f + 1));
              }
              d(0);
            });
          });
        },
        /**
         * Trigger an event, optionally passing additional information. Does not await async handlers, i.e.
         * OpenSeadragon.AsyncEventHandler.
         * @function
         * @param {String} eventName - Name of event to register.
         * @param {Object} eventArgs - Event-specific data.
         * @returns {Boolean} True if the event was fired, false if it was rejected because of rejectEventHandler(eventName)
         */
        raiseEvent: function(t, i) {
          if (Object.prototype.hasOwnProperty.call(this._rejectedEventList, t))
            return e.console.error(`Error adding handler for ${t}. ${this._rejectedEventList[t]}`), !1;
          const r = this.getHandler(t);
          return r && r(this, i || {}), !0;
        },
        /**
         * Trigger an event, optionally passing additional information.
         * This events awaits every asynchronous or promise-returning function, i.e.
         * OpenSeadragon.AsyncEventHandler.
         * @param {String} eventName - Name of event to register.
         * @param {Object} eventArgs - Event-specific data.
         * @param {?} [bindTarget = null] - Promise-resolved value on the event finish
         * @return {OpenSeadragon.Promise|undefined} - Promise resolved upon the event completion.
         */
        raiseEventAwaiting: function(t, i, r = null) {
          const o = this.getAwaitingHandler(t, r);
          return o ? o(this, i || {}) : e.Promise.resolve(r);
        },
        /**
         * Set an event name as being disabled, and provide an optional error message
         * to be printed to the console
         * @param {String} eventName - Name of the event
         * @param {String} [errorMessage] - Optional string to print to the console
         * @private
         */
        rejectEventHandler(t, i = "") {
          this._rejectedEventList[t] = i;
        },
        /**
         * Explicitly allow an event handler to be added for this event type, undoing
         * the effects of rejectEventHandler
         * @param {String} eventName - Name of the event
         * @private
         */
        allowEventHandler(t) {
          delete this._rejectedEventList[t];
        }
      };
    })(n), (function(e) {
      const t = {};
      e.MouseTracker = function(w) {
        const y = arguments;
        e.isPlainObject(w) || (w = {
          element: y[0],
          clickTimeThreshold: y[1],
          clickDistThreshold: y[2]
        }), this.hash = Tt(), this.element = e.getElement(w.element), this.clickTimeThreshold = w.clickTimeThreshold || e.DEFAULT_SETTINGS.clickTimeThreshold, this.clickDistThreshold = w.clickDistThreshold || e.DEFAULT_SETTINGS.clickDistThreshold, this.dblClickTimeThreshold = w.dblClickTimeThreshold || e.DEFAULT_SETTINGS.dblClickTimeThreshold, this.dblClickDistThreshold = w.dblClickDistThreshold || e.DEFAULT_SETTINGS.dblClickDistThreshold, this.userData = w.userData || null, this.stopDelay = w.stopDelay || 50, this.preProcessEventHandler = w.preProcessEventHandler || null, this.contextMenuHandler = w.contextMenuHandler || null, this.enterHandler = w.enterHandler || null, this.leaveHandler = w.leaveHandler || null, this.exitHandler = w.exitHandler || null, this.overHandler = w.overHandler || null, this.outHandler = w.outHandler || null, this.pressHandler = w.pressHandler || null, this.nonPrimaryPressHandler = w.nonPrimaryPressHandler || null, this.releaseHandler = w.releaseHandler || null, this.nonPrimaryReleaseHandler = w.nonPrimaryReleaseHandler || null, this.moveHandler = w.moveHandler || null, this.scrollHandler = w.scrollHandler || null, this.clickHandler = w.clickHandler || null, this.dblClickHandler = w.dblClickHandler || null, this.dragHandler = w.dragHandler || null, this.dragEndHandler = w.dragEndHandler || null, this.pinchHandler = w.pinchHandler || null, this.stopHandler = w.stopHandler || null, this.keyDownHandler = w.keyDownHandler || null, this.keyUpHandler = w.keyUpHandler || null, this.keyHandler = w.keyHandler || null, this.focusHandler = w.focusHandler || null, this.blurHandler = w.blurHandler || null;
        const E = this;
        t[this.hash] = {
          click: function(R) {
            j(E, R);
          },
          dblclick: function(R) {
            oe(E, R);
          },
          keydown: function(R) {
            ae(E, R);
          },
          keyup: function(R) {
            se(E, R);
          },
          keypress: function(R) {
            fe(E, R);
          },
          focus: function(R) {
            Ie(E, R);
          },
          blur: function(R) {
            Ae(E, R);
          },
          contextmenu: function(R) {
            qe(E, R);
          },
          wheel: function(R) {
            Ne(E, R);
          },
          mousewheel: function(R) {
            Fe(E, R);
          },
          DOMMouseScroll: function(R) {
            Fe(E, R);
          },
          MozMousePixelScroll: function(R) {
            Fe(E, R);
          },
          losecapture: function(R) {
            wt(E, R);
          },
          mouseenter: function(R) {
            _t(E, R);
          },
          mouseleave: function(R) {
            $t(E, R);
          },
          mouseover: function(R) {
            kt(E, R);
          },
          mouseout: function(R) {
            me(E, R);
          },
          mousedown: function(R) {
            xt(E, R);
          },
          mouseup: function(R) {
            bi(E, R);
          },
          mousemove: function(R) {
            It(E, R);
          },
          touchstart: function(R) {
            lt(E, R);
          },
          touchend: function(R) {
            ct(E, R);
          },
          touchmove: function(R) {
            pt(E, R);
          },
          touchcancel: function(R) {
            ht(E, R);
          },
          gesturestart: function(R) {
            mt(E, R);
          },
          // Safari/Safari iOS
          gesturechange: function(R) {
            ot(E, R);
          },
          // Safari/Safari iOS
          gotpointercapture: function(R) {
            Bt(E, R);
          },
          lostpointercapture: function(R) {
            Gi(E, R);
          },
          pointerenter: function(R) {
            _t(E, R);
          },
          pointerleave: function(R) {
            $t(E, R);
          },
          pointerover: function(R) {
            kt(E, R);
          },
          pointerout: function(R) {
            me(E, R);
          },
          pointerdown: function(R) {
            xt(E, R);
          },
          pointerup: function(R) {
            bi(E, R);
          },
          pointermove: function(R) {
            It(E, R);
          },
          pointercancel: function(R) {
            qi(E, R);
          },
          pointerupcaptured: function(R) {
            pe(E, R);
          },
          pointermovecaptured: function(R) {
            Pe(E, R);
          },
          tracking: !1,
          // Active pointers lists. Array of GesturePointList objects, one for each pointer device type.
          // GesturePointList objects are added each time a pointer is tracked by a new pointer device type (see getActivePointersListByType()).
          // Active pointers are any pointer being tracked for this element which are in the hit-test area
          //     of the element (for hover-capable devices) and/or have contact or a button press initiated in the element.
          activePointersLists: [],
          // Tracking for double-click gesture
          lastClickPos: null,
          dblClickTimeOut: null,
          // Tracking for pinch gesture
          pinchGPoints: [],
          lastPinchDist: 0,
          currentPinchDist: 0,
          lastPinchCenter: null,
          currentPinchCenter: null,
          // Tracking for drag
          sentDragEvent: !1
        }, e.MouseTracker.havePointerEvents && e.setElementPointerEvents(this.element, "auto"), this.exitHandler && e.console.error("MouseTracker.exitHandler is deprecated. Use MouseTracker.leaveHandler instead."), w.startDisabled || this.setTracking(!0);
      }, e.MouseTracker.prototype = {
        /**
         * Clean up any events or objects created by the tracker.
         * @function
         */
        destroy: function() {
          a(this), this.element = null, t[this.hash] = null, delete t[this.hash];
        },
        /**
         * Are we currently tracking events on this element.
         * @deprecated Just use this.tracking
         * @function
         * @returns {Boolean} Are we currently tracking events on this element.
         */
        isTracking: function() {
          return t[this.hash].tracking;
        },
        /**
         * Enable or disable whether or not we are tracking events on this element.
         * @function
         * @param {Boolean} track True to start tracking, false to stop tracking.
         * @returns {OpenSeadragon.MouseTracker} Chainable.
         */
        setTracking: function(w) {
          return w ? l(this) : a(this), this;
        },
        /**
         * Returns the {@link OpenSeadragon.MouseTracker.GesturePointList|GesturePointList} for the given pointer device type,
         * creating and caching a new {@link OpenSeadragon.MouseTracker.GesturePointList|GesturePointList} if one doesn't already exist for the type.
         * @function
         * @param {String} type - The pointer device type: "mouse", "touch", "pen", etc.
         * @returns {OpenSeadragon.MouseTracker.GesturePointList}
         */
        getActivePointersListByType: function(w) {
          const y = t[this.hash], E = y ? y.activePointersLists.length : 0;
          let R;
          for (let K = 0; K < E; K++)
            if (y.activePointersLists[K].type === w)
              return y.activePointersLists[K];
          return R = new e.MouseTracker.GesturePointList(w), y && y.activePointersLists.push(R), R;
        },
        /**
         * Returns the total number of pointers currently active on the tracked element.
         * @function
         * @returns {Number}
         */
        getActivePointerCount: function() {
          const w = t[this.hash], y = w.activePointersLists.length;
          let E = 0;
          for (let R = 0; R < y; R++)
            E += w.activePointersLists[R].getLength();
          return E;
        },
        /**
         * Do we currently have any assigned gesture handlers.
         * @returns {Boolean} Do we currently have any assigned gesture handlers.
         */
        get hasGestureHandlers() {
          return !!(this.pressHandler || this.nonPrimaryPressHandler || this.releaseHandler || this.nonPrimaryReleaseHandler || this.clickHandler || this.dblClickHandler || this.dragHandler || this.dragEndHandler || this.pinchHandler);
        },
        /**
         * Do we currently have a scroll handler.
         * @returns {Boolean} Do we currently have a scroll handler.
         */
        get hasScrollHandler() {
          return !!this.scrollHandler;
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {OpenSeadragon.MouseTracker.EventProcessInfo} eventInfo
         */
        preProcessEventHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Boolean} event.preventDefault
         *      Set to true to prevent the default user-agent's handling of the contextmenu event.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        contextMenuHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Number} event.pointers
         *      Number of pointers (all types) active in the tracked element.
         * @param {Boolean} event.insideElementPressed
         *      True if the left mouse button is currently being pressed and was
         *      initiated inside the tracked element, otherwise false.
         * @param {Boolean} event.buttonDownAny
         *      Was the button down anywhere in the screen during the event. <span style="color:red;">Deprecated. Use buttons instead.</span>
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        enterHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @since v2.5.0
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Number} event.pointers
         *      Number of pointers (all types) active in the tracked element.
         * @param {Boolean} event.insideElementPressed
         *      True if the left mouse button is currently being pressed and was
         *      initiated inside the tracked element, otherwise false.
         * @param {Boolean} event.buttonDownAny
         *      Was the button down anywhere in the screen during the event. <span style="color:red;">Deprecated. Use buttons instead.</span>
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        leaveHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @deprecated v2.5.0 Use leaveHandler instead
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Number} event.pointers
         *      Number of pointers (all types) active in the tracked element.
         * @param {Boolean} event.insideElementPressed
         *      True if the left mouse button is currently being pressed and was
         *      initiated inside the tracked element, otherwise false.
         * @param {Boolean} event.buttonDownAny
         *      Was the button down anywhere in the screen during the event. <span style="color:red;">Deprecated. Use buttons instead.</span>
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        exitHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @since v2.5.0
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Number} event.pointers
         *      Number of pointers (all types) active in the tracked element.
         * @param {Boolean} event.insideElementPressed
         *      True if the left mouse button is currently being pressed and was
         *      initiated inside the tracked element, otherwise false.
         * @param {Boolean} event.buttonDownAny
         *      Was the button down anywhere in the screen during the event. <span style="color:red;">Deprecated. Use buttons instead.</span>
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        overHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @since v2.5.0
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Number} event.pointers
         *      Number of pointers (all types) active in the tracked element.
         * @param {Boolean} event.insideElementPressed
         *      True if the left mouse button is currently being pressed and was
         *      initiated inside the tracked element, otherwise false.
         * @param {Boolean} event.buttonDownAny
         *      Was the button down anywhere in the screen during the event. <span style="color:red;">Deprecated. Use buttons instead.</span>
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        outHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        pressHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.button
         *      Button which caused the event.
         *      -1: none, 0: primary/left, 1: aux/middle, 2: secondary/right, 3: X1/back, 4: X2/forward, 5: pen eraser.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        nonPrimaryPressHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Boolean} event.insideElementPressed
         *      True if the left mouse button is currently being pressed and was
         *      initiated inside the tracked element, otherwise false.
         * @param {Boolean} event.insideElementReleased
         *      True if the cursor inside the tracked element when the button was released.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        releaseHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.button
         *      Button which caused the event.
         *      -1: none, 0: primary/left, 1: aux/middle, 2: secondary/right, 3: X1/back, 4: X2/forward, 5: pen eraser.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        nonPrimaryReleaseHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        moveHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.scroll
         *      The scroll delta for the event.
         * @param {Boolean} event.shift
         *      True if the shift key was pressed during this event.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead. Touch devices no longer generate scroll event.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Boolean} event.preventDefault
         *      Set to true to prevent the default user-agent's handling of the wheel event.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        scrollHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Boolean} event.quick
         *      True only if the clickDistThreshold and clickTimeThreshold are both passed. Useful for ignoring drag events.
         * @param {Boolean} event.shift
         *      True if the shift key was pressed during this event.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Element} event.originalTarget
         *      The DOM element clicked on.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        clickHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Boolean} event.shift
         *      True if the shift key was pressed during this event.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        dblClickHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {OpenSeadragon.Point} event.delta
         *      The x,y components of the difference between the current position and the last drag event position.  Useful for ignoring or weighting the events.
         * @param {Number} event.speed
         *     Current computed speed, in pixels per second.
         * @param {Number} event.direction
         *     Current computed direction, expressed as an angle counterclockwise relative to the positive X axis (-pi to pi, in radians). Only valid if speed > 0.
         * @param {Boolean} event.shift
         *      True if the shift key was pressed during this event.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        dragHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.speed
         *     Speed at the end of a drag gesture, in pixels per second.
         * @param {Number} event.direction
         *     Direction at the end of a drag gesture, expressed as an angle counterclockwise relative to the positive X axis (-pi to pi, in radians). Only valid if speed > 0.
         * @param {Boolean} event.shift
         *      True if the shift key was pressed during this event.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        dragEndHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {Array.<OpenSeadragon.MouseTracker.GesturePoint>} event.gesturePoints
         *      Gesture points associated with the gesture. Velocity data can be found here.
         * @param {OpenSeadragon.Point} event.lastCenter
         *      The previous center point of the two pinch contact points relative to the tracked element.
         * @param {OpenSeadragon.Point} event.center
         *      The center point of the two pinch contact points relative to the tracked element.
         * @param {Number} event.lastDistance
         *      The previous distance between the two pinch contact points in CSS pixels.
         * @param {Number} event.distance
         *      The distance between the two pinch contact points in CSS pixels.
         * @param {Boolean} event.shift
         *      True if the shift key was pressed during this event.
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        pinchHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {String} event.pointerType
         *     "mouse", "touch", "pen", etc.
         * @param {OpenSeadragon.Point} event.position
         *      The position of the event relative to the tracked element.
         * @param {Number} event.buttons
         *      Current buttons pressed.
         *      Combination of bit flags 0: none, 1: primary (or touch contact), 2: secondary, 4: aux (often middle), 8: X1 (often back), 16: X2 (often forward), 32: pen eraser.
         * @param {Boolean} event.isTouchEvent
         *      True if the original event is a touch event, otherwise false. <span style="color:red;">Deprecated. Use pointerType and/or originalEvent instead.</span>
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        stopHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {Number} event.keyCode
         *      The key code that was pressed.
         * @param {Boolean} event.ctrl
         *      True if the ctrl key was pressed during this event.
         * @param {Boolean} event.shift
         *      True if the shift key was pressed during this event.
         * @param {Boolean} event.alt
         *      True if the alt key was pressed during this event.
         * @param {Boolean} event.meta
         *      True if the meta key was pressed during this event.
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Boolean} event.preventDefault
         *      Set to true to prevent the default user-agent's handling of the keydown event.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        keyDownHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {Number} event.keyCode
         *      The key code that was pressed.
         * @param {Boolean} event.ctrl
         *      True if the ctrl key was pressed during this event.
         * @param {Boolean} event.shift
         *      True if the shift key was pressed during this event.
         * @param {Boolean} event.alt
         *      True if the alt key was pressed during this event.
         * @param {Boolean} event.meta
         *      True if the meta key was pressed during this event.
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Boolean} event.preventDefault
         *      Set to true to prevent the default user-agent's handling of the keyup event.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        keyUpHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {Number} event.keyCode
         *      The key code that was pressed.
         * @param {Boolean} event.ctrl
         *      True if the ctrl key was pressed during this event.
         * @param {Boolean} event.shift
         *      True if the shift key was pressed during this event.
         * @param {Boolean} event.alt
         *      True if the alt key was pressed during this event.
         * @param {Boolean} event.meta
         *      True if the meta key was pressed during this event.
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Boolean} event.preventDefault
         *      Set to true to prevent the default user-agent's handling of the keypress event.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        keyHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        focusHandler: function() {
        },
        /**
         * Implement or assign implementation to these handlers during or after
         * calling the constructor.
         * @function
         * @param {Object} event
         * @param {OpenSeadragon.MouseTracker} event.eventSource
         *      A reference to the tracker instance.
         * @param {Object} event.originalEvent
         *      The original event object.
         * @param {Object} event.userData
         *      Arbitrary user-defined object.
         */
        blurHandler: function() {
        }
      };
      const i = (function() {
        try {
          return window.self !== window.top;
        } catch {
          return !0;
        }
      })();
      function r(w) {
        try {
          return w.addEventListener && w.removeEventListener;
        } catch {
          return !1;
        }
      }
      e.MouseTracker.gesturePointVelocityTracker = /* @__PURE__ */ (function() {
        const w = [];
        let y = 0, E = 0;
        const R = function(Ce, He) {
          return Ce.hash.toString() + He.type + He.id.toString();
        }, K = function() {
          const Ce = w.length, He = e.now();
          let rt, Q;
          const Et = He - E;
          E = He;
          for (let wi = 0; wi < Ce; wi++) {
            const Dt = w[wi], Pt = Dt.gPoint;
            Pt.direction = Math.atan2(Pt.currentPos.y - Dt.lastPos.y, Pt.currentPos.x - Dt.lastPos.x), rt = Dt.lastPos.distanceTo(Pt.currentPos), Dt.lastPos = Pt.currentPos, Q = 1e3 * rt / (Et + 1), Pt.speed = 0.75 * Q + 0.25 * Pt.speed;
          }
        };
        return {
          addPoint: function(Ce, He) {
            const rt = R(Ce, He);
            w.push(
              {
                guid: rt,
                gPoint: He,
                lastPos: He.currentPos
              }
            ), w.length === 1 && (E = e.now(), y = window.setInterval(K, 50));
          },
          removePoint: function(Ce, He) {
            const rt = R(Ce, He);
            let Q = w.length;
            for (let Et = 0; Et < Q; Et++)
              if (w[Et].guid === rt) {
                w.splice(Et, 1), Q--, Q === 0 && window.clearInterval(y);
                break;
              }
          }
        };
      })(), e.MouseTracker.captureElement = document, e.MouseTracker.wheelEventName = "onwheel" in document.createElement("div") ? "wheel" : (
        // Modern browsers support 'wheel'
        document.onmousewheel !== void 0 ? "mousewheel" : (
          // Webkit (and unsupported IE) support at least 'mousewheel'
          "DOMMouseScroll"
        )
      ), e.MouseTracker.subscribeEvents = ["click", "dblclick", "keydown", "keyup", "keypress", "focus", "blur", "contextmenu", e.MouseTracker.wheelEventName], e.MouseTracker.wheelEventName === "DOMMouseScroll" && e.MouseTracker.subscribeEvents.push("MozMousePixelScroll"), window.PointerEvent ? (e.MouseTracker.havePointerEvents = !0, e.MouseTracker.subscribeEvents.push("pointerenter", "pointerleave", "pointerover", "pointerout", "pointerdown", "pointerup", "pointermove", "pointercancel"), e.MouseTracker.havePointerCapture = (function() {
        const w = document.createElement("div");
        return e.isFunction(w.setPointerCapture) && e.isFunction(w.releasePointerCapture);
      })(), e.MouseTracker.havePointerCapture && e.MouseTracker.subscribeEvents.push("gotpointercapture", "lostpointercapture")) : (e.MouseTracker.havePointerEvents = !1, e.MouseTracker.subscribeEvents.push("mouseenter", "mouseleave", "mouseover", "mouseout", "mousedown", "mouseup", "mousemove"), e.MouseTracker.mousePointerId = "legacy-mouse", e.MouseTracker.havePointerCapture = (function() {
        const w = document.createElement("div");
        return e.isFunction(w.setCapture) && e.isFunction(w.releaseCapture);
      })(), e.MouseTracker.havePointerCapture && e.MouseTracker.subscribeEvents.push("losecapture"), "ontouchstart" in window && e.MouseTracker.subscribeEvents.push("touchstart", "touchend", "touchmove", "touchcancel"), "ongesturestart" in window && e.MouseTracker.subscribeEvents.push("gesturestart", "gesturechange")), e.MouseTracker.GesturePointList = function(w) {
        this._gPoints = [], this.type = w, this.buttons = 0, this.contacts = 0, this.clicks = 0, this.captureCount = 0;
      }, e.MouseTracker.GesturePointList.prototype = {
        /**
         * @function
         * @returns {Number} Number of gesture points in the list.
         */
        getLength: function() {
          return this._gPoints.length;
        },
        /**
         * @function
         * @returns {Array.<OpenSeadragon.MouseTracker.GesturePoint>} The list of gesture points in the list as an array (read-only).
         */
        asArray: function() {
          return this._gPoints;
        },
        /**
         * @function
         * @param {OpenSeadragon.MouseTracker.GesturePoint} gesturePoint - A gesture point to add to the list.
         * @returns {Number} Number of gesture points in the list.
         */
        add: function(w) {
          return this._gPoints.push(w);
        },
        /**
         * @function
         * @param {Number} id - The id of the gesture point to remove from the list.
         * @returns {Number} Number of gesture points in the list.
         */
        removeById: function(w) {
          const y = this._gPoints.length;
          for (let E = 0; E < y; E++)
            if (this._gPoints[E].id === w) {
              this._gPoints.splice(E, 1);
              break;
            }
          return this._gPoints.length;
        },
        /**
         * @function
         * @param {Number} index - The index of the gesture point to retrieve from the list.
         * @returns {OpenSeadragon.MouseTracker.GesturePoint|null} The gesture point at the given index, or null if not found.
         */
        getByIndex: function(w) {
          return w < this._gPoints.length ? this._gPoints[w] : null;
        },
        /**
         * @function
         * @param {Number} id - The id of the gesture point to retrieve from the list.
         * @returns {OpenSeadragon.MouseTracker.GesturePoint|null} The gesture point with the given id, or null if not found.
         */
        getById: function(w) {
          const y = this._gPoints.length;
          for (let E = 0; E < y; E++)
            if (this._gPoints[E].id === w)
              return this._gPoints[E];
          return null;
        },
        /**
         * @function
         * @returns {OpenSeadragon.MouseTracker.GesturePoint|null} The primary gesture point in the list, or null if not found.
         */
        getPrimary: function(w) {
          const y = this._gPoints.length;
          for (let E = 0; E < y; E++)
            if (this._gPoints[E].isPrimary)
              return this._gPoints[E];
          return null;
        },
        /**
         * Increment this pointer list's contact count.
         * It will evaluate whether this pointer type is allowed to have multiple contacts.
         * @function
         */
        addContact: function() {
          ++this.contacts, this.contacts > 1 && (this.type === "mouse" || this.type === "pen") && (e.console.warn("GesturePointList.addContact() Implausible contacts value"), this.contacts = 1);
        },
        /**
         * Decrement this pointer list's contact count.
         * It will make sure the count does not go below 0.
         * @function
         */
        removeContact: function() {
          --this.contacts, this.contacts < 0 && (this.contacts = 0);
        }
      };
      function o(w) {
        const y = t[w.hash], E = y.activePointersLists.length;
        for (let R = 0; R < E; R++) {
          const K = y.activePointersLists[R];
          if (K.getLength() > 0) {
            const re = [], J = K.asArray();
            for (let Ce = 0; Ce < J.length; Ce++)
              re.push(J[Ce]);
            for (let Ce = 0; Ce < re.length; Ce++)
              P(w, K, re[Ce]);
          }
        }
        for (let R = 0; R < E; R++)
          y.activePointersLists.pop();
        y.sentDragEvent = !1;
      }
      function l(w) {
        const y = t[w.hash];
        if (!y.tracking) {
          for (let E = 0; E < e.MouseTracker.subscribeEvents.length; E++) {
            const R = e.MouseTracker.subscribeEvents[E];
            e.addEvent(
              w.element,
              R,
              y[R],
              R === e.MouseTracker.wheelEventName ? { passive: !1, capture: !1 } : !1
            );
          }
          o(w), y.tracking = !0;
        }
      }
      function a(w) {
        const y = t[w.hash];
        if (y.tracking) {
          for (let E = 0; E < e.MouseTracker.subscribeEvents.length; E++) {
            const R = e.MouseTracker.subscribeEvents[E];
            e.removeEvent(
              w.element,
              R,
              y[R],
              !1
            );
          }
          o(w), y.tracking = !1;
        }
      }
      function c(w, y) {
        const E = t[w.hash];
        if (y === "pointerevent")
          return {
            upName: "pointerup",
            upHandler: E.pointerupcaptured,
            moveName: "pointermove",
            moveHandler: E.pointermovecaptured
          };
        if (y === "mouse")
          return {
            upName: "pointerup",
            upHandler: E.pointerupcaptured,
            moveName: "pointermove",
            moveHandler: E.pointermovecaptured
          };
        if (y === "touch")
          return {
            upName: "touchend",
            upHandler: E.touchendcaptured,
            moveName: "touchmove",
            moveHandler: E.touchmovecaptured
          };
        throw new Error("MouseTracker.getCaptureEventParams: Unknown pointer type.");
      }
      function d(w, y) {
        if (e.MouseTracker.havePointerCapture)
          if (e.MouseTracker.havePointerEvents)
            try {
              w.element.setPointerCapture(y.id);
            } catch {
              e.console.warn("setPointerCapture() called on invalid pointer ID");
              return;
            }
          else
            w.element.setCapture(!0);
        else {
          const E = c(w, e.MouseTracker.havePointerEvents ? "pointerevent" : y.type);
          i && r(window.top) && e.addEvent(
            window.top,
            E.upName,
            E.upHandler,
            !0
          ), e.addEvent(
            e.MouseTracker.captureElement,
            E.upName,
            E.upHandler,
            !0
          ), e.addEvent(
            e.MouseTracker.captureElement,
            E.moveName,
            E.moveHandler,
            !0
          );
        }
        ee(w, y, !0);
      }
      function f(w, y) {
        if (e.MouseTracker.havePointerCapture)
          if (e.MouseTracker.havePointerEvents) {
            const R = w.getActivePointersListByType(y.type).getById(y.id);
            if (!R || !R.captured)
              return;
            try {
              w.element.releasePointerCapture(y.id);
            } catch {
            }
          } else
            w.element.releaseCapture();
        else {
          const E = c(w, e.MouseTracker.havePointerEvents ? "pointerevent" : y.type);
          i && r(window.top) && e.removeEvent(
            window.top,
            E.upName,
            E.upHandler,
            !0
          ), e.removeEvent(
            e.MouseTracker.captureElement,
            E.moveName,
            E.moveHandler,
            !0
          ), e.removeEvent(
            e.MouseTracker.captureElement,
            E.upName,
            E.upHandler,
            !0
          );
        }
        ee(w, y, !1);
      }
      function p(w) {
        return e.MouseTracker.havePointerEvents ? w.pointerId : e.MouseTracker.mousePointerId;
      }
      function b(w) {
        return e.MouseTracker.havePointerEvents && w.pointerType ? w.pointerType : "mouse";
      }
      function x(w) {
        return e.MouseTracker.havePointerEvents ? w.isPrimary : !0;
      }
      function T(w) {
        return e.getMousePosition(w);
      }
      function I(w, y) {
        return z(T(w), y);
      }
      function z(w, y) {
        const E = e.getElementOffset(y);
        return w.minus(E);
      }
      function W(w, y) {
        return new e.Point((w.x + y.x) / 2, (w.y + y.y) / 2);
      }
      function j(w, y) {
        const E = {
          originalEvent: y,
          eventType: "click",
          pointerType: "mouse",
          isEmulated: !1
        };
        q(w, E), E.preventDefault && !E.defaultPrevented && e.cancelEvent(y), E.stopPropagation && e.stopEvent(y);
      }
      function oe(w, y) {
        const E = {
          originalEvent: y,
          eventType: "dblclick",
          pointerType: "mouse",
          isEmulated: !1
        };
        q(w, E), E.preventDefault && !E.defaultPrevented && e.cancelEvent(y), E.stopPropagation && e.stopEvent(y);
      }
      function ae(w, y) {
        let E = null;
        const R = {
          originalEvent: y,
          eventType: "keydown",
          pointerType: "",
          isEmulated: !1
        };
        q(w, R), w.keyDownHandler && !R.preventGesture && !R.defaultPrevented && (E = {
          eventSource: w,
          keyCode: y.keyCode ? y.keyCode : y.charCode,
          ctrl: y.ctrlKey,
          shift: y.shiftKey,
          alt: y.altKey,
          meta: y.metaKey,
          originalEvent: y,
          preventDefault: R.preventDefault || R.defaultPrevented,
          userData: w.userData
        }, w.keyDownHandler(E)), (E && E.preventDefault || R.preventDefault && !R.defaultPrevented) && e.cancelEvent(y), R.stopPropagation && e.stopEvent(y);
      }
      function se(w, y) {
        let E = null;
        const R = {
          originalEvent: y,
          eventType: "keyup",
          pointerType: "",
          isEmulated: !1
        };
        q(w, R), w.keyUpHandler && !R.preventGesture && !R.defaultPrevented && (E = {
          eventSource: w,
          keyCode: y.keyCode ? y.keyCode : y.charCode,
          ctrl: y.ctrlKey,
          shift: y.shiftKey,
          alt: y.altKey,
          meta: y.metaKey,
          originalEvent: y,
          preventDefault: R.preventDefault || R.defaultPrevented,
          userData: w.userData
        }, w.keyUpHandler(E)), (E && E.preventDefault || R.preventDefault && !R.defaultPrevented) && e.cancelEvent(y), R.stopPropagation && e.stopEvent(y);
      }
      function fe(w, y) {
        let E = null;
        const R = {
          originalEvent: y,
          eventType: "keypress",
          pointerType: "",
          isEmulated: !1
        };
        q(w, R), w.keyHandler && !R.preventGesture && !R.defaultPrevented && (E = {
          eventSource: w,
          keyCode: y.keyCode ? y.keyCode : y.charCode,
          ctrl: y.ctrlKey,
          shift: y.shiftKey,
          alt: y.altKey,
          meta: y.metaKey,
          originalEvent: y,
          preventDefault: R.preventDefault || R.defaultPrevented,
          userData: w.userData
        }, w.keyHandler(E)), (E && E.preventDefault || R.preventDefault && !R.defaultPrevented) && e.cancelEvent(y), R.stopPropagation && e.stopEvent(y);
      }
      function Ie(w, y) {
        const E = {
          originalEvent: y,
          eventType: "focus",
          pointerType: "",
          isEmulated: !1
        };
        q(w, E), w.focusHandler && !E.preventGesture && w.focusHandler(
          {
            eventSource: w,
            originalEvent: y,
            userData: w.userData
          }
        );
      }
      function Ae(w, y) {
        const E = {
          originalEvent: y,
          eventType: "blur",
          pointerType: "",
          isEmulated: !1
        };
        q(w, E), w.blurHandler && !E.preventGesture && w.blurHandler(
          {
            eventSource: w,
            originalEvent: y,
            userData: w.userData
          }
        );
      }
      function qe(w, y) {
        let E = null;
        const R = {
          originalEvent: y,
          eventType: "contextmenu",
          pointerType: "mouse",
          isEmulated: !1
        };
        q(w, R), w.contextMenuHandler && !R.preventGesture && !R.defaultPrevented && (E = {
          eventSource: w,
          position: z(T(y), w.element),
          originalEvent: R.originalEvent,
          preventDefault: R.preventDefault || R.defaultPrevented,
          userData: w.userData
        }, w.contextMenuHandler(E)), (E && E.preventDefault || R.preventDefault && !R.defaultPrevented) && e.cancelEvent(y), R.stopPropagation && e.stopEvent(y);
      }
      function Ne(w, y) {
        De(w, y, y);
      }
      function Fe(w, y) {
        const E = {
          target: y.target || y.srcElement,
          type: "wheel",
          shiftKey: y.shiftKey || !1,
          clientX: y.clientX,
          clientY: y.clientY,
          pageX: y.pageX ? y.pageX : y.clientX,
          pageY: y.pageY ? y.pageY : y.clientY,
          deltaMode: y.type === "MozMousePixelScroll" ? 0 : 1,
          // 0=pixel, 1=line, 2=page
          deltaX: 0,
          deltaZ: 0
        };
        e.MouseTracker.wheelEventName === "mousewheel" ? E.deltaY = -y.wheelDelta / e.DEFAULT_SETTINGS.pixelsPerWheelLine : E.deltaY = y.detail, De(w, E, y);
      }
      function De(w, y, E) {
        let R = 0, K, re = null;
        R = y.deltaY ? y.deltaY < 0 ? 1 : -1 : 0, K = {
          originalEvent: y,
          eventType: "wheel",
          pointerType: "mouse",
          isEmulated: y !== E
        }, q(w, K), w.scrollHandler && !K.preventGesture && !K.defaultPrevented && (re = {
          eventSource: w,
          pointerType: "mouse",
          position: I(y, w.element),
          scroll: R,
          shift: y.shiftKey,
          isTouchEvent: !1,
          originalEvent: E,
          preventDefault: K.preventDefault || K.defaultPrevented,
          userData: w.userData
        }, w.scrollHandler(re)), K.stopPropagation && e.stopEvent(E), (re && re.preventDefault || K.preventDefault && !K.defaultPrevented) && e.cancelEvent(E);
      }
      function wt(w, y) {
        const E = {
          id: e.MouseTracker.mousePointerId,
          type: "mouse"
        }, R = {
          originalEvent: y,
          eventType: "lostpointercapture",
          pointerType: "mouse",
          isEmulated: !1
        };
        q(w, R), y.target === w.element && ee(w, E, !1), R.stopPropagation && e.stopEvent(y);
      }
      function lt(w, y) {
        const E = y.changedTouches.length, R = w.getActivePointersListByType("touch"), K = e.now();
        R.getLength() > y.touches.length - E && e.console.warn("Tracked touch contact count doesn't match event.touches.length");
        const re = {
          originalEvent: y,
          eventType: "pointerdown",
          pointerType: "touch",
          isEmulated: !1
        };
        q(w, re);
        for (let J = 0; J < E; J++) {
          const Ce = {
            id: y.changedTouches[J].identifier,
            type: "touch",
            // Simulate isPrimary
            isPrimary: R.getLength() === 0,
            currentPos: T(y.changedTouches[J]),
            currentTime: K
          };
          ue(w, re, Ce), ge(w, re, Ce, 0), ee(w, Ce, !0);
        }
        re.preventDefault && !re.defaultPrevented && e.cancelEvent(y), re.stopPropagation && e.stopEvent(y);
      }
      function ct(w, y) {
        const E = y.changedTouches.length, R = e.now(), K = {
          originalEvent: y,
          eventType: "pointerup",
          pointerType: "touch",
          isEmulated: !1
        };
        q(w, K);
        for (let re = 0; re < E; re++) {
          const J = {
            id: y.changedTouches[re].identifier,
            type: "touch",
            currentPos: T(y.changedTouches[re]),
            currentTime: R
          };
          Oe(w, K, J, 0), ee(w, J, !1), X(w, K, J);
        }
        K.preventDefault && !K.defaultPrevented && e.cancelEvent(y), K.stopPropagation && e.stopEvent(y);
      }
      function pt(w, y) {
        const E = y.changedTouches.length, R = e.now(), K = {
          originalEvent: y,
          eventType: "pointermove",
          pointerType: "touch",
          isEmulated: !1
        };
        q(w, K);
        for (let re = 0; re < E; re++) {
          const J = {
            id: y.changedTouches[re].identifier,
            type: "touch",
            currentPos: T(y.changedTouches[re]),
            currentTime: R
          };
          Ue(w, K, J);
        }
        K.preventDefault && !K.defaultPrevented && e.cancelEvent(y), K.stopPropagation && e.stopEvent(y);
      }
      function ht(w, y) {
        const E = y.changedTouches.length, R = {
          originalEvent: y,
          eventType: "pointercancel",
          pointerType: "touch",
          isEmulated: !1
        };
        q(w, R);
        for (let K = 0; K < E; K++) {
          const re = {
            id: y.changedTouches[K].identifier,
            type: "touch"
          };
          ve(w, R, re);
        }
        R.stopPropagation && e.stopEvent(y);
      }
      function mt(w, y) {
        return e.eventIsCanceled(y) || y.preventDefault(), !1;
      }
      function ot(w, y) {
        return e.eventIsCanceled(y) || y.preventDefault(), !1;
      }
      function Bt(w, y) {
        const E = {
          originalEvent: y,
          eventType: "gotpointercapture",
          pointerType: b(y),
          isEmulated: !1
        };
        q(w, E), y.target === w.element && ee(w, {
          id: y.pointerId,
          type: b(y)
        }, !0), E.stopPropagation && e.stopEvent(y);
      }
      function Gi(w, y) {
        const E = {
          originalEvent: y,
          eventType: "lostpointercapture",
          pointerType: b(y),
          isEmulated: !1
        };
        q(w, E), y.target === w.element && ee(w, {
          id: y.pointerId,
          type: b(y)
        }, !1), E.stopPropagation && e.stopEvent(y);
      }
      function _t(w, y) {
        const E = {
          id: p(y),
          type: b(y),
          isPrimary: x(y),
          currentPos: T(y),
          currentTime: e.now()
        }, R = {
          originalEvent: y,
          eventType: "pointerenter",
          pointerType: E.type,
          isEmulated: !1
        };
        q(w, R), ue(w, R, E);
      }
      function $t(w, y) {
        const E = {
          id: p(y),
          type: b(y),
          isPrimary: x(y),
          currentPos: T(y),
          currentTime: e.now()
        }, R = {
          originalEvent: y,
          eventType: "pointerleave",
          pointerType: E.type,
          isEmulated: !1
        };
        q(w, R), X(w, R, E);
      }
      function kt(w, y) {
        const E = {
          id: p(y),
          type: b(y),
          isPrimary: x(y),
          currentPos: T(y),
          currentTime: e.now()
        }, R = {
          originalEvent: y,
          eventType: "pointerover",
          pointerType: E.type,
          isEmulated: !1
        };
        q(w, R), le(w, R, E), R.preventDefault && !R.defaultPrevented && e.cancelEvent(y), R.stopPropagation && e.stopEvent(y);
      }
      function me(w, y) {
        const E = {
          id: p(y),
          type: b(y),
          isPrimary: x(y),
          currentPos: T(y),
          currentTime: e.now()
        }, R = {
          originalEvent: y,
          eventType: "pointerout",
          pointerType: E.type,
          isEmulated: !1
        };
        q(w, R), ke(w, R, E), R.preventDefault && !R.defaultPrevented && e.cancelEvent(y), R.stopPropagation && e.stopEvent(y);
      }
      function xt(w, y) {
        const E = {
          id: p(y),
          type: b(y),
          isPrimary: x(y),
          currentPos: T(y),
          currentTime: e.now()
        }, R = e.MouseTracker.havePointerEvents && E.type === "touch", K = {
          originalEvent: y,
          eventType: "pointerdown",
          pointerType: E.type,
          isEmulated: !1
        };
        q(w, K), ge(w, K, E, y.button), K.preventDefault && !K.defaultPrevented && e.cancelEvent(y), K.stopPropagation && e.stopEvent(y), K.shouldCapture && (R ? ee(w, E, !0) : d(w, E));
      }
      function bi(w, y) {
        ei(w, y);
      }
      function pe(w, y) {
        w.getActivePointersListByType(b(y)).getById(y.pointerId) && ei(w, y), e.stopEvent(y);
      }
      function ei(w, y) {
        const E = {
          id: p(y),
          type: b(y),
          isPrimary: x(y),
          currentPos: T(y),
          currentTime: e.now()
        }, R = {
          originalEvent: y,
          eventType: "pointerup",
          pointerType: E.type,
          isEmulated: !1
        };
        q(w, R), Oe(w, R, E, y.button), R.preventDefault && !R.defaultPrevented && e.cancelEvent(y), R.stopPropagation && e.stopEvent(y), R.shouldReleaseCapture && (y.target === w.element ? f(w, E) : ee(w, E, !1));
      }
      function It(w, y) {
        Ct(w, y);
      }
      function Pe(w, y) {
        w.getActivePointersListByType(b(y)).getById(y.pointerId) && Ct(w, y), e.stopEvent(y);
      }
      function Ct(w, y) {
        const E = {
          id: p(y),
          type: b(y),
          isPrimary: x(y),
          currentPos: T(y),
          currentTime: e.now()
        }, R = {
          originalEvent: y,
          eventType: "pointermove",
          pointerType: E.type,
          isEmulated: !1
        };
        q(w, R), Ue(w, R, E), R.preventDefault && !R.defaultPrevented && e.cancelEvent(y), R.stopPropagation && e.stopEvent(y);
      }
      function qi(w, y) {
        const E = {
          id: y.pointerId,
          type: b(y)
        }, R = {
          originalEvent: y,
          eventType: "pointercancel",
          pointerType: E.type,
          isEmulated: !1
        };
        q(w, R), ve(w, R, E), R.stopPropagation && e.stopEvent(y);
      }
      function m(w, y) {
        return y.speed = 0, y.direction = 0, y.contactPos = y.currentPos, y.contactTime = y.currentTime, y.lastPos = y.currentPos, y.lastTime = y.currentTime, w.add(y);
      }
      function P(w, y, E) {
        let R;
        const K = y.getById(E.id);
        return K ? (K.captured && (e.console.warn("stopTrackingPointer() called on captured pointer"), f(w, K)), y.removeContact(), R = y.removeById(E.id)) : R = y.getLength(), R;
      }
      function U(w, y) {
        switch (y.eventType) {
          case "pointermove":
            y.isStoppable = !0, y.isCancelable = !0, y.preventDefault = !1, y.preventGesture = !w.hasGestureHandlers, y.stopPropagation = !1;
            break;
          case "pointerover":
          case "pointerout":
          case "contextmenu":
          case "keydown":
          case "keyup":
          case "keypress":
            y.isStoppable = !0, y.isCancelable = !0, y.preventDefault = !1, y.preventGesture = !1, y.stopPropagation = !1;
            break;
          case "pointerdown":
            y.isStoppable = !0, y.isCancelable = !0, y.preventDefault = !1, y.preventGesture = !w.hasGestureHandlers, y.stopPropagation = !1;
            break;
          case "pointerup":
            y.isStoppable = !0, y.isCancelable = !0, y.preventDefault = !1, y.preventGesture = !w.hasGestureHandlers, y.stopPropagation = !1;
            break;
          case "wheel":
            y.isStoppable = !0, y.isCancelable = !0, y.preventDefault = !1, y.preventGesture = !w.hasScrollHandler, y.stopPropagation = !1;
            break;
          case "gotpointercapture":
          case "lostpointercapture":
          case "pointercancel":
            y.isStoppable = !0, y.isCancelable = !1, y.preventDefault = !1, y.preventGesture = !1, y.stopPropagation = !1;
            break;
          case "click":
            y.isStoppable = !0, y.isCancelable = !0, y.preventDefault = !!w.clickHandler, y.preventGesture = !1, y.stopPropagation = !1;
            break;
          case "dblclick":
            y.isStoppable = !0, y.isCancelable = !0, y.preventDefault = !!w.dblClickHandler, y.preventGesture = !1, y.stopPropagation = !1;
            break;
          case "focus":
          case "blur":
          case "pointerenter":
          case "pointerleave":
          default:
            y.isStoppable = !1, y.isCancelable = !1, y.preventDefault = !1, y.preventGesture = !1, y.stopPropagation = !1;
            break;
        }
      }
      function q(w, y) {
        y.eventSource = w, y.eventPhase = y.originalEvent && typeof y.originalEvent.eventPhase < "u" ? y.originalEvent.eventPhase : 0, y.defaultPrevented = e.eventIsCanceled(y.originalEvent), y.shouldCapture = !1, y.shouldReleaseCapture = !1, y.userData = w.userData, U(w, y), w.preProcessEventHandler && w.preProcessEventHandler(y);
      }
      function ee(w, y, E) {
        const R = w.getActivePointersListByType(y.type), K = R.getById(y.id);
        K ? E && !K.captured ? (K.captured = !0, R.captureCount++) : !E && K.captured && (K.captured = !1, R.captureCount--, R.captureCount < 0 && (R.captureCount = 0, e.console.warn("updatePointerCaptured() - pointsList.captureCount went negative"))) : e.console.warn("updatePointerCaptured() called on untracked pointer");
      }
      function ue(w, y, E) {
        const R = w.getActivePointersListByType(E.type), K = R.getById(E.id);
        K ? (K.insideElement = !0, K.lastPos = K.currentPos, K.lastTime = K.currentTime, K.currentPos = E.currentPos, K.currentTime = E.currentTime, E = K) : (E.captured = !1, E.insideElementPressed = !1, E.insideElement = !0, m(R, E)), w.enterHandler && w.enterHandler(
          {
            eventSource: w,
            pointerType: E.type,
            position: z(E.currentPos, w.element),
            buttons: R.buttons,
            pointers: w.getActivePointerCount(),
            insideElementPressed: E.insideElementPressed,
            buttonDownAny: R.buttons !== 0,
            isTouchEvent: E.type === "touch",
            originalEvent: y.originalEvent,
            userData: w.userData
          }
        );
      }
      function X(w, y, E) {
        const R = w.getActivePointersListByType(E.type), K = R.getById(E.id);
        if (K ? (K.captured ? (K.insideElement = !1, K.lastPos = K.currentPos, K.lastTime = K.currentTime, K.currentPos = E.currentPos, K.currentTime = E.currentTime) : P(w, R, K), E = K) : (E.captured = !1, E.insideElementPressed = !1), w.leaveHandler || w.exitHandler) {
          const re = {
            eventSource: w,
            pointerType: E.type,
            // GitHub PR: https://github.com/openseadragon/openseadragon/pull/1754 (gPoint.currentPos && )
            position: E.currentPos && z(E.currentPos, w.element),
            buttons: R.buttons,
            pointers: w.getActivePointerCount(),
            insideElementPressed: E.insideElementPressed,
            buttonDownAny: R.buttons !== 0,
            isTouchEvent: E.type === "touch",
            originalEvent: y.originalEvent,
            userData: w.userData
          };
          w.leaveHandler && w.leaveHandler(re), w.exitHandler && w.exitHandler(re);
        }
      }
      function le(w, y, E) {
        const R = w.getActivePointersListByType(E.type), K = R.getById(E.id);
        K ? E = K : (E.captured = !1, E.insideElementPressed = !1), w.overHandler && w.overHandler(
          {
            eventSource: w,
            pointerType: E.type,
            position: z(E.currentPos, w.element),
            buttons: R.buttons,
            pointers: w.getActivePointerCount(),
            insideElementPressed: E.insideElementPressed,
            buttonDownAny: R.buttons !== 0,
            isTouchEvent: E.type === "touch",
            originalEvent: y.originalEvent,
            userData: w.userData
          }
        );
      }
      function ke(w, y, E) {
        const R = w.getActivePointersListByType(E.type), K = R.getById(E.id);
        K ? E = K : (E.captured = !1, E.insideElementPressed = !1), w.outHandler && w.outHandler({
          eventSource: w,
          pointerType: E.type,
          position: E.currentPos && z(E.currentPos, w.element),
          buttons: R.buttons,
          pointers: w.getActivePointerCount(),
          insideElementPressed: E.insideElementPressed,
          buttonDownAny: R.buttons !== 0,
          isTouchEvent: E.type === "touch",
          originalEvent: y.originalEvent,
          userData: w.userData
        });
      }
      function ge(w, y, E, R) {
        const K = t[w.hash], re = w.getActivePointersListByType(E.type);
        if (typeof y.originalEvent.buttons < "u" ? re.buttons = y.originalEvent.buttons : R === 0 ? re.buttons |= 1 : R === 1 ? re.buttons |= 4 : R === 2 ? re.buttons |= 2 : R === 3 ? re.buttons |= 8 : R === 4 ? re.buttons |= 16 : R === 5 && (re.buttons |= 32), R !== 0) {
          y.shouldCapture = !1, y.shouldReleaseCapture = !1, w.nonPrimaryPressHandler && !y.preventGesture && !y.defaultPrevented && (y.preventDefault = !0, w.nonPrimaryPressHandler(
            {
              eventSource: w,
              pointerType: E.type,
              position: z(E.currentPos, w.element),
              button: R,
              buttons: re.buttons,
              isTouchEvent: E.type === "touch",
              originalEvent: y.originalEvent,
              userData: w.userData
            }
          ));
          return;
        }
        const J = re.getById(E.id);
        J ? (J.insideElementPressed = !0, J.insideElement = !0, J.originalTarget = y.originalEvent.target, J.contactPos = E.currentPos, J.contactTime = E.currentTime, J.lastPos = J.currentPos, J.lastTime = J.currentTime, J.currentPos = E.currentPos, J.currentTime = E.currentTime, E = J) : (E.captured = !1, E.insideElementPressed = !0, E.insideElement = !0, E.originalTarget = y.originalEvent.target, m(re, E)), re.addContact(), !y.preventGesture && !y.defaultPrevented ? (y.shouldCapture = !0, y.shouldReleaseCapture = !1, y.preventDefault = !0, (w.dragHandler || w.dragEndHandler || w.pinchHandler) && e.MouseTracker.gesturePointVelocityTracker.addPoint(w, E), re.contacts === 1 ? w.pressHandler && !y.preventGesture && w.pressHandler(
          {
            eventSource: w,
            pointerType: E.type,
            position: z(E.contactPos, w.element),
            buttons: re.buttons,
            isTouchEvent: E.type === "touch",
            originalEvent: y.originalEvent,
            userData: w.userData
          }
        ) : re.contacts === 2 && w.pinchHandler && E.type === "touch" && (K.pinchGPoints = re.asArray(), K.lastPinchDist = K.currentPinchDist = K.pinchGPoints[0].currentPos.distanceTo(K.pinchGPoints[1].currentPos), K.lastPinchCenter = K.currentPinchCenter = W(K.pinchGPoints[0].currentPos, K.pinchGPoints[1].currentPos))) : (y.shouldCapture = !1, y.shouldReleaseCapture = !1);
      }
      function Oe(w, y, E, R) {
        const K = t[w.hash], re = w.getActivePointersListByType(E.type);
        let J, Ce, He = !1, rt;
        if (typeof y.originalEvent.buttons < "u" ? re.buttons = y.originalEvent.buttons : R === 0 ? re.buttons ^= -2 : R === 1 ? re.buttons ^= -5 : R === 2 ? re.buttons ^= -3 : R === 3 ? re.buttons ^= -9 : R === 4 ? re.buttons ^= -17 : R === 5 && (re.buttons ^= -33), y.shouldCapture = !1, R !== 0) {
          y.shouldReleaseCapture = !1, w.nonPrimaryReleaseHandler && !y.preventGesture && !y.defaultPrevented && (y.preventDefault = !0, w.nonPrimaryReleaseHandler(
            {
              eventSource: w,
              pointerType: E.type,
              position: z(E.currentPos, w.element),
              button: R,
              buttons: re.buttons,
              isTouchEvent: E.type === "touch",
              originalEvent: y.originalEvent,
              userData: w.userData
            }
          ));
          return;
        }
        let Q = re.getById(E.id);
        Q ? (re.removeContact(), Q.captured && (He = !0), Q.lastPos = Q.currentPos, Q.lastTime = Q.currentTime, Q.currentPos = E.currentPos, Q.currentTime = E.currentTime, Q.insideElement || P(w, re, Q), J = Q.currentPos, Ce = Q.currentTime) : (E.captured = !1, E.insideElementPressed = !1, E.insideElement = !0, m(re, E), Q = E), !y.preventGesture && !y.defaultPrevented && (He ? (y.shouldReleaseCapture = !0, y.preventDefault = !0, (w.dragHandler || w.dragEndHandler || w.pinchHandler) && e.MouseTracker.gesturePointVelocityTracker.removePoint(w, Q), re.contacts === 0 ? (w.releaseHandler && J && w.releaseHandler(
          {
            eventSource: w,
            pointerType: Q.type,
            position: z(J, w.element),
            buttons: re.buttons,
            insideElementPressed: Q.insideElementPressed,
            insideElementReleased: Q.insideElement,
            isTouchEvent: Q.type === "touch",
            originalEvent: y.originalEvent,
            userData: w.userData
          }
        ), w.dragEndHandler && K.sentDragEvent && w.dragEndHandler(
          {
            eventSource: w,
            pointerType: Q.type,
            position: z(Q.currentPos, w.element),
            speed: Q.speed,
            direction: Q.direction,
            shift: y.originalEvent.shiftKey,
            isTouchEvent: Q.type === "touch",
            originalEvent: y.originalEvent,
            userData: w.userData
          }
        ), K.sentDragEvent = !1, (w.clickHandler || w.dblClickHandler) && Q.insideElement && (rt = Ce - Q.contactTime <= w.clickTimeThreshold && Q.contactPos.distanceTo(J) <= w.clickDistThreshold, w.clickHandler && w.clickHandler(
          {
            eventSource: w,
            pointerType: Q.type,
            position: z(Q.currentPos, w.element),
            quick: rt,
            shift: y.originalEvent.shiftKey,
            isTouchEvent: Q.type === "touch",
            originalEvent: y.originalEvent,
            originalTarget: Q.originalTarget,
            userData: w.userData
          }
        ), w.dblClickHandler && rt && (re.clicks++, re.clicks === 1 ? (K.lastClickPos = J, K.dblClickTimeOut = setTimeout(function() {
          re.clicks = 0;
        }, w.dblClickTimeThreshold)) : re.clicks === 2 && (clearTimeout(K.dblClickTimeOut), re.clicks = 0, K.lastClickPos.distanceTo(J) <= w.dblClickDistThreshold && w.dblClickHandler(
          {
            eventSource: w,
            pointerType: Q.type,
            position: z(Q.currentPos, w.element),
            shift: y.originalEvent.shiftKey,
            isTouchEvent: Q.type === "touch",
            originalEvent: y.originalEvent,
            userData: w.userData
          }
        ), K.lastClickPos = null)))) : re.contacts === 2 && w.pinchHandler && Q.type === "touch" && (K.pinchGPoints = re.asArray(), K.lastPinchDist = K.currentPinchDist = K.pinchGPoints[0].currentPos.distanceTo(K.pinchGPoints[1].currentPos), K.lastPinchCenter = K.currentPinchCenter = W(K.pinchGPoints[0].currentPos, K.pinchGPoints[1].currentPos))) : (y.shouldReleaseCapture = !1, w.releaseHandler && J && (w.releaseHandler(
          {
            eventSource: w,
            pointerType: Q.type,
            position: z(J, w.element),
            buttons: re.buttons,
            insideElementPressed: Q.insideElementPressed,
            insideElementReleased: Q.insideElement,
            isTouchEvent: Q.type === "touch",
            originalEvent: y.originalEvent,
            userData: w.userData
          }
        ), y.preventDefault = !0)));
      }
      function Ue(w, y, E) {
        const R = t[w.hash], K = w.getActivePointersListByType(E.type);
        let re;
        typeof y.originalEvent.buttons < "u" && (K.buttons = y.originalEvent.buttons);
        let J = K.getById(E.id);
        if (J)
          J.lastPos = J.currentPos, J.lastTime = J.currentTime, J.currentPos = E.currentPos, J.currentTime = E.currentTime;
        else
          return;
        if (y.shouldCapture = !1, y.shouldReleaseCapture = !1, w.stopHandler && E.type === "mouse" && (clearTimeout(w.stopTimeOut), w.stopTimeOut = setTimeout(function() {
          Ke(w, y.originalEvent, E.type);
        }, w.stopDelay)), K.contacts === 0)
          w.moveHandler && w.moveHandler(
            {
              eventSource: w,
              pointerType: E.type,
              position: z(E.currentPos, w.element),
              buttons: K.buttons,
              isTouchEvent: E.type === "touch",
              originalEvent: y.originalEvent,
              userData: w.userData
            }
          );
        else if (K.contacts === 1)
          w.moveHandler && (J = K.asArray()[0], w.moveHandler(
            {
              eventSource: w,
              pointerType: J.type,
              position: z(J.currentPos, w.element),
              buttons: K.buttons,
              isTouchEvent: J.type === "touch",
              originalEvent: y.originalEvent,
              userData: w.userData
            }
          )), w.dragHandler && !y.preventGesture && !y.defaultPrevented && (J = K.asArray()[0], re = J.currentPos.minus(J.lastPos), w.dragHandler(
            {
              eventSource: w,
              pointerType: J.type,
              position: z(J.currentPos, w.element),
              buttons: K.buttons,
              delta: re,
              speed: J.speed,
              direction: J.direction,
              shift: y.originalEvent.shiftKey,
              isTouchEvent: J.type === "touch",
              originalEvent: y.originalEvent,
              userData: w.userData
            }
          ), y.preventDefault = !0, R.sentDragEvent = !0);
        else if (K.contacts === 2) {
          if (w.moveHandler) {
            const Ce = K.asArray();
            w.moveHandler(
              {
                eventSource: w,
                pointerType: Ce[0].type,
                position: z(W(Ce[0].currentPos, Ce[1].currentPos), w.element),
                buttons: K.buttons,
                isTouchEvent: Ce[0].type === "touch",
                originalEvent: y.originalEvent,
                userData: w.userData
              }
            );
          }
          w.pinchHandler && E.type === "touch" && !y.preventGesture && !y.defaultPrevented && (re = R.pinchGPoints[0].currentPos.distanceTo(R.pinchGPoints[1].currentPos), re !== R.currentPinchDist && (R.lastPinchDist = R.currentPinchDist, R.currentPinchDist = re, R.lastPinchCenter = R.currentPinchCenter, R.currentPinchCenter = W(R.pinchGPoints[0].currentPos, R.pinchGPoints[1].currentPos), w.pinchHandler(
            {
              eventSource: w,
              pointerType: "touch",
              gesturePoints: R.pinchGPoints,
              lastCenter: z(R.lastPinchCenter, w.element),
              center: z(R.currentPinchCenter, w.element),
              lastDistance: R.lastPinchDist,
              distance: R.currentPinchDist,
              shift: y.originalEvent.shiftKey,
              originalEvent: y.originalEvent,
              userData: w.userData
            }
          ), y.preventDefault = !0));
        }
      }
      function ve(w, y, E) {
        const R = w.getActivePointersListByType(E.type), K = R.getById(E.id);
        K && P(w, R, K);
      }
      function Ke(w, y, E) {
        w.stopHandler && w.stopHandler({
          eventSource: w,
          pointerType: E,
          position: I(y, w.element),
          buttons: w.getActivePointersListByType(E).buttons,
          isTouchEvent: E === "touch",
          originalEvent: y,
          userData: w.userData
        });
      }
      function Tt() {
        let w = Date.now().toString(36) + Math.random().toString(36).substring(2);
        for (; w in t; )
          w = Date.now().toString(36) + Math.random().toString(36).substring(2);
        return w;
      }
    })(n), (function(e) {
      e.ControlAnchor = {
        NONE: 0,
        TOP_LEFT: 1,
        TOP_RIGHT: 2,
        BOTTOM_RIGHT: 3,
        BOTTOM_LEFT: 4,
        ABSOLUTE: 5
      }, e.Control = function(t, i, r) {
        const o = t.parentNode;
        typeof i == "number" && (e.console.error("Passing an anchor directly into the OpenSeadragon.Control constructor is deprecated; please use an options object instead.  Support for this deprecated variant is scheduled for removal in December 2013"), i = { anchor: i }), i.attachToViewer = typeof i.attachToViewer > "u" ? !0 : i.attachToViewer, this.autoFade = typeof i.autoFade > "u" ? !0 : i.autoFade, this.element = t, this.anchor = i.anchor, this.container = r, this.anchor === e.ControlAnchor.ABSOLUTE ? (this.wrapper = e.makeNeutralElement("div"), this.wrapper.style.position = "absolute", this.wrapper.style.top = typeof i.top == "number" ? i.top + "px" : i.top, this.wrapper.style.left = typeof i.left == "number" ? i.left + "px" : i.left, this.wrapper.style.height = typeof i.height == "number" ? i.height + "px" : i.height, this.wrapper.style.width = typeof i.width == "number" ? i.width + "px" : i.width, this.wrapper.style.margin = "0px", this.wrapper.style.padding = "0px", this.element.style.position = "relative", this.element.style.top = "0px", this.element.style.left = "0px", this.element.style.height = "100%", this.element.style.width = "100%") : (this.wrapper = e.makeNeutralElement("div"), this.wrapper.style.display = "inline-block", this.anchor === e.ControlAnchor.NONE && (this.wrapper.style.width = this.wrapper.style.height = "100%")), this.wrapper.appendChild(this.element), i.attachToViewer ? this.anchor === e.ControlAnchor.TOP_RIGHT || this.anchor === e.ControlAnchor.BOTTOM_RIGHT ? this.container.insertBefore(
          this.wrapper,
          this.container.firstChild
        ) : this.container.appendChild(this.wrapper) : o.appendChild(this.wrapper);
      }, e.Control.prototype = {
        /**
         * Removes the control from the container.
         * @function
         */
        destroy: function() {
          this.wrapper.removeChild(this.element), this.anchor !== e.ControlAnchor.NONE && this.container.removeChild(this.wrapper);
        },
        /**
         * Determines if the control is currently visible.
         * @function
         * @returns {Boolean} true if currently visible, false otherwise.
         */
        isVisible: function() {
          return this.wrapper.style.display !== "none";
        },
        /**
         * Toggles the visibility of the control.
         * @function
         * @param {Boolean} visible - true to make visible, false to hide.
         */
        setVisible: function(t) {
          this.wrapper.style.display = t ? this.anchor === e.ControlAnchor.ABSOLUTE ? "block" : "inline-block" : "none";
        },
        /**
         * Sets the opacity level for the control.
         * @function
         * @param {Number} opactiy - a value between 1 and 0 inclusively.
         */
        setOpacity: function(t) {
          e.setElementOpacity(this.wrapper, t, !0);
        }
      };
    })(n), (function(e) {
      e.ControlDock = function(i) {
        const r = ["topleft", "topright", "bottomright", "bottomleft"];
        e.extend(!0, this, {
          id: "controldock-" + e.now() + "-" + Math.floor(Math.random() * 1e6),
          container: e.makeNeutralElement("div"),
          controls: []
        }, i), this.container.onsubmit = function() {
          return !1;
        }, this.element && (this.element = e.getElement(this.element), this.element.appendChild(this.container), e.getElementStyle(this.element).position === "static" && (this.element.style.position = "relative"), this.container.style.width = "100%", this.container.style.height = "100%");
        for (let o = 0; o < r.length; o++) {
          let l = r[o];
          this.controls[l] = e.makeNeutralElement("div"), this.controls[l].style.position = "absolute", l.match("left") && (this.controls[l].style.left = "0px"), l.match("right") && (this.controls[l].style.right = "0px"), l.match("top") && (this.controls[l].style.top = "0px"), l.match("bottom") && (this.controls[l].style.bottom = "0px");
        }
        this.container.appendChild(this.controls.topleft), this.container.appendChild(this.controls.topright), this.container.appendChild(this.controls.bottomright), this.container.appendChild(this.controls.bottomleft);
      }, e.ControlDock.prototype = {
        /**
         * @function
         */
        addControl: function(i, r) {
          i = e.getElement(i);
          let o = null;
          if (!(t(this, i) >= 0)) {
            switch (r.anchor) {
              case e.ControlAnchor.TOP_RIGHT:
                o = this.controls.topright, i.style.position = "relative", i.style.paddingRight = "0px", i.style.paddingTop = "0px";
                break;
              case e.ControlAnchor.BOTTOM_RIGHT:
                o = this.controls.bottomright, i.style.position = "relative", i.style.paddingRight = "0px", i.style.paddingBottom = "0px";
                break;
              case e.ControlAnchor.BOTTOM_LEFT:
                o = this.controls.bottomleft, i.style.position = "relative", i.style.paddingLeft = "0px", i.style.paddingBottom = "0px";
                break;
              case e.ControlAnchor.TOP_LEFT:
                o = this.controls.topleft, i.style.position = "relative", i.style.paddingLeft = "0px", i.style.paddingTop = "0px";
                break;
              case e.ControlAnchor.ABSOLUTE:
                o = this.container, i.style.margin = "0px", i.style.padding = "0px";
                break;
              default:
              case e.ControlAnchor.NONE:
                o = this.container, i.style.margin = "0px", i.style.padding = "0px";
                break;
            }
            this.controls.push(
              new e.Control(i, r, o)
            ), i.style.display = "inline-block";
          }
        },
        /**
         * @function
         * @returns {OpenSeadragon.ControlDock} Chainable.
         */
        removeControl: function(i) {
          i = e.getElement(i);
          const r = t(this, i);
          return r >= 0 && (this.controls[r].destroy(), this.controls.splice(r, 1)), this;
        },
        /**
         * @function
         * @returns {OpenSeadragon.ControlDock} Chainable.
         */
        clearControls: function() {
          for (; this.controls.length > 0; )
            this.controls.pop().destroy();
          return this;
        },
        /**
         * @function
         * @returns {Boolean}
         */
        areControlsEnabled: function() {
          for (let i = this.controls.length - 1; i >= 0; i--)
            if (this.controls[i].isVisible())
              return !0;
          return !1;
        },
        /**
         * @function
         * @returns {OpenSeadragon.ControlDock} Chainable.
         */
        setControlsEnabled: function(i) {
          for (let r = this.controls.length - 1; r >= 0; r--)
            this.controls[r].setVisible(i);
          return this;
        }
      };
      function t(i, r) {
        const o = i.controls;
        for (let l = o.length - 1; l >= 0; l--)
          if (o[l].element === r)
            return l;
        return -1;
      }
    })(n), (function(e) {
      e.Placement = e.freezeObject({
        CENTER: 0,
        TOP_LEFT: 1,
        TOP: 2,
        TOP_RIGHT: 3,
        RIGHT: 4,
        BOTTOM_RIGHT: 5,
        BOTTOM: 6,
        BOTTOM_LEFT: 7,
        LEFT: 8,
        properties: {
          0: {
            isLeft: !1,
            isHorizontallyCentered: !0,
            isRight: !1,
            isTop: !1,
            isVerticallyCentered: !0,
            isBottom: !1
          },
          1: {
            isLeft: !0,
            isHorizontallyCentered: !1,
            isRight: !1,
            isTop: !0,
            isVerticallyCentered: !1,
            isBottom: !1
          },
          2: {
            isLeft: !1,
            isHorizontallyCentered: !0,
            isRight: !1,
            isTop: !0,
            isVerticallyCentered: !1,
            isBottom: !1
          },
          3: {
            isLeft: !1,
            isHorizontallyCentered: !1,
            isRight: !0,
            isTop: !0,
            isVerticallyCentered: !1,
            isBottom: !1
          },
          4: {
            isLeft: !1,
            isHorizontallyCentered: !1,
            isRight: !0,
            isTop: !1,
            isVerticallyCentered: !0,
            isBottom: !1
          },
          5: {
            isLeft: !1,
            isHorizontallyCentered: !1,
            isRight: !0,
            isTop: !1,
            isVerticallyCentered: !1,
            isBottom: !0
          },
          6: {
            isLeft: !1,
            isHorizontallyCentered: !0,
            isRight: !1,
            isTop: !1,
            isVerticallyCentered: !1,
            isBottom: !0
          },
          7: {
            isLeft: !0,
            isHorizontallyCentered: !1,
            isRight: !1,
            isTop: !1,
            isVerticallyCentered: !1,
            isBottom: !0
          },
          8: {
            isLeft: !0,
            isHorizontallyCentered: !1,
            isRight: !1,
            isTop: !1,
            isVerticallyCentered: !0,
            isBottom: !1
          }
        }
      });
    })(n), (function(e) {
      const t = {};
      let i = 1;
      e.Viewer = function(m) {
        const P = arguments, U = this;
        let q;
        e.isPlainObject(m) || (m = {
          id: P[0],
          xmlPath: P.length > 1 ? P[1] : void 0,
          prefixUrl: P.length > 2 ? P[2] : void 0,
          controls: P.length > 3 ? P[3] : void 0,
          overlays: P.length > 4 ? P[4] : void 0
        }), m.config && (e.extend(!0, m, m.config), delete m.config);
        const ee = [
          "useCanvas"
          // deprecated
        ];
        if (m.drawerOptions = Object.assign(
          {},
          ee.reduce((X, le) => (X[le] = m[le], delete m[le], X), {}),
          m.drawerOptions
        ), e.extend(!0, this, {
          //internal state and dom identifiers
          id: m.id,
          hash: m.hash || i++,
          /**
           * Index for page to be shown first next time open() is called (only used in sequenceMode).
           * @member {Number} initialPage
           * @memberof OpenSeadragon.Viewer#
           */
          initialPage: 0,
          //dom nodes
          /**
           * The parent element of this Viewer instance, passed in when the Viewer was created.
           * @member {Element} element
           * @memberof OpenSeadragon.Viewer#
           */
          element: null,
          /**
           * A &lt;div&gt; element (provided by {@link OpenSeadragon.ControlDock}), the base element of this Viewer instance.<br><br>
           * Child element of {@link OpenSeadragon.Viewer#element}.
           * @member {Element} container
           * @memberof OpenSeadragon.Viewer#
           */
          container: null,
          /**
           * A &lt;div&gt; element, the element where user-input events are handled for panning and zooming.<br><br>
           * Child element of {@link OpenSeadragon.Viewer#container},
           * positioned on top of {@link OpenSeadragon.Viewer#keyboardCommandArea}.<br><br>
           * The parent of {@link OpenSeadragon.Drawer#canvas} instances.
           * @member {Element} canvas
           * @memberof OpenSeadragon.Viewer#
           */
          canvas: null,
          // Overlays list. An overlay allows to add html on top of the viewer.
          overlays: [],
          // Container inside the canvas where overlays are drawn.
          overlaysContainer: null,
          //private state properties
          previousBody: [],
          //This was originally initialized in the constructor and so could never
          //have anything in it.  now it can because we allow it to be specified
          //in the options and is only empty by default if not specified. Also
          //this array was returned from get_controls which I find confusing
          //since this object has a controls property which is treated in other
          //functions like clearControls.  I'm removing the accessors.
          customControls: [],
          //These are originally not part options but declared as members
          //in initialize.  It's still considered idiomatic to put them here
          //source is here for backwards compatibility. It is not an official
          //part of the API and should not be relied upon.
          source: null,
          /**
           * Handles rendering of tiles in the viewer. Created for each TileSource opened.
           * @member {OpenSeadragon.Drawer} drawer
           * @memberof OpenSeadragon.Viewer#
           */
          drawer: null,
          /**
           * Keeps track of all of the tiled images in the scene.
           * @member {OpenSeadragon.World} world
           * @memberof OpenSeadragon.Viewer#
           */
          world: null,
          /**
           * Handles coordinate-related functionality - zoom, pan, rotation, etc. Created for each TileSource opened.
           * @member {OpenSeadragon.Viewport} viewport
           * @memberof OpenSeadragon.Viewer#
           */
          viewport: null,
          /**
           * @member {OpenSeadragon.Navigator} navigator
           * @memberof OpenSeadragon.Viewer#
           */
          navigator: null,
          //A collection viewport is a separate viewport used to provide
          //simultaneous rendering of sets of tiles
          collectionViewport: null,
          collectionDrawer: null,
          //UI image resources
          //TODO: rename navImages to uiImages
          navImages: null,
          //interface button controls
          buttonGroup: null,
          //TODO: this is defunct so safely remove it
          profiler: null
        }, e.DEFAULT_SETTINGS, m), typeof this.hash > "u")
          throw new Error("A hash must be defined, either by specifying options.id or options.hash.");
        if (typeof t[this.hash] < "u" && e.console.warn("Hash " + this.hash + " has already been used."), t[this.hash] = {
          fsBoundsDelta: new e.Point(1, 1),
          prevContainerSize: null,
          animating: !1,
          forceRedraw: !1,
          needsResize: !1,
          forceResize: !1,
          mouseInside: !1,
          group: null,
          // whether we should be continuously zooming
          zooming: !1,
          // how much we should be continuously zooming by
          zoomFactor: null,
          lastZoomTime: null,
          fullPage: !1,
          onfullscreenchange: null,
          lastClickTime: null,
          draggingToZoom: !1
        }, this._sequenceIndex = 0, this._firstOpen = !0, this._updateRequestId = null, this._loadQueue = [], this.currentOverlays = [], this._updatePixelDensityRatioBind = null, this._lastScrollTime = e.now(), this._fullyLoaded = !1, this._navActionFrames = {}, this._navActionVirtuallyHeld = {}, this._minNavActionFrames = 10, this._activeActions = {
          // variable to keep track of currently pressed action
          // Basic arrow key panning (no modifiers)
          panUp: !1,
          panDown: !1,
          panLeft: !1,
          panRight: !1,
          // Modifier-based actions
          zoomIn: !1,
          // Shift + Up
          zoomOut: !1
          // Shift + Down
        }, e.EventSource.call(this), this.addHandler("open-failed", function(X) {
          const le = e.getString("Errors.OpenFailed", X.eventSource, X.message);
          U._showMessage(le);
        }), e.ControlDock.call(this, m), this.xmlPath && (this.tileSources = [this.xmlPath]), this.element = this.element || document.getElementById(this.id), this.canvas = e.makeNeutralElement("div"), this.canvas.className = "openseadragon-canvas", !document.querySelector("style[data-openseadragon-mobile-css]")) {
          const X = document.createElement("style");
          X.setAttribute("data-openseadragon-mobile-css", "true"), X.textContent = "@media (hover: none) {    .openseadragon-canvas:focus {        outline: none !important;    }}", document.head.appendChild(X);
        }
        (function(X) {
          X.width = "100%", X.height = "100%", X.overflow = "hidden", X.position = "absolute", X.top = "0px", X.left = "0px";
        })(this.canvas.style), e.setElementTouchActionNone(this.canvas), m.tabIndex !== "" && (this.canvas.tabIndex = m.tabIndex === void 0 ? 0 : m.tabIndex), this.container.className = "openseadragon-container", (function(X) {
          X.width = "100%", X.height = "100%", X.position = "relative", X.overflow = "hidden", X.left = "0px", X.top = "0px", X.textAlign = "left";
        })(this.container.style), e.setElementTouchActionNone(this.container), this.container.insertBefore(this.canvas, this.container.firstChild), this.element.appendChild(this.container), this.bodyWidth = document.body.style.width, this.bodyHeight = document.body.style.height, this.bodyOverflow = document.body.style.overflow, this.docOverflow = document.documentElement.style.overflow, this.innerTracker = new e.MouseTracker({
          userData: "Viewer.innerTracker",
          element: this.canvas,
          startDisabled: !this.mouseNavEnabled,
          clickTimeThreshold: this.clickTimeThreshold,
          clickDistThreshold: this.clickDistThreshold,
          dblClickTimeThreshold: this.dblClickTimeThreshold,
          dblClickDistThreshold: this.dblClickDistThreshold,
          contextMenuHandler: e.delegate(this, I),
          keyDownHandler: e.delegate(this, j),
          keyUpHandler: e.delegate(this, W),
          keyHandler: e.delegate(this, oe),
          clickHandler: e.delegate(this, ae),
          dblClickHandler: e.delegate(this, se),
          dragHandler: e.delegate(this, fe),
          dragEndHandler: e.delegate(this, Ie),
          enterHandler: e.delegate(this, Ae),
          leaveHandler: e.delegate(this, qe),
          pressHandler: e.delegate(this, Ne),
          releaseHandler: e.delegate(this, Fe),
          nonPrimaryPressHandler: e.delegate(this, De),
          nonPrimaryReleaseHandler: e.delegate(this, wt),
          scrollHandler: e.delegate(this, ht),
          pinchHandler: e.delegate(this, lt),
          focusHandler: e.delegate(this, ct),
          blurHandler: e.delegate(this, pt)
        }), this.outerTracker = new e.MouseTracker({
          userData: "Viewer.outerTracker",
          element: this.container,
          startDisabled: !this.mouseNavEnabled,
          clickTimeThreshold: this.clickTimeThreshold,
          clickDistThreshold: this.clickDistThreshold,
          dblClickTimeThreshold: this.dblClickTimeThreshold,
          dblClickDistThreshold: this.dblClickDistThreshold,
          enterHandler: e.delegate(this, mt),
          leaveHandler: e.delegate(this, ot)
        }), this.toolbar && (this.toolbar = new e.ControlDock({ element: this.toolbar })), this.bindStandardControls(), t[this.hash].prevContainerSize = r(this.container), window.ResizeObserver ? (this._autoResizePolling = !1, this._resizeObserver = new ResizeObserver(function() {
          t[U.hash].needsResize = !0;
        }), this._resizeObserver.observe(this.container, {})) : this._autoResizePolling = !0, this.world = new e.World({
          viewer: this
        }), this.world.addHandler("add-item", function(X) {
          U.source = U.world.getItemAt(0).source, t[U.hash].forceRedraw = !0, U._updateRequestId || (U._updateRequestId = c(U, Bt));
          const le = X.item, ke = function() {
            const ge = U._areAllFullyLoaded();
            ge !== U._fullyLoaded && (U._fullyLoaded = ge, U.raiseEvent("fully-loaded-change", {
              fullyLoaded: ge
            }));
          };
          le._fullyLoadedHandlerForViewer = ke, le.addHandler("fully-loaded-change", ke);
        }), this.world.addHandler("remove-item", function(X) {
          const le = X.item;
          le._fullyLoadedHandlerForViewer && (le.removeHandler("fully-loaded-change", le._fullyLoadedHandlerForViewer), delete le._fullyLoadedHandlerForViewer), U.world.getItemCount() ? U.source = U.world.getItemAt(0).source : U.source = null, t[U.hash].forceRedraw = !0;
        }), this.world.addHandler("metrics-change", function(X) {
          U.viewport && U.viewport._setContentBounds(U.world.getHomeBounds(), U.world.getContentFactor());
        }), this.world.addHandler("item-index-change", function(X) {
          U.source = U.world.getItemAt(0).source;
        }), this.viewport = new e.Viewport({
          containerSize: t[this.hash].prevContainerSize,
          springStiffness: this.springStiffness,
          animationTime: this.animationTime,
          minZoomImageRatio: this.minZoomImageRatio,
          maxZoomPixelRatio: this.maxZoomPixelRatio,
          visibilityRatio: this.visibilityRatio,
          wrapHorizontal: this.wrapHorizontal,
          wrapVertical: this.wrapVertical,
          defaultZoomLevel: this.defaultZoomLevel,
          minZoomLevel: this.minZoomLevel,
          maxZoomLevel: this.maxZoomLevel,
          viewer: this,
          degrees: this.degrees,
          flipped: this.flipped,
          overlayPreserveContentDirection: this.overlayPreserveContentDirection,
          navigatorRotate: this.navigatorRotate,
          homeFillsViewer: this.homeFillsViewer,
          margins: this.viewportMargins,
          silenceMultiImageWarnings: this.silenceMultiImageWarnings
        }), this.viewport._setContentBounds(this.world.getHomeBounds(), this.world.getContentFactor()), this.imageLoader = new e.ImageLoader({
          jobLimit: this.imageLoaderLimit,
          timeout: m.timeout,
          tileRetryMax: this.tileRetryMax,
          tileRetryDelay: this.tileRetryDelay
        }), this.tileCache = new e.TileCache({
          viewer: this,
          maxImageCacheCount: this.maxImageCacheCount
        }), Object.prototype.hasOwnProperty.call(this.drawerOptions, "useCanvas") && (e.console.error('useCanvas is deprecated, use the "drawer" option to indicate preferred drawer(s)'), this.drawerOptions.useCanvas || (this.drawer = e.HTMLDrawer), delete this.drawerOptions.useCanvas);
        let ue = Array.isArray(this.drawer) ? this.drawer : [this.drawer];
        ue.length === 0 && (ue = [e.DEFAULT_SETTINGS.drawer].flat(), e.console.warn("No valid drawers were selected. Using the default value.")), this.drawer = null;
        for (const X of ue)
          if (this.requestDrawer(X, { mainDrawer: !0, redrawImmediately: !1 }))
            break;
        if (!this.drawer)
          throw e.console.error("No drawer could be created!"), "Error with creating the selected drawer(s)";
        for (this.drawer.setImageSmoothingEnabled(this.imageSmoothingEnabled), this.overlaysContainer = e.makeNeutralElement("div"), this.canvas.appendChild(this.overlaysContainer), this.drawer.canRotate() || (this.rotateLeft && (q = this.buttonGroup.buttons.indexOf(this.rotateLeft), this.buttonGroup.buttons.splice(q, 1), this.buttonGroup.element.removeChild(this.rotateLeft.element)), this.rotateRight && (q = this.buttonGroup.buttons.indexOf(this.rotateRight), this.buttonGroup.buttons.splice(q, 1), this.buttonGroup.element.removeChild(this.rotateRight.element))), this._addUpdatePixelDensityRatioEvent(), this.showNavigator && (this.navigator = new e.Navigator({
          element: this.navigatorElement,
          id: this.navigatorId,
          position: this.navigatorPosition,
          sizeRatio: this.navigatorSizeRatio,
          maintainSizeRatio: this.navigatorMaintainSizeRatio,
          top: this.navigatorTop,
          left: this.navigatorLeft,
          width: this.navigatorWidth,
          height: this.navigatorHeight,
          autoResize: this.navigatorAutoResize,
          autoFade: this.navigatorAutoFade,
          prefixUrl: this.prefixUrl,
          viewer: this,
          navigatorRotate: this.navigatorRotate,
          background: this.navigatorBackground,
          opacity: this.navigatorOpacity,
          borderColor: this.navigatorBorderColor,
          displayRegionColor: this.navigatorDisplayRegionColor,
          crossOriginPolicy: this.crossOriginPolicy,
          animationTime: this.animationTime,
          drawer: this.drawer.getType(),
          drawerOptions: this.drawerOptions,
          loadTilesWithAjax: this.loadTilesWithAjax,
          ajaxHeaders: this.ajaxHeaders,
          ajaxWithCredentials: this.ajaxWithCredentials
        })), this.sequenceMode && this.bindSequenceControls(), this.tileSources && this.open(this.tileSources), q = 0; q < this.customControls.length; q++)
          this.addControl(
            this.customControls[q].id,
            { anchor: this.customControls[q].anchor }
          );
        e.requestAnimationFrame(function() {
          f(U);
        }), e._viewers.set(this.element, this);
      }, e.extend(
        e.Viewer.prototype,
        e.EventSource.prototype,
        e.ControlDock.prototype,
        /** @lends OpenSeadragon.Viewer.prototype */
        {
          /**
           * @function
           * @returns {Boolean}
           */
          isOpen: function() {
            return !!this.world.getItemCount();
          },
          /**
           * Checks whether all TiledImage instances in the viewer's world are fully loaded.
           * This determines if the entire viewer content is ready for optimal display without partial tile loading.
           * @private
           * @returns {Boolean} True if all TiledImages report being fully loaded,
           *                    false if any image still has pending tiles
           */
          _areAllFullyLoaded: function() {
            const m = this.world.getItemCount();
            for (let P = 0; P < m; P++)
              if (!this.world.getItemAt(P).getFullyLoaded())
                return !1;
            return !0;
          },
          /**
           * @function
           * @returns {Boolean} True if all required tiles are loaded, false otherwise
           */
          getFullyLoaded: function() {
            return this._fullyLoaded;
          },
          /**
           * Executes the provided callback when the TiledImage is fully loaded. If already loaded,
           * schedules the callback asynchronously. Otherwise, attaches a one-time event listener
           * for the 'fully-loaded-change' event.
           * @param {Function} callback - Function to execute when loading completes
           * @memberof OpenSeadragon.Viewer.prototype
           */
          whenFullyLoaded: function(m) {
            this.getFullyLoaded() ? setTimeout(m, 1) : this.addOnceHandler("fully-loaded-change", function() {
              m();
            });
          },
          // deprecated
          openDzi: function(m) {
            return e.console.error("[Viewer.openDzi] this function is deprecated; use Viewer.open() instead."), this.open(m);
          },
          // deprecated
          openTileSource: function(m) {
            return e.console.error("[Viewer.openTileSource] this function is deprecated; use Viewer.open() instead."), this.open(m);
          },
          //deprecated
          get buttons() {
            return e.console.warn("Viewer.buttons is deprecated; Please use Viewer.buttonGroup"), this.buttonGroup;
          },
          /**
           * Open tiled images into the viewer, closing any others.
           * To get the TiledImage instance created by open, add an event listener for
           * {@link OpenSeadragon.Viewer.html#.event:open}, which when fired can be used to get access
           * to the instance, i.e., viewer.world.getItemAt(0).
           * @function
           * @param {Array|String|Object|Function} tileSources - This can be a TiledImage
           * specifier, a TileSource specifier, or an array of either. A TiledImage specifier
           * is the same as the options parameter for {@link OpenSeadragon.Viewer#addTiledImage},
           * except for the index property; images are added in sequence.
           * A TileSource specifier is anything you could pass as the tileSource property
           * of the options parameter for {@link OpenSeadragon.Viewer#addTiledImage}.
           * @param {Number} initialPage - If sequenceMode is true, display this page initially
           * for the given tileSources. If specified, will overwrite the Viewer's existing initialPage property.
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:open
           * @fires OpenSeadragon.Viewer.event:open-failed
           */
          open: function(m, P) {
            const U = this;
            if (this.close(), !m)
              return this;
            if (this.sequenceMode && e.isArray(m))
              return this.referenceStrip && (this.referenceStrip.destroy(), this.referenceStrip = null), typeof P < "u" && !isNaN(P) && (this.initialPage = P), this.tileSources = m, this._sequenceIndex = Math.max(0, Math.min(this.tileSources.length - 1, this.initialPage)), this.tileSources.length && (this.open(this.tileSources[this._sequenceIndex]), this.showReferenceStrip && this.addReferenceStrip()), this._updateSequenceButtons(this._sequenceIndex), this;
            if (e.isArray(m) || (m = [m]), !m.length)
              return this;
            this._opening = !0;
            const q = m.length;
            let ee = 0, ue = 0, X;
            const le = function() {
              if (ee + ue === q)
                if (ee) {
                  (U._firstOpen || !U.preserveViewport) && (U.viewport.goHome(!0), U.viewport.update()), U._firstOpen = !1;
                  let ge = m[0];
                  if (ge.tileSource && (ge = ge.tileSource), U.overlays && !U.preserveOverlays)
                    for (let Oe = 0; Oe < U.overlays.length; Oe++)
                      U.currentOverlays[Oe] = l(U, U.overlays[Oe]);
                  U._drawOverlays(), U._opening = !1, U.raiseEvent("open", { source: ge });
                } else
                  U._opening = !1, U.raiseEvent("open-failed", X);
            }, ke = function(ge) {
              (!e.isPlainObject(ge) || !ge.tileSource) && (ge = {
                tileSource: ge
              }), ge.index !== void 0 && (e.console.error("[Viewer.open] setting indexes here is not supported; use addTiledImage instead"), delete ge.index), ge.collectionImmediately === void 0 && (ge.collectionImmediately = !0);
              const Oe = ge.success;
              ge.success = function(ve) {
                if (ee++, ge.tileSource.overlays)
                  for (let Ke = 0; Ke < ge.tileSource.overlays.length; Ke++)
                    U.addOverlay(ge.tileSource.overlays[Ke]);
                Oe && Oe(ve), le();
              };
              const Ue = ge.error;
              ge.error = function(ve) {
                ue++, X || (X = ve), Ue && Ue(ve), le();
              }, U.addTiledImage(ge);
            };
            for (let ge = 0; ge < m.length; ge++)
              ke(m[ge]);
            return this;
          },
          /**
           * Updates data within every tile in the viewer. Should be called
           * when tiles are outdated and should be re-processed. Useful mainly
           * for plugins that change tile data.
           * @function
           * @param {Boolean} [restoreTiles=true] if true, tile processing starts from the tile original data
           * @fires OpenSeadragon.Viewer.event:tile-invalidated
           * @return {OpenSeadragon.Promise<?>}
           */
          requestInvalidate: function(m = !0) {
            if (!t[this.hash])
              return e.Promise.resolve();
            const P = e.now(), U = this.world.requestInvalidate(m, P);
            if (!this.navigator)
              return U;
            const q = this.navigator.world.requestInvalidate(m, P);
            return e.Promise.all([U, q]);
          },
          /**
           * @function
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:close
           */
          close: function() {
            return t[this.hash] ? (this._opening = !1, this.navigator && this.navigator.close(), this.preserveOverlays || (this.clearOverlays(), this.overlaysContainer.innerHTML = ""), t[this.hash].animating = !1, this.world.removeAll(), this.tileCache.clear(), this.imageLoader.clear(), this.raiseEvent("close"), this) : this;
          },
          /**
           * Function to destroy the viewer and clean up everything created by OpenSeadragon.
           *
           * Example:
           * var viewer = OpenSeadragon({
           *   [...]
           * });
           *
           * //when you are done with the viewer:
           * viewer.destroy();
           * viewer = null; //important
           *
           * @function
           * @fires OpenSeadragon.Viewer.event:before-destroy
           * @fires OpenSeadragon.Viewer.event:destroy
           */
          destroy: function() {
            if (t[this.hash]) {
              if (this.raiseEvent("before-destroy"), this._removeUpdatePixelDensityRatioEvent(), this.close(), this.clearOverlays(), this.overlaysContainer.innerHTML = "", this._resizeObserver && this._resizeObserver.disconnect(), this.referenceStrip && (this.referenceStrip.destroy(), this.referenceStrip = null), this._updateRequestId !== null && (e.cancelAnimationFrame(this._updateRequestId), this._updateRequestId = null), this.drawer && this.drawer.destroy(), this.navigator && (this.navigator.destroy(), t[this.navigator.hash] = null, delete t[this.navigator.hash], this.navigator = null), this.buttonGroup)
                this.buttonGroup.destroy();
              else if (this.customButtons)
                for (; this.customButtons.length; )
                  this.customButtons.pop().destroy();
              this.paging && this.paging.destroy(), this.container && this.container.parentNode === this.element && this.element.removeChild(this.container), this.container.onsubmit = null, this.clearControls(), this.innerTracker && this.innerTracker.destroy(), this.outerTracker && this.outerTracker.destroy(), t[this.hash] = null, delete t[this.hash], this.canvas = null, this.container = null, e._viewers.delete(this.element), this.element = null, this.raiseEvent("destroy"), this.removeAllHandlers();
            }
          },
          /**
           * Check if the viewer has been destroyed or not yet initialized.
           * @return {boolean}
           */
          isDestroyed() {
            return !t[this.hash];
          },
          /**
           * Request a drawer for this viewer, as a supported string or drawer constructor.
           * @param {String | OpenSeadragon.DrawerBase} drawerCandidate The type of drawer to try to construct.
           * @param { Object } options
           * @param { Boolean } [options.mainDrawer] Whether to use this as the viewer's main drawer. Default = true.
           * @param { Boolean } [options.redrawImmediately] Whether to immediately draw a new frame. Only used if options.mainDrawer = true. Default = true.
           * @param { Object } [options.drawerOptions] Options for this drawer. Defaults to viewer.drawerOptions.
           * for this viewer type. See {@link OpenSeadragon.Options}.
           * @returns {Object | Boolean} The drawer that was created, or false if the requested drawer is not supported
           */
          requestDrawer(m, P) {
            const U = {
              mainDrawer: !0,
              redrawImmediately: !0,
              drawerOptions: null
            };
            P = e.extend(!0, U, P);
            const q = P.mainDrawer, ee = P.redrawImmediately, ue = P.drawerOptions, X = this.drawer;
            let le = null;
            if (m && m.prototype instanceof e.DrawerBase ? (le = m, m = "custom") : typeof m == "string" && (le = e.determineDrawer(m)), le || e.console.warn("Unsupported drawer %s! Drawer must be an existing string type, or a class that extends OpenSeadragon.DrawerBase.", m), le && le.isSupported()) {
              X && q && X.destroy();
              const ke = new le({
                viewer: this,
                viewport: this.viewport,
                element: this.canvas,
                debugGridColor: this.debugGridColor,
                options: ue || this.drawerOptions[m]
              });
              return q && (this.drawer = ke, ee && this.forceRedraw()), ke;
            }
            return !1;
          },
          /**
           * @function
           * @returns {Boolean}
           */
          isMouseNavEnabled: function() {
            return this.innerTracker.tracking;
          },
          /**
           * @function
           * @param {Boolean} enabled - true to enable, false to disable
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:mouse-enabled
           */
          setMouseNavEnabled: function(m) {
            return this.innerTracker.setTracking(m), this.outerTracker.setTracking(m), this.raiseEvent("mouse-enabled", { enabled: m }), this;
          },
          /**
           * @function
           * @returns {Boolean}
           */
          isKeyboardNavEnabled: function() {
            return this.keyboardNavEnabled;
          },
          /**
           * @function
           * @param {Boolean} enabled - true to enable, false to disable
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:keyboard-enabled
           */
          setKeyboardNavEnabled: function(m) {
            return this.keyboardNavEnabled = m, this.raiseEvent("keyboard-enabled", { enabled: m }), this;
          },
          /**
           * @function
           * @returns {Boolean}
           */
          areControlsEnabled: function() {
            let m = this.controls.length;
            for (let P = 0; P < this.controls.length; P++)
              m = m && this.controls[P].isVisible();
            return m;
          },
          /**
           * Shows or hides the controls (e.g. the default navigation buttons).
           *
           * @function
           * @param {Boolean} true to show, false to hide.
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:controls-enabled
           */
          setControlsEnabled: function(m) {
            return m ? b(this) : f(this), this.raiseEvent("controls-enabled", { enabled: m }), this;
          },
          /**
           * Turns debugging mode on or off for this viewer.
           *
           * @function
           * @param {Boolean} debugMode true to turn debug on, false to turn debug off.
           */
          setDebugMode: function(m) {
            for (let P = 0; P < this.world.getItemCount(); P++)
              this.world.getItemAt(P).debugMode = m;
            this.debugMode = m, this.forceRedraw();
          },
          /**
           * Update headers to include when making AJAX requests.
           *
           * Unless `propagate` is set to false (which is likely only useful in rare circumstances),
           * the updated headers are propagated to all tiled images, each of which will subsequently
           * propagate the changed headers to all their tiles.
           * If applicable, the headers of the viewer's navigator and reference strip will also be updated.
           *
           * Note that the rules for merging headers still apply, i.e. headers returned by
           * {@link OpenSeadragon.TileSource#getTileAjaxHeaders} take precedence over
           * `TiledImage.ajaxHeaders`, which take precedence over the headers here in the viewer.
           *
           * @function
           * @param {Object} ajaxHeaders Updated AJAX headers.
           * @param {Boolean} [propagate=true] Whether to propagate updated headers to tiled images, etc.
           */
          setAjaxHeaders: function(m, P) {
            if (m === null && (m = {}), !e.isPlainObject(m)) {
              e.console.error("[Viewer.setAjaxHeaders] Ignoring invalid headers, must be a plain object");
              return;
            }
            if (P === void 0 && (P = !0), this.ajaxHeaders = m, P) {
              for (let U = 0; U < this.world.getItemCount(); U++)
                this.world.getItemAt(U)._updateAjaxHeaders(!0);
              if (this.navigator && this.navigator.setAjaxHeaders(this.ajaxHeaders, !0), this.referenceStrip && this.referenceStrip.miniViewers)
                for (const U in this.referenceStrip.miniViewers)
                  this.referenceStrip.miniViewers[U].setAjaxHeaders(this.ajaxHeaders, !0);
            }
          },
          /**
           * Adds the given button to this viewer.
           *
           * @function
           * @param {OpenSeadragon.Button} button
           */
          addButton: function(m) {
            this.buttonGroup.addButton(m);
          },
          /**
           * @function
           * @returns {Boolean}
           */
          isFullPage: function() {
            return t[this.hash] && t[this.hash].fullPage;
          },
          /**
           * Toggle full page mode.
           * @function
           * @param {Boolean} fullPage
           *      If true, enter full page mode.  If false, exit full page mode.
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:pre-full-page
           * @fires OpenSeadragon.Viewer.event:full-page
           */
          setFullPage: function(m) {
            const P = document.body, U = P.style, q = document.documentElement.style, ee = this;
            let ue;
            if (m === this.isFullPage())
              return this;
            const X = {
              fullPage: m,
              preventDefaultAction: !1
            };
            if (this.raiseEvent("pre-full-page", X), X.preventDefaultAction)
              return this;
            if (m && this.element) {
              this.elementSize = e.getElementSize(this.element), this.pageScroll = e.getPageScroll(), this.elementMargin = this.element.style.margin, this.element.style.margin = "0", this.elementPadding = this.element.style.padding, this.element.style.padding = "0", this.bodyMargin = U.margin, this.docMargin = q.margin, U.margin = "0", q.margin = "0", this.bodyPadding = U.padding, this.docPadding = q.padding, U.padding = "0", q.padding = "0", this.bodyWidth = U.width, this.docWidth = q.width, U.width = "100%", q.width = "100%", this.bodyHeight = U.height, this.docHeight = q.height, U.height = "100%", q.height = "100%", this.bodyDisplay = U.display, U.display = "block", this.previousBody = [], t[this.hash].prevElementParent = this.element.parentNode, t[this.hash].prevNextSibling = this.element.nextSibling, t[this.hash].prevElementWidth = this.element.style.width, t[this.hash].prevElementHeight = this.element.style.height, ue = P.childNodes.length;
              for (let le = 0; le < ue; le++)
                this.previousBody.push(P.childNodes[0]), P.removeChild(P.childNodes[0]);
              this.toolbar && this.toolbar.element && (this.toolbar.parentNode = this.toolbar.element.parentNode, this.toolbar.nextSibling = this.toolbar.element.nextSibling, P.appendChild(this.toolbar.element), e.addClass(this.toolbar.element, "fullpage")), e.addClass(this.element, "fullpage"), P.appendChild(this.element), this.element.style.height = "100vh", this.element.style.width = "100vw", this.toolbar && this.toolbar.element && (this.element.style.height = e.getElementSize(this.element).y - e.getElementSize(this.toolbar.element).y + "px"), t[this.hash].fullPage = !0, e.delegate(this, mt)({});
            } else {
              this.element.style.margin = this.elementMargin, this.element.style.padding = this.elementPadding, U.margin = this.bodyMargin, q.margin = this.docMargin, U.padding = this.bodyPadding, q.padding = this.docPadding, U.width = this.bodyWidth, q.width = this.docWidth, U.height = this.bodyHeight, q.height = this.docHeight, U.display = this.bodyDisplay, P.removeChild(this.element), ue = this.previousBody.length;
              for (let ge = 0; ge < ue; ge++)
                P.appendChild(this.previousBody.shift());
              e.removeClass(this.element, "fullpage"), t[this.hash].prevElementParent.insertBefore(
                this.element,
                t[this.hash].prevNextSibling
              ), this.toolbar && this.toolbar.element && (P.removeChild(this.toolbar.element), e.removeClass(this.toolbar.element, "fullpage"), this.toolbar.parentNode.insertBefore(
                this.toolbar.element,
                this.toolbar.nextSibling
              ), delete this.toolbar.parentNode, delete this.toolbar.nextSibling), this.element.style.width = t[this.hash].prevElementWidth, this.element.style.height = t[this.hash].prevElementHeight;
              let le = 0;
              const ke = function() {
                e.setPageScroll(ee.pageScroll);
                const ge = e.getPageScroll();
                le++, le < 10 && (ge.x !== ee.pageScroll.x || ge.y !== ee.pageScroll.y) && e.requestAnimationFrame(ke);
              };
              e.requestAnimationFrame(ke), t[this.hash].fullPage = !1, e.delegate(this, ot)({});
            }
            return this.navigator && this.viewport && this.navigator.update(this.viewport), this.raiseEvent("full-page", { fullPage: m }), this;
          },
          /**
           * Toggle full screen mode if supported. Toggle full page mode otherwise.
           * @function
           * @param {Boolean} fullScreen
           *      If true, enter full screen mode.  If false, exit full screen mode.
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:pre-full-screen
           * @fires OpenSeadragon.Viewer.event:full-screen
           */
          setFullScreen: function(m) {
            const P = this;
            if (!e.supportsFullScreen)
              return this.setFullPage(m);
            if (e.isFullScreen() === m)
              return this;
            const U = {
              fullScreen: m,
              preventDefaultAction: !1
            };
            if (this.raiseEvent("pre-full-screen", U), U.preventDefaultAction)
              return this;
            if (m) {
              if (this.setFullPage(!0), !this.isFullPage())
                return this;
              this.fullPageStyleWidth = this.element.style.width, this.fullPageStyleHeight = this.element.style.height, this.element.style.width = "100%", this.element.style.height = "100%";
              const q = function() {
                if (!t[P.hash]) {
                  e.removeEvent(document, e.fullScreenEventName, q), e.removeEvent(document, e.fullScreenErrorEventName, q);
                  return;
                }
                const ee = e.isFullScreen();
                ee || (e.removeEvent(document, e.fullScreenEventName, q), e.removeEvent(document, e.fullScreenErrorEventName, q), P.setFullPage(!1), P.isFullPage() && (P.element.style.width = P.fullPageStyleWidth, P.element.style.height = P.fullPageStyleHeight)), P.navigator && P.viewport && setTimeout(function() {
                  P.navigator.update(P.viewport);
                }), P.raiseEvent("full-screen", { fullScreen: ee });
              };
              e.addEvent(document, e.fullScreenEventName, q), e.addEvent(document, e.fullScreenErrorEventName, q), e.requestFullScreen(document.body);
            } else
              e.exitFullScreen();
            return this;
          },
          /**
           * @function
           * @returns {Boolean}
           */
          isVisible: function() {
            return this.container.style.visibility !== "hidden";
          },
          //
          /**
           * @function
           * @returns {Boolean} returns true if the viewer is in fullscreen
           */
          isFullScreen: function() {
            return e.isFullScreen() && this.isFullPage();
          },
          /**
           * @function
           * @param {Boolean} visible
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:visible
           */
          setVisible: function(m) {
            return this.container.style.visibility = m ? "" : "hidden", this.raiseEvent("visible", { visible: m }), this;
          },
          /**
           * Add a tiled image to the viewer.
           * options.tileSource can be anything that {@link OpenSeadragon.Viewer#open}
           *  supports except arrays of images.
           * Note that you can specify options.width or options.height, but not both.
           * The other dimension will be calculated according to the item's aspect ratio.
           * If collectionMode is on (see {@link OpenSeadragon.Options}), the new image is
           * automatically arranged with the others.
           * @function
           * @param {Object} options
           * @param {String|Object|Function} options.tileSource - The TileSource specifier.
           * A String implies a url used to determine the tileSource implementation
           *      based on the file extension of url. JSONP is implied by *.js,
           *      otherwise the url is retrieved as text and the resulting text is
           *      introspected to determine if its json, xml, or text and parsed.
           * An Object implies an inline configuration which has a single
           *      property sufficient for being able to determine tileSource
           *      implementation. If the object has a property which is a function
           *      named 'getTileUrl', it is treated as a custom TileSource.
           * @param {Number} [options.index] The index of the item. Added on top of
           * all other items if not specified.
           * @param {Boolean} [options.replace=false] If true, the item at options.index will be
           * removed and the new item is added in its place. options.tileSource will be
           * interpreted and fetched if necessary before the old item is removed to avoid leaving
           * a gap in the world.
           * @param {Number} [options.x=0] The X position for the image in viewport coordinates.
           * @param {Number} [options.y=0] The Y position for the image in viewport coordinates.
           * @param {Number} [options.width=1] The width for the image in viewport coordinates.
           * @param {Number} [options.height] The height for the image in viewport coordinates.
           * @param {OpenSeadragon.Rect} [options.fitBounds] The bounds in viewport coordinates
           * to fit the image into. If specified, x, y, width and height get ignored.
           * @param {OpenSeadragon.Placement} [options.fitBoundsPlacement=OpenSeadragon.Placement.CENTER]
           * How to anchor the image in the bounds if options.fitBounds is set.
           * @param {OpenSeadragon.Rect} [options.clip] - An area, in image pixels, to clip to
           * (portions of the image outside of this area will not be visible). Only works on
           * browsers that support the HTML5 canvas.
           * @param {Number} [options.opacity=1] Proportional opacity of the tiled images (1=opaque, 0=hidden)
           * @param {Boolean} [options.preload=false] Default switch for loading hidden images (true loads, false blocks)
           * @param {Boolean} [options.zombieCache] In the case that this method removes any TiledImage instance,
           *      allow the item-referenced cache to remain in memory even without active tiles. Default false.
           * @param {Number} [options.degrees=0] Initial rotation of the tiled image around
           * its top left corner in degrees.
           * @param {Boolean} [options.flipped=false] Whether to horizontally flip the image.
           * @param {String} [options.compositeOperation] How the image is composited onto other images.
           * @param {String} [options.crossOriginPolicy] The crossOriginPolicy for this specific image,
           * overriding viewer.crossOriginPolicy.
           * @param {Boolean} [options.ajaxWithCredentials] Whether to set withCredentials on tile AJAX
           * @param {Boolean} [options.loadTilesWithAjax]
           *      Whether to load tile data using AJAX requests.
           *      Defaults to the setting in {@link OpenSeadragon.Options}.
           * @param {Object} [options.ajaxHeaders]
           *      A set of headers to include when making tile AJAX requests.
           *      Note that these headers will be merged over any headers specified in {@link OpenSeadragon.Options}.
           *      Specifying a falsy value for a header will clear its existing value set at the Viewer level (if any).
           * @param {Function} [options.success] A function that gets called when the image is
           * successfully added. It's passed the event object which contains a single property:
           * "item", which is the resulting instance of TiledImage.
           * @param {Function} [options.error] A function that gets called if the image is
           * unable to be added. It's passed the error event object, which contains "message"
           * and "source" properties.
           * @param {Boolean} [options.collectionImmediately=false] If collectionMode is on,
           * specifies whether to snap to the new arrangement immediately or to animate to it.
           * @param {String|CanvasGradient|CanvasPattern|Function} [options.placeholderFillStyle] - See {@link OpenSeadragon.Options}.
           * @fires OpenSeadragon.World.event:add-item
           * @fires OpenSeadragon.Viewer.event:add-item-failed
           */
          addTiledImage: function(m) {
            e.console.assert(m, "[Viewer.addTiledImage] options is required"), e.console.assert(m.tileSource, "[Viewer.addTiledImage] options.tileSource is required"), e.console.assert(
              !m.replace || m.index > -1 && m.index < this.world.getItemCount(),
              "[Viewer.addTiledImage] if options.replace is used, options.index must be a valid index in Viewer.world"
            );
            const P = this;
            m.replace && (m.replaceItem = P.world.getItemAt(m.index)), this._hideMessage(), m.placeholderFillStyle === void 0 && (m.placeholderFillStyle = this.placeholderFillStyle), m.opacity === void 0 && (m.opacity = this.opacity), m.preload === void 0 && (m.preload = this.preload), m.compositeOperation === void 0 && (m.compositeOperation = this.compositeOperation), m.crossOriginPolicy === void 0 && (m.crossOriginPolicy = m.tileSource.crossOriginPolicy !== void 0 ? m.tileSource.crossOriginPolicy : this.crossOriginPolicy), m.ajaxWithCredentials === void 0 && (m.ajaxWithCredentials = this.ajaxWithCredentials), m.loadTilesWithAjax === void 0 && (m.loadTilesWithAjax = this.loadTilesWithAjax), e.isPlainObject(m.ajaxHeaders) || (m.ajaxHeaders = {});
            const U = {
              options: m
            };
            function q(X) {
              for (let le = 0; le < P._loadQueue.length; le++)
                if (P._loadQueue[le] === U) {
                  P._loadQueue.splice(le, 1);
                  break;
                }
              P._loadQueue.length === 0 && ee(U), P.raiseEvent("add-item-failed", X), m.error && m.error(X);
            }
            function ee(X) {
              P.collectionMode && (P.world.arrange({
                immediately: X.options.collectionImmediately,
                rows: P.collectionRows,
                columns: P.collectionColumns,
                layout: P.collectionLayout,
                tileSize: P.collectionTileSize,
                tileMargin: P.collectionTileMargin
              }), P.world.setAutoRefigureSizes(!0));
            }
            if (e.isArray(m.tileSource)) {
              setTimeout(function() {
                q({
                  message: "[Viewer.addTiledImage] Sequences can not be added; add them one at a time instead.",
                  source: m.tileSource,
                  options: m
                });
              });
              return;
            }
            this._loadQueue.push(U);
            function ue() {
              let X, le, ke;
              for (; P._loadQueue.length && (X = P._loadQueue[0], !!X.tileSource); ) {
                if (P._loadQueue.splice(0, 1), X.options.replace) {
                  const ge = X.options.replaceItem, Oe = P.world.getIndexOfItem(ge);
                  Oe !== -1 && (X.options.index = Oe), !ge._zombieCache && ge.source.equals(X.tileSource) && ge.allowZombieCache(!0), P.world.removeItem(ge);
                }
                le = new e.TiledImage({
                  viewer: P,
                  source: X.tileSource,
                  viewport: P.viewport,
                  drawer: P.drawer,
                  tileCache: P.tileCache,
                  imageLoader: P.imageLoader,
                  x: X.options.x,
                  y: X.options.y,
                  width: X.options.width,
                  height: X.options.height,
                  fitBounds: X.options.fitBounds,
                  fitBoundsPlacement: X.options.fitBoundsPlacement,
                  clip: X.options.clip,
                  placeholderFillStyle: X.options.placeholderFillStyle,
                  opacity: X.options.opacity,
                  preload: X.options.preload,
                  degrees: X.options.degrees,
                  flipped: X.options.flipped,
                  compositeOperation: X.options.compositeOperation,
                  springStiffness: P.springStiffness,
                  animationTime: P.animationTime,
                  minZoomImageRatio: P.minZoomImageRatio,
                  wrapHorizontal: P.wrapHorizontal,
                  wrapVertical: P.wrapVertical,
                  maxTilesPerFrame: P.maxTilesPerFrame,
                  loadDestinationTilesOnAnimation: P.loadDestinationTilesOnAnimation,
                  immediateRender: P.immediateRender,
                  blendTime: P.blendTime,
                  alwaysBlend: P.alwaysBlend,
                  minPixelRatio: P.minPixelRatio,
                  smoothTileEdgesMinZoom: P.smoothTileEdgesMinZoom,
                  iOSDevice: P.iOSDevice,
                  crossOriginPolicy: X.options.crossOriginPolicy,
                  ajaxWithCredentials: X.options.ajaxWithCredentials,
                  loadTilesWithAjax: X.options.loadTilesWithAjax,
                  ajaxHeaders: X.options.ajaxHeaders,
                  debugMode: P.debugMode,
                  subPixelRoundingForTransparency: P.subPixelRoundingForTransparency,
                  callTileLoadedWithCachedData: P.callTileLoadedWithCachedData
                }), P.collectionMode && P.world.setAutoRefigureSizes(!1), P.navigator && (ke = e.extend({}, X.options, {
                  replace: !1,
                  // navigator already removed the layer, nothing to replace
                  originalTiledImage: le,
                  tileSource: X.tileSource
                }), P.navigator.addTiledImage(ke)), P.world.addItem(le, {
                  index: X.options.index
                }), P._loadQueue.length === 0 && ee(X), P.world.getItemCount() === 1 && !P.preserveViewport && P.viewport.goHome(!0), X.options.success && X.options.success({
                  item: le
                }), P.drawer && P.drawer.tiledImageCreated(le);
              }
            }
            o(this, m.tileSource, m, function(X) {
              U.tileSource = X, ue();
            }, function(X) {
              X.options = m, q(X), ue();
            });
          },
          /**
           * Add a simple image to the viewer.
           * The options are the same as the ones in {@link OpenSeadragon.Viewer#addTiledImage}
           * except for options.tileSource which is replaced by options.url.
           * @function
           * @param {Object} options - See {@link OpenSeadragon.Viewer#addTiledImage}
           * for all the options
           * @param {String} options.url - The URL of the image to add.
           * @fires OpenSeadragon.World.event:add-item
           * @fires OpenSeadragon.Viewer.event:add-item-failed
           */
          addSimpleImage: function(m) {
            e.console.assert(m, "[Viewer.addSimpleImage] options is required"), e.console.assert(m.url, "[Viewer.addSimpleImage] options.url is required");
            const P = e.extend({}, m, {
              tileSource: {
                type: "image",
                url: m.url
              }
            });
            delete P.url, this.addTiledImage(P);
          },
          // deprecated
          addLayer: function(m) {
            const P = this;
            e.console.error("[Viewer.addLayer] this function is deprecated; use Viewer.addTiledImage() instead.");
            const U = e.extend({}, m, {
              success: function(q) {
                P.raiseEvent("add-layer", {
                  options: m,
                  drawer: q.item
                });
              },
              error: function(q) {
                P.raiseEvent("add-layer-failed", q);
              }
            });
            return this.addTiledImage(U), this;
          },
          // deprecated
          getLayerAtLevel: function(m) {
            return e.console.error("[Viewer.getLayerAtLevel] this function is deprecated; use World.getItemAt() instead."), this.world.getItemAt(m);
          },
          // deprecated
          getLevelOfLayer: function(m) {
            return e.console.error("[Viewer.getLevelOfLayer] this function is deprecated; use World.getIndexOfItem() instead."), this.world.getIndexOfItem(m);
          },
          // deprecated
          getLayersCount: function() {
            return e.console.error("[Viewer.getLayersCount] this function is deprecated; use World.getItemCount() instead."), this.world.getItemCount();
          },
          // deprecated
          setLayerLevel: function(m, P) {
            return e.console.error("[Viewer.setLayerLevel] this function is deprecated; use World.setItemIndex() instead."), this.world.setItemIndex(m, P);
          },
          // deprecated
          removeLayer: function(m) {
            return e.console.error("[Viewer.removeLayer] this function is deprecated; use World.removeItem() instead."), this.world.removeItem(m);
          },
          /**
           * Force the viewer to redraw its contents.
           * @returns {OpenSeadragon.Viewer} Chainable.
           */
          forceRedraw: function() {
            return t[this.hash].forceRedraw = !0, this;
          },
          /**
           * Force the viewer to reset its size to match its container.
           */
          forceResize: function() {
            t[this.hash].needsResize = !0, t[this.hash].forceResize = !0;
          },
          /**
           * @function
           * @returns {OpenSeadragon.Viewer} Chainable.
           */
          bindSequenceControls: function() {
            const m = e.delegate(this, x), P = e.delegate(this, T), U = e.delegate(this, this.goToNextPage), q = e.delegate(this, this.goToPreviousPage), ee = this.navImages;
            let ue = !0;
            return this.showSequenceControl && ((this.previousButton || this.nextButton) && (ue = !1), this.previousButton = new e.Button({
              element: this.previousButton ? e.getElement(this.previousButton) : null,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold,
              tooltip: e.getString("Tooltips.PreviousPage"),
              srcRest: me(this.prefixUrl, ee.previous.REST),
              srcGroup: me(this.prefixUrl, ee.previous.GROUP),
              srcHover: me(this.prefixUrl, ee.previous.HOVER),
              srcDown: me(this.prefixUrl, ee.previous.DOWN),
              onRelease: q,
              onFocus: m,
              onBlur: P
            }), this.nextButton = new e.Button({
              element: this.nextButton ? e.getElement(this.nextButton) : null,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold,
              tooltip: e.getString("Tooltips.NextPage"),
              srcRest: me(this.prefixUrl, ee.next.REST),
              srcGroup: me(this.prefixUrl, ee.next.GROUP),
              srcHover: me(this.prefixUrl, ee.next.HOVER),
              srcDown: me(this.prefixUrl, ee.next.DOWN),
              onRelease: U,
              onFocus: m,
              onBlur: P
            }), this.navPrevNextWrap || this.previousButton.disable(), (!this.tileSources || !this.tileSources.length) && this.nextButton.disable(), ue && (this.paging = new e.ButtonGroup({
              buttons: [
                this.previousButton,
                this.nextButton
              ],
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold
            }), this.pagingControl = this.paging.element, this.toolbar ? this.toolbar.addControl(
              this.pagingControl,
              { anchor: e.ControlAnchor.BOTTOM_RIGHT }
            ) : this.addControl(
              this.pagingControl,
              { anchor: this.sequenceControlAnchor || e.ControlAnchor.TOP_LEFT }
            ))), this;
          },
          /**
           * @function
           * @returns {OpenSeadragon.Viewer} Chainable.
           */
          bindStandardControls: function() {
            const m = e.delegate(this, this.startZoomInAction), P = e.delegate(this, this.endZoomAction), U = e.delegate(this, this.singleZoomInAction), q = e.delegate(this, this.startZoomOutAction), ee = e.delegate(this, this.singleZoomOutAction), ue = e.delegate(this, ei), X = e.delegate(this, It), le = e.delegate(this, Pe), ke = e.delegate(this, Ct), ge = e.delegate(this, qi), Oe = e.delegate(this, x), Ue = e.delegate(this, T), ve = this.navImages, Ke = [];
            let Tt = !0;
            return this.showNavigationControl && ((this.zoomInButton || this.zoomOutButton || this.homeButton || this.fullPageButton || this.rotateLeftButton || this.rotateRightButton || this.flipButton) && (Tt = !1), this.showZoomControl && (Ke.push(this.zoomInButton = new e.Button({
              element: this.zoomInButton ? e.getElement(this.zoomInButton) : null,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold,
              tooltip: e.getString("Tooltips.ZoomIn"),
              srcRest: me(this.prefixUrl, ve.zoomIn.REST),
              srcGroup: me(this.prefixUrl, ve.zoomIn.GROUP),
              srcHover: me(this.prefixUrl, ve.zoomIn.HOVER),
              srcDown: me(this.prefixUrl, ve.zoomIn.DOWN),
              onPress: m,
              onRelease: P,
              onClick: U,
              onEnter: m,
              onExit: P,
              onFocus: Oe,
              onBlur: Ue
            })), Ke.push(this.zoomOutButton = new e.Button({
              element: this.zoomOutButton ? e.getElement(this.zoomOutButton) : null,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold,
              tooltip: e.getString("Tooltips.ZoomOut"),
              srcRest: me(this.prefixUrl, ve.zoomOut.REST),
              srcGroup: me(this.prefixUrl, ve.zoomOut.GROUP),
              srcHover: me(this.prefixUrl, ve.zoomOut.HOVER),
              srcDown: me(this.prefixUrl, ve.zoomOut.DOWN),
              onPress: q,
              onRelease: P,
              onClick: ee,
              onEnter: q,
              onExit: P,
              onFocus: Oe,
              onBlur: Ue
            }))), this.showHomeControl && Ke.push(this.homeButton = new e.Button({
              element: this.homeButton ? e.getElement(this.homeButton) : null,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold,
              tooltip: e.getString("Tooltips.Home"),
              srcRest: me(this.prefixUrl, ve.home.REST),
              srcGroup: me(this.prefixUrl, ve.home.GROUP),
              srcHover: me(this.prefixUrl, ve.home.HOVER),
              srcDown: me(this.prefixUrl, ve.home.DOWN),
              onRelease: ue,
              onFocus: Oe,
              onBlur: Ue
            })), this.showFullPageControl && Ke.push(this.fullPageButton = new e.Button({
              element: this.fullPageButton ? e.getElement(this.fullPageButton) : null,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold,
              tooltip: e.getString("Tooltips.FullPage"),
              srcRest: me(this.prefixUrl, ve.fullpage.REST),
              srcGroup: me(this.prefixUrl, ve.fullpage.GROUP),
              srcHover: me(this.prefixUrl, ve.fullpage.HOVER),
              srcDown: me(this.prefixUrl, ve.fullpage.DOWN),
              onRelease: X,
              onFocus: Oe,
              onBlur: Ue
            })), this.showRotationControl && (Ke.push(this.rotateLeftButton = new e.Button({
              element: this.rotateLeftButton ? e.getElement(this.rotateLeftButton) : null,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold,
              tooltip: e.getString("Tooltips.RotateLeft"),
              srcRest: me(this.prefixUrl, ve.rotateleft.REST),
              srcGroup: me(this.prefixUrl, ve.rotateleft.GROUP),
              srcHover: me(this.prefixUrl, ve.rotateleft.HOVER),
              srcDown: me(this.prefixUrl, ve.rotateleft.DOWN),
              onRelease: le,
              onFocus: Oe,
              onBlur: Ue
            })), Ke.push(this.rotateRightButton = new e.Button({
              element: this.rotateRightButton ? e.getElement(this.rotateRightButton) : null,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold,
              tooltip: e.getString("Tooltips.RotateRight"),
              srcRest: me(this.prefixUrl, ve.rotateright.REST),
              srcGroup: me(this.prefixUrl, ve.rotateright.GROUP),
              srcHover: me(this.prefixUrl, ve.rotateright.HOVER),
              srcDown: me(this.prefixUrl, ve.rotateright.DOWN),
              onRelease: ke,
              onFocus: Oe,
              onBlur: Ue
            }))), this.showFlipControl && Ke.push(this.flipButton = new e.Button({
              element: this.flipButton ? e.getElement(this.flipButton) : null,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold,
              tooltip: e.getString("Tooltips.Flip"),
              srcRest: me(this.prefixUrl, ve.flip.REST),
              srcGroup: me(this.prefixUrl, ve.flip.GROUP),
              srcHover: me(this.prefixUrl, ve.flip.HOVER),
              srcDown: me(this.prefixUrl, ve.flip.DOWN),
              onRelease: ge,
              onFocus: Oe,
              onBlur: Ue
            })), Tt ? (this.buttonGroup = new e.ButtonGroup({
              buttons: Ke,
              clickTimeThreshold: this.clickTimeThreshold,
              clickDistThreshold: this.clickDistThreshold
            }), this.navControl = this.buttonGroup.element, this.addHandler("open", e.delegate(this, pe)), this.toolbar ? this.toolbar.addControl(
              this.navControl,
              { anchor: this.navigationControlAnchor || e.ControlAnchor.TOP_LEFT }
            ) : this.addControl(
              this.navControl,
              { anchor: this.navigationControlAnchor || e.ControlAnchor.TOP_LEFT }
            )) : this.customButtons = Ke), this;
          },
          /**
           * Gets the active page of a sequence
           * @function
           * @returns {Number}
           */
          currentPage: function() {
            return this._sequenceIndex;
          },
          /**
           * @function
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:page
           */
          goToPage: function(m) {
            return this.tileSources && m >= 0 && m < this.tileSources.length && (this._sequenceIndex = m, this._updateSequenceButtons(m), this.open(this.tileSources[m]), this.referenceStrip && this.referenceStrip.setFocus(m), this.raiseEvent("page", { page: m })), this;
          },
          /**
            * Adds an html element as an overlay to the current viewport.  Useful for
            * highlighting words or areas of interest on an image or other zoomable
            * interface. Unless the viewer has been configured with the preserveOverlays
            * option, overlays added via this method are removed when the viewport
            * is closed (including in sequence mode when changing page).
            * @method
            * @param {Element|String|Object} element - A reference to an element or an id for
            *      the element which will be overlaid. Or an Object specifying the configuration for the overlay.
            *      If using an object, see {@link OpenSeadragon.Overlay} for a list of
            *      all available options.
            * @param {OpenSeadragon.Point|OpenSeadragon.Rect} location - The point or
            *      rectangle which will be overlaid. This is a viewport relative location.
            * @param {OpenSeadragon.Placement} [placement=OpenSeadragon.Placement.TOP_LEFT] - The position of the
            *      viewport which the location coordinates will be treated as relative
            *      to.
            * @param {function} [onDraw] - If supplied the callback is called when the overlay
            *      needs to be drawn. It is the responsibility of the callback to do any drawing/positioning.
            *      It is passed position, size and element.
            * @returns {OpenSeadragon.Viewer} Chainable.
            * @fires OpenSeadragon.Viewer.event:add-overlay
            */
          addOverlay: function(m, P, U, q) {
            let ee;
            if (e.isPlainObject(m) ? ee = m : ee = {
              element: m,
              location: P,
              placement: U,
              onDraw: q
            }, m = e.getElement(ee.element), a(this.currentOverlays, m) >= 0)
              return this;
            const ue = l(this, ee);
            return this.currentOverlays.push(ue), ue.drawHTML(this.overlaysContainer, this.viewport), this.raiseEvent("add-overlay", {
              element: m,
              location: ee.location,
              placement: ee.placement
            }), this;
          },
          /**
           * Updates the overlay represented by the reference to the element or
           * element id moving it to the new location, relative to the new placement.
           * @method
           * @param {Element|String} element - A reference to an element or an id for
           *      the element which is overlaid.
           * @param {OpenSeadragon.Point|OpenSeadragon.Rect} location - The point or
           *      rectangle which will be overlaid. This is a viewport relative location.
           * @param {OpenSeadragon.Placement} [placement=OpenSeadragon.Placement.TOP_LEFT] - The position of the
           *      viewport which the location coordinates will be treated as relative
           *      to.
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:update-overlay
           */
          updateOverlay: function(m, P, U) {
            m = e.getElement(m);
            const q = a(this.currentOverlays, m);
            return q >= 0 && (this.currentOverlays[q].update(P, U), t[this.hash].forceRedraw = !0, this.raiseEvent("update-overlay", {
              element: m,
              location: P,
              placement: U
            })), this;
          },
          /**
           * Removes an overlay identified by the reference element or element id
           * and schedules an update.
           * @method
           * @param {Element|String} element - A reference to the element or an
           *      element id which represent the ovelay content to be removed.
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:remove-overlay
           */
          removeOverlay: function(m) {
            m = e.getElement(m);
            const P = a(this.currentOverlays, m);
            return P >= 0 && (this.currentOverlays[P].destroy(), this.currentOverlays.splice(P, 1), t[this.hash].forceRedraw = !0, this.raiseEvent("remove-overlay", {
              element: m
            })), this;
          },
          /**
           * Removes all currently configured Overlays from this Viewer and schedules
           * an update.
           * @method
           * @returns {OpenSeadragon.Viewer} Chainable.
           * @fires OpenSeadragon.Viewer.event:clear-overlay
           */
          clearOverlays: function() {
            for (; this.currentOverlays.length > 0; )
              this.currentOverlays.pop().destroy();
            return t[this.hash].forceRedraw = !0, this.raiseEvent("clear-overlay", {}), this;
          },
          /**
          * Finds an overlay identified by the reference element or element id
          * and returns it as an object, return null if not found.
          * @method
          * @param {Element|String} element - A reference to the element or an
          *      element id which represents the overlay content.
          * @returns {OpenSeadragon.Overlay} the matching overlay or null if none found.
          */
          getOverlayById: function(m) {
            m = e.getElement(m);
            const P = a(this.currentOverlays, m);
            return P >= 0 ? this.currentOverlays[P] : null;
          },
          /**
           * Updates the sequence buttons.
           * @function OpenSeadragon.Viewer.prototype._updateSequenceButtons
           * @private
           * @param {Number} Sequence Value
           */
          _updateSequenceButtons: function(m) {
            this.nextButton && (!this.tileSources || this.tileSources.length - 1 === m ? this.navPrevNextWrap || this.nextButton.disable() : this.nextButton.enable()), this.previousButton && (m > 0 ? this.previousButton.enable() : this.navPrevNextWrap || this.previousButton.disable());
          },
          /**
           * Display a message in the viewport
           * @function OpenSeadragon.Viewer.prototype._showMessage
           * @private
           * @param {String} text message
           */
          _showMessage: function(m) {
            this._hideMessage();
            const P = e.makeNeutralElement("div");
            P.appendChild(document.createTextNode(m)), this.messageDiv = e.makeCenteredNode(P), e.addClass(this.messageDiv, "openseadragon-message"), this.container.appendChild(this.messageDiv);
          },
          /**
           * Hide any currently displayed viewport message
           * @function OpenSeadragon.Viewer.prototype._hideMessage
           * @private
           */
          _hideMessage: function() {
            const m = this.messageDiv;
            m && (m.parentNode.removeChild(m), delete this.messageDiv);
          },
          /**
           * Gets this viewer's gesture settings for the given pointer device type.
           * @method
           * @param {String} type - The pointer device type to get the gesture settings for ("mouse", "touch", "pen", etc.).
           * @returns {OpenSeadragon.GestureSettings}
           */
          gestureSettingsByDeviceType: function(m) {
            switch (m) {
              case "mouse":
                return this.gestureSettingsMouse;
              case "touch":
                return this.gestureSettingsTouch;
              case "pen":
                return this.gestureSettingsPen;
              default:
                return this.gestureSettingsUnknown;
            }
          },
          // private
          _drawOverlays: function() {
            const m = this.currentOverlays.length;
            for (let P = 0; P < m; P++)
              this.currentOverlays[P].drawHTML(this.overlaysContainer, this.viewport);
          },
          /**
           * Cancel the "in flight" images.
           */
          _cancelPendingImages: function() {
            this._loadQueue = [];
          },
          /**
           * Removes the reference strip and disables displaying it.
           * @function
           */
          removeReferenceStrip: function() {
            this.showReferenceStrip = !1, this.referenceStrip && (this.referenceStrip.destroy(), this.referenceStrip = null);
          },
          /**
           * Enables and displays the reference strip based on the currently set tileSources.
           * Works only when the Viewer has sequenceMode set to true.
           * @function
           */
          addReferenceStrip: function() {
            if (this.showReferenceStrip = !0, this.sequenceMode) {
              if (this.referenceStrip)
                return;
              this.tileSources.length && this.tileSources.length > 1 && (this.referenceStrip = new e.ReferenceStrip({
                id: this.referenceStripElement,
                position: this.referenceStripPosition,
                sizeRatio: this.referenceStripSizeRatio,
                scroll: this.referenceStripScroll,
                height: this.referenceStripHeight,
                width: this.referenceStripWidth,
                tileSources: this.tileSources,
                prefixUrl: this.prefixUrl,
                viewer: this
              }), this.referenceStrip.setFocus(this._sequenceIndex));
            } else
              e.console.warn('Attempting to display a reference strip while "sequenceMode" is off.');
          },
          /**
           * Adds _updatePixelDensityRatio to the window resize event.
           * @private
           */
          _addUpdatePixelDensityRatioEvent: function() {
            this._updatePixelDensityRatioBind = this._updatePixelDensityRatio.bind(this), e.addEvent(window, "resize", this._updatePixelDensityRatioBind);
          },
          /**
           * Removes _updatePixelDensityRatio from the window resize event.
           * @private
           */
          _removeUpdatePixelDensityRatioEvent: function() {
            e.removeEvent(window, "resize", this._updatePixelDensityRatioBind);
          },
          /**
           * Update pixel density ratio and forces a resize operation.
           * @private
           */
          _updatePixelDensityRatio: function() {
            const m = e.pixelDensityRatio, P = e.getCurrentPixelDensityRatio();
            m !== P && (e.pixelDensityRatio = P, this.forceResize());
          },
          /**
           * Sets the image source to the source with index equal to
           * currentIndex - 1. Changes current image in sequence mode.
           * If specified, wraps around (see navPrevNextWrap in
           * {@link OpenSeadragon.Options})
           *
           * @method
           */
          goToPreviousPage: function() {
            let m = this._sequenceIndex - 1;
            this.navPrevNextWrap && m < 0 && (m += this.tileSources.length), this.goToPage(m);
          },
          /**
           * Sets the image source to the source with index equal to
           * currentIndex + 1. Changes current image in sequence mode.
           * If specified, wraps around (see navPrevNextWrap in
           * {@link OpenSeadragon.Options})
           *
           * @method
           */
          goToNextPage: function() {
            let m = this._sequenceIndex + 1;
            this.navPrevNextWrap && m >= this.tileSources.length && (m = 0), this.goToPage(m);
          },
          isAnimating: function() {
            return t[this.hash].animating;
          },
          /**
           * Starts continuous zoom-in animation (typically bound to mouse-down on the zoom-in button).
           * @function
           * @memberof OpenSeadragon.Viewer.prototype
           */
          startZoomInAction: function() {
            t[this.hash].lastZoomTime = e.now(), t[this.hash].zoomFactor = this.zoomPerSecond, t[this.hash].zooming = !0, xt(this);
          },
          /**
           * Starts continuous zoom-out animation (typically bound to mouse-down on the zoom-out button).
           * @function
           * @memberof OpenSeadragon.Viewer.prototype
           */
          startZoomOutAction: function() {
            t[this.hash].lastZoomTime = e.now(), t[this.hash].zoomFactor = 1 / this.zoomPerSecond, t[this.hash].zooming = !0, xt(this);
          },
          /**
           * Stops any continuous zoom animation (typically bound to mouse-up/leave events on a button).
           * @function
           * @memberof OpenSeadragon.Viewer.prototype
           */
          endZoomAction: function() {
            t[this.hash].zooming = !1;
          },
          /**
           * Performs single-step zoom-in operation (typically bound to click/enter on the zoom-in button).
           * @function
           * @memberof OpenSeadragon.Viewer.prototype
           */
          singleZoomInAction: function() {
            this.viewport && (t[this.hash].zooming = !1, this.viewport.zoomBy(
              this.zoomPerClick / 1
            ), this.viewport.applyConstraints());
          },
          /**
           * Performs single-step zoom-out operation (typically bound to click/enter on the zoom-out button).
           * @function
           * @memberof OpenSeadragon.Viewer.prototype
           */
          singleZoomOutAction: function() {
            this.viewport && (t[this.hash].zooming = !1, this.viewport.zoomBy(
              1 / this.zoomPerClick
            ), this.viewport.applyConstraints());
          }
        }
      );
      function r(m) {
        return m = e.getElement(m), new e.Point(
          m.clientWidth === 0 ? 1 : m.clientWidth,
          m.clientHeight === 0 ? 1 : m.clientHeight
        );
      }
      function o(m, P, U, q, ee) {
        const ue = m;
        if (e.type(P) === "string") {
          if (P.match(/^\s*<.*>\s*$/))
            P = e.parseXml(P);
          else if (P.match(/^\s*[{[].*[}\]]\s*$/))
            try {
              P = e.parseJSON(P);
            } catch {
            }
        }
        function X(le, ke) {
          le.ready ? q(le) : (le.addHandler("ready", function() {
            q(le);
          }), le.addHandler("open-failed", function(ge) {
            ee({
              message: ge.message,
              source: ke
            });
          }));
        }
        setTimeout(function() {
          if (e.type(P) === "string")
            P = new e.TileSource({
              url: P,
              crossOriginPolicy: U.crossOriginPolicy !== void 0 ? U.crossOriginPolicy : m.crossOriginPolicy,
              ajaxWithCredentials: m.ajaxWithCredentials,
              ajaxHeaders: e.extend({}, m.ajaxHeaders, U.ajaxHeaders),
              splitHashDataForPost: m.splitHashDataForPost,
              success: function(le) {
                q(le.tileSource);
              }
            }), P.addHandler("open-failed", function(le) {
              ee(le);
            });
          else if (e.isPlainObject(P) || P.nodeType)
            if (P.crossOriginPolicy === void 0 && (U.crossOriginPolicy !== void 0 || m.crossOriginPolicy !== void 0) && (P.crossOriginPolicy = U.crossOriginPolicy !== void 0 ? U.crossOriginPolicy : m.crossOriginPolicy), P.ajaxWithCredentials === void 0 && (P.ajaxWithCredentials = m.ajaxWithCredentials), P.loadTilesWithAjax === void 0 && (P.loadTilesWithAjax = m.loadTilesWithAjax), P.ajaxHeaders === void 0 && (P.ajaxHeaders = m.ajaxHeaders), e.isFunction(P.getTileUrl)) {
              const le = new e.TileSource(P);
              le.getTileUrl = P.getTileUrl, q(le);
            } else {
              const le = e.TileSource.determineType(ue, P);
              if (!le) {
                ee({
                  message: "Unable to load TileSource",
                  source: P
                });
                return;
              }
              const ke = le.prototype.configure.apply(ue, [P]);
              X(new le(ke), P);
            }
          else
            X(P, P);
        });
      }
      function l(m, P) {
        if (P instanceof e.Overlay)
          return P;
        let U = null;
        if (P.element)
          U = e.getElement(P.element);
        else {
          const le = P.id ? P.id : "openseadragon-overlay-" + Math.floor(Math.random() * 1e7);
          U = e.getElement(P.id), U || (U = document.createElement("a"), U.href = "#/overlay/" + le), U.id = le, e.addClass(
            U,
            P.className ? P.className : "openseadragon-overlay"
          );
        }
        let q = P.location, ee = P.width, ue = P.height;
        if (!q) {
          let le = P.x, ke = P.y;
          if (P.px !== void 0) {
            const ge = m.viewport.imageToViewportRectangle(new e.Rect(
              P.px,
              P.py,
              ee || 0,
              ue || 0
            ));
            le = ge.x, ke = ge.y, ee = ee !== void 0 ? ge.width : void 0, ue = ue !== void 0 ? ge.height : void 0;
          }
          q = new e.Point(le, ke);
        }
        let X = P.placement;
        return X && e.type(X) === "string" && (X = e.Placement[P.placement.toUpperCase()]), new e.Overlay({
          element: U,
          location: q,
          placement: X,
          onDraw: P.onDraw,
          checkResize: P.checkResize,
          width: ee,
          height: ue,
          rotationMode: P.rotationMode
        });
      }
      function a(m, P) {
        for (let U = m.length - 1; U >= 0; U--)
          if (m[U].element === P)
            return U;
        return -1;
      }
      function c(m, P) {
        return e.requestAnimationFrame(function() {
          P(m);
        });
      }
      function d(m) {
        e.requestAnimationFrame(function() {
          p(m);
        });
      }
      function f(m) {
        m.autoHideControls && (m.controlsShouldFade = !0, m.controlsFadeBeginTime = e.now() + m.controlsFadeDelay, window.setTimeout(function() {
          d(m);
        }, m.controlsFadeDelay));
      }
      function p(m) {
        if (m.controlsShouldFade) {
          let q = 1 - (e.now() - m.controlsFadeBeginTime) / m.controlsFadeLength;
          q = Math.min(1, q), q = Math.max(0, q);
          for (let ee = m.controls.length - 1; ee >= 0; ee--)
            m.controls[ee].autoFade && m.controls[ee].setOpacity(q);
          q > 0 && d(m);
        }
      }
      function b(m) {
        m.controlsShouldFade = !1;
        for (let P = m.controls.length - 1; P >= 0; P--)
          m.controls[P].setOpacity(1);
      }
      function x() {
        b(this);
      }
      function T() {
        f(this);
      }
      function I(m) {
        const P = {
          tracker: m.eventSource,
          position: m.position,
          originalEvent: m.originalEvent,
          preventDefault: m.preventDefault
        };
        this.raiseEvent("canvas-contextmenu", P), m.preventDefault = P.preventDefault;
      }
      function z(m, P) {
        switch (m) {
          case "ArrowUp":
          case "KeyW":
            return P ? "zoomIn" : "panUp";
          case "ArrowDown":
          case "KeyS":
            return P ? "zoomOut" : "panDown";
          case "ArrowLeft":
          case "KeyA":
            return "panLeft";
          case "ArrowRight":
          case "KeyD":
            return "panRight";
          case "Equal":
            return "zoomIn";
          case "Minus":
            return "zoomOut";
          default:
            return null;
        }
      }
      function W(m) {
        const P = (q, ee) => {
          const ue = z(q, ee);
          ue && this._activeActions[ue] && (this._activeActions[ue] = !1, this._navActionFrames[ue] < this._minNavActionFrames && (this._navActionVirtuallyHeld[ue] = !0));
        }, U = m.originalEvent.code;
        P(U, !0), P(U, !1);
      }
      function j(m) {
        const P = {
          originalEvent: m.originalEvent,
          preventDefaultAction: !this.keyboardNavEnabled,
          preventVerticalPan: m.preventVerticalPan || !this.panVertical,
          preventHorizontalPan: m.preventHorizontalPan || !this.panHorizontal
        };
        if (this.raiseEvent("canvas-key", P), !P.preventDefaultAction && !m.ctrl && !m.alt && !m.meta) {
          const U = m.originalEvent.code, q = m.shift, ee = z(U, q);
          if (ee && !this._activeActions[ee]) {
            this._activeActions[ee] = !0, this._navActionFrames[ee] = 0, m.preventDefault = !0;
            return;
          }
          switch (m.keyCode) {
            case 48:
              this.viewport.goHome(), this.viewport.applyConstraints(), m.preventDefault = !0;
              break;
            case 82:
              m.shift ? this.viewport.flipped ? this.viewport.setRotation(this.viewport.getRotation() + this.rotationIncrement) : this.viewport.setRotation(this.viewport.getRotation() - this.rotationIncrement) : this.viewport.flipped ? this.viewport.setRotation(this.viewport.getRotation() - this.rotationIncrement) : this.viewport.setRotation(this.viewport.getRotation() + this.rotationIncrement), this.viewport.applyConstraints(), m.preventDefault = !0;
              break;
            case 70:
              this.viewport.toggleFlip(), m.preventDefault = !0;
              break;
            case 74:
              this.goToPreviousPage();
              break;
            case 75:
              this.goToNextPage();
              break;
            default:
              m.preventDefault = !1;
              break;
          }
        } else
          m.preventDefault = !1;
      }
      function oe(m) {
        const P = {
          originalEvent: m.originalEvent
        };
        this.raiseEvent("canvas-key-press", P);
      }
      function ae(m) {
        let P;
        document.activeElement === this.canvas || this.canvas.focus(), this.viewport.flipped && (m.position.x = this.viewport.getContainerSize().x - m.position.x);
        const q = {
          tracker: m.eventSource,
          position: m.position,
          quick: m.quick,
          shift: m.shift,
          originalEvent: m.originalEvent,
          originalTarget: m.originalTarget,
          preventDefaultAction: !1
        };
        this.raiseEvent("canvas-click", q), !q.preventDefaultAction && this.viewport && m.quick && (P = this.gestureSettingsByDeviceType(m.pointerType), P.clickToZoom === !0 && (this.viewport.zoomBy(
          m.shift ? 1 / this.zoomPerClick : this.zoomPerClick,
          P.zoomToRefPoint ? this.viewport.pointFromPixel(m.position, !0) : null
        ), this.viewport.applyConstraints()), P.dblClickDragToZoom && (t[this.hash].draggingToZoom === !0 ? (t[this.hash].lastClickTime = null, t[this.hash].draggingToZoom = !1) : t[this.hash].lastClickTime = e.now()));
      }
      function se(m) {
        let P;
        const U = {
          tracker: m.eventSource,
          position: m.position,
          shift: m.shift,
          originalEvent: m.originalEvent,
          preventDefaultAction: !1
        };
        this.raiseEvent("canvas-double-click", U), !U.preventDefaultAction && this.viewport && (P = this.gestureSettingsByDeviceType(m.pointerType), P.dblClickToZoom && (this.viewport.zoomBy(
          m.shift ? 1 / this.zoomPerClick : this.zoomPerClick,
          P.zoomToRefPoint ? this.viewport.pointFromPixel(m.position, !0) : null
        ), this.viewport.applyConstraints()));
      }
      function fe(m) {
        let P;
        const U = {
          tracker: m.eventSource,
          pointerType: m.pointerType,
          position: m.position,
          delta: m.delta,
          speed: m.speed,
          direction: m.direction,
          shift: m.shift,
          originalEvent: m.originalEvent,
          preventDefaultAction: !1
        };
        if (this.raiseEvent("canvas-drag", U), P = this.gestureSettingsByDeviceType(m.pointerType), !U.preventDefaultAction && this.viewport) {
          if (P.dblClickDragToZoom && t[this.hash].draggingToZoom) {
            const q = Math.pow(this.zoomPerDblClickDrag, m.delta.y / 50);
            this.viewport.zoomBy(q);
          } else if (P.dragToPan && !t[this.hash].draggingToZoom) {
            if (this.panHorizontal || (m.delta.x = 0), this.panVertical || (m.delta.y = 0), this.viewport.flipped && (m.delta.x = -m.delta.x), this.constrainDuringPan) {
              const q = this.viewport.deltaPointsFromPixels(m.delta.negate());
              this.viewport.centerSpringX.target.value += q.x, this.viewport.centerSpringY.target.value += q.y;
              const ee = this.viewport.getConstrainedBounds();
              this.viewport.centerSpringX.target.value -= q.x, this.viewport.centerSpringY.target.value -= q.y, ee.xConstrained && (m.delta.x = 0), ee.yConstrained && (m.delta.y = 0);
            }
            this.viewport.panBy(this.viewport.deltaPointsFromPixels(m.delta.negate()), P.flickEnabled && !this.constrainDuringPan);
          }
        }
      }
      function Ie(m) {
        let P;
        const U = {
          tracker: m.eventSource,
          pointerType: m.pointerType,
          position: m.position,
          speed: m.speed,
          direction: m.direction,
          shift: m.shift,
          originalEvent: m.originalEvent,
          preventDefaultAction: !1
        };
        if (this.raiseEvent("canvas-drag-end", U), P = this.gestureSettingsByDeviceType(m.pointerType), !U.preventDefaultAction && this.viewport) {
          if (!t[this.hash].draggingToZoom && P.dragToPan && P.flickEnabled && m.speed >= P.flickMinSpeed) {
            let q = 0;
            this.panHorizontal && (q = P.flickMomentum * m.speed * Math.cos(m.direction));
            let ee = 0;
            this.panVertical && (ee = P.flickMomentum * m.speed * Math.sin(m.direction));
            const ue = this.viewport.pixelFromPoint(
              this.viewport.getCenter(!0)
            ), X = this.viewport.pointFromPixel(
              new e.Point(ue.x - q, ue.y - ee)
            );
            this.viewport.panTo(X, !1);
          }
          this.viewport.applyConstraints();
        }
        P.dblClickDragToZoom && t[this.hash].draggingToZoom === !0 && (t[this.hash].draggingToZoom = !1);
      }
      function Ae(m) {
        this.raiseEvent("canvas-enter", {
          tracker: m.eventSource,
          pointerType: m.pointerType,
          position: m.position,
          buttons: m.buttons,
          pointers: m.pointers,
          insideElementPressed: m.insideElementPressed,
          buttonDownAny: m.buttonDownAny,
          originalEvent: m.originalEvent
        });
      }
      function qe(m) {
        this.raiseEvent("canvas-exit", {
          tracker: m.eventSource,
          pointerType: m.pointerType,
          position: m.position,
          buttons: m.buttons,
          pointers: m.pointers,
          insideElementPressed: m.insideElementPressed,
          buttonDownAny: m.buttonDownAny,
          originalEvent: m.originalEvent
        });
      }
      function Ne(m) {
        if (this.raiseEvent("canvas-press", {
          tracker: m.eventSource,
          pointerType: m.pointerType,
          position: m.position,
          insideElementPressed: m.insideElementPressed,
          insideElementReleased: m.insideElementReleased,
          originalEvent: m.originalEvent
        }), this.gestureSettingsByDeviceType(m.pointerType).dblClickDragToZoom) {
          const U = t[this.hash].lastClickTime, q = e.now();
          if (U === null)
            return;
          q - U < this.dblClickTimeThreshold && (t[this.hash].draggingToZoom = !0), t[this.hash].lastClickTime = null;
        }
      }
      function Fe(m) {
        this.raiseEvent("canvas-release", {
          tracker: m.eventSource,
          pointerType: m.pointerType,
          position: m.position,
          insideElementPressed: m.insideElementPressed,
          insideElementReleased: m.insideElementReleased,
          originalEvent: m.originalEvent
        });
      }
      function De(m) {
        this.raiseEvent("canvas-nonprimary-press", {
          tracker: m.eventSource,
          position: m.position,
          pointerType: m.pointerType,
          button: m.button,
          buttons: m.buttons,
          originalEvent: m.originalEvent
        });
      }
      function wt(m) {
        this.raiseEvent("canvas-nonprimary-release", {
          tracker: m.eventSource,
          position: m.position,
          pointerType: m.pointerType,
          button: m.button,
          buttons: m.buttons,
          originalEvent: m.originalEvent
        });
      }
      function lt(m) {
        let P, U, q;
        const ee = {
          tracker: m.eventSource,
          pointerType: m.pointerType,
          gesturePoints: m.gesturePoints,
          lastCenter: m.lastCenter,
          center: m.center,
          lastDistance: m.lastDistance,
          distance: m.distance,
          shift: m.shift,
          originalEvent: m.originalEvent,
          preventDefaultPanAction: !1,
          preventDefaultZoomAction: !1,
          preventDefaultRotateAction: !1
        };
        if (this.raiseEvent("canvas-pinch", ee), this.viewport) {
          let ue = this.gestureSettingsByDeviceType(m.pointerType);
          if (ue.pinchToZoom && (!ee.preventDefaultPanAction || !ee.preventDefaultZoomAction) && (P = this.viewport.pointFromPixel(m.center, !0), ue.zoomToRefPoint && !ee.preventDefaultPanAction && (U = this.viewport.pointFromPixel(m.lastCenter, !0), q = U.minus(P), this.panHorizontal || (q.x = 0), this.panVertical || (q.y = 0), this.viewport.panBy(q, !0)), ee.preventDefaultZoomAction || this.viewport.zoomBy(m.distance / m.lastDistance, P, !0), this.viewport.applyConstraints()), ue.pinchRotate && !ee.preventDefaultRotateAction) {
            const X = Math.atan2(
              m.gesturePoints[0].currentPos.y - m.gesturePoints[1].currentPos.y,
              m.gesturePoints[0].currentPos.x - m.gesturePoints[1].currentPos.x
            ), le = Math.atan2(
              m.gesturePoints[0].lastPos.y - m.gesturePoints[1].lastPos.y,
              m.gesturePoints[0].lastPos.x - m.gesturePoints[1].lastPos.x
            );
            P = this.viewport.pointFromPixel(m.center, !0), this.viewport.rotateTo(this.viewport.getRotation(!0) + (X - le) * (180 / Math.PI), P, !0);
          }
        }
      }
      function ct(m) {
        this.raiseEvent("canvas-focus", {
          tracker: m.eventSource,
          originalEvent: m.originalEvent
        });
      }
      function pt(m) {
        for (const P in this._activeActions)
          this._activeActions[P] = !1;
        for (const P in this._navActionVirtuallyHeld)
          this._navActionVirtuallyHeld[P] = !1;
        this.raiseEvent("canvas-blur", {
          tracker: m.eventSource,
          originalEvent: m.originalEvent
        });
      }
      function ht(m) {
        let P, U, q;
        const ee = e.now();
        ee - this._lastScrollTime > this.minScrollDeltaTime ? (this._lastScrollTime = ee, P = {
          tracker: m.eventSource,
          position: m.position,
          scroll: m.scroll,
          shift: m.shift,
          originalEvent: m.originalEvent,
          preventDefaultAction: !1,
          preventDefault: !0
        }, this.raiseEvent("canvas-scroll", P), !P.preventDefaultAction && this.viewport && (this.viewport.flipped && (m.position.x = this.viewport.getContainerSize().x - m.position.x), U = this.gestureSettingsByDeviceType(m.pointerType), U.scrollToZoom && (q = Math.pow(this.zoomPerScroll, m.scroll), this.viewport.zoomBy(
          q,
          U.zoomToRefPoint ? this.viewport.pointFromPixel(m.position, !0) : null
        ), this.viewport.applyConstraints())), m.preventDefault = P.preventDefault) : m.preventDefault = !0;
      }
      function mt(m) {
        t[this.hash].mouseInside = !0, b(this), this.raiseEvent("container-enter", {
          tracker: m.eventSource,
          pointerType: m.pointerType,
          position: m.position,
          buttons: m.buttons,
          pointers: m.pointers,
          insideElementPressed: m.insideElementPressed,
          buttonDownAny: m.buttonDownAny,
          originalEvent: m.originalEvent
        });
      }
      function ot(m) {
        m.pointers < 1 && (t[this.hash].mouseInside = !1, t[this.hash].animating || f(this)), this.raiseEvent("container-exit", {
          tracker: m.eventSource,
          pointerType: m.pointerType,
          position: m.position,
          buttons: m.buttons,
          pointers: m.pointers,
          insideElementPressed: m.insideElementPressed,
          buttonDownAny: m.buttonDownAny,
          originalEvent: m.originalEvent
        });
      }
      function Bt(m) {
        $t(m), m.isOpen() ? m._updateRequestId = c(m, Bt) : m._updateRequestId = !1;
      }
      function Gi(m, P) {
        const U = m.viewport, q = U.getZoom(), ee = U.getCenter();
        U.resize(P, m.preserveImageSizeOnResize), U.panTo(ee, !0);
        let ue;
        if (m.preserveImageSizeOnResize)
          ue = t[m.hash].prevContainerSize.x / P.x;
        else {
          const X = new e.Point(0, 0), le = new e.Point(t[m.hash].prevContainerSize.x, t[m.hash].prevContainerSize.y).distanceTo(X);
          ue = new e.Point(P.x, P.y).distanceTo(X) / le * t[m.hash].prevContainerSize.x / P.x;
        }
        U.zoomTo(q * ue, null, !0), t[m.hash].prevContainerSize = P, t[m.hash].forceRedraw = !0, t[m.hash].needsResize = !1, t[m.hash].forceResize = !1;
      }
      function _t(m) {
        for (const X in m._activeActions)
          (m._activeActions[X] || m._navActionVirtuallyHeld[X]) && (m._navActionFrames[X]++, m._navActionFrames[X] >= m._minNavActionFrames && (m._navActionVirtuallyHeld[X] = !1));
        function P(X) {
          return m._activeActions[X] || m._navActionVirtuallyHeld[X];
        }
        const U = m.pixelsPerArrowPress / 10, q = m.viewport.deltaPointsFromPixels(new n.Point(U, U));
        if (P("zoomIn")) {
          m.viewport.zoomBy(1.01, null, !0), m.viewport.applyConstraints();
          return;
        }
        if (P("zoomOut")) {
          m.viewport.zoomBy(0.99, null, !0), m.viewport.applyConstraints();
          return;
        }
        let ee = 0, ue = 0;
        m.preventVerticalPan || (P("panUp") && (ue -= q.y), P("panDown") && (ue += q.y)), m.preventHorizontalPan || (P("panLeft") && (ee -= q.x), P("panRight") && (ee += q.x)), (ee !== 0 || ue !== 0) && (m.viewport.panBy(new n.Point(ee, ue), !0), m.viewport.applyConstraints());
      }
      function $t(m) {
        if (_t(m), m._opening || !t[m.hash])
          return;
        if (m.autoResize || t[m.hash].forceResize) {
          let ue;
          if (m._autoResizePolling) {
            ue = r(m.container);
            const X = t[m.hash].prevContainerSize;
            ue.equals(X) || (t[m.hash].needsResize = !0);
          }
          t[m.hash].needsResize && Gi(m, ue || r(m.container));
        }
        const P = m.viewport.update();
        let U = m.world.update(P) || P;
        P && m.raiseEvent("viewport-change"), m.referenceStrip && (U = m.referenceStrip.update(m.viewport) || U);
        const q = t[m.hash].animating;
        !q && U && (m.raiseEvent("animation-start"), b(m));
        const ee = q && !U;
        ee && (t[m.hash].animating = !1), (U || ee || t[m.hash].forceRedraw || m.world.needsDraw()) && (kt(m), m._drawOverlays(), m.navigator && m.navigator.update(m.viewport), t[m.hash].forceRedraw = !1, U && m.raiseEvent("animation")), ee && (m.raiseEvent("animation-finish"), t[m.hash].mouseInside || f(m)), t[m.hash].animating = U;
      }
      function kt(m) {
        m.imageLoader.clear(), m.world.draw(), m.raiseEvent("update-viewport", {});
      }
      function me(m, P) {
        return m ? m + P : P;
      }
      function xt(m) {
        e.requestAnimationFrame(e.delegate(m, bi));
      }
      function bi() {
        if (t[this.hash].zooming && this.viewport) {
          const m = e.now(), P = m - t[this.hash].lastZoomTime, U = Math.pow(t[this.hash].zoomFactor, P / 1e3);
          this.viewport.zoomBy(U), this.viewport.applyConstraints(), t[this.hash].lastZoomTime = m, xt(this);
        }
      }
      function pe() {
        this.buttonGroup && (this.buttonGroup.emulateEnter(), this.buttonGroup.emulateLeave());
      }
      function ei() {
        this.viewport && this.viewport.goHome();
      }
      function It() {
        this.isFullPage() && !e.isFullScreen() ? this.setFullPage(!1) : this.setFullScreen(!this.isFullPage()), this.buttonGroup && this.buttonGroup.emulateLeave(), this.fullPageButton.element.focus(), this.viewport && this.viewport.applyConstraints();
      }
      function Pe() {
        if (this.viewport) {
          let m = this.viewport.getRotation();
          this.viewport.flipped ? m += this.rotationIncrement : m -= this.rotationIncrement, this.viewport.setRotation(m);
        }
      }
      function Ct() {
        if (this.viewport) {
          let m = this.viewport.getRotation();
          this.viewport.flipped ? m -= this.rotationIncrement : m += this.rotationIncrement, this.viewport.setRotation(m);
        }
      }
      function qi() {
        this.viewport.toggleFlip();
      }
      e.determineDrawer = function(m) {
        for (const P in n) {
          const U = n[P], q = U.prototype;
          if (q && q instanceof n.DrawerBase && e.isFunction(q.getType) && q.getType.call(U) === m)
            return U;
        }
        return null;
      };
    })(n), (function(e) {
      e.Navigator = function(c) {
        const d = c.viewer, f = this;
        let p, b;
        c.element || c.id ? (c.element ? (c.id && e.console.warn("Given option.id for Navigator was ignored since option.element was provided and is being used instead."), c.element.id ? c.id = c.element.id : c.id = "navigator-" + e.now(), this.element = c.element) : this.element = document.getElementById(c.id), c.controlOptions = {
          anchor: e.ControlAnchor.NONE,
          attachToViewer: !1,
          autoFade: !1
        }) : (c.id = "navigator-" + e.now(), this.element = e.makeNeutralElement("div"), c.controlOptions = {
          anchor: e.ControlAnchor.TOP_RIGHT,
          attachToViewer: !0,
          autoFade: c.autoFade
        }, c.position && (c.position === "BOTTOM_RIGHT" ? c.controlOptions.anchor = e.ControlAnchor.BOTTOM_RIGHT : c.position === "BOTTOM_LEFT" ? c.controlOptions.anchor = e.ControlAnchor.BOTTOM_LEFT : c.position === "TOP_RIGHT" ? c.controlOptions.anchor = e.ControlAnchor.TOP_RIGHT : c.position === "TOP_LEFT" ? c.controlOptions.anchor = e.ControlAnchor.TOP_LEFT : c.position === "ABSOLUTE" && (c.controlOptions.anchor = e.ControlAnchor.ABSOLUTE, c.controlOptions.top = c.top, c.controlOptions.left = c.left, c.controlOptions.height = c.height, c.controlOptions.width = c.width))), this.element.id = c.id, this.element.className += " navigator", c = e.extend(!0, {
          sizeRatio: e.DEFAULT_SETTINGS.navigatorSizeRatio
        }, c, {
          element: this.element,
          tabIndex: -1,
          // No keyboard navigation, omit from tab order
          //These need to be overridden to prevent recursion since
          //the navigator is a viewer and a viewer has a navigator
          showNavigator: !1,
          mouseNavEnabled: !1,
          showNavigationControl: !1,
          showSequenceControl: !1,
          immediateRender: !0,
          blendTime: 0,
          animationTime: c.animationTime,
          // disable autoResize since resize behavior is implemented differently by the navigator
          autoResize: !1,
          // prevent resizing the navigator from adding unwanted space around the image
          minZoomImageRatio: 1,
          background: c.background,
          opacity: c.opacity,
          borderColor: c.borderColor,
          displayRegionColor: c.displayRegionColor
        }), c.minPixelRatio = this.minPixelRatio = d.minPixelRatio, e.setElementTouchActionNone(this.element), this.borderWidth = 2, this.fudge = new e.Point(1, 1), this.totalBorderWidths = new e.Point(this.borderWidth * 2, this.borderWidth * 2).minus(this.fudge), c.controlOptions.anchor !== e.ControlAnchor.NONE && (function(T, I) {
          T.margin = "0px", T.border = I + "px solid " + c.borderColor, T.padding = "0px", T.background = c.background, T.opacity = c.opacity, T.overflow = "hidden";
        })(this.element.style, this.borderWidth), this.displayRegion = e.makeNeutralElement("div"), this.displayRegion.id = this.element.id + "-displayregion", this.displayRegion.className = "displayregion", (function(T, I) {
          T.position = "relative", T.top = "0px", T.left = "0px", T.fontSize = "0px", T.overflow = "hidden", T.border = I + "px solid " + c.displayRegionColor, T.margin = "0px", T.padding = "0px", T.background = "transparent", T.float = "left", T.cssFloat = "left", T.zIndex = 999999999, T.cursor = "default", T.boxSizing = "content-box";
        })(this.displayRegion.style, this.borderWidth), e.setElementPointerEventsNone(this.displayRegion), e.setElementTouchActionNone(this.displayRegion), this.displayRegionContainer = e.makeNeutralElement("div"), this.displayRegionContainer.id = this.element.id + "-displayregioncontainer", this.displayRegionContainer.className = "displayregioncontainer", this.displayRegionContainer.style.width = "100%", this.displayRegionContainer.style.height = "100%", e.setElementPointerEventsNone(this.displayRegionContainer), e.setElementTouchActionNone(this.displayRegionContainer), d.addControl(
          this.element,
          c.controlOptions
        ), this._resizeWithViewer = c.controlOptions.anchor !== e.ControlAnchor.ABSOLUTE && c.controlOptions.anchor !== e.ControlAnchor.NONE, c.width && c.height ? (this.setWidth(c.width), this.setHeight(c.height)) : this._resizeWithViewer && (p = e.getElementSize(d.element), this.element.style.height = Math.round(p.y * c.sizeRatio) + "px", this.element.style.width = Math.round(p.x * c.sizeRatio) + "px", this.oldViewerSize = p, b = e.getElementSize(this.element), this.elementArea = b.x * b.y), this.oldContainerSize = new e.Point(0, 0), e.Viewer.apply(this, [c]), this.displayRegionContainer.appendChild(this.displayRegion), this.element.getElementsByTagName("div")[0].appendChild(this.displayRegionContainer);
        function x(T, I) {
          l(f.displayRegionContainer, T), l(f.displayRegion, -T), f.viewport.setRotation(T, I);
        }
        if (c.navigatorRotate) {
          const T = c.viewer.viewport ? c.viewer.viewport.getRotation() : c.viewer.degrees || 0;
          x(T, !0), c.viewer.addHandler("rotate", function(I) {
            x(I.degrees, I.immediately);
          });
        }
        this.innerTracker.destroy(), this.innerTracker = new e.MouseTracker({
          userData: "Navigator.innerTracker",
          element: this.element,
          //this.canvas,
          dragHandler: e.delegate(this, i),
          clickHandler: e.delegate(this, t),
          releaseHandler: e.delegate(this, r),
          scrollHandler: e.delegate(this, o),
          preProcessEventHandler: function(T) {
            T.eventType === "wheel" && (T.preventDefault = !0);
          }
        }), this.outerTracker.userData = "Navigator.outerTracker", e.setElementPointerEventsNone(this.canvas), e.setElementPointerEventsNone(this.container), this.addHandler("reset-size", function() {
          f.viewport && f.viewport.goHome(!0);
        }), d.world.addHandler("item-index-change", function(T) {
          window.setTimeout(function() {
            const I = f.world.getItemAt(T.previousIndex);
            f.world.setItemIndex(I, T.newIndex);
          }, 1);
        }), d.world.addHandler("remove-item", function(T) {
          const I = T.item, z = f._getMatchingItem(I);
          z && f.world.removeItem(z);
        }), this.update(d.viewport);
      }, e.extend(
        e.Navigator.prototype,
        e.EventSource.prototype,
        e.Viewer.prototype,
        /** @lends OpenSeadragon.Navigator.prototype */
        {
          /**
           * Used to notify the navigator when its size has changed.
           * Especially useful when {@link OpenSeadragon.Options}.navigatorAutoResize is set to false and the navigator is resizable.
           * @function
           */
          updateSize: function() {
            if (this.viewport) {
              const c = new e.Point(
                this.container.clientWidth === 0 ? 1 : this.container.clientWidth,
                this.container.clientHeight === 0 ? 1 : this.container.clientHeight
              );
              c.equals(this.oldContainerSize) || (this.viewport.resize(c, !0), this.viewport.goHome(!0), this.oldContainerSize = c, this.world.update(), this.world.draw(), this.update(this.viewer.viewport));
            }
          },
          /**
           * Explicitly sets the width of the navigator, in web coordinates. Disables automatic resizing.
           * @param {Number|String} width - the new width, either a number of pixels or a CSS string, such as "100%"
           */
          setWidth: function(c) {
            this.width = c, this.element.style.width = typeof c == "number" ? c + "px" : c, this._resizeWithViewer = !1, this.updateSize();
          },
          /**
           * Explicitly sets the height of the navigator, in web coordinates. Disables automatic resizing.
           * @param {Number|String} height - the new height, either a number of pixels or a CSS string, such as "100%"
           */
          setHeight: function(c) {
            this.height = c, this.element.style.height = typeof c == "number" ? c + "px" : c, this._resizeWithViewer = !1, this.updateSize();
          },
          /**
            * Flip navigator element
            * @param {Boolean} state - Flip state to set.
            */
          setFlip: function(c) {
            return this.viewport.setFlip(c), this.setDisplayTransform(this.viewer.viewport.getFlip() ? "scale(-1,1)" : "scale(1,1)"), this;
          },
          setDisplayTransform: function(c) {
            a(this.canvas, c), a(this.element, c);
          },
          /**
           * Used to update the navigator minimap's viewport rectangle when a change in the viewer's viewport occurs.
           * @function
           * @param {OpenSeadragon.Viewport} [viewport] The viewport to display. Default: the viewport this navigator is tracking.
           */
          update: function(c) {
            let d, f, p, b, x, T;
            if (c || (c = this.viewer.viewport), d = e.getElementSize(this.viewer.element), this._resizeWithViewer && d.x && d.y && !d.equals(this.oldViewerSize) && (this.oldViewerSize = d, this.maintainSizeRatio || !this.elementArea ? (f = d.x * this.sizeRatio, p = d.y * this.sizeRatio) : (f = Math.sqrt(this.elementArea * (d.x / d.y)), p = this.elementArea / f), this.element.style.width = Math.round(f) + "px", this.element.style.height = Math.round(p) + "px", this.elementArea || (this.elementArea = f * p), this.updateSize()), c && this.viewport) {
              if (b = c.getBoundsNoRotate(!0), x = this.viewport.pixelFromPointNoRotate(b.getTopLeft(), !1), T = this.viewport.pixelFromPointNoRotate(b.getBottomRight(), !1).minus(this.totalBorderWidths), !this.navigatorRotate) {
                const j = c.getRotation(!0);
                l(this.displayRegion, -j);
              }
              const I = this.displayRegion.style;
              I.display = this.world.getItemCount() ? "block" : "none", I.top = x.y.toFixed(2) + "px", I.left = x.x.toFixed(2) + "px";
              const z = T.x - x.x, W = T.y - x.y;
              I.width = Math.round(Math.max(z, 0)) + "px", I.height = Math.round(Math.max(W, 0)) + "px";
            }
          },
          // overrides Viewer.addTiledImage
          addTiledImage: function(c) {
            const d = this, f = c.originalTiledImage;
            delete c.original;
            const p = e.extend({}, c, {
              success: function(b) {
                const x = b.item;
                x._originalForNavigator = f, d._matchBounds(x, f, !0), d._matchOpacity(x, f), d._matchCompositeOperation(x, f);
                function T() {
                  d._matchBounds(x, f);
                }
                function I() {
                  d._matchOpacity(x, f);
                }
                function z() {
                  d._matchCompositeOperation(x, f);
                }
                f.addHandler("bounds-change", T), f.addHandler("clip-change", T), f.addHandler("opacity-change", I), f.addHandler("composite-operation-change", z);
              }
            });
            return e.Viewer.prototype.addTiledImage.apply(this, [p]);
          },
          destroy: function() {
            return e.Viewer.prototype.destroy.apply(this);
          },
          // private
          _getMatchingItem: function(c) {
            const d = this.world.getItemCount();
            for (let f = 0; f < d; f++) {
              let p = this.world.getItemAt(f);
              if (p._originalForNavigator === c)
                return p;
            }
            return null;
          },
          // private
          _matchBounds: function(c, d, f) {
            const p = d.getBoundsNoRotate();
            c.setPosition(p.getTopLeft(), f), c.setWidth(p.width, f), c.setRotation(d.getRotation(), f), c.setClip(d.getClip()), c.setFlip(d.getFlip());
          },
          // private
          _matchOpacity: function(c, d) {
            c.setOpacity(d.opacity);
          },
          // private
          _matchCompositeOperation: function(c, d) {
            c.setCompositeOperation(d.compositeOperation);
          }
        }
      );
      function t(c) {
        const d = {
          tracker: c.eventSource,
          position: c.position,
          quick: c.quick,
          shift: c.shift,
          originalEvent: c.originalEvent,
          preventDefaultAction: !1
        };
        if (this.viewer.raiseEvent("navigator-click", d), !d.preventDefaultAction && c.quick && this.viewer.viewport && (this.panVertical || this.panHorizontal)) {
          this.viewer.viewport.flipped && (c.position.x = this.viewport.getContainerSize().x - c.position.x);
          const f = this.viewport.pointFromPixel(c.position);
          this.panVertical ? this.panHorizontal || (f.x = this.viewer.viewport.getCenter(!0).x) : f.y = this.viewer.viewport.getCenter(!0).y, this.viewer.viewport.panTo(f), this.viewer.viewport.applyConstraints();
        }
      }
      function i(c) {
        const d = {
          tracker: c.eventSource,
          position: c.position,
          delta: c.delta,
          speed: c.speed,
          direction: c.direction,
          shift: c.shift,
          originalEvent: c.originalEvent,
          preventDefaultAction: !1
        };
        this.viewer.raiseEvent("navigator-drag", d), !d.preventDefaultAction && this.viewer.viewport && (this.panHorizontal || (c.delta.x = 0), this.panVertical || (c.delta.y = 0), this.viewer.viewport.flipped && (c.delta.x = -c.delta.x), this.viewer.viewport.panBy(
          this.viewport.deltaPointsFromPixels(
            c.delta
          )
        ), this.viewer.constrainDuringPan && this.viewer.viewport.applyConstraints());
      }
      function r(c) {
        c.insideElementPressed && this.viewer.viewport && this.viewer.viewport.applyConstraints();
      }
      function o(c) {
        const d = {
          tracker: c.eventSource,
          position: c.position,
          scroll: c.scroll,
          shift: c.shift,
          originalEvent: c.originalEvent,
          preventDefault: c.preventDefault
        };
        this.viewer.raiseEvent("navigator-scroll", d), c.preventDefault = d.preventDefault;
      }
      function l(c, d) {
        a(c, "rotate(" + d + "deg)");
      }
      function a(c, d) {
        c.style.webkitTransform = d, c.style.mozTransform = d, c.style.msTransform = d, c.style.oTransform = d, c.style.transform = d;
      }
    })(n), (function(e) {
      const t = {
        Errors: {
          Dzc: "Sorry, we don't support Deep Zoom Collections!",
          Dzi: "Hmm, this doesn't appear to be a valid Deep Zoom Image.",
          Xml: "Hmm, this doesn't appear to be a valid Deep Zoom Image.",
          ImageFormat: "Sorry, we don't support {0}-based Deep Zoom Images.",
          Security: "It looks like a security restriction stopped us from loading this Deep Zoom Image.",
          Status: "This space unintentionally left blank ({0} {1}).",
          OpenFailed: "Unable to open {0}: {1}"
        },
        Tooltips: {
          FullPage: "Toggle full page",
          Home: "Go home",
          ZoomIn: "Zoom in",
          ZoomOut: "Zoom out",
          NextPage: "Next page",
          PreviousPage: "Previous page",
          RotateLeft: "Rotate left",
          RotateRight: "Rotate right",
          Flip: "Flip Horizontally"
        }
      };
      e.extend(
        e,
        /** @lends OpenSeadragon */
        {
          /**
           * @function
           * @param {String} property
           */
          getString: function(i) {
            const r = i.split(".");
            let o = null;
            const l = arguments;
            let a = t, c;
            for (c = 0; c < r.length - 1; c++)
              a = a[r[c]] || {};
            return o = a[r[c]], typeof o != "string" && (e.console.error("Untranslated source string:", i), o = ""), o.replace(/\{\d+\}/g, function(d) {
              const f = parseInt(d.match(/\d+/), 10) + 1;
              return f < l.length ? l[f] : "";
            });
          },
          /**
           * @function
           * @param {String} property
           * @param {*} value
           */
          setString: function(i, r) {
            const o = i.split(".");
            let l = t, a;
            for (a = 0; a < o.length - 1; a++)
              l[o[a]] || (l[o[a]] = {}), l = l[o[a]];
            l[o[a]] = r;
          }
        }
      );
    })(n), (function(e) {
      e.Point = function(t, i) {
        this.x = typeof t == "number" ? t : 0, this.y = typeof i == "number" ? i : 0;
      }, e.Point.prototype = {
        /**
         * @function
         * @returns {OpenSeadragon.Point} a duplicate of this Point
         */
        clone: function() {
          return new e.Point(this.x, this.y);
        },
        /**
         * Add another Point to this point and return a new Point.
         * @function
         * @param {OpenSeadragon.Point} point The point to add vector components.
         * @returns {OpenSeadragon.Point} A new point representing the sum of the
         *  vector components
         */
        plus: function(t) {
          return new e.Point(
            this.x + t.x,
            this.y + t.y
          );
        },
        /**
         * Subtract another Point to this point and return a new Point.
         * @function
         * @param {OpenSeadragon.Point} point The point to subtract vector components.
         * @returns {OpenSeadragon.Point} A new point representing the subtraction of the
         *  vector components
         */
        minus: function(t) {
          return new e.Point(
            this.x - t.x,
            this.y - t.y
          );
        },
        /**
         * Multiply this point by a factor and return a new Point.
         * @function
         * @param {Number} factor The factor to multiply vector components.
         * @returns {OpenSeadragon.Point} A new point representing the multiplication
         *  of the vector components by the factor
         */
        times: function(t) {
          return new e.Point(
            this.x * t,
            this.y * t
          );
        },
        /**
         * Divide this point by a factor and return a new Point.
         * @function
         * @param {Number} factor The factor to divide vector components.
         * @returns {OpenSeadragon.Point} A new point representing the division of the
         *  vector components by the factor
         */
        divide: function(t) {
          return new e.Point(
            this.x / t,
            this.y / t
          );
        },
        /**
         * Compute the opposite of this point and return a new Point.
         * @function
         * @returns {OpenSeadragon.Point} A new point representing the opposite of the
         *  vector components
         */
        negate: function() {
          return new e.Point(-this.x, -this.y);
        },
        /**
         * Compute the distance between this point and another point.
         * @function
         * @param {OpenSeadragon.Point} point The point to compute the distance with.
         * @returns {Number} The distance between the 2 points
         */
        distanceTo: function(t) {
          return Math.sqrt(
            Math.pow(this.x - t.x, 2) + Math.pow(this.y - t.y, 2)
          );
        },
        /**
         * Compute the squared distance between this point and another point.
         * Useful for optimizing things like comparing distances.
         * @function
         * @param {OpenSeadragon.Point} point The point to compute the squared distance with.
         * @returns {Number} The squared distance between the 2 points
         */
        squaredDistanceTo: function(t) {
          return Math.pow(this.x - t.x, 2) + Math.pow(this.y - t.y, 2);
        },
        /**
         * Apply a function to each coordinate of this point and return a new point.
         * @function
         * @param {function} func The function to apply to each coordinate.
         * @returns {OpenSeadragon.Point} A new point with the coordinates computed
         * by the specified function
         */
        apply: function(t) {
          return new e.Point(t(this.x), t(this.y));
        },
        /**
         * Check if this point is equal to another one.
         * @function
         * @param {OpenSeadragon.Point} point The point to compare this point with.
         * @returns {Boolean} true if they are equal, false otherwise.
         */
        equals: function(t) {
          return t instanceof e.Point && this.x === t.x && this.y === t.y;
        },
        /**
         * Rotates the point around the specified pivot
         * From http://stackoverflow.com/questions/4465931/rotate-rectangle-around-a-point
         * @function
         * @param {Number} degress to rotate around the pivot.
         * @param {OpenSeadragon.Point} [pivot=(0,0)] Point around which to rotate.
         * Defaults to the origin.
         * @returns {OpenSeadragon.Point}. A new point representing the point rotated around the specified pivot
         */
        rotate: function(t, i) {
          i = i || new e.Point(0, 0);
          let r, o;
          if (t % 90 === 0)
            switch (e.positiveModulo(t, 360)) {
              case 0:
                r = 1, o = 0;
                break;
              case 90:
                r = 0, o = 1;
                break;
              case 180:
                r = -1, o = 0;
                break;
              case 270:
                r = 0, o = -1;
                break;
            }
          else {
            const c = t * Math.PI / 180;
            r = Math.cos(c), o = Math.sin(c);
          }
          const l = r * (this.x - i.x) - o * (this.y - i.y) + i.x, a = o * (this.x - i.x) + r * (this.y - i.y) + i.y;
          return new e.Point(l, a);
        },
        /**
         * Convert this point to a string in the format (x,y) where x and y are
         * rounded to the nearest integer.
         * @function
         * @returns {String} A string representation of this point.
         */
        toString: function() {
          return "(" + Math.round(this.x * 100) / 100 + "," + Math.round(this.y * 100) / 100 + ")";
        }
      };
    })(n), (function(e) {
      e.TileSource = function(i, r, o, l, a, c) {
        const d = this, f = arguments;
        let p, b;
        if (e.isPlainObject(i) ? p = i : p = {
          width: f[0],
          height: f[1],
          tileSize: f[2],
          tileOverlap: f[3],
          minLevel: f[4],
          maxLevel: f[5]
        }, e.EventSource.call(this), e.extend(!0, this, p), !this.success) {
          for (b = 0; b < arguments.length; b++)
            if (e.isFunction(arguments[b])) {
              this.success = arguments[b];
              break;
            }
        }
        this.success && this.addHandler("ready", function(x) {
          d.success(x);
        }), e.type(arguments[0]) === "string" && (this.url = arguments[0]), this.url ? (this.aspectRatio = 1, this.dimensions = new e.Point(10, 10), this._tileWidth = 0, this._tileHeight = 0, this.tileOverlap = 0, this.minLevel = 0, this.maxLevel = 0, this.ready = !1, this.getImageInfo(this.url)) : (this.ready = !0, this.aspectRatio = p.width && p.height ? p.width / p.height : 1, this.dimensions = new e.Point(p.width, p.height), this.tileSize ? (this._tileWidth = this._tileHeight = this.tileSize, delete this.tileSize) : (this.tileWidth ? (this._tileWidth = this.tileWidth, delete this.tileWidth) : this._tileWidth = 0, this.tileHeight ? (this._tileHeight = this.tileHeight, delete this.tileHeight) : this._tileHeight = 0), this.tileOverlap = p.tileOverlap ? p.tileOverlap : 0, this.minLevel = p.minLevel ? p.minLevel : 0, this.maxLevel = p.maxLevel !== void 0 && p.maxLevel !== null ? p.maxLevel : p.width && p.height ? Math.ceil(
          Math.log(Math.max(p.width, p.height)) / Math.log(2)
        ) : 0, this.success && e.isFunction(this.success) && this.success(this));
      }, e.TileSource.prototype = {
        getTileSize: function(i) {
          return e.console.error(
            "[TileSource.getTileSize] is deprecated. Use TileSource.getTileWidth() and TileSource.getTileHeight() instead"
          ), this._tileWidth;
        },
        /**
         * Return the tileWidth for a given level.
         * Subclasses should override this if tileWidth can be different at different levels
         *   such as in IIIFTileSource.  Code should use this function rather than reading
         *   from ._tileWidth directly.
         * @function
         * @param {Number} level
         */
        getTileWidth: function(i) {
          return this._tileWidth ? this._tileWidth : this.getTileSize(i);
        },
        /**
         * Return the tileHeight for a given level.
         * Subclasses should override this if tileHeight can be different at different levels
         *   such as in IIIFTileSource.  Code should use this function rather than reading
         *   from ._tileHeight directly.
         * @function
         * @param {Number} level
         */
        getTileHeight: function(i) {
          return this._tileHeight ? this._tileHeight : this.getTileSize(i);
        },
        /**
         * Set the maxLevel to the given level, and perform the memoization of
         * getLevelScale with the new maxLevel. This function can be useful if the
         * memoization is required before the first call of getLevelScale, or both
         * memoized getLevelScale and maxLevel should be changed accordingly.
         * @function
         * @param {Number} level
         */
        setMaxLevel: function(i) {
          this.maxLevel = i, this._memoizeLevelScale();
        },
        /**
         * @function
         * @param {Number} level
         */
        getLevelScale: function(i) {
          return this._memoizeLevelScale(), this.getLevelScale(i);
        },
        // private
        _memoizeLevelScale: function() {
          const i = {};
          let r;
          for (r = 0; r <= this.maxLevel; r++)
            i[r] = 1 / Math.pow(2, this.maxLevel - r);
          this.getLevelScale = function(o) {
            return i[o];
          };
        },
        /**
         * @function
         * @param {Number} level
         */
        getNumTiles: function(i) {
          const r = this.getLevelScale(i), o = Math.ceil(r * this.dimensions.x / this.getTileWidth(i)), l = Math.ceil(r * this.dimensions.y / this.getTileHeight(i));
          return new e.Point(o, l);
        },
        /**
         * @function
         * @param {Number} level
         */
        getPixelRatio: function(i) {
          const r = this.dimensions.times(this.getLevelScale(i)), o = 1 / r.x * e.pixelDensityRatio, l = 1 / r.y * e.pixelDensityRatio;
          return new e.Point(o, l);
        },
        /**
         * @function
         * @returns {Number} The highest level in this tile source that can be contained in a single tile.
         */
        getClosestLevel: function() {
          let i, r;
          for (i = this.minLevel + 1; i <= this.maxLevel && (r = this.getNumTiles(i), !(r.x > 1 || r.y > 1)); i++)
            ;
          return i - 1;
        },
        /**
         * @function
         * @param {Number} level
         * @param {OpenSeadragon.Point} point
         */
        getTileAtPoint: function(i, r) {
          const o = r.x >= 0 && r.x <= 1 && r.y >= 0 && r.y <= 1 / this.aspectRatio;
          e.console.assert(o, "[TileSource.getTileAtPoint] must be called with a valid point.");
          const l = this.dimensions.x * this.getLevelScale(i), a = r.x * l, c = r.y * l;
          let d = Math.floor(a / this.getTileWidth(i)), f = Math.floor(c / this.getTileHeight(i));
          return r.x >= 1 && (d = this.getNumTiles(i).x - 1), r.y >= 1 / this.aspectRatio - 1e-15 && (f = this.getNumTiles(i).y - 1), new e.Point(d, f);
        },
        /**
         * @function
         * @param {Number} level
         * @param {Number} x
         * @param {Number} y
         * @param {Boolean} [isSource=false] Whether to return the source bounds of the tile.
         * @returns {OpenSeadragon.Rect} Either where this tile fits (in normalized coordinates) or the
         * portion of the tile to use as the source of the drawing operation (in pixels), depending on
         * the isSource parameter.
         */
        getTileBounds: function(i, r, o, l) {
          const a = this.dimensions.times(this.getLevelScale(i)), c = this.getTileWidth(i), d = this.getTileHeight(i), f = r === 0 ? 0 : c * r - this.tileOverlap, p = o === 0 ? 0 : d * o - this.tileOverlap;
          let b = c + (r === 0 ? 1 : 2) * this.tileOverlap, x = d + (o === 0 ? 1 : 2) * this.tileOverlap;
          const T = 1 / a.x;
          return b = Math.min(b, a.x - f), x = Math.min(x, a.y - p), l ? new e.Rect(0, 0, b, x) : new e.Rect(f * T, p * T, b * T, x * T);
        },
        /**
         * Responsible for retrieving, and caching the
         * image metadata pertinent to this TileSources implementation.
         * There are three scenarios of opening a tile source: providing a parseable string, plain object, or an URL.
         * This method is only called by OSD if the TileSource configuration is a non-parseable string (~url).
         *
         * The string can contain a hash `#` symbol, followed by
         * key=value arguments. If this is the case, this method sends this
         * data as a POST body.
         *
         * @function
         * @param {String} url
         * @throws {Error}
         */
        getImageInfo: function(i) {
          const r = this;
          let o, l, a, c, d, f, p;
          i && (d = i.split("/"), f = d[d.length - 1], p = f.lastIndexOf("."), p > -1 && (d[d.length - 1] = f.slice(0, p)));
          let b = null;
          if (this.splitHashDataForPost) {
            const x = i.indexOf("#");
            x !== -1 && (b = i.substring(x + 1), i = i.substr(0, x));
          }
          l = function(x) {
            typeof x == "string" && (x = e.parseXml(x));
            const T = e.TileSource.determineType(r, x, i);
            if (!T) {
              r.raiseEvent("open-failed", { message: "Unable to load TileSource", source: i });
              return;
            }
            c = T.prototype.configure.apply(r, [x, i, b]), c.ajaxWithCredentials === void 0 && (c.ajaxWithCredentials = r.ajaxWithCredentials), a = new T(c), r.ready = !0, r.raiseEvent("ready", { tileSource: a });
          }, i.match(/\.js$/) ? (o = i.split("/").pop().replace(".js", ""), e.jsonp({
            url: i,
            async: !1,
            callbackName: o,
            callback: l
          })) : e.makeAjaxRequest({
            url: i,
            postData: b,
            withCredentials: this.ajaxWithCredentials,
            headers: this.ajaxHeaders,
            success: function(x) {
              const T = t(x);
              l(T);
            },
            error: function(x, T) {
              let I;
              try {
                I = "HTTP " + x.status + " attempting to load TileSource: " + i;
              } catch {
                let W;
                typeof T > "u" || !T.toString ? W = "Unknown error" : W = T.toString(), I = W + " attempting to load TileSource: " + i;
              }
              e.console.error(I), r.raiseEvent("open-failed", {
                message: I,
                source: i,
                postData: b
              });
            }
          });
        },
        /**
         * Responsible for determining if the particular TileSource supports the
         * data format ( and allowed to apply logic against the url the data was
         * loaded from, if any ). Overriding implementations are expected to do
         * something smart with data and / or url to determine support.  Also
         * understand that iteration order of TileSources is not guaranteed so
         * please make sure your data or url is expressive enough to ensure a simple
         * and sufficient mechanism for clear determination.
         * @function
         * @param {String|Object|Array|Document} data
         * @param {String} url - the url the data was loaded
         *      from if any.
         * @returns {Boolean}
         */
        supports: function(i, r) {
          return !1;
        },
        /**
         * Check whether two tileSources are equal. This is used for example
         * when replacing tile-sources, which turns on the zombie cache before
         * old item removal.
         * @param {OpenSeadragon.TileSource} otherSource
         * @returns {Boolean}
         */
        equals: function(i) {
          return !1;
        },
        /**
         * Responsible for parsing and configuring the
         * image metadata pertinent to this TileSources implementation.
         * This method is not implemented by this class other than to throw an Error
         * announcing you have to implement it.  Because of the variety of tile
         * server technologies, and various specifications for building image
         * pyramids, this method is here to allow easy integration.
         * @function
         * @param {String|Object|Array|Document} data
         * @param {String} url - the url the data was loaded
         *      from if any.
         * @param {String} postData - HTTP POST data in k=v&k2=v2... form or null value obtained from
         *      the protocol URL after '#' sign if flag splitHashDataForPost set to 'true'
         * @returns {Object} options - A dictionary of keyword arguments sufficient
         *      to configure the tile source constructor (include all values you want to
         *      instantiate the TileSource subclass with - what _options_ object should contain).
         * @throws {Error}
         */
        configure: function(i, r, o) {
          throw new Error("Method not implemented.");
        },
        /**
         * Shall this source need to free some objects
         * upon unloading, it must be done here. For example, canvas
         * size must be set to 0 for safari to free.
         * @param {OpenSeadragon.Viewer} viewer
         */
        destroy: function(i) {
        },
        /**
         * Responsible for retrieving the url which will return an image for the
         * region specified by the given x, y, and level components.
         * This method is not implemented by this class other than to throw an Error
         * announcing you have to implement it.  Because of the variety of tile
         * server technologies, and various specifications for building image
         * pyramids, this method is here to allow easy integration.
         * @function
         * @param {Number} level
         * @param {Number} x
         * @param {Number} y
         * @returns {String|Function} url - A string for the url or a function that returns a url string.
         * @throws {Error}
         */
        getTileUrl: function(i, r, o) {
          throw new Error("Method not implemented.");
        },
        /**
         * Must use AJAX in order to work, i.e. loadTilesWithAjax = true is set.
         * If a value is returned, ajax issues POST request to the tile url.
         * If null is returned, ajax issues GET request.
         * The return value must comply to the header 'content type'.
         *
         * Examples (USED HEADER --> getTilePostData CODE):
         * 'Content-type': 'application/x-www-form-urlencoded' -->
         *   return "key1=value=1&key2=value2";
         *
         * 'Content-type': 'application/x-www-form-urlencoded' -->
         *   return JSON.stringify({key: "value", number: 5});
         *
         * 'Content-type': 'multipart/form-data' -->
         *   let result = new FormData();
         *   result.append("data", myData);
         *   return result;
         *
         * IMPORTANT: in case you move all the logic on image fetching
         * to post data, you must re-define 'getTileHashKey(...)' to
         * stay unique for different tile images.
         *
         * @param {Number} level
         * @param {Number} x
         * @param {Number} y
         * @returns {*|null} post data to send with tile configuration request
         */
        getTilePostData: function(i, r, o) {
          return null;
        },
        /**
         * Responsible for retrieving the headers which will be attached to the image request for the
         * region specified by the given x, y, and level components.
         * This option is only relevant if {@link OpenSeadragon.Options}.loadTilesWithAjax is set to true.
         * The headers returned here will override headers specified at the Viewer or TiledImage level.
         * Specifying a falsy value for a header will clear its existing value set at the Viewer or
         * TiledImage level (if any).
         *
         * Note that the headers of existing tiles don't automatically change when this function
         * returns updated headers. To do that, you need to call {@link OpenSeadragon.Viewer#setAjaxHeaders}
         * and propagate the changes.
         *
         * @function
         * @param {Number} level
         * @param {Number} x
         * @param {Number} y
         * @returns {Object}
         */
        getTileAjaxHeaders: function(i, r, o) {
          return {};
        },
        /**
         * The tile cache object is uniquely determined by this key and used to lookup
         * the image data in cache: keys should be different if images are different.
         *
         * You can return falsey tile cache key, in which case the tile will
         * be created without invoking ImageJob --- but with data=null. Then,
         * you are responsible for manually creating the cache data. This is useful
         * particularly if you want to use empty TiledImage with client-side derived data
         * only. The default tile-cache key is then called "" - an empty string.
         *
         * Note: default behaviour does not take into account post data.
         * @param {Number} level tile level it was fetched with
         * @param {Number} x x-coordinate in the pyramid level
         * @param {Number} y y-coordinate in the pyramid level
         * @param {String} url the tile was fetched with
         * @param {Object} ajaxHeaders the tile was fetched with
         * @param {*} postData data the tile was fetched with (type depends on getTilePostData(..) return type)
         * @return {?String} can return the cache key or null, in that case an empty cache is initialized
         *   without downloading any data for internal use: user has to define the cache contents manually, via
         *   the cache interface of this class.
         */
        getTileHashKey: function(i, r, o, l, a, c) {
          function d(f) {
            return a ? f + "+" + JSON.stringify(a) : f;
          }
          return d(typeof l != "string" ? i + "/" + r + "_" + o : l);
        },
        /**
         * @function
         * @param {Number} level
         * @param {Number} x
         * @param {Number} y
         */
        tileExists: function(i, r, o) {
          const l = this.getNumTiles(i);
          return i >= this.minLevel && i <= this.maxLevel && r >= 0 && o >= 0 && r < l.x && o < l.y;
        },
        /**
         * Decide whether tiles have transparency: this is crucial for correct images blending.
         * Overriden on a tile level by setting tile.hasTransparency = true;
         * @param context2D unused, deprecated argument
         * @param url tile.getUrl() value for given tile
         * @param ajaxHeaders tile.ajaxHeaders value for given tile
         * @param post tile.post value for given tile
         * @returns {boolean} true if the image has transparency
         */
        hasTransparency: function(i, r, o, l) {
          return r.match(".png");
        },
        /**
         * Download tile data.
         * Note that if you override this function, you should override also downloadTileAbort().
         * @param {ImageJob} context job context that you have to call finish(...) on.
         * @param {String} [context.src] - URL of image to download.
         * @param {String} [context.loadWithAjax] - Whether to load this image with AJAX.
         * @param {String} [context.ajaxHeaders] - Headers to add to the image request if using AJAX.
         * @param {Boolean} [context.ajaxWithCredentials] - Whether to set withCredentials on AJAX requests.
         * @param {String} [context.crossOriginPolicy] - CORS policy to use for downloads
         * @param {?String|?Object} [context.postData] - HTTP POST data (usually but not necessarily
         *   in k=v&k2=v2... form, see TileSource::getTilePostData) or null
         * @param {*} [context.userData] - Empty object to attach your own data and helper variables to.
         * @param {Function} [context.finish] - Should be called unless abort() was executed upon successful
         *   data retrieval.
         *   Usage: context.finish(data, request, dataType=undefined). Pass the downloaded data object
         *   add also reference to an ajax request if used. Optionally, specify what data type the data is.
         * @param {Function} [context.fail] - Should be called unless abort() was executed upon unsuccessful request.
         *   Usage: context.fail(errMessage, request). Provide error message in case of failure,
         *   add also reference to an ajax request if used.
         * @param {Function} [context.abort] - Called automatically when the job times out.
         *   Usage: if you decide to abort the request (no fail/finish will be called), call context.abort().
         * @param {Function} [context.callback] Private parameter. Called automatically once image has been downloaded
         *   (triggered by finish).
         * @param {Number} [context.timeout] Private parameter. The max number of milliseconds that
         *   this image job may take to complete.
         * @param {string} [context.errorMsg] Private parameter. The final error message, default null (set by finish).
         */
        downloadTileStart: function(i) {
          i.loadWithAjax ? e.makeAjaxRequest({
            url: i.src,
            withCredentials: i.ajaxWithCredentials,
            headers: i.ajaxHeaders,
            responseType: "arraybuffer",
            postData: i.postData,
            success: function(r) {
              let o;
              try {
                o = new window.Blob([r.response]);
              } catch (l) {
                const a = window.BlobBuilder || window.WebKitBlobBuilder || window.MozBlobBuilder || window.MSBlobBuilder;
                if (l.name === "TypeError" && a) {
                  const c = new a();
                  c.append(r.response), o = c.getBlob();
                }
              }
              o.size === 0 ? i.fail("[downloadTileStart] Empty image response.", r) : i.finish(o, r, "rasterBlob");
            },
            error: function(r) {
              i.fail("[downloadTileStart] Image load aborted - XHR error", r);
            }
          }) : i.finish(i.src, null, "imageUrl");
        },
        /**
         * Provide means of aborting the execution.
         * Note that if you override this function, you should override also downloadTileStart().
         * Note that calling job.abort() would create an infinite loop!
         *
         * @param {ImageJob} context job, the same object as with downloadTileStart(..)
         * @param {*} [context.userData] - Empty object to attach (and mainly read) your own data.
         */
        downloadTileAbort: function(i) {
          i.userData.request && i.userData.request.abort();
          const r = i.userData.image;
          i.userData.image && (r.onload = r.onerror = r.onabort = null);
        },
        /**
         * Create cache object from the result of the download process. The
         * cacheObject parameter should be used to attach the data to, there are no
         * conventions on how it should be stored - all the logic is implemented within *TileCache() functions.
         *
         * Note that
         *  - data is cached automatically as cacheObject.data
         *  - if you override any of *TileCache() functions, you should override all of them.
         *  - these functions might be called over shared cache object managed by other TileSources simultaneously.
         * @param {OpenSeadragon.CacheRecord} cacheObject context cache object
         * @param {*} data image data, the data sent to ImageJob.prototype.finish(), by default an Image object
         * @param {OpenSeadragon.Tile} tile instance the cache was created with
         * @deprecated
         */
        createTileCache: function(i, r, o) {
          e.console.error("[TileSource.createTileCache] has been deprecated. Use cache API of a tile instead.");
        },
        /**
         * Cache object destructor, unset all properties you created to allow GC collection.
         * Note that if you override any of *TileCache() functions, you should override all of them.
         * Note that these functions might be called over shared cache object managed by other TileSources simultaneously.
         * Original cache data is cacheObject.data, but do not delete it manually! It is taken care for,
         * you might break things.
         * @param {OpenSeadragon.CacheRecord} cacheObject context cache object
         * @deprecated
         */
        destroyTileCache: function(i) {
          e.console.error("[TileSource.destroyTileCache] has been deprecated. Use cache API of a tile instead.");
        },
        /**
         * Raw data getter, should return anything that is compatible with the system, or undefined
         * if the system can handle it.
         * @param {OpenSeadragon.CacheRecord} cacheObject context cache object
         * @returns {OpenSeadragon.Promise<?>} cache data
         * @deprecated
         */
        getTileCacheData: function(i) {
          return e.console.error("[TileSource.getTileCacheData] has been deprecated. Use cache API of a tile instead."), i.getDataAs(void 0, !1);
        },
        /**
         * Compatibility image element getter
         *  - plugins might need image representation of the data
         *  - div HTML rendering relies on image element presence
         * Note that if you override any of *TileCache() functions, you should override all of them.
         * Note that these functions might be called over shared cache object managed by other TileSources simultaneously.
         *  @param {OpenSeadragon.CacheRecord} cacheObject context cache object
         *  @returns {Image} cache data as an Image
         *  @deprecated
         */
        getTileCacheDataAsImage: function(i) {
          return e.console.error("[TileSource.getTileCacheDataAsImage] has been deprecated. Use cache API of a tile instead."), i.getImage();
        },
        /**
         * Compatibility context 2D getter
         *  - most heavily used rendering method is a canvas-based approach,
         *    convert the data to a canvas and return it's 2D context
         * Note that if you override any of *TileCache() functions, you should override all of them.
         * @param {OpenSeadragon.CacheRecord} cacheObject context cache object
         * @returns {CanvasRenderingContext2D} context of the canvas representation of the cache data
         * @deprecated
         */
        getTileCacheDataAsContext2D: function(i) {
          return e.console.error("[TileSource.getTileCacheDataAsContext2D] has been deprecated. Use cache API of a tile instead."), i.getRenderedContext();
        }
      }, e.extend(!0, e.TileSource.prototype, e.EventSource.prototype);
      function t(i) {
        const r = i.responseText;
        let o = i.status, l, a;
        if (i) {
          if (i.status !== 200 && i.status !== 0)
            throw o = i.status, l = o === 404 ? "Not Found" : i.statusText, new Error(e.getString("Errors.Status", o, l));
        } else throw new Error(e.getString("Errors.Security"));
        if (r.match(/^\s*<.*/))
          try {
            a = i.responseXML && i.responseXML.documentElement ? i.responseXML : e.parseXml(r);
          } catch {
            a = i.responseText;
          }
        else if (r.match(/\s*[{[].*/))
          try {
            a = e.parseJSON(r);
          } catch {
            a = r;
          }
        else
          a = r;
        return a;
      }
      e.TileSource.determineType = function(i, r, o) {
        for (const l in n)
          if (l.match(/.+TileSource$/) && e.isFunction(n[l]) && e.isFunction(n[l].prototype.supports) && n[l].prototype.supports.call(i, r, o))
            return n[l];
        return e.console.error("No TileSource was able to open %s %s", o, r), null;
      };
    })(n), (function(e) {
      e.DziTileSource = function(r, o, l, a, c, d, f, p, b) {
        let x, T;
        if (e.isPlainObject(r) ? T = r : T = {
          width: arguments[0],
          height: arguments[1],
          tileSize: arguments[2],
          tileOverlap: arguments[3],
          tilesUrl: arguments[4],
          fileFormat: arguments[5],
          displayRects: arguments[6],
          minLevel: arguments[7],
          maxLevel: arguments[8]
        }, this._levelRects = {}, this.tilesUrl = T.tilesUrl, this.fileFormat = T.fileFormat, this.displayRects = T.displayRects, this.displayRects)
          for (let I = this.displayRects.length - 1; I >= 0; I--) {
            const z = this.displayRects[I];
            for (x = z.minLevel; x <= z.maxLevel; x++)
              this._levelRects[x] || (this._levelRects[x] = []), this._levelRects[x].push(z);
          }
        e.TileSource.apply(this, [T]);
      }, e.extend(
        e.DziTileSource.prototype,
        e.TileSource.prototype,
        /** @lends OpenSeadragon.DziTileSource.prototype */
        {
          /**
           * Determine if the data and/or url imply the image service is supported by
           * this tile source.
           * @function
           * @param {Object|Array} data
           * @param {String} optional - url
           */
          supports: function(r, o) {
            let l;
            return r.Image ? l = r.Image.xmlns : r.documentElement && (r.documentElement.localName === "Image" || r.documentElement.tagName === "Image") && (l = r.documentElement.namespaceURI), l = (l || "").toLowerCase(), l.indexOf("schemas.microsoft.com/deepzoom/2008") !== -1 || l.indexOf("schemas.microsoft.com/deepzoom/2009") !== -1;
          },
          /**
           *
           * @function
           * @param {Object|XMLDocument} data - the raw configuration
           * @param {String} url - the url the data was retrieved from if any.
           * @param {String} postData - HTTP POST data in k=v&k2=v2... form or null
           * @returns {Object} options - A dictionary of keyword arguments sufficient
           *      to configure this tile sources constructor.
           */
          configure: function(r, o, l) {
            let a;
            return e.isPlainObject(r) ? a = i(this, r) : a = t(this, r), o && !a.tilesUrl && (a.tilesUrl = o.replace(
              /([^/]+?)(\.(dzi|xml|js)?(\?[^/]*)?)?\/?$/,
              "$1_files/"
            ), o.search(/\.(dzi|xml|js)\?/) !== -1 ? a.queryParams = o.match(/\?.*/) : a.queryParams = ""), a;
          },
          /**
           * @function
           * @param {Number} level
           * @param {Number} x
           * @param {Number} y
           */
          getTileUrl: function(r, o, l) {
            return [this.tilesUrl, r, "/", o, "_", l, ".", this.fileFormat, this.queryParams].join("");
          },
          /**
           * Equality comparator
           */
          equals: function(r) {
            return this.tilesUrl === r.tilesUrl;
          },
          /**
           * @function
           * @param {Number} level
           * @param {Number} x
           * @param {Number} y
           */
          tileExists: function(r, o, l) {
            const a = this._levelRects[r];
            let c, d, f, p, b;
            if (this.minLevel && r < this.minLevel || this.maxLevel && r > this.maxLevel)
              return !1;
            if (!a || !a.length)
              return !0;
            for (let x = a.length - 1; x >= 0; x--) {
              const T = a[x];
              if (!(r < T.minLevel || r > T.maxLevel) && (c = this.getLevelScale(r), d = T.x * c, f = T.y * c, p = d + T.width * c, b = f + T.height * c, d = Math.floor(d / this._tileWidth), f = Math.floor(f / this._tileWidth), p = Math.ceil(p / this._tileWidth), b = Math.ceil(b / this._tileWidth), d <= o && o < p && f <= l && l < b))
                return !0;
            }
            return !1;
          }
        }
      );
      function t(r, o) {
        if (!o || !o.documentElement)
          throw new Error(e.getString("Errors.Xml"));
        const l = o.documentElement, a = l.localName || l.tagName, c = o.documentElement.namespaceURI;
        let d = null;
        const f = [];
        let p, b, x, T, I;
        if (a === "Image")
          try {
            if (T = l.getElementsByTagName("Size")[0], T === void 0 && (T = l.getElementsByTagNameNS(c, "Size")[0]), d = {
              Image: {
                xmlns: "http://schemas.microsoft.com/deepzoom/2008",
                Url: l.getAttribute("Url"),
                Format: l.getAttribute("Format"),
                DisplayRect: null,
                Overlap: parseInt(l.getAttribute("Overlap"), 10),
                TileSize: parseInt(l.getAttribute("TileSize"), 10),
                Size: {
                  Height: parseInt(T.getAttribute("Height"), 10),
                  Width: parseInt(T.getAttribute("Width"), 10)
                }
              }
            }, !e.imageFormatSupported(d.Image.Format))
              throw new Error(
                e.getString("Errors.ImageFormat", d.Image.Format.toUpperCase())
              );
            for (p = l.getElementsByTagName("DisplayRect"), p === void 0 && (p = l.getElementsByTagNameNS(c, "DisplayRect")[0]), I = 0; I < p.length; I++)
              b = p[I], x = b.getElementsByTagName("Rect")[0], x === void 0 && (x = b.getElementsByTagNameNS(c, "Rect")[0]), f.push({
                Rect: {
                  X: parseInt(x.getAttribute("X"), 10),
                  Y: parseInt(x.getAttribute("Y"), 10),
                  Width: parseInt(x.getAttribute("Width"), 10),
                  Height: parseInt(x.getAttribute("Height"), 10),
                  MinLevel: parseInt(b.getAttribute("MinLevel"), 10),
                  MaxLevel: parseInt(b.getAttribute("MaxLevel"), 10)
                }
              });
            return f.length && (d.Image.DisplayRect = f), i(r, d);
          } catch (z) {
            throw z instanceof Error ? z : new Error(e.getString("Errors.Dzi"));
          }
        else {
          if (a === "Collection")
            throw new Error(e.getString("Errors.Dzc"));
          if (a === "Error") {
            const W = l.getElementsByTagName("Message")[0].firstChild.nodeValue;
            throw new Error(W);
          }
        }
        throw new Error(e.getString("Errors.Dzi"));
      }
      function i(r, o) {
        const l = o.Image, a = l.Url, c = l.Format, d = l.Size, f = l.DisplayRect || [], p = parseInt(d.Width, 10), b = parseInt(d.Height, 10), x = parseInt(l.TileSize, 10), T = parseInt(l.Overlap, 10), I = [];
        let z;
        for (let W = 0; W < f.length; W++)
          z = f[W].Rect, I.push(new e.DisplayRect(
            parseInt(z.X, 10),
            parseInt(z.Y, 10),
            parseInt(z.Width, 10),
            parseInt(z.Height, 10),
            parseInt(z.MinLevel, 10),
            parseInt(z.MaxLevel, 10)
          ));
        return e.extend(!0, {
          width: p,
          /* width *required */
          height: b,
          /* height *required */
          tileSize: x,
          /* tileSize *required */
          tileOverlap: T,
          /* tileOverlap *required */
          minLevel: null,
          /* minLevel */
          maxLevel: null,
          /* maxLevel */
          tilesUrl: a,
          /* tilesUrl */
          fileFormat: c,
          /* fileFormat */
          displayRects: I
          /* displayRects */
        }, o);
      }
    })(n), (function(e) {
      e.IIIFTileSource = function(a) {
        if (e.extend(!0, this, a), this._id = this["@id"] || this.id || this.identifier || null, !(this.height && this.width && this._id))
          throw new Error("IIIF required parameters (width, height, or id) not provided.");
        if (a.tileSizePerScaleFactor = {}, this.tileFormat = this.tileFormat || "jpg", this.version = a.version, this.isLevel0 = t(a), this.tile_width && this.tile_height)
          a.tileWidth = this.tile_width, a.tileHeight = this.tile_height;
        else if (this.tile_width)
          a.tileSize = this.tile_width;
        else if (this.tile_height)
          a.tileSize = this.tile_height;
        else if (this.tiles)
          if (this.tiles.length === 1)
            a.tileWidth = this.tiles[0].width, a.tileHeight = this.tiles[0].height || this.tiles[0].width, this.scale_factors = this.tiles[0].scaleFactors;
          else {
            this.scale_factors = [];
            for (let c = 0; c < this.tiles.length; c++)
              for (let d = 0; d < this.tiles[c].scaleFactors.length; d++) {
                const f = this.tiles[c].scaleFactors[d];
                this.scale_factors.push(f), a.tileSizePerScaleFactor[f] = {
                  width: this.tiles[c].width,
                  height: this.tiles[c].height || this.tiles[c].width
                };
              }
          }
        else if (i(a)) {
          const c = Math.min(this.height, this.width), d = [256, 512, 1024], f = [];
          for (let p = 0; p < d.length; p++)
            d[p] <= c && f.push(d[p]);
          f.length > 0 ? a.tileSize = Math.max.apply(null, f) : a.tileSize = c;
        } else this.sizes && this.sizes.length > 0 ? (this.emulateLegacyImagePyramid = !0, a.levels = r(this), e.extend(!0, a, {
          width: a.levels[a.levels.length - 1].width,
          height: a.levels[a.levels.length - 1].height,
          tileSize: Math.max(a.height, a.width),
          tileOverlap: 0,
          minLevel: 0,
          maxLevel: a.levels.length - 1
        }), this.levels = a.levels) : e.console.error("Nothing in the info.json to construct image pyramids from");
        if (!a.maxLevel && !this.emulateLegacyImagePyramid)
          if (!this.scale_factors)
            a.maxLevel = Number(Math.round(Math.log(Math.max(this.width, this.height), 2)));
          else {
            const c = Math.max.apply(null, this.scale_factors);
            a.maxLevel = Math.round(Math.log(c) * Math.LOG2E);
          }
        if (this.sizes) {
          let c = this.sizes.length;
          const d = this.sizes.slice().sort((f, p) => f.width - p.width);
          if (d[c - 1].width < this.width && d[c - 1].height < this.height && (d.push({ width: this.width, height: this.height }), c++), c === a.maxLevel + 1) {
            let f = 1;
            if (this.scale_factors && this.scale_factors.length === c)
              for (let p = 0; p < c; p++) {
                const b = this.scale_factors[c - p - 1];
                if (Math.round(this.width / d[p].width) !== b || Math.round(this.height / d[p].height) !== b) {
                  f = 0;
                  break;
                }
              }
            f === 1 && (this.levelSizes = d);
          }
        }
        e.TileSource.apply(this, [a]);
      }, e.extend(
        e.IIIFTileSource.prototype,
        e.TileSource.prototype,
        /** @lends OpenSeadragon.IIIFTileSource.prototype */
        {
          /**
           * Determine if the data and/or url imply the image service is supported by
           * this tile source.
           * @function
           * @param {Object|Array} data
           * @param {String} [url] - url
           */
          supports: function(a, c) {
            return a.protocol && a.protocol === "http://iiif.io/api/image" || a["@context"] && (a["@context"] === "http://library.stanford.edu/iiif/image-api/1.1/context.json" || a["@context"] === "http://iiif.io/api/image/1/context.json") || a.profile && a.profile.indexOf("http://library.stanford.edu/iiif/image-api/compliance.html") === 0 || a.identifier && a.width && a.height ? !0 : !!(a.documentElement && a.documentElement.tagName === "info" && a.documentElement.namespaceURI === "http://library.stanford.edu/iiif/image-api/ns/");
          },
          /**
           * A static function used to prepare an incoming IIIF Image API info.json
           * response for processing by the tile handler. Normalizes data for all
           * versions of IIIF (1.0, 1.1, 2.x, 3.x) and returns a data object that
           * may be passed to the IIIFTileSource.
           *
           * @function
           * @static
           * @param {Object} data - the raw configuration
           * @param {String} url - the url configuration was retrieved from
           * @param {String} postData - HTTP POST data in k=v&k2=v2... form or null
           * @returns {Object} A normalized IIIF data object
           * @example <caption>IIIF 2.x Info Looks like this</caption>
           * {
           * "@context": "http://iiif.io/api/image/2/context.json",
           * "@id": "http://iiif.example.com/prefix/1E34750D-38DB-4825-A38A-B60A345E591C",
           * "protocol": "http://iiif.io/api/image",
           * "height": 1024,
           * "width": 775,
           * "tiles" : [{"width":256, "scaleFactors":[1,2,4,8]}],
           *  "profile": ["http://iiif.io/api/image/2/level1.json", {
           *    "qualities": [ "native", "bitonal", "grey", "color" ],
           *    "formats": [ "jpg", "png", "gif" ]
           *   }]
           * }
           */
          configure: function(a, c, d) {
            if (e.isPlainObject(a)) {
              if (!a["@context"])
                a["@context"] = "http://iiif.io/api/image/1.0/context.json", a["@id"] = c.replace("/info.json", ""), a.version = 1;
              else {
                let f = a["@context"];
                if (Array.isArray(f)) {
                  for (let p = 0; p < f.length; p++)
                    if (typeof f[p] == "string" && (/^http:\/\/iiif\.io\/api\/image\/[1-3]\/context\.json$/.test(f[p]) || f[p] === "http://library.stanford.edu/iiif/image-api/1.1/context.json")) {
                      f = f[p];
                      break;
                    }
                }
                switch (f) {
                  case "http://iiif.io/api/image/1/context.json":
                  case "http://library.stanford.edu/iiif/image-api/1.1/context.json":
                    a.version = 1;
                    break;
                  case "http://iiif.io/api/image/2/context.json":
                    a.version = 2;
                    break;
                  case "http://iiif.io/api/image/3/context.json":
                    a.version = 3;
                    break;
                  default:
                    e.console.error("Data has a @context property which contains no known IIIF context URI.");
                }
              }
              if (a.preferredFormats) {
                for (let f = 0; f < a.preferredFormats.length; f++)
                  if (e.imageFormatSupported(a.preferredFormats[f])) {
                    a.tileFormat = a.preferredFormats[f];
                    break;
                  }
              }
              return a;
            } else {
              const f = o(a);
              return f["@context"] = "http://iiif.io/api/image/1.0/context.json", f["@id"] = c.replace("/info.xml", ""), f.version = 1, f;
            }
          },
          /**
           * Return the tileWidth for the given level.
           * @function
           * @param {Number} level
           */
          getTileWidth: function(a) {
            if (this.emulateLegacyImagePyramid)
              return e.TileSource.prototype.getTileWidth.call(this, a);
            const c = Math.pow(2, this.maxLevel - a);
            return this.tileSizePerScaleFactor && this.tileSizePerScaleFactor[c] ? this.tileSizePerScaleFactor[c].width : this._tileWidth;
          },
          /**
           * Return the tileHeight for the given level.
           * @function
           * @param {Number} level
           */
          getTileHeight: function(a) {
            if (this.emulateLegacyImagePyramid)
              return e.TileSource.prototype.getTileHeight.call(this, a);
            const c = Math.pow(2, this.maxLevel - a);
            return this.tileSizePerScaleFactor && this.tileSizePerScaleFactor[c] ? this.tileSizePerScaleFactor[c].height : this._tileHeight;
          },
          /**
           * @function
           * @param {Number} level
           */
          getLevelScale: function(a) {
            if (this.emulateLegacyImagePyramid) {
              let c = NaN;
              return this.levels.length > 0 && a >= this.minLevel && a <= this.maxLevel && (c = this.levels[a].width / this.levels[this.maxLevel].width), c;
            }
            return e.TileSource.prototype.getLevelScale.call(this, a);
          },
          /**
           * @function
           * @param {Number} level
           */
          getNumTiles: function(a) {
            if (this.emulateLegacyImagePyramid)
              return this.getLevelScale(a) ? new e.Point(1, 1) : new e.Point(0, 0);
            if (this.levelSizes) {
              const c = this.levelSizes[a], d = Math.ceil(c.width / this.getTileWidth(a)), f = Math.ceil(c.height / this.getTileHeight(a));
              return new e.Point(d, f);
            } else
              return e.TileSource.prototype.getNumTiles.call(this, a);
          },
          /**
           * @function
           * @param {Number} level
           * @param {OpenSeadragon.Point} point
           */
          getTileAtPoint: function(a, c) {
            if (this.emulateLegacyImagePyramid)
              return new e.Point(0, 0);
            if (this.levelSizes) {
              const d = c.x >= 0 && c.x <= 1 && c.y >= 0 && c.y <= 1 / this.aspectRatio;
              e.console.assert(d, "[TileSource.getTileAtPoint] must be called with a valid point.");
              const f = this.levelSizes[a].width, p = c.x * f, b = c.y * f;
              let x = Math.floor(p / this.getTileWidth(a)), T = Math.floor(b / this.getTileHeight(a));
              return c.x >= 1 && (x = this.getNumTiles(a).x - 1), c.y >= 1 / this.aspectRatio - 1e-15 && (T = this.getNumTiles(a).y - 1), new e.Point(x, T);
            }
            return e.TileSource.prototype.getTileAtPoint.call(this, a, c);
          },
          /**
           * Responsible for retrieving the url which will return an image for the
           * region specified by the given x, y, and level components.
           * @function
           * @param {Number} level - z index
           * @param {Number} x
           * @param {Number} y
           * @throws {Error}
           */
          getTileUrl: function(a, c, d) {
            if (this.emulateLegacyImagePyramid) {
              let De = null;
              return this.levels.length > 0 && a >= this.minLevel && a <= this.maxLevel && (De = this.levels[a].url), De;
            }
            const f = "0", p = Math.pow(0.5, this.maxLevel - a);
            let b, x, T, I, z, W, j, oe, ae, se, fe, Ie, Ae, qe, Ne;
            return this.levelSizes ? (b = this.levelSizes[a].width, x = this.levelSizes[a].height) : (b = Math.ceil(this.width * p), x = Math.ceil(this.height * p)), T = this.getTileWidth(a), I = this.getTileHeight(a), z = Math.round(T / p), W = Math.round(I / p), this.version === 1 ? Ne = "native." + this.tileFormat : Ne = "default." + this.tileFormat, b < T && x < I ? (this.version === 2 && b === this.width ? Ie = "full" : this.version === 3 && b === this.width && x === this.height ? Ie = "max" : this.version === 3 ? Ie = b + "," + x : Ie = b + ",", j = "full") : (oe = c * z, ae = d * W, se = Math.min(z, this.width - oe), fe = Math.min(W, this.height - ae), c === 0 && d === 0 && se === this.width && fe === this.height ? j = "full" : j = [oe, ae, se, fe].join(","), Ae = Math.min(T, b - c * T), qe = Math.min(I, x - d * I), this.version === 2 && Ae === this.width ? Ie = "full" : this.version === 3 && Ae === this.width && qe === this.height ? Ie = "max" : this.isLevel0 && this.version < 3 ? Ie = Ae + "," : Ie = Ae + "," + qe), [this._id, j, Ie, f, Ne].join("/");
          },
          /**
           * Equality comparator
           */
          equals: function(a) {
            return this._id === a._id;
          },
          __testonly__: {
            canBeTiled: i,
            constructLevels: r
          }
        }
      );
      function t(a) {
        const c = [
          "http://library.stanford.edu/iiif/image-api/compliance.html#level0",
          "http://library.stanford.edu/iiif/image-api/1.1/compliance.html#level0",
          "http://iiif.io/api/image/2/level0.json",
          "level0",
          "https://iiif.io/api/image/3/level0.json"
        ], d = Array.isArray(a.profile) ? a.profile[0] : a.profile;
        return c.indexOf(d) !== -1;
      }
      function i(a) {
        const c = t(a);
        let d = !1;
        return a.version === 2 && a.profile.length > 1 && a.profile[1].supports && (d = a.profile[1].supports.indexOf("sizeByW") !== -1), a.version === 3 && a.extraFeatures && (d = a.extraFeatures.indexOf("sizeByWh") !== -1), !c || d;
      }
      function r(a) {
        const c = [];
        for (let d = 0; d < a.sizes.length; d++)
          c.push({
            url: a._id + "/full/" + a.sizes[d].width + "," + (a.version === 3 ? a.sizes[d].height : "") + "/0/default." + a.tileFormat,
            width: a.sizes[d].width,
            height: a.sizes[d].height
          });
        return c.sort(function(d, f) {
          return d.width - f.width;
        });
      }
      function o(a) {
        if (!a || !a.documentElement)
          throw new Error(e.getString("Errors.Xml"));
        const c = a.documentElement, d = c.tagName;
        let f = null;
        if (d === "info")
          try {
            return f = {}, l(c, f), f;
          } catch (p) {
            throw p instanceof Error ? p : new Error(e.getString("Errors.IIIF"));
          }
        throw new Error(e.getString("Errors.IIIF"));
      }
      function l(a, c, d) {
        if (a.nodeType === 3 && d) {
          let f = a.nodeValue.trim();
          f.match(/^\d*$/) && (f = Number(f)), c[d] ? (e.isArray(c[d]) || (c[d] = [c[d]]), c[d].push(f)) : c[d] = f;
        } else if (a.nodeType === 1)
          for (let f = 0; f < a.childNodes.length; f++)
            l(a.childNodes[f], c, a.nodeName);
      }
    })(n), (function(e) {
      e.IIPTileSource = function(t) {
        if (e.EventSource.call(this), t && t.iipsrv && t.image) {
          e.extend(this, t), this.aspectRatio = 1, this.dimensions = new e.Point(10, 10), this._tileWidth = 0, this._tileHeight = 0, this.tileOverlap = 0, this.minLevel = 0, this.maxLevel = 0, this.ready = !1;
          const i = this.getMetadataUrl();
          this.getImageInfo(i);
        }
      }, e.extend(
        e.IIPTileSource.prototype,
        e.TileSource.prototype,
        /** @lends OpenSeadragon.IIPTileSource.prototype */
        {
          /**
           * Return URL string for image metadata
           * @function
           * @returns {String} url - The IIP URL needed for image metadata
           */
          getMetadataUrl: function() {
            return this.iipsrv + "?FIF=" + this.image + "&obj=IIP,1.0&obj=Max-size&obj=Tile-size&obj=Resolution-number&obj=Resolutions";
          },
          /**
           * Determine if the data and/or url imply the image service is supported by
           * this tile source.
           * @function
           * @param {Object|Array} data
           * @param {String} optional - url
           */
          supports: function(t, i) {
            return t && "iipsrv" in t && "image" in t;
          },
          /**
           * Parse IIP protocol response
           * @function
           * @param {Object|Array} data - raw metadata from an IIP server
           */
          parseIIP: function(t) {
            let i = t.split("Max-size:");
            if (!i[1])
              throw new Error("No Max-size returned");
            let r = i[1].split(" ");
            if (this.width = parseInt(r[0], 10), this.height = parseInt(r[1], 10), this.dimensions = new e.Point(this.width, this.height), this.aspectRatio = this.width / this.height, i = t.split("Tile-size:"), !i[1])
              throw new Error("No Tile-size returned");
            r = i[1].split(" "), this._tileWidth = parseInt(r[0], 10), this._tileHeight = parseInt(r[1], 10), i = t.split("Resolution-number:");
            const o = parseInt(i[1], 10);
            this.minLevel = 0, this.maxLevel = o - 1, this.tileOverlap = 0, i = t.split("Resolutions:"), r = i[1].split(",");
            const l = r.length;
            this.levelSizes = new Array(l);
            for (let a = 0; a < l; a++) {
              const c = r[a].split(" "), d = parseInt(c[0], 10), f = parseInt(c[1], 10);
              this.levelSizes[a] = { width: d, height: f };
            }
          },
          /**
           * Retrieve image metadata from an IIP-compatible server
           *
           * @function
           * @param {String} url
           * @throws {Error}
           */
          getImageInfo: function(t) {
            const i = this;
            e.makeAjaxRequest({
              url: t,
              type: "GET",
              async: !1,
              withCredentials: this.ajaxWithCredentials,
              headers: this.ajaxHeaders,
              success: function(r) {
                try {
                  n.IIPTileSource.prototype.parseIIP.call(i, r.responseText), i.ready = !0, i.raiseEvent("ready", { tileSource: i });
                } catch (o) {
                  const l = "IIPTileSource: Error parsing IIP metadata: " + o.message;
                  i.raiseEvent("open-failed", { message: l, source: t });
                }
              },
              error: function(r, o) {
                const l = "IIPTileSource: Unable to get IIP metadata from " + t;
                e.console.error(l), i.raiseEvent("open-failed", { message: l, source: t });
              }
            });
          },
          /**
           * Parse and configure the image metadata
           * @function
           * @param {String|Object|Array|Document} data
           * @param {String} url - the url the data was loaded
           *      from if any.
           * @param {String} postData - HTTP POST data in k=v&k2=v2... form or null value obtained from
           *      the protocol URL after '#' sign if flag splitHashDataForPost set to 'true'
           * @returns {Object} options - A dictionary of keyword arguments sufficient
           *      to configure the tile source constructor (include all values you want to
           *      instantiate the TileSource subclass with - what _options_ object should contain).
           * @throws {Error}
           */
          configure: function(t, i, r) {
            return t;
          },
          /**
           * @function
           * @param {Number} level
           */
          getNumTiles: function(t) {
            const i = this.levelSizes[t];
            let r = Math.ceil(i.width / this._tileWidth), o = Math.ceil(i.height / this._tileHeight);
            return new e.Point(r, o);
          },
          /**
           * Determine the url which will return an image for the region specified by the given x, y, and level components.
           * Takes into account image processing parameters that have been set in constructor
           * @function
           * @param {Number} level
           * @param {Number} x
           * @param {Number} y
           */
          getTileUrl: function(t, i, r) {
            const o = this.levelSizes[t], l = Math.ceil(o.width / this._tileWidth);
            let a = this.iipsrv + "?FIF=" + this.image + "&";
            this.transform && (this.transform.stack && (a += "SDS=" + this.transform.stack + "&"), this.transform.contrast && (a += "CNT=" + this.transform.contrast + "&"), this.transform.gamma && (a += "GAM=" + this.transform.gamma + "&"), this.transform.invert && this.transform.invert === !0 && (a += "INV&"), this.transform.color && (a += "COL=" + this.transform.color + "&"), this.transform.twist && (a += "CTW=" + this.transform.twist + "&"), this.transform.convolution && (a += "CNV=" + this.transform.convolution + "&"), this.transform.quality && (a += "QLT=" + this.transform.quality + "&"), this.transform.colormap && (a += "CMP=" + this.transform.colormap + "&"), this.transform.minmax && (a += "MINMAX=" + this.transform.minmax + "&"), this.transform.hillshade && (a += "SHD=" + this.transform.hillshade + "&"));
            let c = "JTL";
            this.format === "png" ? c = "PTL" : this.format === "webp" ? c = "WTL" : this.format === "avif" && (c = "ATL");
            const d = r * l + i;
            return a + c + "=" + t + "," + d;
          }
        }
      ), e.extend(!0, e.IIPTileSource.prototype, e.EventSource.prototype);
    })(n), (function(e) {
      e.IrisTileSource = function(t) {
        if (e.TileSource.apply(this, [t]), !t.serverUrl || !t.slideId)
          throw new Error("IrisTileSource requires serverUrl and slideId");
        if (this.serverUrl = t.serverUrl, this.slideId = t.slideId, this.ready = !1, t.metadata)
          this.parseMetadata(t.metadata), this.ready = !0, this.raiseEvent("ready", { tileSource: this });
        else {
          const i = this.getMetadataUrl();
          this.getImageInfo(i);
        }
      }, e.extend(e.IrisTileSource.prototype, e.TileSource.prototype, {
        /**
         * Return URL string for image metadata
         * @function
         * @returns {String} url - The Iris metadata URL
         */
        getMetadataUrl: function() {
          return this.serverUrl + "/slides/" + this.slideId + "/metadata";
        },
        /**
         * Determine if the data implies the image service is supported by this tile source.
         * @function
         * @param {Object} data - The raw metadata object to check
         * @returns {Boolean} - True if supported, false otherwise
         */
        supports: function(t) {
          return t && t.type === "iris" && t.serverUrl && t.slideId;
        },
        /**
         * Parse Iris protocol metadata response
         * @function
         * @param {Object} data - Raw metadata from Iris server
         */
        parseMetadata: function(t) {
          this._tileWidth = 256, this._tileHeight = 256, this.tileSize = this._tileWidth, this.tileOverlap = 0;
          const i = t.extent.layers, r = i.length - 1, o = i[r].scale;
          this.width = Math.ceil(t.extent.width * o), this.height = Math.ceil(t.extent.height * o), this.dimensions = new e.Point(this.width, this.height), this.aspectRatio = this.width / this.height, this.levelSizes = i.map((l) => ({
            width: Math.ceil(l.x_tiles * this._tileWidth),
            height: Math.ceil(l.y_tiles * this._tileHeight),
            xTiles: Math.ceil(l.x_tiles),
            yTiles: Math.ceil(l.y_tiles)
          })), this.levelScales = i.map((l) => l.scale / o), this.minLevel = 0, this.maxLevel = Math.ceil(this.levelSizes.length - 1);
        },
        /**
         * Retrieve image metadata from an Iris-compatible server
         * @function
         * @param {String} url - The metadata URL
         */
        getImageInfo: function(t) {
          const i = this;
          e.makeAjaxRequest({
            url: t,
            type: "GET",
            async: !0,
            success: function(r) {
              try {
                const o = JSON.parse(r.responseText);
                i.parseMetadata(o), i.ready = !0, i.raiseEvent("ready", { tileSource: i });
              } catch (o) {
                const l = "IrisTileSource: Error parsing metadata: " + o.message;
                e.console.error(l), i.raiseEvent("open-failed", { message: l, source: t });
              }
            },
            error: function(r, o) {
              const l = "IrisTileSource: Unable to get metadata from " + t;
              e.console.error(l), i.raiseEvent("open-failed", { message: l, source: t });
            }
          });
        },
        /**
         * Get the number of tiles at a given level
         * @function
         * @param {Number} level - The image depth level
         * @returns {OpenSeadragon.Point} - Number of tiles in x and y directions
         */
        getNumTiles: function(t) {
          return t < this.minLevel || t > this.maxLevel || !this.levelSizes[t] ? new e.Point(0, 0) : new e.Point(
            Math.ceil(this.levelSizes[t].xTiles),
            Math.ceil(this.levelSizes[t].yTiles)
          );
        },
        /**
         * Determine the URL which will return an image for the region specified by the given x, y, and level components.
         * @function
         * @param {Number} level - The zoom level
         * @param {Number} x - The x tile index
         * @param {Number} y - The y tile index
         * @returns {String} - The tile URL
         */
        getTileUrl: function(t, i, r) {
          const o = r * this.levelSizes[t].xTiles + i;
          return `${this.serverUrl}/slides/${this.slideId}/layers/${t}/tiles/${o}`;
        },
        /**
         * Get the scale for a given level
         * @function
         * @param {Number} level - The image depth level
         * @returns {Number} - The scale for the level
         */
        getLevelScale: function(t) {
          return this.levelScales[t];
        },
        /**
         * Retrieve and immediately return the options object
         * @function
         * @param {Object} options - Options object
         * @returns {Object} - The options object
         */
        configure: function(t) {
          return t;
        }
      }), e.extend(!0, e.IrisTileSource.prototype, e.EventSource.prototype);
    })(n), (function(e) {
      e.OsmTileSource = function(t, i, r, o, l) {
        let a;
        e.isPlainObject(t) ? a = t : a = {
          width: arguments[0],
          height: arguments[1],
          tileSize: arguments[2],
          tileOverlap: arguments[3],
          tilesUrl: arguments[4]
        }, (!a.width || !a.height) && (a.width = 65572864, a.height = 65572864), a.tileSize || (a.tileSize = 256, a.tileOverlap = 0), a.tilesUrl || (a.tilesUrl = "http://tile.openstreetmap.org/"), a.minLevel = 8, e.TileSource.apply(this, [a]);
      }, e.extend(
        e.OsmTileSource.prototype,
        e.TileSource.prototype,
        /** @lends OpenSeadragon.OsmTileSource.prototype */
        {
          /**
           * Determine if the data and/or url imply the image service is supported by
           * this tile source.
           * @function
           * @param {Object|Array} data
           * @param {String} optional - url
           */
          supports: function(t, i) {
            return t.type && t.type === "openstreetmaps";
          },
          /**
           *
           * @function
           * @param {Object} data - the raw configuration
           * @param {String} url - the url the data was retrieved from if any.
           * @param {String} postData - HTTP POST data in k=v&k2=v2... form or null
           * @returns {Object} options - A dictionary of keyword arguments sufficient
           *      to configure this tile sources constructor.
           */
          configure: function(t, i, r) {
            return t;
          },
          /**
           * @function
           * @param {Number} level
           * @param {Number} x
           * @param {Number} y
           */
          getTileUrl: function(t, i, r) {
            return this.tilesUrl + (t - 8) + "/" + i + "/" + r + ".png";
          },
          /**
           * Equality comparator
           */
          equals: function(t) {
            return this.tilesUrl === t.tilesUrl;
          }
        }
      );
    })(n), (function(e) {
      e.TmsTileSource = function(t, i, r, o, l) {
        let a;
        e.isPlainObject(t) ? a = t : a = {
          width: arguments[0],
          height: arguments[1],
          tileSize: arguments[2],
          tileOverlap: arguments[3],
          tilesUrl: arguments[4]
        };
        const c = Math.ceil(a.width / 256) * 256, d = Math.ceil(a.height / 256) * 256;
        let f;
        c > d ? f = c / 256 : f = d / 256, a.maxLevel = Math.ceil(Math.log(f) / Math.log(2)) - 1, a.tileSize = 256, a.width = c, a.height = d, e.TileSource.apply(this, [a]);
      }, e.extend(
        e.TmsTileSource.prototype,
        e.TileSource.prototype,
        /** @lends OpenSeadragon.TmsTileSource.prototype */
        {
          /**
           * Determine if the data and/or url imply the image service is supported by
           * this tile source.
           * @function
           * @param {Object|Array} data
           * @param {String} optional - url
           */
          supports: function(t, i) {
            return t.type && t.type === "tiledmapservice";
          },
          /**
           *
           * @function
           * @param {Object} data - the raw configuration
           * @param {String} url - the url the data was retrieved from if any.
           * @param {String} postData - HTTP POST data in k=v&k2=v2... form or null
           * @returns {Object} options - A dictionary of keyword arguments sufficient
           *      to configure this tile sources constructor.
           */
          configure: function(t, i, r) {
            return t;
          },
          /**
           * @function
           * @param {Number} level
           * @param {Number} x
           * @param {Number} y
           */
          getTileUrl: function(t, i, r) {
            const o = this.getNumTiles(t).y - 1;
            return this.tilesUrl + t + "/" + i + "/" + (o - r) + ".png";
          },
          /**
           * Equality comparator
           */
          equals: function(t) {
            return this.tilesUrl === t.tilesUrl;
          }
        }
      );
    })(n), (function(e) {
      e.ZoomifyTileSource = function(t) {
        typeof t.tileSize > "u" && (t.tileSize = 256), typeof t.fileFormat > "u" && (t.fileFormat = "jpg", this.fileFormat = t.fileFormat);
        const i = {
          x: t.width,
          y: t.height
        };
        for (t.imageSizes = [{
          x: t.width,
          y: t.height
        }], t.gridSize = [this._getGridSize(t.width, t.height, t.tileSize)]; parseInt(i.x, 10) > t.tileSize || parseInt(i.y, 10) > t.tileSize; )
          i.x = Math.floor(i.x / 2), i.y = Math.floor(i.y / 2), t.imageSizes.push({
            x: i.x,
            y: i.y
          }), t.gridSize.push(this._getGridSize(i.x, i.y, t.tileSize));
        t.imageSizes.reverse(), t.gridSize.reverse(), t.minLevel = 0, t.maxLevel = t.gridSize.length - 1, e.TileSource.apply(this, [t]);
      }, e.extend(
        e.ZoomifyTileSource.prototype,
        e.TileSource.prototype,
        /** @lends OpenSeadragon.ZoomifyTileSource.prototype */
        {
          //private
          _getGridSize: function(t, i, r) {
            return {
              x: Math.ceil(t / r),
              y: Math.ceil(i / r)
            };
          },
          //private
          _calculateAbsoluteTileNumber: function(t, i, r) {
            let o = 0, l = {};
            for (let a = 0; a < t; a++)
              l = this.gridSize[a], o += l.x * l.y;
            return l = this.gridSize[t], o += l.x * r + i, o;
          },
          /**
           * Determine if the data and/or url imply the image service is supported by
           * this tile source.
           * @function
           * @param {Object|Array} data
           * @param {String} optional - url
           */
          supports: function(t, i) {
            return t.type && t.type === "zoomifytileservice";
          },
          /**
           *
           * @function
           * @param {Object} data - the raw configuration
           * @param {String} url - the url the data was retrieved from if any.
           * @param {String} postData - HTTP POST data in k=v&k2=v2... form or null
           * @returns {Object} options - A dictionary of keyword arguments sufficient
           *      to configure this tile sources constructor.
           */
          configure: function(t, i, r) {
            return t;
          },
          /**
           * @function
           * @param {Number} level
           * @param {Number} x
           * @param {Number} y
           */
          getTileUrl: function(t, i, r) {
            let o = 0;
            const l = this._calculateAbsoluteTileNumber(t, i, r);
            return o = Math.floor(l / 256), this.tilesUrl + "TileGroup" + o + "/" + t + "-" + i + "-" + r + "." + this.fileFormat;
          },
          /**
           * Equality comparator
           */
          equals: function(t) {
            return this.tilesUrl === t.tilesUrl;
          }
        }
      );
    })(n), (function(e) {
      e.LegacyTileSource = function(o) {
        let l, a, c;
        e.isArray(o) && (l = {
          type: "legacy-image-pyramid",
          levels: o
        }), l.levels = t(l.levels), l.levels.length > 0 ? (a = l.levels[l.levels.length - 1].width, c = l.levels[l.levels.length - 1].height) : (a = 0, c = 0, e.console.error("No supported image formats found")), e.extend(!0, l, {
          width: a,
          height: c,
          tileSize: Math.max(c, a),
          tileOverlap: 0,
          minLevel: 0,
          maxLevel: l.levels.length > 0 ? l.levels.length - 1 : 0
        }), e.TileSource.apply(this, [l]), this.levels = l.levels;
      }, e.extend(
        e.LegacyTileSource.prototype,
        e.TileSource.prototype,
        /** @lends OpenSeadragon.LegacyTileSource.prototype */
        {
          /**
           * Determine if the data and/or url imply the image service is supported by
           * this tile source.
           * @function
           * @param {Object|Array} data
           * @param {String} optional - url
           */
          supports: function(o, l) {
            return o.type && o.type === "legacy-image-pyramid" || o.documentElement && o.documentElement.getAttribute("type") === "legacy-image-pyramid";
          },
          /**
           *
           * @function
           * @param {Object|XMLDocument} configuration - the raw configuration
           * @param {String} dataUrl - the url the data was retrieved from if any.
           * @param {String} postData - HTTP POST data in k=v&k2=v2... form or null
           * @returns {Object} options - A dictionary of keyword arguments sufficient
           *      to configure this tile sources constructor.
           */
          configure: function(o, l, a) {
            let c;
            return e.isPlainObject(o) ? c = r(this, o) : c = i(this, o), c;
          },
          /**
           * @function
           * @param {Number} level
           */
          getLevelScale: function(o) {
            let l = NaN;
            return this.levels.length > 0 && o >= this.minLevel && o <= this.maxLevel && (l = this.levels[o].width / this.levels[this.maxLevel].width), l;
          },
          /**
           * @function
           * @param {Number} level
           */
          getNumTiles: function(o) {
            return this.getLevelScale(o) ? new e.Point(1, 1) : new e.Point(0, 0);
          },
          /**
           * This method is not implemented by this class other than to throw an Error
           * announcing you have to implement it.  Because of the variety of tile
           * server technologies, and various specifications for building image
           * pyramids, this method is here to allow easy integration.
           * @function
           * @param {Number} level
           * @param {Number} x
           * @param {Number} y
           * @throws {Error}
           */
          getTileUrl: function(o, l, a) {
            let c = null;
            return this.levels.length > 0 && o >= this.minLevel && o <= this.maxLevel && (c = this.levels[o].url), c;
          },
          /**
           * Equality comparator
           */
          equals: function(o) {
            if (!o.levels || o.levels.length !== this.levels.length)
              return !1;
            for (let l = this.minLevel; l <= this.maxLevel; l++)
              if (this.levels[l].url !== o.levels[l].url)
                return !1;
            return !0;
          }
        }
      );
      function t(o) {
        const l = [];
        let a;
        for (let c = 0; c < o.length; c++)
          a = o[c], a.height && a.width && a.url ? l.push({
            url: a.url,
            width: Number(a.width),
            height: Number(a.height)
          }) : e.console.error("Unsupported image format: %s", a.url ? a.url : "<no URL>");
        return l.sort(function(c, d) {
          return c.height - d.height;
        });
      }
      function i(o, l) {
        if (!l || !l.documentElement)
          throw new Error(e.getString("Errors.Xml"));
        const a = l.documentElement, c = a.tagName;
        let d = null, f = [], p;
        if (c === "image")
          try {
            d = {
              type: a.getAttribute("type"),
              levels: []
            }, f = a.getElementsByTagName("level");
            for (let b = 0; b < f.length; b++)
              p = f[b], d.levels.push({
                url: p.getAttribute("url"),
                width: parseInt(p.getAttribute("width"), 10),
                height: parseInt(p.getAttribute("height"), 10)
              });
            return r(o, d);
          } catch (b) {
            throw b instanceof Error ? b : new Error("Unknown error parsing Legacy Image Pyramid XML.");
          }
        else {
          if (c === "collection")
            throw new Error("Legacy Image Pyramid Collections not yet supported.");
          if (c === "error")
            throw new Error("Error: " + l);
        }
        throw new Error("Unknown element " + c);
      }
      function r(o, l) {
        return l.levels;
      }
    })(n), (function(e) {
      e.ImageTileSource = class extends e.TileSource {
        constructor(t) {
          super(e.extend({
            buildPyramid: !0,
            crossOriginPolicy: !1,
            ajaxWithCredentials: !1
          }, t));
        }
        /**
         * Determine if the data and/or url imply the image service is supported by
         * this tile source.
         * @function
         * @param {Object|Array} data
         * @param {String} url - optional
         */
        supports(t, i) {
          return t.type && t.type === "image";
        }
        /**
         *
         * @function
         * @param {Object} options - the options
         * @param {String} dataUrl - the url the image was retrieved from, if any.
         * @param {String} postData - HTTP POST data in k=v&k2=v2... form or null
         * @returns {Object} options - A dictionary of keyword arguments sufficient
         *      to configure this tile sources constructor.
         */
        configure(t, i, r) {
          return t;
        }
        /**
         * Responsible for retrieving, and caching the
         * image metadata pertinent to this TileSources implementation.
         * @function
         * @param {String} url
         * @throws {Error}
         */
        getImageInfo(t) {
          const i = new Image(), r = this;
          this.crossOriginPolicy && (i.crossOrigin = this.crossOriginPolicy), this.ajaxWithCredentials && (i.useCredentials = this.ajaxWithCredentials), e.addEvent(i, "load", function() {
            r.width = i.naturalWidth, r.height = i.naturalHeight, r.aspectRatio = r.width / r.height, r.dimensions = new e.Point(r.width, r.height), r._tileWidth = r.width, r._tileHeight = r.height, r.tileOverlap = 0, r.minLevel = 0, r.image = i, r.levels = r._buildLevels(i), r.maxLevel = r.levels.length - 1, r.ready = !0, r.raiseEvent("ready", { tileSource: r });
          }), e.addEvent(i, "error", function() {
            r.image = null, r.raiseEvent("open-failed", {
              message: "Error loading image at " + t,
              source: t
            });
          }), this.loadTilesWithAjax && this.ajaxWithCredentials && this.ajaxHeaders ? super.downloadTileStart({
            src: t,
            loadWithAjax: !0,
            ajaxHeaders: this.ajaxHeaders,
            ajaxWithCredentials: this.ajaxWithCredentials,
            crossOriginPolicy: this.crossOriginPolicy,
            fail: (o, l) => {
              r.raiseEvent("open-failed", {
                message: `Error loading image per Ajax at ${t}`,
                source: t
              });
            },
            finish: (o, l) => {
              i.src = URL.createObjectURL(o);
            }
          }) : i.src = t;
        }
        /**
         * @function
         * @param {Number} level
         */
        getLevelScale(t) {
          let i = NaN;
          return t >= this.minLevel && t <= this.maxLevel && (i = this.levels[t].width / this.levels[this.maxLevel].width), i;
        }
        /**
         * @function
         * @param {Number} level
         */
        getNumTiles(t) {
          return this.getLevelScale(t) ? new e.Point(1, 1) : new e.Point(0, 0);
        }
        /**
         * Retrieves a tile url
         * @function
         * @param {Number} level Level of the tile
         * @param {Number} x x coordinate of the tile
         * @param {Number} y y coordinate of the tile
         */
        getTileUrl(t, i, r) {
          return t === this.maxLevel ? this.url : `${this.url}?l=${t}&x=${i}&y=${r}`;
        }
        /**
         * Equality comparator
         */
        equals(t) {
          return this.url === t.url;
        }
        getTilePostData(t, i, r) {
          return { level: t, x: i, y: r };
        }
        /**
         * Retrieves a tile context 2D
         * @deprecated
         */
        getContext2D(t, i, r) {
          return e.console.error("Using [TiledImage.getContext2D] (for plain images only) is deprecated. Use overridden downloadTileStart (https://openseadragon.github.io/examples/advanced-data-model/) instead."), this._createContext2D();
        }
        downloadTileStart(t) {
          const i = t.postData;
          if (i.level === this.maxLevel) {
            t.finish(this.image, null, "image");
            return;
          }
          if (i.level >= this.minLevel && i.level <= this.maxLevel) {
            const r = this.levels[i.level], o = this._createContext2D(this.image, r.width, r.height);
            t.finish(o, null, "context2d");
            return;
          }
          t.fail(`Invalid level ${i.level} for plain image source. Did you forget to set buildPyramid=true?`);
        }
        downloadTileAbort(t) {
        }
        // private
        //
        // Builds the different levels of the pyramid if possible
        // (i.e. if canvas API enabled and no canvas tainting issue).
        _buildLevels(t) {
          const i = [{
            url: t.src,
            width: t.naturalWidth,
            height: t.naturalHeight
          }];
          if (!this.buildPyramid || !e.supportsCanvas || !this.useCanvas)
            return i;
          let r = t.naturalWidth, o = t.naturalHeight;
          for (; r >= 2 && o >= 2; )
            r = Math.floor(r / 2), o = Math.floor(o / 2), i.push({
              width: r,
              height: o
            });
          return i.reverse();
        }
        _createContext2D(t, i, r) {
          const o = document.createElement("canvas"), l = o.getContext("2d");
          return o.width = i, o.height = r, l.drawImage(t, 0, 0, i, r), l;
        }
      };
    })(n), (function(e) {
      e.TileSourceCollection = function(t, i, r, o) {
        e.console.error("TileSourceCollection is deprecated; use World instead");
      };
    })(n), (function(e) {
      e.PriorityQueue = class {
        /**
         * @param {?OpenSeadragon.PriorityQueue} optHeap Optional Heap or
         *     Object to initialize heap with.
         */
        constructor(t = void 0) {
          this.nodes_ = [], t && this.insertAll(t);
        }
        /**
         * Insert the given value into the heap with the given key.
         * @param {K} key The key.
         * @param {V} value The value.
         */
        insert(t, i) {
          this.insertNode(new Node(t, i));
        }
        /**
         * Insert node item.
         * @param node
         */
        insertNode(t) {
          const i = this.nodes_;
          t.index = i.length, i.push(t), this.moveUp_(t.index);
        }
        /**
         * Adds multiple key-value pairs from another Heap or Object
         * @param {?OpenSeadragon.PriorityQueue} heap Object containing the data to add.
         */
        insertAll(t) {
          let i, r;
          if (t instanceof e.PriorityQueue) {
            if (i = t.getKeys(), r = t.getValues(), this.getCount() <= 0) {
              const o = this.nodes_;
              for (let l = 0; l < i.length; l++) {
                const a = new Node(i[l], r[l]);
                a.index = o.length, o.push(a);
              }
              return;
            }
          } else
            throw "insertAll supports only OpenSeadragon.PriorityQueue object!";
          for (let o = 0; o < i.length; o++)
            this.insert(i[o], r[o]);
        }
        /**
         * Retrieves and removes the root value of this heap.
         * @return {Node} The root node item removed from the root of the heap. Returns
         *     undefined if the heap is empty.
         */
        remove() {
          const t = this.nodes_, i = t.length, r = t[0];
          if (!(i <= 0))
            return i == 1 ? t.length = 0 : (t[0] = t.pop(), t[0] && (t[0].index = 0), this.moveDown_(0)), r && delete r.index, r;
        }
        /**
         * Retrieves but does not remove the root value of this heap.
         * @return {V} The value at the root of the heap. Returns
         *     undefined if the heap is empty.
         */
        peek() {
          const t = this.nodes_;
          if (t.length != 0)
            return t[0].value;
        }
        /**
         * Retrieves but does not remove the key of the root node of this heap.
         * @return {string} The key at the root of the heap. Returns undefined if the
         *     heap is empty.
         */
        peekKey() {
          return this.nodes_[0] && this.nodes_[0].key;
        }
        /**
         * Move the node up in hierarchy
         * @param {Node} node the node
         * @param {K} key new ley, must be smaller than current key
         */
        decreaseKey(t, i) {
          t.index === void 0 ? (t.key = i, this.insertNode(t)) : (t.key = i, this.moveUp_(t.index));
        }
        /**
         * Moves the node at the given index down to its proper place in the heap.
         * @param {number} index The index of the node to move down.
         * @private
         */
        moveDown_(t) {
          const i = this.nodes_, r = i.length, o = i[t];
          for (; t < r >> 1; ) {
            const l = this.getLeftChildIndex_(t), a = this.getRightChildIndex_(t), c = a < r && i[a].key < i[l].key ? a : l;
            if (i[c].key > o.key)
              break;
            i[t] = i[c], i[t].index = t, t = c;
          }
          i[t] = o, o && (o.index = t);
        }
        /**
         * Moves the node at the given index up to its proper place in the heap.
         * @param {number} index The index of the node to move up.
         * @private
         */
        moveUp_(t) {
          const i = this.nodes_, r = i[t];
          for (; t > 0; ) {
            const o = this.getParentIndex_(t);
            if (i[o].key > r.key)
              i[t] = i[o], i[t].index = t, t = o;
            else
              break;
          }
          i[t] = r, r && (r.index = t);
        }
        /**
         * Gets the index of the left child of the node at the given index.
         * @param {number} index The index of the node to get the left child for.
         * @return {number} The index of the left child.
         * @private
         */
        getLeftChildIndex_(t) {
          return t * 2 + 1;
        }
        /**
         * Gets the index of the right child of the node at the given index.
         * @param {number} index The index of the node to get the right child for.
         * @return {number} The index of the right child.
         * @private
         */
        getRightChildIndex_(t) {
          return t * 2 + 2;
        }
        /**
         * Gets the index of the parent of the node at the given index.
         * @param {number} index The index of the node to get the parent for.
         * @return {number} The index of the parent.
         * @private
         */
        getParentIndex_(t) {
          return t - 1 >> 1;
        }
        /**
         * Gets the values of the heap.
         * @return {!Array<*>} The values in the heap.
         */
        getValues() {
          const t = this.nodes_, i = [], r = t.length;
          for (let o = 0; o < r; o++)
            i.push(t[o].value);
          return i;
        }
        /**
         * Gets the keys of the heap.
         * @return {!Array<string>} The keys in the heap.
         */
        getKeys() {
          const t = this.nodes_, i = [], r = t.length;
          for (let o = 0; o < r; o++)
            i.push(t[o].key);
          return i;
        }
        /**
         * Whether the heap contains the given value.
         * @param {V} val The value to check for.
         * @return {boolean} Whether the heap contains the value.
         */
        containsValue(t) {
          return this.nodes_.some((i) => i.value == t);
        }
        /**
         * Whether the heap contains the given key.
         * @param {string} key The key to check for.
         * @return {boolean} Whether the heap contains the key.
         */
        containsKey(t) {
          return this.nodes_.some((i) => i.value == t);
        }
        /**
         * Clones a heap and returns a new heap
         * @return {!OpenSeadragon.PriorityQueue} A new Heap with the same key-value pairs.
         */
        clone() {
          return new e.PriorityQueue(this);
        }
        /**
         * The number of key-value pairs in the map
         * @return {number} The number of pairs.
         */
        getCount() {
          return this.nodes_.length;
        }
        /**
         * Returns true if this heap contains no elements.
         * @return {boolean} Whether this heap contains no elements.
         */
        isEmpty() {
          return this.nodes_.length === 0;
        }
        /**
         * Removes all elements from the heap.
         */
        clear() {
          this.nodes_.length = 0;
        }
      }, e.PriorityQueue.Node = class {
        constructor(t, i) {
          this.key = t, this.value = i, this.index = 0;
        }
        clone() {
          return new Node(this.key, this.value);
        }
      };
    })(n), (function(e) {
      class t {
        constructor() {
          this.adjacencyList = {}, this.vertices = {};
        }
        /**
         * Add vertex to graph
         * @param vertex unique vertex ID
         * @return {boolean} true if inserted, false if exists (no-op)
         */
        addVertex(r) {
          return this.vertices[r] ? !1 : (this.vertices[r] = new e.PriorityQueue.Node(0, r), this.adjacencyList[r] = [], !0);
        }
        /**
         * Add edge to graph
         * @param vertex1 id, must exist by calling addVertex()
         * @param vertex2 id, must exist by calling addVertex()
         * @param weight
         * @param transform function that transforms on path vertex1 -> vertex2
         * @return {boolean} true if new edge, false if replaced existing
         */
        addEdge(r, o, l, a) {
          l < 0 && e.console.error("WeightedGraph: negative weights will make for invalid shortest path computation!");
          const c = this.adjacencyList[r], d = c.findIndex((p) => p.target === this.vertices[o]), f = { target: this.vertices[o], origin: this.vertices[r], weight: l, transform: a };
          return d < 0 ? (this.adjacencyList[r].push(f), !0) : (this.adjacencyList[r][d] = f, !1);
        }
        /**
         * @return {{path: ConversionStep[], cost: number}|undefined} cheapest path from start to finish
         */
        dijkstra(r, o) {
          const l = [];
          if (r === o)
            return { path: l, cost: 0 };
          const a = new n.PriorityQueue();
          let c;
          for (let f in this.vertices)
            f = this.vertices[f], f.value === r ? (f.key = 0, a.insertNode(f)) : (f.key = 1 / 0, delete f.index), f._previous = null;
          for (; a.getCount() > 0 && (c = a.remove(), c.value !== o); ) {
            const f = this.adjacencyList[c.value];
            for (const p in f) {
              const b = f[p], x = c.key + b.weight, T = b.target;
              x < T.key && (T._previous = c, a.decreaseKey(T, x));
            }
          }
          if (!c || !c._previous || c.value !== o)
            return;
          const d = c.key;
          for (; c._previous; ) {
            const f = c.value, p = c._previous, b = p.value;
            l.push(this.adjacencyList[b].find((x) => x.target.value === f)), c = p;
          }
          return {
            path: l.reverse(),
            cost: d
          };
        }
      }
      e.DataTypeConverter = class {
        constructor() {
          this.graph = new t(), this.destructors = {}, this.copyings = {};
          const i = (o, l) => new e.Promise((a, c) => {
            if (!e.supportsAsync)
              throw "Not supported in sync mode!";
            const d = new Image();
            d.onerror = d.onabort = c, d.onload = () => a(d), o.tiledImage && o.tiledImage.crossOriginPolicy && (d.crossOrigin = o.tiledImage.crossOriginPolicy), d.src = l;
          }), r = (o, l) => {
            const a = document.createElement("canvas");
            a.width = l.width, a.height = l.height;
            const c = a.getContext("2d", { willReadFrequently: !0 });
            return c.drawImage(l, 0, 0), c;
          };
          this.learn("rasterBlob", "image", (o, l) => new e.Promise((a, c) => {
            const d = (window.URL || window.webkitURL).createObjectURL(l);
            e.supportsAsync || c("Not supported in sync mode!");
            const f = new Image();
            f.onerror = f.onabort = c, f.onload = () => {
              (window.URL || window.webkitURL).revokeObjectURL(l), a(f);
            }, f.src = d;
          })), this.learn("context2d", "rasterBlob", (o, l) => new e.Promise((a, c) => {
            e.supportsAsync || c("Not supported in sync mode!"), l.canvas.toBlob(a);
          })), this.learn("context2d", "imageUrl", (o, l) => l.canvas.toDataURL(), 1, 2), this.learn("image", "imageUrl", (o, l) => l.url), this.learn("image", "context2d", r, 1, 1), this.learn("imageUrl", "image", i, 1, 1), this.learn("image", "image", (o, l) => i(o, l.src), 1, 1), this.learn("imageUrl", "imageUrl", (o, l) => l, 0, 1), this.learn("context2d", "context2d", (o, l) => r(o, l.canvas)), this.learn("rasterBlob", "rasterBlob", (o, l) => l, 0, 1), this.learnDestroy("context2d", (o) => {
            o.canvas.width = 0, o.canvas.height = 0;
          });
        }
        /**
         * Unique identifier (unlike toString.call(x)) to be guessed
         * from the data value. This type guess is more strict than
         * OpenSeadragon.type() implementation, but for most type recognition
         * this test relies on the output of OpenSeadragon.type().
         *
         * Note: although we try to implement the type guessing, do
         * not rely on this functionality! Prefer explicit type declaration.
         *
         * @function guessType
         * @param x object to get unique identifier for
         *  - can be array, in that case, alphabetically-ordered list of inner unique types
         *    is returned (null, undefined are ignored)
         *  - if $.isPlainObject(x) is true, then the object can define
         *    getType function to specify its type
         *  - otherwise, toString.call(x) is applied to get the parameter description
         * @return {string} unique variable descriptor
         */
        guessType(i) {
          if (Array.isArray(i)) {
            const o = [];
            for (const l of i) {
              if (l == null)
                continue;
              const a = this.guessType(l);
              o.includes(a) || o.push(a);
            }
            return o.sort(), `Array [${o.join(",")}]`;
          }
          const r = e.type(i);
          return r === "dom-node" ? r.nodeName.toLowerCase() : r === "object" && e.isFunction(i.getType) ? i.getType() : r;
        }
        /**
         * Teach the system to convert data type 'from' -> 'to'
         * @param {string} from unique ID of the data item 'from'
         * @param {string} to unique ID of the data item 'to'
         * @param {OpenSeadragon.TypeConverter} callback converter that takes two arguments: a tile reference, and
         *  a data object of a type 'from'; and converts this data object to type 'to'. It can return also the value
         *  wrapped in a Promise (returned in resolve) or it can be async function.
         * @param {Number} [costPower=0] positive cost class of the conversion, smaller or equal than 7.
         *   Should reflect the actual cost of the conversion:
         *   - if nothing must be done and only reference is retrieved (or a constant operation done),
         *     return 0 (default)
         *   - if a linear amount of work is necessary,
         *     return 1
         *   ... and so on, basically the number in O() complexity power exponent (for simplification)
         * @param {Number} [costMultiplier=1] multiplier of the cost class, e.g. O(3n^2) would
         *   use costPower=2, costMultiplier=3; can be between 1 and 10^5
         */
        learn(i, r, o, l = 0, a = 1) {
          e.console.assert(l >= 0 && l <= 7, "[DataTypeConverter] Conversion costPower must be between <0, 7>."), e.console.assert(e.isFunction(o), "[DataTypeConverter:learn] Callback must be a valid function!"), i === r ? this.copyings[r] = o : (l++, a = Math.min(Math.max(a, 1), 15), this.graph.addVertex(i), this.graph.addVertex(r), this.graph.addEdge(i, r, l * 10 ^ 5 + a, o), this._known = {});
        }
        /**
         * Teach the system to destroy data type 'type'
         * for example, textures loaded to GPU have to be also manually removed when not needed anymore.
         * Needs to be defined only when the created object has extra deletion process.
         * @param {string} type
         * @param {OpenSeadragon.TypeDestructor} callback destructor, receives the object created,
         *   it is basically a type conversion to 'undefined' - thus the type.
         */
        learnDestroy(i, r) {
          this.destructors[i] = r;
        }
        /**
         * Convert data item x of type 'from' to any of the 'to' types, chosen is the cheapest known conversion.
         * Data is destroyed upon conversion. For different behavior, implement your conversion using the
         * path rules obtained from getConversionPath().
         * Note: conversion DOES NOT COPY data if [to] contains type 'from' (e.g., the cheapest conversion is no conversion).
         * It automatically calls destructor on immediate types, but NOT on the x and the result. You should call these
         * manually if these should be destroyed.
         * @param {OpenSeadragon.Tile} tile
         * @param {any} data data item to convert
         * @param {string} from data item type
         * @param {string} to desired type(s)
         * @return {OpenSeadragon.Promise<?>} promise resolution with type 'to' or undefined if the conversion failed
         */
        convert(i, r, o, ...l) {
          const a = this.getConversionPath(o, l);
          if (!a)
            return e.console.error(`[OpenSeadragon.converter.convert] Conversion ${o} ---> ${l} cannot be done!`), e.Promise.resolve();
          const c = a.length, d = this, f = (p, b, x = !0) => {
            if (b >= c)
              return e.Promise.resolve(p);
            const T = a[b], I = T.transform(i, p);
            return I === void 0 ? (e.console.error("[OpenSeadragon.converter.convert] data mid result undefined value (while converting using %s)", T), e.Promise.resolve()) : (x && d.destroy(p, T.origin.value), (e.type(I) === "promise" ? I : e.Promise.resolve(I)).then((W) => f(W, b + 1)));
          };
          return f(r, 0, !1);
        }
        /**
         * Copy the data item given.
         * @param {OpenSeadragon.Tile} tile
         * @param {any} data data item to convert
         * @param {string} type data type
         * @return {OpenSeadragon.Promise<?>|undefined} promise resolution with data passed from constructor
         */
        copy(i, r, o) {
          const l = this.copyings[o];
          if (l) {
            const a = l(i, r);
            return e.type(a) === "promise" ? a : e.Promise.resolve(a);
          }
          return e.console.warn("[OpenSeadragon.converter.copy] is not supported with type %s", o), e.Promise.resolve(void 0);
        }
        /**
         * Destroy the data item given.
         * @param {string} type data type
         * @param {any} data
         * @return {OpenSeadragon.Promise<any>|undefined} promise resolution with data passed from constructor, or undefined
         *  if not such conversion exists
         */
        destroy(i, r) {
          const o = this.destructors[r];
          if (o) {
            const l = o(i);
            return e.type(l) === "promise" ? l : e.Promise.resolve(l);
          }
        }
        /**
         * Get possible system type conversions and cache result.
         * @param {string} from data item type
         * @param {string|string[]} to array of accepted types
         * @return {ConversionStep[]|undefined} array of required conversions (returns empty array
         *  for from===to), or undefined if the system cannot convert between given types.
         *  Each object has 'transform' function that converts between neighbouring types, such
         *  that x = arr[i].transform(x) is valid input for converter arr[i+1].transform(), e.g.
         *  arr[i+1].transform(arr[i].transform( ... )) is a valid conversion procedure.
         *
         *  Note: if a function is returned, it is a callback called once the data is ready.
         */
        getConversionPath(i, r) {
          let o, l = this._known[i];
          if (l || (this._known[i] = l = {}), Array.isArray(r)) {
            e.console.assert(r.length > 0, "[getConversionPath] conversion 'to' type must be defined.");
            let a = 1 / 0;
            for (const c of r) {
              let d = l[c];
              d === void 0 && (l[c] = d = this.graph.dijkstra(i, c)), d && a > d.cost && (o = d, a = d.cost);
            }
          } else
            e.console.assert(typeof r == "string", "[getConversionPath] conversion 'to' type must be defined."), o = l[r], o === void 0 && (o = this.graph.dijkstra(i, r), this._known[i][r] = o);
          return o ? o.path : void 0;
        }
        /**
         * Return a list of known conversion types
         * @return {string[]}
         */
        getKnownTypes() {
          return Object.keys(this.graph.vertices);
        }
        /**
         * Check whether given type is known to the converter
         * @param {string} type type to test
         * @return {boolean}
         */
        existsType(i) {
          return !!this.graph.vertices[i];
        }
      }, e.converter = new e.DataTypeConverter();
    })(n), (function(e) {
      e.ButtonState = {
        REST: 0,
        GROUP: 1,
        HOVER: 2,
        DOWN: 3
      }, e.Button = function(c) {
        const d = this;
        e.EventSource.call(this), e.extend(!0, this, {
          tooltip: null,
          srcRest: null,
          srcGroup: null,
          srcHover: null,
          srcDown: null,
          clickTimeThreshold: e.DEFAULT_SETTINGS.clickTimeThreshold,
          clickDistThreshold: e.DEFAULT_SETTINGS.clickDistThreshold,
          /**
           * How long to wait before fading.
           * @member {Number} fadeDelay
           * @memberof OpenSeadragon.Button#
           */
          fadeDelay: 0,
          /**
           * How long should it take to fade the button.
           * @member {Number} fadeLength
           * @memberof OpenSeadragon.Button#
           */
          fadeLength: 2e3,
          onPress: null,
          onRelease: null,
          onClick: null,
          onEnter: null,
          onExit: null,
          onFocus: null,
          onBlur: null,
          userData: null
        }, c), this.element = c.element || e.makeNeutralElement("div"), c.element || (this.imgRest = e.makeTransparentImage(this.srcRest), this.imgGroup = e.makeTransparentImage(this.srcGroup), this.imgHover = e.makeTransparentImage(this.srcHover), this.imgDown = e.makeTransparentImage(this.srcDown), this.imgRest.alt = this.imgGroup.alt = this.imgHover.alt = this.imgDown.alt = this.tooltip, e.setElementPointerEventsNone(this.imgRest), e.setElementPointerEventsNone(this.imgGroup), e.setElementPointerEventsNone(this.imgHover), e.setElementPointerEventsNone(this.imgDown), this.element.style.position = "relative", e.setElementTouchActionNone(this.element), this.imgGroup.style.position = this.imgHover.style.position = this.imgDown.style.position = "absolute", this.imgGroup.style.top = this.imgHover.style.top = this.imgDown.style.top = "0px", this.imgGroup.style.left = this.imgHover.style.left = this.imgDown.style.left = "0px", this.imgHover.style.visibility = this.imgDown.style.visibility = "hidden", this.element.appendChild(this.imgRest), this.element.appendChild(this.imgGroup), this.element.appendChild(this.imgHover), this.element.appendChild(this.imgDown)), this.addHandler("press", this.onPress), this.addHandler("release", this.onRelease), this.addHandler("click", this.onClick), this.addHandler("enter", this.onEnter), this.addHandler("exit", this.onExit), this.addHandler("focus", this.onFocus), this.addHandler("blur", this.onBlur), this.currentState = e.ButtonState.GROUP, this.fadeBeginTime = null, this.shouldFade = !1, this.element.style.display = "inline-block", this.element.style.position = "relative", this.element.title = this.tooltip, this.tracker = new e.MouseTracker({
          userData: "Button.tracker",
          element: this.element,
          clickTimeThreshold: this.clickTimeThreshold,
          clickDistThreshold: this.clickDistThreshold,
          enterHandler: function(f) {
            f.insideElementPressed ? (l(d, e.ButtonState.DOWN), d.raiseEvent("enter", { originalEvent: f.originalEvent })) : f.buttonDownAny || l(d, e.ButtonState.HOVER);
          },
          focusHandler: function(f) {
            d.tracker.enterHandler(f), d.raiseEvent("focus", { originalEvent: f.originalEvent });
          },
          leaveHandler: function(f) {
            a(d, e.ButtonState.GROUP), f.insideElementPressed && d.raiseEvent("exit", { originalEvent: f.originalEvent });
          },
          blurHandler: function(f) {
            d.tracker.leaveHandler(f), d.raiseEvent("blur", { originalEvent: f.originalEvent });
          },
          pressHandler: function(f) {
            l(d, e.ButtonState.DOWN), d.raiseEvent("press", { originalEvent: f.originalEvent });
          },
          releaseHandler: function(f) {
            f.insideElementPressed && f.insideElementReleased ? (a(d, e.ButtonState.HOVER), d.raiseEvent("release", { originalEvent: f.originalEvent })) : f.insideElementPressed ? a(d, e.ButtonState.GROUP) : l(d, e.ButtonState.HOVER);
          },
          clickHandler: function(f) {
            f.quick && d.raiseEvent("click", { originalEvent: f.originalEvent });
          },
          keyHandler: function(f) {
            f.keyCode === 13 ? (d.raiseEvent("click", { originalEvent: f.originalEvent }), d.raiseEvent("release", { originalEvent: f.originalEvent }), f.preventDefault = !0) : f.preventDefault = !1;
          }
        }), a(this, e.ButtonState.REST);
      }, e.extend(
        e.Button.prototype,
        e.EventSource.prototype,
        /** @lends OpenSeadragon.Button.prototype */
        {
          /**
           * Used by a button container element (e.g. a ButtonGroup) to transition the button state
           * to ButtonState.GROUP.
           * @function
           */
          notifyGroupEnter: function() {
            l(this, e.ButtonState.GROUP);
          },
          /**
           * Used by a button container element (e.g. a ButtonGroup) to transition the button state
           * to ButtonState.REST.
           * @function
           */
          notifyGroupExit: function() {
            a(this, e.ButtonState.REST);
          },
          /**
           * @function
           */
          disable: function() {
            this.notifyGroupExit(), this.element.disabled = !0, this.tracker.setTracking(!1), e.setElementOpacity(this.element, 0.2, !0);
          },
          /**
           * @function
           */
          enable: function() {
            this.element.disabled = !1, this.tracker.setTracking(!0), e.setElementOpacity(this.element, 1, !0), this.notifyGroupEnter();
          },
          destroy: function() {
            this.imgRest && (this.element.removeChild(this.imgRest), this.imgRest = null), this.imgGroup && (this.element.removeChild(this.imgGroup), this.imgGroup = null), this.imgHover && (this.element.removeChild(this.imgHover), this.imgHover = null), this.imgDown && (this.element.removeChild(this.imgDown), this.imgDown = null), this.removeAllHandlers(), this.tracker.destroy(), this.element = null;
          }
        }
      );
      function t(c) {
        e.requestAnimationFrame(function() {
          i(c);
        });
      }
      function i(c) {
        let d, f, p;
        c.shouldFade && (d = e.now(), f = d - c.fadeBeginTime, p = 1 - f / c.fadeLength, p = Math.min(1, p), p = Math.max(0, p), c.imgGroup && e.setElementOpacity(c.imgGroup, p, !0), p > 0 && t(c));
      }
      function r(c) {
        c.shouldFade = !0, c.fadeBeginTime = e.now() + c.fadeDelay, window.setTimeout(function() {
          t(c);
        }, c.fadeDelay);
      }
      function o(c) {
        c.shouldFade = !1, c.imgGroup && e.setElementOpacity(c.imgGroup, 1, !0);
      }
      function l(c, d) {
        c.element.disabled || (d >= e.ButtonState.GROUP && c.currentState === e.ButtonState.REST && (o(c), c.currentState = e.ButtonState.GROUP), d >= e.ButtonState.HOVER && c.currentState === e.ButtonState.GROUP && (c.imgHover && (c.imgHover.style.visibility = ""), c.currentState = e.ButtonState.HOVER), d >= e.ButtonState.DOWN && c.currentState === e.ButtonState.HOVER && (c.imgDown && (c.imgDown.style.visibility = ""), c.currentState = e.ButtonState.DOWN));
      }
      function a(c, d) {
        c.element.disabled || (d <= e.ButtonState.HOVER && c.currentState === e.ButtonState.DOWN && (c.imgDown && (c.imgDown.style.visibility = "hidden"), c.currentState = e.ButtonState.HOVER), d <= e.ButtonState.GROUP && c.currentState === e.ButtonState.HOVER && (c.imgHover && (c.imgHover.style.visibility = "hidden"), c.currentState = e.ButtonState.GROUP), d <= e.ButtonState.REST && c.currentState === e.ButtonState.GROUP && (r(c), c.currentState = e.ButtonState.REST));
      }
    })(n), (function(e) {
      e.ButtonGroup = function(t) {
        e.extend(!0, this, {
          /**
           * An array containing the buttons themselves.
           * @member {Array} buttons
           * @memberof OpenSeadragon.ButtonGroup#
           */
          buttons: [],
          clickTimeThreshold: e.DEFAULT_SETTINGS.clickTimeThreshold,
          clickDistThreshold: e.DEFAULT_SETTINGS.clickDistThreshold,
          labelText: ""
        }, t);
        let i = this.buttons.concat([]), r = this, o;
        if (this.element = t.element || e.makeNeutralElement("div"), !t.group)
          for (this.element.style.display = "inline-block", o = 0; o < i.length; o++)
            this.element.appendChild(i[o].element);
        e.setElementTouchActionNone(this.element), this.tracker = new e.MouseTracker({
          userData: "ButtonGroup.tracker",
          element: this.element,
          clickTimeThreshold: this.clickTimeThreshold,
          clickDistThreshold: this.clickDistThreshold,
          enterHandler: function(l) {
            let a;
            for (a = 0; a < r.buttons.length; a++)
              r.buttons[a].notifyGroupEnter();
          },
          leaveHandler: function(l) {
            let a;
            if (!l.insideElementPressed)
              for (a = 0; a < r.buttons.length; a++)
                r.buttons[a].notifyGroupExit();
          }
        });
      }, e.ButtonGroup.prototype = {
        /**
         * Adds the given button to this button group.
         *
         * @function
         * @param {OpenSeadragon.Button} button
         */
        addButton: function(t) {
          this.buttons.push(t), this.element.appendChild(t.element);
        },
        /**
         * TODO: Figure out why this is used on the public API and if a more useful
         * api can be created.
         * @function
         * @private
         */
        emulateEnter: function() {
          this.tracker.enterHandler({ eventSource: this.tracker });
        },
        /**
         * TODO: Figure out why this is used on the public API and if a more useful
         * api can be created.
         * @function
         * @private
         */
        emulateLeave: function() {
          this.tracker.leaveHandler({ eventSource: this.tracker });
        },
        destroy: function() {
          for (; this.buttons.length; ) {
            const t = this.buttons.pop();
            this.element.removeChild(t.element), t.destroy();
          }
          this.tracker.destroy(), this.element = null;
        }
      };
    })(n), (function(e) {
      e.Rect = function(t, i, r, o, l) {
        this.x = typeof t == "number" ? t : 0, this.y = typeof i == "number" ? i : 0, this.width = typeof r == "number" ? r : 0, this.height = typeof o == "number" ? o : 0, this.degrees = typeof l == "number" ? l : 0, this.degrees = e.positiveModulo(this.degrees, 360);
        let a, c;
        this.degrees >= 270 ? (a = this.getTopRight(), this.x = a.x, this.y = a.y, c = this.height, this.height = this.width, this.width = c, this.degrees -= 270) : this.degrees >= 180 ? (a = this.getBottomRight(), this.x = a.x, this.y = a.y, this.degrees -= 180) : this.degrees >= 90 && (a = this.getBottomLeft(), this.x = a.x, this.y = a.y, c = this.height, this.height = this.width, this.width = c, this.degrees -= 90);
      }, e.Rect.fromSummits = function(t, i, r) {
        const o = t.distanceTo(i), l = t.distanceTo(r), a = i.minus(t);
        let c = Math.atan(a.y / a.x);
        return a.x < 0 ? c += Math.PI : a.y < 0 && (c += 2 * Math.PI), new e.Rect(
          t.x,
          t.y,
          o,
          l,
          c / Math.PI * 180
        );
      }, e.Rect.prototype = {
        /**
         * @function
         * @returns {OpenSeadragon.Rect} a duplicate of this Rect
         */
        clone: function() {
          return new e.Rect(
            this.x,
            this.y,
            this.width,
            this.height,
            this.degrees
          );
        },
        /**
         * The aspect ratio is simply the ratio of width to height.
         * @function
         * @returns {Number} The ratio of width to height.
         */
        getAspectRatio: function() {
          return this.width / this.height;
        },
        /**
         * Provides the coordinates of the upper-left corner of the rectangle as a
         * point.
         * @function
         * @returns {OpenSeadragon.Point} The coordinate of the upper-left corner of
         *  the rectangle.
         */
        getTopLeft: function() {
          return new e.Point(
            this.x,
            this.y
          );
        },
        /**
         * Provides the coordinates of the bottom-right corner of the rectangle as a
         * point.
         * @function
         * @returns {OpenSeadragon.Point} The coordinate of the bottom-right corner of
         *  the rectangle.
         */
        getBottomRight: function() {
          return new e.Point(this.x + this.width, this.y + this.height).rotate(this.degrees, this.getTopLeft());
        },
        /**
         * Provides the coordinates of the top-right corner of the rectangle as a
         * point.
         * @function
         * @returns {OpenSeadragon.Point} The coordinate of the top-right corner of
         *  the rectangle.
         */
        getTopRight: function() {
          return new e.Point(this.x + this.width, this.y).rotate(this.degrees, this.getTopLeft());
        },
        /**
         * Provides the coordinates of the bottom-left corner of the rectangle as a
         * point.
         * @function
         * @returns {OpenSeadragon.Point} The coordinate of the bottom-left corner of
         *  the rectangle.
         */
        getBottomLeft: function() {
          return new e.Point(this.x, this.y + this.height).rotate(this.degrees, this.getTopLeft());
        },
        /**
         * Computes the center of the rectangle.
         * @function
         * @returns {OpenSeadragon.Point} The center of the rectangle as represented
         *  as represented by a 2-dimensional vector (x,y)
         */
        getCenter: function() {
          return new e.Point(
            this.x + this.width / 2,
            this.y + this.height / 2
          ).rotate(this.degrees, this.getTopLeft());
        },
        /**
         * Returns the width and height component as a vector OpenSeadragon.Point
         * @function
         * @returns {OpenSeadragon.Point} The 2 dimensional vector representing the
         *  width and height of the rectangle.
         */
        getSize: function() {
          return new e.Point(this.width, this.height);
        },
        /**
         * Determines if two Rectangles have equivalent components.
         * @function
         * @param {OpenSeadragon.Rect} rectangle The Rectangle to compare to.
         * @returns {Boolean} 'true' if all components are equal, otherwise 'false'.
         */
        equals: function(t) {
          return t instanceof e.Rect && this.x === t.x && this.y === t.y && this.width === t.width && this.height === t.height && this.degrees === t.degrees;
        },
        /**
        * Multiply all dimensions (except degrees) in this Rect by a factor and
        * return a new Rect.
        * @function
        * @param {Number} factor The factor to multiply vector components.
        * @returns {OpenSeadragon.Rect} A new rect representing the multiplication
        *  of the vector components by the factor
        */
        times: function(t) {
          return new e.Rect(
            this.x * t,
            this.y * t,
            this.width * t,
            this.height * t,
            this.degrees
          );
        },
        /**
        * Translate/move this Rect by a vector and return new Rect.
        * @function
        * @param {OpenSeadragon.Point} delta The translation vector.
        * @returns {OpenSeadragon.Rect} A new rect with altered position
        */
        translate: function(t) {
          return new e.Rect(
            this.x + t.x,
            this.y + t.y,
            this.width,
            this.height,
            this.degrees
          );
        },
        /**
         * Returns the smallest rectangle that will contain this and the given
         * rectangle bounding boxes.
         * @param {OpenSeadragon.Rect} rect
         * @returns {OpenSeadragon.Rect} The new rectangle.
         */
        union: function(t) {
          const i = this.getBoundingBox(), r = t.getBoundingBox(), o = Math.min(i.x, r.x), l = Math.min(i.y, r.y), a = Math.max(
            i.x + i.width,
            r.x + r.width
          ), c = Math.max(
            i.y + i.height,
            r.y + r.height
          );
          return new e.Rect(
            o,
            l,
            a - o,
            c - l
          );
        },
        /**
         * Returns the bounding box of the intersection of this rectangle with the
         * given rectangle.
         * @param {OpenSeadragon.Rect} rect
         * @returns {OpenSeadragon.Rect} the bounding box of the intersection
         * or null if the rectangles don't intersect.
         */
        intersection: function(t) {
          const r = [], o = this.getTopLeft();
          t.containsPoint(o, 1e-10) && r.push(o);
          const l = this.getTopRight();
          t.containsPoint(l, 1e-10) && r.push(l);
          const a = this.getBottomLeft();
          t.containsPoint(a, 1e-10) && r.push(a);
          const c = this.getBottomRight();
          t.containsPoint(c, 1e-10) && r.push(c);
          const d = t.getTopLeft();
          this.containsPoint(d, 1e-10) && r.push(d);
          const f = t.getTopRight();
          this.containsPoint(f, 1e-10) && r.push(f);
          const p = t.getBottomLeft();
          this.containsPoint(p, 1e-10) && r.push(p);
          const b = t.getBottomRight();
          this.containsPoint(b, 1e-10) && r.push(b);
          const x = this._getSegments(), T = t._getSegments();
          for (let ae = 0; ae < x.length; ae++) {
            const se = x[ae];
            for (let fe = 0; fe < T.length; fe++) {
              const Ie = T[fe], Ae = I(
                se[0],
                se[1],
                Ie[0],
                Ie[1]
              );
              Ae && r.push(Ae);
            }
          }
          function I(ae, se, fe, Ie) {
            const Ae = se.minus(ae), qe = Ie.minus(fe), Ne = -qe.x * Ae.y + Ae.x * qe.y;
            if (Ne === 0)
              return null;
            const Fe = (Ae.x * (ae.y - fe.y) - Ae.y * (ae.x - fe.x)) / Ne, De = (qe.x * (ae.y - fe.y) - qe.y * (ae.x - fe.x)) / Ne;
            return -1e-10 <= Fe && Fe <= 1 - 1e-10 && -1e-10 <= De && De <= 1 - 1e-10 ? new e.Point(ae.x + De * Ae.x, ae.y + De * Ae.y) : null;
          }
          if (r.length === 0)
            return null;
          let z = r[0].x, W = r[0].x, j = r[0].y, oe = r[0].y;
          for (let ae = 1; ae < r.length; ae++) {
            const se = r[ae];
            se.x < z && (z = se.x), se.x > W && (W = se.x), se.y < j && (j = se.y), se.y > oe && (oe = se.y);
          }
          return new e.Rect(z, j, W - z, oe - j);
        },
        // private
        _getSegments: function() {
          const t = this.getTopLeft(), i = this.getTopRight(), r = this.getBottomLeft(), o = this.getBottomRight();
          return [
            [t, i],
            [i, o],
            [o, r],
            [r, t]
          ];
        },
        /**
         * Rotates a rectangle around a point.
         * @function
         * @param {Number} degrees The angle in degrees to rotate.
         * @param {OpenSeadragon.Point} [pivot] The point about which to rotate.
         * Defaults to the center of the rectangle.
         * @returns {OpenSeadragon.Rect}
         */
        rotate: function(t, i) {
          if (t = e.positiveModulo(t, 360), t === 0)
            return this.clone();
          i = i || this.getCenter();
          const r = this.getTopLeft().rotate(t, i);
          let l = this.getTopRight().rotate(t, i).minus(r);
          l = l.apply(function(c) {
            return Math.abs(c) < 1e-15 ? 0 : c;
          });
          let a = Math.atan(l.y / l.x);
          return l.x < 0 ? a += Math.PI : l.y < 0 && (a += 2 * Math.PI), new e.Rect(
            r.x,
            r.y,
            this.width,
            this.height,
            a / Math.PI * 180
          );
        },
        /**
         * Retrieves the smallest horizontal (degrees=0) rectangle which contains
         * this rectangle.
         * @returns {OpenSeadragon.Rect}
         */
        getBoundingBox: function() {
          if (this.degrees === 0)
            return this.clone();
          const t = this.getTopLeft(), i = this.getTopRight(), r = this.getBottomLeft(), o = this.getBottomRight(), l = Math.min(t.x, i.x, r.x, o.x), a = Math.max(t.x, i.x, r.x, o.x), c = Math.min(t.y, i.y, r.y, o.y), d = Math.max(t.y, i.y, r.y, o.y);
          return new e.Rect(
            l,
            c,
            a - l,
            d - c
          );
        },
        /**
         * Retrieves the smallest horizontal (degrees=0) rectangle which contains
         * this rectangle and has integers x, y, width and height
         * @returns {OpenSeadragon.Rect}
         */
        getIntegerBoundingBox: function() {
          const t = this.getBoundingBox(), i = Math.floor(t.x), r = Math.floor(t.y), o = Math.ceil(t.width + t.x - i), l = Math.ceil(t.height + t.y - r);
          return new e.Rect(i, r, o, l);
        },
        /**
         * Determines whether a point is inside this rectangle (edge included).
         * @function
         * @param {OpenSeadragon.Point} point
         * @param {Number} [epsilon=0] the margin of error allowed
         * @returns {Boolean} true if the point is inside this rectangle, false
         * otherwise.
         */
        containsPoint: function(t, i) {
          i = i || 0;
          const r = this.getTopLeft(), o = this.getTopRight(), l = this.getBottomLeft(), a = o.minus(r), c = l.minus(r);
          return (t.x - r.x) * a.x + (t.y - r.y) * a.y >= -i && (t.x - o.x) * a.x + (t.y - o.y) * a.y <= i && (t.x - r.x) * c.x + (t.y - r.y) * c.y >= -i && (t.x - l.x) * c.x + (t.y - l.y) * c.y <= i;
        },
        /**
         * Provides a string representation of the rectangle which is useful for
         * debugging.
         * @function
         * @returns {String} A string representation of the rectangle.
         */
        toString: function() {
          return "[" + Math.round(this.x * 100) / 100 + ", " + Math.round(this.y * 100) / 100 + ", " + Math.round(this.width * 100) / 100 + "x" + Math.round(this.height * 100) / 100 + ", " + Math.round(this.degrees * 100) / 100 + "deg]";
        }
      };
    })(n), (function(e) {
      const t = {};
      e.ReferenceStrip = function(p) {
        const b = this, x = p.viewer, T = e.getElementSize(x.element);
        let I, z;
        p.id || (p.id = "referencestrip-" + e.now(), this.element = e.makeNeutralElement("div"), this.element.id = p.id, this.element.className = "referencestrip"), p = e.extend(!0, {
          sizeRatio: e.DEFAULT_SETTINGS.referenceStripSizeRatio,
          position: e.DEFAULT_SETTINGS.referenceStripPosition,
          scroll: e.DEFAULT_SETTINGS.referenceStripScroll,
          clickTimeThreshold: e.DEFAULT_SETTINGS.clickTimeThreshold
        }, p, {
          element: this.element
        }), e.extend(this, p), t[this.id] = {
          animating: !1
        }, this.minPixelRatio = this.viewer.minPixelRatio, this.element.tabIndex = 0;
        const W = this.element.style;
        for (W.marginTop = "0px", W.marginRight = "0px", W.marginBottom = "0px", W.marginLeft = "0px", W.left = "0px", W.bottom = "0px", W.border = "0px", W.background = "#000", W.position = "relative", e.setElementTouchActionNone(this.element), e.setElementOpacity(this.element, 0.8), this.viewer = x, this.tracker = new e.MouseTracker({
          userData: "ReferenceStrip.tracker",
          element: this.element,
          clickHandler: e.delegate(this, i),
          dragHandler: e.delegate(this, r),
          scrollHandler: e.delegate(this, o),
          enterHandler: e.delegate(this, a),
          leaveHandler: e.delegate(this, c),
          keyDownHandler: e.delegate(this, d),
          keyHandler: e.delegate(this, f),
          preProcessEventHandler: function(j) {
            j.eventType === "wheel" && (j.preventDefault = !0);
          }
        }), p.width && p.height ? (this.element.style.width = p.width + "px", this.element.style.height = p.height + "px", x.addControl(
          this.element,
          { anchor: e.ControlAnchor.BOTTOM_LEFT }
        )) : p.scroll === "horizontal" ? (this.element.style.width = T.x * p.sizeRatio * x.tileSources.length + 12 * x.tileSources.length + "px", this.element.style.height = T.y * p.sizeRatio + "px", x.addControl(
          this.element,
          { anchor: e.ControlAnchor.BOTTOM_LEFT }
        )) : (this.element.style.height = T.y * p.sizeRatio * x.tileSources.length + 12 * x.tileSources.length + "px", this.element.style.width = T.x * p.sizeRatio + "px", x.addControl(
          this.element,
          { anchor: e.ControlAnchor.TOP_LEFT }
        )), this.panelWidth = T.x * this.sizeRatio + 8, this.panelHeight = T.y * this.sizeRatio + 8, this.panels = [], this.miniViewers = {}, z = 0; z < x.tileSources.length; z++)
          I = e.makeNeutralElement("div"), I.id = this.element.id + "-" + z, I.style.width = b.panelWidth + "px", I.style.height = b.panelHeight + "px", I.style.display = "inline", I.style.float = "left", I.style.cssFloat = "left", I.style.padding = "2px", e.setElementTouchActionNone(I), e.setElementPointerEventsNone(I), this.element.appendChild(I), I.activePanel = !1, this.panels.push(I);
        l(this, this.scroll === "vertical" ? T.y : T.x, 0), this.setFocus(0);
      }, e.ReferenceStrip.prototype = {
        /**
         * @function
         */
        setFocus: function(p) {
          const b = this.element.querySelector("#" + this.element.id + "-" + p), x = e.getElementSize(this.viewer.canvas), T = Number(this.element.style.width.replace("px", "")), I = Number(this.element.style.height.replace("px", "")), z = -Number(this.element.style.marginLeft.replace("px", "")), W = -Number(this.element.style.marginTop.replace("px", ""));
          let j;
          this.currentSelected !== b && (this.currentSelected && (this.currentSelected.style.background = "#000"), this.currentSelected = b, this.currentSelected.style.background = "#999", this.scroll === "horizontal" ? (j = Number(p) * (this.panelWidth + 3), j > z + x.x - this.panelWidth ? (j = Math.min(j, T - x.x), this.element.style.marginLeft = -j + "px", l(this, x.x, -j)) : j < z && (j = Math.max(0, j - x.x / 2), this.element.style.marginLeft = -j + "px", l(this, x.x, -j))) : (j = Number(p) * (this.panelHeight + 3), j > W + x.y - this.panelHeight ? (j = Math.min(j, I - x.y), this.element.style.marginTop = -j + "px", l(this, x.y, -j)) : j < W && (j = Math.max(0, j - x.y / 2), this.element.style.marginTop = -j + "px", l(this, x.y, -j))), this.currentPage = p, a.call(this, { eventSource: this.tracker }));
        },
        /**
         * @function
         */
        update: function() {
          return !!t[this.id].animating;
        },
        destroy: function() {
          if (this.miniViewers)
            for (const p in this.miniViewers)
              this.miniViewers[p].destroy();
          this.tracker.destroy(), this.element && this.viewer.removeControl(this.element);
        }
      };
      function i(p) {
        if (p.quick) {
          let b;
          this.scroll === "horizontal" ? b = Math.floor(p.position.x / (this.panelWidth + 4)) : b = Math.floor(p.position.y / this.panelHeight), this.viewer.goToPage(b);
        }
        this.element.focus();
      }
      function r(p) {
        if (this.dragging = !0, this.element) {
          const b = Number(this.element.style.marginLeft.replace("px", "")), x = Number(this.element.style.marginTop.replace("px", "")), T = Number(this.element.style.width.replace("px", "")), I = Number(this.element.style.height.replace("px", "")), z = e.getElementSize(this.viewer.canvas);
          this.scroll === "horizontal" ? -p.delta.x > 0 ? b > -(T - z.x) && (this.element.style.marginLeft = b + p.delta.x * 2 + "px", l(this, z.x, b + p.delta.x * 2)) : -p.delta.x < 0 && b < 0 && (this.element.style.marginLeft = b + p.delta.x * 2 + "px", l(this, z.x, b + p.delta.x * 2)) : -p.delta.y > 0 ? x > -(I - z.y) && (this.element.style.marginTop = x + p.delta.y * 2 + "px", l(this, z.y, x + p.delta.y * 2)) : -p.delta.y < 0 && x < 0 && (this.element.style.marginTop = x + p.delta.y * 2 + "px", l(this, z.y, x + p.delta.y * 2));
        }
      }
      function o(p) {
        if (this.element) {
          const b = Number(this.element.style.marginLeft.replace("px", "")), x = Number(this.element.style.marginTop.replace("px", "")), T = Number(this.element.style.width.replace("px", "")), I = Number(this.element.style.height.replace("px", "")), z = e.getElementSize(this.viewer.canvas);
          this.scroll === "horizontal" ? p.scroll > 0 ? b > -(T - z.x) && (this.element.style.marginLeft = b - p.scroll * 60 + "px", l(this, z.x, b - p.scroll * 60)) : p.scroll < 0 && b < 0 && (this.element.style.marginLeft = b - p.scroll * 60 + "px", l(this, z.x, b - p.scroll * 60)) : p.scroll < 0 ? x > z.y - I && (this.element.style.marginTop = x + p.scroll * 60 + "px", l(this, z.y, x + p.scroll * 60)) : p.scroll > 0 && x < 0 && (this.element.style.marginTop = x + p.scroll * 60 + "px", l(this, z.y, x + p.scroll * 60)), p.preventDefault = !0;
        }
      }
      function l(p, b, x) {
        let T, I, z, W, j, oe;
        for (p.scroll === "horizontal" ? T = p.panelWidth : T = p.panelHeight, I = Math.ceil(b / T) + 5, z = Math.ceil((Math.abs(x) + b) / T) + 1, I = z - I, I = I < 0 ? 0 : I, j = I; j < z && j < p.panels.length; j++)
          if (oe = p.panels[j], !oe.activePanel) {
            let ae;
            const se = p.viewer.tileSources[j];
            se.referenceStripThumbnailUrl ? ae = {
              type: "image",
              url: se.referenceStripThumbnailUrl
            } : ae = se, W = new e.Viewer({
              id: oe.id,
              tileSources: [ae],
              element: oe,
              navigatorSizeRatio: p.sizeRatio,
              showNavigator: !1,
              mouseNavEnabled: !1,
              showNavigationControl: !1,
              showSequenceControl: !1,
              immediateRender: !0,
              blendTime: 0,
              animationTime: 0,
              loadTilesWithAjax: p.viewer.loadTilesWithAjax,
              ajaxHeaders: p.viewer.ajaxHeaders,
              drawer: "canvas"
              //always use canvas for the reference strip
            }), e.setElementPointerEventsNone(W.canvas), e.setElementPointerEventsNone(W.container), W.innerTracker.setTracking(!1), W.outerTracker.setTracking(!1), p.miniViewers[oe.id] = W, oe.activePanel = !0;
          }
      }
      function a(p) {
        const b = p.eventSource.element;
        this.scroll === "horizontal" ? b.style.marginBottom = "0px" : b.style.marginLeft = "0px";
      }
      function c(p) {
        const b = p.eventSource.element;
        this.scroll === "horizontal" ? b.style.marginBottom = "-" + e.getElementSize(b).y / 2 + "px" : b.style.marginLeft = "-" + e.getElementSize(b).x / 2 + "px";
      }
      function d(p) {
        if (!p.ctrl && !p.alt && !p.meta)
          switch (p.keyCode) {
            case 38:
              o.call(this, { eventSource: this.tracker, position: null, scroll: 1, shift: null }), p.preventDefault = !0;
              break;
            case 40:
              o.call(this, { eventSource: this.tracker, position: null, scroll: -1, shift: null }), p.preventDefault = !0;
              break;
            case 37:
              o.call(this, { eventSource: this.tracker, position: null, scroll: -1, shift: null }), p.preventDefault = !0;
              break;
            case 39:
              o.call(this, { eventSource: this.tracker, position: null, scroll: 1, shift: null }), p.preventDefault = !0;
              break;
            default:
              p.preventDefault = !1;
              break;
          }
        else
          p.preventDefault = !1;
      }
      function f(p) {
        if (!p.ctrl && !p.alt && !p.meta)
          switch (p.keyCode) {
            case 61:
              o.call(this, { eventSource: this.tracker, position: null, scroll: 1, shift: null }), p.preventDefault = !0;
              break;
            case 45:
              o.call(this, { eventSource: this.tracker, position: null, scroll: -1, shift: null }), p.preventDefault = !0;
              break;
            case 48:
            //0|)
            case 119:
            //w
            case 87:
              o.call(this, { eventSource: this.tracker, position: null, scroll: 1, shift: null }), p.preventDefault = !0;
              break;
            case 115:
            //s
            case 83:
              o.call(this, { eventSource: this.tracker, position: null, scroll: -1, shift: null }), p.preventDefault = !0;
              break;
            case 97:
              o.call(this, { eventSource: this.tracker, position: null, scroll: -1, shift: null }), p.preventDefault = !0;
              break;
            case 100:
              o.call(this, { eventSource: this.tracker, position: null, scroll: 1, shift: null }), p.preventDefault = !0;
              break;
            default:
              p.preventDefault = !1;
              break;
          }
        else
          p.preventDefault = !1;
      }
    })(n), (function(e) {
      e.DisplayRect = function(t, i, r, o, l, a) {
        e.Rect.apply(this, [t, i, r, o]), this.minLevel = l, this.maxLevel = a;
      }, e.extend(e.DisplayRect.prototype, e.Rect.prototype);
    })(n), (function(e) {
      e.Spring = function(i) {
        const r = arguments;
        typeof i != "object" && (i = {
          initial: r.length && typeof r[0] == "number" ? r[0] : void 0,
          /**
           * Spring stiffness.
           * @member {Number} springStiffness
           * @memberof OpenSeadragon.Spring#
           */
          springStiffness: r.length > 1 ? r[1].springStiffness : 5,
          /**
           * Animation duration per spring.
           * @member {Number} animationTime
           * @memberof OpenSeadragon.Spring#
           */
          animationTime: r.length > 1 ? r[1].animationTime : 1.5
        }), e.console.assert(
          typeof i.springStiffness == "number" && i.springStiffness !== 0,
          "[OpenSeadragon.Spring] options.springStiffness must be a non-zero number"
        ), e.console.assert(
          typeof i.animationTime == "number" && i.animationTime >= 0,
          "[OpenSeadragon.Spring] options.animationTime must be a number greater than or equal to 0"
        ), i.exponential && (this._exponential = !0, delete i.exponential), e.extend(!0, this, i), this.current = {
          value: typeof this.initial == "number" ? this.initial : this._exponential ? 0 : 1,
          time: e.now()
          // always work in milliseconds
        }, e.console.assert(
          !this._exponential || this.current.value !== 0,
          "[OpenSeadragon.Spring] value must be non-zero for exponential springs"
        ), this.start = {
          value: this.current.value,
          time: this.current.time
        }, this.target = {
          value: this.current.value,
          time: this.current.time
        }, this._exponential && (this.start._logValue = Math.log(this.start.value), this.target._logValue = Math.log(this.target.value), this.current._logValue = Math.log(this.current.value));
      }, e.Spring.prototype = {
        /**
         * @function
         * @param {Number} target
         */
        resetTo: function(i) {
          e.console.assert(
            !this._exponential || i !== 0,
            "[OpenSeadragon.Spring.resetTo] target must be non-zero for exponential springs"
          ), this.start.value = this.target.value = this.current.value = i, this.start.time = this.target.time = this.current.time = e.now(), this._exponential && (this.start._logValue = Math.log(this.start.value), this.target._logValue = Math.log(this.target.value), this.current._logValue = Math.log(this.current.value));
        },
        /**
         * @function
         * @param {Number} target
         */
        springTo: function(i) {
          e.console.assert(
            !this._exponential || i !== 0,
            "[OpenSeadragon.Spring.springTo] target must be non-zero for exponential springs"
          ), this.start.value = this.current.value, this.start.time = this.current.time, this.target.value = i, this.target.time = this.start.time + 1e3 * this.animationTime, this._exponential && (this.start._logValue = Math.log(this.start.value), this.target._logValue = Math.log(this.target.value));
        },
        /**
         * @function
         * @param {Number} delta
         */
        shiftBy: function(i) {
          this.start.value += i, this.target.value += i, this._exponential && (e.console.assert(
            this.target.value !== 0 && this.start.value !== 0,
            "[OpenSeadragon.Spring.shiftBy] spring value must be non-zero for exponential springs"
          ), this.start._logValue = Math.log(this.start.value), this.target._logValue = Math.log(this.target.value));
        },
        setExponential: function(i) {
          this._exponential = i, this._exponential && (e.console.assert(
            this.current.value !== 0 && this.target.value !== 0 && this.start.value !== 0,
            "[OpenSeadragon.Spring.setExponential] spring value must be non-zero for exponential springs"
          ), this.start._logValue = Math.log(this.start.value), this.target._logValue = Math.log(this.target.value), this.current._logValue = Math.log(this.current.value));
        },
        /**
         * @function
         * @returns true if the spring is still updating its value, false if it is
         * already at the target value.
         */
        update: function() {
          this.current.time = e.now();
          let i, r;
          if (this._exponential ? (i = this.start._logValue, r = this.target._logValue) : (i = this.start.value, r = this.target.value), this.current.time >= this.target.time)
            this.current.value = this.target.value;
          else {
            let o = i + (r - i) * t(
              this.springStiffness,
              (this.current.time - this.start.time) / (this.target.time - this.start.time)
            );
            this._exponential ? this.current.value = Math.exp(o) : this.current.value = o;
          }
          return this.current.value !== this.target.value;
        },
        /**
         * Returns whether the spring is at the target value
         * @function
         * @returns {Boolean} True if at target value, false otherwise
         */
        isAtTargetValue: function() {
          return this.current.value === this.target.value;
        }
      };
      function t(i, r) {
        return (1 - Math.exp(i * -r)) / (1 - Math.exp(-i));
      }
    })(n), (function(e) {
      e.ImageJob = function(i) {
        e.extend(!0, this, {
          timeout: e.DEFAULT_SETTINGS.timeout,
          jobId: null,
          tries: 0
        }, i), this.data = null, this.userData = {}, this.errorMsg = null;
      }, e.ImageJob.prototype = {
        /**
         * Starts the image job.
         * @method
         * @memberof OpenSeadragon.ImageJob#
         */
        start: function() {
          this.tries++;
          const i = this, r = this.abort;
          this.jobId = window.setTimeout(function() {
            i.fail("Image load exceeded timeout (" + i.timeout + " ms)", null);
          }, this.timeout), this.abort = function() {
            i.source.downloadTileAbort(i), typeof r == "function" && r();
          }, this.source.downloadTileStart(this);
        },
        /**
         * Finish this job.
         * @param {*} data data that has been downloaded
         * @param {XMLHttpRequest} request reference to the request if used
         * @param {string} dataType data type identifier
         *   fallback compatibility behavior: dataType treated as errorMessage if data is falsey value
         * @memberof OpenSeadragon.ImageJob#
         */
        finish: function(i, r, o) {
          if (this.jobId) {
            if (i == null || i === !1) {
              this.fail(o || "[downloadTileStart->finish()] Retrieved data is invalid!", r);
              return;
            }
            this.data = i, this.request = r, this.errorMsg = null, this.dataType = o, this.jobId && window.clearTimeout(this.jobId), this.callback(this);
          }
        },
        /**
         * Finish this job as a failure.
         * @param {string} errorMessage description upon failure
         * @param {XMLHttpRequest} request reference to the request if used
         */
        fail: function(i, r) {
          this.data = null, this.request = r, this.errorMsg = i, this.dataType = null, this.jobId && (window.clearTimeout(this.jobId), this.jobId = null), this.callback(this);
        }
      }, e.ImageLoader = function(i) {
        e.extend(!0, this, {
          jobLimit: e.DEFAULT_SETTINGS.imageLoaderLimit,
          timeout: e.DEFAULT_SETTINGS.timeout,
          jobQueue: [],
          failedTiles: [],
          jobsInProgress: 0
        }, i);
      }, e.ImageLoader.prototype = {
        /**
         * Add an unloaded image to the loader queue.
         * @method
         * @param {Object} options - Options for this job.
         * @param {String} [options.src] - URL of image to download.
         * @param {Tile} [options.tile] - Tile that belongs the data to. The tile instance
         *      is not internally used and serves for custom TileSources implementations.
         * @param {TileSource} [options.source] - Image loading strategy
         * @param {String} [options.loadWithAjax] - Whether to load this image with AJAX.
         * @param {String} [options.ajaxHeaders] - Headers to add to the image request if using AJAX.
         * @param {String|Boolean} [options.crossOriginPolicy] - CORS policy to use for downloads
         * @param {String} [options.postData] - POST parameters (usually but not necessarily in k=v&k2=v2... form,
         *      see TileSource::getTilePostData) or null
         * @param {Boolean} [options.ajaxWithCredentials] - Whether to set withCredentials on AJAX
         *      requests.
         * @param {Function} [options.callback] - Called once image has been downloaded.
         * @param {Function} [options.abort] - Called when this image job is aborted.
         * @returns {boolean} true if job was immediatelly started, false if queued
         */
        addJob: function(i) {
          if (!i.source) {
            e.console.error("ImageLoader.prototype.addJob() requires [options.source]. TileSource since new API defines how images are fetched. Creating a dummy TileSource.");
            const a = e.TileSource.prototype;
            i.source = {
              downloadTileStart: a.downloadTileStart,
              downloadTileAbort: a.downloadTileAbort
            };
          }
          const r = this, o = {
            src: i.src,
            tile: i.tile || {},
            source: i.source,
            loadWithAjax: i.loadWithAjax,
            ajaxHeaders: i.loadWithAjax ? i.ajaxHeaders : null,
            crossOriginPolicy: i.crossOriginPolicy,
            ajaxWithCredentials: i.ajaxWithCredentials,
            postData: i.postData,
            callback: (a) => t(r, a, i.callback),
            abort: i.abort,
            timeout: this.timeout
          }, l = new e.ImageJob(o);
          return !this.jobLimit || this.jobsInProgress < this.jobLimit ? (l.start(), this.jobsInProgress++, !0) : (this.jobQueue.push(l), !1);
        },
        /**
         * @returns {boolean} true if a job can be submitted
         */
        canAcceptNewJob() {
          return !this.jobLimit || this.jobsInProgress < this.jobLimit;
        },
        /**
         * Clear any unstarted image loading jobs from the queue.
         * @method
         */
        clear: function() {
          for (let i = 0; i < this.jobQueue.length; i++) {
            const r = this.jobQueue[i];
            typeof r.abort == "function" && r.abort();
          }
          this.jobQueue = [];
        }
      };
      function t(i, r, o) {
        r.errorMsg && r.data === null && r.tries < 1 + i.tileRetryMax && i.failedTiles.push(r);
        let l;
        i.jobsInProgress--, i.canAcceptNewJob() && i.jobQueue.length > 0 && (l = i.jobQueue.shift(), l.start(), i.jobsInProgress++), i.tileRetryMax > 0 && i.jobQueue.length === 0 && i.canAcceptNewJob() && i.failedTiles.length > 0 && (l = i.failedTiles.shift(), setTimeout(function() {
          l.start();
        }, i.tileRetryDelay), i.jobsInProgress++), o(r.data, r.errorMsg, r.request, r.dataType, r.tries);
      }
    })(n), (function(e) {
      e.Tile = function(t, i, r, o, l, a, c, d, f, p, b, x) {
        this.level = t, this.x = i, this.y = r, this.bounds = o, this.positionedBounds = new n.Rect(o.x, o.y, o.width, o.height), this.sourceBounds = p, this.exists = l, this._url = a, this.postData = b, c && (this.context2D = c), this.loadWithAjax = d, this.ajaxHeaders = f, x === void 0 && (e.console.warn("Tile constructor needs 'cacheKey' variable: creation tile cache in Tile class is deprecated. TileSource.prototype.getTileHashKey will be used."), x = e.TileSource.prototype.getTileHashKey(t, i, r, a, f, b)), this._cKey = x || "", this._ocKey = x || "", this.loaded = !1, this.loading = !1, this.position = null, this.size = null, this.flipped = !1, this.blendStart = null, this.opacity = null, this.squaredDistance = null, this.visibility = null, this.hasTransparency = !1, this.beingDrawn = !1, this.lastTouchTime = 0, this.isRightMost = !1, this.isBottomMost = !1, this.tiledImage = null, this._caches = {}, this.processing = !1, this.processingPromise = e.Promise.resolve();
      }, e.Tile.prototype = {
        /**
         * Provides a string representation of this tiles level and (x,y)
         * components.
         * @function
         * @returns {String}
         */
        toString: function() {
          return this.level + "/" + this.x + "_" + this.y;
        },
        /**
         * The unique main cache key for this tile. Created automatically
         *  from the given tiledImage.source.getTileHashKey(...) implementation.
         * @member {String} cacheKey
         * @memberof OpenSeadragon.Tile#
         * @private
         */
        get cacheKey() {
          return this._cKey;
        },
        set cacheKey(t) {
          if (t === this.cacheKey)
            return;
          this.getCache(t) || e.console.warn("[Tile.cacheKey] should not be set manually. Use addCache() with setAsMain=true."), this._updateMainCacheKey(t);
        },
        /**
         * By default equal to tile.cacheKey, marks a cache associated with this tile
         * that holds the cache original data (it was loaded with). In case you
         * change the tile data, the tile original data should be left with the cache
         * 'originalCacheKey' and the new, modified data should be stored in cache 'cacheKey'.
         * This key is used in cache resolution: in case new tile data is requested, if
         * this cache key exists in the cache it is loaded.
         * @member {String} originalCacheKey
         * @memberof OpenSeadragon.Tile#
         * @private
         */
        set originalCacheKey(t) {
          throw "Original Cache Key cannot be managed manually!";
        },
        get originalCacheKey() {
          return this._ocKey;
        },
        /**
         * The Image object for this tile.
         * @member {Object} image
         * @memberof OpenSeadragon.Tile#
         * @deprecated
         * @returns {Image}
         */
        get image() {
          return e.console.error("[Tile.image] property has been deprecated. Use [Tile.getData] instead."), this.getImage();
        },
        /**
         * The URL of this tile's image.
         * @member {String} url
         * @memberof OpenSeadragon.Tile#
         * @deprecated
         * @returns {String}
         */
        get url() {
          return e.console.error("[Tile.url] property has been deprecated. Use [Tile.getUrl] instead."), this.getUrl();
        },
        /**
         * The HTML div element for this tile
         * @member {Element} element
         * @memberof OpenSeadragon.Tile#
         * @deprecated
         */
        get element() {
          e.console.error("Tile::element property is deprecated. Use cache API instead. Moreover, this property might be unstable.");
          const t = this.getCache();
          return !t || !t.loaded ? null : t.type !== n.HTMLDrawer.canvasCacheType || t.type !== n.HTMLDrawer.imageCacheType ? (e.console.error("Access to HtmlDrawer property via Tile instance: HTMLDrawer must be used!"), null) : t.data.element;
        },
        /**
         * The HTML img element for this tile.
         * @member {Element} imgElement
         * @memberof OpenSeadragon.Tile#
         * @deprecated
         */
        get imgElement() {
          e.console.error("Tile::imgElement property is deprecated. Use cache API instead. Moreover, this property might be unstable.");
          const t = this.getCache();
          return !t || !t.loaded ? null : t.type !== n.HTMLDrawer.canvasCacheType || t.type !== n.HTMLDrawer.imageCacheType ? (e.console.error("Access to HtmlDrawer property via Tile instance: HTMLDrawer must be used!"), null) : t.data.imgElement;
        },
        /**
         * The alias of this.element.style.
         * @member {String} style
         * @memberof OpenSeadragon.Tile#
         * @deprecated
         */
        get style() {
          e.console.error("Tile::style property is deprecated. Use cache API instead. Moreover, this property might be unstable.");
          const t = this.getCache();
          return !t || !t.loaded ? null : t.type !== n.HTMLDrawer.canvasCacheType || t.type !== n.HTMLDrawer.imageCacheType ? (e.console.error("Access to HtmlDrawer property via Tile instance: HTMLDrawer must be used!"), null) : t.data.style;
        },
        /**
         * Get the Image object for this tile.
         * @returns {?Image}
         * @deprecated
         */
        getImage: function() {
          e.console.error("[Tile.getImage] property has been deprecated. Use 'tile-invalidated' routine event instead.");
          const t = this.getCache(this.cacheKey);
          if (t)
            return t.transformTo("image"), t.data;
        },
        /**
         * Get the url string for this tile.
         * @returns {String}
         */
        getUrl: function() {
          return typeof this._url == "function" ? this._url() : this._url;
        },
        /**
         * Get the CanvasRenderingContext2D instance for tile image data drawn
         * onto Canvas if enabled and available
         * @deprecated
         * @returns {CanvasRenderingContext2D|undefined}
         */
        getCanvasContext: function() {
          e.console.error("[Tile.getCanvasContext] property has been deprecated. Use 'tile-invalidated' routine event instead.");
          const t = this.getCache(this.cacheKey);
          if (t)
            return t.transformTo("context2d"), t.data;
        },
        /**
         * The context2D of this tile if it is provided directly by the tile source.
         * @deprecated
         * @type {CanvasRenderingContext2D}
         */
        get context2D() {
          return e.console.error("[Tile.context2D] property has been deprecated. Use 'tile-invalidated' routine event instead."), this.getCanvasContext();
        },
        /**
         * The context2D of this tile if it is provided directly by the tile source.
         * @deprecated
         */
        set context2D(t) {
          e.console.error("[Tile.context2D] property has been deprecated. Use 'tile-invalidated' routine event instead."), this._caches[this.cacheKey] && this.removeCache(this.cacheKey), this.addCache(this.cacheKey, t, "context2d", !0, !1);
        },
        /**
         * The default cache for this tile.
         * @deprecated
         * @type OpenSeadragon.CacheRecord
         */
        get cacheImageRecord() {
          return e.console.error("[Tile.cacheImageRecord] property has been deprecated. Use Tile::getCache."), this.getCache(this.cacheKey);
        },
        /**
         * The default cache for this tile.
         * @deprecated
         */
        set cacheImageRecord(t) {
          e.console.error("[Tile.cacheImageRecord] property has been deprecated. Use Tile::addCache."), this._caches[this.cacheKey] && this.removeCache(this.cacheKey), t && (t.loaded ? this.addCache(this.cacheKey, t.data, t.type, !0, !1) : t.await().then((r) => this.addCache(this.cacheKey, r, t.type, !0, !1)));
        },
        /**
         * Cache key for main cache that is 'cache-equal', but different from original cache key
         * @return {string}
         * @private
         */
        buildDistinctMainCacheKey: function() {
          return this.cacheKey === this.originalCacheKey ? "mod://" + this.originalCacheKey : this.cacheKey;
        },
        /**
         * Read tile cache data object (CacheRecord)
         * @param {string} [key=this.cacheKey] cache key to read that belongs to this tile
         * @return {OpenSeadragon.CacheRecord}
         * @private
         */
        getCache: function(t = this._cKey) {
          const i = this._caches[t];
          return i && i.withTileReference(this), i;
        },
        /**
         * Create tile cache for given data object.
         *
         * Using `setAsMain` updates also main tile cache key - the main cache key used to draw this tile.
         * In that case, the cache should be ready to be rendered immediatelly (converted to one of the supported formats
         * of the currently employed drawer).
         *
         * NOTE: if the existing cache already exists,
         * data parameter is ignored and inherited from the existing cache object.
         * WARNING: if you override main tile cache key to point to a different cache, the invalidation routine
         * will no longer work. If you need to modify tile main data, prefer to use invalidation routine instead.
         *
         * @param {string} key cache key, if unique, new cache object is created, else existing cache attached
         * @param {*} data this data will be IGNORED if cache already exists; therefore if
         *   `typeof data === 'function'` holds (both async and normal functions), the data is called to obtain
         *   the data item: this is an optimization to load data only when necessary.
         * @param {string} [type=undefined] data type, will be guessed if not provided (not recommended),
         *   if data is a callback the type is a mandatory field, not setting it results in undefined behaviour
         * @param {boolean} [setAsMain=false] if true, the key will be set as the tile.cacheKey,
         *   no effect if key === this.cacheKey
         * @param [_safely=true] private
         * @returns {OpenSeadragon.CacheRecord|null} - The cache record the tile was attached to.
         * @private
         */
        addCache: function(t, i, r = void 0, o = !1, l = !0) {
          const a = this.tiledImage;
          if (!a)
            return null;
          r || (this.__typeWarningReported || (e.console.warn(this, "[Tile.addCache] called without type specification. Automated deduction is potentially unsafe: prefer specification of data type explicitly."), this.__typeWarningReported = !0), typeof i == "function" && e.console.error("[TileCache.cacheTile] options.data as a callback requires type argument! Current is " + r), r = e.converter.guessType(i));
          const c = t === this.cacheKey;
          if (l && (c || o)) {
            const p = a.viewer.drawer.getSupportedDataFormats(), b = e.converter.getConversionPath(r, p);
            e.console.assert(b, `[Tile.addCache] data was set for the default tile cache we are unableto render. Make sure OpenSeadragon.converter was taught to convert ${r} to (one of): ${b.toString()}`);
          }
          const d = a._tileCache.cacheTile({
            data: i,
            dataType: r,
            tile: this,
            cacheKey: t,
            cutoff: a.source.getClosestLevel()
          }), f = this._caches[t];
          return f !== d && (this._caches[t] = d, f && (f.removeTile(this), a._tileCache.safeUnloadCache(f))), !c && o && this._updateMainCacheKey(t), d;
        },
        /**
         * Add cache object to the tile
         *
         * @param {string} key cache key, if unique, new cache object is created, else existing cache attached
         * @param {OpenSeadragon.CacheRecord} cache the cache object to attach to this tile
         * @param {boolean} [setAsMain=false] if true, the key will be set as the tile.cacheKey,
         *   no effect if key === this.cacheKey
         * @param [_safely=true] private
         * @returns {OpenSeadragon.CacheRecord|null} - Returns cache parameter reference if attached.
         * @private
         */
        setCache(t, i, r = !1, o = !0) {
          const l = this.tiledImage;
          if (!l)
            return null;
          const a = t === this.cacheKey;
          if (o && (e.console.assert(i instanceof e.CacheRecord, "[Tile.setCache] cache must be a CacheRecord object!"), a || r)) {
            const d = l.viewer.drawer.getSupportedDataFormats(), f = e.converter.getConversionPath(i.type, d);
            e.console.assert(f, `[Tile.setCache] data was set for the default tile cache we are unableto render. Make sure OpenSeadragon.converter was taught to convert ${i.type} to (one of): ${f.toString()}`);
          }
          const c = this._caches[t];
          return c !== i && (this._caches[t] = i, i.addTile(this), c && (c.removeTile(this), l._tileCache.safeUnloadCache(c))), !a && r && this._updateMainCacheKey(t), i;
        },
        /**
         * Sets the main cache key for this tile and
         * performs necessary updates
         * @param value
         * @private
         */
        _updateMainCacheKey: function(t) {
          let i = this._caches[this._cKey];
          i && i.destroyInternalCache(), this._cKey = t;
        },
        /**
         * Get the number of caches available to this tile
         * @returns {number} number of caches
         */
        getCacheSize: function() {
          return Object.values(this._caches).length;
        },
        /**
         * Free tile cache. Removes by default the cache record if no other tile uses it.
         * @param {string} key cache key, required
         * @param {boolean} [freeIfUnused=true] set to false if zombie should be created
         * @return {OpenSeadragon.CacheRecord|undefined} reference to the cache record if it was removed,
         *   undefined if removal was refused to perform (e.g. does not exist, it is an original data target etc.)
         * @private
         */
        removeCache: function(t, i = !0) {
          const r = this._caches[t];
          if (!r) {
            this.tiledImage._tileCache.unloadCacheForTile(this, t, i, !0);
            return;
          }
          const o = this.cacheKey, l = this.originalCacheKey, a = o === l;
          if (!a && l === t) {
            e.console.warn(
              "[Tile.removeCache] original data must not be manually deleted: other parts of the code might rely on it!",
              "If you want the tile not to preserve the original data, toggle of data perseverance in tile.setData()."
            );
            return;
          }
          if (o === t)
            if (!a && this._caches[l])
              this._updateMainCacheKey(l);
            else {
              e.console.warn(
                "[Tile.removeCache] trying to remove the only cache that can be used to draw the tile!",
                "If you want to remove the main cache, first set different cache as main with tile.addCache()"
              );
              return;
            }
          return this.tiledImage._tileCache.unloadCacheForTile(this, t, i, !1) && delete this._caches[t], r;
        },
        /**
         * Get the ratio between current and original size.
         * @function
         * @deprecated
         * @returns {number}
         */
        getScaleForEdgeSmoothing: function() {
          e.console.warn("[Tile.getScaleForEdgeSmoothing] is deprecated, the following error is the consequence:");
          const t = this.getCanvasContext();
          return t ? t.canvas.width / (this.size.x * e.pixelDensityRatio) : (e.console.warn(
            "[Tile.drawCanvas] attempting to get tile scale %s when tile's not cached",
            this.toString()
          ), 1);
        },
        /**
         * Get a translation vector that when applied to the tile position produces integer coordinates.
         * Needed to avoid swimming and twitching.
         * @function
         * @param {Number} [scale=1] - Scale to be applied to position.
         * @returns {OpenSeadragon.Point}
         */
        getTranslationForEdgeSmoothing: function(t, i, r) {
          const o = Math.max(1, Math.ceil((r.x - i.x) / 2)), l = Math.max(1, Math.ceil((r.y - i.y) / 2));
          return new e.Point(o, l).minus(
            this.position.times(e.pixelDensityRatio).times(t || 1).apply(function(a) {
              return a % 1;
            })
          );
        },
        /**
         * Reflect that a cache object was renamed. Called internally from TileCache.
         * Do NOT call manually.
         * @function
         * @private
         */
        reflectCacheRenamed: function(t, i) {
          let r = this._caches[t];
          r && (t === this._ocKey && (this._ocKey = i), t === this._cKey && (this._cKey = i), this._caches[i] = r, delete this._caches[t]);
        },
        /**
         * Check if two tiles are data-equal
         * @param {OpenSeadragon.Tile} tile
         */
        equals(t) {
          return this._ocKey === t._ocKey;
        },
        /**
         * Removes tile from the system: it will still be present in the
         * OSD memory, but marked as loaded=false, and its data will be erased if erase set to true.
         * @param {boolean} [erase=false]
         */
        unload: function(t = !1) {
          this.loaded && this.tiledImage._tileCache.unloadTile(this, t);
        },
        /**
         * this method shall be called only by cache system when the tile is already empty of data
         * @private
         */
        _unload: function() {
          this.tiledImage = null, this._caches = {}, this._cacheSize = 0, this.loaded = !1, this.loading = !1, this._cKey = this._ocKey;
        }
      };
    })(n), (function(e) {
      e.OverlayPlacement = e.Placement, e.OverlayRotationMode = e.freezeObject({
        NO_ROTATION: 1,
        EXACT: 2,
        BOUNDING_BOX: 3
      }), e.Overlay = function(t, i, r) {
        let o;
        e.isPlainObject(t) ? o = t : o = {
          element: t,
          location: i,
          placement: r
        }, this.elementWrapper = document.createElement("div"), this.element = o.element, this.elementWrapper.appendChild(this.element), this.element.id && (this.elementWrapper.id = "overlay-wrapper-" + this.element.id), this.elementWrapper.classList.add("openseadragon-overlay-wrapper"), this.style = this.elementWrapper.style, this._init(o);
      }, e.Overlay.prototype = {
        // private
        _init: function(t) {
          this.location = t.location, this.placement = t.placement === void 0 ? e.Placement.TOP_LEFT : t.placement, this.onDraw = t.onDraw, this.checkResize = t.checkResize === void 0 ? !0 : t.checkResize, this.width = t.width === void 0 ? null : t.width, this.height = t.height === void 0 ? null : t.height, this.rotationMode = t.rotationMode || e.OverlayRotationMode.EXACT, this.location instanceof e.Rect && (this.width = this.location.width, this.height = this.location.height, this.location = this.location.getTopLeft(), this.placement = e.Placement.TOP_LEFT), this.scales = this.width !== null && this.height !== null, this.bounds = new e.Rect(
            this.location.x,
            this.location.y,
            this.width,
            this.height
          ), this.position = this.location;
        },
        /**
         * Internal function to adjust the position of an overlay
         * depending on it size and placement.
         * @function
         * @param {OpenSeadragon.Point} position
         * @param {OpenSeadragon.Point} size
         */
        adjust: function(t, i) {
          const r = e.Placement.properties[this.placement];
          r && (r.isHorizontallyCentered ? t.x -= i.x / 2 : r.isRight && (t.x -= i.x), r.isVerticallyCentered ? t.y -= i.y / 2 : r.isBottom && (t.y -= i.y));
        },
        /**
         * @function
         */
        destroy: function() {
          const t = this.elementWrapper, i = this.style;
          t.parentNode && (t.parentNode.removeChild(t), t.prevElementParent && (i.display = "none", document.body.appendChild(t))), this.onDraw = null, i.top = "", i.left = "", i.position = "", this.width !== null && (i.width = ""), this.height !== null && (i.height = "");
          const r = e.getCssPropertyWithVendorPrefix(
            "transformOrigin"
          ), o = e.getCssPropertyWithVendorPrefix(
            "transform"
          );
          r && o && (i[r] = "", i[o] = "");
        },
        /**
         * @function
         * @param {Element} container
         */
        drawHTML: function(t, i) {
          const r = this.elementWrapper;
          r.parentNode !== t && (r.prevElementParent = r.parentNode, r.prevNextSibling = r.nextSibling, t.appendChild(r), this.style.position = "absolute", this.size = e.getElementSize(this.elementWrapper));
          const o = this._getOverlayPositionAndSize(i), l = o.position, a = this.size = o.size;
          let c = "";
          i.overlayPreserveContentDirection && (c = i.flipped ? " scaleX(-1)" : " scaleX(1)");
          const d = i.flipped ? -o.rotate : o.rotate, f = i.flipped ? " scaleX(-1)" : "";
          if (this.onDraw)
            this.onDraw(l, a, this.element);
          else {
            const p = this.style, b = this.element.style;
            b.display = "block", p.left = l.x + "px", p.top = l.y + "px", this.width !== null && (b.width = a.x + "px"), this.height !== null && (b.height = a.y + "px");
            const x = e.getCssPropertyWithVendorPrefix(
              "transformOrigin"
            ), T = e.getCssPropertyWithVendorPrefix(
              "transform"
            );
            x && T && (d && !i.flipped ? (b[T] = "", p[x] = this._getTransformOrigin(), p[T] = "rotate(" + d + "deg)") : !d && i.flipped ? (b[T] = c, p[x] = this._getTransformOrigin(), p[T] = f) : d && i.flipped ? (b[T] = c, p[x] = this._getTransformOrigin(), p[T] = "rotate(" + d + "deg)" + f) : (b[T] = "", p[x] = "", p[T] = "")), p.display = "flex";
          }
        },
        // private
        _getOverlayPositionAndSize: function(t) {
          let i = t.pixelFromPoint(this.location, !0), r = this._getSizeInPixels(t);
          this.adjust(i, r);
          let o = 0;
          if (t.getRotation(!0) && this.rotationMode !== e.OverlayRotationMode.NO_ROTATION)
            if (this.rotationMode === e.OverlayRotationMode.BOUNDING_BOX && this.width !== null && this.height !== null) {
              const l = new e.Rect(i.x, i.y, r.x, r.y), a = this._getBoundingBox(l, t.getRotation(!0));
              i = a.getTopLeft(), r = a.getSize();
            } else
              o = t.getRotation(!0);
          return t.flipped && (i.x = t.getContainerSize().x - i.x), {
            position: i,
            size: r,
            rotate: o
          };
        },
        // private
        _getSizeInPixels: function(t) {
          let i = this.size.x, r = this.size.y;
          if (this.width !== null || this.height !== null) {
            const o = t.deltaPixelsFromPointsNoRotate(
              new e.Point(this.width || 0, this.height || 0),
              !0
            );
            this.width !== null && (i = o.x), this.height !== null && (r = o.y);
          }
          if (this.checkResize && (this.width === null || this.height === null)) {
            const o = this.size = e.getElementSize(this.elementWrapper);
            this.width === null && (i = o.x), this.height === null && (r = o.y);
          }
          return new e.Point(i, r);
        },
        // private
        _getBoundingBox: function(t, i) {
          const r = this._getPlacementPoint(t);
          return t.rotate(i, r).getBoundingBox();
        },
        // private
        _getPlacementPoint: function(t) {
          const i = new e.Point(t.x, t.y), r = e.Placement.properties[this.placement];
          return r && (r.isHorizontallyCentered ? i.x += t.width / 2 : r.isRight && (i.x += t.width), r.isVerticallyCentered ? i.y += t.height / 2 : r.isBottom && (i.y += t.height)), i;
        },
        // private
        _getTransformOrigin: function() {
          let t = "";
          const i = e.Placement.properties[this.placement];
          return i && (i.isLeft ? t = "left" : i.isRight && (t = "right"), i.isTop ? t += " top" : i.isBottom && (t += " bottom")), t;
        },
        /**
         * Changes the overlay settings.
         * @function
         * @param {OpenSeadragon.Point|OpenSeadragon.Rect|Object} location
         * If an object is specified, the options are the same than the constructor
         * except for the element which can not be changed.
         * @param {OpenSeadragon.Placement} placement
         */
        update: function(t, i) {
          const r = e.isPlainObject(t) ? t : {
            location: t,
            placement: i
          };
          this._init({
            location: r.location || this.location,
            placement: r.placement !== void 0 ? r.placement : this.placement,
            onDraw: r.onDraw || this.onDraw,
            checkResize: r.checkResize || this.checkResize,
            width: r.width !== void 0 ? r.width : this.width,
            height: r.height !== void 0 ? r.height : this.height,
            rotationMode: r.rotationMode || this.rotationMode
          });
        },
        /**
         * Returns the current bounds of the overlay in viewport coordinates
         * @function
         * @param {OpenSeadragon.Viewport} viewport the viewport
         * @returns {OpenSeadragon.Rect} overlay bounds
         */
        getBounds: function(t) {
          e.console.assert(
            t,
            "A viewport must now be passed to Overlay.getBounds."
          );
          let i = this.width, r = this.height;
          if (i === null || r === null) {
            const l = t.deltaPointsFromPixelsNoRotate(this.size, !0);
            i === null && (i = l.x), r === null && (r = l.y);
          }
          const o = this.location.clone();
          return this.adjust(o, new e.Point(i, r)), this._adjustBoundsForRotation(
            t,
            new e.Rect(o.x, o.y, i, r)
          );
        },
        // private
        _adjustBoundsForRotation: function(t, i) {
          if (!t || t.getRotation(!0) === 0 || this.rotationMode === e.OverlayRotationMode.EXACT)
            return i;
          if (this.rotationMode === e.OverlayRotationMode.BOUNDING_BOX) {
            if (this.width === null || this.height === null)
              return i;
            const r = this._getOverlayPositionAndSize(t);
            return t.viewerElementToViewportRectangle(new e.Rect(
              r.position.x,
              r.position.y,
              r.size.x,
              r.size.y
            ));
          }
          return i.rotate(
            -t.getRotation(!0),
            this._getPlacementPoint(i)
          );
        }
      };
    })(n), (function(e) {
      const t = e;
      t.DrawerBase = class {
        constructor(r) {
          e.console.assert(r.viewer, "[Drawer] options.viewer is required"), e.console.assert(r.viewport, "[Drawer] options.viewport is required"), e.console.assert(r.element, "[Drawer] options.element is required"), this._id = this.getType() + e.now(), this.viewer = r.viewer, this.viewport = r.viewport, this.debugGridColor = typeof r.debugGridColor == "string" ? [r.debugGridColor] : r.debugGridColor || e.DEFAULT_SETTINGS.debugGridColor, this.options = e.extend({}, this.defaultOptions, r.options), this.container = e.getElement(r.element), this._renderingTarget = this._createDrawingElement(), this.canvas.style.width = "100%", this.canvas.style.height = "100%", this.canvas.style.position = "absolute", this.canvas.style.left = "0", e.setElementOpacity(this.canvas, this.viewer.opacity, !0), e.setElementPointerEventsNone(this.canvas), e.setElementTouchActionNone(this.canvas), this.container.style.textAlign = "left", this.container.appendChild(this.canvas), this._checkInterfaceImplementation(), this.setInternalCacheNeedsRefresh();
        }
        /**
         * Retrieve default options for the current drawer.
         * The base implementation provides default shared options.
         * Overrides should enumerate all defaults or extend from this implementation.
         *   return $.extend({}, super.options, { ... custom drawer instance options ... });
         * @returns {BaseDrawerOptions} common options
         */
        get defaultOptions() {
          return {
            usePrivateCache: !1,
            preloadCache: !0
          };
        }
        // protect the canvas member with a getter
        get canvas() {
          return this._renderingTarget;
        }
        get element() {
          return e.console.error("Drawer.element is deprecated. Use Drawer.container instead."), this.container;
        }
        /**
         * Get unique drawer ID
         * @return {string}
         */
        getId() {
          return this._id;
        }
        /**
         * @abstract
         * @returns {String | undefined} What type of drawer this is. Must be overridden by extending classes.
         */
        getType() {
          e.console.error("Drawer.getType must be implemented by child class");
        }
        /**
         * Retrieve required data formats the data must be converted to.
         * This list MUST BE A VALID SUBSET OF getSupportedDataFormats()
         * @abstract
         * @return {string[]}
         */
        getRequiredDataFormats() {
          return this.getSupportedDataFormats();
        }
        /**
         * Retrieve data types
         * @abstract
         * @return {string[]}
         */
        getSupportedDataFormats() {
          throw "Drawer.getSupportedDataFormats must define its supported rendering data types!";
        }
        /**
         * Check a particular cache record is compatible.
         * This function _MUST_ be called: if it returns a falsey
         * value, the rendering _MUST NOT_ proceed. It should
         * await next animation frames and check again for availability.
         * @param {OpenSeadragon.Tile} tile
         * @return {any|undefined} undefined if cache not available, compatible data otherwise.
         */
        getDataToDraw(r) {
          const o = r.getCache(r.cacheKey);
          if (!o) {
            e.console.warn("Attempt to draw tile %s when not cached!", r);
            return;
          }
          const l = o.getDataForRendering(this, r);
          return l && l.data;
        }
        /**
         * @abstract
         * @returns {Boolean} Whether the drawer implementation is supported by the browser. Must be overridden by extending classes.
         */
        static isSupported() {
          e.console.error("Drawer.isSupported must be implemented by child class");
        }
        /**
         * @abstract
         * @returns {Element} the element to draw into
         * @private
         */
        _createDrawingElement() {
          return e.console.error("Drawer._createDrawingElement must be implemented by child class"), null;
        }
        /**
         * @abstract
         * @param {Array} tiledImages - An array of TiledImages that are ready to be drawn.
         * @private
         */
        draw(r) {
          e.console.error("Drawer.draw must be implemented by child class");
        }
        /**
         * @abstract
         * @returns {Boolean} True if rotation is supported.
         */
        canRotate() {
          e.console.error("Drawer.canRotate must be implemented by child class");
        }
        /**
         * @abstract
         */
        destroy() {
          e.console.error("Drawer.destroy must be implemented by child class");
        }
        /**
         * Destroy internal cache. Should be called within destroy() when
         * usePrivateCache is set to true. Ensures cleanup of anything created
         * by internalCacheCreate(...).
         */
        destroyInternalCache() {
          this.viewer.tileCache.clearDrawerInternalCache(this);
        }
        /**
         * @param {TiledImage} tiledImage the tiled image that is calling the function
         * @returns {Boolean} Whether this drawer requires enforcing minimum tile overlap to avoid showing seams.
         * @private
         */
        minimumOverlapRequired(r) {
          return !1;
        }
        /**
         * @abstract
         * @param {Boolean} [imageSmoothingEnabled] - Whether or not the image is
         * drawn smoothly on the canvas; see imageSmoothingEnabled in
         * {@link OpenSeadragon.Options} for more explanation.
         */
        setImageSmoothingEnabled(r) {
          e.console.error("Drawer.setImageSmoothingEnabled must be implemented by child class");
        }
        /**
         * Optional public API to draw a rectangle (e.g. for debugging purposes)
         * Child classes can override this method if they wish to support this
         * @param {OpenSeadragon.Rect} rect
         */
        drawDebuggingRect(r) {
          e.console.warn("[drawer].drawDebuggingRect is not implemented by this drawer");
        }
        // Deprecated functions
        clear() {
          e.console.warn("[drawer].clear() is deprecated. The drawer is responsible for clearing itself as needed before drawing tiles.");
        }
        /**
         * If options.usePrivateCache is true, this method MUST RETURN the private cache content
         * @param {OpenSeadragon.CacheRecord} cache
         * @param {OpenSeadragon.Tile} tile
         * @return any
         */
        internalCacheCreate(r, o) {
        }
        /**
         * It is possible to perform any necessary cleanup on internal cache, necessary if you
         * need to clean up some memory (e.g. destroy canvas by setting with & height to 0).
         * @param {*} data object returned by internalCacheCreate(...)
         */
        internalCacheFree(r) {
        }
        /**
         * Call to invalidate internal cache. It will be rebuilt. With synchronous converions,
         * it will be rebuilt immediatelly. With asynchronous, it will be rebuilt once invalidation
         * routine happens, e.g. you should call also requestInvalidate() if you need to happen
         * it as soon as possible.
         */
        setInternalCacheNeedsRefresh() {
          this._dataNeedsRefresh = e.now();
        }
        /**
         * When a Tiled Image is initialized and ready, this method is called.
         * Unlike with events, here it is guaranteed that all external code has finished
         * processing (under normal circumstances) and the tiled image should not change.
         * @param {OpenSeadragon.TiledImage} tiledImage target image that has been created
         */
        tiledImageCreated(r) {
        }
        // Private functions
        /**
         * Ensures that child classes have provided implementations for public API methods
         * draw, canRotate, destroy, and setImageSmoothinEnabled. Throws an exception if the original
         * placeholder methods are still in place.
         * @private
         *
         */
        _checkInterfaceImplementation() {
          if (this._createDrawingElement === e.DrawerBase.prototype._createDrawingElement)
            throw new Error("[drawer]._createDrawingElement must be implemented by child class");
          if (this.draw === e.DrawerBase.prototype.draw)
            throw new Error("[drawer].draw must be implemented by child class");
          if (this.canRotate === e.DrawerBase.prototype.canRotate)
            throw new Error("[drawer].canRotate must be implemented by child class");
          if (this.destroy === e.DrawerBase.prototype.destroy)
            throw new Error("[drawer].destroy must be implemented by child class");
          if (this.setImageSmoothingEnabled === e.DrawerBase.prototype.setImageSmoothingEnabled)
            throw new Error("[drawer].setImageSmoothingEnabled must be implemented by child class");
        }
        // Utility functions
        /**
         * Scale from OpenSeadragon viewer rectangle to drawer rectangle
         * (ignoring rotation)
         * @param {OpenSeadragon.Rect} rectangle - The rectangle in viewport coordinate system.
         * @returns {OpenSeadragon.Rect} Rectangle in drawer coordinate system.
         */
        viewportToDrawerRectangle(r) {
          const o = this.viewport.pixelFromPointNoRotate(r.getTopLeft(), !0), l = this.viewport.deltaPixelsFromPointsNoRotate(r.getSize(), !0);
          return new e.Rect(
            o.x * e.pixelDensityRatio,
            o.y * e.pixelDensityRatio,
            l.x * e.pixelDensityRatio,
            l.y * e.pixelDensityRatio
          );
        }
        /**
         * This function converts the given point from to the drawer coordinate by
         * multiplying it with the pixel density.
         * This function does not take rotation into account, thus assuming provided
         * point is at 0 degree.
         * @param {OpenSeadragon.Point} point - the pixel point to convert
         * @returns {OpenSeadragon.Point} Point in drawer coordinate system.
         */
        viewportCoordToDrawerCoord(r) {
          const o = this.viewport.pixelFromPointNoRotate(r, !0);
          return new e.Point(
            o.x * e.pixelDensityRatio,
            o.y * e.pixelDensityRatio
          );
        }
        // Internal utility functions
        /**
         * Calculate width and height of the canvas based on viewport dimensions
         * and pixelDensityRatio
         * @private
         * @returns {OpenSeadragon.Point} {x, y} size of the canvas
         */
        _calculateCanvasSize() {
          const r = e.pixelDensityRatio, o = this.viewport.getContainerSize();
          return new t.Point(Math.round(o.x * r), Math.round(o.y * r));
        }
        /**
         * Called by implementations to fire the tiled-image-drawn event (used by tests)
         * @private
         */
        _raiseTiledImageDrawnEvent(r, o) {
          this.viewer && this.viewer.raiseEvent("tiled-image-drawn", {
            tiledImage: r,
            tiles: o
          });
        }
        /**
         * Called by implementations to fire the drawer-error event
         * @private
         */
        _raiseDrawerErrorEvent(r, o) {
          this.viewer && this.viewer.raiseEvent("drawer-error", {
            tiledImage: r,
            drawer: this,
            error: o
          });
        }
      };
    })(n), (function(e) {
      const t = e;
      class i extends t.DrawerBase {
        constructor(o) {
          super(o), this.viewer.rejectEventHandler("tile-drawing", "The HTMLDrawer does not raise the tile-drawing event"), this.viewer.allowEventHandler("tile-drawn");
          function l(c, d) {
            const f = e.makeNeutralElement("div"), p = d.cloneNode();
            p.style.msInterpolationMode = "nearest-neighbor", p.style.width = "100%", p.style.height = "100%";
            const b = f.style;
            return b.position = "absolute", {
              element: f,
              imgElement: p,
              style: b,
              data: d
            };
          }
          e.converter.learn("image", i.imageCacheType, l, 1, 1), e.converter.learn(i.imageCacheType, "image", (c, d) => d.data, 1, 3);
          function a(c) {
            c.imgElement && c.imgElement.parentNode && c.imgElement.parentNode.removeChild(c.imgElement), c.element && c.element.parentNode && c.element.parentNode.removeChild(c.element);
          }
          e.converter.learnDestroy(i.imageCacheType, a);
        }
        static get imageCacheType() {
          return "htmlDrawer[image]";
        }
        static get canvasCacheType() {
          return "htmlDrawer[canvas]";
        }
        /**
         * @returns {Boolean} always true
         */
        static isSupported() {
          return !0;
        }
        /**
         *
         * @returns 'html'
         */
        getType() {
          return "html";
        }
        getSupportedDataFormats() {
          return [i.imageCacheType, i.canvasCacheType];
        }
        /**
         * @param {TiledImage} tiledImage the tiled image that is calling the function
         * @returns {Boolean} Whether this drawer requires enforcing minimum tile overlap to avoid showing seams.
         * @private
         */
        minimumOverlapRequired(o) {
          return !0;
        }
        /**
         * create the HTML element (e.g. canvas, div) that the image will be drawn into
         * @returns {Element} the div to draw into
         */
        _createDrawingElement() {
          return e.makeNeutralElement("div");
        }
        /**
         * Draws the TiledImages
         */
        draw(o) {
          const l = this;
          this._prepareNewFrame(), o.forEach(function(a) {
            a.opacity !== 0 && l._drawTiles(a);
          });
        }
        /**
         * @returns {Boolean} False - rotation is not supported.
         */
        canRotate() {
          return !1;
        }
        /**
         * Destroy the drawer (unload current loaded tiles)
         */
        destroy() {
          this.container.removeChild(this.canvas);
        }
        /**
         * This function is ignored by the HTML Drawer. Implementing it is required by DrawerBase.
         * @param {Boolean} [imageSmoothingEnabled] - Whether or not the image is
         * drawn smoothly on the canvas; see imageSmoothingEnabled in
         * {@link OpenSeadragon.Options} for more explanation.
         */
        setImageSmoothingEnabled() {
        }
        /**
         * Clears the Drawer so it's ready to draw another frame.
         * @private
         *
         */
        _prepareNewFrame() {
          this.canvas.innerHTML = "";
        }
        /**
         * Draws a TiledImage.
         * @private
         *
         */
        _drawTiles(o) {
          const l = o.getTilesToDraw().map((a) => a.tile);
          if (!(o.opacity === 0 || l.length === 0 && !o.placeholderFillStyle))
            for (let a = l.length - 1; a >= 0; a--) {
              const c = l[a];
              this._drawTile(c), this.viewer && this.viewer.raiseEvent("tile-drawn", {
                tiledImage: o,
                tile: c
              });
            }
        }
        /**
         * Draws the given tile.
         * @private
         * @param {OpenSeadragon.Tile} tile - The tile to draw.
         * @param {Function} drawingHandler - Method for firing the drawing event if using canvas.
         * drawingHandler({context, tile, rendered})
         */
        _drawTile(o) {
          e.console.assert(o, "[Drawer._drawTile] tile is required");
          let l = this.canvas;
          if (!o.loaded) {
            e.console.warn(
              "Attempting to draw tile %s when it's not yet loaded.",
              o.toString()
            );
            return;
          }
          const a = this.getDataToDraw(o);
          a && (a.element.parentNode !== l && l.appendChild(a.element), a.imgElement.parentNode !== a.element && a.element.appendChild(a.imgElement), a.style.top = o.position.y + "px", a.style.left = o.position.x + "px", a.style.height = o.size.y + "px", a.style.width = o.size.x + "px", o.flipped && (a.style.transform = "scaleX(-1)"), e.setElementOpacity(a.element, o.opacity));
        }
      }
      e.HTMLDrawer = i;
    })(n), (function(e) {
      const t = e;
      class i extends t.DrawerBase {
        constructor(d) {
          super(d), this.context = this.canvas.getContext("2d"), this.sketchCanvas = null, this.sketchContext = null, this._imageSmoothingEnabled = !0, this.viewer.allowEventHandler("tile-drawn"), this.viewer.allowEventHandler("tile-drawing");
        }
        /**
         * @returns {Boolean} true if canvas is supported by the browser, otherwise false
         */
        static isSupported() {
          return e.supportsCanvas;
        }
        getType() {
          return "canvas";
        }
        getSupportedDataFormats() {
          return ["context2d"];
        }
        /**
         * create the HTML element (e.g. canvas, div) that the image will be drawn into
         * @returns {Element} the canvas to draw into
         */
        _createDrawingElement() {
          const d = e.makeNeutralElement("canvas"), f = this._calculateCanvasSize();
          return d.width = f.x, d.height = f.y, d;
        }
        /**
         * Draws the TiledImages
         */
        draw(d) {
          this._prepareNewFrame(), this.viewer.viewport.getFlip() !== this._viewportFlipped && this._flip();
          for (const f of d)
            f.opacity !== 0 && this._drawTiles(f);
        }
        /**
         * @returns {Boolean} True - rotation is supported.
         */
        canRotate() {
          return !0;
        }
        /**
         * Destroy the drawer (unload current loaded tiles)
         */
        destroy() {
          this.canvas.width = 1, this.canvas.height = 1, this.sketchCanvas = null, this.sketchContext = null, this.container.removeChild(this.canvas);
        }
        /**
         * @param {TiledImage} tiledImage the tiled image that is calling the function
         * @returns {Boolean} Whether this drawer requires enforcing minimum tile overlap to avoid showing seams.
         * @private
         */
        minimumOverlapRequired(d) {
          return !0;
        }
        /**
         * Turns image smoothing on or off for this viewer. Note: Ignored in some (especially older) browsers that do not support this property.
         *
         * @function
         * @param {Boolean} [imageSmoothingEnabled] - Whether or not the image is
         * drawn smoothly on the canvas; see imageSmoothingEnabled in
         * {@link OpenSeadragon.Options} for more explanation.
         */
        setImageSmoothingEnabled(d) {
          this._imageSmoothingEnabled = !!d, this._updateImageSmoothingEnabled(this.context), this.viewer.forceRedraw();
        }
        /**
         * Draw a rectangle onto the canvas
         * @param {OpenSeadragon.Rect} rect
         */
        drawDebuggingRect(d) {
          const f = this.context;
          f.save(), f.lineWidth = 2 * e.pixelDensityRatio, f.strokeStyle = this.debugGridColor[0], f.fillStyle = this.debugGridColor[0], f.strokeRect(
            d.x * e.pixelDensityRatio,
            d.y * e.pixelDensityRatio,
            d.width * e.pixelDensityRatio,
            d.height * e.pixelDensityRatio
          ), f.restore();
        }
        /**
         * Test whether the current context is flipped or not
         * @private
         */
        get _viewportFlipped() {
          return this.context.getTransform().a < 0;
        }
        /**
         * Fires the tile-drawing event.
         * @private
         */
        _raiseTileDrawingEvent(d, f, p, b) {
          this.viewer.raiseEvent("tile-drawing", {
            tiledImage: d,
            context: f,
            tile: p,
            rendered: b
          });
        }
        /**
         * Clears the Drawer so it's ready to draw another frame.
         * @private
         *
         */
        _prepareNewFrame() {
          const d = this._calculateCanvasSize();
          if ((this.canvas.width !== d.x || this.canvas.height !== d.y) && (this.canvas.width = d.x, this.canvas.height = d.y, this._updateImageSmoothingEnabled(this.context), this.sketchCanvas !== null)) {
            const f = this._calculateSketchCanvasSize();
            this.sketchCanvas.width = f.x, this.sketchCanvas.height = f.y, this._updateImageSmoothingEnabled(this.sketchContext);
          }
          this._clear();
        }
        /**
         * @private
         * @param {Boolean} useSketch Whether to clear sketch canvas or main canvas
         * @param {OpenSeadragon.Rect} [bounds] The rectangle to clear
         */
        _clear(d, f) {
          const p = this._getContext(d);
          if (f)
            p.clearRect(f.x, f.y, f.width, f.height);
          else {
            const b = p.canvas;
            p.clearRect(0, 0, b.width, b.height);
          }
        }
        /**
         * Draws a TiledImage.
         * @private
         *
         */
        _drawTiles(d) {
          const f = d.getTilesToDraw().map((se) => se.tile);
          if (d.opacity === 0 || f.length === 0 && !d.placeholderFillStyle)
            return;
          let p = f[0], b;
          p && (b = d.opacity < 1 || d.compositeOperation && d.compositeOperation !== "source-over" || !d._isBottomItem() && d.source.hasTransparency(null, p.getUrl(), p.ajaxHeaders, p.postData));
          let x, T;
          const I = this.viewport.getZoom(!0), z = d.viewportToImageZoom(I);
          if (f.length > 1 && z > d.smoothTileEdgesMinZoom && !d.iOSDevice && d.getRotation(!0) % 360 === 0) {
            b = !0;
            const se = p.length && this.getDataToDraw(p);
            se ? x = se.canvas.width / (p.size.x * e.pixelDensityRatio) : x = 1, T = p.getTranslationForEdgeSmoothing(
              x,
              this._getCanvasSize(!1),
              this._getCanvasSize(!0)
            );
          }
          let W;
          b && (x || (W = this.viewport.viewportToViewerElementRectangle(
            d.getClippedBounds(!0)
          ).getIntegerBoundingBox(), W = W.times(e.pixelDensityRatio)), this._clear(!0, W)), x || this._setRotations(d, b);
          let j = !1;
          if (d._clip) {
            this._saveContext(b);
            let se = d.imageToViewportRectangle(d._clip, !0);
            se = se.rotate(-d.getRotation(!0), d._getRotationPoint(!0));
            let fe = this.viewportToDrawerRectangle(se);
            x && (fe = fe.times(x)), T && (fe = fe.translate(T)), this._setClip(fe, b), j = !0;
          }
          if (d._croppingPolygons) {
            const se = this;
            j || this._saveContext(b);
            try {
              const fe = d._croppingPolygons.map(function(Ie) {
                return Ie.map(function(Ae) {
                  const qe = d.imageToViewportCoordinates(Ae.x, Ae.y, !0).rotate(-d.getRotation(!0), d._getRotationPoint(!0));
                  let Ne = se.viewportCoordToDrawerCoord(qe);
                  return x && (Ne = Ne.times(x)), T && (Ne = Ne.plus(T)), Ne;
                });
              });
              this._clipWithPolygons(fe, b);
            } catch (fe) {
              e.console.error(fe);
            }
            j = !0;
          }
          if (d._hasOpaqueTile = !1, d.placeholderFillStyle && d._hasOpaqueTile === !1) {
            let se = this.viewportToDrawerRectangle(d.getBoundsNoRotate(!0));
            x && (se = se.times(x)), T && (se = se.translate(T));
            let fe = null;
            typeof d.placeholderFillStyle == "function" ? fe = d.placeholderFillStyle(d, this.context) : fe = d.placeholderFillStyle, this._drawRectangle(se, fe, b);
          }
          const oe = a(d.subPixelRoundingForTransparency);
          let ae = !1;
          oe === e.SUBPIXEL_ROUNDING_OCCURRENCES.ALWAYS ? ae = !0 : oe === e.SUBPIXEL_ROUNDING_OCCURRENCES.ONLY_AT_REST && (ae = !(this.viewer && this.viewer.isAnimating()));
          for (let se = 0; se < f.length; se++)
            p = f[se], this._drawTile(
              p,
              d,
              b,
              x,
              T,
              ae,
              d.source
            ), this.viewer && this.viewer.raiseEvent("tile-drawn", {
              tiledImage: d,
              tile: p
            });
          j && this._restoreContext(b), x || (d.getRotation(!0) % 360 !== 0 && this._restoreRotationChanges(b), this.viewport.getRotation(!0) % 360 !== 0 && this._restoreRotationChanges(b)), b && (x && this._setRotations(d), this.blendSketch({
            opacity: d.opacity,
            scale: x,
            translate: T,
            compositeOperation: d.compositeOperation,
            bounds: W
          }), x && (d.getRotation(!0) % 360 !== 0 && this._restoreRotationChanges(!1), this.viewport.getRotation(!0) % 360 !== 0 && this._restoreRotationChanges(!1))), this._drawDebugInfo(d, f), this._raiseTiledImageDrawnEvent(d, f);
        }
        /**
         * Draws special debug information for a TiledImage if in debug mode.
         * @private
         * @param {OpenSeadragon.Tile[]} lastDrawn - An unordered list of Tiles drawn last frame.
         */
        _drawDebugInfo(d, f) {
          if (d.debugMode)
            for (let p = f.length - 1; p >= 0; p--) {
              const b = f[p];
              try {
                this._drawDebugInfoOnTile(b, f.length, p, d);
              } catch (x) {
                e.console.error(x);
              }
            }
        }
        /**
         * This function will create multiple polygon paths on the drawing context by provided polygons,
         * then clip the context to the paths.
         * @private
         * @param {OpenSeadragon.Point[][]} polygons - an array of polygons. A polygon is an array of OpenSeadragon.Point
         * @param {Boolean} useSketch - Whether to use the sketch canvas or not.
         */
        _clipWithPolygons(d, f) {
          const p = this._getContext(f);
          p.beginPath();
          for (const b of d)
            for (const [x, T] of b.entries())
              p[x === 0 ? "moveTo" : "lineTo"](T.x, T.y);
          p.clip();
        }
        /**
         * Draws the given tile.
         * @private
         * @param {OpenSeadragon.Tile} tile - The tile to draw.
         * @param {OpenSeadragon.TiledImage} tiledImage - The tiled image being drawn.
         * @param {Boolean} useSketch - Whether to use the sketch canvas or not.
         * where <code>rendered</code> is the context with the pre-drawn image.
         * @param {Float} [scale=1] - Apply a scale to tile position and size. Defaults to 1.
         * @param {OpenSeadragon.Point} [translate] A translation vector to offset tile position
         * @param {Boolean} [shouldRoundPositionAndSize] - Tells whether to round
         * position and size of tiles supporting alpha channel in non-transparency
         * context.
         * @param {OpenSeadragon.TileSource} source - The source specification of the tile.
         */
        _drawTile(d, f, p, b, x, T, I) {
          if (e.console.assert(d, "[Drawer._drawTile] tile is required"), e.console.assert(f, "[Drawer._drawTile] drawingHandler is required"), !d.loaded) {
            e.console.warn(
              "Attempting to draw tile %s when it's not yet loaded.",
              d.toString()
            );
            return;
          }
          const z = this.getDataToDraw(d);
          if (!z)
            return;
          const W = this._getContext(p);
          b = b || 1;
          let j = d.position.times(e.pixelDensityRatio), oe = d.size.times(e.pixelDensityRatio);
          W.save(), typeof b == "number" && b !== 1 && (j = j.times(b), oe = oe.times(b)), x instanceof e.Point && (j = j.plus(x)), W.globalAlpha === 1 && d.hasTransparency && (T && (j.x = Math.round(j.x), j.y = Math.round(j.y), oe.x = Math.round(oe.x), oe.y = Math.round(oe.y)), W.clearRect(
            j.x,
            j.y,
            oe.x,
            oe.y
          )), this._raiseTileDrawingEvent(f, W, d, z);
          let ae, se;
          d.sourceBounds ? (ae = Math.min(d.sourceBounds.width, z.canvas.width), se = Math.min(d.sourceBounds.height, z.canvas.height)) : (ae = z.canvas.width, se = z.canvas.height), W.translate(j.x + oe.x / 2, 0), d.flipped && W.scale(-1, 1), W.drawImage(
            z.canvas,
            0,
            0,
            ae,
            se,
            -oe.x / 2,
            j.y,
            oe.x,
            oe.y
          ), W.restore();
        }
        /**
         * Get the context of the main or sketch canvas
         * @private
         * @param {Boolean} useSketch
         * @returns {CanvasRenderingContext2D}
         */
        _getContext(d) {
          let f = this.context;
          if (d) {
            if (this.sketchCanvas === null) {
              this.sketchCanvas = document.createElement("canvas");
              const p = this._calculateSketchCanvasSize();
              if (this.sketchCanvas.width = p.x, this.sketchCanvas.height = p.y, this.sketchContext = this.sketchCanvas.getContext("2d"), this.viewport.getRotation() === 0) {
                const b = this;
                this.viewer.addHandler("rotate", function x() {
                  if (b.viewport.getRotation() === 0)
                    return;
                  b.viewer.removeHandler("rotate", x);
                  const T = b._calculateSketchCanvasSize();
                  b.sketchCanvas.width = T.x, b.sketchCanvas.height = T.y;
                });
              }
              this._updateImageSmoothingEnabled(this.sketchContext);
            }
            f = this.sketchContext;
          }
          return f;
        }
        /**
         * Save the context of the main or sketch canvas
         * @private
         * @param {Boolean} useSketch
         */
        _saveContext(d) {
          this._getContext(d).save();
        }
        /**
         * Restore the context of the main or sketch canvas
         * @private
         * @param {Boolean} useSketch
         */
        _restoreContext(d) {
          this._getContext(d).restore();
        }
        // private
        _setClip(d, f) {
          const p = this._getContext(f);
          p.beginPath(), p.rect(d.x, d.y, d.width, d.height), p.clip();
        }
        // private
        // used to draw a placeholder rectangle
        _drawRectangle(d, f, p) {
          const b = this._getContext(p);
          b.save(), b.fillStyle = f, b.fillRect(d.x, d.y, d.width, d.height), b.restore();
        }
        /**
         * Blends the sketch canvas in the main canvas.
         * @param {Object} options The options
         * @param {Float} options.opacity The opacity of the blending.
         * @param {Float} [options.scale=1] The scale at which tiles were drawn on
         * the sketch. Default is 1.
         * Use scale to draw at a lower scale and then enlarge onto the main canvas.
         * @param {OpenSeadragon.Point} [options.translate] A translation vector
         * that was used to draw the tiles
         * @param {String} [options.compositeOperation] - How the image is
         * composited onto other images; see compositeOperation in
         * {@link OpenSeadragon.Options} for possible values.
         * @param {OpenSeadragon.Rect} [options.bounds] The part of the sketch
         * canvas to blend in the main canvas. If specified, options.scale and
         * options.translate get ignored.
         */
        blendSketch(d, f, p, b) {
          let x = d;
          e.isPlainObject(x) || (x = {
            opacity: d,
            scale: f,
            translate: p,
            compositeOperation: b
          }), d = x.opacity, b = x.compositeOperation;
          const T = x.bounds;
          if (this.context.save(), this.context.globalAlpha = d, b && (this.context.globalCompositeOperation = b), T)
            T.x < 0 && (T.width += T.x, T.x = 0), T.x + T.width > this.canvas.width && (T.width = this.canvas.width - T.x), T.y < 0 && (T.height += T.y, T.y = 0), T.y + T.height > this.canvas.height && (T.height = this.canvas.height - T.y), this.context.drawImage(
              this.sketchCanvas,
              T.x,
              T.y,
              T.width,
              T.height,
              T.x,
              T.y,
              T.width,
              T.height
            );
          else {
            f = x.scale || 1, p = x.translate;
            const I = p instanceof e.Point ? p : new e.Point(0, 0);
            let z = 0, W = 0;
            if (p) {
              const j = this.sketchCanvas.width - this.canvas.width, oe = this.sketchCanvas.height - this.canvas.height;
              z = Math.round(j / 2), W = Math.round(oe / 2);
            }
            this.context.drawImage(
              this.sketchCanvas,
              I.x - z * f,
              I.y - W * f,
              (this.canvas.width + 2 * z) * f,
              (this.canvas.height + 2 * W) * f,
              -z,
              -W,
              this.canvas.width + 2 * z,
              this.canvas.height + 2 * W
            );
          }
          this.context.restore();
        }
        // private
        _drawDebugInfoOnTile(d, f, p, b) {
          const x = this.viewer.world.getIndexOfItem(b) % this.debugGridColor.length, T = this.context;
          T.save(), T.lineWidth = 2 * e.pixelDensityRatio, T.font = "small-caps bold " + 13 * e.pixelDensityRatio + "px arial", T.strokeStyle = this.debugGridColor[x], T.fillStyle = this.debugGridColor[x], this._setRotations(b), this._viewportFlipped && this._flip({ point: d.position.plus(d.size.divide(2)) }), T.strokeRect(
            d.position.x * e.pixelDensityRatio,
            d.position.y * e.pixelDensityRatio,
            d.size.x * e.pixelDensityRatio,
            d.size.y * e.pixelDensityRatio
          );
          const I = (d.position.x + d.size.x / 2) * e.pixelDensityRatio, z = (d.position.y + d.size.y / 2) * e.pixelDensityRatio;
          T.translate(I, z);
          const W = this.viewport.getRotation(!0);
          T.rotate(Math.PI / 180 * -W), T.translate(-I, -z), d.x === 0 && d.y === 0 && (T.fillText(
            "Zoom: " + this.viewport.getZoom(),
            d.position.x * e.pixelDensityRatio,
            (d.position.y - 30) * e.pixelDensityRatio
          ), T.fillText(
            "Pan: " + this.viewport.getBounds().toString(),
            d.position.x * e.pixelDensityRatio,
            (d.position.y - 20) * e.pixelDensityRatio
          )), T.fillText(
            "Level: " + d.level,
            (d.position.x + 10) * e.pixelDensityRatio,
            (d.position.y + 20) * e.pixelDensityRatio
          ), T.fillText(
            "Column: " + d.x,
            (d.position.x + 10) * e.pixelDensityRatio,
            (d.position.y + 30) * e.pixelDensityRatio
          ), T.fillText(
            "Row: " + d.y,
            (d.position.x + 10) * e.pixelDensityRatio,
            (d.position.y + 40) * e.pixelDensityRatio
          ), T.fillText(
            "Order: " + p + " of " + f,
            (d.position.x + 10) * e.pixelDensityRatio,
            (d.position.y + 50) * e.pixelDensityRatio
          ), T.fillText(
            "Size: " + d.size.toString(),
            (d.position.x + 10) * e.pixelDensityRatio,
            (d.position.y + 60) * e.pixelDensityRatio
          ), T.fillText(
            "Position: " + d.position.toString(),
            (d.position.x + 10) * e.pixelDensityRatio,
            (d.position.y + 70) * e.pixelDensityRatio
          ), this.viewport.getRotation(!0) % 360 !== 0 && this._restoreRotationChanges(), b.getRotation(!0) % 360 !== 0 && this._restoreRotationChanges(), T.restore();
        }
        // private
        _updateImageSmoothingEnabled(d) {
          d.msImageSmoothingEnabled = this._imageSmoothingEnabled, d.imageSmoothingEnabled = this._imageSmoothingEnabled;
        }
        /**
         * Get the canvas size
         * @private
         * @param {Boolean} sketch If set to true return the size of the sketch canvas
         * @returns {OpenSeadragon.Point} The size of the canvas
         */
        _getCanvasSize(d) {
          const f = this._getContext(d).canvas;
          return new e.Point(f.width, f.height);
        }
        /**
         * Get the canvas center
         * @private
         * @param {Boolean} sketch If set to true return the center point of the sketch canvas
         * @returns {OpenSeadragon.Point} The center point of the canvas
         */
        _getCanvasCenter() {
          return new e.Point(this.canvas.width / 2, this.canvas.height / 2);
        }
        /**
         * Set rotations for viewport & tiledImage
         * @private
         * @param {OpenSeadragon.TiledImage} tiledImage
         * @param {Boolean} [useSketch=false]
         */
        _setRotations(d, f = !1) {
          let p = !1;
          this.viewport.getRotation(!0) % 360 !== 0 && (this._offsetForRotation({
            degrees: this.viewport.getRotation(!0),
            useSketch: f,
            saveContext: p
          }), p = !1), d.getRotation(!0) % 360 !== 0 && this._offsetForRotation({
            degrees: d.getRotation(!0),
            point: this.viewport.pixelFromPointNoRotate(
              d._getRotationPoint(!0),
              !0
            ),
            useSketch: f,
            saveContext: p
          });
        }
        // private
        _offsetForRotation(d) {
          const f = d.point ? d.point.times(e.pixelDensityRatio) : this._getCanvasCenter(), p = this._getContext(d.useSketch);
          p.save(), p.translate(f.x, f.y), p.rotate(Math.PI / 180 * d.degrees), p.translate(-f.x, -f.y);
        }
        // private
        _flip(d) {
          d = d || {};
          const f = d.point ? d.point.times(e.pixelDensityRatio) : this._getCanvasCenter(), p = this._getContext(d.useSketch);
          p.translate(f.x, 0), p.scale(-1, 1), p.translate(-f.x, 0);
        }
        // private
        _restoreRotationChanges(d) {
          this._getContext(d).restore();
        }
        // private
        _calculateCanvasSize() {
          const d = e.pixelDensityRatio, f = this.viewport.getContainerSize();
          return {
            // canvas width and height are integers
            x: Math.round(f.x * d),
            y: Math.round(f.y * d)
          };
        }
        // private
        _calculateSketchCanvasSize() {
          const d = this._calculateCanvasSize();
          if (this.viewport.getRotation() === 0)
            return d;
          const f = Math.ceil(Math.sqrt(
            d.x * d.x + d.y * d.y
          ));
          return {
            x: f,
            y: f
          };
        }
      }
      e.CanvasDrawer = i;
      const r = e.SUBPIXEL_ROUNDING_OCCURRENCES.NEVER;
      function o(c) {
        return c !== e.SUBPIXEL_ROUNDING_OCCURRENCES.ALWAYS && c !== e.SUBPIXEL_ROUNDING_OCCURRENCES.ONLY_AT_REST && c !== e.SUBPIXEL_ROUNDING_OCCURRENCES.NEVER;
      }
      function l(c) {
        return o(c) ? r : c;
      }
      function a(c) {
        if (typeof c == "number")
          return l(c);
        if (!c || !e.Browser)
          return r;
        let d = c[e.Browser.vendor];
        return o(d) && (d = c["*"]), l(d);
      }
    })(n), (function(e) {
      const t = e;
      t.WebGLDrawer = class extends t.DrawerBase {
        constructor(r) {
          super(r), this._destroyed = !1, this._gl = null, this._firstPass = null, this._secondPass = null, this._glFrameBuffer = null, this._renderToTexture = null, this._outputCanvas = null, this._outputContext = null, this._clippingCanvas = null, this._clippingContext = null, this._renderingCanvas = null, this._backupCanvasDrawer = null, this._imageSmoothingEnabled = !0, this._unpackWithPremultipliedAlpha = !!this.options.unpackWithPremultipliedAlpha, this.viewer.rejectEventHandler("tile-drawn", "The WebGLDrawer does not raise the tile-drawn event"), this.viewer.rejectEventHandler("tile-drawing", "The WebGLDrawer does not raise the tile-drawing event"), this._setupCanvases(), this._setupRenderer(), this._supportedFormats = ["context2d", "image"], this.context = this._outputContext;
        }
        get defaultOptions() {
          return {
            // use detached cache: our type conversion will not collide (and does not have to preserve CPU data ref)
            usePrivateCache: !0,
            preloadCache: !1,
            unpackWithPremultipliedAlpha: !1
          };
        }
        getSupportedDataFormats() {
          return this._supportedFormats;
        }
        // Public API required by all Drawer implementations
        /**
        * Clean up the renderer, removing all resources
        */
        destroy() {
          if (this._destroyed)
            return;
          const r = this._gl, o = r.getParameter(r.MAX_TEXTURE_IMAGE_UNITS);
          for (let a = 0; a < o; ++a)
            r.activeTexture(r.TEXTURE0 + a), r.bindTexture(r.TEXTURE_2D, null), r.bindTexture(r.TEXTURE_CUBE_MAP, null);
          r.bindBuffer(r.ARRAY_BUFFER, null), r.bindBuffer(r.ELEMENT_ARRAY_BUFFER, null), r.bindRenderbuffer(r.RENDERBUFFER, null), r.bindFramebuffer(r.FRAMEBUFFER, null), r.deleteBuffer(this._secondPass.bufferOutputPosition), r.deleteFramebuffer(this._glFrameBuffer), this._renderingCanvas.width = this._renderingCanvas.height = 1, this._clippingCanvas.width = this._clippingCanvas.height = 1, this._outputCanvas.width = this._outputCanvas.height = 1, this._renderingCanvas = null, this._clippingCanvas = this._clippingContext = null, this._outputCanvas = this._outputContext = null;
          const l = r.getExtension("WEBGL_lose_context");
          l && l.loseContext(), this._gl = null, this._backupCanvasDrawer && (this._backupCanvasDrawer.destroy(), this._backupCanvasDrawer = null), this.container.removeChild(this.canvas), this.viewer.drawer === this && (this.viewer.drawer = null), this.destroyInternalCache(), this._destroyed = !0;
        }
        // Public API required by all Drawer implementations
        /**
        *
        * @returns {Boolean} true
        */
        canRotate() {
          return !0;
        }
        // Public API required by all Drawer implementations
        /**
        * @returns {Boolean} true if canvas and webgl are supported
        */
        static isSupported() {
          const r = document.createElement("canvas"), o = e.isFunction(r.getContext) && r.getContext("webgl"), l = o && o.getExtension("WEBGL_lose_context");
          return l && l.loseContext(), !!o;
        }
        /**
         *
         * @returns {string} 'webgl'
         */
        getType() {
          return "webgl";
        }
        /**
         * @param {TiledImage} tiledImage the tiled image that is calling the function
         * @returns {Boolean} Whether this drawer requires enforcing minimum tile overlap to avoid showing seams.
         * @private
         */
        minimumOverlapRequired(r) {
          return r.isTainted();
        }
        /**
        * create the HTML element (canvas in this case) that the image will be drawn into
        * @private
        * @returns {Element} the canvas to draw into
        */
        _createDrawingElement() {
          const r = e.makeNeutralElement("canvas"), o = this._calculateCanvasSize();
          return r.width = o.x, r.height = o.y, r;
        }
        /**
         * Get the backup renderer (CanvasDrawer) to use if data cannot be used by webgl
         * Lazy loaded
         * @private
         * @returns {CanvasDrawer}
         */
        _getBackupCanvasDrawer() {
          return this._backupCanvasDrawer || (this._backupCanvasDrawer = this.viewer.requestDrawer("canvas", { mainDrawer: !1 }), this._backupCanvasDrawer.canvas.style.setProperty("visibility", "hidden"), this._backupCanvasDrawer.getSupportedDataFormats = () => this._supportedFormats, this._backupCanvasDrawer.getDataToDraw = this.getDataToDraw.bind(this)), this._backupCanvasDrawer;
        }
        /**
        *
        * @param {Array} tiledImages Array of TiledImage objects to draw
        */
        draw(r) {
          const o = this._gl, l = this.viewport.getBoundsNoRotateWithMargins(!0), a = {
            bounds: l,
            center: new t.Point(l.x + l.width / 2, l.y + l.height / 2),
            rotation: this.viewport.getRotation(!0) * Math.PI / 180
          }, c = this.viewport.flipped ? -1 : 1, d = e.Mat3.makeTranslation(-a.center.x, -a.center.y), f = e.Mat3.makeScaling(2 / a.bounds.width * c, -2 / a.bounds.height), p = e.Mat3.makeRotation(-a.rotation), b = f.multiply(p).multiply(d);
          o.bindFramebuffer(o.FRAMEBUFFER, null), o.clear(o.COLOR_BUFFER_BIT), this._outputContext.clearRect(0, 0, this._outputCanvas.width, this._outputCanvas.height);
          let x = !1;
          r.forEach((T, I) => {
            if (T.isTainted()) {
              x && (this._outputContext.drawImage(this._renderingCanvas, 0, 0), o.bindFramebuffer(o.FRAMEBUFFER, null), o.clear(o.COLOR_BUFFER_BIT), x = !1);
              const z = this._getBackupCanvasDrawer();
              z.draw([T]), this._outputContext.drawImage(z.canvas, 0, 0);
            } else {
              const z = T.getTilesToDraw();
              if (T.placeholderFillStyle && T._hasOpaqueTile === !1 && this._drawPlaceholder(T), z.length === 0 || T.getOpacity() === 0)
                return;
              const W = z[0], j = T.compositeOperation || this.viewer.compositeOperation || T._clip || T._croppingPolygons || T.debugMode, oe = j || T.opacity < 1 || W.tile.hasTransparency;
              j && (x && this._outputContext.drawImage(this._renderingCanvas, 0, 0), o.bindFramebuffer(o.FRAMEBUFFER, null), o.clear(o.COLOR_BUFFER_BIT)), o.useProgram(this._firstPass.shaderProgram), oe ? (o.bindFramebuffer(o.FRAMEBUFFER, this._glFrameBuffer), o.clear(o.COLOR_BUFFER_BIT)) : o.bindFramebuffer(o.FRAMEBUFFER, null);
              let ae = b;
              const se = T.getRotation(!0);
              if (se % 360 !== 0) {
                const Fe = e.Mat3.makeRotation(-se * Math.PI / 180), De = T.getBoundsNoRotate(!0).getCenter(), wt = e.Mat3.makeTranslation(De.x, De.y), lt = e.Mat3.makeTranslation(-De.x, -De.y), ct = wt.multiply(Fe).multiply(lt);
                ae = b.multiply(ct);
              }
              const fe = this._gl.getParameter(this._gl.MAX_TEXTURE_IMAGE_UNITS);
              if (fe <= 0)
                throw new Error(`WegGL error: bad value for gl parameter MAX_TEXTURE_IMAGE_UNITS (${fe}). This could happen
                        if too many contexts have been created and not released, or there is another problem with the graphics card.`);
              const Ie = new Float32Array(fe * 12), Ae = new Array(fe), qe = new Array(fe), Ne = new Array(fe);
              for (let Fe = 0; Fe < z.length; Fe++) {
                const De = z[Fe].tile, wt = Fe % fe, lt = wt + 1, ct = this.getDataToDraw(De);
                if (ct && ct.texture && this._getTileData(De, T, ct, ae, wt, Ie, Ae, qe, Ne), lt === fe || Fe === z.length - 1) {
                  for (let pt = 0; pt <= lt; pt++)
                    o.activeTexture(o.TEXTURE0 + pt), o.bindTexture(o.TEXTURE_2D, Ae[pt]);
                  o.bindBuffer(o.ARRAY_BUFFER, this._firstPass.bufferTexturePosition), o.bufferData(o.ARRAY_BUFFER, Ie, o.DYNAMIC_DRAW), qe.forEach((pt, ht) => {
                    o.uniformMatrix3fv(this._firstPass.uTransformMatrices[ht], !1, pt);
                  }), o.uniform1fv(this._firstPass.uOpacities, new Float32Array(Ne)), o.bindBuffer(o.ARRAY_BUFFER, this._firstPass.bufferOutputPosition), o.vertexAttribPointer(this._firstPass.aOutputPosition, 2, o.FLOAT, !1, 0, 0), o.bindBuffer(o.ARRAY_BUFFER, this._firstPass.bufferTexturePosition), o.vertexAttribPointer(this._firstPass.aTexturePosition, 2, o.FLOAT, !1, 0, 0), o.bindBuffer(o.ARRAY_BUFFER, this._firstPass.bufferIndex), o.vertexAttribPointer(this._firstPass.aIndex, 1, o.FLOAT, !1, 0, 0), o.drawArrays(o.TRIANGLES, 0, 6 * lt);
                }
              }
              oe && (o.useProgram(this._secondPass.shaderProgram), o.bindFramebuffer(o.FRAMEBUFFER, null), o.activeTexture(o.TEXTURE0), o.bindTexture(o.TEXTURE_2D, this._renderToTexture), this._gl.uniform1f(this._secondPass.uOpacityMultiplier, T.opacity), o.bindBuffer(o.ARRAY_BUFFER, this._secondPass.bufferTexturePosition), o.vertexAttribPointer(this._secondPass.aTexturePosition, 2, o.FLOAT, !1, 0, 0), o.bindBuffer(o.ARRAY_BUFFER, this._secondPass.bufferOutputPosition), o.vertexAttribPointer(this._secondPass.aOutputPosition, 2, o.FLOAT, !1, 0, 0), o.drawArrays(o.TRIANGLES, 0, 6)), x = !0, j && (this._applyContext2dPipeline(T, z, I), x = !1, o.bindFramebuffer(o.FRAMEBUFFER, null), o.clear(o.COLOR_BUFFER_BIT)), I === 0 && this._raiseTiledImageDrawnEvent(T, z.map((Fe) => Fe.tile));
            }
          }), x && this._outputContext.drawImage(this._renderingCanvas, 0, 0);
        }
        // Public API required by all Drawer implementations
        /**
        * Sets whether image smoothing is enabled or disabled
        * @param {Boolean} enabled If true, uses gl.LINEAR as the TEXTURE_MIN_FILTER and TEXTURE_MAX_FILTER, otherwise gl.NEAREST.
        */
        setImageSmoothingEnabled(r) {
          this._imageSmoothingEnabled !== r && (this._imageSmoothingEnabled = r, this.setInternalCacheNeedsRefresh(), this.viewer.forceRedraw());
        }
        /**
        * Sets whether textures are unpacked with premultiplied alpha
        * @param {Boolean} enabled If true, sets gl.UNPACK_PREMULTIPLY_ALPHA_WEBGL to true.
        */
        setUnpackWithPremultipliedAlpha(r) {
          this._unpackWithPremultipliedAlpha !== r && (this._unpackWithPremultipliedAlpha = r, this._gl && this._gl.pixelStorei(this._gl.UNPACK_PREMULTIPLY_ALPHA_WEBGL, r), this.setInternalCacheNeedsRefresh(), this.viewer.forceRedraw());
        }
        /**
        * Draw a rect onto the output canvas for debugging purposes
        * @param {OpenSeadragon.Rect} rect
        */
        drawDebuggingRect(r) {
          const o = this._outputContext;
          o.save(), o.lineWidth = 2 * e.pixelDensityRatio, o.strokeStyle = this.debugGridColor[0], o.fillStyle = this.debugGridColor[0], o.strokeRect(
            r.x * e.pixelDensityRatio,
            r.y * e.pixelDensityRatio,
            r.width * e.pixelDensityRatio,
            r.height * e.pixelDensityRatio
          ), o.restore();
        }
        /**
        * Draw data from the rendering canvas onto the output canvas, with clipping,
        * cropping and/or debug info as requested.
        * @private
        * @param {OpenSeadragon.TiledImage} tiledImage - the tiledImage to draw
        * @param {Array} tilesToDraw - array of objects containing tiles that were drawn
        */
        _applyContext2dPipeline(r, o, l) {
          if (this._outputContext.save(), this._outputContext.globalCompositeOperation = l === 0 ? null : r.compositeOperation || this.viewer.compositeOperation, r._croppingPolygons || r._clip ? (this._renderToClippingCanvas(r), this._outputContext.drawImage(this._clippingCanvas, 0, 0)) : this._outputContext.drawImage(this._renderingCanvas, 0, 0), this._outputContext.restore(), r.debugMode) {
            const a = this.viewer.viewport.getFlip();
            a && this._flip(), this._drawDebugInfo(o, r, a), a && this._flip();
          }
        }
        // private
        _getTileData(r, o, l, a, c, d, f, p, b) {
          const x = l.texture, T = l.position, I = l.overlapFraction;
          d.set(T, c * 12);
          const z = r.positionedBounds.width * I.x, W = r.positionedBounds.height * I.y, j = r.positionedBounds.x + (r.x === 0 ? 0 : z), oe = r.positionedBounds.y + (r.y === 0 ? 0 : W), ae = r.positionedBounds.x + r.positionedBounds.width - (r.isRightMost ? 0 : z), se = r.positionedBounds.y + r.positionedBounds.height - (r.isBottomMost ? 0 : W), fe = new e.Mat3([
            ae - j,
            0,
            0,
            // right - x = width
            0,
            se - oe,
            0,
            // bottom - y = height
            j,
            oe,
            1
          ]);
          r.flipped && fe.scaleAndTranslateSelf(-1, 1, 1, 0), fe.scaleAndTranslateOtherSetSelf(a), b[c] = r.opacity, f[c] = x, p[c] = fe.values;
        }
        // private
        _textureFilter() {
          return this._imageSmoothingEnabled ? this._gl.LINEAR : this._gl.NEAREST;
        }
        // private
        _setupRenderer() {
          const r = this._gl;
          r || e.console.error("_setupCanvases must be called before _setupRenderer"), this._unitQuad = this._makeQuadVertexBuffer(0, 1, 0, 1), this._makeFirstPassShaderProgram(), this._makeSecondPassShaderProgram(), this._renderToTexture = r.createTexture(), r.activeTexture(r.TEXTURE0), r.bindTexture(r.TEXTURE_2D, this._renderToTexture), r.texImage2D(r.TEXTURE_2D, 0, r.RGBA, this._renderingCanvas.width, this._renderingCanvas.height, 0, r.RGBA, r.UNSIGNED_BYTE, null), r.texParameteri(r.TEXTURE_2D, r.TEXTURE_MIN_FILTER, this._textureFilter()), r.texParameteri(r.TEXTURE_2D, r.TEXTURE_WRAP_S, r.CLAMP_TO_EDGE), r.texParameteri(r.TEXTURE_2D, r.TEXTURE_WRAP_T, r.CLAMP_TO_EDGE), this._glFrameBuffer = r.createFramebuffer(), r.bindFramebuffer(r.FRAMEBUFFER, this._glFrameBuffer), r.framebufferTexture2D(
            r.FRAMEBUFFER,
            r.COLOR_ATTACHMENT0,
            // attach texture as COLOR_ATTACHMENT0
            r.TEXTURE_2D,
            // attach a 2D texture
            this._renderToTexture,
            // the texture to attach
            0
          ), r.enable(r.BLEND), r.blendFunc(r.ONE, r.ONE_MINUS_SRC_ALPHA);
        }
        //private
        _makeFirstPassShaderProgram() {
          const r = this._glNumTextures = this._gl.getParameter(this._gl.MAX_TEXTURE_IMAGE_UNITS), o = () => [...Array(r).keys()].map((x) => `uniform mat3 u_matrix_${x};`).join(`
`), l = () => [...Array(r).keys()].map((x) => `${x > 0 ? "else " : ""}if(int(a_index) == ${x}) { transform_matrix = u_matrix_${x}; }`).join(`
`), a = `
            attribute vec2 a_output_position;
            attribute vec2 a_texture_position;
            attribute float a_index;

            ${o()} // create a uniform mat3 for each potential tile to draw

            varying vec2 v_texture_position;
            varying float v_image_index;

            void main() {

                mat3 transform_matrix; // value will be set by the if/elses in makeConditional()

                ${l()}

                gl_Position = vec4(transform_matrix * vec3(a_output_position, 1), 1);

                v_texture_position = a_texture_position;
                v_image_index = a_index;
            }
            `, c = `
            precision mediump float;

            // our textures
            uniform sampler2D u_images[${r}];
            // our opacities
            uniform float u_opacities[${r}];

            // the varyings passed in from the vertex shader.
            varying vec2 v_texture_position;
            varying float v_image_index;

            void main() {
                // can't index directly with a variable, need to use a loop iterator hack
                for(int i = 0; i < ${r}; ++i){
                    if(i == int(v_image_index)){
                        gl_FragColor = texture2D(u_images[i], v_texture_position) * u_opacities[i];
                    }
                }
            }
            `, d = this._gl, f = this.constructor.initShaderProgram(d, a, c);
          d.useProgram(f), this._firstPass = {
            shaderProgram: f,
            aOutputPosition: d.getAttribLocation(f, "a_output_position"),
            aTexturePosition: d.getAttribLocation(f, "a_texture_position"),
            aIndex: d.getAttribLocation(f, "a_index"),
            uTransformMatrices: [...Array(this._glNumTextures).keys()].map((x) => d.getUniformLocation(f, `u_matrix_${x}`)),
            uImages: d.getUniformLocation(f, "u_images"),
            uOpacities: d.getUniformLocation(f, "u_opacities"),
            bufferOutputPosition: d.createBuffer(),
            bufferTexturePosition: d.createBuffer(),
            bufferIndex: d.createBuffer()
          }, d.uniform1iv(this._firstPass.uImages, [...Array(r).keys()]);
          const p = new Float32Array(r * 12);
          for (let x = 0; x < r; ++x)
            p.set(Float32Array.from(this._unitQuad), x * 12);
          d.bindBuffer(d.ARRAY_BUFFER, this._firstPass.bufferOutputPosition), d.bufferData(d.ARRAY_BUFFER, p, d.STATIC_DRAW), d.enableVertexAttribArray(this._firstPass.aOutputPosition), d.bindBuffer(d.ARRAY_BUFFER, this._firstPass.bufferTexturePosition), d.enableVertexAttribArray(this._firstPass.aTexturePosition), d.bindBuffer(d.ARRAY_BUFFER, this._firstPass.bufferIndex);
          const b = [...Array(this._glNumTextures).keys()].map((x) => Array(6).fill(x)).flat();
          d.bufferData(d.ARRAY_BUFFER, new Float32Array(b), d.STATIC_DRAW), d.enableVertexAttribArray(this._firstPass.aIndex);
        }
        // private
        _makeSecondPassShaderProgram() {
          const r = `
            attribute vec2 a_output_position;
            attribute vec2 a_texture_position;

            varying vec2 v_texture_position;

            void main() {
                // Transform to clip space (0:1 --> -1:1)
                gl_Position = vec4(vec3(a_output_position * 2.0 - 1.0, 1), 1);

                v_texture_position = a_texture_position;
            }
            `, o = `
            precision mediump float;

            // our texture
            uniform sampler2D u_image;

            // the texCoords passed in from the vertex shader.
            varying vec2 v_texture_position;

            // the opacity multiplier for the image
            uniform float u_opacity_multiplier;

            void main() {
                gl_FragColor = texture2D(u_image, v_texture_position);
                gl_FragColor *= u_opacity_multiplier;
            }
            `, l = this._gl, a = this.constructor.initShaderProgram(l, r, o);
          l.useProgram(a), this._secondPass = {
            shaderProgram: a,
            aOutputPosition: l.getAttribLocation(a, "a_output_position"),
            aTexturePosition: l.getAttribLocation(a, "a_texture_position"),
            uImage: l.getUniformLocation(a, "u_image"),
            uOpacityMultiplier: l.getUniformLocation(a, "u_opacity_multiplier"),
            bufferOutputPosition: l.createBuffer(),
            bufferTexturePosition: l.createBuffer()
          }, l.bindBuffer(l.ARRAY_BUFFER, this._secondPass.bufferOutputPosition), l.bufferData(l.ARRAY_BUFFER, this._unitQuad, l.STATIC_DRAW), l.enableVertexAttribArray(this._secondPass.aOutputPosition), l.bindBuffer(l.ARRAY_BUFFER, this._secondPass.bufferTexturePosition), l.bufferData(l.ARRAY_BUFFER, this._unitQuad, l.DYNAMIC_DRAW), l.enableVertexAttribArray(this._secondPass.aTexturePosition);
        }
        // private
        _resizeRenderer() {
          const r = this._gl, o = this._renderingCanvas.width, l = this._renderingCanvas.height;
          r.viewport(0, 0, o, l), r.deleteTexture(this._renderToTexture), this._renderToTexture = r.createTexture(), r.activeTexture(r.TEXTURE0), r.bindTexture(r.TEXTURE_2D, this._renderToTexture), r.texImage2D(r.TEXTURE_2D, 0, r.RGBA, o, l, 0, r.RGBA, r.UNSIGNED_BYTE, null), r.texParameteri(r.TEXTURE_2D, r.TEXTURE_MIN_FILTER, this._textureFilter()), r.texParameteri(r.TEXTURE_2D, r.TEXTURE_WRAP_S, r.CLAMP_TO_EDGE), r.texParameteri(r.TEXTURE_2D, r.TEXTURE_WRAP_T, r.CLAMP_TO_EDGE), r.bindFramebuffer(r.FRAMEBUFFER, this._glFrameBuffer), r.framebufferTexture2D(r.FRAMEBUFFER, r.COLOR_ATTACHMENT0, r.TEXTURE_2D, this._renderToTexture, 0);
        }
        // private
        _setupCanvases() {
          const r = this;
          this._outputCanvas = this.canvas, this._outputContext = this._outputCanvas.getContext("2d"), this._renderingCanvas = document.createElement("canvas"), this._clippingCanvas = document.createElement("canvas"), this._clippingContext = this._clippingCanvas.getContext("2d"), this._renderingCanvas.width = this._clippingCanvas.width = this._outputCanvas.width, this._renderingCanvas.height = this._clippingCanvas.height = this._outputCanvas.height, this._gl = this._renderingCanvas.getContext("webgl"), this._gl.pixelStorei(this._gl.UNPACK_PREMULTIPLY_ALPHA_WEBGL, this._unpackWithPremultipliedAlpha), this._resizeHandler = function() {
            r._outputCanvas !== r.viewer.drawer.canvas && (r._outputCanvas.style.width = r.viewer.drawer.canvas.clientWidth + "px", r._outputCanvas.style.height = r.viewer.drawer.canvas.clientHeight + "px");
            const o = r._calculateCanvasSize();
            (r._outputCanvas.width !== o.x || r._outputCanvas.height !== o.y) && (r._outputCanvas.width = o.x, r._outputCanvas.height = o.y), r._renderingCanvas.style.width = r._outputCanvas.clientWidth + "px", r._renderingCanvas.style.height = r._outputCanvas.clientHeight + "px", r._renderingCanvas.width = r._clippingCanvas.width = r._outputCanvas.width, r._renderingCanvas.height = r._clippingCanvas.height = r._outputCanvas.height, r._resizeRenderer();
          }, this.viewer.addHandler("resize", this._resizeHandler);
        }
        internalCacheCreate(r, o) {
          const l = o.tiledImage, a = this._gl;
          let c, d, f = r.data, p = !1;
          if (f instanceof CanvasRenderingContext2D && (f = f.canvas, p = !0), !l.isTainted())
            if (p && e.isCanvasTainted(f))
              l.setTainted(!0), e.console.warn("WebGL cannot be used to draw this TiledImage because it has tainted data. Does crossOriginPolicy need to be set?"), this._raiseDrawerErrorEvent(l, "Tainted data cannot be used by the WebGLDrawer. Falling back to CanvasDrawer for this TiledImage."), this.setInternalCacheNeedsRefresh();
            else {
              let b, x;
              o.sourceBounds ? (b = Math.min(o.sourceBounds.width, f.width) / f.width, x = Math.min(o.sourceBounds.height, f.height) / f.height) : (b = 1, x = 1), c = a.createTexture();
              const T = l.source.tileOverlap, I = this._calculateOverlapFraction(o, l);
              if (T > 0) {
                const z = (o.x === 0 ? 0 : I.x) * b, W = (o.y === 0 ? 0 : I.y) * x, j = (o.isRightMost ? 1 : 1 - I.x) * b, oe = (o.isBottomMost ? 1 : 1 - I.y) * x;
                d = this._makeQuadVertexBuffer(z, j, W, oe);
              } else b === 1 && x === 1 ? d = this._unitQuad : d = this._makeQuadVertexBuffer(0, b, 0, x);
              a.activeTexture(a.TEXTURE0), a.bindTexture(a.TEXTURE_2D, c), a.texParameteri(a.TEXTURE_2D, a.TEXTURE_WRAP_S, a.CLAMP_TO_EDGE), a.texParameteri(a.TEXTURE_2D, a.TEXTURE_WRAP_T, a.CLAMP_TO_EDGE), a.texParameteri(a.TEXTURE_2D, a.TEXTURE_MIN_FILTER, this._textureFilter()), a.texParameteri(a.TEXTURE_2D, a.TEXTURE_MAG_FILTER, this._textureFilter());
              try {
                return a.pixelStorei(a.UNPACK_PREMULTIPLY_ALPHA_WEBGL, this._unpackWithPremultipliedAlpha), a.texImage2D(a.TEXTURE_2D, 0, a.RGBA, a.RGBA, a.UNSIGNED_BYTE, f), {
                  texture: c,
                  position: d,
                  overlapFraction: I
                };
              } catch (z) {
                l.setTainted(!0), e.console.error("Error uploading image data to WebGL. Falling back to canvas renderer.", z), this._raiseDrawerErrorEvent(l, "Unknown error when uploading texture. Falling back to CanvasDrawer for this TiledImage."), this.setInternalCacheNeedsRefresh();
              }
            }
          if (f instanceof Image) {
            const b = document.createElement("canvas");
            b.width = f.width, b.height = f.height;
            const x = b.getContext("2d", { willReadFrequently: !0 });
            x.drawImage(f, 0, 0), f = x;
          }
          return f instanceof CanvasRenderingContext2D ? f : (e.console.error("Unsupported data used for WebGL Drawer - probably a bug!"), {});
        }
        internalCacheFree(r) {
          r && r.texture && (this._gl.deleteTexture(r.texture), r.texture = null);
        }
        // private
        _makeQuadVertexBuffer(r, o, l, a) {
          return new Float32Array([
            r,
            a,
            o,
            a,
            r,
            l,
            r,
            l,
            o,
            a,
            o,
            l
          ]);
        }
        // private
        _calculateOverlapFraction(r, o) {
          const l = o.source.tileOverlap, a = r.sourceBounds.width, c = r.sourceBounds.height, d = (r.x === 0 ? 0 : l) + (r.isRightMost ? 0 : l), f = (r.y === 0 ? 0 : l) + (r.isBottomMost ? 0 : l), p = l / (a + d), b = l / (c + f);
          return {
            x: p,
            y: b
          };
        }
        _setClip() {
        }
        // private
        _renderToClippingCanvas(r) {
          if (this._clippingContext.clearRect(0, 0, this._clippingCanvas.width, this._clippingCanvas.height), this._clippingContext.save(), this.viewer.viewport.getFlip()) {
            const o = new e.Point(this.canvas.width / 2, this.canvas.height / 2);
            this._clippingContext.translate(o.x, 0), this._clippingContext.scale(-1, 1), this._clippingContext.translate(-o.x, 0);
          }
          if (r._clip) {
            const l = [
              { x: r._clip.x, y: r._clip.y },
              { x: r._clip.x + r._clip.width, y: r._clip.y },
              { x: r._clip.x + r._clip.width, y: r._clip.y + r._clip.height },
              { x: r._clip.x, y: r._clip.y + r._clip.height }
            ].map((a) => {
              const c = r.imageToViewportCoordinates(a.x, a.y, !0).rotate(this.viewer.viewport.getRotation(!0), this.viewer.viewport.getCenter(!0));
              return this.viewportCoordToDrawerCoord(c);
            });
            this._clippingContext.beginPath(), l.forEach((a, c) => {
              this._clippingContext[c === 0 ? "moveTo" : "lineTo"](a.x, a.y);
            }), this._clippingContext.clip(), this._setClip();
          }
          if (r._croppingPolygons) {
            const o = r._croppingPolygons.map((l) => l.map((a) => {
              const c = r.imageToViewportCoordinates(a.x, a.y, !0).rotate(this.viewer.viewport.getRotation(!0), this.viewer.viewport.getCenter(!0));
              return this.viewportCoordToDrawerCoord(c);
            }));
            this._clippingContext.beginPath(), o.forEach((l) => {
              l.forEach((a, c) => {
                this._clippingContext[c === 0 ? "moveTo" : "lineTo"](a.x, a.y);
              });
            }), this._clippingContext.clip();
          }
          if (this.viewer.viewport.getFlip()) {
            const o = new e.Point(this.canvas.width / 2, this.canvas.height / 2);
            this._clippingContext.translate(o.x, 0), this._clippingContext.scale(-1, 1), this._clippingContext.translate(-o.x, 0);
          }
          this._clippingContext.drawImage(this._renderingCanvas, 0, 0), this._clippingContext.restore();
        }
        /**
         * Set rotations for viewport & tiledImage
         * @private
         * @param {OpenSeadragon.TiledImage} tiledImage
         */
        _setRotations(r) {
          let o = !1;
          this.viewport.getRotation(!0) % 360 !== 0 && (this._offsetForRotation({
            degrees: this.viewport.getRotation(!0),
            saveContext: o
          }), o = !1), r.getRotation(!0) % 360 !== 0 && this._offsetForRotation({
            degrees: r.getRotation(!0),
            point: this.viewport.pixelFromPointNoRotate(
              r._getRotationPoint(!0),
              !0
            ),
            saveContext: o
          });
        }
        // private
        _offsetForRotation(r) {
          const o = r.point ? r.point.times(e.pixelDensityRatio) : this._getCanvasCenter(), l = this._outputContext;
          l.save(), l.translate(o.x, o.y), l.rotate(Math.PI / 180 * r.degrees), l.translate(-o.x, -o.y);
        }
        // private
        _flip(r) {
          r = r || {};
          const o = r.point ? r.point.times(e.pixelDensityRatio) : this._getCanvasCenter(), l = this._outputContext;
          l.translate(o.x, 0), l.scale(-1, 1), l.translate(-o.x, 0);
        }
        // private
        _drawDebugInfo(r, o, l) {
          for (let a = r.length - 1; a >= 0; a--) {
            const c = r[a].tile;
            try {
              this._drawDebugInfoOnTile(c, r.length, a, o, l);
            } catch (d) {
              e.console.error(d);
            }
          }
        }
        // private
        _drawDebugInfoOnTile(r, o, l, a, c) {
          const d = this.viewer.world.getIndexOfItem(a) % this.debugGridColor.length, f = this.context;
          f.save(), f.lineWidth = 2 * e.pixelDensityRatio, f.font = "small-caps bold " + 13 * e.pixelDensityRatio + "px arial", f.strokeStyle = this.debugGridColor[d], f.fillStyle = this.debugGridColor[d], this._setRotations(a), c && this._flip({ point: r.position.plus(r.size.divide(2)) }), f.strokeRect(
            r.position.x * e.pixelDensityRatio,
            r.position.y * e.pixelDensityRatio,
            r.size.x * e.pixelDensityRatio,
            r.size.y * e.pixelDensityRatio
          );
          const p = (r.position.x + r.size.x / 2) * e.pixelDensityRatio, b = (r.position.y + r.size.y / 2) * e.pixelDensityRatio;
          f.translate(p, b);
          const x = this.viewport.getRotation(!0);
          f.rotate(Math.PI / 180 * -x), f.translate(-p, -b), r.x === 0 && r.y === 0 && (f.fillText(
            "Zoom: " + this.viewport.getZoom(),
            r.position.x * e.pixelDensityRatio,
            (r.position.y - 30) * e.pixelDensityRatio
          ), f.fillText(
            "Pan: " + this.viewport.getBounds().toString(),
            r.position.x * e.pixelDensityRatio,
            (r.position.y - 20) * e.pixelDensityRatio
          )), f.fillText(
            "Level: " + r.level,
            (r.position.x + 10) * e.pixelDensityRatio,
            (r.position.y + 20) * e.pixelDensityRatio
          ), f.fillText(
            "Column: " + r.x,
            (r.position.x + 10) * e.pixelDensityRatio,
            (r.position.y + 30) * e.pixelDensityRatio
          ), f.fillText(
            "Row: " + r.y,
            (r.position.x + 10) * e.pixelDensityRatio,
            (r.position.y + 40) * e.pixelDensityRatio
          ), f.fillText(
            "Order: " + l + " of " + o,
            (r.position.x + 10) * e.pixelDensityRatio,
            (r.position.y + 50) * e.pixelDensityRatio
          ), f.fillText(
            "Size: " + r.size.toString(),
            (r.position.x + 10) * e.pixelDensityRatio,
            (r.position.y + 60) * e.pixelDensityRatio
          ), f.fillText(
            "Position: " + r.position.toString(),
            (r.position.x + 10) * e.pixelDensityRatio,
            (r.position.y + 70) * e.pixelDensityRatio
          ), this.viewport.getRotation(!0) % 360 !== 0 && this._restoreRotationChanges(), a.getRotation(!0) % 360 !== 0 && this._restoreRotationChanges(), f.restore();
        }
        _drawPlaceholder(r) {
          const o = r.getBounds(!0), l = this.viewportToDrawerRectangle(r.getBounds(!0)), a = this._outputContext;
          let c;
          typeof r.placeholderFillStyle == "function" ? c = r.placeholderFillStyle(r, a) : c = r.placeholderFillStyle, this._offsetForRotation({ degrees: this.viewer.viewport.getRotation(!0) }), a.fillStyle = c, a.translate(l.x, l.y), a.rotate(Math.PI / 180 * o.degrees), a.translate(-l.x, -l.y), a.fillRect(l.x, l.y, l.width, l.height), this._restoreRotationChanges();
        }
        /**
         * Get the canvas center
         * @private
         * @returns {OpenSeadragon.Point} The center point of the canvas
         */
        _getCanvasCenter() {
          return new e.Point(this.canvas.width / 2, this.canvas.height / 2);
        }
        // private
        _restoreRotationChanges() {
          this._outputContext.restore();
        }
        // modified from https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/Tutorial/Adding_2D_content_to_a_WebGL_context
        static initShaderProgram(r, o, l) {
          function a(p, b, x) {
            const T = p.createShader(b);
            return p.shaderSource(T, x), p.compileShader(T), p.getShaderParameter(T, p.COMPILE_STATUS) ? T : (e.console.error(
              `An error occurred compiling the shaders: ${p.getShaderInfoLog(T)}`
            ), p.deleteShader(T), null);
          }
          const c = a(r, r.VERTEX_SHADER, o), d = a(r, r.FRAGMENT_SHADER, l), f = r.createProgram();
          return r.attachShader(f, c), r.attachShader(f, d), r.linkProgram(f), r.getProgramParameter(f, r.LINK_STATUS) ? f : (e.console.error(
            `Unable to initialize the shader program: ${r.getProgramInfoLog(
              f
            )}`
          ), null);
        }
      };
    })(n), (function(e) {
      e.Viewport = function(t) {
        const i = arguments;
        i.length && i[0] instanceof e.Point && (t = {
          containerSize: i[0],
          contentSize: i[1],
          config: i[2]
        }), t.config && (e.extend(!0, t, t.config), delete t.config), this._margins = e.extend({
          left: 0,
          top: 0,
          right: 0,
          bottom: 0
        }, t.margins || {}), delete t.margins, t.initialDegrees = t.degrees, delete t.degrees, e.extend(!0, this, {
          //required settings
          containerSize: null,
          contentSize: null,
          //internal state properties
          zoomPoint: null,
          rotationPivot: null,
          viewer: null,
          //configurable options
          springStiffness: e.DEFAULT_SETTINGS.springStiffness,
          animationTime: e.DEFAULT_SETTINGS.animationTime,
          minZoomImageRatio: e.DEFAULT_SETTINGS.minZoomImageRatio,
          maxZoomPixelRatio: e.DEFAULT_SETTINGS.maxZoomPixelRatio,
          visibilityRatio: e.DEFAULT_SETTINGS.visibilityRatio,
          wrapHorizontal: e.DEFAULT_SETTINGS.wrapHorizontal,
          wrapVertical: e.DEFAULT_SETTINGS.wrapVertical,
          defaultZoomLevel: e.DEFAULT_SETTINGS.defaultZoomLevel,
          minZoomLevel: e.DEFAULT_SETTINGS.minZoomLevel,
          maxZoomLevel: e.DEFAULT_SETTINGS.maxZoomLevel,
          initialDegrees: e.DEFAULT_SETTINGS.degrees,
          flipped: e.DEFAULT_SETTINGS.flipped,
          homeFillsViewer: e.DEFAULT_SETTINGS.homeFillsViewer,
          silenceMultiImageWarnings: e.DEFAULT_SETTINGS.silenceMultiImageWarnings
        }, t), this._updateContainerInnerSize(), this.centerSpringX = new e.Spring({
          initial: 0,
          springStiffness: this.springStiffness,
          animationTime: this.animationTime
        }), this.centerSpringY = new e.Spring({
          initial: 0,
          springStiffness: this.springStiffness,
          animationTime: this.animationTime
        }), this.zoomSpring = new e.Spring({
          exponential: !0,
          initial: 1,
          springStiffness: this.springStiffness,
          animationTime: this.animationTime
        }), this.degreesSpring = new e.Spring({
          initial: t.initialDegrees,
          springStiffness: this.springStiffness,
          animationTime: this.animationTime
        }), this._oldCenterX = this.centerSpringX.current.value, this._oldCenterY = this.centerSpringY.current.value, this._oldZoom = this.zoomSpring.current.value, this._oldDegrees = this.degreesSpring.current.value, this._setContentBounds(new e.Rect(0, 0, 1, 1), 1), this.goHome(!0), this.update();
      }, e.Viewport.prototype = {
        // deprecated
        get degrees() {
          return e.console.warn("Accessing [Viewport.degrees] is deprecated. Use viewport.getRotation instead."), this.getRotation();
        },
        // deprecated
        set degrees(t) {
          e.console.warn("Setting [Viewport.degrees] is deprecated. Use viewport.rotateTo, viewport.rotateBy, or viewport.setRotation instead."), this.rotateTo(t);
        },
        /**
         * Updates the viewport's home bounds and constraints for the given content size.
         * @function
         * @param {OpenSeadragon.Point} contentSize - size of the content in content units
         * @returns {OpenSeadragon.Viewport} Chainable.
         * @fires OpenSeadragon.Viewer.event:reset-size
         */
        resetContentSize: function(t) {
          return e.console.assert(t, "[Viewport.resetContentSize] contentSize is required"), e.console.assert(t instanceof e.Point, "[Viewport.resetContentSize] contentSize must be an OpenSeadragon.Point"), e.console.assert(t.x > 0, "[Viewport.resetContentSize] contentSize.x must be greater than 0"), e.console.assert(t.y > 0, "[Viewport.resetContentSize] contentSize.y must be greater than 0"), this._setContentBounds(new e.Rect(0, 0, 1, t.y / t.x), t.x), this;
        },
        // deprecated
        setHomeBounds: function(t, i) {
          e.console.error("[Viewport.setHomeBounds] this function is deprecated; The content bounds should not be set manually."), this._setContentBounds(t, i);
        },
        // Set the viewport's content bounds
        // @param {OpenSeadragon.Rect} bounds - the new bounds in viewport coordinates
        // without rotation
        // @param {Number} contentFactor - how many content units per viewport unit
        // @fires OpenSeadragon.Viewer.event:reset-size
        // @private
        _setContentBounds: function(t, i) {
          e.console.assert(t, "[Viewport._setContentBounds] bounds is required"), e.console.assert(t instanceof e.Rect, "[Viewport._setContentBounds] bounds must be an OpenSeadragon.Rect"), e.console.assert(t.width > 0, "[Viewport._setContentBounds] bounds.width must be greater than 0"), e.console.assert(t.height > 0, "[Viewport._setContentBounds] bounds.height must be greater than 0"), this._contentBoundsNoRotate = t.clone(), this._contentSizeNoRotate = this._contentBoundsNoRotate.getSize().times(
            i
          ), this._contentBounds = t.rotate(this.getRotation()).getBoundingBox(), this._contentSize = this._contentBounds.getSize().times(i), this._contentAspectRatio = this._contentSize.x / this._contentSize.y, this.viewer && this.viewer.raiseEvent("reset-size", {
            contentSize: this._contentSizeNoRotate.clone(),
            contentFactor: i,
            homeBounds: this._contentBoundsNoRotate.clone(),
            contentBounds: this._contentBounds.clone()
          });
        },
        /**
         * Returns the home zoom in "viewport zoom" value.
         * @function
         * @returns {Number} The home zoom in "viewport zoom".
         */
        getHomeZoom: function() {
          if (this.defaultZoomLevel)
            return this.defaultZoomLevel;
          const t = this._contentAspectRatio / this.getAspectRatio();
          let i;
          return this.homeFillsViewer ? i = t >= 1 ? t : 1 : i = t >= 1 ? 1 : t, i / this._contentBounds.width;
        },
        /**
         * Returns the home bounds in viewport coordinates.
         * @function
         * @returns {OpenSeadragon.Rect} The home bounds in vewport coordinates.
         */
        getHomeBounds: function() {
          return this.getHomeBoundsNoRotate().rotate(-this.getRotation());
        },
        /**
         * Returns the home bounds in viewport coordinates.
         * This method ignores the viewport rotation. Use
         * {@link OpenSeadragon.Viewport#getHomeBounds} to take it into account.
         * @function
         * @returns {OpenSeadragon.Rect} The home bounds in vewport coordinates.
         */
        getHomeBoundsNoRotate: function() {
          const t = this._contentBounds.getCenter(), i = 1 / this.getHomeZoom(), r = i / this.getAspectRatio();
          return new e.Rect(
            t.x - i / 2,
            t.y - r / 2,
            i,
            r
          );
        },
        /**
         * @function
         * @param {Boolean} immediately
         * @fires OpenSeadragon.Viewer.event:home
         */
        goHome: function(t) {
          return this.viewer && this.viewer.raiseEvent("home", {
            immediately: t
          }), this.fitBounds(this.getHomeBounds(), t);
        },
        /**
         * @function
         */
        getMinZoom: function() {
          const t = this.getHomeZoom();
          return this.minZoomLevel ? this.minZoomLevel : this.minZoomImageRatio * t;
        },
        /**
         * @function
         */
        getMaxZoom: function() {
          let t = this.maxZoomLevel;
          return t || (t = this._contentSize.x * this.maxZoomPixelRatio / this._containerInnerSize.x, t /= this._contentBounds.width), Math.max(t, this.getHomeZoom());
        },
        /**
         * @function
         */
        getAspectRatio: function() {
          return this._containerInnerSize.x / this._containerInnerSize.y;
        },
        /**
         * @function
         * @returns {OpenSeadragon.Point} The size of the container, in screen coordinates.
         */
        getContainerSize: function() {
          return new e.Point(
            this.containerSize.x,
            this.containerSize.y
          );
        },
        /**
         * The margins push the "home" region in from the sides by the specified amounts.
         * @function
         * @returns {Object} Properties (Numbers, in screen coordinates): left, top, right, bottom.
         */
        getMargins: function() {
          return e.extend({}, this._margins);
        },
        /**
         * The margins push the "home" region in from the sides by the specified amounts.
         * @function
         * @param {Object} margins - Properties (Numbers, in screen coordinates): left, top, right, bottom.
         */
        setMargins: function(t) {
          e.console.assert(e.type(t) === "object", "[Viewport.setMargins] margins must be an object"), this._margins = e.extend({
            left: 0,
            top: 0,
            right: 0,
            bottom: 0
          }, t), this._updateContainerInnerSize(), this.viewer && this.viewer.forceRedraw();
        },
        /**
         * Returns the bounds of the visible area in viewport coordinates.
         * @function
         * @param {Boolean} current - Pass true for the current location; defaults to false (target location).
         * @returns {OpenSeadragon.Rect} The location you are zoomed/panned to, in viewport coordinates.
         */
        getBounds: function(t) {
          return this.getBoundsNoRotate(t).rotate(-this.getRotation(t));
        },
        /**
         * Returns the bounds of the visible area in viewport coordinates.
         * This method ignores the viewport rotation. Use
         * {@link OpenSeadragon.Viewport#getBounds} to take it into account.
         * @function
         * @param {Boolean} current - Pass true for the current location; defaults to false (target location).
         * @returns {OpenSeadragon.Rect} The location you are zoomed/panned to, in viewport coordinates.
         */
        getBoundsNoRotate: function(t) {
          const i = this.getCenter(t), r = 1 / this.getZoom(t), o = r / this.getAspectRatio();
          return new e.Rect(
            i.x - r / 2,
            i.y - o / 2,
            r,
            o
          );
        },
        /**
         * @function
         * @param {Boolean} current - Pass true for the current location; defaults to false (target location).
         * @returns {OpenSeadragon.Rect} The location you are zoomed/panned to,
         * including the space taken by margins, in viewport coordinates.
         */
        getBoundsWithMargins: function(t) {
          return this.getBoundsNoRotateWithMargins(t).rotate(
            -this.getRotation(t),
            this.getCenter(t)
          );
        },
        /**
         * @function
         * @param {Boolean} current - Pass true for the current location; defaults to false (target location).
         * @returns {OpenSeadragon.Rect} The location you are zoomed/panned to,
         * including the space taken by margins, in viewport coordinates.
         */
        getBoundsNoRotateWithMargins: function(t) {
          const i = this.getBoundsNoRotate(t), r = this._containerInnerSize.x * this.getZoom(t);
          return i.x -= this._margins.left / r, i.y -= this._margins.top / r, i.width += (this._margins.left + this._margins.right) / r, i.height += (this._margins.top + this._margins.bottom) / r, i;
        },
        /**
         * @function
         * @param {Boolean} current - Pass true for the current location; defaults to false (target location).
         */
        getCenter: function(t) {
          const i = new e.Point(
            this.centerSpringX.current.value,
            this.centerSpringY.current.value
          ), r = new e.Point(
            this.centerSpringX.target.value,
            this.centerSpringY.target.value
          );
          if (t)
            return i;
          if (!this.zoomPoint)
            return r;
          const o = this.pixelFromPoint(this.zoomPoint, !0), l = this.getZoom(), a = 1 / l, c = a / this.getAspectRatio(), d = new e.Rect(
            i.x - a / 2,
            i.y - c / 2,
            a,
            c
          ), b = this._pixelFromPoint(this.zoomPoint, d).minus(o).rotate(-this.getRotation(!0)).divide(this._containerInnerSize.x * l);
          return r.plus(b);
        },
        /**
         * @function
         * @param {Boolean} current - Pass true for the current location; defaults to false (target location).
         */
        getZoom: function(t) {
          return t ? this.zoomSpring.current.value : this.zoomSpring.target.value;
        },
        // private
        _applyZoomConstraints: function(t) {
          return Math.max(
            Math.min(t, this.getMaxZoom()),
            this.getMinZoom()
          );
        },
        /**
         * @function
         * @private
         * @param {OpenSeadragon.Rect} bounds
         * @returns {OpenSeadragon.Rect} constrained bounds.
         */
        _applyBoundaryConstraints: function(t) {
          const i = this.viewportToViewerElementRectangle(t).getBoundingBox(), r = this.viewportToViewerElementRectangle(this._contentBoundsNoRotate).getBoundingBox();
          let o = !1, l = !1;
          if (!this.wrapHorizontal) {
            const d = i.x + i.width, f = r.x + r.width;
            let p, b, x;
            i.width > r.width ? p = this.visibilityRatio * r.width : p = this.visibilityRatio * i.width, b = r.x - d + p, x = f - i.x - p, p > r.width ? (i.x += (b + x) / 2, o = !0) : x < 0 ? (i.x += x, o = !0) : b > 0 && (i.x += b, o = !0);
          }
          if (!this.wrapVertical) {
            const d = i.y + i.height, f = r.y + r.height;
            let p, b, x;
            i.height > r.height ? p = this.visibilityRatio * r.height : p = this.visibilityRatio * i.height, b = r.y - d + p, x = f - i.y - p, p > r.height ? (i.y += (b + x) / 2, l = !0) : x < 0 ? (i.y += x, l = !0) : b > 0 && (i.y += b, l = !0);
          }
          const a = o || l, c = a ? this.viewerElementToViewportRectangle(i) : t.clone();
          return c.xConstrained = o, c.yConstrained = l, c.constraintApplied = a, c;
        },
        /**
         * @function
         * @private
         * @param {Boolean} [immediately=false] - whether the function that triggered this event was
         * called with the "immediately" flag
         */
        _raiseConstraintsEvent: function(t) {
          this.viewer && this.viewer.raiseEvent("constrain", {
            immediately: t
          });
        },
        /**
         * Enforces the minZoom, maxZoom and visibilityRatio constraints by
         * zooming and panning to the closest acceptable zoom and location.
         * @function
         * @param {Boolean} [immediately=false]
         * @returns {OpenSeadragon.Viewport} Chainable.
         * @fires OpenSeadragon.Viewer.event:constrain if constraints were applied
         */
        applyConstraints: function(t) {
          const i = this.getZoom(), r = this._applyZoomConstraints(i);
          i !== r && this.zoomTo(r, this.zoomPoint, t);
          const o = this.getConstrainedBounds(!1);
          return o.constraintApplied && (this.fitBounds(o, t), this._raiseConstraintsEvent(t)), this;
        },
        /**
         * Equivalent to {@link OpenSeadragon.Viewport#applyConstraints}
         * @function
         * @param {Boolean} [immediately=false]
         * @returns {OpenSeadragon.Viewport} Chainable.
         * @fires OpenSeadragon.Viewer.event:constrain
         */
        ensureVisible: function(t) {
          return this.applyConstraints(t);
        },
        /**
         * @function
         * @private
         * @param {OpenSeadragon.Rect} bounds
         * @param {Object} options (immediately=false, constraints=false)
         * @returns {OpenSeadragon.Viewport} Chainable.
         */
        _fitBounds: function(t, i) {
          i = i || {};
          const r = i.immediately || !1, o = i.constraints || !1, l = this.getAspectRatio(), a = t.getCenter(), c = new e.Rect(
            t.x,
            t.y,
            t.width,
            t.height,
            t.degrees + this.getRotation()
          ).getBoundingBox();
          c.getAspectRatio() >= l ? c.height = c.width / l : c.width = c.height * l, c.x = a.x - c.width / 2, c.y = a.y - c.height / 2;
          let d = 1 / c.width;
          if (r)
            return this.panTo(a, !0), this.zoomTo(d, null, !0), o && this.applyConstraints(!0), this;
          const f = this.getCenter(!0), p = this.getZoom(!0);
          this.panTo(f, !0), this.zoomTo(p, null, !0);
          const b = this.getBounds(), x = this.getZoom();
          if (x === 0 || Math.abs(d / x - 1) < 1e-8)
            return this.zoomTo(d, null, !0), this.panTo(a, r), o && this.applyConstraints(!1), this;
          if (o) {
            this.panTo(a, !1), d = this._applyZoomConstraints(d), this.zoomTo(d, null, !1);
            const T = this.getConstrainedBounds();
            this.panTo(f, !0), this.zoomTo(p, null, !0), this.fitBounds(T);
          } else {
            const I = c.rotate(-this.getRotation()).getTopLeft().times(d).minus(b.getTopLeft().times(x)).divide(d - x);
            this.zoomTo(d, I, r);
          }
          return this;
        },
        /**
         * Makes the viewport zoom and pan so that the specified bounds take
         * as much space as possible in the viewport.
         * Note: this method ignores the constraints (minZoom, maxZoom and
         * visibilityRatio).
         * Use {@link OpenSeadragon.Viewport#fitBoundsWithConstraints} to enforce
         * them.
         * @function
         * @param {OpenSeadragon.Rect} bounds
         * @param {Boolean} [immediately=false]
         * @returns {OpenSeadragon.Viewport} Chainable.
         */
        fitBounds: function(t, i) {
          return this._fitBounds(t, {
            immediately: i,
            constraints: !1
          });
        },
        /**
         * Makes the viewport zoom and pan so that the specified bounds take
         * as much space as possible in the viewport while enforcing the constraints
         * (minZoom, maxZoom and visibilityRatio).
         * Note: because this method enforces the constraints, part of the
         * provided bounds may end up outside of the viewport.
         * Use {@link OpenSeadragon.Viewport#fitBounds} to ignore them.
         * @function
         * @param {OpenSeadragon.Rect} bounds
         * @param {Boolean} [immediately=false]
         * @returns {OpenSeadragon.Viewport} Chainable.
         */
        fitBoundsWithConstraints: function(t, i) {
          return this._fitBounds(t, {
            immediately: i,
            constraints: !0
          });
        },
        /**
         * Zooms so the image just fills the viewer vertically.
         * @param {Boolean} immediately
         * @returns {OpenSeadragon.Viewport} Chainable.
         */
        fitVertically: function(t) {
          const i = new e.Rect(
            this._contentBounds.x + this._contentBounds.width / 2,
            this._contentBounds.y,
            0,
            this._contentBounds.height
          );
          return this.fitBounds(i, t);
        },
        /**
         * Zooms so the image just fills the viewer horizontally.
         * @param {Boolean} immediately
         * @returns {OpenSeadragon.Viewport} Chainable.
         */
        fitHorizontally: function(t) {
          const i = new e.Rect(
            this._contentBounds.x,
            this._contentBounds.y + this._contentBounds.height / 2,
            this._contentBounds.width,
            0
          );
          return this.fitBounds(i, t);
        },
        /**
         * Returns bounds taking constraints into account
         * Added to improve constrained panning
         * @param {Boolean} current - Pass true for the current location; defaults to false (target location).
         * @returns {OpenSeadragon.Rect} The bounds in viewport coordinates after applying constraints. The returned $.Rect
         *                               contains additional properties constraintsApplied, xConstrained and yConstrained.
         *                               These flags indicate whether the viewport bounds were modified by the constraints
         *                               of the viewer rectangle, and in which dimension(s).
         */
        getConstrainedBounds: function(t) {
          const i = this.getBounds(t);
          return this._applyBoundaryConstraints(i);
        },
        /**
         * @function
         * @param {OpenSeadragon.Point} delta
         * @param {Boolean} immediately
         * @returns {OpenSeadragon.Viewport} Chainable.
         * @fires OpenSeadragon.Viewer.event:pan
         */
        panBy: function(t, i) {
          const r = new e.Point();
          return i ? (r.x = this.centerSpringX.current.value, r.y = this.centerSpringY.current.value) : (r.x = this.centerSpringX.target.value, r.y = this.centerSpringY.target.value), this.panTo(r.plus(t), i);
        },
        /**
         * @function
         * @param {OpenSeadragon.Point} center
         * @param {Boolean} immediately
         * @returns {OpenSeadragon.Viewport} Chainable.
         * @fires OpenSeadragon.Viewer.event:pan
         */
        panTo: function(t, i) {
          return i ? (this.centerSpringX.resetTo(t.x), this.centerSpringY.resetTo(t.y)) : (this.centerSpringX.springTo(t.x), this.centerSpringY.springTo(t.y)), this.viewer && this.viewer.raiseEvent("pan", {
            center: t,
            immediately: i
          }), this;
        },
        /**
         * @function
         * @returns {OpenSeadragon.Viewport} Chainable.
         * @fires OpenSeadragon.Viewer.event:zoom
         */
        zoomBy: function(t, i, r) {
          return this.zoomTo(
            this.zoomSpring.target.value * t,
            i,
            r
          );
        },
        /**
         * Zooms to the specified zoom level
         * @function
         * @param {Number} zoom The zoom level to zoom to.
         * @param {OpenSeadragon.Point} [refPoint] The point which will stay at
         * the same screen location. Defaults to the viewport center.
         * @param {Boolean} [immediately=false]
         * @returns {OpenSeadragon.Viewport} Chainable.
         * @fires OpenSeadragon.Viewer.event:zoom
         */
        zoomTo: function(t, i, r) {
          const o = this;
          return this.zoomPoint = i instanceof e.Point && !isNaN(i.x) && !isNaN(i.y) ? i : null, r ? this._adjustCenterSpringsForZoomPoint(function() {
            o.zoomSpring.resetTo(t);
          }) : this.zoomSpring.springTo(t), this.viewer && this.viewer.raiseEvent("zoom", {
            zoom: t,
            refPoint: i,
            immediately: r
          }), this;
        },
        /**
         * Rotates this viewport to the angle specified.
         * @function
         * @param {Number} degrees The degrees to set the rotation to.
         * @param {Boolean} [immediately=false] Whether to animate to the new angle
         * or rotate immediately.
         * * @returns {OpenSeadragon.Viewport} Chainable.
         */
        setRotation: function(t, i) {
          return this.rotateTo(t, null, i);
        },
        /**
         * Gets the current rotation in degrees.
         * @function
         * @param {Boolean} [current=false] True for current rotation, false for target.
         * @returns {Number} The current rotation in degrees.
         */
        getRotation: function(t) {
          return t ? this.degreesSpring.current.value : this.degreesSpring.target.value;
        },
        /**
         * Rotates this viewport to the angle specified around a pivot point. Alias for rotateTo.
         * @function
         * @param {Number} degrees The degrees to set the rotation to.
         * @param {OpenSeadragon.Point} [pivot] (Optional) point in viewport coordinates
         * around which the rotation should be performed. Defaults to the center of the viewport.
         * @param {Boolean} [immediately=false] Whether to animate to the new angle
         * or rotate immediately.
         * * @returns {OpenSeadragon.Viewport} Chainable.
         */
        setRotationWithPivot: function(t, i, r) {
          return this.rotateTo(t, i, r);
        },
        /**
         * Rotates this viewport to the angle specified.
         * @function
         * @param {Number} degrees The degrees to set the rotation to.
         * @param {OpenSeadragon.Point} [pivot] (Optional) point in viewport coordinates
         * around which the rotation should be performed. Defaults to the center of the viewport.
         * @param {Boolean} [immediately=false] Whether to animate to the new angle
         * or rotate immediately.
         * @returns {OpenSeadragon.Viewport} Chainable.
         */
        rotateTo: function(t, i, r) {
          if (!this.viewer || !this.viewer.drawer.canRotate())
            return this;
          if (this.degreesSpring.target.value === t && this.degreesSpring.isAtTargetValue())
            return this;
          if (this.rotationPivot = i instanceof e.Point && !isNaN(i.x) && !isNaN(i.y) ? i : null, r)
            if (this.rotationPivot) {
              if (!(t - this._oldDegrees))
                return this.rotationPivot = null, this;
              this._rotateAboutPivot(t);
            } else
              this.degreesSpring.resetTo(t);
          else {
            const o = e.positiveModulo(this.degreesSpring.current.value, 360);
            let l = e.positiveModulo(t, 360);
            const a = l - o;
            a > 180 ? l -= 360 : a < -180 && (l += 360);
            const c = o - l;
            this.degreesSpring.resetTo(t + c), this.degreesSpring.springTo(t);
          }
          return this._setContentBounds(
            this.viewer.world.getHomeBounds(),
            this.viewer.world.getContentFactor()
          ), this.viewer.forceRedraw(), this.viewer.raiseEvent("rotate", { degrees: t, immediately: !!r, pivot: this.rotationPivot || this.getCenter() }), this;
        },
        /**
         * Rotates this viewport by the angle specified.
         * @function
         * @param {Number} degrees The degrees by which to rotate the viewport.
         * @param {OpenSeadragon.Point} [pivot] (Optional) point in viewport coordinates
         * around which the rotation should be performed. Defaults to the center of the viewport.
         * * @param {Boolean} [immediately=false] Whether to animate to the new angle
         * or rotate immediately.
         * @returns {OpenSeadragon.Viewport} Chainable.
         */
        rotateBy: function(t, i, r) {
          return this.rotateTo(this.degreesSpring.target.value + t, i, r);
        },
        /**
         * @function
         * @returns {OpenSeadragon.Viewport} Chainable.
         * @fires OpenSeadragon.Viewer.event:resize
         */
        resize: function(t, i) {
          const r = this.getBoundsNoRotate(), o = r;
          let l;
          this.containerSize.x = t.x, this.containerSize.y = t.y, this._updateContainerInnerSize(), i && (l = t.x / this.containerSize.x, o.width = r.width * l, o.height = o.width / this.getAspectRatio()), this.viewer && this.viewer.raiseEvent("resize", {
            newContainerSize: t,
            maintain: i
          });
          const a = this.fitBounds(o, !0);
          return this.viewer && this.viewer.raiseEvent("after-resize", {
            newContainerSize: t,
            maintain: i
          }), a;
        },
        // private
        _updateContainerInnerSize: function() {
          this._containerInnerSize = new e.Point(
            Math.max(1, this.containerSize.x - (this._margins.left + this._margins.right)),
            Math.max(1, this.containerSize.y - (this._margins.top + this._margins.bottom))
          );
        },
        /**
         * Update the zoom, degrees, and center (X and Y) springs.
         * @function
         * @returns {Boolean} True if the viewport is still animating, false otherwise.
         */
        update: function() {
          const t = this;
          this._adjustCenterSpringsForZoomPoint(function() {
            t.zoomSpring.update();
          }), this.degreesSpring.isAtTargetValue() && (this.rotationPivot = null), this.centerSpringX.update(), this.centerSpringY.update(), this.rotationPivot ? this._rotateAboutPivot(!0) : this.degreesSpring.update();
          const i = this.centerSpringX.current.value !== this._oldCenterX || this.centerSpringY.current.value !== this._oldCenterY || this.zoomSpring.current.value !== this._oldZoom || this.degreesSpring.current.value !== this._oldDegrees;
          return this._oldCenterX = this.centerSpringX.current.value, this._oldCenterY = this.centerSpringY.current.value, this._oldZoom = this.zoomSpring.current.value, this._oldDegrees = this.degreesSpring.current.value, i || !this.zoomSpring.isAtTargetValue() || !this.centerSpringX.isAtTargetValue() || !this.centerSpringY.isAtTargetValue() || !this.degreesSpring.isAtTargetValue();
        },
        // private - pass true to use spring, or a number for degrees for immediate rotation
        _rotateAboutPivot: function(t) {
          const i = t === !0, r = this.rotationPivot.minus(this.getCenter());
          this.centerSpringX.shiftBy(r.x), this.centerSpringY.shiftBy(r.y), i ? this.degreesSpring.update() : this.degreesSpring.resetTo(t);
          const o = this.degreesSpring.current.value - this._oldDegrees, l = r.rotate(o * -1).times(-1);
          this.centerSpringX.shiftBy(l.x), this.centerSpringY.shiftBy(l.y);
        },
        // private
        _adjustCenterSpringsForZoomPoint: function(t) {
          if (this.zoomPoint) {
            const i = this.pixelFromPoint(this.zoomPoint, !0);
            t();
            const o = this.pixelFromPoint(this.zoomPoint, !0).minus(i), l = this.deltaPointsFromPixels(
              o,
              !0
            );
            this.centerSpringX.shiftBy(l.x), this.centerSpringY.shiftBy(l.y), this.zoomSpring.isAtTargetValue() && (this.zoomPoint = null);
          } else
            t();
        },
        /**
         * Convert a delta (translation vector) from viewport coordinates to pixels
         * coordinates. This method does not take rotation into account.
         * Consider using deltaPixelsFromPoints if you need to account for rotation.
         * @param {OpenSeadragon.Point} deltaPoints - The translation vector to convert.
         * @param {Boolean} [current=false] - Pass true for the current location;
         * defaults to false (target location).
         * @returns {OpenSeadragon.Point}
         */
        deltaPixelsFromPointsNoRotate: function(t, i) {
          return t.times(
            this._containerInnerSize.x * this.getZoom(i)
          );
        },
        /**
         * Convert a delta (translation vector) from viewport coordinates to pixels
         * coordinates.
         * @param {OpenSeadragon.Point} deltaPoints - The translation vector to convert.
         * @param {Boolean} [current=false] - Pass true for the current location;
         * defaults to false (target location).
         * @returns {OpenSeadragon.Point}
         */
        deltaPixelsFromPoints: function(t, i) {
          return this.deltaPixelsFromPointsNoRotate(
            t.rotate(this.getRotation(i)),
            i
          );
        },
        /**
         * Convert a delta (translation vector) from pixels coordinates to viewport
         * coordinates. This method does not take rotation into account.
         * Consider using deltaPointsFromPixels if you need to account for rotation.
         * @param {OpenSeadragon.Point} deltaPixels - The translation vector to convert.
         * @param {Boolean} [current=false] - Pass true for the current location;
         * defaults to false (target location).
         * @returns {OpenSeadragon.Point}
         */
        deltaPointsFromPixelsNoRotate: function(t, i) {
          return t.divide(
            this._containerInnerSize.x * this.getZoom(i)
          );
        },
        /**
         * Convert a delta (translation vector) from pixels coordinates to viewport
         * coordinates.
         * @param {OpenSeadragon.Point} deltaPixels - The translation vector to convert.
         * @param {Boolean} [current=false] - Pass true for the current location;
         * defaults to false (target location).
         * @returns {OpenSeadragon.Point}
         */
        deltaPointsFromPixels: function(t, i) {
          return this.deltaPointsFromPixelsNoRotate(t, i).rotate(-this.getRotation(i));
        },
        /**
         * Convert viewport coordinates to pixels coordinates.
         * This method does not take rotation into account.
         * Consider using pixelFromPoint if you need to account for rotation.
         * @param {OpenSeadragon.Point} point the viewport coordinates
         * @param {Boolean} [current=false] - Pass true for the current location;
         * defaults to false (target location).
         * @returns {OpenSeadragon.Point}
         */
        pixelFromPointNoRotate: function(t, i) {
          return this._pixelFromPointNoRotate(
            t,
            this.getBoundsNoRotate(i)
          );
        },
        /**
         * Convert viewport coordinates to pixel coordinates.
         * @param {OpenSeadragon.Point} point the viewport coordinates
         * @param {Boolean} [current=false] - Pass true for the current location;
         * defaults to false (target location).
         * @returns {OpenSeadragon.Point}
         */
        pixelFromPoint: function(t, i) {
          return this._pixelFromPoint(t, this.getBoundsNoRotate(i));
        },
        // private
        _pixelFromPointNoRotate: function(t, i) {
          return t.minus(
            i.getTopLeft()
          ).times(
            this._containerInnerSize.x / i.width
          ).plus(
            new e.Point(this._margins.left, this._margins.top)
          );
        },
        // private
        _pixelFromPoint: function(t, i) {
          return this._pixelFromPointNoRotate(
            t.rotate(this.getRotation(!0), this.getCenter(!0)),
            i
          );
        },
        /**
         * Convert pixel coordinates to viewport coordinates.
         * This method does not take rotation into account.
         * Consider using pointFromPixel if you need to account for rotation.
         * @param {OpenSeadragon.Point} pixel Pixel coordinates
         * @param {Boolean} [current=false] - Pass true for the current location;
         * defaults to false (target location).
         * @returns {OpenSeadragon.Point}
         */
        pointFromPixelNoRotate: function(t, i) {
          const r = this.getBoundsNoRotate(i);
          return t.minus(
            new e.Point(this._margins.left, this._margins.top)
          ).divide(
            this._containerInnerSize.x / r.width
          ).plus(
            r.getTopLeft()
          );
        },
        /**
         * Convert pixel coordinates to viewport coordinates.
         * @param {OpenSeadragon.Point} pixel Pixel coordinates
         * @param {Boolean} [current=false] - Pass true for the current location;
         * defaults to false (target location).
         * @returns {OpenSeadragon.Point}
         */
        pointFromPixel: function(t, i) {
          return this.pointFromPixelNoRotate(t, i).rotate(
            -this.getRotation(i),
            this.getCenter(i)
          );
        },
        // private
        _viewportToImageDelta: function(t, i) {
          const r = this._contentBoundsNoRotate.width;
          return new e.Point(
            t * this._contentSizeNoRotate.x / r,
            i * this._contentSizeNoRotate.x / r
          );
        },
        /**
         * Translates from OpenSeadragon viewer coordinate system to image coordinate system.
         * This method can be called either by passing X,Y coordinates or an
         * OpenSeadragon.Point
         * Note: not accurate with multi-image; use TiledImage.viewportToImageCoordinates instead.
         * @function
         * @param {(OpenSeadragon.Point|Number)} viewerX either a point or the X
         * coordinate in viewport coordinate system.
         * @param {Number} [viewerY] Y coordinate in viewport coordinate system.
         * @returns {OpenSeadragon.Point} a point representing the coordinates in the image.
         */
        viewportToImageCoordinates: function(t, i) {
          if (t instanceof e.Point)
            return this.viewportToImageCoordinates(t.x, t.y);
          if (this.viewer) {
            const r = this.viewer.world.getItemCount();
            if (r > 1)
              this.silenceMultiImageWarnings || e.console.error("[Viewport.viewportToImageCoordinates] is not accurate with multi-image; use TiledImage.viewportToImageCoordinates instead.");
            else if (r === 1)
              return this.viewer.world.getItemAt(0).viewportToImageCoordinates(t, i, !0);
          }
          return this._viewportToImageDelta(
            t - this._contentBoundsNoRotate.x,
            i - this._contentBoundsNoRotate.y
          );
        },
        // private
        _imageToViewportDelta: function(t, i) {
          const r = this._contentBoundsNoRotate.width;
          return new e.Point(
            t / this._contentSizeNoRotate.x * r,
            i / this._contentSizeNoRotate.x * r
          );
        },
        /**
         * Translates from image coordinate system to OpenSeadragon viewer coordinate system
         * This method can be called either by passing X,Y coordinates or an
         * OpenSeadragon.Point
         * Note: not accurate with multi-image; use TiledImage.imageToViewportCoordinates instead.
         * @function
         * @param {(OpenSeadragon.Point | Number)} imageX the point or the
         * X coordinate in image coordinate system.
         * @param {Number} [imageY] Y coordinate in image coordinate system.
         * @returns {OpenSeadragon.Point} a point representing the coordinates in the viewport.
         */
        imageToViewportCoordinates: function(t, i) {
          if (t instanceof e.Point)
            return this.imageToViewportCoordinates(t.x, t.y);
          if (this.viewer) {
            const o = this.viewer.world.getItemCount();
            if (o > 1)
              this.silenceMultiImageWarnings || e.console.error("[Viewport.imageToViewportCoordinates] is not accurate with multi-image; use TiledImage.imageToViewportCoordinates instead.");
            else if (o === 1)
              return this.viewer.world.getItemAt(0).imageToViewportCoordinates(t, i, !0);
          }
          const r = this._imageToViewportDelta(t, i);
          return r.x += this._contentBoundsNoRotate.x, r.y += this._contentBoundsNoRotate.y, r;
        },
        /**
         * Translates from a rectangle which describes a portion of the image in
         * pixel coordinates to OpenSeadragon viewport rectangle coordinates.
         * This method can be called either by passing X,Y,width,height or an
         * OpenSeadragon.Rect
         * Note: not accurate with multi-image; use TiledImage.imageToViewportRectangle instead.
         * @function
         * @param {(OpenSeadragon.Rect | Number)} imageX the rectangle or the X
         * coordinate of the top left corner of the rectangle in image coordinate system.
         * @param {Number} [imageY] the Y coordinate of the top left corner of the rectangle
         * in image coordinate system.
         * @param {Number} [pixelWidth] the width in pixel of the rectangle.
         * @param {Number} [pixelHeight] the height in pixel of the rectangle.
         * @returns {OpenSeadragon.Rect} This image's bounds in viewport coordinates
         */
        imageToViewportRectangle: function(t, i, r, o) {
          let l = t;
          if (l instanceof e.Rect || (l = new e.Rect(t, i, r, o)), this.viewer) {
            const d = this.viewer.world.getItemCount();
            if (d > 1)
              this.silenceMultiImageWarnings || e.console.error("[Viewport.imageToViewportRectangle] is not accurate with multi-image; use TiledImage.imageToViewportRectangle instead.");
            else if (d === 1)
              return this.viewer.world.getItemAt(0).imageToViewportRectangle(
                t,
                i,
                r,
                o,
                !0
              );
          }
          const a = this.imageToViewportCoordinates(l.x, l.y), c = this._imageToViewportDelta(l.width, l.height);
          return new e.Rect(
            a.x,
            a.y,
            c.x,
            c.y,
            l.degrees
          );
        },
        /**
         * Translates from a rectangle which describes a portion of
         * the viewport in point coordinates to image rectangle coordinates.
         * This method can be called either by passing X,Y,width,height or an
         * OpenSeadragon.Rect
         * Note: not accurate with multi-image; use TiledImage.viewportToImageRectangle instead.
         * @function
         * @param {(OpenSeadragon.Rect | Number)} viewerX either a rectangle or
         * the X coordinate of the top left corner of the rectangle in viewport
         * coordinate system.
         * @param {Number} [viewerY] the Y coordinate of the top left corner of the rectangle
         * in viewport coordinate system.
         * @param {Number} [pointWidth] the width of the rectangle in viewport coordinate system.
         * @param {Number} [pointHeight] the height of the rectangle in viewport coordinate system.
         */
        viewportToImageRectangle: function(t, i, r, o) {
          let l = t;
          if (l instanceof e.Rect || (l = new e.Rect(t, i, r, o)), this.viewer) {
            const d = this.viewer.world.getItemCount();
            if (d > 1)
              this.silenceMultiImageWarnings || e.console.error("[Viewport.viewportToImageRectangle] is not accurate with multi-image; use TiledImage.viewportToImageRectangle instead.");
            else if (d === 1)
              return this.viewer.world.getItemAt(0).viewportToImageRectangle(
                t,
                i,
                r,
                o,
                !0
              );
          }
          const a = this.viewportToImageCoordinates(l.x, l.y), c = this._viewportToImageDelta(l.width, l.height);
          return new e.Rect(
            a.x,
            a.y,
            c.x,
            c.y,
            l.degrees
          );
        },
        /**
         * Convert pixel coordinates relative to the viewer element to image
         * coordinates.
         * Note: not accurate with multi-image.
         * @param {OpenSeadragon.Point} pixel
         * @returns {OpenSeadragon.Point}
         */
        viewerElementToImageCoordinates: function(t) {
          const i = this.pointFromPixel(t, !0);
          return this.viewportToImageCoordinates(i);
        },
        /**
         * Convert pixel coordinates relative to the image to
         * viewer element coordinates.
         * Note: not accurate with multi-image.
         * @param {OpenSeadragon.Point} pixel
         * @returns {OpenSeadragon.Point}
         */
        imageToViewerElementCoordinates: function(t) {
          const i = this.imageToViewportCoordinates(t);
          return this.pixelFromPoint(i, !0);
        },
        /**
         * Convert pixel coordinates relative to the window to image coordinates.
         * Note: not accurate with multi-image.
         * @param {OpenSeadragon.Point} pixel
         * @returns {OpenSeadragon.Point}
         */
        windowToImageCoordinates: function(t) {
          e.console.assert(
            this.viewer,
            "[Viewport.windowToImageCoordinates] the viewport must have a viewer."
          );
          const i = t.minus(
            e.getElementPosition(this.viewer.element)
          );
          return this.viewerElementToImageCoordinates(i);
        },
        /**
         * Convert image coordinates to pixel coordinates relative to the window.
         * Note: not accurate with multi-image.
         * @param {OpenSeadragon.Point} pixel
         * @returns {OpenSeadragon.Point}
         */
        imageToWindowCoordinates: function(t) {
          return e.console.assert(
            this.viewer,
            "[Viewport.imageToWindowCoordinates] the viewport must have a viewer."
          ), this.imageToViewerElementCoordinates(t).plus(
            e.getElementPosition(this.viewer.element)
          );
        },
        /**
         * Convert pixel coordinates relative to the viewer element to viewport
         * coordinates.
         * @param {OpenSeadragon.Point} pixel
         * @returns {OpenSeadragon.Point}
         */
        viewerElementToViewportCoordinates: function(t) {
          return this.pointFromPixel(t, !0);
        },
        /**
         * Convert viewport coordinates to pixel coordinates relative to the
         * viewer element.
         * @param {OpenSeadragon.Point} point
         * @returns {OpenSeadragon.Point}
         */
        viewportToViewerElementCoordinates: function(t) {
          return this.pixelFromPoint(t, !0);
        },
        /**
         * Convert a rectangle in pixel coordinates relative to the viewer element
         * to viewport coordinates.
         * @param {OpenSeadragon.Rect} rectangle the rectangle to convert
         * @returns {OpenSeadragon.Rect} the converted rectangle
         */
        viewerElementToViewportRectangle: function(t) {
          return e.Rect.fromSummits(
            this.pointFromPixel(t.getTopLeft(), !0),
            this.pointFromPixel(t.getTopRight(), !0),
            this.pointFromPixel(t.getBottomLeft(), !0)
          );
        },
        /**
         * Convert a rectangle in viewport coordinates to pixel coordinates relative
         * to the viewer element.
         * @param {OpenSeadragon.Rect} rectangle the rectangle to convert
         * @returns {OpenSeadragon.Rect} the converted rectangle
         */
        viewportToViewerElementRectangle: function(t) {
          return e.Rect.fromSummits(
            this.pixelFromPoint(t.getTopLeft(), !0),
            this.pixelFromPoint(t.getTopRight(), !0),
            this.pixelFromPoint(t.getBottomLeft(), !0)
          );
        },
        /**
         * Convert pixel coordinates relative to the window to viewport coordinates.
         * @param {OpenSeadragon.Point} pixel
         * @returns {OpenSeadragon.Point}
         */
        windowToViewportCoordinates: function(t) {
          e.console.assert(
            this.viewer,
            "[Viewport.windowToViewportCoordinates] the viewport must have a viewer."
          );
          const i = t.minus(
            e.getElementPosition(this.viewer.element)
          );
          return this.viewerElementToViewportCoordinates(i);
        },
        /**
         * Convert viewport coordinates to pixel coordinates relative to the window.
         * @param {OpenSeadragon.Point} point
         * @returns {OpenSeadragon.Point}
         */
        viewportToWindowCoordinates: function(t) {
          return e.console.assert(
            this.viewer,
            "[Viewport.viewportToWindowCoordinates] the viewport must have a viewer."
          ), this.viewportToViewerElementCoordinates(t).plus(
            e.getElementPosition(this.viewer.element)
          );
        },
        /**
         * Convert a viewport zoom to an image zoom.
         * Image zoom: ratio of the original image size to displayed image size.
         * 1 means original image size, 0.5 half size...
         * Viewport zoom: ratio of the displayed image's width to viewport's width.
         * 1 means identical width, 2 means image's width is twice the viewport's width...
         * Note: not accurate with multi-image.
         * @function
         * @param {Number} viewportZoom The viewport zoom
         * target zoom.
         * @returns {Number} imageZoom The image zoom
         */
        viewportToImageZoom: function(t) {
          if (this.viewer) {
            const a = this.viewer.world.getItemCount();
            if (a > 1)
              this.silenceMultiImageWarnings || e.console.error("[Viewport.viewportToImageZoom] is not accurate with multi-image.");
            else if (a === 1)
              return this.viewer.world.getItemAt(0).viewportToImageZoom(t);
          }
          const i = this._contentSizeNoRotate.x, r = this._containerInnerSize.x, o = this._contentBoundsNoRotate.width, l = r / i * o;
          return t * l;
        },
        /**
         * Convert an image zoom to a viewport zoom.
         * Image zoom: ratio of the original image size to displayed image size.
         * 1 means original image size, 0.5 half size...
         * Viewport zoom: ratio of the displayed image's width to viewport's width.
         * 1 means identical width, 2 means image's width is twice the viewport's width...
         * Note: not accurate with multi-image; use [TiledImage.imageToViewportZoom] for the specific image of interest.
         * @function
         * @param {Number} imageZoom The image zoom
         * target zoom.
         * @returns {Number} viewportZoom The viewport zoom
         */
        imageToViewportZoom: function(t) {
          if (this.viewer) {
            const a = this.viewer.world.getItemCount();
            if (a > 1)
              this.silenceMultiImageWarnings || e.console.error("[Viewport.imageToViewportZoom] is not accurate with multi-image. Instead, use [TiledImage.imageToViewportZoom] for the specific image of interest");
            else if (a === 1)
              return this.viewer.world.getItemAt(0).imageToViewportZoom(t);
          }
          const i = this._contentSizeNoRotate.x, r = this._containerInnerSize.x, o = this._contentBoundsNoRotate.width, l = i / r / o;
          return t * l;
        },
        /**
         * Toggles flip state and demands a new drawing on navigator and viewer objects.
         * @function
         * @returns {OpenSeadragon.Viewport} Chainable.
         */
        toggleFlip: function() {
          return this.setFlip(!this.getFlip()), this;
        },
        /**
         * Get flip state stored on viewport.
         * @function
         * @returns {Boolean} Flip state.
         */
        getFlip: function() {
          return this.flipped;
        },
        /**
         * Sets flip state according to the state input argument.
         * @function
         * @param {Boolean} state - Flip state to set.
         * @returns {OpenSeadragon.Viewport} Chainable.
         */
        setFlip: function(t) {
          return this.flipped === t ? this : (this.flipped = t, this.viewer.navigator && this.viewer.navigator.setFlip(this.getFlip()), this.viewer.forceRedraw(), this.viewer.raiseEvent("flip", { flipped: t }), this);
        },
        /**
         * Gets current max zoom pixel ratio
         * @function
         * @returns {Number} Max zoom pixel ratio
         */
        getMaxZoomPixelRatio: function() {
          return this.maxZoomPixelRatio;
        },
        /**
         * Sets max zoom pixel ratio
         * @function
         * @param {Number} ratio - Max zoom pixel ratio
         * @param {Boolean} [applyConstraints=true] - Apply constraints after setting ratio;
         * Takes effect only if current zoom is greater than set max zoom pixel ratio
         * @param {Boolean} [immediately=false] - Whether to animate to new zoom
         */
        setMaxZoomPixelRatio: function(t, i = !0, r = !1) {
          e.console.assert(!isNaN(t), "[Viewport.setMaxZoomPixelRatio] ratio must be a number"), !isNaN(t) && (this.maxZoomPixelRatio = t, i && this.getZoom() > this.getMaxZoom() && this.applyConstraints(r));
        }
      };
    })(n), (function(e) {
      e.TiledImage = function(t) {
        this._initialized = !1, e.console.assert(t.tileCache, "[TiledImage] options.tileCache is required"), e.console.assert(t.drawer, "[TiledImage] options.drawer is required"), e.console.assert(t.viewer, "[TiledImage] options.viewer is required"), e.console.assert(t.imageLoader, "[TiledImage] options.imageLoader is required"), e.console.assert(t.source, "[TiledImage] options.source is required"), e.console.assert(
          !t.clip || t.clip instanceof e.Rect,
          "[TiledImage] options.clip must be an OpenSeadragon.Rect if present"
        ), e.EventSource.call(this), this._tileCache = t.tileCache, delete t.tileCache, this._drawer = t.drawer, delete t.drawer, this._imageLoader = t.imageLoader, delete t.imageLoader, t.clip instanceof e.Rect && (this._clip = t.clip.clone()), delete t.clip;
        const i = t.x || 0;
        delete t.x;
        const r = t.y || 0;
        delete t.y, this.normHeight = t.source.dimensions.y / t.source.dimensions.x, this.contentAspectX = t.source.dimensions.x / t.source.dimensions.y;
        let o = 1;
        t.width ? (o = t.width, delete t.width, t.height && (e.console.error("specifying both width and height to a tiledImage is not supported"), delete t.height)) : t.height && (o = t.height / this.normHeight, delete t.height);
        const l = t.fitBounds;
        delete t.fitBounds;
        const a = t.fitBoundsPlacement || n.Placement.CENTER;
        delete t.fitBoundsPlacement;
        const c = t.degrees || 0;
        delete t.degrees;
        const d = t.ajaxHeaders;
        delete t.ajaxHeaders, e.extend(!0, this, {
          //internal state properties
          viewer: null,
          tilesMatrix: {},
          // A '3d' dictionary [level][x][y] --> Tile.
          coverage: {},
          // A '3d' dictionary [level][x][y] --> Boolean; shows what areas have been drawn.
          loadingCoverage: {},
          // A '3d' dictionary [level][x][y] --> Boolean; shows what areas are loaded or are being loaded/blended.
          lastDrawn: [],
          // An unordered list of Tiles drawn last frame.
          lastResetTime: 0,
          // Last time for which the tiledImage was reset.
          _needsDraw: !0,
          // Does the tiledImage need to be drawn again?
          _needsUpdate: !0,
          // Does the tiledImage need to update the viewport again?
          _hasOpaqueTile: !1,
          // Do we have even one fully opaque tile?
          _tilesLoading: 0,
          // The number of pending tile requests.
          _zombieCache: !1,
          // Allow cache to stay in memory upon deletion.
          _tilesToDraw: [],
          // info about the tiles currently in the viewport, two deep: array[level][tile]
          _lastDrawn: [],
          // array of tiles that were last fetched by the drawer
          _arrayCacheMap: [],
          // array cache to avoid constant re-creation and GC overload
          _isBlending: !1,
          // Are any tiles still being blended?
          _wasBlending: !1,
          // Were any tiles blending before the last draw?
          _isTainted: !1,
          // Has a Tile been found with tainted data?
          //configurable settings
          springStiffness: e.DEFAULT_SETTINGS.springStiffness,
          animationTime: e.DEFAULT_SETTINGS.animationTime,
          minZoomImageRatio: e.DEFAULT_SETTINGS.minZoomImageRatio,
          wrapHorizontal: e.DEFAULT_SETTINGS.wrapHorizontal,
          wrapVertical: e.DEFAULT_SETTINGS.wrapVertical,
          immediateRender: e.DEFAULT_SETTINGS.immediateRender,
          loadDestinationTilesOnAnimation: e.DEFAULT_SETTINGS.loadDestinationTilesOnAnimation,
          blendTime: e.DEFAULT_SETTINGS.blendTime,
          alwaysBlend: e.DEFAULT_SETTINGS.alwaysBlend,
          minPixelRatio: e.DEFAULT_SETTINGS.minPixelRatio,
          smoothTileEdgesMinZoom: e.DEFAULT_SETTINGS.smoothTileEdgesMinZoom,
          iOSDevice: e.DEFAULT_SETTINGS.iOSDevice,
          debugMode: e.DEFAULT_SETTINGS.debugMode,
          crossOriginPolicy: e.DEFAULT_SETTINGS.crossOriginPolicy,
          ajaxWithCredentials: e.DEFAULT_SETTINGS.ajaxWithCredentials,
          placeholderFillStyle: e.DEFAULT_SETTINGS.placeholderFillStyle,
          opacity: e.DEFAULT_SETTINGS.opacity,
          preload: e.DEFAULT_SETTINGS.preload,
          compositeOperation: e.DEFAULT_SETTINGS.compositeOperation,
          subPixelRoundingForTransparency: e.DEFAULT_SETTINGS.subPixelRoundingForTransparency,
          maxTilesPerFrame: e.DEFAULT_SETTINGS.maxTilesPerFrame,
          _currentMaxTilesPerFrame: (t.maxTilesPerFrame || e.DEFAULT_SETTINGS.maxTilesPerFrame) * 10
        }, t), this._preload = this.preload, delete this.preload, this._fullyLoaded = !1, this._xSpring = new e.Spring({
          initial: i,
          springStiffness: this.springStiffness,
          animationTime: this.animationTime
        }), this._ySpring = new e.Spring({
          initial: r,
          springStiffness: this.springStiffness,
          animationTime: this.animationTime
        }), this._scaleSpring = new e.Spring({
          initial: o,
          springStiffness: this.springStiffness,
          animationTime: this.animationTime
        }), this._degreesSpring = new e.Spring({
          initial: c,
          springStiffness: this.springStiffness,
          animationTime: this.animationTime
        }), this._updateForScale(), l && this.fitBounds(l, a, !0), this._ownAjaxHeaders = {}, this.setAjaxHeaders(d, !1), this._initialized = !0;
      }, e.extend(
        e.TiledImage.prototype,
        e.EventSource.prototype,
        /** @lends OpenSeadragon.TiledImage.prototype */
        {
          /**
           * @returns {Boolean} Whether the TiledImage needs to be drawn.
           */
          needsDraw: function() {
            return this._needsDraw;
          },
          /**
           * Mark the tiled image as needing to be (re)drawn
           */
          redraw: function() {
            this._needsDraw = !0;
          },
          /**
           * @returns {Boolean} Whether all tiles necessary for this TiledImage to draw at the current view have been loaded.
           */
          getFullyLoaded: function() {
            return this._fullyLoaded;
          },
          /**
           * Executes the provided callback when the TiledImage is fully loaded. If already loaded,
           * schedules the callback asynchronously. Otherwise, attaches a one-time event listener
           * for the 'fully-loaded-change' event.
           * @param {Function} callback - Function to execute when loading completes
           */
          whenFullyLoaded: function(t) {
            this.getFullyLoaded() ? setTimeout(t, 1) : this.addOnceHandler("fully-loaded-change", function() {
              t();
            });
          },
          // private
          _setFullyLoaded: function(t) {
            t !== this._fullyLoaded && (this._fullyLoaded = t, this.raiseEvent("fully-loaded-change", {
              fullyLoaded: this._fullyLoaded
            }));
          },
          /**
           * Forces the system consider all tiles in this tiled image
           * as outdated, and fire tile update event on relevant tiles
           * Detailed description is available within the 'tile-invalidated'
           * event.
           * @param {Boolean} [restoreTiles=true] if true, tile processing starts from the tile original data
           * @param {boolean} [viewportOnly=false] optionally invalidate only viewport-visible tiles if true
           * @param {number} [tStamp=OpenSeadragon.now()] optionally provide tStamp of the update event
           */
          requestInvalidate: function(t = !0, i = !1, r = e.now()) {
            const o = i ? this._lastDrawn.map((l) => l.tile) : this._tileCache.getLoadedTilesFor(this);
            return this.viewer.world.requestTileInvalidateEvent(o, r, t);
          },
          /**
           * Clears all tiles and triggers an update on the next call to
           * {@link OpenSeadragon.TiledImage#update}.
           */
          reset: function() {
            this._tileCache.clearTilesFor(this), this._currentMaxTilesPerFrame = this.maxTilesPerFrame * 10, this.lastResetTime = e.now(), this._needsDraw = !0, this._fullyLoaded = !1;
          },
          /**
           * Updates the TiledImage's bounds, animating if needed. Based on the new
           * bounds, updates the levels and tiles to be drawn into the viewport.
           * @param viewportChanged Whether the viewport changed meaning tiles need to be updated.
           * @returns {Boolean} Whether the TiledImage needs to be drawn.
           */
          update: function(t) {
            const i = this._xSpring.update(), r = this._ySpring.update(), o = this._scaleSpring.update(), l = this._degreesSpring.update(), a = i || r || o || l || this._needsUpdate;
            if (a || t || !this._fullyLoaded) {
              const c = this._updateLevelsForViewport();
              this._setFullyLoaded(c);
            }
            return this._needsUpdate = !1, a ? (this._updateForScale(), this._raiseBoundsChange(), this._needsDraw = !0, !0) : !1;
          },
          /**
           * Mark this TiledImage as having been drawn, so that it will only be drawn
           * again if something changes about the image. If the image is still blending,
           * this will have no effect.
           * @returns {Boolean} whether the item still needs to be drawn due to blending
           */
          setDrawn: function() {
            return this._needsDraw = this._isBlending || this._wasBlending || this.opacity > 0 && this._lastDrawn.length < 1, this._needsDraw;
          },
          /**
           * Set the internal _isTainted flag for this TiledImage. Lazy loaded - not
           * checked each time a Tile is loaded, but can be set if a consumer of the
           * tiles (e.g. a Drawer) discovers a Tile to have tainted data so that further
           * checks are not needed and alternative rendering strategies can be used.
           * @private
           */
          setTainted(t) {
            this._isTainted = t;
          },
          /**
           * @private
           * @returns {Boolean} whether the TiledImage has been marked as tainted
           */
          isTainted() {
            return this._isTainted;
          },
          /**
           * Destroy the TiledImage (unload current loaded tiles).
           */
          destroy: function() {
            this.reset(), this.source.destroy(this.viewer);
          },
          /**
           * Get this TiledImage's bounds in viewport coordinates.
           * @param {Boolean} [current=false] - Pass true for the current location;
           * false for target location.
           * @returns {OpenSeadragon.Rect} This TiledImage's bounds in viewport coordinates.
           */
          getBounds: function(t) {
            return this.getBoundsNoRotate(t).rotate(this.getRotation(t), this._getRotationPoint(t));
          },
          /**
           * Get this TiledImage's bounds in viewport coordinates without taking
           * rotation into account.
           * @param {Boolean} [current=false] - Pass true for the current location;
           * false for target location.
           * @returns {OpenSeadragon.Rect} This TiledImage's bounds in viewport coordinates.
           */
          getBoundsNoRotate: function(t) {
            return t ? new e.Rect(
              this._xSpring.current.value,
              this._ySpring.current.value,
              this._worldWidthCurrent,
              this._worldHeightCurrent
            ) : new e.Rect(
              this._xSpring.target.value,
              this._ySpring.target.value,
              this._worldWidthTarget,
              this._worldHeightTarget
            );
          },
          // deprecated
          getWorldBounds: function() {
            return e.console.error("[TiledImage.getWorldBounds] is deprecated; use TiledImage.getBounds instead"), this.getBounds();
          },
          /**
           * Get the bounds of the displayed part of the tiled image.
           * @param {Boolean} [current=false] Pass true for the current location,
           * false for the target location.
           * @returns {$.Rect} The clipped bounds in viewport coordinates.
           */
          getClippedBounds: function(t) {
            let i = this.getBoundsNoRotate(t);
            if (this._clip) {
              const o = (t ? this._worldWidthCurrent : this._worldWidthTarget) / this.source.dimensions.x, l = this._clip.times(o);
              i = new e.Rect(
                i.x + l.x,
                i.y + l.y,
                l.width,
                l.height
              );
            }
            return i.rotate(this.getRotation(t), this._getRotationPoint(t));
          },
          /**
           * @function
           * @param {Number} level
           * @param {Number} x
           * @param {Number} y
           * @returns {OpenSeadragon.Rect} Where this tile fits (in normalized coordinates).
           */
          getTileBounds: function(t, i, r) {
            const o = this.source.getNumTiles(t), l = (o.x + i % o.x) % o.x, a = (o.y + r % o.y) % o.y, c = this.source.getTileBounds(t, l, a);
            return this.getFlip() && (c.x = Math.max(0, 1 - c.x - c.width)), c.x += (i - l) / o.x, c.y += this._worldHeightCurrent / this._worldWidthCurrent * ((r - a) / o.y), c;
          },
          /**
           * @returns {OpenSeadragon.Point} This TiledImage's content size, in original pixels.
           */
          getContentSize: function() {
            return new e.Point(this.source.dimensions.x, this.source.dimensions.y);
          },
          /**
           * @returns {OpenSeadragon.Point} The TiledImage's content size, in window coordinates.
           */
          getSizeInWindowCoordinates: function() {
            const t = this.imageToWindowCoordinates(new e.Point(0, 0)), i = this.imageToWindowCoordinates(this.getContentSize());
            return new e.Point(i.x - t.x, i.y - t.y);
          },
          // private
          _viewportToImageDelta: function(t, i, r) {
            const o = r ? this._scaleSpring.current.value : this._scaleSpring.target.value;
            return new e.Point(
              t * (this.source.dimensions.x / o),
              i * (this.source.dimensions.y * this.contentAspectX / o)
            );
          },
          /**
           * Translates from OpenSeadragon viewer coordinate system to image coordinate system.
           * This method can be called either by passing X,Y coordinates or an {@link OpenSeadragon.Point}.
           * @param {Number|OpenSeadragon.Point} viewerX - The X coordinate or point in viewport coordinate system.
           * @param {Number} [viewerY] - The Y coordinate in viewport coordinate system.
           * @param {Boolean} [current=false] - Pass true to use the current location; false for target location.
           * @returns {OpenSeadragon.Point} A point representing the coordinates in the image.
           */
          viewportToImageCoordinates: function(t, i, r) {
            let o;
            return t instanceof e.Point ? (r = i, o = t) : o = new e.Point(t, i), o = o.rotate(-this.getRotation(r), this._getRotationPoint(r)), r ? this._viewportToImageDelta(
              o.x - this._xSpring.current.value,
              o.y - this._ySpring.current.value
            ) : this._viewportToImageDelta(
              o.x - this._xSpring.target.value,
              o.y - this._ySpring.target.value
            );
          },
          // private
          _imageToViewportDelta: function(t, i, r) {
            const o = r ? this._scaleSpring.current.value : this._scaleSpring.target.value;
            return new e.Point(
              t / this.source.dimensions.x * o,
              i / this.source.dimensions.y / this.contentAspectX * o
            );
          },
          /**
           * Translates from image coordinate system to OpenSeadragon viewer coordinate system
           * This method can be called either by passing X,Y coordinates or an {@link OpenSeadragon.Point}.
           * @param {Number|OpenSeadragon.Point} imageX - The X coordinate or point in image coordinate system.
           * @param {Number} [imageY] - The Y coordinate in image coordinate system.
           * @param {Boolean} [current=false] - Pass true to use the current location; false for target location.
           * @returns {OpenSeadragon.Point} A point representing the coordinates in the viewport.
           */
          imageToViewportCoordinates: function(t, i, r) {
            t instanceof e.Point && (r = i, i = t.y, t = t.x);
            const o = this._imageToViewportDelta(t, i, r);
            return r ? (o.x += this._xSpring.current.value, o.y += this._ySpring.current.value) : (o.x += this._xSpring.target.value, o.y += this._ySpring.target.value), o.rotate(this.getRotation(r), this._getRotationPoint(r));
          },
          /**
           * Translates from a rectangle which describes a portion of the image in
           * pixel coordinates to OpenSeadragon viewport rectangle coordinates.
           * This method can be called either by passing X,Y,width,height or an {@link OpenSeadragon.Rect}.
           * @param {Number|OpenSeadragon.Rect} imageX - The left coordinate or rectangle in image coordinate system.
           * @param {Number} [imageY] - The top coordinate in image coordinate system.
           * @param {Number} [pixelWidth] - The width in pixel of the rectangle.
           * @param {Number} [pixelHeight] - The height in pixel of the rectangle.
           * @param {Boolean} [current=false] - Pass true to use the current location; false for target location.
           * @returns {OpenSeadragon.Rect} A rect representing the coordinates in the viewport.
           */
          imageToViewportRectangle: function(t, i, r, o, l) {
            let a = t;
            a instanceof e.Rect ? l = i : a = new e.Rect(t, i, r, o);
            const c = this.imageToViewportCoordinates(a.getTopLeft(), l), d = this._imageToViewportDelta(a.width, a.height, l);
            return new e.Rect(
              c.x,
              c.y,
              d.x,
              d.y,
              a.degrees + this.getRotation(l)
            );
          },
          /**
           * Translates from a rectangle which describes a portion of
           * the viewport in point coordinates to image rectangle coordinates.
           * This method can be called either by passing X,Y,width,height or an {@link OpenSeadragon.Rect}.
           * @param {Number|OpenSeadragon.Rect} viewerX - The left coordinate or rectangle in viewport coordinate system.
           * @param {Number} [viewerY] - The top coordinate in viewport coordinate system.
           * @param {Number} [pointWidth] - The width in viewport coordinate system.
           * @param {Number} [pointHeight] - The height in viewport coordinate system.
           * @param {Boolean} [current=false] - Pass true to use the current location; false for target location.
           * @returns {OpenSeadragon.Rect} A rect representing the coordinates in the image.
           */
          viewportToImageRectangle: function(t, i, r, o, l) {
            let a = t;
            t instanceof e.Rect ? l = i : a = new e.Rect(t, i, r, o);
            const c = this.viewportToImageCoordinates(a.getTopLeft(), l), d = this._viewportToImageDelta(a.width, a.height, l);
            return new e.Rect(
              c.x,
              c.y,
              d.x,
              d.y,
              a.degrees - this.getRotation(l)
            );
          },
          /**
           * Convert pixel coordinates relative to the viewer element to image
           * coordinates.
           * @param {OpenSeadragon.Point} pixel
           * @returns {OpenSeadragon.Point}
           */
          viewerElementToImageCoordinates: function(t) {
            const i = this.viewport.pointFromPixel(t, !0);
            return this.viewportToImageCoordinates(i);
          },
          /**
           * Convert pixel coordinates relative to the image to
           * viewer element coordinates.
           * @param {OpenSeadragon.Point} pixel
           * @returns {OpenSeadragon.Point}
           */
          imageToViewerElementCoordinates: function(t) {
            const i = this.imageToViewportCoordinates(t);
            return this.viewport.pixelFromPoint(i, !0);
          },
          /**
           * Convert pixel coordinates relative to the window to image coordinates.
           * @param {OpenSeadragon.Point} pixel
           * @returns {OpenSeadragon.Point}
           */
          windowToImageCoordinates: function(t) {
            const i = t.minus(
              n.getElementPosition(this.viewer.element)
            );
            return this.viewerElementToImageCoordinates(i);
          },
          /**
           * Convert image coordinates to pixel coordinates relative to the window.
           * @param {OpenSeadragon.Point} pixel
           * @returns {OpenSeadragon.Point}
           */
          imageToWindowCoordinates: function(t) {
            return this.imageToViewerElementCoordinates(t).plus(
              n.getElementPosition(this.viewer.element)
            );
          },
          // private
          // Convert rectangle in viewport coordinates to this tiled image point
          // coordinates (x in [0, 1] and y in [0, aspectRatio])
          _viewportToTiledImageRectangle: function(t) {
            const i = this._scaleSpring.current.value;
            return t = t.rotate(-this.getRotation(!0), this._getRotationPoint(!0)), new e.Rect(
              (t.x - this._xSpring.current.value) / i,
              (t.y - this._ySpring.current.value) / i,
              t.width / i,
              t.height / i,
              t.degrees
            );
          },
          /**
           * Convert a viewport zoom to an image zoom.
           * Image zoom: ratio of the original image size to displayed image size.
           * 1 means original image size, 0.5 half size...
           * Viewport zoom: ratio of the displayed image's width to viewport's width.
           * 1 means identical width, 2 means image's width is twice the viewport's width...
           * @function
           * @param {Number} viewportZoom The viewport zoom
           * @returns {Number} imageZoom The image zoom
           */
          viewportToImageZoom: function(t) {
            return this._scaleSpring.current.value * this.viewport._containerInnerSize.x / this.source.dimensions.x * t;
          },
          /**
           * Convert an image zoom to a viewport zoom.
           * Image zoom: ratio of the original image size to displayed image size.
           * 1 means original image size, 0.5 half size...
           * Viewport zoom: ratio of the displayed image's width to viewport's width.
           * 1 means identical width, 2 means image's width is twice the viewport's width...
           * Note: not accurate with multi-image.
           * @function
           * @param {Number} imageZoom The image zoom
           * @returns {Number} viewportZoom The viewport zoom
           */
          imageToViewportZoom: function(t) {
            const i = this._scaleSpring.current.value * this.viewport._containerInnerSize.x / this.source.dimensions.x;
            return t / i;
          },
          /**
           * Sets the TiledImage's position in the world.
           * @param {OpenSeadragon.Point} position - The new position, in viewport coordinates.
           * @param {Boolean} [immediately=false] - Whether to animate to the new position or snap immediately.
           * @fires OpenSeadragon.TiledImage.event:bounds-change
           */
          setPosition: function(t, i) {
            const r = this._xSpring.target.value === t.x && this._ySpring.target.value === t.y;
            if (i) {
              if (r && this._xSpring.current.value === t.x && this._ySpring.current.value === t.y)
                return;
              this._xSpring.resetTo(t.x), this._ySpring.resetTo(t.y), this._needsDraw = !0, this._needsUpdate = !0;
            } else {
              if (r)
                return;
              this._xSpring.springTo(t.x), this._ySpring.springTo(t.y), this._needsDraw = !0, this._needsUpdate = !0;
            }
            r || this._raiseBoundsChange();
          },
          /**
           * Sets the TiledImage's width in the world, adjusting the height to match based on aspect ratio.
           * @param {Number} width - The new width, in viewport coordinates.
           * @param {Boolean} [immediately=false] - Whether to animate to the new size or snap immediately.
           * @fires OpenSeadragon.TiledImage.event:bounds-change
           */
          setWidth: function(t, i) {
            this._setScale(t, i);
          },
          /**
           * Sets the TiledImage's height in the world, adjusting the width to match based on aspect ratio.
           * @param {Number} height - The new height, in viewport coordinates.
           * @param {Boolean} [immediately=false] - Whether to animate to the new size or snap immediately.
           * @fires OpenSeadragon.TiledImage.event:bounds-change
           */
          setHeight: function(t, i) {
            this._setScale(t / this.normHeight, i);
          },
          /**
           * Sets an array of polygons to crop the TiledImage during draw tiles.
           * The render function will use the default non-zero winding rule.
           * @param {OpenSeadragon.Point[][]} polygons - represented in an array of point object in image coordinates.
           * Example format: [
           *  [{x: 197, y:172}, {x: 226, y:172}, {x: 226, y:198}, {x: 197, y:198}], // First polygon
           *  [{x: 328, y:200}, {x: 330, y:199}, {x: 332, y:201}, {x: 329, y:202}]  // Second polygon
           *  [{x: 321, y:201}, {x: 356, y:205}, {x: 341, y:250}] // Third polygon
           * ]
           */
          setCroppingPolygons: function(t) {
            const i = function(o) {
              return o instanceof e.Point || typeof o.x == "number" && typeof o.y == "number";
            }, r = function(o) {
              return o.map(function(l) {
                try {
                  if (i(l))
                    return { x: l.x, y: l.y };
                  throw new Error();
                } catch {
                  throw new Error("A Provided cropping polygon point is not supported");
                }
              });
            };
            try {
              if (!e.isArray(t))
                throw new Error("Provided cropping polygon is not an array");
              this._croppingPolygons = t.map(function(o) {
                return r(o);
              }), this._needsDraw = !0;
            } catch (o) {
              e.console.error("[TiledImage.setCroppingPolygons] Cropping polygon format not supported"), e.console.error(o), this.resetCroppingPolygons();
            }
          },
          /**
           * Resets the cropping polygons, thus next render will remove all cropping
           * polygon effects.
           */
          resetCroppingPolygons: function() {
            this._croppingPolygons = null, this._needsDraw = !0;
          },
          /**
           * Positions and scales the TiledImage to fit in the specified bounds.
           * Note: this method fires OpenSeadragon.TiledImage.event:bounds-change
           * twice
           * @param {OpenSeadragon.Rect} bounds The bounds to fit the image into.
           * @param {OpenSeadragon.Placement} [anchor=OpenSeadragon.Placement.CENTER]
           * How to anchor the image in the bounds.
           * @param {Boolean} [immediately=false] Whether to animate to the new size
           * or snap immediately.
           * @fires OpenSeadragon.TiledImage.event:bounds-change
           */
          fitBounds: function(t, i, r) {
            i = i || e.Placement.CENTER;
            const o = e.Placement.properties[i];
            let l = this.contentAspectX, a = 0, c = 0, d = 1, f = 1;
            if (this._clip && (l = this._clip.getAspectRatio(), d = this._clip.width / this.source.dimensions.x, f = this._clip.height / this.source.dimensions.y, t.getAspectRatio() > l ? (a = this._clip.x / this._clip.height * t.height, c = this._clip.y / this._clip.height * t.height) : (a = this._clip.x / this._clip.width * t.width, c = this._clip.y / this._clip.width * t.width)), t.getAspectRatio() > l) {
              const p = t.height / f;
              let b = 0;
              o.isHorizontallyCentered ? b = (t.width - t.height * l) / 2 : o.isRight && (b = t.width - t.height * l), this.setPosition(
                new e.Point(t.x - a + b, t.y - c),
                r
              ), this.setHeight(p, r);
            } else {
              const p = t.width / d;
              let b = 0;
              o.isVerticallyCentered ? b = (t.height - t.width / l) / 2 : o.isBottom && (b = t.height - t.width / l), this.setPosition(
                new e.Point(t.x - a, t.y - c + b),
                r
              ), this.setWidth(p, r);
            }
          },
          /**
           * @returns {OpenSeadragon.Rect|null} The TiledImage's current clip rectangle,
           * in image pixels, or null if none.
           */
          getClip: function() {
            return this._clip ? this._clip.clone() : null;
          },
          /**
           * @param {OpenSeadragon.Rect|null} newClip - An area, in image pixels, to clip to
           * (portions of the image outside of this area will not be visible). Only works on
           * browsers that support the HTML5 canvas.
           * @fires OpenSeadragon.TiledImage.event:clip-change
           */
          setClip: function(t) {
            e.console.assert(
              !t || t instanceof e.Rect,
              "[TiledImage.setClip] newClip must be an OpenSeadragon.Rect or null"
            ), t instanceof e.Rect ? this._clip = t.clone() : this._clip = null, this._needsUpdate = !0, this._needsDraw = !0, this.raiseEvent("clip-change");
          },
          /**
           * @returns {Boolean} Whether the TiledImage should be flipped before rendering.
           */
          getFlip: function() {
            return this.flipped;
          },
          /**
           * @param {Boolean} flip Whether the TiledImage should be flipped before rendering.
           * @fires OpenSeadragon.TiledImage.event:bounds-change
           */
          setFlip: function(t) {
            this.flipped = t;
          },
          get flipped() {
            return this._flipped;
          },
          set flipped(t) {
            const i = this._flipped !== !!t;
            this._flipped = !!t, i && this._initialized && (this.update(!0), this._needsDraw = !0, this._raiseBoundsChange());
          },
          get wrapHorizontal() {
            return this._wrapHorizontal;
          },
          set wrapHorizontal(t) {
            const i = this._wrapHorizontal !== !!t;
            this._wrapHorizontal = !!t, this._initialized && i && (this.update(!0), this._needsDraw = !0);
          },
          get wrapVertical() {
            return this._wrapVertical;
          },
          set wrapVertical(t) {
            const i = this._wrapVertical !== !!t;
            this._wrapVertical = !!t, this._initialized && i && (this.update(!0), this._needsDraw = !0);
          },
          get debugMode() {
            return this._debugMode;
          },
          set debugMode(t) {
            this._debugMode = !!t, this._needsDraw = !0;
          },
          /**
           * @returns {Number} The TiledImage's current opacity.
           */
          getOpacity: function() {
            return this.opacity;
          },
          /**
           * @param {Number} opacity Opacity the tiled image should be drawn at.
           * @fires OpenSeadragon.TiledImage.event:opacity-change
           */
          setOpacity: function(t) {
            this.opacity = t;
          },
          get opacity() {
            return this._opacity;
          },
          set opacity(t) {
            t !== this.opacity && (this._opacity = t, this._needsDraw = !0, this._needsUpdate = !0, this.raiseEvent("opacity-change", {
              opacity: this.opacity
            }));
          },
          /**
           * @returns {Boolean} whether the tiledImage can load its tiles even when it has zero opacity.
           */
          getPreload: function() {
            return this._preload;
          },
          /**
           * Set true to load even when hidden. Set false to block loading when hidden.
           */
          setPreload: function(t) {
            this._preload = !!t, this._needsDraw = !0;
          },
          /**
           * Get the rotation of this tiled image in degrees.
           * @param {Boolean} [current=false] True for current rotation, false for target.
           * @returns {Number} the rotation of this tiled image in degrees.
           */
          getRotation: function(t) {
            return t ? this._degreesSpring.current.value : this._degreesSpring.target.value;
          },
          /**
           * Set the current rotation of this tiled image in degrees.
           * @param {Number} degrees the rotation in degrees.
           * @param {Boolean} [immediately=false] Whether to animate to the new angle
           * or rotate immediately.
           * @fires OpenSeadragon.TiledImage.event:bounds-change
           */
          setRotation: function(t, i) {
            this._degreesSpring.target.value === t && this._degreesSpring.isAtTargetValue() || (i ? this._degreesSpring.resetTo(t) : this._degreesSpring.springTo(t), this._needsDraw = !0, this._needsUpdate = !0, this._raiseBoundsChange());
          },
          /**
           * Get the region of this tiled image that falls within the viewport.
           * @returns {OpenSeadragon.Rect} the region of this tiled image that falls within the viewport.
           * Returns false for images with opacity==0 unless preload==true
           */
          getDrawArea: function() {
            if (this._opacity === 0 && !this._preload)
              return !1;
            let t = this._viewportToTiledImageRectangle(
              this.viewport.getBoundsWithMargins(!0)
            );
            if (!this.wrapHorizontal && !this.wrapVertical) {
              const i = this._viewportToTiledImageRectangle(
                this.getClippedBounds(!0)
              );
              t = t.intersection(i);
            }
            return t;
          },
          getLoadArea: function() {
            let t = this._viewportToTiledImageRectangle(
              this.viewport.getBoundsWithMargins(!1)
            );
            if (!this.wrapHorizontal && !this.wrapVertical) {
              const i = this._viewportToTiledImageRectangle(
                this.getClippedBounds(!1)
              );
              t = t.intersection(i);
            }
            return t;
          },
          /**
           *
           * @returns {Array} Array of Tiles that make up the current view
           */
          getTilesToDraw: function() {
            const t = this._lastDrawn;
            let i = 0;
            for (const r of this._tilesToDraw)
              if (Array.isArray(r))
                for (const o of r)
                  t[i++] = o;
              else r && (t[i++] = r);
            t.length = i, this._updateTilesInViewport(t), i = 0;
            for (const r of this._tilesToDraw)
              if (Array.isArray(r))
                for (const o of r)
                  o.tile.beingDrawn = !0, t[i++] = o;
              else r && (r.tile.beingDrawn = !0, t[i++] = r);
            return t.length = i, t;
          },
          /**
           * Get the point around which this tiled image is rotated
           * @private
           * @param {Boolean} current True for current rotation point, false for target.
           * @returns {OpenSeadragon.Point}
           */
          _getRotationPoint: function(t) {
            return this.getBoundsNoRotate(t).getCenter();
          },
          get compositeOperation() {
            return this._compositeOperation;
          },
          set compositeOperation(t) {
            t !== this._compositeOperation && (this._compositeOperation = t, this._needsDraw = !0, this.raiseEvent("composite-operation-change", {
              compositeOperation: this._compositeOperation
            }));
          },
          /**
           * @returns {String} The TiledImage's current compositeOperation.
           */
          getCompositeOperation: function() {
            return this._compositeOperation;
          },
          /**
           * @param {String} compositeOperation the tiled image should be drawn with this globalCompositeOperation.
           * @fires OpenSeadragon.TiledImage.event:composite-operation-change
           */
          setCompositeOperation: function(t) {
            this.compositeOperation = t;
          },
          /**
           * Update headers to include when making AJAX requests.
           *
           * Unless `propagate` is set to false (which is likely only useful in rare circumstances),
           * the updated headers are propagated to all tiles and queued image loader jobs.
           *
           * Note that the rules for merging headers still apply, i.e. headers returned by
           * {@link OpenSeadragon.TileSource#getTileAjaxHeaders} take precedence over
           * the headers here in the tiled image (`TiledImage.ajaxHeaders`).
           *
           * @function
           * @param {Object} ajaxHeaders Updated AJAX headers, which will be merged over any headers specified in {@link OpenSeadragon.Options}.
           * @param {Boolean} [propagate=true] Whether to propagate updated headers to existing tiles and queued image loader jobs.
           */
          setAjaxHeaders: function(t, i) {
            if (t === null && (t = {}), !e.isPlainObject(t)) {
              e.console.error("[TiledImage.setAjaxHeaders] Ignoring invalid headers, must be a plain object");
              return;
            }
            this._ownAjaxHeaders = t, this._updateAjaxHeaders(i);
          },
          /**
           * Update headers to include when making AJAX requests.
           *
           * This function has the same effect as calling {@link OpenSeadragon.TiledImage#setAjaxHeaders},
           * except that the headers for this tiled image do not change. This is especially useful
           * for propagating updated headers from {@link OpenSeadragon.TileSource#getTileAjaxHeaders}
           * to existing tiles.
           *
           * @private
           * @function
           * @param {Boolean} [propagate=true] Whether to propagate updated headers to existing tiles and queued image loader jobs.
           */
          _updateAjaxHeaders: function(t) {
            if (t === void 0 && (t = !0), e.isPlainObject(this.viewer.ajaxHeaders) ? this.ajaxHeaders = e.extend({}, this.viewer.ajaxHeaders, this._ownAjaxHeaders) : this.ajaxHeaders = this._ownAjaxHeaders, t) {
              let i, r, o, l;
              for (const a in this.tilesMatrix) {
                i = this.source.getNumTiles(a);
                const c = this.tilesMatrix[a];
                for (const d in c) {
                  r = (i.x + d % i.x) % i.x;
                  for (const f in c[d])
                    if (o = (i.y + f % i.y) % i.y, l = c[d][f], l.loadWithAjax = this.loadTilesWithAjax, l.loadWithAjax) {
                      const p = this.source.getTileAjaxHeaders(a, r, o);
                      l.ajaxHeaders = e.extend({}, this.ajaxHeaders, p);
                    } else
                      l.ajaxHeaders = null;
                }
              }
              for (let a = 0; a < this._imageLoader.jobQueue.length; a++) {
                const c = this._imageLoader.jobQueue[a];
                c.loadWithAjax = c.tile.loadWithAjax, c.ajaxHeaders = c.tile.loadWithAjax ? c.tile.ajaxHeaders : null;
              }
            }
          },
          /**
           * Enable cache preservation even without this tile image,
           * by default disabled. It means that upon removing,
           * the tile cache does not get immediately erased but
           * stays in the memory to be potentially re-used by other
           * TiledImages.
           * @param {boolean} allow
           */
          allowZombieCache: function(t) {
            this._zombieCache = t;
          },
          // private
          _setScale: function(t, i) {
            const r = this._scaleSpring.target.value === t;
            if (i) {
              if (r && this._scaleSpring.current.value === t)
                return;
              this._scaleSpring.resetTo(t), this._updateForScale(), this._needsDraw = !0, this._needsUpdate = !0;
            } else {
              if (r)
                return;
              this._scaleSpring.springTo(t), this._updateForScale(), this._needsDraw = !0, this._needsUpdate = !0;
            }
            r || this._raiseBoundsChange();
          },
          // private
          _updateForScale: function() {
            this._worldWidthTarget = this._scaleSpring.target.value, this._worldHeightTarget = this.normHeight * this._scaleSpring.target.value, this._worldWidthCurrent = this._scaleSpring.current.value, this._worldHeightCurrent = this.normHeight * this._scaleSpring.current.value;
          },
          // private
          _raiseBoundsChange: function() {
            this.raiseEvent("bounds-change");
          },
          // private
          _isBottomItem: function() {
            return this.viewer.world.getItemAt(0) === this;
          },
          // private
          _getLevelsInterval: function() {
            let t = Math.max(
              this.source.minLevel,
              Math.floor(Math.log(this.minZoomImageRatio) / Math.log(2))
            );
            const i = this.viewport.deltaPixelsFromPointsNoRotate(
              this.source.getPixelRatio(0),
              !0
            ).x * this._scaleSpring.current.value;
            let r = Math.min(
              Math.abs(this.source.maxLevel),
              Math.abs(Math.floor(
                Math.log(i / this.minPixelRatio) / Math.log(2)
              ))
            );
            return r = Math.max(r, this.source.minLevel || 0), t = Math.min(t, r), {
              lowestLevel: t,
              highestLevel: r
            };
          },
          // returns boolean flag of whether the image should be marked as fully loaded
          _updateLevelsForViewport: function() {
            const t = this._getLevelsInterval(), i = t.lowestLevel, r = t.highestLevel, o = this.getDrawArea();
            let l = o, a = this._getCachedArray("bestLoadTileCandidates", 0);
            this.loadDestinationTilesOnAnimation && (l = this.getLoadArea());
            const c = e.now();
            for (const p of this._lastDrawn)
              p.tile.beingDrawn = !1;
            if (this._tilesToDraw.length = 0, this._tilesLoading = 0, this.loadingCoverage = {}, !o)
              return this._needsDraw = !1, this._fullyLoaded;
            const d = this._getCachedArray("levelList", r - i + 1);
            for (let p = 0, b = r; b >= i; b--, p++)
              d[p] = b;
            for (let p = r + 1; p <= this.source.maxLevel; p++) {
              const b = this.tilesMatrix[p] && this.tilesMatrix[p][0] && this.tilesMatrix[p][0][0];
              if (b && b.isBottomMost && b.isRightMost && b.loaded) {
                d.push(p);
                break;
              }
            }
            let f = !1;
            for (let p = 0; p < d.length; p++) {
              const b = d[p], x = this.viewport.deltaPixelsFromPointsNoRotate(
                this.source.getPixelRatio(b),
                !0
              ).x * this._scaleSpring.current.value;
              if (p === d.length - 1 || x >= this.minPixelRatio)
                f = !0;
              else if (!f)
                continue;
              const T = this.viewport.deltaPixelsFromPointsNoRotate(
                this.source.getPixelRatio(b),
                !1
              ).x * this._scaleSpring.current.value, I = this.viewport.deltaPixelsFromPointsNoRotate(
                this.source.getPixelRatio(
                  Math.max(
                    this.source.getClosestLevel(),
                    0
                  )
                ),
                !1
              ).x * this._scaleSpring.current.value, z = this.immediateRender ? 1 : I, W = Math.min(1, (x - 0.5) / 0.5), j = z / Math.abs(
                z - T
              ), oe = this._updateLevel(
                b,
                W,
                j,
                o,
                l,
                c,
                a
              );
              if (a = oe.bestLoadTileCandidates, this._tilesToDraw[b] = oe.tilesToDraw, this._providesCoverage(this.coverage, b))
                break;
            }
            return a && a.length > 0 ? (setTimeout(() => {
              for (const p of a)
                p && this._loadTile(p, c);
            }), this._needsDraw = !0, !1) : this._tilesLoading === 0;
          },
          /**
           * Update all tiles that contribute to the current view
           * @private
           *
           */
          _updateTilesInViewport: function(t) {
            const i = e.now(), r = this;
            this._tilesLoading = 0, this._wasBlending = this._isBlending, this._isBlending = !1, this.loadingCoverage = {};
            const o = t.length ? t[0].level : 0;
            if (!this.getDrawArea())
              return;
            let a = 0;
            for (const c of t) {
              const d = c.tile;
              if (d && d.loaded) {
                const f = r._blendTile(
                  d,
                  d.x,
                  d.y,
                  c.level,
                  c.levelOpacity,
                  i,
                  o
                );
                r._isBlending = r._isBlending || f, r._needsDraw = r._needsDraw || f || r._wasBlending;
              }
              this._providesCoverage(this.coverage, c.level) && (a = Math.max(a, c.level));
            }
            if (a > 0)
              for (const c in this._tilesToDraw)
                c < a && (this._tilesToDraw[c] = void 0);
          },
          /**
           * Updates the opacity of a tile according to the time it has been on screen
           * to perform a fade-in.
           * Updates coverage once a tile is fully opaque.
           * Returns whether the fade-in has completed.
           * @private
           *
           * @param {OpenSeadragon.Tile} tile
           * @param {Number} x
           * @param {Number} y
           * @param {Number} level
           * @param {Number} levelOpacity
           * @param {Number} currentTime
           * @param {Boolean} lowestLevel
           * @returns {Boolean} true if blending did not yet finish
           */
          _blendTile: function(t, i, r, o, l, a, c) {
            let d = 1e3 * this.blendTime, f, p;
            return t.blendStart || (t.blendStart = a), f = a - t.blendStart, p = d ? Math.min(1, f / d) : 1, o === c && (p = 1, f = d), this.alwaysBlend && (p *= l), t.opacity = p, p === 1 && (this._setCoverage(this.coverage, o, i, r, !0), this._hasOpaqueTile = !0), f < d;
          },
          /**
           * Updates all tiles at a given resolution level.
           * @private
           * @param {Number} level
           * @param {Number} levelOpacity
           * @param {Number} levelVisibility
           * @param {OpenSeadragon.Rect} drawArea
           * @param {OpenSeadragon.Rect} loadArea
           * @param {Number} currentTime
           * @param {OpenSeadragon.Tile[]} bestLoadTileCandidates Array of the current best tiles
           * @returns {Object} Dictionary {
           *    bestLoadTileCandidates: OpenSeadragon.Tile - the current "best" tiles to draw,
           *    tilesToDraw: OpenSeadragon.Tile) - the updated tiles
           * }
           */
          _updateLevel: function(t, i, r, o, l, a, c) {
            const d = o.getBoundingBox().getTopLeft(), f = o.getBoundingBox().getBottomRight();
            this.viewer && this.viewer.raiseEvent("update-level", {
              tiledImage: this,
              havedrawn: !0,
              // deprecated, kept for backwards compatibility
              level: t,
              opacity: i,
              visibility: r,
              drawArea: o,
              topleft: d,
              bottomright: f,
              currenttime: a,
              // todo misleading name, consider changing
              best: c
            });
            const p = this.source.getNumTiles(t), b = this.viewport.pixelFromPoint(this.viewport.getCenter());
            this._resetCoverage(this.coverage, t), l && this._resetCoverage(this.loadingCoverage, t);
            let x = null, T = 0;
            return this._visitTiles(t, o, (I, z, W) => {
              const j = this._getTile(
                I,
                z,
                t,
                a,
                p
              );
              if (x || (x = this._getCachedArray(t, W)), this.viewer && this.viewer.raiseEvent("update-tile", {
                tiledImage: this,
                tile: j
              }), this._setCoverage(this.coverage, t, I, z, !1), j.exists && (j.loaded && j.opacity === 1 && this._setCoverage(this.coverage, t, I, z, !0), this._positionTile(
                j,
                this.source.tileOverlap,
                this.viewport,
                b,
                r
              ), j.loaded && (x[T++] = {
                tile: j,
                level: t,
                levelOpacity: i,
                currentTime: a
              })), l) {
                let oe = j.loaded || j.loading || this._isCovered(this.loadingCoverage, t, I, z);
                if (this._setCoverage(this.loadingCoverage, t, I, z, oe), !j.exists)
                  return;
                !j.loaded && !j.loading && this._tryFindTileCacheRecord(j) && (oe = !0), j.loading ? this._tilesLoading++ : oe || (c = this._compareTiles(c, j, this._currentMaxTilesPerFrame));
              }
            }), this._currentMaxTilesPerFrame > this.maxTilesPerFrame && (this._currentMaxTilesPerFrame = Math.max(Math.ceil(this._currentMaxTilesPerFrame / 2), this.maxTilesPerFrame)), x && (x.length = T), {
              bestLoadTileCandidates: c,
              tilesToDraw: x || []
            };
          },
          /**
           * Visit all tiles in an a given area on a given level.
           * @private
           * @param {Number} level
           * @param {OpenSeadragon.Rect} area
           * @param {Function} callback - x, y, total - tile x, y position and total number of tiles
           */
          _visitTiles: function(t, i, r) {
            const o = i.getBoundingBox(), l = this._getCornerTiles(t, o.getTopLeft(), o.getBottomRight()), a = l.topLeft, c = l.bottomRight, d = this.source.getNumTiles(t);
            this.getFlip() && (c.x += 1, this.wrapHorizontal || (c.x = Math.min(c.x, d.x - 1)));
            const f = Math.max(0, (c.x - a.x) * (c.y - a.y));
            for (let p = a.x; p <= c.x; p++)
              for (let b = a.y; b <= c.y; b++) {
                let x;
                if (this.getFlip()) {
                  const T = (d.x + p % d.x) % d.x;
                  x = p + d.x - T - T - 1;
                } else
                  x = p;
                i.intersection(this.getTileBounds(t, x, b)) !== null && r(x, b, f);
              }
          },
          /**
           * @private
           * @param {OpenSeadragon.Tile} tile
           * @param {Boolean} overlap
           * @param {OpenSeadragon.Viewport} viewport
           * @param {OpenSeadragon.Point} viewportCenter
           * @param {Number} levelVisibility
           */
          _positionTile: function(t, i, r, o, l) {
            const a = t.bounds.getTopLeft();
            a.x *= this._scaleSpring.current.value, a.y *= this._scaleSpring.current.value, a.x += this._xSpring.current.value, a.y += this._ySpring.current.value;
            const c = t.bounds.getSize();
            c.x *= this._scaleSpring.current.value, c.y *= this._scaleSpring.current.value, t.positionedBounds.x = a.x, t.positionedBounds.y = a.y, t.positionedBounds.width = c.x, t.positionedBounds.height = c.y;
            const d = r.pixelFromPointNoRotate(a, !0), f = r.pixelFromPointNoRotate(a, !1);
            let p = r.deltaPixelsFromPointsNoRotate(c, !0);
            const b = r.deltaPixelsFromPointsNoRotate(c, !1), x = f.plus(b.divide(2)), T = o.squaredDistanceTo(x);
            this.viewer.drawer.minimumOverlapRequired(this) && (i || (p = p.plus(new e.Point(1, 1))), t.isRightMost && this.wrapHorizontal && (p.x += 0.75), t.isBottomMost && this.wrapVertical && (p.y += 0.75)), t.position = d, t.size = p, t.squaredDistance = T, t.visibility = l;
          },
          // private
          _getCornerTiles: function(t, i, r) {
            let o, l;
            this.wrapHorizontal ? (o = e.positiveModulo(i.x, 1), l = e.positiveModulo(r.x, 1)) : (o = Math.max(0, i.x), l = Math.min(1, r.x));
            let a, c;
            const d = 1 / this.source.aspectRatio;
            this.wrapVertical ? (a = e.positiveModulo(i.y, d), c = e.positiveModulo(r.y, d)) : (a = Math.max(0, i.y), c = Math.min(d, r.y));
            const f = this.source.getTileAtPoint(t, new e.Point(o, a)), p = this.source.getTileAtPoint(t, new e.Point(l, c)), b = this.source.getNumTiles(t);
            return this.wrapHorizontal && (f.x += b.x * Math.floor(i.x), p.x += b.x * Math.floor(r.x)), this.wrapVertical && (f.y += b.y * Math.floor(i.y / d), p.y += b.y * Math.floor(r.y / d)), {
              topLeft: f,
              bottomRight: p
            };
          },
          /**
           * @private
           * @inner
           * Try to find existing cache of the tile
           * @param {OpenSeadragon.Tile} tile
           */
          _tryFindTileCacheRecord: function(t) {
            const i = this._tileCache.getCacheRecord(t.originalCacheKey);
            return i ? (t.loading = !0, this._setTileLoaded(t, i.data, null, null, i.type), !0) : !1;
          },
          /**
           * @private
           * @inner
           * Obtains a tile at the given location.
           * @private
           * @param {Number} x
           * @param {Number} y
           * @param {Number} level
           * @param {Number} time
           * @param {Number} numTiles
           * @returns {OpenSeadragon.Tile}
           */
          _getTile: function(t, i, r, o, l) {
            let a, c, d, f, p, b, x, T, I, z = this.tilesMatrix, W = this.source, j = z[r];
            return j || (z[r] = j = {}), j[t] || (j[t] = {}), !j[t][i] || !j[t][i].flipped != !this.flipped ? (a = (l.x + t % l.x) % l.x, c = (l.y + i % l.y) % l.y, d = this.getTileBounds(r, t, i), f = W.getTileBounds(r, a, c, !0), p = W.tileExists(r, a, c), b = W.getTileUrl(r, a, c), x = W.getTilePostData(r, a, c), this.loadTilesWithAjax ? (T = W.getTileAjaxHeaders(r, a, c), e.isPlainObject(this.ajaxHeaders) && (T = e.extend({}, this.ajaxHeaders, T))) : T = null, I = new e.Tile(
              r,
              t,
              i,
              d,
              p,
              b,
              void 0,
              this.loadTilesWithAjax,
              T,
              f,
              x,
              W.getTileHashKey(r, a, c, b, T, x)
            ), this.getFlip() ? a === 0 && (I.isRightMost = !0) : a === l.x - 1 && (I.isRightMost = !0), c === l.y - 1 && (I.isBottomMost = !0), I.flipped = this.flipped, j[t][i] = I) : I = j[t][i], I.lastTouchTime = o, I;
          },
          /**
           * Dispatch a job to the ImageLoader to load the Image for a Tile.
           * @private
           * @param {OpenSeadragon.Tile} tile
           * @param {Number} time
           */
          _loadTile: function(t, i) {
            const r = this;
            t.loading = !0, t.tiledImage = this, this._imageLoader.addJob({
              src: t.getUrl(),
              tile: t,
              source: this.source,
              postData: t.postData,
              loadWithAjax: t.loadWithAjax,
              ajaxHeaders: t.ajaxHeaders,
              crossOriginPolicy: this.crossOriginPolicy,
              ajaxWithCredentials: this.ajaxWithCredentials,
              callback: function(o, l, a, c, d) {
                r._onTileLoad(t, i, o, l, a, c, d);
              },
              abort: function() {
                t.loading = !1;
              }
            }) || this.viewer.raiseEvent("job-queue-full", {
              tile: t,
              tiledImage: this,
              time: i
            });
          },
          /**
           * Callback fired when a Tile's Image finished downloading.
           * @private
           * @param {OpenSeadragon.Tile} tile
           * @param {Number} time
           * @param {*} data image data
           * @param {String} errorMsg
           * @param {XMLHttpRequest} tileRequest
           * @param {String} [dataType=undefined] data type, derived automatically if not set
           * @param {number} tries - The number of times the tile has been retried.
           */
          _onTileLoad: function(t, i, r, o, l, a, c) {
            if (r == null) {
              e.console.error("Tile %s failed to load: %s - error: %s", t, t.getUrl(), o), this.viewer.raiseEvent("tile-load-failed", {
                tile: t,
                tiledImage: this,
                time: i,
                message: o,
                tileRequest: l,
                tries: c,
                maxReached: this.viewer.tileRetryMax === 0 ? !0 : c >= this.viewer.tileRetryMax
              }), t.loading = !1, t.exists = !1;
              return;
            } else
              t.exists = !0;
            if (i < this.lastResetTime) {
              e.console.warn("Ignoring tile %s loaded before reset: %s", t, t.getUrl()), t.loading = !1;
              return;
            }
            this._setTileLoaded(t, r, null, l, a);
          },
          /**
           * @private
           * @param {OpenSeadragon.Tile} tile
           * @param {*} data image data, the data sent to ImageJob.prototype.finish(), by default an Image object,
           *   can be null: in that case, cache is assigned to a tile without further processing
           * @param {?Number} cutoff ignored, @deprecated
           * @param {?XMLHttpRequest} tileRequest
           * @param {?String} [dataType=undefined] data type, derived automatically if not set
           */
          _setTileLoaded: function(t, i, r, o, l) {
            t.tiledImage = this, e.console.assert(l !== void 0, "TileSource::downloadTileStart must return a dataType.");
            let a = !1;
            t.addCache(t.cacheKey, () => (a = !0, i), l, !1, !1);
            let c = null, d = 0, f = !1;
            const p = this, b = e.now();
            function x() {
              d--, !(d > 0) && (f = !0, t.hasTransparency = t.hasTransparency || p.source.hasTransparency(
                void 0,
                t.getUrl(),
                t.ajaxHeaders,
                t.postData
              ), t.loading = !1, t.loaded = !0, p.redraw(), c(t));
            }
            function T() {
              return f && e.console.error("Event 'tile-loaded' argument getCompletionCallback must be called synchronously. Its return value should be called asynchronously."), d++, x;
            }
            function I() {
              const z = T();
              p.viewer.raiseEventAwaiting("tile-loaded", {
                tile: t,
                tiledImage: p,
                tileRequest: o,
                promise: new e.Promise((W) => {
                  c = W;
                }),
                get image() {
                  return e.console.error("[tile-loaded] event 'image' has been deprecated. Use 'tile-invalidated' event to modify data instead."), i;
                },
                get data() {
                  return e.console.error("[tile-loaded] event 'data' has been deprecated. Use 'tile-invalidated' event to modify data instead."), i;
                },
                getCompletionCallback: function() {
                  return e.console.error("[tile-loaded] getCompletionCallback is deprecated: it introduces race conditions: use async event handlers instead, execution order is deducted by addHandler(...) priority argument."), T();
                }
              }).catch(() => {
                e.console.error("[tile-loaded] event finished with failure: there might be a problem with a plugin you are using.");
              }).then(z);
            }
            if (a)
              p.viewer.world.requestTileInvalidateEvent([t], b, !1, !0).then(I);
            else {
              const z = t.getCache(t.originalCacheKey);
              for (const W of z._tiles)
                if (W.cacheKey !== t.cacheKey) {
                  const j = W.getCache();
                  t.setCache(W.cacheKey, j, !0, !1);
                  break;
                } else if (W.processing) {
                  W.processingPromise.then((j) => {
                    const oe = j.getCache();
                    t.setCache(j.cacheKey, oe, !0, !1), I();
                  });
                  return;
                }
              I();
            }
          },
          /**
           * Determines the 'best tiles' from the given 'last best' tiles and the
           * tile in question. Keeps the best tiles sorted according to visibility and distance.
           * @private
           *
           * @param {OpenSeadragon.Tile[]} previousBest The best tiles so far. Must be sorted. If not,
           *    call previousBest.sort(this._sortTilesComparator) first.
           * @param {OpenSeadragon.Tile} tile The new tile to consider.
           * @param {Number} maxNTiles The max number of best tiles.
           * @returns {OpenSeadragon.Tile[]} The new best tiles.
           */
          _compareTiles: function(t, i, r) {
            if (!t)
              return [i];
            let o = !1;
            for (let l = 0; l < t.length; l++) {
              const a = t[l];
              if (this._sortTilesComparator(a, i) > 0) {
                t.splice(l, 0, i), o = !0;
                break;
              }
            }
            return o || t.push(i), t.length > r && t.pop(), t;
          },
          /**
           * Comparator for tile sorting according to visibility and distance.
           * @private
           *
           * @param {OpenSeadragon.Tile} a The tile a.
           * @param {OpenSeadragon.Tile} b The tile b.
           */
          _sortTilesComparator: function(t, i) {
            return t === null ? 1 : i === null ? -1 : t.visibility === i.visibility ? t.squaredDistance - i.squaredDistance : i.visibility - t.visibility;
          },
          /**
           * To avoid repeatedly creating arrays for each frame, we cache them.
           * @param {any} key
           * @param {Number} [length=undefined]
           * @private
           */
          _getCachedArray: function(t, i = void 0) {
            let r = this._arrayCacheMap[t];
            return r ? i !== void 0 && (r.length = i) : i !== void 0 ? r = this._arrayCacheMap[t] = new Array(i) : r = this._arrayCacheMap[t] = [], r;
          },
          /**
           * Returns true if the given tile provides coverage to lower-level tiles of
           * lower resolution representing the same content. If neither x nor y is
           * given, returns true if the entire visible level provides coverage.
           *
           * Note that out-of-bounds tiles provide coverage in this sense, since
           * there's no content that they would need to cover. Tiles at non-existent
           * levels that are within the image bounds, however, do not.
           * @private
           *
           * @param {Object} coverage - A '3d' dictionary [level][x][y] --> Boolean.
           * @param {Number} level - The resolution level of the tile.
           * @param {Number} x - The X position of the tile.
           * @param {Number} y - The Y position of the tile.
           * @returns {Boolean}
           */
          _providesCoverage: function(t, i, r, o) {
            let l, a, c, d;
            if (!t[i])
              return !1;
            if (r === void 0 || o === void 0) {
              l = t[i];
              for (c in l)
                if (Object.prototype.hasOwnProperty.call(l, c)) {
                  a = l[c];
                  for (d in a)
                    if (Object.prototype.hasOwnProperty.call(a, d) && !a[d])
                      return !1;
                }
              return !0;
            }
            return t[i][r] === void 0 || t[i][r][o] === void 0 || t[i][r][o] === !0;
          },
          /**
           * Returns true if the given tile is completely covered by higher-level
           * tiles of higher resolution representing the same content. If neither x
           * nor y is given, returns true if the entire visible level is covered.
           * @private
           *
           * @param {Object} coverage - A '3d' dictionary [level][x][y] --> Boolean.
           * @param {Number} level - The resolution level of the tile.
           * @param {Number} x - The X position of the tile.
           * @param {Number} y - The Y position of the tile.
           * @returns {Boolean}
           */
          _isCovered: function(t, i, r, o) {
            return r === void 0 || o === void 0 ? this._providesCoverage(t, i + 1) : this._providesCoverage(t, i + 1, 2 * r, 2 * o) && this._providesCoverage(t, i + 1, 2 * r, 2 * o + 1) && this._providesCoverage(t, i + 1, 2 * r + 1, 2 * o) && this._providesCoverage(t, i + 1, 2 * r + 1, 2 * o + 1);
          },
          /**
           * Sets whether the given tile provides coverage or not.
           * @private
           *
           * @param {Object} coverage - A '3d' dictionary [level][x][y] --> Boolean.
           * @param {Number} level - The resolution level of the tile.
           * @param {Number} x - The X position of the tile.
           * @param {Number} y - The Y position of the tile.
           * @param {Boolean} covers - Whether the tile provides coverage.
           */
          _setCoverage: function(t, i, r, o, l) {
            if (!t[i]) {
              e.console.warn(
                "Setting coverage for a tile before its level's coverage has been reset: %s",
                i
              );
              return;
            }
            t[i][r] || (t[i][r] = {}), t[i][r][o] = l;
          },
          /**
           * Resets coverage information for the given level. This should be called
           * after every draw routine. Note that at the beginning of the next draw
           * routine, coverage for every visible tile should be explicitly set.
           * @private
           *
           * @param {Object} coverage - A '3d' dictionary [level][x][y] --> Boolean.
           * @param {Number} level - The resolution level of tiles to completely reset.
           */
          _resetCoverage: function(t, i) {
            t[i] = {};
          }
        }
      );
    })(n), (function(e) {
      const t = Symbol("DRAWER_INTERNAL_CACHE");
      e.CacheRecord = class {
        constructor() {
          this.revive();
        }
        /**
         * Access the cache record data directly. Preferred way of data access.
         * Might be undefined if this.loaded = false.
         * You can access the data in synchronous way, but the data might not be available.
         * If you want to access the data indirectly (await), use this.transformTo or this.getDataAs
         * @returns {any}
         */
        get data() {
          return this._data;
        }
        /**
         * Read the cache type. The type can dynamically change, but should be consistent at
         * one point in the time. For available types see the OpenSeadragon.Converter, or the tutorials.
         * @returns {string}
         */
        get type() {
          return this._type;
        }
        /**
         * Await ongoing process so that we get cache ready on callback.
         * @returns {OpenSeadragon.Promise<?>}
         */
        await() {
          return this._promise ? this._promise : e.Promise.resolve(this._data);
        }
        getImage() {
          return e.console.error("[CacheRecord.getImage] options.image is deprecated. Moreover, it might not work correctly as the cache system performs conversion asynchronously in case the type needs to be converted."), this.transformTo("image"), this.data;
        }
        getRenderedContext() {
          return e.console.error("[CacheRecord.getRenderedContext] options.getRenderedContext  is deprecated. Moreover, it might not work correctly as the cache system performs conversion asynchronously in case the type needs to be converted."), this.transformTo("context2d"), this.data;
        }
        /**
         * Set the cache data. Asynchronous.
         * @param {any} data
         * @param {string} type
         * @returns {OpenSeadragon.Promise<?>} the old cache data that has been overwritten
         */
        setDataAs(i, r) {
          if (e.console.assert(i != null, "[CacheRecord.setDataAs] needs valid data to set!"), this._conversionJobQueue) {
            let o = null;
            const l = new e.Promise((a, c) => {
              o = a;
            });
            return this._conversionJobQueue.push(() => o(this._overwriteData(i, r))), l;
          }
          return this._overwriteData(i, r);
        }
        /**
         * Access the cache record data indirectly. Preferred way of data access. Asynchronous.
         * @param {string} [type=undefined]
         * @param {boolean} [copy=true] if false and same type is retrieved as the cache type,
         *  copy is not performed: note that this is potentially dangerous as it might
         *  introduce race conditions (you get a cache data direct reference you modify).
         * @returns {OpenSeadragon.Promise<?>} desired data type in promise, undefined if the cache was destroyed
         */
        getDataAs(i = void 0, r = !0) {
          return this.loaded ? i === this._type ? r ? e.converter.copy(this._tRef, this._data, i || this._type) : this._promise : this._transformDataIfNeeded(this._tRef, this._data, i || this._type, r) || this._promise : this._promise.then((o) => this._transformDataIfNeeded(this._tRef, o, i || this._type, r) || o);
        }
        _transformDataIfNeeded(i, r, o, l) {
          if (this._destroyed)
            return e.Promise.resolve();
          let a;
          return o !== this._type ? a = e.converter.convert(i, r, this._type, o) : l && (a = e.converter.copy(i, r, o)), a ? a.then((c) => {
            if (this._destroyed) {
              e.converter.destroy(c, o);
              return;
            }
            return c;
          }) : !1;
        }
        /**
         * Access of the data by drawers, synchronous function. Should always access a valid main cache.
         * This is ensured by invalidation routine that executes data modification on a copy record, and
         * then synchronously swaps records (main caches) to the new data between render calls.
         *
         * If a drawer decides to have internal cache with synchronous behavior, it is (if necessary)
         * performed during this phase.
         *
         * @param {OpenSeadragon.DrawerBase} drawer drawer reference which requests the data: the drawer
         *   defines the supported formats this cache should return **synchronously**
         * @param {OpenSeadragon.Tile} tileToDraw reference to the tile that is in the process of drawing and
         *   for which we request the data; if we attempt to draw such tile while main cache target is destroyed,
         *   attempt to reset the tile state to force system to re-download it again
         * @returns {OpenSeadragon.CacheRecord|OpenSeadragon.InternalCacheRecord|undefined} desired data if available,
         *   wrapped in the cache container. This data is guaranteed to be loaded & in the type supported by the drawer.
         *   Returns undefined if the data is not ready for rendering.
         * @private
         */
        getDataForRendering(i, r) {
          const o = i.options.usePrivateCache;
          if (!this.loaded) {
            e.console.error("Attempt to draw tile when not loaded main cache!");
            return;
          }
          if (this._destroyed) {
            e.console.error("Attempt to draw tile with destroyed main cache!"), r._unload();
            return;
          }
          if (!i.getSupportedDataFormats().includes(this.type)) {
            e.console.error("Attempt to draw tile with unsupported type for the target drawer!"), this.prepareForRendering(i);
            return;
          }
          if (o) {
            if (!i.options.preloadCache)
              return this.prepareInternalCacheSync(i);
            const a = this._getInternalCacheRef(i);
            if (!a || !a.loaded) {
              e.console.error("Attempt to draw tile with internal cache non-ready state!");
              return;
            }
            return a;
          }
          return this;
        }
        /**
         * Preparation for rendering ensures the CacheRecord is in a format supported by the current
         * drawer. Furthermore, if internal cache is to be used by a drawer with preloading enabled,
         * it happens in this step.
         *
         * Note: Should not be called if cache type is already among supported types.
         * @private
         * @param {OpenSeadragon.DrawerBase} drawer
         * @return {OpenSeadragon.Promise<*>} reference to the data,
         *  or null if not data yet loaded/ready (usually due to error)
         */
        prepareForRendering(i) {
          const r = i.getRequiredDataFormats();
          if (!this.loaded)
            return this.await().then((l) => this.prepareForRendering(i));
          let o;
          return r.includes(this.type) ? o = this.await() : o = this.transformTo(r), i.options.usePrivateCache && i.options.preloadCache ? o.then((l) => this.prepareInternalCacheAsync(i)) : o;
        }
        /**
         * Internal cache is defined by a Drawer. Async preparation happens as the last step in the
         * invalidation routine.
         * Must not be called if drawer.options.usePrivateCache == false. Called inside prepareForRenderine
         * by cache itself if preloadCache == true (supports async behavior).
         *
         * @private
         * @param {OpenSeadragon.DrawerBase} drawer
         * @return {OpenSeadragon.Promise<*>} reference to the data wrapped in a promise,
         *  or null if not data yet loaded/ready (usually due to error)
         */
        prepareInternalCacheAsync(i) {
          let r = this._getInternalCacheRef(i);
          if (this._checkInternalCacheUpToDate(r, i))
            return r.await();
          r && !r.loaded && r.await().then(() => r.destroy()), e.console.assert(this._tRef, "Data Create called from invalidation routine needs tile reference!");
          const o = i.internalCacheCreate(this, this._tRef);
          e.console.assert(o !== void 0, "[DrawerBase.internalCacheCreate] must return a value if usePrivateCache is enabled!");
          const l = i.getId();
          return r = this[t][l] = new e.InternalCacheRecord(
            o,
            l,
            (a) => i.internalCacheFree(a)
          ), r.await();
        }
        /**
         * Internal cache is defined by a Drawer. Sync preparation happens directly before rendering.
         * Must not be called if drawer.options.usePrivateCache == false. Called inside getDataForRendering
         * by cache itself if preloadCache == false (without support for async behavior).
         * @private
         * @param {OpenSeadragon.DrawerBase} drawer
         * @return {OpenSeadragon.InternalCacheRecord} reference to the cache
         */
        prepareInternalCacheSync(i) {
          let r = this._getInternalCacheRef(i);
          if (this._checkInternalCacheUpToDate(r, i))
            return r;
          r && r.destroy(), e.console.assert(this._tRef, "Data Create called from drawing loop needs tile reference!");
          const o = i.internalCacheCreate(this, this._tRef);
          e.console.assert(o !== void 0, "[DrawerBase.internalCacheCreate] must return a value if usePrivateCache is enabled!");
          const l = i.getId();
          return r = this[t][l] = new e.InternalCacheRecord(
            o,
            l,
            (a) => i.internalCacheFree(a)
          ), r;
        }
        /**
         * Get an internal cache reference for given drawer
         * @param {OpenSeadragon.DrawerBase} drawer
         * @return {OpenSeadragon.InternalCacheRecord|undefined}
         * @private
         */
        _getInternalCacheRef(i) {
          if (!i.options.usePrivateCache) {
            e.console.error("[CacheRecord.prepareInternalCacheSync] must not be called when usePrivateCache is false.");
            return;
          }
          let o = this[t];
          return o || (o = this[t] = {}), o[i.getId()];
        }
        /**
         * @param {OpenSeadragon.InternalCacheRecord} internalCache
         * @param {OpenSeadragon.DrawerBase} drawer
         * @return {boolean} false if the internal cache is outdated
         * @private
         */
        _checkInternalCacheUpToDate(i, r) {
          return i && i.loaded && i.tstamp >= r._dataNeedsRefresh;
        }
        /**
         * Transform cache to desired type and get the data after conversion.
         * Does nothing if the type equals to the current type. Asynchronous.
         * Transformation is LAZY, meaning conversions are performed only to
         * match the last conversion request target type.
         * @param {string|string[]} type if array provided, the system will
         *   try to optimize for the best type to convert to.
         * @return {OpenSeadragon.Promise<?>}
         */
        transformTo(i = this._type) {
          if (!this.loaded) {
            this._conversionJobQueue = this._conversionJobQueue || [];
            let r = null;
            const o = new e.Promise((l, a) => {
              r = l;
            });
            return this._conversionJobQueue.push(() => {
              this._destroyed || (typeof i == "string" && i !== this._type || Array.isArray(i) && !i.includes(this._type) ? (this._convert(this._type, i), this._promise.then((l) => r(l))) : this._promise.then((l) => (this._checkAwaitsConvert(), r(l))));
            }), o;
          }
          return (typeof i == "string" && i !== this._type || Array.isArray(i) && !i.includes(this._type)) && this._convert(this._type, i), this._promise;
        }
        /**
         * If cache ceases to be the primary one, free data
         * @param {string} drawerId if undefined, all caches are freed, else only target one
         * @private
         */
        destroyInternalCache(i = void 0) {
          const r = this[t];
          if (r)
            if (i) {
              const o = r[i];
              o && (o.destroy(), delete r[i]);
            } else {
              for (const o in r)
                r[o].destroy();
              delete this[t];
            }
        }
        /**
         * Conversion requires tile references:
         * keep the most 'up to date' ref here. It is called and managed automatically.
         * @param {OpenSeadragon.Tile} ref
         * @return {OpenSeadragon.CacheRecord} self reference for builder pattern
         * @private
         */
        withTileReference(i) {
          return this._tRef = i, this;
        }
        /**
         * Get cache description. Used for system messages and errors.
         * @return {string}
         */
        toString() {
          const i = this._tRef || this._tiles.length && this._tiles[0];
          return i ? `Cache ${this.type} [used e.g. by ${i.toString()}]` : "Orphan cache!";
        }
        /**
         * Set initial state, prepare for usage.
         * Must not be called on active cache, e.g. first call destroy().
         */
        revive() {
          e.console.assert(!this.loaded && !this._type, "[CacheRecord::revive] must not be called when loaded!"), this._tiles = [], this._data = null, this._type = null, this.loaded = !1, this._promise = null, this._destroyed = !1;
        }
        /**
         * Free all the data and call data destructors if defined.
         */
        destroy() {
          if (!this._destroyed) {
            if (delete this._conversionJobQueue, this._destroyed = !0, this.loaded)
              this._destroySelfUnsafe(this._data, this._type);
            else if (this._promise) {
              const i = this._type;
              this._promise.then((r) => this._destroySelfUnsafe(r, i)).catch(e.console.error);
            }
          }
        }
        _destroySelfUnsafe(i, r) {
          e.converter.destroy(i, r), this.destroyInternalCache(), this._destroyed && (this.loaded = !1, this._tiles = null, this._data = null, this._type = null, this._tRef = null, this._promise = null);
        }
        /**
         * Add tile dependency on this record
         * @param tile
         * @param data can be null|undefined => optimization, will skip data initialization and just adds tile reference
         * @param type
         */
        addTile(i, r, o) {
          if (!this._destroyed)
            if (e.console.assert(i, "[CacheRecord.addTile] tile is required"), r != null && this._tiles.length < 1)
              typeof r == "function" && (r = r()), this.type && this._promise ? r instanceof e.Promise ? this._promise = r.then((l) => {
                this._overwriteData(l, o);
              }) : this._overwriteData(r, o) : (r instanceof e.Promise ? (this._promise = r.then((l) => (this._data = l, this.loaded = !0, l)), this._data = null) : (this._promise = e.Promise.resolve(r), this._data = r, this.loaded = !0), this._type = o), this._tiles.push(i);
            else {
              const l = this._tiles.includes(i);
              !l && this.type && this._promise ? this._tiles.push(i) : l || e.console.warn("Tile %s caching attempt without data argument on uninitialized cache entry!", i);
            }
        }
        /**
         * Remove tile dependency on this record.
         * @param tile
         * @returns {Boolean} true if record removed
         */
        removeTile(i) {
          if (this._destroyed)
            return !1;
          for (let r = 0; r < this._tiles.length; r++)
            if (this._tiles[r] === i)
              return this._tiles.splice(r, 1), this._tRef === i && (this._tRef = this._tiles[r - 1]), !0;
          return e.console.warn("[CacheRecord.removeTile] trying to remove unknown tile", i), !1;
        }
        /**
         * Get the amount of tiles sharing this record.
         * @return {number}
         */
        getTileCount() {
          return this._tiles ? this._tiles.length : 0;
        }
        /**
         * Private conversion that makes sure collided requests are
         * processed eventually
         * @private
         */
        _checkAwaitsConvert() {
          !this._conversionJobQueue || this._destroyed || setTimeout(() => {
            if (!this._conversionJobQueue || this._destroyed)
              return;
            const i = this._conversionJobQueue[0];
            this._conversionJobQueue.splice(0, 1), this._conversionJobQueue.length === 0 && delete this._conversionJobQueue, i();
          });
        }
        _triggerNeedsDraw() {
          this._tiles.length > 0 && this._tiles[0].tiledImage.viewer.forceRedraw();
        }
        /**
         * Safely overwrite the cache data and return the old data
         * @private
         */
        _overwriteData(i, r) {
          if (this._destroyed)
            return e.converter.destroy(i, r), e.Promise.resolve();
          if (this.loaded) {
            if (this._data === i && this._type === r)
              return this._promise;
            e.converter.destroy(this._data, this._type), this._type = r, this._data = i, this._promise = e.Promise.resolve(i);
            const o = this[t];
            if (o)
              for (const l in o)
                o[l].setDataAs(i, r);
            return this._triggerNeedsDraw(), this._promise;
          }
          return this._promise.then(() => {
            if (this._data === i && this._type === r)
              return this._data;
            e.converter.destroy(this._data, this._type), this._type = r, this._data = i, this._promise = e.Promise.resolve(i);
            const o = this[t];
            if (o)
              for (const l in o)
                o[l].setDataAs(i, r);
            return this._triggerNeedsDraw(), this._data;
          });
        }
        /**
         * Private conversion that makes sure the cache knows its data is ready
         * @param to array or a string - allowed types
         * @param from string - type origin
         * @private
         */
        _convert(i, r) {
          const o = e.converter, l = o.getConversionPath(i, r);
          if (!l) {
            e.console.error(`[CacheRecord._convert] Conversion ${i} ---> ${r} cannot be done!`);
            return;
          }
          const a = this._data, c = l.length, d = this, f = (p, b) => {
            if (b >= c)
              return d._data = p, d.loaded = !0, d._checkAwaitsConvert(), e.Promise.resolve(p);
            const x = l[b], T = x.transform(d._tRef, p);
            if (T === void 0)
              throw d.loaded = !1, `[CacheRecord._convert] data mid result undefined value (while converting using ${x}})`;
            return o.destroy(p, x.origin.value), (e.type(T) === "promise" ? T : e.Promise.resolve(T)).then((z) => f(z, b + 1));
          };
          this.loaded = !1, this._data = void 0, this._type = l[c - 1].target.value, this._promise = f(a, 0);
        }
      }, e.InternalCacheRecord = class {
        constructor(i, r, o) {
          this.tstamp = e.now(), this._ondestroy = o, this._type = r, i instanceof e.Promise ? (this._promise = i, i.then((l) => {
            this.loaded = !0, this._data = l;
          })) : (this._promise = null, this.loaded = !0, this._data = i);
        }
        /**
         * Sync access to the data
         * @returns {any}
         */
        get data() {
          return this._data;
        }
        /**
         * Sync access to the current type
         * @returns {string}
         */
        get type() {
          return this._type;
        }
        /**
         * Await ongoing process so that we get cache ready on callback.
         * @returns {OpenSeadragon.Promise<?>}
         */
        await() {
          return this._promise ? this._promise : e.Promise.resolve(this._data);
        }
        /**
         * Must be called before transformTo or setDataAs. To keep
         * compatible api with CacheRecord where tile refs are known.
         * @param {OpenSeadragon.Tile} referenceTile reference tile for conversion
         * @return {OpenSeadragon.InternalCacheRecord} self reference for builder pattern
         */
        withTileReference(i) {
          return this._temporaryTileRef = i, this;
        }
        /**
         * Free all the data and call data destructors if defined.
         */
        destroy() {
          this.loaded && (this._ondestroy && this._ondestroy(this._data), this._data = null, this.loaded = !1);
        }
      }, e.TileCache = class {
        constructor(i) {
          i = i || {}, this._maxCacheItemCount = i.maxImageCacheCount || e.DEFAULT_SETTINGS.maxImageCacheCount, this._tilesLoaded = [], this._zombiesLoaded = [], this._zombiesLoadedCount = 0, this._cachesLoaded = [], this._cachesLoadedCount = 0;
        }
        /**
         * @returns {Number} The total number of tiles that have been loaded by
         * this TileCache. Note that the tile might be recorded here mutliple times,
         * once for each cache it uses.
         */
        numTilesLoaded() {
          return this._tilesLoaded.length;
        }
        /**
         * @returns {Number} The total number of cached objects (+ zombies)
         */
        numCachesLoaded() {
          return this._zombiesLoadedCount + this._cachesLoadedCount;
        }
        /**
         * Caches the specified tile, removing an old tile if necessary to stay under the
         * maxImageCacheCount specified on construction. Note that if multiple tiles reference
         * the same image, there may be more tiles than maxImageCacheCount; the goal is to keep
         * the number of images below that number. Note, as well, that even the number of images
         * may temporarily surpass that number, but should eventually come back down to the max specified.
         * @private
         * @param {Object} options - Cache creation parameters.
         * @param {OpenSeadragon.Tile} options.tile - The tile to cache.
         * @param {?String} [options.cacheKey=undefined] - Cache Key to use. Defaults to options.tile.cacheKey
         * @param {String} options.tile.cacheKey - The unique key used to identify this tile in the cache.
         *   Used if options.cacheKey not set.
         * @param {Image} options.image - The image of the tile to cache. Deprecated.
         * @param {*} options.data - The data of the tile to cache. If `typeof data === 'function'` holds,
         *   the data is called to obtain the data item: this is an optimization to load data only when necessary.
         * @param {string} [options.dataType] - The data type of the tile to cache. Required.
         * @param {Number} [options.cutoff=0] - If adding this tile goes over the cache max count, this
         *   function will release an old tile. The cutoff option specifies a tile level at or below which
         *   tiles will not be released.
         * @returns {OpenSeadragon.CacheRecord} - The cache record the tile was attached to.
         */
        cacheTile(i) {
          e.console.assert(i, "[TileCache.cacheTile] options is required");
          const r = i.tile;
          e.console.assert(r, "[TileCache.cacheTile] options.tile is required"), e.console.assert(r.cacheKey, "[TileCache.cacheTile] options.tile.cacheKey is required"), i.image instanceof Image && (e.console.warn("[TileCache.cacheTile] options.image is deprecated!"), i.data = i.image, i.dataType = "image");
          const o = i.cacheKey || r.cacheKey;
          let l = this._cachesLoaded[o];
          if (!l)
            if (i.data === void 0 && (e.console.error("[TileCache.cacheTile] options.image was renamed to options.data. '.image' attribute has been deprecated and will be removed in the future."), i.data = i.image), l = this._zombiesLoaded[o], l)
              l._destroyed ? l.revive() : (typeof i.data == "function" && i.data(), delete i.data), delete this._zombiesLoaded[o], this._zombiesLoadedCount--, this._cachesLoaded[o] = l, this._cachesLoadedCount++;
            else {
              const a = i.data !== void 0 && i.data !== null && i.data !== !1;
              e.console.assert(a, "[TileCache.cacheTile] options.data is required to create an CacheRecord"), l = this._cachesLoaded[o] = new e.CacheRecord(), this._cachesLoadedCount++;
            }
          return i.dataType || (e.console.error("[TileCache.cacheTile] options.dataType is newly required. For easier use of the cache system, use the tile instance API."), typeof i.data == "function" && e.console.error("[TileCache.cacheTile] options.dataType is mandatory  when data item is a callback!"), i.dataType = e.converter.guessType(i.data)), l.addTile(r, i.data, i.dataType), this._freeOldRecordRoutine(r, i.cutoff || 0), l;
        }
        /**
         * Changes cache key
         * @private
         * @param {Object} options - Cache creation parameters.
         * @param {String} options.oldCacheKey - Current key
         * @param {String} options.newCacheKey - New key to set
         * @return {OpenSeadragon.CacheRecord | null}
         */
        renameCache(i) {
          const r = i.newCacheKey, o = i.oldCacheKey;
          let l = this._cachesLoaded[o];
          if (l) {
            if (this._cachesLoaded[r])
              return e.console.error(
                "Cannot rename cache %s to %s: the target cache is occupied!",
                o,
                r
              ), null;
            this._cachesLoaded[r] = l, delete this._cachesLoaded[o];
          } else {
            if (l = this._zombiesLoaded[o], e.console.assert(l, "[TileCache.renameCache] oldCacheKey must reference existing cache!"), this._zombiesLoaded[r])
              return e.console.error(
                "Cannot rename zombie cache %s to %s: the target cache is occupied!",
                o,
                r
              ), null;
            this._zombiesLoaded[r] = l, delete this._zombiesLoaded[o];
          }
          for (const a of l._tiles)
            a.reflectCacheRenamed(o, r);
          return l;
        }
        /**
         * Reads a cache if it exists and creates a new copy of a target, different cache if it does not
         * @param {Object} options
         * @param {OpenSeadragon.Tile} options.tile - The tile to own ot add record for the cache.
         * @param {String} options.copyTargetKey - The unique key used to identify this tile in the cache.
         * @param {String} options.newCacheKey - The unique key the copy will be created for.
         * @param {String} [options.desiredType=undefined] - For optimization purposes, the desired type. Can
         *   be ignored.
         * @param {Number} [options.cutoff=0] - If adding this tile goes over the cache max count, this
         *   function will release an old tile. The cutoff option specifies a tile level at or below which
         *   tiles will not be released.
         * @returns {OpenSeadragon.Promise<OpenSeadragon.CacheRecord>} - New record.
         * @private
         */
        cloneCache(i) {
          const r = i.tile, o = i.copyTargetKey, l = this._cachesLoaded[o] || this._zombiesLoaded[o];
          e.console.assert(l, "[TileCache.cloneCache] attempt to clone non-existent cache %s!", o), e.console.assert(
            !this._cachesLoaded[i.newCacheKey],
            "[TileCache.cloneCache] attempt to copy clone to existing cache %s!",
            i.newCacheKey
          );
          const a = i.desiredType || void 0;
          return l.getDataAs(a, !0).then((c) => {
            const d = this._cachesLoaded[i.newCacheKey] = new e.CacheRecord();
            return d.addTile(r, c, l.type), this._cachesLoadedCount++, this._freeOldRecordRoutine(r, i.cutoff || 0), d;
          });
        }
        /**
         * Inject new cache to the system
         * @param {Object} options
         * @param {OpenSeadragon.Tile} options.tile - Reference tile. All tiles sharing original data will be affected.
         * @param {OpenSeadragon.CacheRecord} options.cache - Cache that will be injected.
         * @param {String} options.targetKey - The target cache key to inhabit. Can replace existing cache.
         * @param {Boolean} options.setAsMainCache - If true, tiles main cache gets updated to consumerKey.
         *   Otherwise, if consumerKey==tile.cacheKey the cache is set as main too.
         * @param {Boolean} options.tileAllowNotLoaded - if true, tile that is not loaded is also processed,
         *   this is internal parameter used in tile-loaded completion routine, as we need to prepare tile but
         *   it is not yet loaded and cannot be marked as so (otherwise the system would think it is ready)
         * @private
         */
        injectCache(i) {
          const r = i.targetKey, o = i.tile;
          if (!i.tileAllowNotLoaded && !o.loaded && !o.loading) {
            e.console.warn("Attempt to inject cache on tile in invalid state: this is probably a bug!");
            return;
          }
          const l = this._cachesLoaded[r];
          if (l) {
            const c = [...l._tiles];
            for (const d of c)
              this.unloadCacheForTile(d, r, !0, !1);
          }
          this._cachesLoaded[r] && e.console.error("The inject routine should've freed cache!");
          const a = i.cache;
          this._cachesLoaded[r] = a;
          for (const c of o.getCache(o.originalCacheKey)._tiles)
            c.setCache(r, a, i.setAsMainCache, !1);
        }
        /**
         * Replace cache (and update tile references) by another cache
         * @param {Object} options
         * @param {OpenSeadragon.Tile} options.tile - The tile to own ot add record for the cache.
         * @param {String} options.victimKey - Cache that will be erased. In fact, the victim _replaces_ consumer,
         *   inheriting its tiles and key.
         * @param {String} options.consumerKey - The cache that consumes the victim. In fact, it gets destroyed and
         *   replaced by victim, which inherits all its metadata.
         * @param {Boolean} options.setAsMainCache - If true, tiles main cache gets updated to consumerKey.
         *   Otherwise, if consumerKey==tile.cacheKey the cache is set as main too.
         * @param {Boolean} options.tileAllowNotLoaded - if true, tile that is not loaded is also processed,
         *   this is internal parameter used in tile-loaded completion routine, as we need to prepare tile but
         *   it is not yet loaded and cannot be marked as so (otherwise the system would think it is ready)
         * @private
         */
        replaceCache(i) {
          const r = i.victimKey, o = i.consumerKey, l = this._cachesLoaded[r], a = i.tile;
          if (!l || !i.tileAllowNotLoaded && !a.loaded && !a.loading) {
            e.console.warn("Attempt to consume cache on tile in invalid state: this is probably a bug!");
            return;
          }
          const c = this._cachesLoaded[o];
          if (c) {
            const f = [...c._tiles];
            for (const p of f)
              this.unloadCacheForTile(p, o, !0, !1);
          }
          this._cachesLoaded[o] && e.console.error("The consume routine should've freed cache!");
          const d = this.renameCache({
            oldCacheKey: r,
            newCacheKey: o
          });
          if (d)
            for (const f of a.getCache(a.originalCacheKey)._tiles)
              f.setCache(o, d, i.setAsMainCache, !1);
        }
        /**
         * This method ensures other tiles are restored if one of the tiles
         * was requested restore().
         * @param tile
         * @param originalCache
         * @param freeIfUnused if true, zombie is not created
         * @private
         */
        restoreTilesThatShareOriginalCache(i, r, o) {
          for (const l of r._tiles)
            l.cacheKey !== l.originalCacheKey && (this.unloadCacheForTile(l, l.cacheKey, o, !0), delete l._caches[l.cacheKey], l.cacheKey = l.originalCacheKey);
        }
        _freeOldRecordRoutine(i, r) {
          let o = this._tilesLoaded.length, l = -1;
          if (this._cachesLoadedCount + this._zombiesLoadedCount > this._maxCacheItemCount)
            if (this._zombiesLoadedCount > 0)
              for (const a in this._zombiesLoaded) {
                this._zombiesLoaded[a].destroy(), delete this._zombiesLoaded[a], this._zombiesLoadedCount--;
                break;
              }
            else {
              let a = null, c, d, f, p, b;
              for (let x = this._tilesLoaded.length - 1; x >= 0; x--)
                if (c = this._tilesLoaded[x], !(c.level <= r || c.beingDrawn || c.loading || c.processing)) {
                  if (!a) {
                    a = c, l = x;
                    continue;
                  }
                  p = c.lastTouchTime, d = a.lastTouchTime, b = c.level, f = a.level, (p < d || p === d && b > f) && (a = c, l = x);
                }
              a && l >= 0 && (this._unloadTile(a, !0), o = l);
            }
          i.getCacheSize() === 0 ? this._tilesLoaded[o] = i : l >= 0 && this._tilesLoaded.splice(o, 1);
        }
        /**
         * Clears all tiles associated with the specified tiledImage.
         * @param {OpenSeadragon.TiledImage} tiledImage
         */
        clearTilesFor(i) {
          e.console.assert(i, "[TileCache.clearTilesFor] tiledImage is required");
          let r, o = this._cachesLoadedCount + this._zombiesLoadedCount > this._maxCacheItemCount;
          if (i._zombieCache && o && this._zombiesLoadedCount > 0) {
            for (const l in this._zombiesLoaded)
              this._zombiesLoaded[l].destroy(), delete this._zombiesLoaded[l];
            this._zombiesLoadedCount = 0, o = this._cachesLoadedCount > this._maxCacheItemCount;
          }
          for (let l = this._tilesLoaded.length - 1; l >= 0; l--)
            r = this._tilesLoaded[l], r.tiledImage === i && (r.loaded ? r.tiledImage === i && this._unloadTile(r, !i._zombieCache || o, l) : this._tilesLoaded.splice(l, 1));
        }
        /**
         * Delete all data in the cache
         * @param {boolean} withZombies
         */
        clear(i = !0) {
          for (const r in this._zombiesLoaded)
            this._zombiesLoaded[r].destroy();
          for (const r in this._tilesLoaded)
            this._unloadTile(r, !0, void 0);
          this._tilesLoaded = [], this._zombiesLoaded = [], this._zombiesLoadedCount = 0, this._cachesLoaded = [], this._cachesLoadedCount = 0;
        }
        /**
         * Clean up internal drawer data for a given drawer
         * @param {OpenSeadragon.DrawerBase} drawer
         */
        clearDrawerInternalCache(i) {
          const r = i.getId();
          for (const o of this._zombiesLoaded)
            o && o.destroyInternalCache(r);
          for (const o of this._cachesLoaded)
            o && o.destroyInternalCache(r);
        }
        /**
         * Returns reference to all tiles loaded by a particular
         * tiled image item
         * @param {OpenSeadragon.TiledImage|null} tiledImage if null, gets all tiles, else filters out tiles
         *   that belong to a specific image
         */
        getLoadedTilesFor(i) {
          return i ? this._tilesLoaded.filter((r) => r.tiledImage === i) : [...this._tilesLoaded];
        }
        /**
         * Get cache record (might be a unattached record, i.e. a zombie)
         * @param cacheKey
         * @returns {OpenSeadragon.CacheRecord|undefined}
         */
        getCacheRecord(i) {
          return e.console.assert(i, "[TileCache.getCacheRecord] cacheKey is required"), this._cachesLoaded[i] || this._zombiesLoaded[i];
        }
        /**
         * Delete cache safely from the system if it is not needed
         * @param {OpenSeadragon.CacheRecord} cache
         */
        safeUnloadCache(i) {
          if (i && !i._destroyed && i.getTileCount() < 1) {
            for (const r in this._zombiesLoaded) {
              const o = this._zombiesLoaded[r];
              if (o === i) {
                delete this._zombiesLoaded[r], o.destroy();
                return;
              }
            }
            e.console.error("Attempt to delete an orphan cache that is not in zombie list: this could be a bug!", i), i.destroy();
          }
        }
        /**
         * Delete cache record for a given til
         * @param {OpenSeadragon.Tile} tile
         * @param {string} key cache key
         * @param {boolean} destroy if true, empty cache is destroyed, else left as a zombie
         * @param {boolean} okIfNotExists sometimes we call destruction just to make sure, if true do not report as error
         * @private
         */
        unloadCacheForTile(i, r, o, l) {
          const a = this._cachesLoaded[r];
          return a ? a.removeTile(i) ? (a.getTileCount() || (o ? a.destroy() : (this._zombiesLoaded[r] = a, this._zombiesLoadedCount++), delete this._cachesLoaded[r], this._cachesLoadedCount--), !0) : (e.console.error("[TileCache.unloadCacheForTile] System tried to delete tile from cache it does not belong to! This could mean a bug in the cache system."), !1) : (l || e.console.warn("[TileCache.unloadCacheForTile] Attempting to delete missing cache!"), !1);
        }
        /**
         * Unload tile: this will free the tile data and mark the tile as unloaded.
         * @param {OpenSeadragon.Tile} tile
         * @param {boolean} destroy if set to true, tile data is not preserved as zombies but deleted immediatelly
         */
        unloadTile(i, r = !1) {
          if (!i.loaded) {
            e.console.warn("Attempt to unload already unloaded tile.");
            return;
          }
          const o = this._tilesLoaded.findIndex((l) => l === i);
          this._unloadTile(i, r, o);
        }
        /**
         * @param {OpenSeadragon.Tile} tile tile to unload
         * @param {boolean} destroy destroy tile cache if the cache tile counts falls to zero
         * @param {Number} deleteAtIndex index to remove the tile record at, will not remove from _tilesLoaded if not set
         * @private
         */
        _unloadTile(i, r, o) {
          e.console.assert(i, "[TileCache._unloadTile] tile is required");
          for (const a in i._caches)
            this.unloadCacheForTile(i, a, r, !1);
          if (o !== void 0 && this._tilesLoaded.splice(o, 1), !i.loaded)
            return;
          const l = i.tiledImage;
          i._unload(), l.viewer.raiseEvent("tile-unloaded", {
            tile: i,
            tiledImage: l,
            destroyed: r
          });
        }
      };
    })(n), (function(e) {
      e.World = function(t) {
        const i = this;
        e.console.assert(t.viewer, "[World] options.viewer is required"), e.EventSource.call(this), this.viewer = t.viewer, this._items = [], this._needsDraw = !1, this._autoRefigureSizes = !0, this._needsSizesFigured = !1, this._delegatedFigureSizes = function(r) {
          i._autoRefigureSizes ? i._figureSizes() : i._needsSizesFigured = !0;
        }, this._figureSizes();
      }, e.extend(
        e.World.prototype,
        e.EventSource.prototype,
        /** @lends OpenSeadragon.World.prototype */
        {
          /**
           * Add the specified item.
           * @param {OpenSeadragon.TiledImage} item - The item to add.
           * @param {Object} options - Options affecting insertion.
           * @param {Number} [options.index] - Index for the item. If not specified, goes at the top.
           * @fires OpenSeadragon.World.event:add-item
           * @fires OpenSeadragon.World.event:metrics-change
           */
          addItem: function(t, i) {
            if (e.console.assert(t, "[World.addItem] item is required"), e.console.assert(t instanceof e.TiledImage, "[World.addItem] only TiledImages supported at this time"), i = i || {}, i.index !== void 0) {
              const r = Math.max(0, Math.min(this._items.length, i.index));
              this._items.splice(r, 0, t);
            } else
              this._items.push(t);
            this._autoRefigureSizes ? this._figureSizes() : this._needsSizesFigured = !0, this._needsDraw = !0, t.addHandler("bounds-change", this._delegatedFigureSizes), t.addHandler("clip-change", this._delegatedFigureSizes), this.raiseEvent("add-item", {
              item: t
            });
          },
          /**
           * Get the item at the specified index.
           * @param {Number} index - The item's index.
           * @returns {OpenSeadragon.TiledImage} The item at the specified index.
           */
          getItemAt: function(t) {
            return e.console.assert(t !== void 0, "[World.getItemAt] index is required"), this._items[t];
          },
          /**
           * Get the index of the given item or -1 if not present.
           * @param {OpenSeadragon.TiledImage} item - The item.
           * @returns {Number} The index of the item or -1 if not present.
           */
          getIndexOfItem: function(t) {
            return e.console.assert(t, "[World.getIndexOfItem] item is required"), e.indexOf(this._items, t);
          },
          /**
           * @returns {Number} The number of items used.
           */
          getItemCount: function() {
            return this._items.length;
          },
          /**
           * Change the index of a item so that it appears over or under others.
           * @param {OpenSeadragon.TiledImage} item - The item to move.
           * @param {Number} index - The new index.
           * @fires OpenSeadragon.World.event:item-index-change
           */
          setItemIndex: function(t, i) {
            e.console.assert(t, "[World.setItemIndex] item is required"), e.console.assert(i !== void 0, "[World.setItemIndex] index is required");
            const r = this.getIndexOfItem(t);
            if (i >= this._items.length)
              throw new Error("Index bigger than number of layers.");
            i === r || r === -1 || (this._items.splice(r, 1), this._items.splice(i, 0, t), this._needsDraw = !0, this.raiseEvent("item-index-change", {
              item: t,
              previousIndex: r,
              newIndex: i
            }));
          },
          /**
           * Remove an item.
           * @param {OpenSeadragon.TiledImage} item - The item to remove.
           * @fires OpenSeadragon.World.event:remove-item
           * @fires OpenSeadragon.World.event:metrics-change
           */
          removeItem: function(t) {
            e.console.assert(t, "[World.removeItem] item is required");
            const i = e.indexOf(this._items, t);
            i !== -1 && (t.removeHandler("bounds-change", this._delegatedFigureSizes), t.removeHandler("clip-change", this._delegatedFigureSizes), t.destroy(), this._items.splice(i, 1), this._figureSizes(), this._needsDraw = !0, this._raiseRemoveItem(t));
          },
          /**
           * Remove all items.
           * @fires OpenSeadragon.World.event:remove-item
           * @fires OpenSeadragon.World.event:metrics-change
           */
          removeAll: function() {
            this.viewer._cancelPendingImages();
            let t;
            for (let r = 0; r < this._items.length; r++)
              t = this._items[r], t.removeHandler("bounds-change", this._delegatedFigureSizes), t.removeHandler("clip-change", this._delegatedFigureSizes), t.destroy();
            const i = this._items;
            this._items = [], this._figureSizes(), this._needsDraw = !0;
            for (let r = 0; r < i.length; r++)
              t = i[r], this._raiseRemoveItem(t);
          },
          /**
           * Forces the system consider all tiles across all tiled images
           * as outdated, and fire tile update event on relevant tiles
           * Detailed description is available within the 'tile-invalidated'
           * event.
           * @param {Boolean} [restoreTiles=true] if true, tile processing starts from the tile original data
           * @param {number} [tStamp=OpenSeadragon.now()] optionally provide tStamp of the update event
           * @function
           * @fires OpenSeadragon.Viewer.event:tile-invalidated
           * @return {OpenSeadragon.Promise<?>}
           */
          requestInvalidate: function(t = !0, i = e.now()) {
            e.__updated = i;
            let r = 1 / 0;
            for (const f of this._items)
              f._lastDrawn.length && (r = Math.min(r, f._lastDrawn[0].tile.lastTouchTime));
            const o = this.viewer.tileCache.getLoadedTilesFor(null), l = new Array(o.length);
            let a = 0, c = 0;
            const d = this.viewer.tileCache;
            for (let f = 0; f < o.length; f++) {
              const p = o[f], b = p.lastTouchTime >= r, x = p.level <= (p.tiledImage.source.getClosestLevel() || 0);
              b || x ? l[a++] = p : (d._unloadTile(p, !1, f - c), c++);
            }
            return l.length = a, this.requestTileInvalidateEvent(l, i, t);
          },
          /**
           * Requests tile data update.
           * @function OpenSeadragon.Viewer.prototype._updateSequenceButtons
           * @private
           * @param {Iterable<OpenSeadragon.Tile>} tilesToProcess tiles to update
           * @param {Number} tStamp timestamp in milliseconds, if active timestamp of the same value is executing,
           *   changes are added to the cycle, else they await next iteration
           * @param {Boolean} [restoreTiles=true] if true, tile processing starts from the tile original data
           * @param {Boolean} [_allowTileUnloaded=false] internal flag for calling on tiles that come new to the system
           * @fires OpenSeadragon.Viewer.event:tile-invalidated
           * @return {OpenSeadragon.Promise<?>}
           */
          requestTileInvalidateEvent: function(t, i, r = !0, o = !1) {
            if (!this.viewer.isOpen())
              return e.Promise.resolve();
            const l = [], a = [];
            for (const p of t) {
              if (!p || !o && !p.loaded)
                continue;
              const b = p.getCache(p.originalCacheKey);
              if (!(b.__invStamp && b.__invStamp >= i)) {
                for (const x of b._tiles)
                  x.processing = i, x.processingPromise = new e.Promise((T) => {
                    a.push(() => {
                      x.processing = !1, T(x);
                    });
                  });
                b.__invStamp = i, l.push(p);
              }
            }
            if (!l.length)
              return e.Promise.resolve();
            const c = this.viewer.viewer || this.viewer, d = this.viewer.drawer, f = l.map((p) => {
              const b = p.tiledImage, x = p.getCache(p.originalCacheKey);
              let T = null;
              const I = (j) => {
                if (T)
                  return T.getDataAs(j, !1);
                const oe = r ? p.originalCacheKey : p.cacheKey, ae = p.getCache(oe);
                return ae ? (j = j || ae.type, T = new e.CacheRecord().withTileReference(p), ae.getDataAs(j, !0).then((se) => (T.addTile(p, se, j), T.data))) : (e.console.error("[Tile::getData] There is no cache available for tile with key %s", oe), e.Promise.reject());
              }, z = (j, oe) => T ? T.setDataAs(j, oe) : (T = new e.CacheRecord().withTileReference(p), T.addTile(p, j, oe), e.Promise.resolve()), W = () => {
                if (T) {
                  const j = p.buildDistinctMainCacheKey();
                  b._tileCache.injectCache({
                    tile: p,
                    cache: T,
                    targetKey: j,
                    setAsMainCache: !0,
                    tileAllowNotLoaded: p.loading
                  });
                } else r && b._tileCache.restoreTilesThatShareOriginalCache(p, p.getCache(p.originalCacheKey), !0);
              };
              return c.raiseEventAwaiting("tile-invalidated", {
                tile: p,
                tiledImage: b,
                outdated: () => this.viewer.isDestroyed() || x.__invStamp !== i || !p.loaded && !p.loading,
                getData: I,
                setData: z,
                resetData: () => {
                  T && (T.destroy(), T = null);
                }
              }).then((j) => this.viewer.isDestroyed() ? null : x.__invStamp === i && (p.loaded || p.loading) ? T ? T.prepareForRendering(d).then((ae) => {
                ae && x.__invStamp === i && (W(), x.__invStamp = null);
              }) : r ? p.getCache(p.originalCacheKey).prepareForRendering(d).then((se) => {
                se && x.__invStamp === i && (W(), x.__invStamp = null);
              }) : p.getCache().prepareForRendering(d).then(() => {
                W(), x.__invStamp = null;
              }) : (T && (T.destroy(), T = null), null)).catch((j) => {
                e.console.error("Update routine error:", j);
              });
            });
            return e.Promise.all(f).then(() => {
              for (const p of a)
                p();
              this.viewer.isDestroyed() || this.draw();
            });
          },
          /**
           * Clears all tiles and triggers updates for all items.
           */
          resetItems: function() {
            for (let t = 0; t < this._items.length; t++)
              this._items[t].reset();
          },
          /**
           * Updates (i.e. animates bounds of) all items.
           * @function
           * @param viewportChanged Whether the viewport changed, which indicates that
           * all TiledImages need to be updated.
           */
          update: function(t) {
            let i = !1;
            for (let r = 0; r < this._items.length; r++)
              i = this._items[r].update(t) || i;
            return i;
          },
          /**
           * Draws all items.
           */
          draw: function() {
            this.viewer.drawer.draw(this._items), this._needsDraw = !1;
            for (const t of this._items)
              this._needsDraw = t.setDrawn() || this._needsDraw;
          },
          /**
           * @returns {Boolean} true if any items need updating.
           */
          needsDraw: function() {
            for (let t = 0; t < this._items.length; t++)
              if (this._items[t].needsDraw())
                return !0;
            return this._needsDraw;
          },
          /**
           * @returns {OpenSeadragon.Rect} The smallest rectangle that encloses all items, in viewport coordinates.
           */
          getHomeBounds: function() {
            return this._homeBounds.clone();
          },
          /**
           * To facilitate zoom constraints, we keep track of the pixel density of the
           * densest item in the World (i.e. the item whose content size to viewport size
           * ratio is the highest) and save it as this "content factor".
           * @returns {Number} the number of content units per viewport unit.
           */
          getContentFactor: function() {
            return this._contentFactor;
          },
          /**
           * As a performance optimization, setting this flag to false allows the bounds-change event handler
           * on tiledImages to skip calculations on the world bounds. If a lot of images are going to be positioned in
           * rapid succession, this is a good idea. When finished, setAutoRefigureSizes should be called with true
           * or the system may behave oddly.
           * @param {Boolean} [value] The value to which to set the flag.
           */
          setAutoRefigureSizes: function(t) {
            this._autoRefigureSizes = t, t & this._needsSizesFigured && (this._figureSizes(), this._needsSizesFigured = !1);
          },
          /**
           * Arranges all of the TiledImages with the specified settings.
           * @param {Object} options - Specifies how to arrange.
           * @param {Boolean} [options.immediately=false] - Whether to animate to the new arrangement.
           * @param {String} [options.layout] - See collectionLayout in {@link OpenSeadragon.Options}.
           * @param {Number} [options.rows] - See collectionRows in {@link OpenSeadragon.Options}.
           * @param {Number} [options.columns] - See collectionColumns in {@link OpenSeadragon.Options}.
           * @param {Number} [options.tileSize] - See collectionTileSize in {@link OpenSeadragon.Options}.
           * @param {Number} [options.tileMargin] - See collectionTileMargin in {@link OpenSeadragon.Options}.
           * @fires OpenSeadragon.World.event:metrics-change
           */
          arrange: function(t) {
            t = t || {};
            const i = t.immediately || !1, r = t.layout || e.DEFAULT_SETTINGS.collectionLayout, o = t.rows || e.DEFAULT_SETTINGS.collectionRows, l = t.columns || e.DEFAULT_SETTINGS.collectionColumns, a = t.tileSize || e.DEFAULT_SETTINGS.collectionTileSize, c = t.tileMargin || e.DEFAULT_SETTINGS.collectionTileMargin, d = a + c;
            let f;
            !t.rows && l ? f = l : f = Math.ceil(this._items.length / o);
            let p = 0, b = 0, x, T, I, z, W;
            this.setAutoRefigureSizes(!1);
            for (let j = 0; j < this._items.length; j++)
              j && j % f === 0 && (r === "horizontal" ? (b += d, p = 0) : (p += d, b = 0)), x = this._items[j], T = x.getBoundsNoRotate(), T.width > T.height ? I = a : I = a * (T.width / T.height), z = I * (T.height / T.width), W = new e.Point(
                p + (a - I) / 2,
                b + (a - z) / 2
              ), x.setPosition(W, i), x.setWidth(I, i), r === "horizontal" ? p += d : b += d;
            this.setAutoRefigureSizes(!0);
          },
          // private
          _figureSizes: function() {
            const t = this._homeBounds ? this._homeBounds.clone() : null, i = this._contentSize ? this._contentSize.clone() : null, r = this._contentFactor || 0;
            if (!this._items.length)
              this._homeBounds = new e.Rect(0, 0, 1, 1), this._contentSize = new e.Point(1, 1), this._contentFactor = 1;
            else {
              let o = this._items[0], l = o.getBounds();
              this._contentFactor = o.getContentSize().x / l.width;
              let a = o.getClippedBounds().getBoundingBox(), c = a.x, d = a.y, f = a.x + a.width, p = a.y + a.height;
              for (let b = 1; b < this._items.length; b++)
                o = this._items[b], l = o.getBounds(), this._contentFactor = Math.max(
                  this._contentFactor,
                  o.getContentSize().x / l.width
                ), a = o.getClippedBounds().getBoundingBox(), c = Math.min(c, a.x), d = Math.min(d, a.y), f = Math.max(f, a.x + a.width), p = Math.max(p, a.y + a.height);
              this._homeBounds = new e.Rect(c, d, f - c, p - d), this._contentSize = new e.Point(
                this._homeBounds.width * this._contentFactor,
                this._homeBounds.height * this._contentFactor
              );
            }
            (this._contentFactor !== r || !this._homeBounds.equals(t) || !this._contentSize.equals(i)) && this.raiseEvent("metrics-change", {});
          },
          // private
          _raiseRemoveItem: function(t) {
            this.raiseEvent("remove-item", { item: t });
          }
        }
      );
    })(n);
  })(gn)), gn.exports;
}
var Sg = Pg();
const mn = /* @__PURE__ */ _c(Sg);
function Rg(s) {
  var n = "http://www.w3.org/2000/svg";
  s.Viewer.prototype.svgOverlay = function() {
    return this._svgOverlayInfo ? this._svgOverlayInfo : (this._svgOverlayInfo = new e(this), this._svgOverlayInfo);
  };
  var e = function(t) {
    var i = this;
    this._viewer = t, this._containerWidth = 0, this._containerHeight = 0, this._svg = document.createElementNS(n, "svg"), this._svg.style.position = "absolute", this._svg.style.left = 0, this._svg.style.top = 0, this._svg.style.width = "100%", this._svg.style.height = "100%", this._viewer.canvas.appendChild(this._svg), this._node = document.createElementNS(n, "g"), this._svg.appendChild(this._node), this.svgWidth = 0, this.debugMode = !1, this._viewer.addHandler("animation", function() {
      i.resize();
    }), this._viewer.addHandler("open", () => {
      this._viewer.viewport.getCenter(), this._viewer.imagingHelper, i.resize();
    }), this._viewer.addHandler("rotate", function(r) {
      i.resize();
    }), this._viewer.addHandler("flip", function() {
      i.resize();
    }), this._viewer.addHandler("resize", function() {
      i.resize();
    }), this.resize(), this.hide();
  };
  e.prototype = {
    // ----------
    node: function() {
      return this._node;
    },
    svg: function() {
      return this._svg;
    },
    hide() {
      this._svg.style.display = "none";
    },
    show() {
      this._svg.style.display = "unset";
    },
    isVisible() {
      return this._svg.style.display !== "none";
    },
    clear() {
      this._node.replaceChildren();
    },
    activateDebugMode() {
      this.debugMode || (this.debugMode = !0, this.addInfoBox(), this._svg.style.backgroundColor = "rgba(255,50,50,.1)", this._svg.style.border = "8px solid rgba(255,50,50,.5)", this._svg.style.boxSizing = "border-box");
    },
    addInfoBox() {
      this.debugMode && (this._infoBox = document.createElement("div"), this._infoBox.id = "info-box", this._infoBox.style.position = "absolute", this._infoBox.style.left = "auto", this._infoBox.style.right = "1em", this._infoBox.style.top = "1em", this._infoBox.style.width = "25%", this._infoBox.style.height = "auto", this._infoBox.style.backgroundColor = "rgba(50,50,250,.1)", this._infoBox.style.boxSizing = "border-box", this._viewer.canvas.appendChild(this._infoBox));
    },
    /**
     *
     * @param node SVGSVGElement A SVGElement that will be loaded into the overlay
     */
    loadSVG(t) {
      let i = t.attributes.width;
      i ? i = i.value : i = t.viewBox.baseVal.width, this.svgWidth = i;
      let r = t.attributes.height;
      return r ? r = r.value : r = t.viewBox.baseVal.height, this.svgHeight = r, this._node.firstChild && this._node.replaceChildren(), this._node.replaceChildren(...t.children), new s.Point(parseFloat(i), parseFloat(r));
    },
    // ----------
    resize: function() {
      this._viewer.viewport.getCenter(!0);
      const t = this._viewer;
      var i = t.viewport._containerInnerSize.x, r = t.viewport.getZoom(!0), o = i * r;
      let l = this.svgWidth / 2, a = this.svgHeight / 2, c = new s.Point(this._viewer.imagingHelper.imgWidth / 2, this._viewer.imagingHelper.imgHeight / 2), d = t.viewport.imageToViewportCoordinates(c.x, c.y);
      d.x = d.x - l, d.y = d.y - a, d = this._viewer.viewport.pixelFromPoint(d, !0);
      var f = t.viewport.getRotation(), p = t.viewport.getFlip();
      if (p && (o = -o, d.x = -d.x + i), this._node.firstChild) {
        const b = "translate(" + d.x + "px," + d.y + "px) scale(" + o + ") rotate(" + f + "deg)";
        this._node.style.transform = b;
      }
      this.debugMode && (this._infoBox.innerHTML = `
                <div>ImageWidth: ${this._viewer.imagingHelper.imgWidth}</div>
                <div>svgWidth: ${this.svgWidth}</div>
                <div>viewPortWidth: ${this._viewer.viewport.getContainerSize().x}</div>
                <div>ViewerContainerSize: ${this._viewer.imagingHelper.getViewerContainerSize()}</div>
                <div>DataWidth: ${this._viewer.imagingHelper.logicalToDataX(1)}</div>
                <div>zoom: ${o}</div>
                <div>p.x: ${d.x}</div>
                <div>pRaw: ${c}</div>
                <div>svgWidth: ${l}</div>
                <div>p: ${d}</div>
            `);
    },
    // ----------
    onClick: function(t, i) {
      new s.MouseTracker({
        element: t,
        clickHandler: i
      }).setTracking(!0);
    }
  };
}
(function(s, n) {
  if (!s.version || s.version.major < 2)
    throw new Error(
      "OpenSeadragonImagingHelper requires OpenSeadragon version 2.0.0+"
    );
  return s.Viewer.prototype.activateImagingHelper = function(e) {
    return this.imagingHelper || (e = e || {}, e.viewer = this, this.imagingHelper = new n.ImagingHelper(e)), this.imagingHelper;
  }, n.ImagingHelper = function(e) {
    if (s.EventSource.call(this), e = e || {}, !e.viewer)
      throw new Error("A viewer must be specified.");
    if (e.viewer.imagingHelper)
      throw new Error("Viewer already has an ImagingHelper.");
    this._viewer = e.viewer, typeof e.worldIndex == "number" ? this._worldIndex = e.worldIndex : this._worldIndex = 0, this._viewer.imagingHelper = this, this.options = e, this.imgWidth = 0, this.imgHeight = 0, this.imgAspectRatio = 0, this._zoomFactor = 1, this._minZoom = 1e-3, this._maxZoom = 10, this._zoomStepPercent = 30, this._haveImage = !1, this._viewerSize = null, this._viewportWidth = 0, this._viewportHeight = 0, this._viewportOrigin = new s.Point(0, 0), this._viewportCenter = new s.Point(0, 0), this._onOpen = s.delegate(this, this.onOpen), this._onClose = s.delegate(this, this.onClose), this._onAnimation = s.delegate(this, this.onAnimation), this._onAnimationFinish = s.delegate(this, this.onAnimationFinish), this._onResize = s.delegate(this, this.onResize), this._onFullPage = s.delegate(this, this.onFullPage), this._onFullScreen = s.delegate(this, this.onFullScreen), this._onWorldAddItem = s.delegate(this, this.onWorldAddItem), this._onWorldRemoveItem = s.delegate(this, this.onWorldRemoveItem), this._onWorldItemIndexChange = s.delegate(
      this,
      this.onWorldItemIndexChange
    ), this._onWorldMetricsChange = s.delegate(
      this,
      this.onWorldMetricsChange
    ), e.onImageViewChanged && this.addHandler("image-view-changed", e.onImageViewChanged), this._viewer.addHandler("open", this._onOpen), this._viewer.addHandler("close", this._onClose), this._viewer.addHandler("animation", this._onAnimation), this._viewer.addHandler("animation-finish", this._onAnimationFinish), this._viewer.addHandler("resize", this._onResize), this._viewer.addHandler("full-page", this._onFullPage), this._viewer.addHandler("full-screen", this._onFullScreen);
  }, n.ImagingHelper.prototype = Object.create(s.EventSource.prototype), n.ImagingHelper.version = "<%= pkg.version.obj %>", Object.defineProperty(n.ImagingHelper.prototype, "constructor", {
    enumerable: !1,
    value: n.ImagingHelper
  }), n.ImagingHelper.prototype.destroy = function() {
    this.options.onImageViewChanged && this.removeHandler(
      "image-view-changed",
      this.options.onImageViewChanged
    ), this._viewer.removeHandler("open", this._onOpen), this._viewer.removeHandler("close", this._onClose), this._viewer.removeHandler("animation", this._onAnimation), this._viewer.removeHandler("animation-finish", this._onAnimationFinish), this._viewer.removeHandler("resize", this._onResize), this._viewer.removeHandler("full-page", this._onFullPage), this._viewer.removeHandler("full-screen", this._onFullScreen), this._viewer.imagingHelper = null, this._viewer.destroy(), this._viewer = null, this.options.viewer = null;
  }, n.ImagingHelper.prototype._raiseImageViewChanged = function() {
    this.raiseEvent("image-view-changed", {
      viewportWidth: this._viewportWidth,
      viewportHeight: this._viewportHeight,
      viewportOrigin: this._viewportOrigin,
      viewportCenter: this._viewportCenter,
      zoomFactor: this._zoomFactor
    });
  }, n.ImagingHelper.prototype._trackZoomPan = function() {
    var e = this._viewer.viewport.getBounds(!0);
    this._viewportOrigin.x = e.x, this._viewportOrigin.y = e.y * this.imgAspectRatio, this._viewportWidth = e.width, this._viewportHeight = e.height * this.imgAspectRatio, this._viewportCenter.x = this._viewportOrigin.x + this._viewportWidth / 2, this._viewportCenter.y = this._viewportOrigin.y + this._viewportHeight / 2, this._zoomFactor = this.getViewerContainerSize().x / (this._viewportWidth * this.imgWidth), this._raiseImageViewChanged();
  }, n.ImagingHelper.prototype.getViewerContainerSize = function() {
    var e = this._viewer.container;
    return new s.Point(e.clientWidth, e.clientHeight);
  }, n.ImagingHelper.prototype.notifyResize = function() {
    var e, t, i;
    this._haveImage && (e = this.getViewerContainerSize(), e.equals(this._viewerSize) || (this._viewerSize = e, t = new s.Point(
      this._viewportCenter.x,
      this._viewportCenter.y / this.imgAspectRatio
    ), i = this._zoomFactor, this._viewer.viewport.resize(e, !1), this._viewer.viewport.zoomTo(
      i * this.imgWidth / e.x,
      null,
      !0
    ), this._viewer.viewport.panTo(t, !0), this._raiseImageViewChanged()));
  }, n.ImagingHelper.prototype.getMinZoom = function() {
    return this._minZoom;
  }, n.ImagingHelper.prototype.setMinZoom = function(e) {
    this._minZoom = e, this._viewer.minZoomLevel = e * this.imgWidth / this.getViewerContainerSize().x;
  }, n.ImagingHelper.prototype.getMaxZoom = function() {
    return this._maxZoom;
  }, n.ImagingHelper.prototype.setMaxZoom = function(e) {
    this._maxZoom = e, this._viewer.maxZoomLevel = e * this.imgWidth / this.getViewerContainerSize().x;
  }, n.ImagingHelper.prototype.getZoomStepPercent = function() {
    return this._zoomStepPercent;
  }, n.ImagingHelper.prototype.setZoomStepPercent = function(e) {
    this._zoomStepPercent = e;
  }, n.ImagingHelper.prototype.setView = function(e, t, i, r) {
    this._haveImage && ((this._viewportWidth !== e || this._viewportHeight !== t) && this._viewer.viewport.zoomTo(1 / e, null, r), (this._viewportCenter.x !== i.x || this._viewportCenter.y !== i.y) && this._viewer.viewport.panTo(
      new s.Point(
        i.x,
        i.y / this.imgAspectRatio
      ),
      r
    ));
  }, n.ImagingHelper.prototype.getZoomFactor = function() {
    return this._zoomFactor;
  }, n.ImagingHelper.prototype.setZoomFactor = function(e, t) {
    this._haveImage && e !== this._zoomFactor && e > 0 && this._viewer.viewport.zoomTo(
      e * this.imgWidth / this.getViewerContainerSize().x,
      new s.Point(
        this._viewportCenter.x,
        this._viewportCenter.y / this.imgAspectRatio
      ),
      t
    );
  }, n.ImagingHelper.prototype.zoomIn = function(e) {
    var t = this._zoomFactor;
    t *= 1 + this._zoomStepPercent / 100, t > this._maxZoom && (t = this._maxZoom), this.setZoomFactor(t, e);
  }, n.ImagingHelper.prototype.zoomOut = function(e) {
    var t = this._zoomFactor;
    t /= 1 + this._zoomStepPercent / 100, t < this._minZoom && (t = this._minZoom), this.setZoomFactor(t, e);
  }, n.ImagingHelper.prototype.zoomAboutLogicalPoint = function(e, t, i) {
    this._haveImage && e !== this._zoomFactor && e > 0 && this._viewer.viewport.zoomTo(
      e * this.imgWidth / this.getViewerContainerSize().x,
      new s.Point(t.x, t.y / this.imgAspectRatio),
      i
    );
  }, n.ImagingHelper.prototype.zoomInAboutLogicalPoint = function(e, t) {
    var i = this._zoomFactor;
    i *= 1 + this._zoomStepPercent / 100, i > this._maxZoom && (i = this._maxZoom), this.zoomAboutLogicalPoint(i, e, t);
  }, n.ImagingHelper.prototype.zoomOutAboutLogicalPoint = function(e, t) {
    var i = this._zoomFactor;
    i /= 1 + this._zoomStepPercent / 100, i < this._minZoom && (i = this._minZoom), this.zoomAboutLogicalPoint(i, e, t);
  }, n.ImagingHelper.prototype.centerAboutLogicalPoint = function(e, t) {
    this._haveImage && (this._viewportCenter.x !== e.x || this._viewportCenter.y !== e.y) && this._viewer.viewport.panTo(
      new s.Point(e.x, e.y / this.imgAspectRatio),
      t
    );
  }, n.ImagingHelper.prototype.physicalToLogicalPoint = function(e) {
    return new s.Point(
      this.physicalToLogicalX(e.x),
      this.physicalToLogicalY(e.y)
    );
  }, n.ImagingHelper.prototype.logicalToPhysicalPoint = function(e) {
    return new s.Point(
      this.logicalToPhysicalX(e.x),
      this.logicalToPhysicalY(e.y)
    );
  }, n.ImagingHelper.prototype.physicalToLogicalX = function(e) {
    return this._haveImage ? this._viewportOrigin.x + e / this.getViewerContainerSize().x * this._viewportWidth : 0;
  }, n.ImagingHelper.prototype.physicalToLogicalY = function(e) {
    return this._haveImage ? this._viewportOrigin.y + e / this.getViewerContainerSize().y * this._viewportHeight : 0;
  }, n.ImagingHelper.prototype.logicalToPhysicalX = function(e) {
    return this._haveImage ? (e - this._viewportOrigin.x) / this._viewportWidth * this.getViewerContainerSize().x : 0;
  }, n.ImagingHelper.prototype.logicalToPhysicalY = function(e) {
    return this._haveImage ? (e - this._viewportOrigin.y) / this._viewportHeight * this.getViewerContainerSize().y : 0;
  }, n.ImagingHelper.prototype.physicalToLogicalDistance = function(e) {
    return this._haveImage ? e / this.getViewerContainerSize().x * this._viewportWidth : 0;
  }, n.ImagingHelper.prototype.logicalToPhysicalDistance = function(e) {
    return this._haveImage ? e / this._viewportWidth * this.getViewerContainerSize().x : 0;
  }, n.ImagingHelper.prototype.logicalToDataPoint = function(e) {
    return new s.Point(
      this.logicalToDataX(e.x),
      this.logicalToDataY(e.y)
    );
  }, n.ImagingHelper.prototype.dataToLogicalPoint = function(e) {
    return new s.Point(
      this.dataToLogicalX(e.x),
      this.dataToLogicalY(e.y)
    );
  }, n.ImagingHelper.prototype.physicalToDataPoint = function(e) {
    if (this._viewer.world.getItemCount() === 1)
      return new s.Point(
        this.physicalToDataX(e.x),
        this.physicalToDataY(e.y)
      );
    var t = this._viewer.world.getItemAt(this._worldIndex);
    return t.viewerElementToImageCoordinates(e);
  }, n.ImagingHelper.prototype.dataToPhysicalPoint = function(e) {
    return new s.Point(
      this.dataToPhysicalX(e.x),
      this.dataToPhysicalY(e.y)
    );
  }, n.ImagingHelper.prototype.logicalToDataX = function(e) {
    return this._haveImage ? e * this.imgWidth : 0;
  }, n.ImagingHelper.prototype.logicalToDataY = function(e) {
    return this._haveImage ? e * this.imgHeight : 0;
  }, n.ImagingHelper.prototype.dataToLogicalX = function(e) {
    return this._haveImage && this.imgWidth > 0 ? e / this.imgWidth : 0;
  }, n.ImagingHelper.prototype.dataToLogicalY = function(e) {
    return this._haveImage && this.imgHeight > 0 ? e / this.imgHeight : 0;
  }, n.ImagingHelper.prototype.physicalToDataX = function(e) {
    if (this._viewer.world.getItemCount() === 1)
      return this._haveImage && this.getViewerContainerSize().x > 0 ? (this._viewportOrigin.x + e / this.getViewerContainerSize().x * this._viewportWidth) * this.imgWidth : 0;
    var t = this._viewer.world.getItemAt(this._worldIndex), i = t.viewerElementToImageCoordinates(
      new s.Point(e, 0)
    );
    return i.x;
  }, n.ImagingHelper.prototype.physicalToDataY = function(e) {
    if (this._viewer.world.getItemCount() === 1)
      return this._haveImage && this.getViewerContainerSize().y > 0 ? (this._viewportOrigin.y + e / this.getViewerContainerSize().y * this._viewportHeight) * this.imgHeight : 0;
    var t = this._viewer.world.getItemAt(this._worldIndex), i = t.viewerElementToImageCoordinates(
      new s.Point(0, e)
    );
    return i.y;
  }, n.ImagingHelper.prototype.dataToPhysicalX = function(e) {
    return this._haveImage && this.imgWidth > 0 ? (e / this.imgWidth - this._viewportOrigin.x) / this._viewportWidth * this.getViewerContainerSize().x : 0;
  }, n.ImagingHelper.prototype.dataToPhysicalY = function(e) {
    return this._haveImage && this.imgHeight > 0 ? (e / this.imgHeight - this._viewportOrigin.y) / this._viewportHeight * this.getViewerContainerSize().y : 0;
  }, n.ImagingHelper.prototype.onOpen = function() {
    var e = this._viewer.world.getItemAt(this._worldIndex);
    this._haveImage = !0, this.imgWidth = e.source.dimensions.x, this.imgHeight = e.source.dimensions.y, this.imgAspectRatio = this.imgWidth / this.imgHeight, this._trackZoomPan();
  }, n.ImagingHelper.prototype.onClose = function() {
    this._haveImage = !1, this.imgWidth = 0, this.imgHeight = 0, this.imgAspectRatio = 0;
  }, n.ImagingHelper.prototype.onAnimation = function() {
    this._trackZoomPan();
  }, n.ImagingHelper.prototype.onAnimationFinish = function() {
    this._trackZoomPan();
  }, n.ImagingHelper.prototype.onResize = function() {
    this._viewer.autoResize && this._trackZoomPan();
  }, n.ImagingHelper.prototype.onFullPage = function() {
    this._trackZoomPan();
  }, n.ImagingHelper.prototype.onFullScreen = function() {
    this._trackZoomPan();
  }, n.ImagingHelper;
})(
  mn || window.OpenSeadragon,
  window.OpenSeadragonImaging = window.OpenSeadragonImaging || {}
);
var Ag = Object.defineProperty, kg = Object.getOwnPropertyDescriptor, Ig = Object.getPrototypeOf, Dg = Reflect.get, ft = (s, n, e, t) => {
  for (var i = t > 1 ? void 0 : t ? kg(n, e) : n, r = s.length - 1, o; r >= 0; r--)
    (o = s[r]) && (i = (t ? o(n, e, i) : o(i)) || i);
  return t && i && Ag(n, e, i), i;
}, Og = (s, n, e) => Dg(Ig(s), e, n);
let Qe = class extends Pr {
  constructor() {
    super(...arguments), this.rotationValues = /* @__PURE__ */ new Map(), this.firstTile = !1, this.viewer = void 0, this.openseadragonImagePath = "/static/assets/images/lightbox/", this.eof = !1, this.bof = !1, this.hideUI = !1, this.navDeferred = !1, this.darkMode = !0, this.open = !1, this.currentResolution = "", this.resolutions = [], this.dataVisible = !1, this.hasData = !1, this.resOpened = !1;
  }
  // private disableRotationCacheOnce = false
  setURLProvider(s) {
    this.urlProvider = s;
  }
  initOpenSeaDragon() {
    this.viewer && this.viewer.destroy(), this.navDeferred = !1;
    const s = this.shadowRoot?.getElementById("open-sea-dragon");
    s && (this.firstTile = !1, Object(mn).hasOwnProperty("svgOverlay") || Rg(mn), this.viewer = mn({
      element: s,
      id: "open-sea-dragon",
      prefixUrl: this.openseadragonImagePath,
      showFullPageControl: !1,
      showRotationControl: !0,
      // loadTilesWithAjax: true,
      // ajaxHeaders: headerObject,
      // ajaxWithCredentials: true,
      crossOriginPolicy: "Anonymous"
    }), this.viewer.activateImagingHelper && this.viewer.activateImagingHelper({}), this.viewer.addHandler("open", () => {
    }), this.viewer.addHandler("open-failed", (n) => {
      const e = this.viewer?.svgOverlay ? this.viewer.svgOverlay() : void 0;
      (!e || !e.isVisible()) && this.opened(!1, n.message);
    }), this.viewer.addHandler("tile-load-failed", (n) => {
      if (this.firstTile) {
        const e = this.viewer?.svgOverlay ? this.viewer.svgOverlay() : void 0;
        (!e || !e.isVisible()) && this.opened(!1, n.message);
      }
      this.firstTile = !1;
    }), this.viewer.addHandler("tile-loaded", () => {
      this.firstTile && this.opened(!0, ""), this.firstTile = !1;
    }), this.viewer.addHandler("rotate", (n) => {
      this.urlProvider?.url && (n.degrees % 360 === 0 ? this.rotationValues.delete(this.urlProvider.url) : this.rotationValues.set(this.urlProvider.url, n.degrees % 360));
    }));
  }
  nextFile(s) {
    let n;
    if (this.viewerError = void 0, !this.viewer) {
      this.viewerError = "The viewer instance was not successfully initialized, so I can't show anything.";
      return;
    }
    if (!this.urlProvider) {
      this.viewerError = "The urlProvider is missing. That's a programming error.";
      return;
    }
    try {
      s == "next" ? n = this.urlProvider.next() : n = this.urlProvider.prev();
    } catch (e) {
      this.viewerError = `An error occurred, so I don't know what file to fetch: ${e}`;
      return;
    }
    n ? this._openFile() : this.viewerError = "there is nowhere to go";
  }
  reloadFile() {
    this._openFile();
  }
  showHideUI(s = null) {
    this.hideUI = s === null ? !this.hideUI : s;
  }
  getDefaultSingleFileOptions(s, n) {
    return {
      type: "image",
      url: n,
      loadTilesWithAjax: !0,
      ajaxHeaders: s,
      ajaxWithCredentials: !0
    };
  }
  getSVGFileOptions(s, n) {
    return {
      tileSource: {
        height: window.innerHeight * 2,
        width: window.innerWidth * 2,
        tileSize: window.innerWidth * 2,
        minLevel: 1,
        getTileUrl: () => {
        }
      },
      width: Math.min(s * 3, window.innerWidth * 2),
      height: Math.min(n * 3, window.innerHeight * 2)
    };
  }
  _openFile() {
    this.tryOpen(() => {
      if (this.urlProvider?.url && this.viewer) {
        this.eof = this.urlProvider.eof(), this.bof = this.urlProvider.bof();
        try {
          const s = this.rotationValues.get(this.urlProvider.url) ?? 0, n = this.viewer.svgOverlay ? this.viewer.svgOverlay() : void 0;
          n && (n.clear(), n.hide()), this.viewer.viewport.getRotation() != s && this.viewer.viewport.setRotation(s), this.firstTile = !0;
          const e = this.apiContext.getHeaders("application/json");
          let t = {};
          e.forEach((r, o) => {
            t[o] = r;
          });
          let i;
          if (this.urlProvider?.fileType && this.urlProvider.fileType.toLowerCase() === "svg") {
            if (n) {
              const r = this;
              this._loadSVG(t, this.urlProvider.url).then((o) => {
                try {
                  const l = new DOMParser().parseFromString(o, "image/svg+xml").documentElement;
                  if (l instanceof SVGElement) {
                    const a = n.loadSVG(l);
                    i = this.getSVGFileOptions(a.x, a.y), r.viewer && r.viewer.open(i), n.show();
                  } else
                    throw Error("no SVG");
                } catch (l) {
                  n.hide(), this.opened(!1, `Could not open SVG: ${l}`);
                }
              }).catch((o) => {
                n.hide(), this.opened(!1, `Could not open SVG: ${o}`);
              });
            }
          } else
            i = this.getDefaultSingleFileOptions(t, this.urlProvider.url), this.viewer.open(i);
        } catch (s) {
          this.viewerError = `An error occurred: ${s}`;
        }
      } else
        this.viewerError = "An error occurred: no url provider or no viewer in _openFile.";
    });
  }
  async _loadSVG(s, n) {
    const e = await fetch(n, { headers: s, method: "GET" });
    if (Number(e.headers.get("content-length")) > 2e6) throw Error("For safety the viewer is not showing SVGs larger than 2MB.");
    return await e.text();
  }
  disconnectedCallback() {
    this.viewer && this.viewer.destroy();
  }
  updated(s) {
    s.has("open") && (this.open ? (this.initOpenSeaDragon(), this.nextFile("next")) : this.viewer && (this.viewer.destroy(), setTimeout(() => {
      const n = new CustomEvent("closed", {
        bubbles: !0,
        cancelable: !0,
        detail: this
      });
      this.dispatchEvent(n);
    }, 10))), s.has("dataVisible") && (this.dataVisible || this.scroll(0, 0));
  }
  willUpdate(s) {
  }
  apiConnected() {
  }
  openDialog() {
    this.open = !0;
  }
  tryOpen(s) {
    this.emitBeforeEvent(
      "beforeOpen",
      {},
      () => {
        this.navDeferred = !1;
      },
      () => {
        this.navDeferred = !1, s();
      }
    ) || (this.navDeferred = !0);
  }
  opened(s, n) {
    setTimeout(() => {
      const e = new CustomEvent("opened", {
        bubbles: !0,
        cancelable: !1,
        detail: {
          component: this,
          result: s,
          errMsg: n
        }
      });
      this.dispatchEvent(e);
    }, 10);
  }
  splitterClicked() {
    this.dataVisible = !this.dataVisible;
  }
  renderError() {
    return this.viewerError ? Be`
            <div class="kiosk-lightbox-error">
                <p>${this.viewerError}</p>
            </div>` : Te;
  }
  doClose() {
    this.open = !1, this.navDeferred = !1;
  }
  doNext() {
    try {
      this.nextFile("next");
    } catch {
    }
    this.navDeferred = !1;
  }
  doPrev() {
    try {
      this.nextFile("prev");
    } catch {
    }
    this.navDeferred = !1;
  }
  tryClose() {
    this.emitBeforeEvent(
      "beforeClose",
      {},
      () => {
        this.navDeferred = !1;
      },
      () => {
        this.doClose();
      }
    ) || (this.navDeferred = !0);
  }
  tryNext() {
    this.urlProvider?.eof() || this.emitBeforeEvent(
      "beforeNext",
      {},
      () => {
        this.navDeferred = !1;
      },
      () => {
        this.doNext();
      }
    ) || (this.navDeferred = !0);
  }
  tryPrev() {
    this.urlProvider?.bof() || this.emitBeforeEvent(
      "beforePrev",
      {},
      () => {
        this.navDeferred = !1;
      },
      () => {
        this.doPrev();
      }
    ) || (this.navDeferred = !0);
  }
  close(s = !1) {
    this.navDeferred = !1, s ? this.tryClose() : this.doClose();
  }
  toggleBackground() {
    this.darkMode = !this.darkMode;
  }
  toggleResolution() {
    this.resOpened = !this.resOpened;
  }
  switchResolution(s) {
    const n = s.currentTarget?.dataset.resolution;
    if (n && n != this.currentResolution) {
      this.currentResolution = n;
      const e = new CustomEvent("ResolutionChanged", {
        bubbles: !0,
        composed: !0,
        cancelable: !1
      });
      this.dispatchEvent(e);
    }
    s.stopPropagation(), this.resOpened = !1;
  }
  renderResolutionButton() {
    return this.resolutions.length > 1 ? Be`
                <div class="kiosk-lightbox-button resolution-btn" @click="${this.toggleResolution}">
                    <span>RES</span>
                    ${this.resOpened ? Be`
                        <div class="resolutions">
                            ${this.resolutions.map((s) => Be`
                                <div class="res-item" data-resolution=${s} @click="${this.switchResolution}">
                                    <div class="res-item-checker">
                                        ${s.toLowerCase() === this.currentResolution.toLowerCase() ? Be`<i
                                            class="fas fa-check"> </i>` : Te}
                                    </div>
                                    <div>${s}</div>
                                </div>
                            `)}

                        </div>` : Te}
                </div>
            ` : Te;
  }
  renderNavButtons() {
    return Be`
            <div class="kiosk-lightbox-buttons ${this.hideUI ? "hide-ui" : ""}">
                ${this.renderResolutionButton()}
                <div class="kiosk-lightbox-button" @click="${this.toggleBackground}"><i
                    class="fa-circle-half-stroke"></i></div>
                <div class="kiosk-lightbox-button ${this.bof ? "nav-button-deactivated" : ""}" @click="${this.tryPrev}">
                    <i class="fa-prev"></i></div>
                <div class="kiosk-lightbox-button ${this.eof ? "nav-button-deactivated" : ""}" @click="${this.tryNext}">
                    <i class="fa-next"></i></div>
                <div class="kiosk-lightbox-button" @click="${this.tryClose}"><i class="fa-close"></i></div>
            </div>
        `;
  }
  renderOpenSeaDragon() {
    const s = { expanded: this.dataVisible, collapsed: !this.dataVisible, nodata: !this.hasData };
    return Be`
            <div class="kiosk-lightbox-outer ${this.darkMode ? "background-dark" : "background-light"}">
                ${this.navDeferred ? Te : this.renderNavButtons()}
                ${this.renderError()}
                <div class="kiosk-lightbox-inner ${this.hideUI ? "hide-ui" : Te}"
                     style="${this.viewerError ? "visibility: hidden" : Te}">
                    <div id="open-sea-dragon"
                         class="kiosk-lightbox-viewer ${Tg(s)}">
                    </div>
                </div>
                ${this.hasData ? Be`
                    <div class="kiosk-lightbox-data" style="${this.viewerError ? "visibility: hidden" : Te}">
                        <div class="kiosk-lightbox-splitter" @click="${this.splitterClicked}">
                            <i class="fa-view-list"></i>
                        </div>
                        <slot></slot>
                    </div>` : Te}
            </div>`;
  }
  apiRender() {
    return this.open ? Be`${this.renderOpenSeaDragon()}` : Be``;
  }
};
Qe.styles = wr(Cg);
Qe.properties = {
  ...Og(Qe, Qe, "properties")
};
ft([
  At()
], Qe.prototype, "viewerError", 2);
ft([
  At()
], Qe.prototype, "eof", 2);
ft([
  At()
], Qe.prototype, "bof", 2);
ft([
  At()
], Qe.prototype, "hideUI", 2);
ft([
  At()
], Qe.prototype, "navDeferred", 2);
ft([
  At()
], Qe.prototype, "darkMode", 2);
ft([
  Ge({ type: Boolean, reflect: !0 })
], Qe.prototype, "open", 2);
ft([
  Ge({ type: String, reflect: !0 })
], Qe.prototype, "currentResolution", 2);
ft([
  Ge()
], Qe.prototype, "resolutions", 2);
ft([
  Ge()
], Qe.prototype, "dataVisible", 2);
ft([
  Ge({ type: Boolean })
], Qe.prototype, "hasData", 2);
ft([
  At()
], Qe.prototype, "resOpened", 2);
Qe = ft([
  Bn("kiosk-lightbox")
], Qe);
export {
  Mp as ComboBox,
  Of as Grid,
  it as KioskContextSelector,
  pi as KioskDialog,
  Qe as KioskLightbox,
  vt as KioskTZComboBox
};
